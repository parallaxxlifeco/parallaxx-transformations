/* PARALLAXX TRANSFORMATIONS - About page Wix Custom Element. Tag: parallaxx-about-page.
   GENERATED by build-about-bundle.py from "Parallaxx About v4.dc.html".
   DO NOT EDIT THIS FILE - edit the .dc.html and rerun the build.
   In the Wix editor: turn the site Header + Footer OFF for this page, and
   place PtNav (active="about") and PtFooter as their own custom elements. */
(function(){
  if (customElements.get('parallaxx-about-page')) return;

  var CSS = `/* ══════════════════════════════════════════════════════════════════════════
   ABOUT DANIEL LAWSON  ·  v7  ·  "THE HUT"
   Built on the Parallaxx v4 system.

   ─────────────────────────────────────────────────────────────────────────
   THIS VERSION IS BUILT TO A DOCUMENTED STANDARD, NOT TO TASTE.

   v4, v5 and v6 were all argued from assertion. The structure below is taken
   from "ABOUT PAGE — Best Practice Brief.md", which sits next to this file.
   Every block maps to a finding. To change the structure, argue with the
   brief first -- that is what it is for.

   THE TWO SOURCES THAT DECIDED IT

   Nielsen Norman Group. Three rounds, 70+ users observed one-on-one, 100
   sites usability-tested, 65 expert-reviewed, 85 guidelines. Their ranked
   list of what frustrates people on an About page:
       3rd  shallow content lacking substance
       4th  stock photography
       5th  walls of text with poor white space
       9th  aggressive sales tactics
   Note the ORDER. Being thin fails HIGHER than being long. That is why this
   page did not get cut back to a bio, and why it is image-led instead.

   Orbit Media, from 500+ Analytics accounts: the About page is a top 5-10
   most-visited page on nearly every site, and 11% of its visitors go to the
   Contact page next. It is a measurable conversion path -- so it gets exactly
   ONE call to action, and contact details are not buried.

   ─────────────────────────────────────────────────────────────────────────
   BLOCK MAP, each against the element it satisfies

     01 WHO       navy    · Characters + summary + evidence high on the page
     02 RESOLVE   cream   · Orbit "Resolution" + proof beside the claim
     03 BAND      photo   · the page stops reading
     04 CONFLICT  cream   · Orbit "Conflict". THREE beats, not five
     05 BELIEVE   navy    · Orbit "Plot"
     06 ROOMS     navy 2  · evidence types: media, volume, awards, certs
     07 VISION    cream   · Orbit "Vision" + "Setting" + the single CTA

   RESOLVE MOVED FROM 5 TO 2 ON 5 AUG, and this is the most consequential
   ordering decision on the page. NN/g's eye-tracking study -- 130,000
   fixations, 120 participants -- puts 74% of viewing time inside the first
   TWO screenfuls, 42% inside the top fifth of a page and 65% inside the top
   two fifths. Measured against that, the old order spent both high-attention
   screens on Daniel's past and put the only block that answers "what does he
   do and who for", with both client quotes in it, at roughly 65% down -- in
   the 26% band.

   That is not a length problem, it is a load-order one. About is the most
   self-selected page on the site: nobody arrives by accident, they clicked
   "how I got here". You cannot lose a reader who chose to be here by making
   them scroll. You can lose one by spending their attention in the wrong
   order. So the page now states what it does, shows who it is for and lets
   two clients confirm it, and only then explains where any of it came from.
   The three beats stop being the claim and become the evidence for one
   already made.

   ALTERNATION, since two navies now touch. #04122A and #0A1D3C are both
   sanctioned section grounds in this system -- Home and The Reconnected Man
   run them against each other -- so ROOMS takes the lighter one and the seam
   reads as a change of key. The arc takes #F7F3EA so the two cream sections
   either side of the band are not the identical value.

   WHAT CHANGED FROM v6, AND WHY

   · THE CHRONOLOGY WENT. Orbit's model asks for CONFLICT, not history. The
     headmaster and the engines were chronology and they are gone. The
     machine, the deleted want and the turn are conflict, and they stayed.
     Five beats became three.

   · PROOF CAME BACK, IN THE RIGHT SHAPE. v5 stacked six testimonials into a
     wall. v6 deleted all of them. Both were wrong. Orbit's evidence research
     finds leading pages give a third to a half of vertical space to evidence
     and place it BESIDE THE CLAIM IT VERIFIES; NN/g list external
     endorsements among the things users actively want and missing trust
     indicators among the things that frustrate them. So there are exactly
     two client quotes on this page, one man and one woman, and each sits
     INSIDE the card whose claim it verifies. There is no testimonials block.

   · RESOLUTION EXISTS NOW. "What problem does he actually solve, and for
     whom" was missing from all three previous versions despite being a named
     element in the model and despite being top of NN/g's list of what users
     came to find. It is section 05 and it is the most important new writing
     on the page.

   · VISION EXISTS NOW. Also missing from all three. Section 07.

   · SETTING EXISTS NOW. Where he is and how to reach him, as a scannable
     definition list rather than prose.

   ─────────────────────────────────────────────────────────────────────────
   THE SPINE, AND WHERE IT CAME FROM

   The one thing that stops this being the home page rewritten: the record and
   the confession are now THE SAME OBJECT.

   From the Personal Awareness Archive, "01 - The Hut", in Daniel's own words:
       "I built rooms so I would never have to ask to be let in."
       "GIVE IT ALL, the ecosystem, the studio -- all wood piles."
       "You can't sell a man the way out of a room you're still standing in."

   Which means section 06 is not a brag list. Every room on it is at once the
   proof of his authority and the evidence of his pattern, and the page says
   so out loud. No other page on this site can do that, and it is why this
   version stopped duplicating Home.

   ▸ CLEARED 2 Aug 2026. All three lines are approved for publication.
     They came out of private self-work and had never been published, so they
     were held for a decision; that decision is made and this is now settled
     copy. Do not quietly soften them in a later pass -- the H1, the deletion
     paragraph in beat two and the band line are the load-bearing sentences
     on the page and everything else is arranged around them.

   ─────────────────────────────────────────────────────────────────────────
   PHOTOGRAPHY  ·  NN/g frustration #4 is stock photography

   Ten slots. Real Wix assets are wired in where they exist. Everything else
   renders a numbered placeholder carrying its shot brief, so the gap is loud
   in the build and the brief travels with the file. Search SHOT 01-10.

   The placeholder is drawn quietly -- hairline dash, small type, numbered
   badge -- so the LAYOUT can be judged before the photographs land.

   ─────────────────────────────────────────────────────────────────────────
   THE WOMEN'S AVATAR  ·  audited against the W-series archive, 1 Aug 2026

   Every guardrail below is a real voice from Women/W01-W03, not a
   preference. This page will be read by her before her own page exists, so
   these apply to the ABOUT page whether or not the room has launched.

   · NEVER BLAME HER WORK, AMBITION OR SUCCESS. All load bearing, all of it
     works. Attack any of it and she is gone in one line. Section 05's
     calibration paragraph protects it explicitly.

   · NAME THE SPECIFIC TRADE-OFF, NOT THE CONSUMED LIFE. Daniel tested
     "career is consuming your life" on Jennifer, a consultant on partner
     track. Her answer: TOO BIG. From inside it is not a consumed life, it is
     a series of small reasonable cancellations she chose herself.

   · HER NOUN IS "TRADE-OFF". She reached for it unprompted. Not burnout,
     not overwhelm, not lost. A trade-off is a decision, which means she
     still holds herself responsible, which means she can be addressed as an
     adult. It is now in her card in section 05.

   · THE COSTUME IS AVAILABILITY, NOT COMPETENCE. Christine, client: "First
     of all not able to say no. And people mistreat me often because I don't
     correct it." And "I'm the therapist now... everybody was just dumping."
     She is not only the most capable person in the room, she is the one who
     cannot decline -- and the reason she cannot decline sounds like
     professionalism. Both bullets in her card are sourced to that.

   · THE DELETION IS THE BRIDGE, AND IT IS WHY THE HERO LINE WORKS FOR BOTH.
     Christine: "I don't have expectations of people. I never had."
     Véda Bacchus: "I dared not ASK for what I needed."
     Two unrelated women, one a paying client, describing the same terminal
     move as the man archive. The hero used to say people "stopped being in
     the room", which is HIS pattern -- he goes absent at home. SHE never
     left the room; she runs it. So the hero now says they stopped ASKING,
     which is the one claim with sources on both sides.

   · DO NOT PROMISE TRANSFORMATION. Christine's wins, in her own terms: a
     clear no, twice, that "got me nervous, but that was basically it"; a
     message she did not send; two or three moments of feeling alive again.
     That is the ceiling. Section 05's calibration paragraph is built from
     it, and the old line "changes what a house feels like within weeks" was
     cut for being a timed transformation claim in a man's unit of measure.

   · THE DOORWAY IS THE TROUGH, NOT THE PEAK. Jennifer: "I hope I don't get a
     burnout, but a bore-out." Nobody in the file arrived at peak overwhelm;
     at the peak she is too busy to reflect. Nothing here is written for a
     woman drowning at 11pm and it should stay that way.

   · REST IS NOT THE OFFER. Julia: "when you work less and you have less
     exposure, you're not allowed to progress." Less exposure is the thing
     she is AFRAID of. Any line built on "you deserve a break" is aimed at
     the wrong object.

   · NO FEMININE-ENERGY VOCABULARY, NO "CONTAINER", NO "CIRCLE". The word
     circle arrives already meaning candles and a talking object and has
     decided in advance what she thinks the room is. This page says "groups".

   · HER REGISTER IS PROFESSIONAL, PLAIN AND SLIGHTLY DRY.

   WHY SHE WOULD TAKE ANY OF THIS FROM HIM, per W01: not that he understands
   women. That he has run this exact machine and got out of it. Julia
   described the reward loop as a toxic relationship -- told at year end it
   was not enough, so next year give 120%. Beat one of section 02 is Daniel
   living that and resigning. On the men's relationship pages he cannot claim
   to be ahead of the avatar. Here he can, because he actually left. That
   beat is the most important thing on this page FOR HER, and it is first.

   STILL UNSOURCED. Must not be written as though it were:
     · No captured quote from any woman on why she would choose a man-led
       room. Practice knowledge, not evidence. Nothing here claims it.
     · Both interviews are consultants, and German-speaking. Nothing yet from
       a high-achieving woman outside consulting.
     · Nothing in three interviews touched gender at all.
     · Gena, quoted in section 05, is a real Parallaxx client and real proof,
       but she is not a sourced member of this avatar. She is evidence that a
       woman got value here, not evidence about the consultant.

   ─────────────────────────────────────────────────────────────────────────
   THE ACCENT RULE
     GOLD  #E8C65F  Daniel's voice. Handwriting, pull-quotes, hairlines,
                    section labels, GIVE IT ALL. Bronze #A08A5E on cream.
     CORAL #FF501F  The reader's move. ONE coral button on this page, in
                    section 07. NN/g rank aggressive sales tactics 9th.
   ══════════════════════════════════════════════════════════════════════════ */

  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
  html{scroll-behavior:smooth}
  #pxa-root img{display:block;max-width:100%}
  #pxa-root h1,#pxa-root h2,#pxa-root h3,#pxa-root h4{text-wrap:balance}
  #pxa-root p{text-wrap:pretty}
  #pxa-root ::selection{background:#FF501F;color:#fff}

/* ── TYPE ROLES ─────────────────────────────────────────────── */
  .px-serif{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em}
  .px-hand{font-family:'Lumios Marker','Permanent Marker',cursive;font-weight:400}
  .px-label{font-size:.73rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;color:#5E6B85}
  /* CONTRAST FIX. #A08A5E is the house bronze and it is correct for large
     display type -- pull-quotes, handwriting, rules. At .66rem label size on
     cream it measures 2.83:1, which fails WCAG AA (4.5) badly. #82663D is
     the same hue darkened to 4.55:1. Bronze stays bronze where it is big;
     it darkens where it is small. */
  .px-label.bronze{color:#82663D}

/* ── LAYOUT ─────────────────────────────────────────────────── */
  .px-wrap{max-width:1240px;margin:0 auto}
  .px-head{max-width:min(1140px,94vw);margin-left:auto;margin-right:auto}
  .px-sec{padding:clamp(52px,6.5vw,92px) clamp(20px,4vw,52px);position:relative}

/* ── BUTTONS ──────────────────────────────────────────────────
   One coral button on the page. Everything else that links somewhere is a
   ghost or a gold card. NN/g rank aggressive sales tactics 9th on the
   frustration list; Orbit's finding is ONE focused CTA, not several. */
  .px-btn{display:inline-flex;align-items:center;gap:.55em;background:#FF501F;color:#fff;font-family:'Montserrat',system-ui,sans-serif;font-weight:700;font-size:1.18rem;padding:.92em 1.9em;border:none;border-radius:999px;text-decoration:none;cursor:pointer;transition:background .3s,transform .3s,box-shadow .3s;box-shadow:0 10px 30px -12px rgba(255,80,31,.5)}
  .px-btn:hover{background:#FF6A3D;transform:translateY(-2px)}

/* ── REVEAL PRIMITIVES ──────────────────────────────────────────
   FAIL-SAFE BY DEFAULT. The hidden start state is gated behind #pxa-root.anim,
   which the JS adds as its FIRST act. If the script never runs -- raw file off
   disk, DC runtime absent, CSP blocking the CDN, a throw upstream -- the class
   is never added and the page renders fully visible instead of a navy screen
   with nothing on it. Never put the hidden state in the ungated rule. */
  .px-line{display:block;overflow:hidden;padding-bottom:.2em;margin-bottom:-.2em}
  .px-line > span{display:block;padding-right:.08em}
  #pxa-root.anim .px-line > span{transform:translateY(135%);will-change:transform}
  #pxa-root.anim .px-fade{opacity:0;transform:translateY(22px);will-change:transform,opacity}
  @media(prefers-reduced-motion:reduce){
    #pxa-root.anim .px-line > span{transform:none!important}
    #pxa-root.anim .px-fade{opacity:1!important;transform:none!important}
    *{animation:none!important}
  }

/* ── THE PLACEHOLDER ──────────────────────────────────────────
   Ten appear at once, so it is drawn QUIETLY -- hairline dash, small type, a
   numbered badge. Loud enough that nobody ships it by accident, calm enough
   that the layout can be judged before the photographs exist. */
  .px-ph{position:relative;display:flex;flex-direction:column;align-items:flex-start;justify-content:flex-end;gap:8px;
    padding:46px 22px 22px;width:100%;height:100%;min-height:200px;overflow:hidden;
    background:linear-gradient(160deg,rgba(255,80,31,.07),rgba(255,80,31,.02));
    border:1px dashed rgba(255,80,31,.55);border-radius:16px}
  .px-ph .no{position:absolute;top:14px;left:14px;font-size:.6rem;font-weight:800;letter-spacing:.14em;
    text-transform:uppercase;color:#FF501F;border:1px solid rgba(255,80,31,.45);border-radius:999px;padding:.34em .8em}
  .px-ph strong{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;font-size:1rem;line-height:1.32;color:#F1ECE1;letter-spacing:-.01em}
  .px-ph span{font-size:.8rem;line-height:1.62;color:#9FB0C9}
  .px-ph em{font-style:normal;font-size:.72rem;line-height:1.55;color:#FF8A63}
  .px-ph.on-cream{background:linear-gradient(160deg,rgba(255,80,31,.06),rgba(160,138,94,.05));border-color:rgba(160,138,94,.55)}
  .px-ph.on-cream .no{color:#A08A5E;border-color:rgba(160,138,94,.5)}
  .px-ph.on-cream strong{color:#1E2A3D}
  .px-ph.on-cream span{color:#6E655B}
  .px-ph.on-cream em{color:#A0552F}

/* ── 01 HERO ──────────────────────────────────────────────────
   The face bleeds off the right edge instead of sitting in a card. NN/g on
   summaries: start telling the story the moment people land, in scannable
   form, with a real photograph of the actual person. */
  /* 92svh, NOT 100. NN/g's eye-tracking work puts 74% of viewing time in the
     first two screenfuls and warns against "false floors" -- a screen that
     looks finished. A full-height hero closed by a hairline and a numbers
     strip is exactly that. At 92svh the next section's label clears the fold
     and the page tells the reader there is more, which is the same rule
     Home's brief states as "never let screen one look finished". */
  #pxa-hero{position:relative;min-height:92svh;display:flex;flex-direction:column;justify-content:center;
    padding:126px clamp(20px,4vw,52px) 36px;overflow:hidden;
    background:radial-gradient(120% 95% at 18% 34%,#0A2148 0%,#061938 48%,#030C1E 100%)}
  #pxa-hero .shot{position:absolute;top:0;right:0;width:46%;height:100%;overflow:hidden}
  #pxa-hero .shot img{width:100%;height:100%;object-fit:cover;object-position:52% 22%;filter:grayscale(.12) contrast(1.05)}
  /* The blend edge. Without it the photo ends on a hard vertical line and the
     hero reads as two rectangles glued together. */
  #pxa-hero .shot::after{content:'';position:absolute;inset:0;
    background:linear-gradient(90deg,#061938 0%,rgba(6,25,56,.72) 26%,rgba(6,25,56,.05) 68%,rgba(3,12,28,.45) 100%)}
  #pxa-hero .inner{position:relative;z-index:2;max-width:1240px;margin:0 auto;width:100%;flex:1;display:flex;flex-direction:column;justify-content:center}
  @media(max-width:900px){
    #pxa-hero .shot{width:100%;opacity:.4}
    #pxa-hero .shot::after{background:linear-gradient(180deg,rgba(4,18,42,.72),rgba(3,12,28,.95))}
  }

/* ── THE RIBBON ───────────────────────────────────────────────
   Inside the hero, not under it. Orbit's phrase for where evidence belongs is
   "put billboards on highways" -- the top of the most-visited page is the
   highway. */
  #pxa-ribbon{position:relative;z-index:2;max-width:1240px;margin:0 auto;width:100%;
    border-top:1px solid rgba(232,198,95,.16);padding-top:24px}
  #pxa-nums{display:flex;flex-wrap:wrap;gap:clamp(20px,4vw,52px);align-items:baseline}
  @media(max-width:700px){#pxa-nums{display:grid;grid-template-columns:1fr 1fr;gap:16px 14px;align-items:start}#pxa-nums>div:last-child{grid-column:1/-1}}
  #pxa-nums b{display:block;font-family:'Poppins','Montserrat',sans-serif;font-weight:400;font-size:clamp(24px,2.8vw,38px);line-height:1;color:#F1ECE1;letter-spacing:-.02em}
  #pxa-nums i{display:block;font-style:normal;font-size:.7rem;letter-spacing:.18em;text-transform:uppercase;font-weight:700;color:#5E6B85;margin-top:7px}

/* ── 04 CONFLICT — alternating editorial rows ─────────────────
   Three beats. NN/g frustration #5 is walls of text; alternating rows put a
   picture between every idea, which is also what keeps the page scannable
   without going thin (frustration #3). */
  .px-row{display:grid;grid-template-columns:.94fr 1.06fr;gap:clamp(26px,4.2vw,68px);align-items:center;
    margin-bottom:clamp(38px,4.6vw,68px)}
  /* The media box sits on cream, so its EMPTY state has to be light too -- a
     navy box behind a cream-variant placeholder renders the brief's headline
     in navy-on-navy and silently loses it. */
  .px-row .media{position:relative;aspect-ratio:4/3;border-radius:16px;overflow:hidden;background:#E7E0D2}
  .px-row .media img{width:100%;height:100%;object-fit:cover}
  .px-row.flip .media{order:2}
  .px-row .no{font-size:.73rem;letter-spacing:.22em;text-transform:uppercase;font-weight:800;color:#82663D;margin-bottom:10px}
  .px-row h3{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em;
    font-size:clamp(21px,2.5vw,32px);line-height:1.2;color:#1E2A3D;margin-bottom:12px}
  .px-row p{font-size:1rem;line-height:1.76;color:#5E5850;max-width:48ch}
  .px-row p strong{color:#1E2A3D;font-weight:600}
  .px-row blockquote{margin:14px 0 0;padding-left:20px;border-left:2px solid #A08A5E;
    font-family:'Poppins','Montserrat',sans-serif;font-style:italic;font-size:clamp(16px,1.7vw,21px);
    line-height:1.45;color:#A08A5E;max-width:42ch}
  .px-row.turn .no{color:#CF2000}
  .px-row.turn .media{aspect-ratio:1/1}

/* ── VIDEO FACADE ─────────────────────────────────────────────
   CURRENTLY UNUSED. Kept because the slot in section 02 is live the moment a
   45-60 second cut exists -- see the note there. Deleting this means writing
   it again later.

   Zero third-party requests until click. The play control is bottom-LEFT on
   purpose -- a centred button lands squarely on the speaker's mouth. Always
   state the runtime. Coral, because pressing play is a reader action. */
  .px-vid{position:relative;display:block;width:100%;padding:0;border:0;cursor:pointer;background:#04122A;border-radius:16px;overflow:hidden;aspect-ratio:16/9;
    box-shadow:0 26px 60px -40px rgba(30,42,61,.55),0 0 0 1px rgba(160,138,94,.22);transition:transform .3s ease,box-shadow .3s ease}
  .px-vid:hover{transform:translateY(-3px);box-shadow:0 30px 70px -38px rgba(30,42,61,.7),0 0 0 1px rgba(160,138,94,.45)}
  .px-vid > img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:saturate(.94) brightness(.9);transition:filter .3s ease}
  .px-vid:hover > img{filter:saturate(1) brightness(1)}
  .px-vid .play{position:absolute;left:20px;bottom:20px;width:56px;height:56px;border-radius:999px;background:rgba(255,80,31,.94);box-shadow:0 10px 30px -8px rgba(255,80,31,.6);transition:transform .3s ease}
  .px-vid .play::after{content:'';position:absolute;top:50%;left:53%;transform:translate(-50%,-50%);border-style:solid;border-width:10px 0 10px 16px;border-color:transparent transparent transparent #fff}
  .px-vid:hover .play{transform:scale(1.09)}
  .px-vid .len{position:absolute;right:12px;bottom:12px;padding:5px 10px;border-radius:8px;background:rgba(4,18,42,.82);backdrop-filter:blur(6px);font-size:.68rem;font-weight:700;letter-spacing:.06em;color:#D7DEEA}
  .px-vid iframe,.px-vid video.live{position:absolute;inset:0;width:100%;height:100%;border:0;object-fit:cover}

/* ── 03 THE BAND ────────────────────────────────────────────── */
  #pxa-band{position:relative;min-height:clamp(340px,54vh,540px);display:flex;align-items:flex-end;overflow:hidden;
    background:radial-gradient(120% 100% at 50% 30%,#0A2148 0%,#061938 46%,#030C1E 100%)}
  #pxa-band .bg{position:absolute;top:-8%;left:0;width:100%;height:116%;object-fit:cover;object-position:center 24%}
  #pxa-band .veil{position:absolute;inset:0;z-index:2;background:linear-gradient(180deg,rgba(4,18,42,.58) 0%,rgba(4,18,42,.34) 42%,rgba(4,18,42,.95) 100%)}
  #pxa-band .copy{position:relative;z-index:3;max-width:1240px;margin:0 auto;width:100%;
    padding:clamp(40px,6vh,76px) clamp(20px,4vw,52px);display:grid;grid-template-columns:1fr;
    gap:clamp(26px,4vw,56px);align-items:end}
  #pxa-band2{position:relative;min-height:clamp(380px,60vh,580px);display:flex;align-items:center;overflow:hidden;background:#04122A}
  #pxa-band2 .bg{position:absolute;top:-8%;left:0;width:100%;height:116%;object-fit:cover;object-position:center 42%}
  #pxa-band2 .veil{position:absolute;inset:0;z-index:2;background:linear-gradient(180deg,rgba(4,18,42,.66) 0%,rgba(4,18,42,.52) 38%,rgba(4,18,42,.97) 100%)}
  #pxa-band2 .scrim{position:absolute;inset:0;z-index:2;background:linear-gradient(90deg,rgba(4,18,42,.88) 0%,rgba(4,18,42,.74) 38%,rgba(4,18,42,.18) 72%,rgba(4,18,42,0) 100%)}
  #pxa-band2 .copy{position:relative;z-index:3;max-width:1140px;margin:0 auto;width:100%;padding:clamp(48px,7vh,86px) clamp(20px,4vw,52px)}
  #pxa-band2 .belief{border-left:2px solid rgba(232,198,95,.55);padding-left:24px;margin-bottom:26px;max-width:66ch}
  #pxa-band2 .belief:last-of-type{margin-bottom:0}
  #pxa-band2 .belief p{font-size:clamp(17px,1.5vw,21px);line-height:1.66;color:#EDF1F8}
  @media(max-width:900px){
    #pxa-band2{min-height:clamp(340px,58vh,480px)}
    #pxa-band2 .scrim{background:linear-gradient(90deg,rgba(4,18,42,.92) 0%,rgba(4,18,42,.84) 58%,rgba(4,18,42,.6) 100%)}
  }
  @media(max-width:900px){#pxa-band{min-height:clamp(300px,56vh,440px)}#pxa-band .copy{grid-template-columns:1fr}}

/* ── 05 BELIEF FRAMES ─────────────────────────────────────────
   A belief in a white box is a claim. A belief with a photograph under it is
   a demonstration. The brief sits at the TOP of the frame and the caption
   owns the bottom -- bottom-aligning both hides one behind the other. */
  #pxa-stand{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:38px}
  .px-frame{position:relative;border-radius:18px;overflow:hidden;aspect-ratio:3/4;background:#0A1D3C;
    border:1px solid rgba(232,198,95,.14)}
  .px-frame img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;filter:saturate(.94)}
  .px-frame .cap{position:absolute;inset:auto 0 0 0;z-index:2;padding:64px 24px 24px;
    background:linear-gradient(180deg,transparent,rgba(3,12,28,.94) 62%)}
  .px-frame .cap h3{font-size:.7rem;font-weight:800;letter-spacing:.2em;text-transform:uppercase;color:#E8C65F;margin-bottom:10px}
  .px-frame .cap p{font-size:.94rem;line-height:1.68;color:#D7DEEA}
  .px-frame .px-ph{position:absolute;inset:0;border-radius:18px;min-height:0;justify-content:flex-start}

/* ── 02 RESOLUTION ────────────────────────────────────────────
   Two columns of who-this-is-for. NO CLIENT QUOTES -- see the note in the
   markup. Client proof lives on the testimonials page and is linked once, at
   the very bottom, in a sentence. */
  #pxa-who{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:34px}
  .px-who{background:#FBF8F2;border:1px solid #E2DACB;border-radius:20px;padding:28px 30px}
  .px-who .eyebrow{font-size:.64rem;letter-spacing:.2em;text-transform:uppercase;font-weight:800;color:#82663D;margin-bottom:12px}
  .px-who h3{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;font-size:clamp(19px,2.1vw,25px);
    line-height:1.24;color:#1E2A3D;margin-bottom:14px;letter-spacing:-.015em}
  .px-who ul{list-style:none;margin:0}
  .px-who li{position:relative;padding-left:19px;font-size:.95rem;line-height:1.7;color:#5E5850;margin-bottom:8px}
  .px-who li::before{content:'';position:absolute;left:0;top:.72em;width:5px;height:5px;border-radius:999px;background:#A08A5E}

/* ── 06 ROOM TILES ────────────────────────────────────────────
   Not a brag list. See the spine note at the top of this file: every room
   here is at once the authority and the pattern. */
  #pxa-tiles{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:34px}
  .px-tile{position:relative;border-radius:18px;overflow:hidden;aspect-ratio:3/4;background:#0A1D3C}
  .px-tile img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
  .px-tile .cap{position:absolute;left:0;right:0;bottom:0;z-index:2;padding:56px 22px 20px;
    background:linear-gradient(180deg,transparent,rgba(3,12,28,.9));color:#F1ECE1}
  .px-tile .cap b{display:block;font-size:.66rem;letter-spacing:.2em;text-transform:uppercase;font-weight:800;color:#E8C65F;margin-bottom:6px}
  .px-tile .cap span{font-family:'Poppins','Montserrat',sans-serif;font-size:1.02rem;line-height:1.3}
  .px-tile .px-ph{position:absolute;inset:0;border-radius:18px;min-height:0}

/* ── GIVE IT ALL CARD + THE ADJACENT WING ─────────────────────
   Written as CSS classes rather than the DC runtime's inline hover attribute,
   because the deployed bundle renders in a shadow root where that attribute
   does not exist. The build script fails if one creeps back in.
   Both are GOLD and neither lifts on hover: they are the record, not the ask.
   ▸ If either offsite page is rebuilt on v4, promote it to a lifting card. */
  .px-fam{display:flex;align-items:center;justify-content:space-between;gap:24px;flex-wrap:wrap;
    background:rgba(232,198,95,.06);border:1px solid rgba(232,198,95,.22);border-radius:16px;padding:22px 26px;
    text-decoration:none;transition:background .25s ease,border-color .25s ease}
  .px-fam:hover{background:rgba(232,198,95,.11);border-color:rgba(232,198,95,.42)}
  .px-fam .badge{display:inline-flex;align-items:center;justify-content:center;width:40px;height:40px;border-radius:10px;background:#E8C65F;color:#061938;font-weight:800;font-size:1.05rem;flex:0 0 auto}
  .px-fam .t{color:#F1ECE1;font-weight:700;font-size:.96rem;margin-bottom:3px}
  .px-fam .s{color:#7C89A3;font-size:.86rem}
  .px-fam .go{color:#E8C65F;font-weight:700;font-size:.88rem;white-space:nowrap}

  #pxa-off{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px}
  .px-off{display:flex;align-items:flex-start;gap:16px;background:rgba(232,198,95,.04);border:1px solid rgba(232,198,95,.18);border-radius:16px;padding:20px 22px;text-decoration:none;transition:background .3s ease,border-color .3s ease}
  .px-off:hover{background:rgba(232,198,95,.09);border-color:rgba(232,198,95,.36)}
  .px-off .ico{flex:0 0 auto;width:36px;height:36px;border-radius:10px;background:rgba(232,198,95,.12);border:1px solid rgba(232,198,95,.3);display:flex;align-items:center;justify-content:center;font-size:.95rem;color:#E8C65F}
  .px-off h4{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;font-size:1.04rem;color:#F1ECE1;margin-bottom:5px;letter-spacing:-.01em}
  .px-off p{font-size:.86rem;line-height:1.65;color:#8E9BB2;margin-bottom:10px}
  .px-off span.go{font-size:.77rem;font-weight:700;letter-spacing:.05em;color:#E8C65F}

/* ── THE RECORD STRIP ─────────────────────────────────────────
   Orbit's evidence types 6-13: media mentions, years, volume, certifications.
   Footnote size, because they confirm a decision rather than cause one -- but
   ON this page, which is the finding that matters. */
  #pxa-press{display:flex;align-items:center;justify-content:center;gap:clamp(12px,2.6vw,32px);flex-wrap:wrap;font-size:.86rem;color:#7C89A3;font-weight:600}
  #pxa-press i{font-style:normal;color:#2A3550}
  .px-chips{display:flex;flex-wrap:wrap;justify-content:center;gap:7px;margin-top:20px}
  .px-chips span{font-size:clamp(.63rem,.7vw,.63rem);font-weight:700;letter-spacing:.045em;text-transform:uppercase;white-space:nowrap;color:#7A87A1;border:1px solid rgba(232,198,95,.22);border-radius:999px;padding:.58em .78em}

/* ── 07 SETTING + CLOSE ───────────────────────────────────────
   NN/g list location and contact among the things users came for, and Orbit
   measured 11% of About visitors going to Contact next. So the close carries
   where he is, how to reach him, and the page's only button. */
  #pxa-close{display:grid;grid-template-columns:1.15fr .85fr;gap:clamp(30px,5vw,72px);align-items:start}
  .px-setting{background:#FBF8F2;border:1px solid #E2DACB;border-radius:20px;padding:26px 28px}
  .px-setting dt{font-size:.64rem;letter-spacing:.2em;text-transform:uppercase;font-weight:800;color:#82663D;margin-bottom:6px}
  .px-setting dd{font-size:.95rem;line-height:1.66;color:#5E5850;margin-bottom:18px}
  .px-setting dd:last-child{margin-bottom:0}
  .px-setting a{color:#8A5A47;font-weight:600;text-decoration:none;border-bottom:1px solid rgba(160,138,94,.5)}

/* ── MOBILE ─────────────────────────────────────────────────── */
  @media(max-width:900px){
    .px-row{grid-template-columns:1fr;gap:20px}
    .px-row.flip .media{order:0}
    .px-row .media,.px-row.turn .media{aspect-ratio:16/10}
    #pxa-close{grid-template-columns:1fr}
  }
  @media(max-width:820px){
    #pxa-who,#pxa-off{grid-template-columns:1fr}
  }
  @media(max-width:640px){
    #pxa-stand,#pxa-tiles{grid-template-columns:1fr}
    .px-frame,.px-tile{aspect-ratio:4/3}
  }
  @media(max-width:560px){
    #pxa-root h1.px-serif,#pxa-root h2.px-serif,#pxa-root h3.px-serif{font-size:clamp(21px,6.1vw,27px)!important;line-height:1.24!important}
    #pxa-root section,#pxa-root .px-sec{padding-top:clamp(40px,10vw,52px)!important;padding-bottom:clamp(40px,10vw,52px)!important}
    .px-hand{font-size:clamp(14px,4.6vw,21px)!important}
    #pxa-nums{gap:18px}
  }


  /* LAST SECTION SITS ON THE FOOTER. Its 92px bottom padding plus the
     footer's own 46px top padding left a gap you could park a car in, and
     it reads worse than it measures because the CTA column is much shorter
     than the setting list beside it, so the empty space is lopsided. */
  #pxa-root > section:last-of-type{padding-bottom:clamp(26px,2.6vw,40px)}

/* ══════════════ SITE NAV, BAKED IN ═══════════════════════════════════════
   Copied from PtNav v3.dc.html on 5 Aug. Same arrangement as the two home
   pages: the Wix header strip will not load a custom element, so the nav has
   to travel inside the page bundle.

   THIS IS A SECOND COPY AND IT WILL NOT TRACK THE SOURCE. Change
   PtNav v3.dc.html and this does not move. If the Wix strips ever start
   loading elements, delete this block, the markup and the JS, and place the
   element instead.

   No selector here collides with the page: every nav rule is prefixed pt-
   and every page rule is px- or pxa-. Checked, not assumed.
   ═════════════════════════════════════════════════════════════════════════ */
/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · PRIMARY NAVIGATION · v3
   -------------------------------------------------------------------
   STRUCTURE (this is the whole argument of the redesign)
     LOGO          home. no "Home" text item — the mark does that job.
     FOR MEN   ▾   → The Reconnected Man
     FOR WOMEN ▾   → The Reconnected Woman
     ABOUT
     TESTIMONIALS
     CONTACT       coral pill, far right.

   WHY THE DOORS COME FIRST
   The sorting page's entire premise is that a visitor must choose a
   side before anything else can land. The nav has to agree with that.
   "For Men" and "For Women" are therefore the first and largest thing
   after the mark, and everything that is ABOUT DANIEL (About,
   Testimonials) sits after the reader's own doors, not before them.

   THE PARENTS ARE REAL LINKS
   "For Men" goes to the men's page; the dropdown is an addition, not
   the only way through. A nav item that can only be hovered is a dead
   end on touch and a dead end for keyboards.

   ───────────────────────────────────────────────────────────────────
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   Gold marks where you ARE (the active item, the hovered child).
   Coral marks the one thing you can DO (Contact). Nothing else in
   this bar is allowed to be coral — the moment a second thing is,
   the pill stops meaning anything.
   ═══════════════════════════════════════════════════════════════════ */
:root{
  --px-navy:#061938;
  --px-navy-deep:#04122A;
  --px-navy-ink:#030C1C;
  --px-gold:#E8C65F;
  --px-gold-lift:#F3DD93;
  --px-coral:#FF501F;
  --px-coral-lift:#FF6A3D;
  --px-cream:#F1ECE1;
  --px-mist:#B1BFD7;
  --px-slate:#7C89A3;
}

/* ═══ SHELL ═══════════════════════════════════════════════════════ */
#pt-nav,#pt-nav *{box-sizing:border-box}
#pt-nav{
  position:fixed;top:0;left:0;right:0;z-index:90;
  font-family:'Montserrat',system-ui,-apple-system,sans-serif;
  border-bottom:1px solid transparent;
  background:transparent;
  transition:background .4s ease,border-color .4s ease,backdrop-filter .4s ease;
  -webkit-font-smoothing:antialiased;
}
/* Navy on scroll. NEVER black. */
#pt-nav.is-stuck{
  background:rgba(6,25,56,.92);
  backdrop-filter:blur(16px);
  -webkit-backdrop-filter:blur(16px);
  border-bottom-color:rgba(232,198,95,.16);
}
#pt-bar{
  max-width:1360px;margin:0 auto;
  padding:16px clamp(20px,4vw,52px);
  display:flex;align-items:center;gap:clamp(16px,2.2vw,28px);
  position:relative;
}

/* ═══ LOGO ════════════════════════════════════════════════════════ */
#pt-logo{flex:0 0 auto;display:flex;align-items:center;line-height:0;
  border-radius:6px;text-decoration:none;transition:opacity .25s ease}
#pt-logo:hover{opacity:.82}
#pt-logo:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:6px}
/* If the CDN ever fails, the alt text renders as ordinary cream wordmark
   text rather than a giant blue underlined link. */
#pt-logo img{height:30px;width:auto;display:block;
  font-size:.82rem;font-weight:700;letter-spacing:.02em;color:#F1ECE1}
@media(max-width:600px){#pt-logo img{height:26px}}

/* ═══ LINK RAIL ═══════════════════════════════════════════════════ */
#pt-links{
  display:flex;align-items:center;gap:clamp(20px,2.4vw,34px);
  margin-left:auto;
}
.pt-item{position:relative;display:flex;align-items:center}
.pt-link{
  display:inline-flex;align-items:center;gap:.35em;
  font-size:.9rem;font-weight:600;letter-spacing:.005em;
  color:#B1BFD7;white-space:nowrap;text-decoration:none;
  padding:6px 0;border-radius:4px;
  transition:color .2s ease;
}
.pt-link:hover,.pt-link:focus-visible{color:#F1ECE1;outline:none}
.pt-link:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:4px}

/* WHERE YOU ARE. Gold, plus a rule under it so it survives being
   read by someone who cannot separate gold from mist. */
.pt-item.is-active > .pt-link,
.pt-link.is-active{color:#E8C65F}
.pt-item.is-active > .pt-link::after,
.pt-link.is-active::after{
  content:"";position:absolute;left:0;right:0;bottom:-2px;height:2px;
  background:rgba(232,198,95,.75);border-radius:2px;
}
.pt-link{position:relative}

/* ═══ CARET + DROPDOWN ════════════════════════════════════════════ */
.pt-caret{
  background:none;border:none;padding:6px 2px;margin:0;
  font-family:inherit;font-size:.72em;line-height:1;
  color:inherit;cursor:pointer;
  display:inline-flex;align-items:center;
  transition:transform .25s ease,color .2s ease;
  color:#7C89A3;
}
.pt-item:hover .pt-caret{color:#B1BFD7}
.pt-caret:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:3px;border-radius:4px}
.pt-item:hover .pt-caret,
.pt-item.is-open .pt-caret{transform:rotate(180deg)}

.pt-menu{
  position:absolute;top:100%;left:50%;
  transform:translateX(-50%);
  min-width:252px;
  display:none;flex-direction:column;gap:2px;
  margin-top:14px;
  background:rgba(4,18,42,.98);
  backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);
  border:1px solid rgba(232,198,95,.18);
  border-radius:14px;padding:10px;
  box-shadow:0 30px 60px -20px rgba(3,12,28,.8);
}
/* The 14px gap between the item and the menu would break a hover
   chain, so the bridge below keeps the pointer "inside" while it
   crosses. Without it the menu flickers shut mid-travel. */
.pt-menu::before{content:"";position:absolute;top:-16px;left:0;right:0;height:16px}

/* Desktop opens on hover AND on keyboard focus. No JS involved. */
@media(min-width:1024px){
  .pt-item:hover .pt-menu,
  .pt-item:focus-within .pt-menu,
  .pt-item.is-open .pt-menu{display:flex}
}
.pt-menu a{
  padding:11px 14px;border-radius:9px;
  font-size:.88rem;font-weight:500;color:#D7DEEA;
  white-space:nowrap;text-decoration:none;
  transition:background .2s ease,color .2s ease;
}
.pt-menu a:hover,.pt-menu a:focus-visible{
  background:rgba(232,198,95,.1);color:#E8C65F;outline:none}
.pt-menu a:focus-visible{box-shadow:inset 0 0 0 1px rgba(232,198,95,.55)}
.pt-menu .pt-sub{
  display:block;font-size:.63rem;letter-spacing:.18em;text-transform:uppercase;
  font-weight:800;color:#7C89A3;margin-bottom:3px}
.pt-menu a:hover .pt-sub{color:rgba(232,198,95,.72)}

/* ═══ CONTACT · THE ONLY CORAL IN THE BAR ═════════════════════════ */
#pt-cta-wrap{display:flex;align-items:center;gap:14px;flex:0 0 auto;margin-left:clamp(8px,1.6vw,20px)}
#pt-cta{
  display:inline-flex;align-items:center;gap:.5em;
  background:#FF501F;color:#FFFFFF;
  font-weight:700;font-size:.86rem;
  padding:.72em 1.45em;border-radius:999px;
  white-space:nowrap;text-decoration:none;
  transition:background .25s ease,transform .25s ease,box-shadow .25s ease;
}
#pt-cta:hover,#pt-cta:focus-visible{
  background:#FF6A3D;transform:translateY(-1px);
  box-shadow:0 10px 24px -10px rgba(255,80,31,.7);outline:none}
#pt-cta:focus-visible{outline:2px solid #F3DD93;outline-offset:3px}
#pt-cta .arrow{font-size:.9em;transition:transform .25s ease}
#pt-cta:hover .arrow{transform:translateX(3px)}

/* ═══ BURGER ══════════════════════════════════════════════════════ */
#pt-burger{
  display:none;background:none;border:none;cursor:pointer;
  flex-direction:column;justify-content:center;gap:5px;
  padding:8px;margin-right:-8px;border-radius:6px}
#pt-burger:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:2px}
#pt-burger span{display:block;width:24px;height:2px;background:#F1ECE1;border-radius:2px;
  transition:transform .3s ease,opacity .2s ease}
#pt-nav.menu-open #pt-burger span:nth-child(1){transform:translateY(7px) rotate(45deg)}
#pt-nav.menu-open #pt-burger span:nth-child(2){opacity:0}
#pt-nav.menu-open #pt-burger span:nth-child(3){transform:translateY(-7px) rotate(-45deg)}

/* ═══ MOBILE ══════════════════════════════════════════════════════ */
@media(max-width:1023px){
  #pt-burger{display:flex}
  #pt-nav{background:rgba(6,25,56,.92);backdrop-filter:blur(16px);
    -webkit-backdrop-filter:blur(16px)}

  #pt-links{
    position:absolute;top:100%;left:0;right:0;
    flex-direction:column;align-items:stretch;gap:2px;
    margin-left:0;
    /* SOLID, AND NO BACKDROP FILTER ON THIS ONE. The bar above keeps its
       blur; this panel cannot have one. Nested backdrop-filters inside a
       shadow root make Chromium composite this element against an already
       filtered backdrop, and the page behind reads straight through a
       background that is 98.5% opaque. On the built-in version of this nav
       the hero headline was legible through the open menu. The bar's own
       blur still gives the effect where it matters. */
    background:#061938;
    border-bottom:1px solid rgba(232,198,95,.16);
    padding:10px clamp(16px,4vw,44px) 22px;
    max-height:calc(100vh - 68px);overflow-y:auto;
    display:none;
  }
  #pt-nav.menu-open #pt-links{display:flex}

  .pt-item{flex-wrap:wrap;justify-content:space-between;
    border-bottom:1px solid rgba(241,236,225,.07)}
  .pt-item:last-child{border-bottom:none}
  .pt-link{flex:1 1 auto;padding:15px 2px;font-size:1rem}
  .pt-item.is-active > .pt-link::after,.pt-link.is-active::after{display:none}
  .pt-caret{flex:0 0 auto;padding:15px 10px;font-size:.85em}

  /* Child list is a plain indented block on mobile. A floating panel
     inside a panel is a place to get lost. */
  .pt-menu{
    position:static;transform:none;min-width:0;width:100%;
    margin:0 0 12px;padding:0 0 0 14px;
    background:transparent;backdrop-filter:none;-webkit-backdrop-filter:none;
    border:none;border-left:2px solid rgba(232,198,95,.28);
    border-radius:0;box-shadow:none;
    flex-basis:100%;
  }
  .pt-menu::before{display:none}
  .pt-item.is-open .pt-menu{display:flex}
  .pt-menu a{padding:12px 12px;font-size:.94rem}

  #pt-cta-wrap{margin-left:auto}
  #pt-cta{font-size:.8rem;padding:.68em 1.2em}
  #pt-cta .cta-long{display:none}
}
@media(max-width:420px){
  #pt-cta{padding:.66em 1.05em}
  #pt-cta .arrow{display:none}
}


/* ═══ THE 320px BAR ═══════════════════════════════════════════════
   At 320 the logo, the Contact pill and the burger add up to more than
   the bar, and the burger hangs 9px off the right edge. Nothing here is
   a redesign: the logo loses 4px, the bar loses 6px of padding either
   side, and the pill tightens. Added 4 Aug 2026 to all three pages and
   to PtNav v3.dc.html, which stays the source of record. */
@media(max-width:360px){
  #pt-bar{padding-left:14px;padding-right:14px;gap:10px}
  #pt-logo img{height:22px}
  #pt-cta{padding:.6em .82em;font-size:.8rem}
  #pt-burger{width:34px;margin-right:-4px}
}

@media(prefers-reduced-motion:reduce){
  #pt-nav,#pt-nav *{transition:none!important}
}`;

  var HTML = `
<!-- ══════════════ SITE NAV, BAKED IN ═══════════════════════════════════
     From PtNav v3.dc.html. Turn the Wix site Header OFF for this page or the
     visitor gets two navs. Second copy: see the note in the stylesheet.
     ═══════════════════════════════════════════════════════════════════════ -->
<header id="pt-nav">
  <div id="pt-bar">

    <!-- ══ LOGO = HOME. No text "Home" item; the mark is the link. ══ -->
    <a id="pt-logo" href="https://www.parallaxxtransformations.com" aria-label="Parallaxx Transformations — home">
      <img src="https://static.wixstatic.com/media/e1784d_fe3c841c471f47d088f0cd631a89d883~mv2.png"
           alt="Parallaxx Transformations">
    </a>

    <nav id="pt-links" aria-label="Primary">

      <!-- ══ FOR MEN ══ parent is a real link to the men's page ══ -->
      <div class="pt-item pt-has-drop" data-nav="men">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/men">For Men</a>
        <button class="pt-caret" type="button" aria-label="Show For Men menu" aria-expanded="false" aria-controls="pt-menu-men">&#9662;</button>
        <div class="pt-menu" id="pt-menu-men">
          <a href="https://www.parallaxxtransformations.com/men">Start Here</a>
          <a href="https://www.parallaxxtransformations.com/the-reconnected-man">The Reconnected Man</a>
        </div>
      </div>

      <!-- ══ FOR WOMEN ══ -->
      <div class="pt-item pt-has-drop" data-nav="women">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/women">For Women</a>
        <button class="pt-caret" type="button" aria-label="Show For Women menu" aria-expanded="false" aria-controls="pt-menu-women">&#9662;</button>
        <div class="pt-menu" id="pt-menu-women">
          <a href="https://www.parallaxxtransformations.com/women">Start Here</a>
          <a href="https://www.parallaxxtransformations.com/the-reconnected-woman">The Reconnected Woman</a>
        </div>
      </div>

      <div class="pt-item" data-nav="about">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/about-daniel-lawson">About</a>
      </div>

      <div class="pt-item" data-nav="testimonials">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">Testimonials</a>
      </div>

    </nav>

    <div id="pt-cta-wrap">
      <!-- CONTACT. Coral pill — Parallaxx-only accent. GIA uses a gold
           pill in this exact position: same shape, different colour.
           That is the whole family/identity trick. -->
      <a id="pt-cta" data-nav="contact" href="https://www.parallaxxtransformations.com/contact-daniel-lawson">
        Contact<span class="cta-long">&nbsp;</span><span class="arrow" aria-hidden="true">&#8594;</span>
      </a>
      <button id="pt-burger" type="button" aria-label="Menu" aria-expanded="false" aria-controls="pt-links">
        <span></span><span></span><span></span>
      </button>
    </div>

  </div>
</header>

<div id="pxa-root" style="background:#04122A;color:#B1BFD7;font-family:'Montserrat',system-ui,sans-serif;font-size:16px;line-height:1.7;overflow-x:clip;position:relative">
<!-- ══════════════ 01 · WHO ════════════════════════════════════════════════
     ORBIT "CHARACTERS" + NN/G "SUMMARIES" + EVIDENCE HIGH ON THE PAGE.

     NN/g: start telling the story the moment people land, in scannable form,
     with a real photograph of the actual person. Users form fast judgments
     from very little information -- the halo effect -- so screen one carries
     disproportionate weight.

     The live page currently opens "DANIEL'S CLIENTS HIRE HIM WHEN THEY WANT
     TO SHINE AS A HUMAN IN A MEANING DRIVEN WORLD" -- third person, in caps,
     a sentence about how in demand he is. That is precisely the marketing
     jargon NN/g found users see straight through.
     ═══════════════════════════════════════════════════════════════════════ -->
<section id="pxa-hero">
  <!-- ▸ SHOT 01 — THE HERO FRAME. Currently the crop already on the live
       About page. It holds the fold, but it is a studio frame and NN/g rank
       stock-looking photography 4th on the frustration list. The replacement
       to shoot: Daniel in a real room he actually works in, three-quarter,
       straight down the lens, available light, no stage and no mic. Portrait
       orientation, 2000px minimum on the short edge -- it runs full-height
       off the edge of the viewport. -->
  <div class="shot" aria-hidden="true">
    <img src="https://static.wixstatic.com/media/e1784d_b79c8e912d16444584b8a4726542b886~mv2.png/v1/crop/x_828,y_138,w_1046,h_1225/fill/w_900,h_1400,al_c,q_90,enc_avif,quality_auto/What%20you%20resist%20persists_%20(3).png"
         alt="" loading="eager">
  </div>

  <div class="inner">
    <div style="max-width:min(640px,92%)">
      <p class="px-hand px-fade" style="font-size:1.6rem;color:#E8C65F;margin-bottom:18px">this is how I got here</p>

      <!-- ▸ THE H1 IS THE BEAT HOME DELIBERATELY HOLDS BACK. Home's block 03
           ends "Worked fine for about thirty years" and its own copy doc names
           the next sentence as belonging here. Home sets it up, this pays it
           off, and neither page says the same sentence twice.
           The hut line itself is still the spine -- beat 02 and section 06. -->
      <h1 class="px-serif" style="font-size:clamp(26px,3.3vw,44px);line-height:1.1;color:#F1ECE1;margin-bottom:24px">
        <span class="px-line"><span style="font-weight:500">I hit every target they set.</span></span>
        <span class="px-line"><span style="font-weight:300;font-style:italic;font-size:clamp(17px,2.5vw,34px);color:#B1BFD7">It cost me my soul.</span></span>
      </h1>

      <!-- ONE SENTENCE, AND IT HAS TO CARRY BOTH AVATARS. Not "married men" --
           that is an offer's language, and this is the only sentence a
           stranger is guaranteed to read. It names the MECHANISM, which the
           married man and the high-performing woman genuinely share. -->
      <p class="px-fade" style="font-size:clamp(17px,1.5vw,21px);line-height:1.62;color:#C4CEE0;max-width:36ch;margin-bottom:12px">
        I'm Daniel Lawson. I spent thirty years handling everything on my own, and now I coach people who do the same.
      </p>

    </div>
  </div>

  <div id="pxa-ribbon" class="px-fade">
    <div id="pxa-nums">
      <div><b>1000+</b><i>Lives changed</i></div>
      <div><b>9</b><i>Countries</i></div>
      <div><b>6+</b><i>Years facilitating</i></div>
      <div><b>ICG</b><i>Accredited coach</i></div>
      <div><b>GIVE IT ALL</b><i>Founder &amp; host</i></div>
    </div>
  </div>
</section>


<!-- ══════════════ 02 · WHAT I ACTUALLY DO ═════════════════════════════════
     ORBIT ELEMENT: "RESOLUTION — what problem do they solve?"

     THE MOST IMPORTANT NEW WRITING ON THE PAGE. Missing from v4, v5 and v6
     entirely, despite being a named element in the model and despite "what
     does this person actually do" sitting at the top of NN/g's list of what
     users came to find.

     NO CLIENT TESTIMONIALS ON THIS PAGE. Removed 5 Aug, and the reasoning
     matters because the research argues the other way if you read it
     carelessly.

     Orbit's finding is that evidence belongs BESIDE THE CLAIM IT VERIFIES on
     a high-traffic page rather than siloed. True -- but the operative half is
     "the claim it verifies". The claim this page makes is *I am someone worth
     talking to*. Client outcome quotes verify a different claim, *the work
     works*, which is the offer pages' job and has a page of its own.

     A CONVERSION PAGE IS NOT A SALES PAGE. This page does convert -- Orbit
     measured 11% of About readers going to Contact next -- but what converts
     on an About page is trust in the PERSON. Two clients describing their
     results makes the page about their results, on the one page on the site
     whose subject is Daniel.

     THE TRUST JOB IS STILL DONE, by record instead of by testimony: the
     numbers ribbon on screen one, the press names, the accreditations, the
     nine countries, GIVE IT ALL. NN/g list missing trust indicators among the
     things that frustrate users; none of those has been removed. What has
     gone is other people's voices, which were crowding the only page where
     his should be the only one.

     ▸ IF THIS EVER GETS RE-ADDED, it should be for a stated reason that beats
       the above, not because a best-practice article says pages convert
       better with testimonials on them. They do. This one is not that kind
       of page.

     THE OUTCOMES ARE DELIBERATELY SMALL. From the W-series archive: every
     woman interviewed described modest internal wins -- a clear no, twice;
     not sending the message; two or three moments of feeling alive again.
     The archive's instruction is explicit: do not promise transformation.
     Same calibration applied to the men's side.
     ═══════════════════════════════════════════════════════════════════════ -->
<section class="px-sec" style="background:#F1ECE1;color:#3A3630">
  <div class="px-head" style="max-width:1140px">
    <p class="px-label bronze px-fade" style="margin-bottom:16px">01. What I do</p>

    <h2 class="px-serif" style="font-size:clamp(28px,3.6vw,46px);line-height:1.18;color:#1E2A3D;margin-bottom:18px">
      <span class="px-line"><span>I help people want things again,</span></span>
      <span class="px-line"><span style="font-style:italic;color:#72685B;font-size:.78em">and say it directly.</span></span>
    </h2>

    <p class="px-fade" style="font-size:1.04rem;line-height:1.76;color:#5E5850;max-width:62ch">
      Ask me what I wanted and I'd have said &#8220;I'm easy&#8221;, and meant it. Years ago I stopped asking, and that turned into suppressing the want before I even felt it. I'd worked out it was easier not to.<br><br>That's what I work on now. All of it comes back to reconnecting with yourself.<br><br>I work with men years into a relationship who've disconnected but still care about her. And women who are the most capable person in the room but don't feel like it in their own lives.</p>


    <!-- CALIBRATION. This paragraph does more work than it looks like it does:
         it is the anti-hype clause, and it is what keeps the page on the right
         side of NN/g's 9th frustration. -->
    <div class="px-fade" style="max-width:62ch;margin-top:30px;padding-left:22px;border-left:2px solid rgba(160,138,94,.5)">
      <p style="font-size:1rem;line-height:1.76;color:#5E5850">
        None of this asks you to be less ambitious or less capable. What you built works.
      </p>
    </div>
  </div>
</section>


<!-- ══════════════ 03 · THE BAND ═══════════════════════════════════════════
     The one place the page stops reading, and the natural home for the
     biggest photograph on the site.

     ▸ The line is 01 - The Hut in first person. The archive original was
       'You can't sell a man the way out of a room you're still standing in.'
       -- a private note to himself about his own business. Second person and
       'sell' both pointed it at the visitor, so it read as a rule aimed at
       him rather than a confession. Rewritten 5 Aug: first person, no sales
       frame, and the double meaning is literally true both ways.
     ═══════════════════════════════════════════════════════════════════════ -->
<section id="pxa-band">
  <img class="bg" data-px-parallax loading="lazy"
       alt="Daniel Lawson facilitating a room"
       src="https://static.wixstatic.com/media/111174_ad3e5fa47d294ff890516b996bbd2d3c~mv2.jpg">
  <div class="veil"></div>
  <div class="copy">
    <p class="px-serif px-fade" style="font-size:clamp(23px,3.2vw,44px);line-height:1.22;color:#F1ECE1;max-width:none">
      I was the one everyone relied on.
      <span style="display:block;font-style:italic;color:#E8C65F">Nobody knew what was going on with me.</span>
    </p>

  </div>
</section>


<!-- ══════════════ 04 · THE CONFLICT ═══════════════════════════════════════
     ORBIT ELEMENT: "CONFLICT — why did they start this?"

     THREE beats, not five. v6 opened on the headmaster and the engines, which
     is chronology -- it explains how he became useful, which nobody needs.
     Conflict is what he was inside, what it cost, and what broke it.
     ═══════════════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="pxa-arc" style="background:#F7F3EA;color:#3A3630">
  <div class="px-head">

    <!-- ▸ DIALOG SLOT — DELIBERATELY EMPTY. Read this before filling it.
         Orbit's model asks "what do these people sound like", and v7.1 filled
         it with 111174_4e1bcc7d... That was wrong: the clip is a RECONNECTED
         MAN asset, not a self-introduction, and it is declared .px-vid.reel
         on both avatar pages -- a 9:16 vertical, which this 16:9 slot would
         have cropped through his head. Removed 5 Aug.

         AND A LONG-FORM ORIGIN STORY DOES NOT GO HERE EITHER. Home's own
         build note already made this argument about the 12-15 minute
         interviews: "Nobody three sections above a CTA commits to thirteen
         minutes. And the ones who click LEAVE FOR YOUTUBE and do not come
         back. That isn't proof, it's a leak with a play button on it."
         It is worse on this page than on Home, because the origin story IS
         this page. An embed here competes with the three beats below it and
         says the same thing at four times the length.

         WHAT WOULD EARN THE SLOT: a 45-60 second cut, hosted on Wix, that is
         a TASTE rather than the whole thing -- the same recommendation Home
         carries. Until that exists the page is already in his voice from the
         first line, which is what the element is actually asking for.

         If it ever gets built: <button class="px-vid px-fade"> with data-mp4,
         a poster img, .play and .len. The CSS is still in the stylesheet and
         the facade handler is still in the script, so it is markup only.

         A link to the long-form belongs at the END, in the setting list,
         where leaving the page costs nothing because they have read it. -->

    <p class="px-label bronze px-fade" style="margin-bottom:14px">02. Why this exists</p>
    <h2 class="px-serif" style="font-size:clamp(28px,3.7vw,48px);line-height:1.2;color:#1E2A3D;margin-bottom:clamp(34px,4vw,56px)">
      <span class="px-line"><span>I was very good at a life</span></span>
      <span class="px-line"><span style="font-style:italic;color:#72685B;font-size:.78em">I was disconnected from.</span></span>
    </h2>

    <!-- ── BEAT ONE · THE MACHINE ───────────────────────────────
         The credential he cannot use on the men's pages. On the relationship
         work he cannot claim to be ahead of the avatar. Here he can, because
         he actually left -- and it is the only part of his history that
         speaks straight to a high performer whose firm has just told her 99%
         was not enough. Verbatim where it is in quotes. -->
    <div class="px-row px-fade">
      <div class="media">
        <img src="https://static.wixstatic.com/media/111174_cf1cabe1fd064e459f2317f9d009da0c~mv2.jpg"
             alt="Daniel Lawson at a trade show stand during his corporate years" loading="lazy">
      </div>
      <div>
        <p class="no">01</p>
        <h3>The goalposts shifted every year.</h3>
        <p>Business development manager for a tech company in Australia. I was good at it, which is the trap.</p>
        <blockquote>“I always hit my target, but I traded my soul. Then the next year it goes up another 10%, so I've got to do all of it again, plus a little extra.”</blockquote>
        <p style="margin-top:14px">I resigned, and lost my sense of identity with it. I'd never checked what was underneath the performance of a good-looking life.</p>
        <p style="margin-top:14px">I can't tell you what it's like to be a man twenty years into a marriage, or what a woman goes through in her professional life. What I do know is being stuck in the machine, disconnected from what matters. And I found the way out.</p>
      </div>
    </div>

    <!-- ── BEAT TWO · THE HUT ───────────────────────────────────
         ▸ SOURCED FROM 01 - The Hut. The deletion and the half-ask are both
           in there near-verbatim. Approved for publication 2 Aug 2026. -->
    <div class="px-row flip px-fade">
      <div class="media">
        <img src="https://static.wixstatic.com/media/111174_107f479d04e9492189d6c7928e1ae8e5~mv2.jpg"
             alt="Daniel Lawson facilitating a full room at a GIVE IT ALL evening" loading="lazy">
      </div>
      <div>
        <p class="no">02</p>
        <h3>I created my own community.</h3>
        <p>Rather than ask for inclusion, I built the things others needed, so being needed was guaranteed by design. Stages, communities, an ecosystem of experience. It worked so well, and it kept me safe. Safe from what I really wanted from life.</p>
        <p style="margin-top:14px">A pattern from before the career, the business, and the building. I'd learned it from a young age: <strong>No want, no ask, no risk, no rejection.</strong> And if I had to, I asked at about 40%, with an easy way out built in. Meant I never got turned down. Rarely got what I wanted either.</p>
      </div>
    </div>

    <!-- ── BEAT THREE · THE TURN ────────────────────────────────
         The real asset, daniel-conversation.jpg, goes on the beat where he
         ARRIVES rather than on either of the two where he is still inside it.
         Square crop, coral marker: one row is allowed to break the grid,
         because one moment in the story is the hinge. -->
    <div class="px-row turn px-fade">
      <div class="media">
        <img src="https://static.wixstatic.com/media/111174_36d2347a86fd4315b960a3f1082c14af~mv2.jpg"
             alt="Daniel Lawson laughing, eyes closed, on the coast" loading="lazy">
      </div>
      <div>
        <p class="no">03</p>
        <h3>Full expression of desire, and heartbreak.</h3>
        <p>I fell in love for the first time at thirty-one. Heading into year two, something felt off. But nothing was wrong, and that was easy enough to let go. Instead I did the thing I'd avoided my whole life. I put my wants on the table and asked fully, so I'd get a full answer. My fears came true. We ended it, and I found myself on my villa floor in a puddle of tears, heartbroken for the first time in my life.</p>
        <p style="margin-top:14px">The ask is how I found out it was already over. Without it we'd have wasted years tolerating mediocrity and never known. It was also the first time I felt anything deeply, and that opened a whole range of emotions I'd never had access to. <strong>A no stopped being a failure and became an answer.</strong> Now asking directly feels like a superpower. Yes is great. No is the truth, and the truth is what I need for the next move.</p>
      </div>
    </div>

    <p class="px-hand px-fade" style="font-size:1.4rem;color:#7C663A">no judgment. most of us do this until we know better</p>
  </div>
</section>


<!-- ══════════════ 05 · WHAT I BELIEVE ═════════════════════════════════════
     ORBIT ELEMENT: "PLOT — what do these people believe?"

     Rebuilt 5 Aug as a full-bleed band. The previous version was three cards
     with caps labels and second-person copy, which read as a pillars grid on
     a sales page rather than an About page. Two faults: the page switches out
     of first person for the only time after the hero, and the middle card
     ("nothing bad happened") contradicted beat 03, where something bad did
     happen and the story is stronger for it.

     Two gradients, not one. The vertical veil alone greys the whole picture;
     the 90deg scrim darkens only the copy side so the photograph survives on
     the right. On mobile the copy runs full width, so the scrim carries
     further across -- see the media query.
     ═══════════════════════════════════════════════════════════════════════ -->
<section id="pxa-band2">
  <img class="bg" data-px-parallax loading="lazy"
       alt="A full room at a GIVE IT ALL evening"
       src="https://static.wixstatic.com/media/111174_9dda2b35c46647a4b1c394be6146a0c7~mv2.jpg">
  <div class="veil"></div>
  <div class="scrim"></div>
  <div class="copy">
    <p class="px-hand px-fade" style="font-size:1.6rem;color:#E8C65F;margin-bottom:10px">what I came to discover</p>
    <h2 class="px-serif px-fade" style="font-size:clamp(26px,3.2vw,42px);line-height:1.18;color:#F1ECE1;margin-bottom:30px">Three things I now stand by.</h2>

    <div class="belief px-fade"><p>The aim was never to feel better. The whole thing is about feeling more. All of it. The sadness and the grief are what unlocked my joy and excitement.</p></div>
    <div class="belief px-fade"><p>A straight answer beats a prolonged assumption, even when it wasn't the answer I wanted.</p></div>
    <div class="belief px-fade"><p>The quality of my relationships decides the quality of my life. Everything I build sits downstream of that.</p></div>

    <p class="px-hand px-fade" style="font-size:1.75rem;color:#E8C65F;margin-top:30px">connect back with yourself, and life comes back in full colour</p>
  </div>
</section>


<!-- ══════════════ 06 · THE RECORD ═════════════════════════════════════════
     Cut back 5 Aug. This was "03 · The rooms": a headline, a paragraph naming
     GIVE IT ALL and Luiza, three photo tiles and two offer cards linking out
     to the facilitation and speaking pages.

     WHY IT WENT. Three tiles with logos and "Visit ↗ / Open ↗" buttons is a
     services menu, and "voted the best networking event in Medellin" is a
     sales line sitting inside a personal story. Same fault as the audience
     panels and the beliefs cards: the page stops being about him and starts
     selling. Anyone who wants the offers has the nav and the footer.

     WHAT STAYED, AND WHY. The record strip is the only trust indicator on the
     page. NN/g list missing trust indicators among the things that frustrate
     users; Orbit's phrase is "put billboards on highways". Press and
     accreditations are facts about him, not offers, so they belong on an
     About page in a way the tiles never did.
     ═══════════════════════════════════════════════════════════════════════ -->
<section class="px-sec" style="background:#0A1D3C">
  <div class="px-head" style="max-width:1140px">
    <p class="px-fade" style="text-align:center;font-size:.73rem;letter-spacing:.22em;text-transform:uppercase;color:#8D9AB4;font-weight:700;margin-bottom:20px">Seen in · Credentialled by</p>
    <div id="pxa-press" class="px-fade">
      <span>ABC News</span><i>·</i>
      <span>Wanderlust Magazine</span><i>·</i>
      <span>Brainz Magazine</span><i>·</i>
      <span>Solarcon</span><i>·</i>
      <span>The Coaching Institute</span><i>·</i>
      <span>Int'l Coach Guild</span>
    </div>
    <div class="px-chips px-fade">
      <span>ICG Credentialed Practitioner</span>
      <span>ICG Advanced Practitioner</span>
      <span>Meta Dynamics Level 3</span>
      <span>Meta Dynamics L1 &amp; Foundations</span>
      <span>Cert. Positive Psychology</span>
    </div>
  </div>
</section>


<!-- ══════════════ 07 · WHERE THIS GOES, WHERE I AM ════════════════════════
     ORBIT ELEMENTS: "VISION — where is this going?" and "SETTING — where is
     this business?". Both were absent from every previous version despite
     being named elements, and NN/g list location and contact among the things
     users came to the page to find.

     THE VISION PARAGRAPH IS DELIBERATELY UNDERSOLD. The W-series archive is
     explicit that the women's room is not positioned yet and that the sales
     page must not be written before the transcripts are in. So this states the
     direction and refuses to describe the product -- which is both honest and
     the correct commercial call.

     ONE BUTTON ON THE PAGE, HERE. Orbit measured 11% of About page visitors
     going to Contact next, so the ask is real. It is just singular.
     Ends on cream so PtFooter's navy CTA block lands as a change of key.
     ═══════════════════════════════════════════════════════════════════════ -->
<section class="px-sec" style="background:#F1ECE1;color:#3A3630">
  <div class="px-head" style="max-width:1140px">
    <div id="pxa-close">

      <div>

        <blockquote class="px-fade" style="border-left:2px solid #A08A5E;padding-left:24px;margin:0 0 26px">
          <p class="px-serif" style="font-size:clamp(18px,2vw,25px);line-height:1.45;color:#7C663A;font-style:italic">“We have two lives; the second begins when we realise we only have one.”</p>
          <cite style="display:block;font-size:.72rem;font-weight:800;letter-spacing:.2em;text-transform:uppercase;color:#78664A;margin-top:14px;font-style:normal">Confucius</cite>
        </blockquote>

        <p class="px-fade" style="font-size:1.03rem;line-height:1.75;color:#5E5850;margin-bottom:28px">
          Mine started at a heartbreak. Yours could start with a conversation, which is cheaper. No form, no funnel, no booking. Just a personal message with what's going on. Let's see how I can help.
        </p>

        <a class="px-btn px-fade" href="https://wa.me/6282146446112?text=Hi%20Daniel.%20I%20read%20your%20About%20page.%20I%20would%20love%20to%20connect%20further%20about..." target="_blank" rel="noopener">Send WhatsApp Message <span aria-hidden="true">&#8594;</span></a>
        <p class="px-fade" style="margin-top:8px"><a href="mailto:dan@parallaxxtransformations.com" style="display:inline-flex;align-items:center;min-height:44px;font-size:.92rem;font-weight:600;color:#8A5A47;text-decoration:none;border-bottom:1px solid rgba(160,138,94,.5)">Or email me</a></p>

      </div>

      <!-- SETTING. NN/g: users came for location and contact. A definition
           list rather than prose, because this is the one block on the page
           nobody reads in sentences. -->
      <dl class="px-setting px-fade">
        <dt>Based</dt>
        <dd>Kiwi by birth, Australian by business, Bulgarian by relationship. No fixed base. Currently between Bali and Bulgaria.</dd>

        <dt>Works</dt>
        <dd>Online worldwide, and in person wherever the rooms are running. Mexico, Australia, USA, Bali, Dominican Republic, Bulgaria, Colombia.</dd>

        <dt>The work</dt>
        <dd><a href="https://www.parallaxxtransformations.com/men">The men's work</a> and <a href="https://www.parallaxxtransformations.com/women">the women's work</a> each have a page of their own.</dd>

        <dt>The long version</dt>
        <dd>The whole origin story, told fully, is <a href="https://youtu.be/1Dn8Cf5bRYo">on video here</a>. It runs about forty minutes.</dd>

        <dt>What people said</dt>
        <dd>There is <a href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">a whole page of it</a>. None of it is on this one on purpose.</dd>

        <dt>Reach me</dt>
        <dd><a href="https://www.parallaxxtransformations.com/contact-daniel-lawson">Share what's going on for you</a>. It comes to me, not to a team.</dd>
      </dl>

    </div>
  </div>
</section>

</div>`;

  function addFonts(){
    if (document.getElementById('px-fonts')) return;
    var p1=document.createElement('link'); p1.rel='preconnect'; p1.href='https://fonts.googleapis.com'; document.head.appendChild(p1);
    var p2=document.createElement('link'); p2.rel='preconnect'; p2.href='https://fonts.gstatic.com'; p2.crossOrigin=''; document.head.appendChild(p2);
    var f0=document.createElement('link'); f0.rel='stylesheet'; f0.href='https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap'; document.head.appendChild(f0);
    var f1=document.createElement('link'); f1.rel='stylesheet'; f1.href='https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap'; document.head.appendChild(f1);
    /* @font-face has to live in the DOCUMENT, not the shadow root. Chrome
       ignores font-face rules declared inside a shadow tree. */
    var ff=document.createElement('style'); ff.id='px-fonts'; ff.textContent=`/* LUMIOS MARKER is not on Google Fonts -- upload the .woff2 to Wix Media and
   drop the URL in below. Until then the stack falls through to Permanent
   Marker, which is the closest free marker face, so nothing renders as a
   system script by accident. */
@font-face{font-family:'Lumios Marker';font-display:swap;
  src:url('LUMIOS_MARKER_WOFF2_URL') format('woff2');}`; document.head.appendChild(ff);
  }

  function loadScript(src){
    return new Promise(function(res,rej){
      var ex=document.querySelector('script[data-px="'+src+'"]');
      if(ex){ if(ex.getAttribute('data-loaded')){res();} else { ex.addEventListener('load',function(){res();}); ex.addEventListener('error',rej);} return; }
      var s=document.createElement('script'); s.src=src; s.async=false; s.setAttribute('data-px',src);
      s.addEventListener('load',function(){ s.setAttribute('data-loaded','1'); res(); });
      s.addEventListener('error',rej);
      document.head.appendChild(s);
    });
  }

  function loadLibs(){
    var g=loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js')
      .then(function(){ return loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js'); });
    var l=loadScript('https://unpkg.com/lenis@1.1.13/dist/lenis.min.js');
    return Promise.all([ g.catch(function(){}), l.catch(function(){}) ]);
  }

  /* This rewrites ancestor heights, which moves every scroll position on the
     page. ScrollTrigger has already cached those positions, so without a
     refresh afterwards its triggers fire at the wrong offsets. Returns
     whether it actually changed anything so the caller can avoid refreshing
     for nothing. */
  /* Measures the real content inside the shadow root. A fixed-position child
     (the site nav) takes no space in flow, so summing children and skipping
     fixed ones is the only honest number: scrollHeight would miss it too and
     offsetHeight of the host is the very thing we are trying to check. */
  /* Sums the real content inside the shadow root. A fixed-position child (a
     baked-in site nav) takes no space in flow, so summing children and
     skipping fixed ones is the only honest number. */
  function contentHeight(host){
    var total = 0;
    try{
      var kids = host.shadowRoot ? host.shadowRoot.children : [];
      for (var i=0;i<kids.length;i++){
        var c = kids[i];
        if (c.tagName === 'STYLE') continue;
        var cs = window.getComputedStyle(c);
        if (cs.position === 'fixed' || cs.display === 'none') continue;
        total += c.getBoundingClientRect().height;
      }
    }catch(e){}
    return total;
  }

  /* WHAT THIS IS ACTUALLY FIXING, measured on the live About page 5 Aug.
     59851px of page with 5790px of content in it: fifty thousand pixels of
     empty section under the footer. Wix writes the widget height the editor
     last recorded into an INLINE STYLESHEET RULE, not an inline style:

         #comp-msfv7drl { height: 59851px }
         #comp-msfv7drl { --custom-element-height: 59851px }

     Two consequences, and the previous version of this function missed both.

     1. A plain host.style.height = 'auto' LOSES to that rule. Every write
        here has to be setProperty with 'important' or nothing moves.
     2. The rule sits on the PARENT and the host stretches to match, so host
        and parent measure identically. Comparing each ancestor against the
        HOST and asking for a 600px difference therefore compares a number
        with itself. It never fired, on any page, for the exact case it was
        written for. Everything is measured against the CONTENT now.

     Verified in the browser before shipping: 59851 -> 5790, and it held.

     THE RUNAWAY GUARDS STAY. They are not paranoia: this loop once grew a
     document to Chrome's 2^24 clamp. Every element is one-shot via the
     WeakSet, and the whole thing bails if the page is already absurd. */
  function collapseAncestors(host){
    var changed = false;
    try{
      if (document.documentElement.scrollHeight > PX_SANE_MAX) return false;
      var content = contentHeight(host);
      if (content < 50) return false;

      if (!pxCollapsed.has(host) && host.getBoundingClientRect().height > content + 24){
        pxCollapsed.add(host);
        if (window.getComputedStyle(host).display === 'inline') host.style.setProperty('display','block','important');
        host.style.setProperty('height','auto','important');
        host.style.setProperty('min-height','0px','important');
        host.style.setProperty('max-height','none','important');
        changed = true;
      }

      var n = host.parentElement, guard = 0;
      while(n && n !== document.body && guard++ < 14){
        if(!pxCollapsed.has(n) && n.getBoundingClientRect().height > content + 240){
          pxCollapsed.add(n);
          n.style.setProperty('height','auto','important');
          n.style.setProperty('min-height','0px','important');
          n.style.setProperty('max-height','none','important');
          n.style.setProperty('--custom-element-height','auto','important');
          changed = true;
        }
        n = n.parentElement;
      }
    }catch(e){}
    return changed;
  }

  function collapseAndRefresh(host){
    var changed = collapseAncestors(host);
    if (changed && window.ScrollTrigger) {
      try{ window.ScrollTrigger.refresh(); }catch(e){}
    }
  }

  function boot(root){
    if(!root || !root.getElementById('pxa-root')) return;

    /* THE SHADOW-ROOT TRAP. The build step rewrites every
       root.getElementById() into root.getElementById(), where root is the
       SHADOW ROOT -- not an element. A ShadowRoot has no classList and no
       style, so any code that touched root directly would throw the moment it
       ran in the deployed bundle while working perfectly in the DC preview.
       So everything below talks to 'host', which resolves to the actual
       #pxa-root DIV on both paths. */
    const host = root.getElementById('pxa-root');
    if(!host) return;
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const hasGSAP = !!(window.gsap && window.ScrollTrigger);

    /* ── REVEAL ───────────────────────────────────────────────────────────
       .anim is what hides things, so it goes on ONLY on the path that also
       animates them back. Every other path -- no GSAP (including GSAP still
       in flight when we mount), reduced motion, or a throw partway through
       the reveal loop -- lands on reveal(), which strips .anim and force-sets
       the end state. There is no branch that hides content without owning the
       job of showing it again. */
    const reveal = ()=>{
      host.classList.remove('anim');
      host.querySelectorAll('.px-line > span').forEach(l=>{ l.style.transform = 'none'; });
      host.querySelectorAll('.px-fade').forEach(el=>{ el.style.opacity = '1'; el.style.transform = 'none'; });
    };

    if(hasGSAP && !reduce){
      host.classList.add('anim');
      try{
        const G = window.gsap, ST = window.ScrollTrigger;
        G.registerPlugin(ST);
        const inView = (el)=>{ const r = el.getBoundingClientRect(); return r.top < window.innerHeight * 0.88; };

        host.querySelectorAll('.px-line > span').forEach(l=>{
          if(inView(l)) G.to(l,{y:'0%',duration:1.05,ease:'expo.out'});
          else G.to(l,{y:'0%',duration:1.05,ease:'expo.out',scrollTrigger:{trigger:l,start:'top 88%',once:true}});
        });
        host.querySelectorAll('.px-fade').forEach(el=>{
          if(inView(el)) G.to(el,{opacity:1,y:0,duration:.95,ease:'power3.out'});
          else G.to(el,{opacity:1,y:0,duration:.95,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 90%',once:true}});
        });

        /* PARALLAX. Only ever applied to a real photograph -- a drifting
           placeholder reads as a broken layout rather than a missing asset. */
        host.querySelectorAll('[data-px-parallax]').forEach(img=>{
          G.to(img,{yPercent:-8,ease:'none',scrollTrigger:{trigger:img,start:'top bottom',end:'bottom top',scrub:true}});
        });

        if(window.Lenis){
          const lenis = new window.Lenis({duration:1.15,smoothWheel:true,touchMultiplier:1.4});
          const raf = (t)=>{ lenis.raf(t); requestAnimationFrame(raf); };
          requestAnimationFrame(raf);
          lenis.on('scroll', ST.update);
        }
      }catch(err){
        /* Something in the animation path threw. Better an unanimated page
           than a half-hidden one. */
        console.warn('[pxa] reveal failed, showing everything:', err);
        reveal();
      }
    } else {
      /* No GSAP, or reduced motion. Nothing stays hidden. */
      reveal();
    }

    /* ── VIDEO FACADE ─────────────────────────────────────────────────────
       No third-party request until the click. data-mp4 -> native video,
       data-yt -> youtube-nocookie. data-live guards re-init. */
    host.querySelectorAll('.px-vid').forEach(btn=>{
      btn.addEventListener('click',()=>{
        if(btn.dataset.live === '1') return;
        btn.dataset.live = '1';
        const mp4 = btn.getAttribute('data-mp4'), yt = btn.getAttribute('data-yt');
        btn.querySelectorAll('.play,.len').forEach(n=>n.remove());
        if(mp4){
          const v = document.createElement('video');
          v.className = 'live'; v.src = mp4; v.controls = true; v.autoplay = true;
          v.playsInline = true; v.setAttribute('playsinline','');
          btn.appendChild(v); v.play().catch(()=>{});
        } else if(yt){
          const f = document.createElement('iframe');
          f.src = 'https://www.youtube-nocookie.com/embed/' + yt + '?autoplay=1&rel=0&modestbranding=1&playsinline=1';
          f.allow = 'autoplay; encrypted-media; picture-in-picture';
          f.setAttribute('allowfullscreen','');
          btn.appendChild(f);
        }
      });
    });
  

    /* ── SITE NAV, BAKED IN ──────────────────────────────────────────
       From PtNav v3.dc.html, wrapped so its locals cannot collide with the
       page's. Arrow IIFE on purpose: the nav reads this.props for the Wix
       editor override, and a plain function would rebind this. In the
       deployed bundle this.props is undefined, so it falls through to the
       URL table, which is the behaviour we want anyway.
       The builder rewrites getElementById calls onto the shadow root, so
       these resolve inside it rather than against the document. */
    (() => {

    const nav = root.getElementById('pt-nav');
    if(!nav) return;
    const links  = root.getElementById('pt-links');
    const burger = root.getElementById('pt-burger');
    const items  = [...nav.querySelectorAll('.pt-has-drop')];
    const MOBILE = () => window.innerWidth < 1024;

    /* ── WHERE YOU ARE ──────────────────────────────────────────────
       Worked out from the URL, because this element lives ONCE in the
       site's header strip and therefore serves every page from a single
       instance. A per-page 'active' prop cannot do that: one instance
       carries one value, so "For Women" would sit underlined on the
       men's page too.

       The prop still wins when it is set, for the Wix editor preview
       and for any page whose path does not match the table below. */
    const PATHS = [
      [/^\/(men|the-reconnected-man|the-archetype-quiz)\b/,      'men'],
      [/^\/(women|the-reconnected-woman|priority-audit)\b/,      'women'],
      [/^\/about-daniel-lawson\b/,                              'about'],
      [/^\/(daniel-lawson-speaking|facilitating)\b/,             'about'],
      [/^\/testimonials-daniel-lawson\b/,                       'testimonials'],
      [/^\/(contact-daniel-lawson|book-a-call)/,                 'contact']
    ];
    const fromPath = () => {
      const p = (location.pathname || '').replace(/\/+$/, '').toLowerCase() || '/';
      for(const [re, key] of PATHS) if(re.test(p)) return key;
      return '';
    };
    const active = (this.props && this.props.active) || fromPath();
    if(active){
      const el = nav.querySelector('[data-nav="'+active+'"]');
      if(el) el.classList.add('is-active');
    }

    /* ── NAVY ON SCROLL ─────────────────────────────────────────── */
    let ticking = false;
    const onScroll = () => {
      if(ticking) return; ticking = true;
      requestAnimationFrame(() => {
        nav.classList.toggle('is-stuck', window.scrollY > 40);
        ticking = false;
      });
    };
    window.addEventListener('scroll', onScroll, {passive:true});
    onScroll();

    /* ── MOBILE PANEL ───────────────────────────────────────────── */
    const closeMenu = () => {
      nav.classList.remove('menu-open');
      burger.setAttribute('aria-expanded','false');
      items.forEach(closeDrop);
    };
    burger.addEventListener('click', () => {
      const open = !nav.classList.contains('menu-open');
      nav.classList.toggle('menu-open', open);
      burger.setAttribute('aria-expanded', String(open));
      if(!open) items.forEach(closeDrop);
    });

    /* ── SUBMENUS ───────────────────────────────────────────────────
       Desktop hover and keyboard focus are pure CSS. The caret only
       exists for pointers that cannot hover, and for anyone who would
       rather tap the arrow than the word. */
    function closeDrop(it){
      it.classList.remove('is-open');
      const c = it.querySelector('.pt-caret');
      if(c) c.setAttribute('aria-expanded','false');
    }
    items.forEach(it => {
      const caret = it.querySelector('.pt-caret');
      caret.addEventListener('click', e => {
        e.preventDefault(); e.stopPropagation();
        const open = !it.classList.contains('is-open');
        items.forEach(closeDrop);
        it.classList.toggle('is-open', open);
        caret.setAttribute('aria-expanded', String(open));
      });
    });

    /* ── DISMISSAL ──────────────────────────────────────────────── */
    links.querySelectorAll('a').forEach(a =>
      a.addEventListener('click', () => { if(MOBILE()) closeMenu(); }));

    /* composedPath, not e.target. A click inside a shadow root is RETARGETED
       by the time it reaches the document: e.target becomes the host element,
       so nav.contains(e.target) is false for a click on the burger itself and
       this handler closed the panel in the same tick the burger opened it.
       The menu looked completely dead. composedPath() reports the real path
       through the shadow tree, and in the light DOM it still contains nav, so
       one line covers both runtimes. */
    document.addEventListener('click', e => {
      const path = e.composedPath ? e.composedPath() : [e.target];
      if(path.indexOf(nav) === -1){ items.forEach(closeDrop); if(MOBILE()) closeMenu(); }
    });

    document.addEventListener('keydown', e => {
      if(e.key !== 'Escape') return;
      items.forEach(closeDrop);
      if(nav.classList.contains('menu-open')){ closeMenu(); burger.focus(); }
    });

    /* Crossing the breakpoint with the phone panel open would leave a
       stranded overlay on the desktop layout. */
    let wasMobile = MOBILE();
    window.addEventListener('resize', () => {
      const now = MOBILE();
      if(now !== wasMobile){ wasMobile = now; closeMenu(); }
    });
    })();
  }

  class ParallaxxAboutPage extends HTMLElement {
    connectedCallback(){
      if (this._mounted) return; this._mounted = true;
      addFonts();
      var shadow = this.attachShadow({mode:'open'});
      shadow.innerHTML = '<style>'+CSS+'</style>'+HTML;
      var host = this;
      loadLibs().then(function(){ try{ boot(shadow); }catch(e){ console.error('[pxa] boot failed:', e); } })
        .catch(function(){ try{ boot(shadow); }catch(e){} });
      requestAnimationFrame(function(){ collapseAndRefresh(host); });
      [400,1200,2500,4000,6000].forEach(function(t){ setTimeout(function(){ collapseAndRefresh(host); }, t); });
      /* Images arriving late change the content height, and Wix can
         re-apply its stored height after its own layout settles. Watching
         the host is cheaper and more reliable than guessing more timeouts. */
      if (window.ResizeObserver){
        try{ new ResizeObserver(function(){ collapseAndRefresh(host); }).observe(host); }catch(e){}
      }
      window.addEventListener('resize', function(){ collapseAndRefresh(host); }, {passive:true});
    }
  }
  customElements.define('parallaxx-about-page', ParallaxxAboutPage);
})();
