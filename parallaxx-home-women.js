/* PARALLAXX TRANSFORMATIONS - Home, women's avatar. Wix Custom Element.
   Tag: parallaxx-home-women
   GENERATED by build-home-women-bundle.py from "Parallaxx Home Women.dc.html".
   DO NOT EDIT THIS FILE - edit the .dc.html and rerun the build.
   In the Wix editor: place PtNav in the site header strip and PtFooter in the
   footer strip, and leave both ON for this page. This element is the page
   content between them, not the whole page. */
(function(){
  if (customElements.get('parallaxx-home-women')) return;

  var CSS = `/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX - HOME - v4  "EVERYTHING IS GLASS"
   -------------------------------------------------------------------
   v3 had a bespoke hero and then fell back on layout defaults --
   centred headings, three white cards, a quote grid. That is the house
   style of every coach site on the internet, and it is what happens
   when the IDEA stops at the fold.

   So: the whole page is made of glass. Every section is another pane
   in another state. The visitor keeps meeting the same material.

     01 HERO      dirty pane. he wipes it. Daniel is behind it.
     02 MIRROR    his confessions arrive FOGGED, and wipe clear on scroll
     03 REFRAME   draggable divider: his side vs her side of the same pane
     04 MASKS     five men, five different kinds of dirt
     05 SYSTEM    three panes of glass. pull one out, see what stays dirty.
     06 DANIEL    the ONLY image on the site with no glass on it at all
     07 PROOF     one face, one quote
     08 CREDS     demoted. proof closes, it does not open.

   TOKENS shared with GIVE IT ALL:  navy #061938 gold #E8C65F Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  Lumios Marker  cream #F1ECE1

   ===================================================================
   THE ACCENT RULE.  GOLD IS DANIEL. CORAL IS YOU.
   -------------------------------------------------------------------
   GOLD  #E8C65F -- Daniel's VOICE. Everything he says.
                    His handwriting (Lumios Marker). His emphasis inside prose.
                    His pull-quotes. Section labels. Hairlines.
                    Shared with GIVE IT ALL -- it is the family colour.

   CORAL #FF501F -- THE READER. His words, his actions, his moves.
                    Buttons. Links. Controls. The divider handle. Active
                    states. The confession cards -- because those are HIS
                    sentences, not Daniel's. And the microcopy that tells
                    him to do something.

   Coral never appears as decorative emphasis in prose. Reason is
   commercial, not aesthetic: colour is a scarcity currency, and every
   coral word on the page devalues the coral BUTTON. Keep coral rare and
   the eye finds the CTA without being asked.

   Structural emphasis inside body copy is ink (#1E2A3D on cream,
   #F1ECE1 on navy) -- never an accent.
   =================================================================== 
   ═══════════════════════════════════════════════════════════════════ */
  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
  ::selection{background:#FF501F;color:#fff}
  img{display:block;max-width:100%}

  /* ══ THE HOST ═════════════════════════════════════════════════════
     A custom element is display:inline by default. Inline means no block
     box, a baseline to sit on, and a wrapper free to size itself wrong --
     which is what leaves a band of PAGE background under the component.
     On a Wix page that band is white.

     Both declarations matter. display:block fixes the box. The background
     means that if anything ever does leave a gap, the gap is navy and
     invisible rather than a white cliff at the foot of the site.

     This never showed up in preview because the harnesses set display on
     the tag from OUTSIDE. The harness was quietly fixing it and Wix was
     not, so it belongs in here, where it ships. Added 4 Aug 2026 to all
     three pages at once. #04122A, matching #px-root on this page.
     ══════════════════════════════════════════════════════════════════ */
  :host{display:block;background:#04122A}

  #px-root{background:#04122A;color:#B1BFD7;font-family:'Montserrat',system-ui,sans-serif;font-size:16px;line-height:1.7;overflow-x:clip;position:relative}
  /* DISPLAY FACE. Was Cormorant Garamond at weight 300 with negative tracking --
     settings that flatter a high-contrast serif and gut a geometric sans.
     Poppins at 300 goes weak and spindly at 60px, so the display weight comes
     UP to 400/500 and the tracking goes slightly negative only at large sizes,
     which is where Poppins' generous default spacing actually needs reining in. */
  .px-serif{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em}
  .px-hand{font-family:'Lumios Marker','Permanent Marker',cursive}
  @media(max-width:560px){
    .px-hand{font-size:clamp(14px,4.6vw,21px)!important;white-space:nowrap}
  }
  .px-wrap{max-width:1240px;margin:0 auto}
  .px-sec{padding:clamp(52px,6.5vw,92px) clamp(20px,4vw,52px);position:relative}
  .px-label{font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;color:#5E6B85}

  .px-btn{display:inline-flex;align-items:center;gap:.55em;background:#FF501F;color:#fff;font-weight:700;font-size:.98rem;padding:1.05em 2.2em;border-radius:999px;text-decoration:none;transition:background .3s,transform .3s,box-shadow .3s;box-shadow:0 10px 30px -12px rgba(255,80,31,.5)}
  .px-btn:hover{background:#FF6A3D;transform:translateY(-2px)}
  .px-ghost{display:inline-flex;align-items:center;gap:.55em;color:#B1BFD7;border:1px solid rgba(232,198,95,.4);font-weight:600;font-size:.94rem;padding:1em 2em;border-radius:999px;text-decoration:none;transition:background .3s,border-color .3s}
  .px-ghost:hover{background:rgba(241,236,225,.08);border-color:#F1ECE1;color:#F1ECE1}

  /* KINETIC TYPE.
     Each .px-line is ONE line, masked and slid up. Which means each one must
     actually FIT on one line -- if it wraps, the mask breaks and you get
     orphaned single-word rows.

     The bug: these headlines were sitting in a max-width:68ch box. 'ch' is
     measured against the BODY font size (~9px per char), so that box is only
     ~630px wide -- while a 54px display headline needs ~1000px. It wrapped
     four times and orphaned a word.

     So: headline containers get their own width, in px, sized for DISPLAY
     type -- never the body measure. And each line refuses to wrap. */
  .px-line{display:block;overflow:hidden}
  /* NO NOWRAP. Each line is its own span already, so nowrap only ever meant
     "clip me if the box is narrower than the text" -- which is what shaved the
     f off "yourself". Wrapping is the safe failure. */
  .px-line > span{display:block;transform:translateY(110%);will-change:transform;padding-right:.08em}
  @media(max-width:820px){
  }
  .px-head{max-width:min(1140px,94vw);margin-left:auto;margin-right:auto}
  .px-fade{opacity:0;transform:translateY(22px);will-change:transform,opacity}

  .px-ph{position:relative;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;gap:11px;padding:30px;
    background:repeating-linear-gradient(45deg,rgba(255,80,31,.05) 0 12px,rgba(255,80,31,.10) 12px 24px);
    border:2px dashed #FF501F;border-radius:18px;min-height:280px}
  .px-ph b{font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;color:#FF501F;font-weight:800}
  .px-ph span{font-size:.87rem;line-height:1.7;max-width:36ch;color:#E6BCAC}
  .px-ph em{font-style:normal;font-size:.73rem;line-height:1.6;color:#8E9BB2;max-width:38ch}
  .px-ph.on-cream span{color:#8A5A47}.px-ph.on-cream em{color:#A6968A}

  /* ── 01 HERO ─────────────────────────────────────────────── */
  #px-hero{touch-action:pan-y;position:relative;height:100svh;min-height:640px;overflow:hidden;background:#04122A}
  #px-glass{touch-action:pan-y;position:absolute;inset:0;width:100%;height:100%;display:block}
  /* THE FALLBACK — mobile, reduced-motion, and low-power machines.
     This is not a second-class path. There is no cursor on a phone, so
     there is no wipe -- which means without this the biggest half of the
     audience would sit behind permanently dirty glass and NEVER see the
     face. The face is the entire payoff. So on the fallback the glass
     clears on SCROLL instead, driven by --px-clear (0 -> 1) below.

     background-position anchors on the face, not the centre: on a tall
     phone a centre-crop of a 16:10 frame throws his head off the right. */
  #px-fallback{position:absolute;inset:0;background:#04122A;display:none;--px-clear:0}
  #px-fallback .fb-img{position:absolute;inset:0;background-size:cover;background-position:70% 18%;
    filter:saturate(calc(.55 + .45*var(--px-clear)))
           contrast(calc(1.05 - .05*var(--px-clear)))
           brightness(calc(.5 + .5*var(--px-clear)))
           blur(calc(9px*(1 - var(--px-clear))))}
  #px-fallback .fb-glass{position:absolute;inset:0;
    background:rgba(180,160,120,calc(.30*(1 - var(--px-clear))))}
  #px-hero-veil{position:absolute;inset:0;pointer-events:none;
    background:linear-gradient(90deg,rgba(4,18,42,.90) 0%,rgba(4,18,42,.58) 44%,rgba(4,18,42,.04) 78%),
               linear-gradient(0deg,rgba(4,18,42,.86) 0%,rgba(4,18,42,0) 52%)}
  #px-hero-copy{position:absolute;inset:0;display:flex;align-items:center;padding:0 clamp(20px,5vw,90px);pointer-events:none}
  #px-hero-copy .inner{max-width:min(720px,58vw);pointer-events:auto}
  /* ON A PHONE THE COPY SITS LOW, NOT CENTRED.
     Centred is right on a 16:10 desktop frame, where the face is off to the
     right and the type has its own column. On a 9:19 phone the image is
     centre-cropped, so his face moves to the middle -- and centred type then
     lands straight across his eyes. Eye contact is the whole reason this
     frame exists, so the type gives way, not the face.

     Sitting it low also spends the dead space that was sitting under the
     copy, and it reads against the bottom of #px-hero-veil, which is the
     darkest part of the overlay. */
  @media(max-width:900px){
    #px-hero-copy .inner{max-width:100%}
    #px-hero-copy{align-items:flex-end;padding-bottom:clamp(26px,6vh,58px)}
    #px-hero{min-height:560px}
  }

  /* -- 02 MIRROR : the confessions ------------------------------
     They used to sit in a 6-up grid -- all on screen at once, so the eye
     SCANNED them and they blurred into "a list of sad things". Not one
     landed.

     Now they're staggered down an offset column: only one or two are ever
     in view, so the SCROLL is what delivers them one at a time. Each wipes
     clear as it individually arrives -- guaranteed, on every device, with
     no interaction required.

     Hover only ACCELERATES it. A reward for the curious, never a toll.
     We do not gate the best copy on the page behind a mechanic.  ----- */
  /* -- 02 THE MIRROR : a message thread with yourself -----------
     The six confessions were cards, then a mosaic, and both read as a
     COMPONENT -- a set of items presented to him.

     They're a conversation. The honest self messages him at 2am; the
     polished self has an answer for every one of them.

     LEFT  = incoming. The man behind the glass. Unbidden.
     RIGHT = him. His own excuses. Marked 'delivered'. He sent those.
     The last message gets NO REPLY -- an empty dashed box where his
     answer should be. That silence is the whole section.

     Layout: thread left, sticky column right. A 720px thread centred in
     a 1240px page wastes both flanks and reads as endless. Now the man
     who's sending the messages sits beside them and never leaves. ---- */
  /* THE PHONE.
     v9 made the thread shorter but left it 640px wide -- a letterbox, not a
     phone. It has to hold the DEVICE ratio: narrow and tall (~9:19).
     So the left column shrinks to hold a phone, and the right column takes
     the width it gives back. */
  /* The phone is a fixed 9:19.4 slab -- about 730px tall. The text beside it
     is shorter. align-items:start left a dead 200px hole under the copy.
     CENTRE the text against the phone: the leftover height splits into two
     even margins instead of pooling at the bottom as a gap. */
  #px-mirror-grid{display:grid;grid-template-columns:.82fr 1.18fr;gap:clamp(30px,4.5vw,72px);
    align-items:stretch;max-width:1180px;margin:0 auto}
  /* The column runs the FULL height of the phone and distributes into it:
     copy at the top, the dark box sitting underneath it, bottom edges level
     with the screen. align-items was centre, which left the box floating and
     the two columns ending at different heights. */
  #px-sticky{position:relative;display:flex;flex-direction:column}
  #px-mirror-grid > .px-fade:first-child{display:flex;align-items:flex-start}
  #px-sticky > .px-darkbox{margin-top:auto}

  .px-phone{width:min(360px,100%);aspect-ratio:9/19.4;margin:0 auto;position:relative;
    background:#1A1A1E;border-radius:42px;padding:11px;
    box-shadow:0 40px 90px -40px rgba(30,42,61,.55), 0 0 0 1px rgba(160,138,94,.22)}
  .px-screen{position:relative;height:100%;background:#FBF8F2;border-radius:32px;overflow:hidden;
    display:flex;flex-direction:column}
  .px-notch{position:absolute;top:9px;left:50%;transform:translateX(-50%);
    width:82px;height:6px;border-radius:99px;background:#2B2B31;z-index:4}

  .px-thread-hd{display:flex;align-items:center;gap:9px;padding:22px 16px 11px;
    border-bottom:1px solid rgba(160,138,94,.2);background:#FBF8F2;position:relative;z-index:3;flex:0 0 auto}
  .px-av{width:29px;height:29px;border-radius:50%;background:#12233F;color:#E8C65F;
    display:flex;align-items:center;justify-content:center;font-weight:800;font-size:10px;flex:0 0 auto}

  /* NO INTERNAL SCROLL. The whole conversation fits on one screen.
     v10 translated a tape inside a masked window and you had to chase the
     top message. Nobody should have to work to read the thing that's meant
     to describe their life. Smaller type, one screen, done. */
  /* TOP DOWN. It reads as a conversation being written rather than one you
     have scrolled back through, and the eye starts where it naturally starts.
     Still stable: appending at the bottom moves nothing above it, which was
     the whole reason for leaving justify-content:CENTER behind. */
  #px-window{position:relative;flex:1;padding:14px 13px 0;display:flex;flex-direction:column;
    justify-content:flex-start;overflow-y:auto;overscroll-behavior:contain;
    scrollbar-width:none;-ms-overflow-style:none}
  #px-window::-webkit-scrollbar{display:none}
  /* Bottom gap lives on the tape, not the window. padding-bottom on an
     overflowing flex container is dropped by most engines. */
  #px-tape{padding:0 0 22px}

  /* IT TYPES ITSELF, LIVE. The fog-wipe is gone.
     A fogged bubble that clears is a REVEAL -- it says "here is a thing that
     was already written, now legible." That reads as a component, and worse,
     as him texting himself.
     Typing reads as a thought ARRIVING. Nothing exists until it surfaces.

     And the two sides are deliberately timed differently, which is the whole
     characterisation:
       the THOUGHT takes ~700ms of typing dots to form. It's effortful.
       the ANSWER fires back in ~180ms with no dots at all. Reflexive. He's
       said it a thousand times and it needs no thinking.
     He never has to work that hard to talk himself out of it. */
  .px-in,.px-out{display:none}
  .px-in.shown,.px-out.shown{display:flex;opacity:0;transform:translateY(7px);filter:blur(5px);
    transition:opacity .46s ease, transform .52s cubic-bezier(.22,.61,.36,1), filter .46s ease}
  /* HIS BELIEF is reflexive, so it lands fast and shallow. */
  .px-out.shown{transform:translateY(4px);filter:blur(3px);
    transition:opacity .34s ease, transform .38s ease, filter .34s ease}
  /* THE REPLY SURFACES. Slow, out of focus, rising further. It is the voice
     that already knows the answer, so it should arrive like something coming
     into view rather than something interrupting. */
  .px-in.shown{transform:translateY(6px);filter:blur(3px);
    transition:opacity .34s ease, transform .38s ease, filter .34s ease}
  .px-in.on,.px-out.on{opacity:1;transform:none;filter:blur(0)}
  @media(prefers-reduced-motion:reduce){
    .px-in.shown,.px-out.shown{transform:none;filter:none;transition:opacity .3s ease}
  }

  /* INCOMING -- the thought. */
  .px-in{justify-content:flex-start;margin-bottom:5px}   /* no display here -- .shown owns it */
  .px-in > div{max-width:90%;position:relative;border-radius:15px 15px 15px 4px}
  .px-bub{background:#25344A;color:#EDE9E0;padding:9px 13px;font-size:11px;line-height:1.45;
    border-radius:15px 15px 15px 4px}
  .px-in.last > div{border:1px solid rgba(232,198,95,.6);box-shadow:0 0 0 3px rgba(232,198,95,.1)}
  /* The caret is a pseudo-element, so setting textContent on the bubble does
     not disturb it. steps(1,end) gives a hard blink rather than a pulse. */
  .px-bub.typing::after{content:'';display:inline-block;width:1.5px;height:.92em;
    margin-left:2px;vertical-align:-1px;background:#E8C65F;
    animation:px-caret .68s steps(1,end) infinite}
  @keyframes px-caret{0%,50%{opacity:1}50.01%,100%{opacity:0}}
  @media(prefers-reduced-motion:reduce){ .px-bub.typing::after{display:none} }

  /* THE TYPING INDICATOR. One node, moved down the thread and re-skinned as
     it goes, so there is only ever one and it can't be left behind. */
  /* THE DOTS NEVER MOVE. v1 re-inserted this node before each message, which
     is a DOM move plus a reflow on every beat. It just lives at the end of the
     thread -- which, bottom-anchored, is exactly where the next message lands. */
  /* FIXED HEIGHT, opacity only. Animating the height bounced the entire
     bottom-anchored stack up and down on every single beat. */
  #px-typing{display:flex;margin-bottom:5px;height:34px;opacity:0;pointer-events:none;
    transition:opacity .22s ease}
  #px-typing.on{opacity:1}
  #px-typing.gone{display:none}
  #px-typing .d{background:#1E2A3D;padding:11px 13px;border-radius:15px 15px 15px 4px;
    display:flex;gap:4px;align-items:center}
  #px-typing i{width:5px;height:5px;border-radius:50%;background:#8A9AB5;display:block;
    animation:px-dot 1.05s infinite ease-in-out}
  #px-typing i:nth-child(2){animation-delay:.16s}
  #px-typing i:nth-child(3){animation-delay:.32s}
  @keyframes px-dot{0%,60%,100%{opacity:.28;transform:translateY(0)}
    30%{opacity:1;transform:translateY(-3px)}}

  /* OUTGOING -- his excuse. a brush-off. */
  .px-out{justify-content:flex-end;margin-bottom:12px}   /* no display here -- .shown owns it */
  .px-out > div{max-width:80%}
  .px-out .b{background:#EDE7DC;color:#807869;padding:8px 12px;border-radius:15px 15px 4px 15px;
    font-size:11px;line-height:1.45}
  .px-meta{text-align:right;font-size:8px;letter-spacing:.1em;text-transform:uppercase;
    font-weight:700;color:#C6BBAA;margin:3px 3px 0 0}

  /* THE SILENCE. */
  .px-none{margin-bottom:0}
  .px-none > div{width:142px}
  .px-none .b{border:1px dashed rgba(255,80,31,.45);border-radius:15px 15px 4px 15px;height:32px;
    display:flex;align-items:center;justify-content:center;background:rgba(255,80,31,.03);
    font-size:8.5px;letter-spacing:.14em;text-transform:uppercase;font-weight:700;color:#FF501F}

  @media(max-width:860px){
    #px-mirror-grid{grid-template-columns:1fr}
    #px-sticky{order:-1;margin-bottom:30px}
    .px-phone{width:min(320px,90%)}
  }

  /* -- 03 REFRAME : the divider IS the section ------------------
     v4 made it a card in a box -- a demo sitting in its own module.
     Now the glass is the section background and the copy lives INSIDE
     the two sides. Drag the handle and the argument moves with it. -- */
  #px-reframe{touch-action:pan-y;position:relative;min-height:46svh;display:flex;align-items:center;overflow:hidden;background:#0A1D3C;cursor:ew-resize;user-select:none}
  #px-split{position:absolute;inset:0;width:100%;height:100%;display:block}
  #px-rf-veil{position:absolute;inset:0;pointer-events:none;background:linear-gradient(0deg,rgba(6,25,56,.94) 0%,rgba(6,25,56,.62) 45%,rgba(6,25,56,.72) 100%)}
  #px-handle{position:absolute;top:0;bottom:0;width:2px;background:#FF501F;box-shadow:0 0 30px rgba(255,80,31,.8);pointer-events:none;z-index:3}
  #px-handle::after{content:'';position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);width:54px;height:54px;border-radius:50%;background:#FF501F;box-shadow:0 8px 30px rgba(255,80,31,.55)}
  #px-handle::before{content:'';position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);width:20px;height:10px;z-index:1;
    background:linear-gradient(90deg,#fff 0 4px,transparent 4px 8px,#fff 8px 12px,transparent 12px 16px,#fff 16px 20px);opacity:.9}
  /* Both sides pulled in toward the middle. They were hard against the outer
   edges of a 3:1 band, which put the two headlines about as far apart as
   they can physically be and made the eye travel the whole width twice. */
  #px-rf-copy{position:relative;z-index:2;width:100%;padding:0 clamp(28px,8vw,140px);pointer-events:none}
  .px-rf-side{max-width:34ch}
  .px-rf-tag{display:inline-block;padding:7px 14px;border-radius:999px;font-size:.64rem;letter-spacing:.16em;text-transform:uppercase;font-weight:800;margin-bottom:16px}

  @media(max-width:700px){
    /* The grid is align-items:CENTER, so each column centres on its own
       height. The two sides carry different amounts of text, and once the
       columns get narrow enough to wrap unevenly the two tags stop sitting
       on the same line. Top-align them and they read as a pair again, which
       is the whole point of a before and after. */
    #px-rf-grid{align-items:start;gap:14px}
    .px-rf-side{max-width:none}
    .px-rf-tag{font-size:.55rem;letter-spacing:.09em;padding:6px 10px;margin-bottom:10px}
    #px-rf-copy{padding:0 18px}
  }
  @media(max-width:560px){
    /* Six names plus five separators ran to three lines at .85rem. At .72
       with a tighter gap the run is about 600px against 700px of space over
       two lines, so it holds two with room rather than only just. */
    #px-creds{font-size:.72rem!important;gap:9px!important}
    #px-stats{font-size:.78rem!important;gap:13px!important;margin-top:18px!important}
  }

  /* -- 04 THE FIVE PANES ----------------------------------------
     v7 gave each archetype its own bespoke CSS "film" -- frost for the
     Fortress, rain-runs for the Defender, glare for the Idealist. Clever,
     and nobody could read it. Texture cannot carry a personality. It was
     decoration wearing the costume of meaning, and the cards ended up 80%
     empty gradient and 20% words -- when the WORDS were the section.

     So: one consistent film on all five. They are all glass; "five kinds
     of glass" is a distinction the reader has no key for. And behind each
     pane, a FACE -- the first ten seconds of that archetype's video.

     Hover clears ONE pane. Five dirty windows in a row, and as he moves
     across, one at a time comes alive and looks back at him. That is the
     whole page compressed into a card.

     It does not spend the quiz. The clip gives him APPETITE; the quiz
     tells him WHICH ONE IS HIS. Different products.
     ------------------------------------------------------------------ */
  #px-panes{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;max-width:1240px;margin:0 auto}
  .px-pane{position:relative;display:flex;flex-direction:column;justify-content:flex-end;
    aspect-ratio:4/5;                       /* a FACE wants portrait. square crops the shoulders off. */
    padding:20px 18px;border-radius:14px;cursor:pointer;overflow:hidden;
    background:#0A1D3C;border:1px solid rgba(232,198,95,.14);
    transition:border-color .35s ease,transform .35s ease}

  /* THE VIDEO. preload=none -- nothing loads until he reaches for it.
     Five autoplaying clips on a page with four WebGL canvases would be
     brutal, and 90% of visitors never touch a single one. */
  .px-pane video, .px-pane .poster{position:absolute;inset:0;width:100%;height:100%;
    object-fit:cover;pointer-events:none}
  /* AT REST THE CLIP IS VISIBLE, BUT DRAINED.
     The card used to be a flat navy tile: the film had nothing to be film
     OVER, so it read as decoration rather than obstruction. Now the frame
     sits underneath at low saturation and low brightness, supplying FORM
     only -- a shape that reads as a man. Colour still belongs to the
     archetype film, which is what makes the five cards distinguishable at
     a glance. On play both resolve to full. */
  .px-pane video{opacity:1;filter:saturate(.16) brightness(.5) contrast(.92);
    transition:opacity .9s ease, filter .9s ease}
  .px-pane.on video{opacity:1;filter:saturate(1) brightness(1) contrast(1)}
  .px-pane .poster{background-size:cover;background-position:center 22%;
    filter:saturate(.16) brightness(.5) contrast(.92);transition:filter .9s ease}
  .px-pane.on .poster{filter:saturate(1) brightness(1) contrast(1)}

  /* ═══ THE FILMS ═══════════════════════════════════════════════════
     Each archetype is a DIFFERENT PHYSICAL THING covering the glass, and
     each one comes OFF in the way that thing would come off. The film is
     no longer being asked to communicate the archetype on its own -- the
     face and the words do that -- so it's free to be flavour, and
     flavour can be as literal as it likes.

       01 FORTRESS  bricks     ->  they fall away, bottom row first
       02 DEFENDER  graph grid ->  it retracts and burns off
       03 IDEALIST  golden bloom -> the fantasy collapses to a point
       04 PERFORMER stage curtain -> it parts, and the show is over
       05 ROMANTIC  dust sheet ->  it slides off, like uncovering furniture

     Every one of them is CSS transforms on a handful of divs. No GPU,
     no images, no libraries. ═══════════════════════════════════════ */
  /* ═══ PALETTE DISCIPLINE FOR THE FILMS ════════════════════════════
     The films are THE DIRT. They are neither Daniel nor the reader, so
     they have NO claim on gold or coral.

     v8 broke this twice: a brick-red stage curtain (a SECOND red on a
     page where coral is the one red and it means "press me" -- a full
     height red curtain quietly devalues the CTA sitting under it), and a
     muddy brown in the bloom that isn't in the system at all. Meanwhile
     three of the five were the same blue-grey, which is the "row of blue
     boxes" problem all over again.

     So the five are told apart by MATERIAL and VALUE, not by hue. Every
     one lives in navy / slate / mist / cream, with gold used only where
     it is literally LIGHT.

       01  deep navy masonry      DARK    heavy
       02  mist rules on slate    MID     precise
       03  cream-gold glare       BRIGHT  blown out
       04  navy velvet + gold     DARK    theatrical
       05  pale cream sheet       PALE    silent

     dark -> mid -> bright -> dark -> pale. A real rhythm across the row,
     and coral is left alone to mean exactly one thing. ═══════════════ */
  /* Not quite opaque. The cover stays a cover -- it still hides him -- but
     there is now something behind it that you can just make out. */
  .px-film{position:absolute;inset:0;z-index:1;pointer-events:none;overflow:hidden;
    opacity:.87;transition:opacity .5s ease}

  /* 01 — BRICKS. A fortress is a wall, so give him a wall. The heaviest,
     darkest thing in the row: this one should feel like WEIGHT.
     Falls bottom-up -- the base goes first and the rest comes down after it. */
  .px-brick{position:absolute;border-radius:1px;
    background:linear-gradient(150deg,rgba(52,74,108,.97),rgba(22,42,74,.96));
    box-shadow:inset 0 1px 0 rgba(177,191,215,.20), inset 0 -2px 0 rgba(3,10,24,.55);
    transition:transform 1.05s cubic-bezier(.45,0,.7,.2), opacity .85s ease}
  .px-pane.on .px-brick{transform:translateY(150%) rotate(var(--r)) ;opacity:0}

  /* 02 — GRAPH PAPER. Everything filed, ruled, catalogued. You get the
     schema of how he feels. Cold, exact, clinical. Retracts and burns off. */
  .px-grid{position:absolute;inset:-8%;
    background:
      repeating-linear-gradient(0deg,rgba(177,191,215,.40) 0 1px,transparent 1px 13px),
      repeating-linear-gradient(90deg,rgba(177,191,215,.40) 0 1px,transparent 1px 13px),
      linear-gradient(160deg,rgba(86,112,152,.80),rgba(46,72,112,.86));
    transition:transform 1.1s cubic-bezier(.2,.8,.2,1), opacity .9s ease}
  .px-pane.on .px-grid{transform:scale(1.35);opacity:0}

  /* 03 — THE GLARE. Blown out by an idealised light -- and gold IS this
     site's light, so this is the one film that gets to be warm. Cream at
     the core, gold falling off, and it lands on NAVY at the edge. No brown.
     On hover the fantasy collapses to a point. */
  .px-bloom{position:absolute;inset:0;
    background:radial-gradient(circle at 56% 34%,rgba(248,242,228,.97),rgba(232,198,95,.72) 38%,rgba(20,44,80,.80) 82%);
    transition:transform 1.15s cubic-bezier(.5,0,.2,1), opacity .95s ease;transform-origin:56% 34%}
  .px-pane.on .px-bloom{transform:scale(.06);opacity:0}

  /* 04 — THE CURTAIN. He's been on stage his whole life.
     NAVY VELVET, not red. A stage curtain doesn't have to be scarlet, and a
     dark velvet one looks more expensive anyway. The gold does the SPOTLIGHT
     work on the ribs -- which is what gold is for -- and coral is left alone
     to mean "press me". */
  .px-curtain{position:absolute;top:0;bottom:0;width:50.6%;
    transition:transform 1.2s cubic-bezier(.55,0,.2,1)}
  .px-curtain.l{left:0;background:linear-gradient(90deg,rgba(10,26,54,.98),rgba(34,60,100,.94) 58%,rgba(8,22,48,.98));
    box-shadow:inset -12px 0 26px -8px rgba(2,8,20,.9)}
  .px-curtain.r{right:0;background:linear-gradient(270deg,rgba(10,26,54,.98),rgba(34,60,100,.94) 58%,rgba(8,22,48,.98));
    box-shadow:inset 12px 0 26px -8px rgba(2,8,20,.9)}
  .px-curtain::after{content:'';position:absolute;inset:0;      /* velvet ribs, catching a gold light */
    background:repeating-linear-gradient(90deg,rgba(2,8,20,.34) 0 2px,transparent 2px 10px,rgba(232,198,95,.14) 10px 13px)}
  .px-pane.on .px-curtain.l{transform:translateX(-101%)}
  .px-pane.on .px-curtain.r{transform:translateX(101%)}

  /* 05 — THE DUST SHEET. A life under wraps. The palest thing in the row,
     and the quietest -- cream going to mist. Nothing happening at all. */
  .px-sheet{position:absolute;inset:0;
    background:linear-gradient(178deg,rgba(241,236,225,.94),rgba(207,209,214,.90) 52%,rgba(177,191,215,.92));
    box-shadow:inset 0 12px 26px -12px rgba(255,255,255,.6), inset 0 -20px 30px -22px rgba(30,42,61,.35);
    transition:transform 1.25s cubic-bezier(.35,0,.2,1)}
  .px-sheet::after{content:'';position:absolute;inset:0;        /* the weave */
    background:repeating-linear-gradient(96deg,rgba(255,255,255,.16) 0 3px,transparent 3px 9px)}
  .px-pane.on .px-sheet{transform:translateY(101%)}

  /* the smear of glass that sits over every film, and the scrim that keeps
     the words readable. */
  .px-pane::after{content:'';position:absolute;inset:0;pointer-events:none;z-index:2;
    background:repeating-linear-gradient(101deg,rgba(255,255,255,.05) 0 1px,transparent 1px 6px),
               linear-gradient(0deg,rgba(4,18,42,.94) 0%,rgba(4,18,42,.52) 32%,transparent 60%);
    transition:opacity .9s ease}
  /* SCRIM OFF WHEN IT PLAYS. The clip has its own burnt-in captions along
     the bottom -- a navy gradient sitting on top of them is just dirt on a
     subtitle. */
  .px-pane.on::after{opacity:.14}
  .px-pane.on{border-color:rgba(255,80,31,.6);box-shadow:0 30px 60px -30px rgba(3,12,28,.95), 0 0 0 1px rgba(255,80,31,.25)}

  /* THE WORDS.
     Number + NAME live at the TOP. They stay put, always, so he never loses
     track of which pane he's looking at.
     The DESCRIPTION lives at the bottom -- and gets out of the way the
     instant the clip runs, because that's where the captions are. */
  /* DO NOT put a blanket '.px-pane > *{position:relative}' here.
     It has the same specificity as '.px-film' (one class each) and sits
     later in the sheet, so it WINS -- and quietly turns the film container
     into a zero-height static div. Every brick, curtain and dust sheet then
     lays itself out inside nothing and renders as nothing. No error, no
     warning, just five empty cards. Target the text directly instead. */
  .px-pane > .n{position:absolute;top:18px;left:18px;z-index:3;
    font-family:'Poppins','Montserrat',sans-serif;font-size:1.9rem;
    color:rgba(232,198,95,.6);line-height:1}
  .px-pane > h3, .px-pane > p{position:relative;z-index:3}
  /* THE WHOLE BOTTOM BLOCK CLEARS OUT WHEN THE CLIP RUNS.
     Title and description both. The clip has its own captions, and the
     captions say who is talking -- so the title is redundant the moment
     his face is on screen, and redundant text over a video is just dirt
     on a subtitle.

     opacity ONLY -- no display, no height change, no transform. The block
     keeps its box, so nothing under it reflows and the card doesn't twitch.
     The number stays in the corner as the one persistent marker of which
     pane he's on. */
  .px-pane h3{font-size:.95rem;font-weight:800;color:#F1ECE1;line-height:1.3;margin:0 0 7px;
    text-shadow:0 1px 12px rgba(4,18,42,.9);transition:opacity .45s ease}
  .px-pane p{font-size:.84rem;line-height:1.5;color:#C4CEE0;margin:0;
    transition:opacity .45s ease}
  .px-pane.on h3, .px-pane.on p{opacity:0;pointer-events:none}

  /* the cue. only on the panes that actually have a clip. */
  /* A PLAY BUTTON, not the word WATCH. Tiny uppercase gold type is a caption;
     a circle with a triangle in it is a thing you press. */
  .px-pane .cue{position:absolute;top:14px;right:14px;z-index:4;
    width:30px;height:30px;border-radius:999px;font-size:0;
    background:rgba(4,18,42,.5);border:1px solid rgba(241,236,225,.28);
    backdrop-filter:blur(6px);
    transition:opacity .35s ease,background .35s ease,border-color .35s ease,transform .35s ease}
  .px-pane .cue::after{content:'';position:absolute;top:50%;left:54%;transform:translate(-50%,-50%);
    border-style:solid;border-width:5px 0 5px 8px;border-color:transparent transparent transparent #F1ECE1}
  .px-pane:hover .cue{background:#FF501F;border-color:#FF501F;transform:scale(1.12)}
  .px-pane.on .cue{opacity:0}

  /* FOCUS. Hovering one card dims the other four, so the row stops being a
     grid of equals and becomes a thing he is choosing between. */
  #px-panes:hover .px-pane:not(:hover){opacity:.45;filter:saturate(.7)}
  .px-pane{transition:border-color .35s ease,transform .35s ease,opacity .35s ease,filter .35s ease,box-shadow .35s ease}
  .px-pane:hover{transform:translateY(-8px);box-shadow:0 30px 60px -30px rgba(3,12,28,.9)}
  @media(prefers-reduced-motion:reduce){
    .px-brick,.px-grid,.px-bloom,.px-curtain,.px-sheet{transition:opacity .3s ease !important}
    .px-pane.on .px-brick,.px-pane.on .px-grid,.px-pane.on .px-bloom,
    .px-pane.on .px-curtain,.px-pane.on .px-sheet{transform:none !important;opacity:0 !important}
  }

  @media(max-width:980px){#px-panes{grid-template-columns:repeat(2,1fr)}}
  /* 360px cap is load-bearing: it puts the 720px-wide clip at exactly 2x. */
  /* MOBILE: A SWIPE RAIL, NOT A STACK.
     Five 4:5 cards stacked one per row is roughly 2,250px of scrolling to
     get past a section whose only job is "which of these is you". The rail
     puts the question on one screen. Cards sit at 72% so the next one is
     visibly cut off at the edge -- that peek is what tells him to swipe;
     a rail whose last card ends flush at the viewport edge reads as a
     single card and nobody ever swipes it.
     scroll-padding-inline matches the rail padding so a snapped card lands
     against the same margin as the text above it rather than the screen. */
  @media(max-width:560px){
    #px-panes{grid-template-columns:none;grid-auto-flow:column;grid-auto-columns:72%;
      gap:12px;overflow-x:auto;overscroll-behavior-x:contain;
      scroll-snap-type:x mandatory;scroll-padding-inline:20px;
      padding:2px 20px 14px;margin-inline:-20px;
      scrollbar-width:none;-ms-overflow-style:none}
    #px-panes::-webkit-scrollbar{display:none}
    .px-pane{width:auto;max-width:none;scroll-snap-align:start}
    .px-pane h3{font-size:.9rem}
    .px-pane p{font-size:.8rem}
    /* The dots are a readout, not a control. Tapping a dot to move a rail
       is a worse target than the rail itself, which is already swipeable. */
    #px-panes-dots{display:flex;align-items:center;justify-content:center;gap:7px;margin-top:11px}
    /* The peek tells him there is more; the arrow tells him what to DO about
       it. It retires the moment he moves the rail, because an affordance
       that outlives its own lesson is just clutter. */
    #px-panes-nudge{display:block;margin-left:7px;color:#E8C65F;font-size:16px;line-height:1;
      opacity:.95;transition:opacity .35s ease;animation:px-nudge 1.5s ease-in-out infinite}
    #px-panes-dots.moved #px-panes-nudge{opacity:0;animation:none}
    @keyframes px-nudge{0%,100%{transform:translateX(0)}50%{transform:translateX(6px)}}
    @media(prefers-reduced-motion:reduce){ #px-panes-nudge{animation:none} }
    #px-panes-dots i{width:6px;height:6px;border-radius:50%;background:rgba(241,236,225,.26);
      transition:background .25s ease,transform .25s ease}
    #px-panes-dots i.on{background:#E8C65F;transform:scale(1.25)}
  }
  #px-panes-dots{display:none}
  #px-panes-nudge{display:none}

  /* THE PORTRAIT TRAVELS WITH THE STORY. It is 4:5 against 285 words, so the
     left column used to finish a third of the way down and leave a column of
     dead cream. Sticky beats two columns here: a personal story set in two
     columns reads like a newspaper, and this one has to feel like being
     talked to. */
  #px-daniel > div:first-child{position:sticky;top:96px}
  @media(max-width:900px){#px-daniel > div:first-child{position:static}}

  /* ── 05 SYSTEM : three panes ──────────────────────────────── */
  /* SQUARE, not 4:3. The reveal behind this glass is a vertical photograph of
     two people, and forcing a portrait frame into a landscape stage is the
     exact mistake that gave the divider a band of teeth. Square holds the
     POSTURE -- her leaning her weight on him, her arm round his neck, her
     hand on his chest -- which is the whole argument. A landscape crop keeps
     the faces and throws the reaching away.

     BUT SQUARE IS TALL, and a square stage in a 1.12fr column on a wide
     screen becomes an enormous box -- the section stopped fitting on a 14"
     laptop, which is what most people are actually reading this on. So the
     stage is CAPPED and centred in its column. It stays square; it just
     stops trying to be 600px tall. The whole section has to be takeable in
     one screen, or the three clicks never happen. */
  #px-vv-stage{position:relative;aspect-ratio:1/1;border-radius:18px;overflow:hidden;background:#04122A;
    width:100%}
  #px-vv-c{position:absolute;inset:0;width:100%;height:100%;display:block}
  /* THE SCRIM RUNS TO THE EDGE. It has to -- it's a gradient, and a gradient
     that stops short of the frame leaves a hard seam with a strip of photo
     below it.

     The previous version lifted the whole overlay to bottom:6% and tried to
     patch the gap with an ::after. That failed for a dull reason: a
     percentage height on the pseudo-element resolves against the OVERLAY's
     height, not the stage's, so the patch came out the wrong size and you
     got the strip anyway.

     Lift the TEXT, not the BOX. Bottom padding does it, and the gradient
     still reaches the corner. */
  #px-vv-overlay{position:absolute;left:0;right:0;bottom:0;padding:30px 26px 44px;pointer-events:none;
    background:linear-gradient(0deg,rgba(4,18,42,.95) 30%,rgba(4,18,42,.72) 62%,rgba(4,18,42,0))}
  /* align-items:center + a shared min-height so the three read as one row
     even when a label wraps. letter-spacing on uppercase adds a trailing gap
     on the last glyph, which throws optical centring -- the left padding
     compensates so the text sits centred to the eye, not to the box. */
  .px-pill{flex:1;min-height:46px;display:flex;align-items:center;justify-content:center;text-align:center;
    padding:11px 7px 11px 8px;border-radius:12px;font-weight:800;font-size:.72rem;letter-spacing:.08em;
    text-transform:uppercase;cursor:pointer;transition:all .25s ease;border-width:2px;border-style:solid}
  @media(max-width:560px){ .px-pill{font-size:.66rem;letter-spacing:.05em;min-height:44px;padding:10px 5px} }
  .px-card{background:#fff;border-radius:20px;padding:28px 30px;border-top:3px solid #E8C65F;transition:border-color .3s ease}

  /* Row 1 = stage | card.  Row 2 = pills+caption | (nothing).
     So row 1's height IS the image's height, and the card centres against the
     PICTURE rather than against picture-plus-buttons. And it hugs its own
     content -- no cathedral of white space around three lines of text. */
  #px-vv-grid{display:grid;grid-template-columns:minmax(0,470px) minmax(0,1fr);
    grid-template-rows:auto auto;column-gap:clamp(24px,3vw,48px);row-gap:14px;
    max-width:1080px;margin:0 auto}
  #px-vv-card{grid-column:2;grid-row:1;align-self:center}
  #px-vv-controls{grid-column:1;grid-row:2;padding-top:6px}
  @media(max-width:820px){
    #px-vv-grid{grid-template-columns:1fr}
    #px-vv-stage{grid-column:1;grid-row:1}
    #px-vv-controls{grid-column:1;grid-row:2;padding-top:16px}
    #px-vv-card{grid-column:1;grid-row:3;align-self:auto;margin-top:8px}
  }
  /* THE FORK. Home carries the shared truth. The brutal, gendered pain
     lives behind these two doors, where it can hit as hard as it likes. */
  .px-doors{display:grid;grid-template-columns:1fr 1fr;gap:16px;max-width:760px;margin:26px auto 0}
  @media(max-width:680px){.px-doors{grid-template-columns:1fr}}
  .px-door{display:block;text-align:left;background:rgba(241,236,225,.04);border:1px solid rgba(241,236,225,.12);
    border-radius:18px;padding:26px 28px;text-decoration:none;transition:border-color .3s ease,background .3s ease,transform .3s ease}
  .px-door:hover{border-color:rgba(255,80,31,.55);background:rgba(255,80,31,.07);transform:translateY(-3px)}
  .px-door .eyebrow{font-size:.64rem;letter-spacing:.2em;text-transform:uppercase;font-weight:800;color:#E8C65F;margin-bottom:10px}
  .px-door h4{font-family:'Poppins','Montserrat',sans-serif;font-weight:300;font-size:clamp(21px,2.2vw,29px);color:#F1ECE1;margin-bottom:10px;line-height:1.2}
  .px-door p{font-size:.9rem;line-height:1.7;color:#8E9BB2;margin-bottom:14px}
  .px-door span.go{font-size:.82rem;font-weight:700;color:#FF6A3D}

  /* THE DOORS MOVED TO CREAM, so they need a cream skin. The navy versions
     above (translucent white on dark, gold eyebrow, cream heading) go
     invisible on #F1ECE1 -- 4% white on cream is nothing, and #F1ECE1 text
     on #F1ECE1 is literally nothing.
     Gold also has to go: it has no contrast on cream. Bronze #A08A5E is the
     cream-side equivalent, same voice, legible. */
  .px-door.on-cream{background:#FBF8F2;border:1px solid #E2DACB;
    box-shadow:0 18px 40px -30px rgba(30,42,61,.45)}
  .px-door.on-cream:hover{border-color:rgba(255,80,31,.5);background:#fff;
    box-shadow:0 24px 50px -28px rgba(30,42,61,.5)}
  .px-door.on-cream .eyebrow{color:#A08A5E}
  .px-door.on-cream h4{color:#1E2A3D}
  .px-door.on-cream p{color:#5E5850}
  .px-door.on-cream span.go{color:#FF501F}
  /* ── 07 PROOF : real faces, real names ────────────────────────────
     These were flattened into PNGs on the live Testimonials page -- photo,
     quote and name baked into one image. Which means Google reads NONE of it,
     a screen reader gets nothing, and it cannot reflow on a phone. Every one
     of these quotes was invisible to search. Now it's text, and the faces are
     cropped out of the source files with Wix's own crop transform -- so no
     re-upload, no reshoot, and the words are indexable. */
  /* SMALL FACES. This is the whole design rule for the section:
     THE WORDS ARE THE PROOF. THE FACE IS ONLY THE VERIFICATION.

     v1 ran these as 380px squares and it backfired -- Tobias's is a corporate
     headshot on a grey studio backdrop, Gena's is a moody photoshoot profile
     where she isn't even looking at the lens. Fine photographs; blown up they
     stop reading as CLIENTS and start reading as IMAGERY. And imagery invites
     the one question you never want asked here: are these people real?

     A face needs to be big enough to say "a human being said this" and not one
     pixel bigger. The moment it's large enough to admire as a photograph it is
     competing with the sentence it exists to authenticate. */
  .px-face{width:64px;height:64px;flex:0 0 auto;object-fit:cover;border-radius:999px;display:block;
    filter:saturate(.9) contrast(1.02);
    box-shadow:0 0 0 1px rgba(232,198,95,.30), 0 8px 18px -10px rgba(3,12,28,.9)}
  .px-face.lg{width:78px;height:78px}
  .px-byline{display:flex;align-items:center;gap:14px}
  .px-byline .who{font-size:.9rem;font-weight:700;color:#B1BFD7;line-height:1.45}
  .px-byline .who span{display:block;font-size:.76rem;font-weight:500;color:#7C89A3;margin-top:2px}

  /* ── VIDEO FACADE ─────────────────────────────────────────────────
     A YouTube <iframe> drags in ~1MB of JS and several third-party requests
     BEFORE anybody presses anything. Five of them on a page that already runs
     four WebGL canvases would be indefensible -- and most visitors play none.

     So these are just thumbnails. The real iframe is created on CLICK, with
     autoplay, and it replaces the button in place. Zero cost until asked. */
  .px-vid{position:relative;display:block;width:100%;padding:0;border:0;cursor:pointer;
    background:#04122A;border-radius:16px;overflow:hidden;aspect-ratio:16/9;
    box-shadow:0 26px 60px -34px rgba(3,12,28,.95), 0 0 0 1px rgba(232,198,95,.14);
    transition:transform .3s ease, box-shadow .3s ease}
  .px-vid:hover{transform:translateY(-3px);box-shadow:0 30px 70px -32px rgba(3,12,28,1), 0 0 0 1px rgba(232,198,95,.34)}
  .px-vid > img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
    filter:saturate(.92) brightness(.86);transition:filter .3s ease}
  .px-vid:hover > img{filter:saturate(1) brightness(1)}
  /* THE PLAY BUTTON IS NOT IN THE MIDDLE.
     It's a talking head, filmed on a webcam, and he is dead centre in frame.
     A 62px centred button lands squarely on his MOUTH -- the one part of a
     testimonial that has to look like it's speaking. Bottom-left instead:
     out of his face, into the quiet corner, and it reads as a control rather
     than a sticker. */
  .px-vid .play{position:absolute;left:20px;bottom:20px;
    width:58px;height:58px;border-radius:999px;background:rgba(255,80,31,.94);
    box-shadow:0 10px 30px -8px rgba(255,80,31,.6);transition:transform .3s ease}
  .px-vid .play::after{content:'';position:absolute;top:50%;left:53%;transform:translate(-50%,-50%);
    border-style:solid;border-width:10px 0 10px 16px;border-color:transparent transparent transparent #fff}
  .px-vid:hover .play{transform:scale(1.09)}
  /* SAY THE RUNTIME. This is a 13-minute interview and pretending otherwise
     just ambushes him. Naming it up front costs a few clicks and improves
     every one that's left -- nobody presses play by accident and bounces. */
  .px-vid .len{position:absolute;right:12px;bottom:12px;padding:5px 10px;border-radius:8px;
    background:rgba(4,18,42,.82);backdrop-filter:blur(6px);
    font-size:.68rem;font-weight:700;letter-spacing:.06em;color:#B1BFD7}
  .px-vid iframe{position:absolute;inset:0;width:100%;height:100%;border:0}

  #px-voices{display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(20px,2.8vw,38px);
    padding-top:32px;border-top:1px solid rgba(241,236,225,.1)}
  .px-voice{margin:0;display:flex;flex-direction:column}
  .px-voice blockquote{margin:0 0 18px;font-family:'Poppins','Montserrat',sans-serif;
    font-style:italic;font-size:clamp(18px,1.6vw,22px);line-height:1.4;color:#F1ECE1;flex:1}
  /* ══════════════ MOBILE TYPOGRAPHY ══════════════
     text-wrap:balance evens the rag on a heading instead of letting the last
     line fall to one orphaned word, which is most of what reads as "broken
     lines" on a phone. pretty does the same for the last line of body copy.
     Both degrade to normal wrapping where unsupported, so there is no
     fallback to write.

     The size override needs !important because every heading carries its
     clamp as an INLINE style, and inline beats a media query at any
     specificity. Scoped to h1/h2/h3.px-serif so the smaller serif pull
     quotes, which are divs, keep their own scale and the hierarchy holds.

     The clamps bottom out at 27-30px. On a 390px screen with 40px of
     padding that is about eleven characters a line, which is what was
     snapping headlines into ragged three-word stacks. */
  h1,h2,h3,h4{text-wrap:balance}
  p{text-wrap:pretty}
  @media(max-width:560px){
    h1.px-serif,h2.px-serif,h3.px-serif{font-size:clamp(21px,6.1vw,27px)!important;line-height:1.24!important}
    section,.px-sec{padding-top:clamp(40px,10vw,52px)!important;padding-bottom:clamp(40px,10vw,52px)!important}
  }
  @media(max-width:820px){#px-voices{grid-template-columns:1fr;gap:28px}
    .px-voice blockquote{flex:none}}

  /* fires only when all three panes are in. he ends on the promise. */
  #px-vv-cta{max-height:0;opacity:0;overflow:hidden;transition:max-height .7s ease,opacity .6s ease,margin .6s ease;margin-top:0}
  #px-vv-cta.show{max-height:240px;opacity:1;margin-top:22px}

  @media(prefers-reduced-motion:reduce){
    .px-line > span{transform:none!important}
    .px-fade{opacity:1!important;transform:none!important}
  }

/* ═══════════════════════════════════════════════════════════════════
   HOME · WOMEN — additions on top of the v4 sheet.
   Nothing here overrides a token. Same navy, same cream, same gold for
   Daniel's voice, same coral for her move.

   THE METAPHOR SWAP. On his page the glass sits between him and the
   people he love. On hers it sits between her and the finish line.
   She can see it. It is lit. And it moves.
   ═══════════════════════════════════════════════════════════════════ */

/* ── NO NUMBERED SECTION LABELS ──────────────────────────────────
   Removed 2 Aug. "01. The mirror", "02. What it has cost", "03. Where
   the bar came from" and the rest were editorial signposts: they
   described the ARGUMENT to whoever was building it, not the page to
   whoever is reading it. A visitor does not navigate a landing page by
   section number, and the numbering had already drifted into two 05s
   without anybody noticing, which is the proof that nobody was reading
   them.

   The handwritten eyebrows stay. Those are in Daniel's voice and they
   orient without indexing. px-label itself stays too: it still does
   real work inside the audit, where "1 of 15", "Before your result"
   and the rules between result blocks are functional rather than
   decorative.

   If they ever come back, they need one numbering pass across the
   whole page rather than per section edits. */

/* ── IMAGE PLACEHOLDER ────────────────────────────────────────────
   Every one of these is a shot that does not exist yet. They are
   deliberately loud. Ship nothing with one of these still in it. */
.px-img-ph{position:relative;display:flex;flex-direction:column;justify-content:flex-end;
  border:2px dashed rgba(232,198,95,.55);border-radius:18px;background:
  repeating-linear-gradient(135deg,rgba(232,198,95,.05) 0 12px,rgba(232,198,95,.10) 12px 24px);
  padding:20px;min-height:220px;overflow:hidden}
.px-img-ph.on-cream{border-color:rgba(160,138,94,.6);background:
  repeating-linear-gradient(135deg,rgba(160,138,94,.06) 0 12px,rgba(160,138,94,.12) 12px 24px)}
.px-img-ph .tag{position:absolute;top:14px;left:16px;font-size:.6rem;letter-spacing:.2em;
  text-transform:uppercase;font-weight:800;color:#E8C65F}
.px-img-ph.on-cream .tag{color:#A08A5E}
.px-img-ph b{display:block;font-family:'Poppins',sans-serif;font-weight:500;font-size:1.02rem;
  color:#F1ECE1;letter-spacing:-.01em;margin-bottom:6px}
.px-img-ph.on-cream b{color:#1F2A3D}
.px-img-ph span{display:block;font-size:.78rem;line-height:1.55;color:#8FA0BC}
.px-img-ph.on-cream span{color:#6E6A66}
.px-img-ph code{font-size:.72rem;color:#FF501F;font-weight:700}

/* ── 01 · HERO ───────────────────────────────────────────────────
   NO FOG, NO CLEARING. --px-clear is pinned at 1, which zeroes the
   inherited blur and restores full saturation, contrast and brightness.
   She sees him properly the moment she lands. The scroll-clear was a
   metaphor that cost legibility and paid nothing back.

   And the section is shorter than a full viewport on purpose. There are
   five elements in here; at 100svh they float in the middle of a very
   tall empty box. */
/* 68svh, not 78. At 78 there was a dead band between the microcopy and
   the scroll cue with nothing in it, and the mirror below stayed fully
   off screen so there was no evidence the page continued. */
#px-hero{height:clamp(500px,68svh,720px);min-height:0}
/* ALIGNMENT. The inherited sheet gives this its own gutter, clamp(20px,
   5vw,90px), which put the H1 at x=72 on a 1440 screen while every
   section below it starts its content at x=100. The first line on the
   page was the one line that did not line up with the page. It now uses
   the section gutter and the same 1240 column, so the headline sits
   directly above the mirror's first word. */
#px-hero-copy{padding:0 clamp(20px,4vw,52px) clamp(50px,9vh,110px)}
#px-hero-copy .inner{width:100%;max-width:1240px;margin:0 auto}
#px-hero-copy .inner > *{max-width:min(720px,58vw)}
#px-fallback{--px-clear:1}
#px-fallback #px-fb-img{background-image:var(--px-hero-img,
    url('https://static.wixstatic.com/media/111174_0c88555d1fd14867a5ee171ca191ca0b~mv2.png'));
  /* Wix-hosted so the custom element has no local dependency. The graded
     master sits in this folder as hero-daniel-through.jpg if it ever needs
     re-uploading. Override --px-hero-img to swap it without touching this. */
  /* 84% 28%, not the sheet's 70% 18%. On desktop the plate is near the
     viewport ratio so x does nothing and y has ~60px of travel. On a phone
     the cover crop shows a 31%-wide slice, and at 84% that slice runs
     58-89%, which puts his face at ~68% across the window instead of
     jammed against the right edge. */
  background-position:74% 15%}
/* SPECIFICITY NOTE. The inherited v4 sheet styles this element as
   "#px-fallback .fb-img", which is (1,1,0). A bare "#px-fb-img" is (1,0,0)
   and loses, silently, so size and position rules here did nothing at all
   until the parent id was added. If you add another rule for this element,
   qualify it the same way. */

/* THE CROP IS DONE HERE, NOT IN THE FILE.
   At cover he is a figure in a room, which makes the hero a SCENE. The copy
   then has to compete with it for what the page is about, and the two do not
   rhyme: her pressure on the left, a settled man on the right.

   At 165% he is chest-up in the right third, which makes him the SPEAKER.
   The copy becomes what he is saying about her and the relationship needs no
   explaining. Same photograph, different job.

   Mobile stays on cover. A fixed percentage is measured against the
   container WIDTH, so 165% on a 390px viewport renders 644x429 inside an
   844-tall box and leaves gaps top and bottom. Cover already crops hard on a
   phone anyway: it shows a 31%-wide slice, so he is large there without help. */
@media(min-width:700px){
  /* 165%, which is about half the distance from cover to the 235% version.
     He reads as a person rather than a figure in a room, without becoming a
     close-up. It is also as far as this plate can be pushed: the file is
     1536px native, so 235% was resolving roughly 3400px of detail out of it
     and going visibly soft.

     NO ZOOM ANY MORE. The 2:1 plate IS the final framing, shot and extended
     to it, so cover displays it close to 1:1 and nothing is being blown up.
     The 165% version was resolving about 931 native pixels across 2880, which
     is where the softness came from.

     x only bites on a narrow viewport, where cover crops to a portrait slice;
     74% keeps his face inside it. y only bites on a very wide short one,
     where 15% keeps his eye line off the top edge. */
  #px-fallback #px-fb-img{background-size:cover;background-position:74% 15%}
}
#px-hero-cue{position:absolute;left:50%;transform:translateX(-50%);bottom:26px;z-index:3;
  color:#E8C65F;font-size:1.4rem;line-height:1;opacity:.55;pointer-events:none;
  animation:pxCue 2.6s ease-in-out infinite}
@keyframes pxCue{0%,100%{transform:translateX(-50%) translateY(0)}50%{transform:translateX(-50%) translateY(6px)}}
@media(prefers-reduced-motion:reduce){#px-hero-cue{animation:none}}

/* ── 02 · THE THREAD ─────────────────────────────────────────────
   The window is scrollable and its scrollbar is hidden by the inherited
   sheet, so without this there is no cue that anything sits above the
   fold. The fade appears only when she has something to scroll back to. */
/* ── 02 · PHONE HEIGHT ───────────────────────────────────────────
   The inherited phone is aspect-ratio 9/19.4, which on desktop runs a
   long way past the foot of the column beside it and leaves a dead
   navy field to its right. Above the collapse breakpoint the aspect
   ratio is dropped and the phone takes the height of the row, which
   the text column sets. The two columns then finish together.

   Nothing is lost by shortening it: .px-screen is a flex column and
   #px-window is flex:1, so the tape simply scrolls in a shorter
   window, and the window is bottom-anchored so the live pair is always
   the one on screen. The min-height stops a short text column from
   collapsing it into a letterbox. */
@media(min-width:1041px){
  #px-mirror-grid{align-items:stretch}
  /* The inherited sheet caps this at 1180 and centres it, which sits it
     30px inside the 1240 column every other section uses. */
  #px-mirror-grid{max-width:none;margin:0}
  /* The phone is taken out of the row's height calculation entirely:
     absolute inside its own column, so the row is sized by the text
     beside it and the phone then fills that height. Left in flow it
     sizes to its own tape, which is twenty messages long, and the row
     grows to fit it. */
  #px-phone-col{position:relative;min-height:520px}
  .px-phone{position:absolute;top:0;bottom:0;left:50%;transform:translateX(-50%);
    aspect-ratio:auto;height:auto;margin:0}
  .px-screen{min-height:0}
  #px-window{min-height:0}
}
#px-window{scroll-behavior:auto;-webkit-overflow-scrolling:touch;touch-action:pan-y}
#px-window:focus-visible{outline:2px solid #FF501F;outline-offset:-2px}
#px-window.has-above{
  -webkit-mask-image:linear-gradient(to bottom,transparent 0,#000 30px);
  mask-image:linear-gradient(to bottom,transparent 0,#000 30px)}

/* ── 03 · WHAT IT HAS ALREADY COST ───────────────────────────────
   THREE THINGS HAVE STOOD HERE AND TWO WERE WRONG. A photographic
   before/after could not carry it, because the difference between the
   two states is OWNERSHIP and ownership is not visible in a room: a
   tidy kitchen with a laptop against a tidy kitchen with two glasses
   reads as work versus wine, which lands as "you need to relax". That
   is the one promise she rejects, because rest is her fear.

   Then a calendar, twice, equally full. It rendered the claim honestly
   and that is exactly why it failed: two grids of identical density
   have no contrast, and a dense week is the thing she already looks at
   all day. Recognition with no relief is fatigue, not insight.

   WHAT THE SECTION IS ACTUALLY FOR. Between the mirror and the
   machine, the page had no cost in it. It never said what this has
   taken. She can dismiss "you're tired", because she isn't, she is
   capable. She can dismiss "you're too busy", because she likes busy.
   She cannot dismiss how long it has been, because each year was
   individually justified and the pattern only exists stacked.

   So: six lines, her own words, dated. First and last are identical.
   Nothing accuses her of anything, every line was true when she said
   it, and the loop closes itself. Then the turn to the pause, which is
   the one piece of new information in the section and the thing the
   audit exists to answer. Do not put a graphic back in here. */
/* The plate is graded so its shadows land on #04122A. No border, no
   radius on the outer edge and no shadow: it has to read as a hole in
   the section rather than a card sitting on it. */
#px-machine-img{display:block;width:100%;height:auto;border-radius:4px}
#px-machine-quote{color:#E8C65F;text-align:center;max-width:min(64ch,1020px);
  font-size:clamp(.98rem,1.72vw,1.72rem);line-height:1.42;
  margin:clamp(38px,5vw,64px) auto 0}
#px-machine-quote span{display:inline-block}
/* Below 640 there is no width at which either sentence fits one line,
   so it stops pretending and simply wraps. */
@media(max-width:640px){#px-machine-quote{max-width:32ch;font-size:1.08rem}
  #px-machine-quote span{display:inline}}
/* THE INHERITED SHEET PUTS white-space:nowrap ON EVERY .px-hand UNDER 560px.
   That is right for the one-line handwritten notes it was written for, and
   wrong here: this is two full sentences, so on a phone it ran 873px wide
   inside a 287px column and was clipped rather than wrapped. Unreadable on
   the device most of this traffic arrives on. */
@media(max-width:560px){#px-machine-quote{white-space:normal}}
/* Above the collapse point the plate fills the column so it finishes
   level with the closing line beside it. Cropped from the top, because
   the wall above his head is the least loaded part of the frame and
   the table edge at the bottom is what grounds it. */
/* Above the collapse point the two columns finish level. The plate is
   NOT cropped to do it: the text column stretches instead and the
   closing block is pushed to the foot with margin-top:auto, so "only
   now there's nobody left moving it but you" sits on the same line as
   the bottom edge of the picture of exactly that. */
.px-mach-close{margin-top:clamp(30px,4vw,46px);padding-top:28px;
  border-top:1px solid rgba(241,236,225,.15)}
@media(min-width:901px){
  #px-machine-cols > div:last-child{display:flex;flex-direction:column}
  .px-mach-close{margin-top:auto}
}
/* THE PLATE SETS THE ROW HEIGHT, so if it is much taller than the copy
   the margin-top:auto that bottom-aligns the closing block turns into a
   crater. Narrower column, shorter plate, and the two columns come out
   close enough that the auto margin is a gap rather than a hole. */
#px-machine-cols{display:grid;grid-template-columns:minmax(0,.40fr) minmax(0,1fr);
  gap:clamp(26px,4vw,54px);align-items:stretch}
@media(max-width:900px){#px-machine-cols{grid-template-columns:1fr}}
/* ── 02 · WHAT IT HAS COST, AND WHAT IT WILL ─────────────────────
   FOUR THINGS HAVE STOOD HERE. A photographic before/after, which
   could not carry ownership. Two calendars, equally full, which
   rendered the claim honestly and therefore had no contrast, and
   showed her the thing she already stares at all day. A list of six
   years of her own excuses, which was accurate and which repeated the
   phone thread's register: more defences, answered. Recognition three
   times over, and never once a consequence.

   WHAT THE SECTION IS FOR. Between the mirror and the machine the page
   had no cost in it. His avatar is moved by what he loses if he does
   nothing. Hers had no equivalent anywhere on the page.

   AND THE FEAR IS NOT BURNOUT. She dismisses tired, because she is
   capable and she knows it. Hers is that she is waiting on a feeling,
   caught up, which the arrangement is built never to deliver, and that
   the things waiting on it are not sitting still while they wait.

   The mechanism is Daniel's own: ideals get placed in the future,
   "when this, then that", until the mind is trained to look past every
   arrival. So the goalposts move from her side too, and that half is
   the half she can do something about. Blunt, not clever. The list is
   deliberately flat, no build and no connective tissue, because five
   plain statements with air between them do more damage than a
   paragraph. Do not make it lyrical. */
#px-cost{display:block}
#px-cost-cols{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);
  gap:clamp(28px,5vw,72px);align-items:start}
.px-pull{font-size:clamp(18px,2vw,25px);line-height:1.34;color:#1F2A3D;
  font-weight:400;margin:0;padding-top:22px;border-top:1px solid rgba(160,138,94,.34)}
/* The section's verdict. Marked with a bronze rule rather than set in
   gold: gold is Daniel speaking in the first person, and this sentence
   is addressed to her, not quoted from him. Gold on cream is also a
   contrast failure. Bronze is the cream-side equivalent. */
.px-verdict{font-size:clamp(18px,2vw,25px);line-height:1.34;color:#1F2A3D;font-weight:400;
  margin:0 0 18px;padding-left:clamp(16px,1.6vw,22px);border-left:2px solid #A08A5E}
/* THE LEDGER. Five plain statements arriving one at a time do more
   than five arriving together: it reads as a count rather than a list,
   and the pause between them is where each one lands. The bronze
   marker draws in from the left as the row arrives, which is the only
   ornament in the section and the only thing that says invoice.

   No coral anywhere in here. There is no move for her to make in this
   section, and coral on a cost would sell the cost. */
.px-cost-list{list-style:none;margin:0;padding:0}
.px-cost-list li{position:relative;border-top:1px solid rgba(160,138,94,.26);
  padding:13px 0 13px 22px;color:#1F2A3D;font-size:clamp(15px,1.45vw,17px);line-height:1.5;
  opacity:0;transform:translateY(8px);
  transition:opacity .7s ease,transform .7s ease}
.px-cost-list li.on{opacity:1;transform:none}
.px-cost-list li:before{content:'';position:absolute;left:0;top:1.5em;width:0;height:1px;
  background:#A08A5E;transition:width .6s ease .18s}
.px-cost-list li.on:before{width:12px}
.px-cost-list li i{font-style:italic;color:#5A6274}
.px-cost-list li:last-child{border-bottom:1px solid rgba(160,138,94,.26)}
#px-cost-fwd b{color:#1F2A3D;font-weight:600}
#px-cost-fwd{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);
  gap:clamp(28px,5vw,72px);align-items:start;margin-top:clamp(30px,4vw,48px);
  padding-top:30px;border-top:1px solid rgba(160,138,94,.34)}
@media(max-width:860px){#px-cost-cols,#px-cost-fwd{grid-template-columns:1fr;gap:28px}
  .px-pull{padding-top:18px}}


/* ── 07 · PROOF, real text ───────────────────────────────────────
   The face is a CDN crop of the flattened testimonial PNG. Everything
   else on the card is live text, which is the entire reason for doing
   it this way. .px-face is object-fit:cover so object-position can do
   the final framing per person: they were all photographed
   differently and no single value works for three faces. */
/* STRETCH. The three quotes are different lengths, so at align-items:start
   the cards ended at three different heights and the row read as ragged.
   Equal height, with the name pushed to the foot of each card by the
   margin-top:auto below, so the three names line up as well. */
#px-voices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));
  gap:clamp(18px,2.4vw,28px);align-items:stretch}
.px-voice figcaption{margin-top:auto}
.px-voice{margin:0;background:rgba(255,255,255,.045);
  border:1px solid rgba(232,198,95,.16);border-radius:18px;
  padding:clamp(22px,2.4vw,30px);display:flex;flex-direction:column}
.px-face{width:84px;height:84px;border-radius:50%;object-fit:cover;display:block;
  margin-bottom:18px;box-shadow:0 0 0 1px rgba(232,198,95,.35)}
.px-voice blockquote{margin:0 0 20px;color:#F1ECE1;font-size:1rem;line-height:1.6;
  font-weight:300}
.px-voice blockquote:before{content:'\\201C'}
.px-voice blockquote:after{content:'\\201D'}
.px-voice figcaption{margin-top:auto;padding-top:16px;
  border-top:1px solid rgba(232,198,95,.16)}
.px-voice figcaption b{display:block;color:#E8C65F;font-size:.95rem;font-weight:600}
.px-voice figcaption span{display:block;color:#8FA0BC;font-size:.82rem;margin-top:2px}
@media(max-width:900px){#px-voices{grid-template-columns:1fr}}

/* ── 06 · DANIEL, compact ────────────────────────────────────────
   The portrait is 240px, not 420. He appears three times on this page
   now and this is the third; at full height it turns the handover into
   a profile. Small picture, unchanged words. */
/* The copy sets the row height here, the opposite way round to section
   03. So the portrait stretches to meet it rather than stopping short
   and leaving the column ragged. object-fit crops the sides, which is
   why the crop was made wider than the face in the first place. */
#px-daniel-grid{display:grid;grid-template-columns:minmax(0,300px) minmax(0,1fr);
  gap:clamp(28px,4vw,58px);align-items:stretch}
#px-daniel-grid > div:first-child{display:flex}
#px-daniel-img{display:block;width:100%;height:100%;min-height:380px;object-fit:cover;
  object-position:50% 22%;border-radius:16px;
  box-shadow:0 26px 54px -38px rgba(31,42,61,.55),0 0 0 1px rgba(160,138,94,.18)}
@media(max-width:760px){#px-daniel-grid{grid-template-columns:1fr}
  #px-daniel-img{max-width:280px;height:auto;aspect-ratio:4/5}}

/* ── 05 · THE RELATIONAL CONNECTION SYSTEM ───────────────────────
   Three cards that flip. The card IS the state: front is present,
   back is missing, and the line underneath reads the combination.

   Height is set by a hidden sizer rather than a fixed min-height,
   because the faces have to be absolutely positioned for the 3D flip
   and the three cards carry very different amounts of text. The sizer
   is the front face repeated, invisible and out of the accessibility
   tree, so the card is always exactly as tall as its tallest face.

   Values carries a gold rule and one extra paragraph the other two do
   not have. That asymmetry is the point of the section. */
/* THE RULE ACROSS THE TOP IS THE SYSTEM. One continuous hairline sits
   behind all three cards, dim. Each card lights its own segment gold
   while it is on, and the segment goes dark the moment it is turned
   over. Three separate boxes never read as one model. A line that
   breaks does it without a word, and the all-off state is three dark
   segments over "Busy. Good busy." */
/* CARDS LEFT, READ-OUT RIGHT. Stacked in a row the consequence of a
   flip sat below the cards, so turning one and reading what it did
   were two separate glances. Side by side they are one. */
/* STRETCH, NOT CENTRE. Centred, the panel floated with dead navy under
   it and read as a caption pinned in mid-air. Stretched, the two columns
   are the same height and the section reads as one object. The text is
   still optically centred, inside the panel rather than against it. */
#px-rcs-cols{display:grid;grid-template-columns:minmax(0,.9fr) minmax(0,1fr);
  gap:clamp(30px,4.5vw,66px);align-items:stretch}

/* THE RULE BECAME A SPINE. Same idea turned ninety degrees: one dim
   line down the left of the stack, each card lighting its own segment
   in its own colour, going dark the moment it is turned over. */
/* Equal rows, so the three cards match. Stacked they are each their
   own grid row and would otherwise size to their own copy. */
#px-rcs-grid{position:relative;padding-left:26px;display:grid;
  grid-template-columns:1fr;grid-auto-rows:1fr;
  gap:clamp(12px,1.4vw,16px);align-items:stretch}
#px-rcs-grid:before{content:'';position:absolute;left:11px;top:0;bottom:0;width:2px;
  background:rgba(177,191,215,.14);border-radius:2px}
.px-rcs-card:before{content:'';position:absolute;left:-15px;top:0;bottom:0;width:3px;
  border-radius:3px;background:var(--ac);
  box-shadow:0 0 18px -2px color-mix(in srgb,var(--ac) 60%,transparent);
  transition:background .5s ease,box-shadow .5s ease}
.px-rcs-card[aria-pressed="true"]:before{background:rgba(177,191,215,.13);box-shadow:none}

/* THE MARK IS WHAT MAKES THE FLIP WORTH WATCHING. Swapping one block
   of text for another gives the eye nothing. Each dimension has a
   mark, and the mark breaks: the horizon loses the thing it was aimed
   at, the stack levels out with nothing ranked first, the line stops
   short of arriving. Readable in half a second, before a word. */
.px-rcs-glyph{width:38px;height:38px;display:block;overflow:visible;
  grid-column:1;grid-row:1 / span 3;align-self:center}
.px-rcs-glyph path,.px-rcs-glyph circle{fill:none;stroke:var(--ac);stroke-width:1.5;
  stroke-linecap:round;stroke-linejoin:round}
.px-rcs-glyph .fill{fill:var(--ac);stroke:none}
.px-rcs-glyph .lift{stroke-width:2.4}
.px-rcs-glyph.off path,.px-rcs-glyph.off circle{stroke:#5E6B85}
.px-rcs-glyph.off .stub{stroke-dasharray:2 4;opacity:.7}
.px-rcs-card{position:relative;display:block;width:100%;padding:0;border:0;background:none;
  font-family:inherit;text-align:left;cursor:pointer;perspective:1400px;
  -webkit-tap-highlight-color:transparent}
.px-rcs-card:focus-visible{outline:2px solid var(--ac);outline-offset:6px;border-radius:20px}
.px-rcs-inner{position:relative;display:block;width:100%;height:100%;
  transform-style:preserve-3d;transition:transform .62s cubic-bezier(.4,.05,.2,1)}
.px-rcs-card[aria-pressed="true"] .px-rcs-inner{transform:rotateY(180deg)}
/* ONE ACCENT PER DIMENSION, all three from the existing tokens.
   Mist for vision, gold for values, cream for velocity. Deliberately
   NOT the bronze / mid / gold ramp used by the ring on the turn
   screen: that ramp belongs to needs, boundaries and emotions, and
   repeating it here would suggest the two triads map onto each other.
   They do not. Coral stays out of this section entirely, because
   nothing in it is her move. */
.px-rcs-face{position:absolute;inset:0;display:grid;
  grid-template-columns:auto minmax(0,1fr);grid-template-rows:auto auto auto auto;
  column-gap:16px;align-content:center;
  -webkit-backface-visibility:hidden;backface-visibility:hidden;
  border-radius:18px;padding:clamp(20px,2.2vw,28px);
  background:linear-gradient(165deg,color-mix(in srgb,var(--ac) 20%,transparent) 0%,
    color-mix(in srgb,var(--ac) 7%,transparent) 55%,
    color-mix(in srgb,var(--ac) 3%,transparent) 100%);
  border:1px solid color-mix(in srgb,var(--ac) 52%,transparent);
  box-shadow:inset 0 1px 0 color-mix(in srgb,var(--ac) 22%,transparent)}
.px-rcs-face.front .px-label{color:var(--ac)}
.px-rcs-face.back{transform:rotateY(180deg);background:rgba(10,29,60,.55);
  border-color:rgba(177,191,215,.11)}
.px-rcs-face.back .px-label{color:#5E6B85}
.px-rcs-face.back .p{color:#8FA0BC}
.px-rcs-face.back .cue{color:#4E5B75}

.px-rcs-face .px-label{margin-bottom:5px;grid-column:2}
.px-rcs-face.back .px-label{color:#8FA0BC}
.px-rcs-face.front .h{color:color-mix(in srgb,var(--ac) 82%,#F1ECE1)}
.px-rcs-face .h{display:block;color:#F1ECE1;font-size:1.1rem;line-height:1.25;
  font-weight:500;margin-bottom:7px;grid-column:2}
.px-rcs-face .p{display:block;font-size:.9rem;line-height:1.55;color:#B1BFD7;grid-column:2}
.px-rcs-face .here{display:block;color:#E8C65F;font-size:.86rem;line-height:1.55;
  margin-top:12px;padding-left:13px;border-left:2px solid rgba(232,198,95,.55)}
.px-rcs-face .cue{display:block;padding-top:10px;font-size:.58rem;grid-column:2;
  letter-spacing:.18em;text-transform:uppercase;font-weight:800;color:#6C7C97}
.px-rcs-card:hover .cue{color:var(--ac)}
/* the sizer: the front face cloned, kept in flow so it gives the card
   its height, and hidden from sight and from screen readers. It keeps
   the face classes so every inner rule still applies to it. */
.px-rcs-face.px-rcs-sizer{position:relative;visibility:hidden;pointer-events:none;
  transform:none;backface-visibility:visible;border-color:transparent}
/* BOTH faces are cloned into the sizer, stacked in one grid cell, so the
   card takes the height of whichever is taller. The fronts are now a
   label and a heading only, which makes the BACK the tall face on every
   card. Sizing off the front alone would clip the back. */
.px-rcs-sizewrap{display:grid;pointer-events:none}
.px-rcs-sizewrap > .px-rcs-sizer{grid-area:1 / 1}
/* THE READ-OUT. Its own panel, because a line of type floating under
   three cards reads as a caption. In a box it reads as the result of
   what she just did, which is what it is. Left aligned: it is now long
   enough that centred lines would ragged out. */
#px-rcs-state{--sa:#F1ECE1;margin:0;max-width:none;
  display:flex;flex-direction:column;justify-content:center;
  background:rgba(255,255,255,.045);border:1px solid rgba(255,255,255,.07);
  border-left:2px solid var(--sa);border-radius:0 16px 16px 0;
  padding:clamp(24px,2.8vw,34px) clamp(26px,3vw,38px);
  min-height:11.5em;transition:border-color .45s ease}
#px-rcs-state .lab{font-size:.66rem;letter-spacing:.2em;text-transform:uppercase;
  font-weight:800;color:var(--sa);margin:0 0 10px;transition:color .45s ease}
/* 54ch. On a wide screen the column runs past 80 characters, which is
   where a paragraph stops being read and starts being scanned. */
#px-rcs-state .txt{font-family:'Poppins',Montserrat,sans-serif;font-weight:300;
  letter-spacing:-.01em;font-size:clamp(1rem,1.6vw,1.18rem);line-height:1.55;
  color:#F1ECE1;margin:0;max-width:54ch}
#px-rcs-state .txt + .txt{margin-top:.85em}
#px-rcs-state .lab,#px-rcs-state .txt{transition:opacity .26s ease,color .45s ease}
#px-rcs-state.swap .lab,#px-rcs-state.swap .txt{opacity:0}
@media(max-width:560px){#px-rcs-state{min-height:0}}
@media(max-width:900px){
  #px-rcs-cols{grid-template-columns:1fr;gap:30px}
  #px-rcs-state{min-height:0}
}
@media(prefers-reduced-motion:reduce){
  /* no rotation. The back simply replaces the front. */
  .px-rcs-inner{transition:none}
  .px-rcs-card[aria-pressed="true"] .px-rcs-inner{transform:none}
  .px-rcs-face{backface-visibility:visible}
  .px-rcs-face.back{transform:none;opacity:0;pointer-events:none}
  .px-rcs-card[aria-pressed="true"] .px-rcs-face.front{opacity:0}
  .px-rcs-card[aria-pressed="true"] .px-rcs-face.back{opacity:1}
  #px-rcs-state span{transition:none}
}

/* ══════════════════════════════════════════════════════════════════
   04 · THE PRIORITY AUDIT — ported from Priority Audit.dc.html
   ------------------------------------------------------------------
   THIS IS THE STANDALONE'S OWN STYLESHEET, NOT A REIMPLEMENTATION.
   It arrived here verbatim apart from two lines: #pa-root no longer
   paints a background, because the page section owns that, and the
   intro screen is on by default since there is no router in front of
   it. Everything else is byte for byte, so the two readings of the
   same score cannot drift into looking like different instruments.

   The component is self scoped under #pa-root, so nothing here leaks
   into the rest of the page and nothing on the page reaches in.
   ══════════════════════════════════════════════════════════════════ */
/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX TRANSFORMATIONS  ·  THE PRIORITY AUDIT
   -------------------------------------------------------------------
   Fifteen statements about her working week, five per pillar, five
   point frequency scale. The three pillars stay hidden until item
   fifteen is answered, then arrive as the thing her own answers
   produced. Three scores, ranked. No grand total. No band. No
   severity anywhere.

   THIS IS THE COUNTERPART TO THE MEN'S FIVE ARCHETYPE QUIZ, NOT A
   COPY OF IT. His ends in a label, because he arrives already
   doubting himself and a label is a relief. Joining a category is the
   one thing this avatar will not do, so hers ends in a measurement
   she can read and argue with.

   SCREENS
     01 INTRO   two sentences, one button, no form and no email
     02 ITEMS   one at a time, five tap targets, back preserves answers
     03 TURN    cream. the pillars revealed. no score on this screen
     04 RESULT  bars, then six blocks, one scrolling column
     05 CLOSE   the conversation, the PDF, and the bridge sentence

   ═══════════════════════════════════════════════════════════════════
   THE ACCENT RULE.  GOLD IS DANIEL. CORAL IS HER MOVE.
   -------------------------------------------------------------------
   GOLD  #E8C65F  everything Daniel says. Section labels, hairlines,
                  the handwriting, the bridge quote, the selected
                  state on the scale (that is a reading being taken,
                  not an action being sold).
   CORAL #FF501F  her moves only. Start. See your three. Bring this to
                  a conversation. And the fill on her highest bar,
                  which the build spec calls for by name.

   Coral never appears as decorative emphasis in prose. Structural
   emphasis inside body copy is cream on navy, ink on cream. Nothing
   pulses. Backgrounds alternate navy and cream, never two creams
   adjacent: navy intro, navy items, cream turn, navy result head,
   cream her half, navy close.
   ═══════════════════════════════════════════════════════════════════ */

  /* THE RESET IS WRAPPED IN :where() ON PURPOSE.
     It has to be scoped, because in the Design Code runtime this sheet is
     loaded into the live page and an unscoped star-margin-zero rule would flatten
     the Wix header and footer sitting around it. But an ID-scoped star selector scores
     an ID, which beats every class in this stylesheet: .pa-wrap lost its
     max-width padding, .pa-col lost its auto margins, and the whole
     component rendered hard against the left edge. :where() scopes the
     selector while contributing nothing to specificity, so the reset is
     scoped AND still the weakest rule in the file. */
  :where(#pa-root) *,:where(#pa-root) *::before,:where(#pa-root) *::after{box-sizing:border-box;margin:0;padding:0}
  #pa-root ::selection{background:#FF501F;color:#fff}

  #pa-root{
    --navy:#04122A; --navy-2:#0A1D3C; --navy-ink:#030C1C;
    --cream:#F1ECE1; --cream-2:#F7F3EA;
    --gold:#E8C65F; --gold-lift:#F3DD93; --bronze:#A08A5E;
    --coral:#FF501F; --coral-lift:#FF6A3D; --coral-ink:#C0391A;
    --mist:#B1BFD7; --slate:#7C89A3; --label:#5E6B85;
    --ink:#1F2A3D; --ink-soft:#5A6274; --line:#12233F; --line-cream:#DED6C4;
    background:transparent;
    color:var(--mist);
    font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,sans-serif;
    font-size:16px;line-height:1.7;position:relative;overflow-x:clip;
    -webkit-font-smoothing:antialiased;
  }

  .pa-serif{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em;line-height:1.18;color:var(--cream)}
  .pa-hand{font-family:'Lumios Marker','Permanent Marker',cursive;color:var(--gold);line-height:1.35}
  .pa-wrap{max-width:1240px;margin:0 auto;padding-left:clamp(20px,4vw,52px);padding-right:clamp(20px,4vw,52px)}
  .pa-col{max-width:660px;margin-left:auto;margin-right:auto}
  .pa-label{font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;color:var(--label)}
  .pa-gold-label{color:var(--gold);opacity:.9}
  #pa-root strong{font-weight:600;color:var(--cream)}
  .on-cream strong{color:var(--ink)}
  .sr{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap}

  /* ── BUTTONS ─────────────────────────────────────────────────── */
  .pa-btn{display:inline-flex;align-items:center;justify-content:center;gap:.55em;
    background:var(--coral);color:#fff;font-weight:700;font-size:.98rem;font-family:inherit;
    padding:1.05em 2.2em;border:0;border-radius:999px;text-decoration:none;cursor:pointer;
    transition:background .3s,transform .3s,box-shadow .3s;
    box-shadow:0 10px 30px -12px rgba(255,80,31,.5)}
  .pa-btn:hover{background:var(--coral-lift);transform:translateY(-2px)}
  .pa-btn:focus-visible{outline:2px solid var(--gold);outline-offset:3px}

  .pa-ghost{display:inline-flex;align-items:center;justify-content:center;gap:.55em;
    color:var(--mist);background:none;border:1px solid rgba(232,198,95,.4);
    font-weight:600;font-size:.94rem;font-family:inherit;padding:1em 2em;border-radius:999px;
    text-decoration:none;cursor:pointer;transition:background .3s,border-color .3s,color .3s}
  .pa-ghost:hover{background:rgba(241,236,225,.08);border-color:var(--cream);color:var(--cream)}
  .pa-ghost:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .on-cream .pa-ghost{color:var(--ink-soft);border-color:rgba(160,138,94,.55)}
  .on-cream .pa-ghost:hover{background:rgba(160,138,94,.1);border-color:var(--bronze);color:var(--ink)}

  .pa-quiet{background:none;border:0;color:var(--slate);font-family:inherit;font-size:.8rem;
    letter-spacing:.06em;cursor:pointer;padding:.6em .2em;transition:color .25s}
  .pa-quiet:hover{color:var(--cream)}
  .pa-quiet:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .on-cream .pa-quiet{color:var(--ink-soft)}
  .on-cream .pa-quiet:hover{color:var(--ink)}

  /* ── SCREENS ─────────────────────────────────────────────────── */
  .pa-screen{display:none}
  .pa-screen.is-on{display:block}
  .pa-stage{min-height:clamp(520px,78svh,860px);display:flex;flex-direction:column;justify-content:center;
    padding:clamp(40px,7vw,86px) 0}
  .pa-fadein{opacity:0;transform:translateY(16px);
    transition:opacity .6s cubic-bezier(.16,1,.3,1),transform .6s cubic-bezier(.16,1,.3,1)}
  .pa-fadein.is-in{opacity:1;transform:none}

  /* ── 01 INTRO ────────────────────────────────────────────────── */
  #pa-intro{background:var(--navy)}
  #pa-intro .pa-col{text-align:center;max-width:880px}
  #pa-intro h1{font-size:clamp(1.7rem,3.5vw,2.55rem);margin:20px auto 0;max-width:none}
  #pa-intro h1 .l{display:block}
  #pa-intro h1 em .l{display:block}
  #pa-intro h1 em{font-style:normal;color:var(--mist);font-weight:300;display:block;font-size:.55em;
    margin:.52em auto 0;letter-spacing:0;max-width:none;line-height:1.3}
  #pa-intro p.lede{margin:26px auto 0;max-width:min(830px,100%);font-size:.97rem;
    line-height:1.62;text-wrap:pretty}
  #pa-intro .pa-hand{margin-top:34px;font-size:clamp(1rem,2.4vw,1.22rem)}
  #pa-intro .cta{margin-top:36px;display:flex;flex-direction:column;align-items:center;gap:15px}
  #pa-intro .meta{font-size:.72rem;letter-spacing:.16em;text-transform:uppercase;color:var(--slate);font-weight:700}

  .pa-legend{margin:38px auto 0;max-width:560px;border-top:1px solid rgba(232,198,95,.28);padding-top:16px;
    display:flex;justify-content:space-between;gap:10px;
    font-size:.66rem;letter-spacing:.16em;text-transform:uppercase;font-weight:700;color:var(--label)}
  .pa-legend i{font-style:normal;color:var(--slate)}
  @media(max-width:520px){.pa-legend i{display:none}}

  /* ── 02 ITEMS ────────────────────────────────────────────────── */
  #pa-items{background:var(--navy)}
  .pa-prog{max-width:660px;margin:0 auto 40px;width:100%}
  .pa-prog-top{display:flex;align-items:baseline;justify-content:space-between;gap:14px;margin-bottom:11px}
  .pa-prog-count{font-family:'Poppins',sans-serif;font-size:.9rem;color:var(--cream);letter-spacing:.02em}
  .pa-prog-count b{color:var(--gold);font-weight:500}
  .pa-prog-bar{height:2px;background:var(--line);border-radius:99px;overflow:hidden}
  .pa-prog-fill{height:100%;width:0;background:linear-gradient(90deg,var(--gold-lift),var(--gold));
    border-radius:99px;transition:width .45s cubic-bezier(.16,1,.3,1)}

  #pa-stmt{font-size:clamp(1.12rem,2.2vw,1.5rem);line-height:1.34;min-height:2.7em;
    display:flex;align-items:center;max-width:34ch;margin-bottom:28px}
  .pa-ask{font-size:.7rem;letter-spacing:.2em;text-transform:uppercase;color:var(--slate);font-weight:700;margin-bottom:18px}

  .pa-scale{display:flex;flex-direction:column;gap:9px}
  .pa-opt{display:flex;align-items:center;gap:15px;width:100%;text-align:left;
    background:rgba(18,35,63,.42);border:1px solid var(--line);border-radius:14px;
    padding:16px 19px;color:var(--mist);font-family:inherit;font-size:1rem;font-weight:500;
    cursor:pointer;transition:border-color .22s,background .22s,transform .22s,color .22s}
  .pa-opt:hover{border-color:rgba(232,198,95,.55);background:rgba(18,35,63,.78);transform:translateX(3px);color:var(--cream)}
  .pa-opt:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .pa-opt .dot{flex:0 0 auto;width:26px;height:26px;border-radius:50%;display:grid;place-items:center;
    border:1px solid var(--line);transition:background .22s,border-color .22s}
  .pa-opt .dot::after{content:'';width:9px;height:9px;border-radius:50%;background:transparent;transition:background .22s}
  .pa-opt:hover .dot{border-color:rgba(232,198,95,.7)}
  .pa-opt[aria-checked="true"]{border-color:var(--gold);background:rgba(232,198,95,.09);color:var(--cream)}
  .pa-opt[aria-checked="true"] .dot{border-color:var(--gold)}
  .pa-opt[aria-checked="true"] .dot::after{background:var(--gold)}

  .pa-navrow{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-top:26px}
  .pa-hint{font-size:.7rem;letter-spacing:.1em;color:var(--label)}
  @media(max-width:620px){.pa-hint{display:none}}

  /* ── 03 THE TURN ─────────────────────────────────────────────── */
  #pa-turn{background:var(--cream)}
  #pa-turn .pa-col{max-width:820px}
  #pa-turn .pa-stage{min-height:0;padding:clamp(46px,6vw,74px) 0}

  /* ── THE RING ──────────────────────────────────────────────────────
     UNSCORED, AND IT STAYS THAT WAY. The V1 wheel had nine boxes to
     fill in, which fifteen items cannot populate: that is under two
     questions per sub-area, and a two-item scale is noise. It also
     said 'neglect' nine times before she had answered anything.

     So this is not a chart. It is a picture of a claim: those fifteen
     were three things, and the three make one thing. The last line of
     this screen asserts that each one holds the other two up, and
     until now that assertion had no picture. Three equal arcs, equal
     lengths, no numbers, no totals, no boxes.

     Gold family because it is Daniel naming his own framework. Coral
     never appears here; nothing on this screen is her move except the
     button. */
  #pa-turn-grid{display:grid;grid-template-columns:210px 1fr;gap:clamp(22px,3vw,38px);
    align-items:center;margin-top:24px}
  .pa-ring{width:100%;max-width:210px;margin:0 auto}
  .pa-ring svg{width:100%;height:auto;display:block;overflow:visible}
  .pa-ring .arc{fill:none;stroke-width:15;stroke-linecap:round;
    stroke-dasharray:181;stroke-dashoffset:181;
    transition:stroke-dashoffset 1.1s cubic-bezier(.16,1,.3,1)}
  .pa-ring .arc.a1{stroke:#A08A5E}
  .pa-ring .arc.a2{stroke:#C9A96B;transition-delay:.13s}
  .pa-ring .arc.a3{stroke:#E8C65F;transition-delay:.26s}
  .pa-ring.is-in .arc{stroke-dashoffset:0}
  .pa-ring .mid{font-family:'Poppins','Montserrat',sans-serif;font-weight:500;fill:#A08A5E;
    font-size:19px;letter-spacing:.02em;text-anchor:middle}
  /* The swatch is the only thing tying an arc to its definition, so it
     carries the same three values in the same order. */
  .pa-def{display:grid;grid-template-columns:auto 1fr;gap:0 12px;align-items:start}
  .pa-def .sw{width:10px;height:10px;border-radius:50%;margin-top:.52em}
  .pa-def:nth-child(1) .sw{background:#A08A5E}
  .pa-def:nth-child(2) .sw{background:#C9A96B}
  .pa-def:nth-child(3) .sw{background:#E8C65F}
  .pa-def h3,.pa-def p{grid-column:2}

  @media(max-width:720px){
    #pa-turn-grid{grid-template-columns:1fr;gap:22px;margin-top:20px}
    .pa-ring{max-width:132px}
  }
  /* NO GSAP, NO MOTION, NO PROBLEM. The ring is complete on arrival. */
  @media(prefers-reduced-motion:reduce){
    .pa-ring .arc{stroke-dashoffset:0;transition:none}
  }
  #pa-turn h2{color:var(--ink);font-size:clamp(1.55rem,3.1vw,2.15rem);font-weight:500}
  .pa-defs{display:flex;flex-direction:column;gap:0}
  .pa-def{padding:14px 0;border-top:1px solid rgba(160,138,94,.42)}
  .pa-def:last-child{border-bottom:1px solid rgba(160,138,94,.42)}
  .pa-def h3{font-family:'Poppins',sans-serif;font-weight:500;font-size:1.08rem;color:var(--ink);
    letter-spacing:-.01em;margin-bottom:2px}
  .pa-def p{color:var(--ink-soft);font-size:.92rem;line-height:1.55;max-width:52ch}
  #pa-turn .after{margin-top:22px;color:var(--ink-soft);max-width:56ch;font-size:.95rem}
  #pa-turn .cta{margin-top:26px}

  /* ── 04 RESULT ───────────────────────────────────────────────── */
  #pa-result{background:var(--navy)}
  .pa-r-head{padding:clamp(46px,7vw,88px) 0 clamp(28px,4vw,40px);
    background:radial-gradient(120% 90% at 50% 0%,rgba(18,35,63,.9),transparent 70%)}
  .pa-r-head h2{font-size:clamp(1.7rem,4vw,2.7rem);margin:12px 0 0;max-width:20ch}

  /* THE BARS. Gold hairline track, coral fill on the highest only.
     The number sits beside the bar because this avatar reads data all
     day and a bar with no digit on it reads as an opinion. */
  .pa-key{margin:20px 0 0;font-size:.93rem;color:var(--mist);max-width:58ch;
    border-left:1px solid rgba(232,198,95,.45);padding-left:16px}
  .pa-bars{margin:30px 0 0;display:flex;flex-direction:column;gap:32px}
  /* The tag names the block this bar owns further down the page. Gold,
     because it is Daniel labelling the reading, not her taking an action. */
  .pa-bar-tag{margin-top:8px;font-size:.62rem;letter-spacing:.2em;text-transform:uppercase;
    font-weight:700;color:var(--label)}
  .pa-bar.is-top .pa-bar-tag,.pa-bar.is-pressure .pa-bar-tag{color:var(--gold)}
  .pa-bar-head{display:flex;align-items:baseline;justify-content:space-between;gap:14px;margin-bottom:9px}
  .pa-bar .name{font-family:'Poppins',sans-serif;font-size:1.06rem;color:var(--slate);letter-spacing:-.01em;transition:color .3s}
  .pa-bar .val{font-family:'Poppins',sans-serif;font-size:1.06rem;color:var(--slate);transition:color .3s}
  .pa-bar .val i{font-style:normal;opacity:.45;font-size:.82em}
  .pa-track{height:8px;border-radius:99px;background:rgba(232,198,95,.13);
    box-shadow:inset 0 0 0 1px rgba(232,198,95,.3);overflow:hidden}
  .pa-fill{height:100%;width:0;border-radius:99px;background:rgba(177,191,215,.45);
    transition:width .9s cubic-bezier(.16,1,.3,1)}
  .pa-bar.is-top .name,.pa-bar.is-top .val{color:var(--cream)}
  .pa-bar.is-top .pa-fill{background:var(--coral)}
  @media(prefers-reduced-motion:reduce){.pa-fill{transition:none}}

  .pa-flat{margin:30px 0 0;padding:20px 22px;border-left:2px solid var(--gold);
    background:rgba(18,35,63,.5);border-radius:0 14px 14px 0;color:var(--mist);font-size:.97rem}
  .pa-flat[hidden]{display:none}
  .pa-gap{height:clamp(40px,6vw,72px)}

  /* Section rules. The label is Daniel naming the block, so it is gold. */
  .pa-rule{display:flex;align-items:center;gap:16px;margin:clamp(44px,6vw,68px) 0 22px}
  .pa-rule span{white-space:nowrap;font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;
    font-weight:700;color:var(--gold)}
  .pa-rule::after{content:'';flex:1;height:1px;background:linear-gradient(90deg,rgba(232,198,95,.4),transparent)}
  .on-cream .pa-rule span{color:var(--bronze)}
  .on-cream .pa-rule::after{background:linear-gradient(90deg,rgba(160,138,94,.55),transparent)}

  .pa-prose p{margin-bottom:1.1em;font-size:1.02rem}
  .pa-prose p:last-child{margin-bottom:0}
  .pa-pillar-name{font-family:'Poppins',sans-serif;font-weight:500;letter-spacing:-.015em;
    font-size:clamp(1.5rem,3.2vw,2.1rem);color:var(--cream);margin-bottom:16px}
  .on-cream .pa-pillar-name{color:var(--ink)}
  .pa-inset{margin:26px 0;padding:20px 22px;border-left:2px solid rgba(232,198,95,.55);
    background:rgba(18,35,63,.42);border-radius:0 14px 14px 0}
  .pa-inset h4{font-family:'Poppins',sans-serif;font-weight:500;font-size:.98rem;color:var(--gold);
    letter-spacing:.01em;margin-bottom:7px}
  .pa-inset p{font-size:.99rem;color:var(--mist)}

  /* Her half of the result, on cream. */
  #pa-her{background:var(--cream);color:var(--ink-soft);padding:clamp(48px,7vw,88px) 0}
  #pa-her .pa-prose p{color:var(--ink-soft)}
  #pa-her .pa-inset{border-left-color:var(--bronze);background:rgba(255,255,255,.62);
    box-shadow:0 12px 34px -26px rgba(31,42,61,.55)}
  #pa-her .pa-inset h4{color:var(--bronze)}
  #pa-her .pa-inset p{color:var(--ink-soft)}
  #pa-her .pa-rule:first-of-type{margin-top:0}

  .pa-qs{margin:14px 0 0;list-style:none;display:flex;flex-direction:column;gap:0}
  .pa-qs li{padding:19px 0;border-top:1px solid rgba(160,138,94,.4);
    font-family:'Poppins',sans-serif;font-weight:400;font-size:1.06rem;line-height:1.45;
    color:var(--ink);letter-spacing:-.005em}
  .pa-qs li:last-child{border-bottom:1px solid rgba(160,138,94,.4)}
  .pa-qset + .pa-qset{margin-top:30px}
  .pa-qset h4{font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;
    color:var(--bronze);margin-bottom:2px}
  .pa-note{margin-top:18px;font-size:.87rem;color:#8A8580}

  /* ── 05 CLOSE ────────────────────────────────────────────────── */
  #pa-close{background:var(--navy);padding:clamp(48px,7vw,92px) 0 clamp(54px,8vw,100px)}
  .pa-bridge{border-left:2px solid var(--gold);padding:22px 24px;background:rgba(18,35,63,.5);
    border-radius:0 16px 16px 0;font-family:'Poppins',sans-serif;font-weight:300;
    font-size:clamp(1.05rem,2.3vw,1.3rem);line-height:1.55;color:var(--cream);letter-spacing:-.01em}
  .pa-bridge p + p{margin-top:.7em}
  .pa-close-cta{margin-top:38px;display:flex;flex-wrap:wrap;gap:14px;align-items:center}
  .pa-close-sub{margin-top:16px;font-size:.87rem;color:var(--slate);max-width:52ch}
  .pa-restart{margin-top:34px;padding-top:24px;border-top:1px solid rgba(232,198,95,.16)}

  /* ── RESPONSIVE ──────────────────────────────────────────────── */
  @media(max-width:620px){
    #pa-stmt{max-width:none;min-height:0;margin-bottom:22px}
    #pa-intro h1 .l,#pa-intro h1 em .l{display:inline}
    .pa-opt{padding:15px 17px;font-size:.96rem}
    .pa-stage{min-height:clamp(480px,72svh,760px);padding:clamp(30px,7vw,60px) 0}
    .pa-close-cta{flex-direction:column;align-items:stretch}
    .pa-close-cta .pa-btn,.pa-close-cta .pa-ghost{width:100%}
  }

  /* ── REDUCED MOTION. Nothing is gated on an animation. ───────── */
  @media(prefers-reduced-motion:reduce){
    #pa-root *{animation:none!important}
    .pa-fadein{opacity:1!important;transform:none!important;transition:none!important}
    .pa-btn:hover,.pa-opt:hover{transform:none}
  }

  /* ── PRINT. The ghost CTA is window.print(), so this IS the PDF.
     Navy prints as a wall of ink, so the whole result flattens to
     white with ink type, and the controls go. ─────────────────── */
  @media print{
    #pa-root,#pa-result,#pa-close,#pa-her,.pa-r-head{background:#fff!important;color:#1F2A3D!important}
    #pa-root{font-size:11pt}
    .pa-serif,.pa-pillar-name,.pa-r-head h2{color:#04122A!important}
    .pa-prose p,.pa-inset p,.pa-flat{color:#3A4459!important}
    .pa-label,.pa-rule span,.pa-inset h4,.pa-qset h4{color:#8A6F45!important}
    .pa-bar .name,.pa-bar .val{color:#1F2A3D!important}
    .pa-key{color:#3A4459!important;border-left-color:#A08A5E!important}
    .pa-bar-tag{color:#8A6F45!important}
    .pa-track{background:#EDE7DC!important;box-shadow:inset 0 0 0 1px #C9BE9F!important}
    .pa-fill{background:#B9C2CF!important}
    .pa-bar.is-top .pa-fill{background:#FF501F!important}
    .pa-inset,.pa-flat,.pa-bridge{background:#F7F3EA!important;border-left-color:#A08A5E!important}
    .pa-bridge{color:#1F2A3D!important}
    .pa-qs li{color:#1F2A3D!important;border-color:#C9BE9F!important}
    .pa-btn,.pa-ghost,.pa-quiet,.pa-restart,.pa-close-cta,.pa-close-sub{display:none!important}
    .pa-fadein{opacity:1!important;transform:none!important}
    .pa-rule{break-after:avoid}
    .pa-inset,.pa-bar{break-inside:avoid}
    /* Screen breathing room becomes dead paper. */
    .pa-gap{height:0}
    .pa-r-head{padding-top:0}
    .pa-rule{margin-top:clamp(20px,3vw,30px)}
  }

/* ── page-only: ONE COLOUR ALL THE WAY THROUGH ───────────────────
   The standalone alternates navy intro, navy items, cream turn, navy
   result, cream her half, navy close. On its own page that is six
   screens of a deck and the changes read as chapters.

   In this page it is one section that swaps material four times while
   she is halfway through answering, and a background flipping under
   her mid instrument reads as a bug rather than as a shift. Cream
   throughout, and the tonal drop is handed back to the RCS section
   underneath, which is navy and now does that job on its own.

   Everything below re-points the dark-only colours. Nothing in the
   ported sheet is edited: the screens simply carry .on-cream and this
   block supplies what that class did not already cover. */
#px-audit-sec,#pa-intro,#pa-items,#pa-turn,#pa-result,#pa-her,#pa-close{
  background:var(--cream-2) !important}
#pa-root .on-cream .pa-serif,
#pa-root .on-cream #pa-stmt{color:var(--ink)}
#pa-root .on-cream .pa-prog-count{color:var(--ink)}
#pa-root .on-cream .pa-prog-count b{color:var(--bronze)}
#pa-root .on-cream .pa-prog-bar{background:rgba(160,138,94,.24)}
#pa-root .on-cream .pa-prog-fill{background:var(--bronze)}
#pa-root .on-cream .pa-ask,
#pa-root .on-cream .pa-hint{color:var(--ink-soft)}
#pa-root .on-cream .pa-opt{background:#fff;border-color:#D9D2C6;color:var(--ink-soft)}
#pa-root .on-cream .pa-opt:hover{border-color:var(--bronze);
  background:rgba(160,138,94,.06);color:var(--ink)}
#pa-root .on-cream .pa-opt[aria-checked="true"]{border-color:var(--bronze);
  background:rgba(160,138,94,.16);color:var(--ink)}
/* result head, bars and callouts */
#pa-root .on-cream .pa-r-head{background:none}
#pa-root .on-cream .pa-key{color:var(--ink-soft);border-left-color:var(--bronze)}
#pa-root .on-cream .pa-bar .name,
#pa-root .on-cream .pa-bar .val{color:#8A8378}
#pa-root .on-cream .pa-bar.is-top .name,
#pa-root .on-cream .pa-bar.is-top .val{color:var(--ink)}
#pa-root .on-cream .pa-bar-tag{color:#8A8378}
#pa-root .on-cream .pa-bar.is-top .pa-bar-tag,
#pa-root .on-cream .pa-bar.is-pressure .pa-bar-tag{color:var(--bronze)}
#pa-root .on-cream .pa-track{background:rgba(160,138,94,.14);
  box-shadow:inset 0 0 0 1px rgba(160,138,94,.34)}
#pa-root .on-cream .pa-fill{background:#B9C2CF}
#pa-root .on-cream .pa-bar.is-top .pa-fill{background:var(--coral)}
#pa-root .on-cream .pa-prose p{color:var(--ink-soft)}
#pa-root .on-cream .pa-inset,
#pa-root .on-cream .pa-flat{background:#fff;border-left-color:var(--bronze);
  box-shadow:0 12px 34px -26px rgba(31,42,61,.55)}
#pa-root .on-cream .pa-inset h4{color:var(--bronze)}
#pa-root .on-cream .pa-inset p,
#pa-root .on-cream .pa-flat{color:var(--ink-soft)}
/* the close */
#pa-root .on-cream .pa-bridge{background:#fff;border-left-color:var(--bronze);
  color:var(--ink);box-shadow:0 12px 34px -26px rgba(31,42,61,.55)}
#pa-root .on-cream .pa-close-sub{color:var(--ink-soft)}
#pa-root .on-cream .pa-restart{border-top-color:rgba(160,138,94,.34)}
#pa-root .on-cream .pa-sys b{color:var(--ink)}
/* gold at .9 opacity on cream is close to unreadable. Bronze is the
   cream-side value for Daniel's voice and it is already in the tokens. */
#pa-root .on-cream .pa-gold-label{color:var(--bronze);opacity:1}

/* ── page-only: THE SCALE IS A HORIZONTAL PILL ROW ───────────────
   The ported control is a vertical stack of rows with a radio dot,
   which is right for a standalone page where the scale owns the whole
   screen. Inside this page the first statement is an INSERT sitting in
   a section of running copy, and a five-row stack reads as a form
   dropped into an article. A single row of pills reads as one
   question, which is what it is, and it keeps the same shape on every
   screen after it so nothing changes under her while she answers.

   The dot goes with it: a pill has no room for one and the fill state
   already says selected. Everything else about the control is the
   ported behaviour, including the roving tabindex, the arrow keys and
   the 300ms advance.

   SELECTED IS GOLD, NOT CORAL. That is the accent rule from the
   standalone's own header: gold is a reading being taken, coral is an
   action being sold. The earlier cream version of this page filled the
   selected pill coral, which quietly devalued the coral buttons around
   it. One line to change back if you disagree. */
.pa-scale{flex-direction:row;flex-wrap:wrap;gap:8px}
.pa-opt{flex:1 1 auto;width:auto;justify-content:center;text-align:center;white-space:nowrap;
  border-radius:999px;padding:14px 18px;font-size:.9rem;font-weight:600;gap:0}
.pa-opt:hover{transform:none}
.pa-opt .dot{display:none}
@media(max-width:700px){
  .pa-opt,.pa-live-row .pa-opt{flex:1 1 calc(50% - 4px);padding:13px 10px;font-size:.86rem}
}
@media(max-width:380px){.pa-opt,.pa-live-row .pa-opt{flex:1 1 100%}}

/* ── page-only additions ─────────────────────────────────────────
   The intro is cream because section 03 above it is navy, and two
   navies adjacent kills the alternation. The live first statement
   replaces the standalone's Start button: on this page she is already
   answering, so there is nothing to start. */
#pa-intro{background:var(--cream-2) !important}
/* CENTRED, AND WIDER. This was left aligned in the 620px column while
   the live statement inside it was centred, so the block disagreed
   with itself: a left edge for the words, a centre line for the card.
   Widening also takes the headline from three lines to two and the
   two paragraphs from three to two, which is most of the "too much
   text" without cutting a word.

   The item screens stay left aligned. That is the canonical build and
   it is a legible shift: this screen is the page talking, every screen
   after it is the instrument. */
#pa-intro .pa-col{text-align:center;max-width:820px;margin:0 auto}
#pa-intro .lede,#pa-intro .pa-promise{margin-left:auto;margin-right:auto;max-width:64ch}
#pa-intro .pa-fine{text-align:center}
#pa-first{max-width:660px;margin:26px auto 0}
#pa-intro .pa-stage{min-height:0;padding:clamp(52px,6.5vw,92px) 0}
/* THE FIFTEEN TAKE THE WHOLE SCREEN. The ported stage is 78svh, which
   is right on a standalone page where nothing follows it. In the page
   it leaves the next section peeking under the statement, and she
   answers item nine with the system section in view. One screen, one
   question, nothing else. */
#pa-items .pa-stage{min-height:100svh}
#pa-intro h2{color:var(--ink);font-size:clamp(25px,3vw,40px);line-height:1.18;font-weight:500;
  margin:0 0 20px;text-wrap:balance}
#pa-intro .lede{color:var(--ink-soft);margin:0 auto 16px;font-size:1rem;text-align:center}
#pa-intro .pa-hand{margin:0 0 14px;font-size:1.15rem;color:var(--bronze)}
#pa-first .pa-label{color:var(--bronze);margin-bottom:14px}
/* The insert is centred and the pills size to their labels rather than
   stretching, so the card reads as one question rather than as a form
   row. The left rule stays: it is the only thing marking this as live
   rather than illustrative. */
.pa-live-row{background:#fff;border-radius:14px;text-align:center;
  padding:clamp(26px,3.2vw,36px) clamp(20px,3vw,30px);
  box-shadow:0 1px 2px rgba(31,42,61,.05);border-left:3px solid var(--bronze)}
.pa-live-row .q{font-size:1.05rem;line-height:1.45;color:var(--ink);margin:0 0 24px}
.pa-live-row .pa-scale{justify-content:center}
/* Equal widths rather than hugging their labels: five content-sized
   pills do not fit the card at 620px and wrap to a second row, which
   breaks the one-question read. Equal and stretched, they fill one
   line and are centred by definition. */
.pa-live-row .pa-opt{flex:1 1 0;padding:14px 8px;font-size:.84rem}
.pa-live-row .pa-opt{background:#fff;border-color:#D9D2C6;color:var(--ink-soft)}
.pa-live-row .pa-opt:hover{border-color:var(--bronze);background:rgba(160,138,94,.06);color:var(--ink)}
.pa-live-row .pa-opt[aria-checked="true"]{border-color:var(--bronze);
  background:rgba(160,138,94,.16);color:var(--ink)}
.pa-promise{color:var(--ink-soft);line-height:1.66;font-size:.95rem;margin:26px auto 0}
.pa-fine{font-size:.8rem;color:#8A8580;margin:14px auto 0}
/* WHERE THIS SITS. The audit measures needs, boundaries and emotions.
   The coaching runs on vision, values and velocity, and this
   instrument is a piece of values. Page only for now: the standalone
   does not carry it yet. */
.pa-sys b{color:var(--cream)}


/* The result sheet is one scrolling column with labelled rules
   between the blocks, matching Priority Audit.dc.html so the two
   readings of the same score cannot look like different instruments. */
.px-au-r-head{font-size:clamp(22px,2.6vw,32px);line-height:1.2;color:#1F2A3D;
  font-weight:500;margin:8px 0 14px}
.px-au-key{color:#5A6274;line-height:1.66;font-size:.95rem;margin:0 0 6px}
#px-au-result p{color:#5A6274;line-height:1.68;margin:0 0 16px}
.px-au-rule{display:flex;align-items:center;gap:14px;margin:32px 0 18px}
.px-au-rule:before,.px-au-rule:after{content:'';height:1px;background:rgba(160,138,94,.3)}
.px-au-rule:before{width:0}
.px-au-rule:after{flex:1}
.px-au-rule span{font-size:.66rem;letter-spacing:.2em;text-transform:uppercase;
  font-weight:800;color:#A08A5E;white-space:nowrap}
.px-au-pillar{font-family:'Poppins',Montserrat,sans-serif;font-weight:500;letter-spacing:-.015em;
  font-size:clamp(22px,2.6vw,32px);color:#1F2A3D !important;margin:0 0 12px !important}
.px-au-inset{background:#fff;border-left:3px solid #A08A5E;border-radius:0 12px 12px 0;
  padding:16px 20px;margin:0 0 16px}
.px-au-inset h4{font-size:.66rem;letter-spacing:.2em;text-transform:uppercase;font-weight:800;
  color:#A08A5E;margin:0 0 6px}
.px-au-inset p{margin:0 !important;color:#1F2A3D !important}
.px-au-note{font-size:.82rem;color:#8A8580 !important;margin-top:14px !important}
.px-au-qs{list-style:none;margin:0;padding:0}
.px-au-qs li{color:#1F2A3D;font-size:1.02rem;line-height:1.5;padding:14px 0 14px 20px;
  border-top:1px solid rgba(160,138,94,.2);position:relative}
.px-au-qs li:first-child{border-top:0;padding-top:2px}
.px-au-qs li:before{content:'';position:absolute;left:0;top:1.32em;width:8px;height:1px;
  background:#A08A5E}
.px-au-qs li:first-child:before{top:.6em}
.px-au-close{margin-top:34px;padding-top:28px;border-top:2px solid rgba(160,138,94,.4)}
.px-au-bridge{color:#1F2A3D;line-height:1.66;margin:0 0 22px;font-size:1.02rem}
.px-au-fine{font-size:.8rem;color:#8A8580;margin:18px 0 0}
.px-au-ghost{display:block;margin:18px 0 0;background:none;border:0;padding:4px 0;
  font-family:inherit;font-size:.86rem;font-weight:600;color:#5A6274;cursor:pointer;
  text-decoration:underline;text-underline-offset:4px;text-decoration-color:rgba(160,138,94,.5)}
.px-au-ghost:hover{color:#FF501F}
.px-au-again{margin-top:22px;background:none;border:0;padding:4px 0;font-family:inherit;
  font-size:.78rem;letter-spacing:.1em;text-transform:uppercase;font-weight:700;
  color:#A08A5E;cursor:pointer}
.px-au-again:hover{color:#FF501F}
@media(max-width:560px){
  .px-au-scale button{flex:1 1 45%;white-space:normal}
  .px-au-scale.big button{flex:1 1 100%}
  .px-au-bar{grid-template-columns:88px 1fr auto;gap:10px}
}

@media(max-width:900px){
  #px-reframe-cols,#px-daniel-cols{grid-template-columns:1fr !important}
}

/* ══════════════════════════════════════════════════════════════════
   SITE FOOTER, BUILT IN RATHER THAN PLACED
   PtFooter is its own custom element and is meant to sit once in the
   Wix footer strip, which is what makes it editable in one place for
   every page. It would not load there, so it ships inside this page
   instead. That is a real cost and it should be written down: this
   copy of the footer will not track PtFooter v3.dc.html. Change one
   and the other does not move.

   PtFooter v3.dc.html stays the source of record. If it starts loading
   in Wix again, delete this block and the markup at the foot of the
   page rather than maintaining two.

   Every selector here is pt- prefixed and nothing in the page uses that
   prefix, so the two sheets do not touch.
   ══════════════════════════════════════════════════════════════════ */
/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · SITE FOOTER · v3  ·  THREE COLUMNS + UTILITY ROW
   -------------------------------------------------------------------
   THE PROBLEM WITH v2
   Twenty-four links in four equal columns. Every one of them the same
   size, the same colour, the same weight. On a site whose entire
   premise is "pick one of two doors", the last thing on the page was
   a sitemap that made twenty-four things look equally important.

   THE FIX IS NOT FEWER LINKS. IT IS HIERARCHY.
   Seven pages on this site are linked from NOWHERE except this footer:
   Blog, both podcasts, Progress Journal, Three Toxic Lies, Identity
   2.0, As Seen In. Delete them from here and they are orphaned:
   unreachable by a human, and invisible to search because nothing
   points at them. So they stay. They just stop shouting.

   WHAT THE VISITOR SEES NOW
     THREE COLUMNS, LARGE   the two doors, then Daniel. nine links.
                            the whole site, as far as anyone arriving
                            cares.
     ONE QUIET ROW, SMALL   everything else, inline, dot-separated,
                            in the register of a footnote rather than
                            a menu. Reachable. Not competing.

   Reading order does the work: doors first at full weight, then the
   row you only look at if you were already looking for something
   specific, then legal. Each tier is quieter than the one above it.

   WHY THERE IS NO BLURB UNDER THE LOGO
   A footer does not need to restate the pitch. Everything above it has
   already spoken, and by the time anyone is down here they are looking
   for a link, not for the argument again.

   WHY THE GIVE IT ALL BANNER IS GONE
   It was a full-width tinted panel with a badge and two lines of copy,
   for a brand that reads as a men's event. Under a footer that serves
   two avatars it was the single loudest thing on the page, and it was
   shouting at half the audience. It is now one gold line in Daniel's
   column, which is the correct size for a sibling brand.

   ───────────────────────────────────────────────────────────────────
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   There is NO coral down here, on purpose. Coral marks the reader's
   move, and every ask has already been made: the nav carries a coral
   Contact pill on every page, and the page above closes with its own.
   GIVE IT ALL is gold, because it is his world, not your move.
   ═══════════════════════════════════════════════════════════════════ */
:root{
  --px-navy:#061938;
  --px-navy-deep:#04122A;
  --px-navy-ink:#030C1C;
  --px-gold:#E8C65F;
  --px-gold-lift:#F3DD93;
  --px-coral:#FF501F;
  --px-coral-lift:#FF6A3D;
  --px-cream:#F1ECE1;
  --px-mist:#B1BFD7;
  --px-slate:#7C89A3;
}

#pt-foot,#pt-foot *{box-sizing:border-box}
#pt-foot{
  background:#030C1C;
  border-top:1px solid rgba(232,198,95,.16);
  font-family:'Montserrat',system-ui,-apple-system,sans-serif;
  color:#B1BFD7;-webkit-font-smoothing:antialiased}
#pt-foot-inner{max-width:1360px;margin:0 auto;
  padding:clamp(34px,3.6vw,46px) clamp(20px,4vw,52px) 24px}

/* ═══ TIER 1 · IDENTITY + THE THREE COLUMNS ═══════════════════════
   Two doors and Daniel. The doors are the reader's two routes in;
   the third is everywhere Daniel himself shows up, which is what the
   GIVE IT ALL bar used to be doing at ten times the size. */
#pt-foot-top{
  display:grid;
  grid-template-columns:minmax(260px,1fr) repeat(3,minmax(0,.72fr));
  gap:clamp(30px,3.4vw,52px);
  align-items:start;
  margin-bottom:clamp(28px,3vw,36px)}
/* Identity spans the full width once the columns need the room. */
@media(max-width:1080px){#pt-foot-top{grid-template-columns:repeat(3,minmax(0,1fr));gap:38px 30px}
  #pt-ident{grid-column:1/-1}}
/* THREE COLUMNS ALL THE WAY DOWN. This used to fall to two at 680 and to
   one at 440, which on a 390px phone made the footer 878px tall, taller
   than most of the sections above it and by a long way the tallest thing
   nobody came for. Held at three it is 563px, a third shorter, and the
   two-up version was the worst of both: it put Daniel alone on a row and
   left half of it empty.

   Holding three also keeps the footer the same object it is on desktop.
   Three doors, three columns, whatever the device. The type comes down
   at the bottom breakpoint to buy the width, which is the right trade
   for a block that is scanned rather than read. */
@media(max-width:680px){#pt-foot-top{gap:30px 20px}}
@media(max-width:440px){
  #pt-foot-top{gap:22px 12px}
  .pt-door h4{font-size:.6rem;letter-spacing:.12em;margin-bottom:10px;padding-bottom:8px}
  .pt-door a{font-size:.78rem;line-height:1.28;padding:4px 0}
}

#pt-foot-logo{display:inline-block;line-height:0;margin-bottom:14px;text-decoration:none}
#pt-foot-logo img{height:30px;width:auto;display:block;
  font-size:.8rem;font-weight:700;color:#F1ECE1}

#pt-social{display:flex;gap:18px;flex-wrap:wrap;margin-top:16px}
#pt-social a{font-size:.85rem;font-weight:600;color:#B1BFD7;
  text-decoration:none;transition:color .2s ease}
#pt-social a:hover,#pt-social a:focus-visible{color:#E8C65F;outline:none}

/* ── THE DOORS. Deliberately larger than anything else down here. ── */
.pt-door h4{
  font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;
  font-weight:800;color:#A88F4E;margin:0 0 12px;
  padding-bottom:9px;border-bottom:1px solid rgba(232,198,95,.18)}
/* THE WHOLE BLOCK IS DELIBERATELY SHALLOW. Nothing down here is being
   sold. It exists so that seven otherwise-orphaned pages are reachable
   and so somebody hunting for terms or a podcast can find them. Every
   vertical value in this file was cut roughly a third for that reason,
   and the type went with it. If it starts feeling like a section again,
   it has grown.

   .92rem, not 1.02. At 1.02 these matched the page's body copy exactly,
   which made the footer read as another content section rather than as
   the foot of the page. They are still the largest thing down here, and
   still clearly larger than the utility row at .82, so the hierarchy the
   note above describes is intact. The size came off the type and went
   into padding instead, so the tap target grew rather than shrank. */
.pt-door a{
  display:block;color:#D7DEEA;font-size:.92rem;font-weight:500;
  line-height:1.3;padding:5px 0;margin-bottom:1px;text-decoration:none;
  border-radius:3px;transition:color .2s ease}
.pt-door a:first-of-type{padding-top:0}
.pt-door a:last-child{margin-bottom:0}
.pt-door a:hover,.pt-door a:focus-visible{color:#E8C65F;outline:none}

/* THE SIBLING BRAND, AS A LINE RATHER THAN A BANNER.
   It stays GOLD, because gold is Daniel and GIVE IT ALL is his room.
   That single colour is now the whole signal — it used to be a
   42px badge, a border, a tinted panel and two lines of body copy,
   which made a men's-retreat brand the loudest thing in a footer
   serving two avatars. */
.pt-door a.ext{color:#E8C65F;font-weight:600}
.pt-door a.ext:hover,.pt-door a.ext:focus-visible{color:#F3DD93}
.pt-door a.ext .mk{font-size:.8em;margin-left:.35em;opacity:.8}

/* ═══ TIER 2 · THE UTILITY ROW ════════════════════════════════════
   Everything the site has that is not a door. Small, inline, dot
   separated. This is the register of a footnote, and that is the
   point: it is here for the person who came looking for it, and
   invisible to the person who did not. */
#pt-util{
  display:flex;flex-wrap:wrap;align-items:center;
  gap:7px 0;
  padding-top:18px;border-top:1px solid rgba(241,236,225,.1);
  margin-bottom:14px}
#pt-util a{
  color:#6E7B94;font-size:.82rem;text-decoration:none;
  padding:3px 0;border-radius:3px;white-space:nowrap;
  transition:color .2s ease}
#pt-util a:hover,#pt-util a:focus-visible{color:#E8C65F;outline:none}
/* Separator TRAILS its link rather than leading the next one. With
   ::before, every line the row wraps onto opens with an orphaned dot.
   Trailing means a wrapped line starts on a word, which is what the
   eye expects, and the dot left at a line end reads as continuation. */
#pt-util a:not(:last-child)::after{
  content:"·";color:#3E4A63;margin:0 11px;display:inline-block}

/* ═══ TIER 3 · LEGAL ══════════════════════════════════════════════ */
#pt-legal{
  display:flex;justify-content:space-between;gap:16px;flex-wrap:wrap;
  color:#5E6B85;font-size:.8rem;line-height:1.6}
#pt-legal .links{display:inline-flex;gap:18px;flex-wrap:wrap}
#pt-legal a{color:inherit;text-decoration:none;
  border-bottom:1px solid rgba(232,198,95,.4);padding-bottom:1px;
  transition:color .2s ease,border-color .2s ease}
#pt-legal a:hover,#pt-legal a:focus-visible{
  color:#B1BFD7;border-bottom-color:#E8C65F;outline:none}

#pt-foot a:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:3px}
@media(prefers-reduced-motion:reduce){#pt-foot,#pt-foot *{transition:none!important}}

/* ══════════════════════════════════════════════════════════════════
   SITE NAV, BUILT IN RATHER THAN PLACED
   Same story as the footer below it. PtNav belongs in the Wix header
   strip so it is editable in one place for every page; it would not
   load there, so it ships inside this page.

   PtNav v3.dc.html stays the source of record. If the strip starts
   working, delete this block and the markup rather than maintaining
   two copies.

   ONE THING TO WATCH. The bar is position:fixed. Inside a shadow root
   that still resolves against the viewport, but ANY ancestor carrying
   transform, filter, perspective or will-change becomes its containing
   block instead, and the nav would scroll away with the page. If that
   happens in Wix, the culprit is a wrapper on Wix's side, not here.
   ══════════════════════════════════════════════════════════════════ */
/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · PRIMARY NAVIGATION · v3
   -------------------------------------------------------------------
   STRUCTURE (this is the whole argument of the redesign)
     LOGO          home. no "Home" text item — the mark does that job.
     FOR MEN   ▾   → The Reconnected Man
     FOR WOMEN ▾   → The Reconnected Woman
     ABOUT
     TESTIMONIALS
     CONTACT       coral pill, far right.

   WHY THE DOORS COME FIRST
   The sorting page's entire premise is that a visitor must choose a
   side before anything else can land. The nav has to agree with that.
   "For Men" and "For Women" are therefore the first and largest thing
   after the mark, and everything that is ABOUT DANIEL (About,
   Testimonials) sits after the reader's own doors, not before them.

   THE PARENTS ARE REAL LINKS
   "For Men" goes to the men's page; the dropdown is an addition, not
   the only way through. A nav item that can only be hovered is a dead
   end on touch and a dead end for keyboards.

   ───────────────────────────────────────────────────────────────────
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   Gold marks where you ARE (the active item, the hovered child).
   Coral marks the one thing you can DO (Contact). Nothing else in
   this bar is allowed to be coral — the moment a second thing is,
   the pill stops meaning anything.
   ═══════════════════════════════════════════════════════════════════ */
:root{
  --px-navy:#061938;
  --px-navy-deep:#04122A;
  --px-navy-ink:#030C1C;
  --px-gold:#E8C65F;
  --px-gold-lift:#F3DD93;
  --px-coral:#FF501F;
  --px-coral-lift:#FF6A3D;
  --px-cream:#F1ECE1;
  --px-mist:#B1BFD7;
  --px-slate:#7C89A3;
}

/* ═══ SHELL ═══════════════════════════════════════════════════════ */
#pt-nav,#pt-nav *{box-sizing:border-box}
#pt-nav{
  position:fixed;top:0;left:0;right:0;z-index:90;
  font-family:'Montserrat',system-ui,-apple-system,sans-serif;
  border-bottom:1px solid transparent;
  background:transparent;
  transition:background .4s ease,border-color .4s ease,backdrop-filter .4s ease;
  -webkit-font-smoothing:antialiased;
}
/* Navy on scroll. NEVER black. */
#pt-nav.is-stuck{
  background:rgba(6,25,56,.92);
  backdrop-filter:blur(16px);
  -webkit-backdrop-filter:blur(16px);
  border-bottom-color:rgba(232,198,95,.16);
}
#pt-bar{
  max-width:1360px;margin:0 auto;
  padding:16px clamp(20px,4vw,52px);
  display:flex;align-items:center;gap:clamp(16px,2.2vw,28px);
  position:relative;
}

/* ═══ LOGO ════════════════════════════════════════════════════════ */
#pt-logo{flex:0 0 auto;display:flex;align-items:center;line-height:0;
  border-radius:6px;text-decoration:none;transition:opacity .25s ease}
#pt-logo:hover{opacity:.82}
#pt-logo:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:6px}
/* If the CDN ever fails, the alt text renders as ordinary cream wordmark
   text rather than a giant blue underlined link. */
#pt-logo img{height:30px;width:auto;display:block;
  font-size:.82rem;font-weight:700;letter-spacing:.02em;color:#F1ECE1}
@media(max-width:600px){#pt-logo img{height:26px}}

/* ═══ LINK RAIL ═══════════════════════════════════════════════════ */
#pt-links{
  display:flex;align-items:center;gap:clamp(20px,2.4vw,34px);
  margin-left:auto;
}
.pt-item{position:relative;display:flex;align-items:center}
.pt-link{
  display:inline-flex;align-items:center;gap:.35em;
  font-size:.9rem;font-weight:600;letter-spacing:.005em;
  color:#B1BFD7;white-space:nowrap;text-decoration:none;
  padding:6px 0;border-radius:4px;
  transition:color .2s ease;
}
.pt-link:hover,.pt-link:focus-visible{color:#F1ECE1;outline:none}
.pt-link:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:4px}

/* WHERE YOU ARE. Gold, plus a rule under it so it survives being
   read by someone who cannot separate gold from mist. */
.pt-item.is-active > .pt-link,
.pt-link.is-active{color:#E8C65F}
.pt-item.is-active > .pt-link::after,
.pt-link.is-active::after{
  content:"";position:absolute;left:0;right:0;bottom:-2px;height:2px;
  background:rgba(232,198,95,.75);border-radius:2px;
}
.pt-link{position:relative}

/* ═══ CARET + DROPDOWN ════════════════════════════════════════════ */
.pt-caret{
  background:none;border:none;padding:6px 2px;margin:0;
  font-family:inherit;font-size:.72em;line-height:1;
  color:inherit;cursor:pointer;
  display:inline-flex;align-items:center;
  transition:transform .25s ease,color .2s ease;
  color:#7C89A3;
}
.pt-item:hover .pt-caret{color:#B1BFD7}
.pt-caret:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:3px;border-radius:4px}
.pt-item:hover .pt-caret,
.pt-item.is-open .pt-caret{transform:rotate(180deg)}

.pt-menu{
  position:absolute;top:100%;left:50%;
  transform:translateX(-50%);
  min-width:252px;
  display:none;flex-direction:column;gap:2px;
  margin-top:14px;
  background:rgba(4,18,42,.98);
  backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);
  border:1px solid rgba(232,198,95,.18);
  border-radius:14px;padding:10px;
  box-shadow:0 30px 60px -20px rgba(3,12,28,.8);
}
/* The 14px gap between the item and the menu would break a hover
   chain, so the bridge below keeps the pointer "inside" while it
   crosses. Without it the menu flickers shut mid-travel. */
.pt-menu::before{content:"";position:absolute;top:-16px;left:0;right:0;height:16px}

/* Desktop opens on hover AND on keyboard focus. No JS involved. */
@media(min-width:1024px){
  .pt-item:hover .pt-menu,
  .pt-item:focus-within .pt-menu,
  .pt-item.is-open .pt-menu{display:flex}
}
.pt-menu a{
  padding:11px 14px;border-radius:9px;
  font-size:.88rem;font-weight:500;color:#D7DEEA;
  white-space:nowrap;text-decoration:none;
  transition:background .2s ease,color .2s ease;
}
.pt-menu a:hover,.pt-menu a:focus-visible{
  background:rgba(232,198,95,.1);color:#E8C65F;outline:none}
.pt-menu a:focus-visible{box-shadow:inset 0 0 0 1px rgba(232,198,95,.55)}
.pt-menu .pt-sub{
  display:block;font-size:.63rem;letter-spacing:.18em;text-transform:uppercase;
  font-weight:800;color:#7C89A3;margin-bottom:3px}
.pt-menu a:hover .pt-sub{color:rgba(232,198,95,.72)}

/* ═══ CONTACT · THE ONLY CORAL IN THE BAR ═════════════════════════ */
#pt-cta-wrap{display:flex;align-items:center;gap:14px;flex:0 0 auto;margin-left:clamp(8px,1.6vw,20px)}
#pt-cta{
  display:inline-flex;align-items:center;gap:.5em;
  background:#FF501F;color:#FFFFFF;
  font-weight:700;font-size:.86rem;
  padding:.72em 1.45em;border-radius:999px;
  white-space:nowrap;text-decoration:none;
  transition:background .25s ease,transform .25s ease,box-shadow .25s ease;
}
#pt-cta:hover,#pt-cta:focus-visible{
  background:#FF6A3D;transform:translateY(-1px);
  box-shadow:0 10px 24px -10px rgba(255,80,31,.7);outline:none}
#pt-cta:focus-visible{outline:2px solid #F3DD93;outline-offset:3px}
#pt-cta .arrow{font-size:.9em;transition:transform .25s ease}
#pt-cta:hover .arrow{transform:translateX(3px)}

/* ═══ BURGER ══════════════════════════════════════════════════════ */
#pt-burger{
  display:none;background:none;border:none;cursor:pointer;
  flex-direction:column;justify-content:center;gap:5px;
  padding:8px;margin-right:-8px;border-radius:6px}
#pt-burger:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:2px}
#pt-burger span{display:block;width:24px;height:2px;background:#F1ECE1;border-radius:2px;
  transition:transform .3s ease,opacity .2s ease}
#pt-nav.menu-open #pt-burger span:nth-child(1){transform:translateY(7px) rotate(45deg)}
#pt-nav.menu-open #pt-burger span:nth-child(2){opacity:0}
#pt-nav.menu-open #pt-burger span:nth-child(3){transform:translateY(-7px) rotate(-45deg)}

/* ═══ MOBILE ══════════════════════════════════════════════════════ */
@media(max-width:1023px){
  #pt-burger{display:flex}
  #pt-nav{background:rgba(6,25,56,.92);backdrop-filter:blur(16px);
    -webkit-backdrop-filter:blur(16px)}

  #pt-links{
    position:absolute;top:100%;left:0;right:0;
    flex-direction:column;align-items:stretch;gap:2px;
    margin-left:0;
    /* SOLID, AND NO BACKDROP FILTER ON THIS ONE. The bar above keeps its
       blur; this panel cannot have one. Nested backdrop-filters inside a
       shadow root make Chromium composite this element against an already
       filtered backdrop, and the page behind reads straight through a
       background that is 98.5% opaque. On the built-in version of this nav
       the hero headline was legible through the open menu. The bar's own
       blur still gives the effect where it matters. */
    background:#061938;
    border-bottom:1px solid rgba(232,198,95,.16);
    padding:10px clamp(16px,4vw,44px) 22px;
    max-height:calc(100vh - 68px);overflow-y:auto;
    display:none;
  }
  #pt-nav.menu-open #pt-links{display:flex}

  .pt-item{flex-wrap:wrap;justify-content:space-between;
    border-bottom:1px solid rgba(241,236,225,.07)}
  .pt-item:last-child{border-bottom:none}
  .pt-link{flex:1 1 auto;padding:15px 2px;font-size:1rem}
  .pt-item.is-active > .pt-link::after,.pt-link.is-active::after{display:none}
  .pt-caret{flex:0 0 auto;padding:15px 10px;font-size:.85em}

  /* Child list is a plain indented block on mobile. A floating panel
     inside a panel is a place to get lost. */
  .pt-menu{
    position:static;transform:none;min-width:0;width:100%;
    margin:0 0 12px;padding:0 0 0 14px;
    background:transparent;backdrop-filter:none;-webkit-backdrop-filter:none;
    border:none;border-left:2px solid rgba(232,198,95,.28);
    border-radius:0;box-shadow:none;
    flex-basis:100%;
  }
  .pt-menu::before{display:none}
  .pt-item.is-open .pt-menu{display:flex}
  .pt-menu a{padding:12px 12px;font-size:.94rem}

  #pt-cta-wrap{margin-left:auto}
  #pt-cta{font-size:.8rem;padding:.68em 1.2em}
  #pt-cta .cta-long{display:none}
}
@media(max-width:420px){
  #pt-cta{padding:.66em 1.05em}
  #pt-cta .arrow{display:none}
}

@media(prefers-reduced-motion:reduce){
  #pt-nav,#pt-nav *{transition:none!important}
}`;

  var HTML = `<div id="px-root">

<header id="pt-nav">
  <div id="pt-bar">

    <!-- ══ LOGO = HOME. No text "Home" item; the mark is the link. ══ -->
    <a id="pt-logo" href="https://www.parallaxxtransformations.com" aria-label="Parallaxx Transformations — home">
      <img src="https://static.wixstatic.com/media/e1784d_fe3c841c471f47d088f0cd631a89d883~mv2.png"
           alt="Parallaxx Transformations">
    </a>

    <nav id="pt-links" aria-label="Primary">

      <!-- ══ FOR MEN ══ parent is a real link to the men's page ══ -->
      <div class="pt-item pt-has-drop" data-nav="men">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/men">For Men</a>
        <button class="pt-caret" type="button" aria-label="Show For Men menu" aria-expanded="false" aria-controls="pt-menu-men">&#9662;</button>
        <div class="pt-menu" id="pt-menu-men">
          <a href="https://www.parallaxxtransformations.com/the-reconnected-man">
            <span class="pt-sub">Programme</span>The Reconnected Man</a>
        </div>
      </div>

      <!-- ══ FOR WOMEN ══ -->
      <div class="pt-item pt-has-drop" data-nav="women">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/women">For Women</a>
        <button class="pt-caret" type="button" aria-label="Show For Women menu" aria-expanded="false" aria-controls="pt-menu-women">&#9662;</button>
        <div class="pt-menu" id="pt-menu-women">
          <a href="https://www.parallaxxtransformations.com/the-reconnected-woman">
            <span class="pt-sub">Programme</span>The Reconnected Woman</a>
        </div>
      </div>

      <div class="pt-item" data-nav="about">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/about-daniel-lawson">About</a>
      </div>

      <div class="pt-item" data-nav="testimonials">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">Testimonials</a>
      </div>

    </nav>

    <div id="pt-cta-wrap">
      <!-- CONTACT. Coral pill — Parallaxx-only accent. GIA uses a gold
           pill in this exact position: same shape, different colour.
           That is the whole family/identity trick. -->
      <a id="pt-cta" data-nav="contact" href="https://www.parallaxxtransformations.com/contact-daniel-lawson">
        Contact<span class="cta-long">&nbsp;</span><span class="arrow" aria-hidden="true">&#8594;</span>
      </a>
      <button id="pt-burger" type="button" aria-label="Menu" aria-expanded="false" aria-controls="pt-links">
        <span></span><span></span><span></span>
      </button>
    </div>

  </div>
</header>

<!-- ══════════════════════════════════════════════════════════════
     01 · THE HERO
     His hero is a diagnosis he already half believes, because his wife
     said it first. Nobody has said anything to her, so hers states a
     fact about the arrangement instead of a verdict on her.

     COPY LOCKED 1 AUG 2026. Do not soften "still behind" into "still
     feeling behind". Behind is a position, not a mood, and position is
     what she cares about. Do not reinstate a scroll-cue question: the
     hero ends on her own sentence, unanswered, and section 02 answers it.

     THE WIPE IS GONE ON PURPOSE. His page fogs the glass and lets him
     rub it clear, because his problem is that he cannot see. Hers is
     the opposite. She sees all of it and can name all of it. What she
     has never had is a criterion, so "rub harder for a better view" is
     the exact argument this page exists to dismantle. The pane clears
     as she scrolls instead. At her pace, without pushing.
     ══════════════════════════════════════════════════════════════ -->
<section id="px-hero">
  <div id="px-fallback" style="display:block"><div class="fb-img" id="px-fb-img"></div><div class="fb-glass"></div></div>
  <div id="px-hero-veil"></div>

  <!-- ▸ IMAGE 01 — DANIEL, AND HE IS THE ONLY PERSON ON THIS PAGE.
       Not a stock woman, not a generative one, and not the metaphor.
       This page sells one hour with one man, so the trust chain is him
       and it should start in the first screen rather than at section 06.

       THE FRAME. On his page he is the man behind the glass and it turns
       out to be a mirror. She does not see herself in him, so the glass
       means something else here: he ran her machine and got out of it.
       The fog is on HER side. He is already through.

       THE ONE THING THAT MUST NOT HAPPEN. He is not looking at the
       camera. A man addressing her through glass on a women's page is a
       man about to explain something, which is the fatal read. Three
       quarter or profile, mid conversation, listening. -->
  <!-- The plate is in: hero-daniel-through.jpg, graded and set as the default
       on #px-fb-img below. Swap it by overriding --px-hero-img, nothing else. -->

  <div id="px-hero-copy">
    <div class="inner">
      <h1 class="px-serif" style="font-size:clamp(30px,3.9vw,52px);line-height:1.14;color:#F1ECE1;margin-bottom:22px">
        <span class="px-line"><span style="font-weight:500">You've hit every target.</span></span>
        <span class="px-line"><span style="font-style:italic;font-weight:300;font-size:.86em;color:#B1BFD7">And you're still behind.</span></span>
      </h1>
      <p class="px-fade px-serif" style="max-width:32ch;color:#DDE5F0;font-size:1.22rem;font-weight:300;font-style:italic;margin-bottom:28px">
        "Everything is a priority right now."
      </p>
      <div class="px-fade" style="pointer-events:auto;display:flex;flex-wrap:wrap;gap:14px;align-items:center">
        <a class="px-btn" href="#px-audit-sec">Take the Priority Audit <span aria-hidden="true">→</span></a>
        <span style="font-size:.78rem;color:#5E6B85;line-height:1.5;display:inline-block">
          Ninety seconds.<br>Answer&rsquo;s on the screen.</span>
      </div>

      <!-- THE QUALIFIER. Added 3 Aug 2026. The men's hero has always carried
           one and hers had nothing doing that job. A qualifier that filters
           people OUT is what makes the ones left feel found. Same three words
           as the women's door on the home page, so the door quotes its own
           page rather than inventing. Men's equivalent, same date:
           "Relationship. Years in. Care about her." -->
      <p class="px-fade" style="font-size:.78rem;line-height:1.6;color:#5E6B85;margin-top:20px;letter-spacing:.02em">
        Independent. Capable. Successful.
      </p>
    </div>
  </div>

  <div id="px-hero-cue" class="px-fade" aria-hidden="true">↓</div>

</section>


<!-- ══════════════════════════════════════════════════════════════
     02 · THE MIRROR
     Same mechanic as his, same phone, same typing. Different hour and
     different excuses. His run at 11:40pm. Hers run on a Sunday
     afternoon, which is the only reliably quiet slot in her calendar
     and the hour the week's shape arrives.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-mirror" style="background:#0A1D3C">
  <div class="px-wrap">
    <div id="px-mirror-grid" style="display:grid;grid-template-columns:minmax(280px,380px) 1fr;gap:clamp(32px,5vw,72px);align-items:stretch">

      <div class="px-fade" id="px-phone-col">
        <div class="px-phone">
          <div class="px-screen">
            <div class="px-notch"></div>
            <div class="px-thread-hd">
              <div>
                <div style="font-weight:700;font-size:12px;color:#1F2A3D">Sunday · 4:12 pm</div>
                <div style="font-size:10px;color:#8A8580">the part of you that already knows all of this</div>
              </div>
            </div>
            <div id="px-window" data-lenis-prevent tabindex="0">
              <div id="px-tape">
                <div class="px-out"><div><div class="b">It's just a busy season right now.</div></div></div>
                <div class="px-in"><div><div class="px-bub">You've said that for six years.</div></div></div>

                <div class="px-out"><div><div class="b">Besides, I'm not overworked. I'm in demand.</div></div></div>
                <div class="px-in"><div><div class="px-bub">It's the only proof you'll accept that you're any good.</div></div></div>

                <div class="px-out"><div><div class="b">And they really need me on this one.</div></div></div>
                <div class="px-in"><div><div class="px-bub">They like you because you don&rsquo;t decline.</div></div></div>

                <div class="px-out"><div><div class="b">I'm doing more than anyone.</div></div></div>
                <div class="px-in"><div><div class="px-bub">Yes. If you did less, it would have to be more intentional.</div></div></div>

                <div class="px-out"><div><div class="b">It's the way things are. I can't change it.</div></div></div>
                <div class="px-in"><div><div class="px-bub">You choose not to. Changing it means deciding what you actually want.</div></div></div>

                <div class="px-out"><div><div class="b">I'd love a week with nothing in it.</div></div></div>
                <div class="px-in"><div><div class="px-bub">You had four days in March. Second day you were already fixing something.</div></div></div>

                <div class="px-out"><div><div class="b">I'd love more time with friends and family.</div></div></div>
                <div class="px-in"><div><div class="px-bub">You hope they cancel first. Then it isn't you who did it.</div></div></div>

                <div class="px-out"><div><div class="b">I don't have expectations of people anyway.</div></div></div>
                <div class="px-in"><div><div class="px-bub">You do. You just stopped saying them, so nobody gets to turn you down.</div></div></div>

                <div class="px-out"><div><div class="b">I'm not unhappy.</div></div></div>
                <div class="px-in"><div><div class="px-bub">No. You're bored. And you don't know what you'd want instead.</div></div></div>

                <div class="px-out"><div><div class="b">So what, you want me to work less?</div></div></div>
                <div class="px-in last"><div><div class="px-bub">No. You've just never once been the one who decides when it's enough.</div></div></div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div>
        <h2 class="px-serif" style="font-size:clamp(26px,3.2vw,44px);line-height:1.18;color:#F1ECE1;margin-bottom:26px;text-wrap:balance">
          <span class="px-line"><span style="font-weight:500">Your responses are reasonable.</span></span>
          <span class="px-line"><span style="font-style:italic;font-weight:300;font-size:.86em;color:#B1BFD7">And why they've remained the same.</span></span>
        </h2>
        <p class="px-fade" style="max-width:52ch;margin-bottom:16px">
          It's all reasonable. And that's why it's easy to brush off. None of it is news to you. The same
          conversation you have had with yourself in a quiet moment before, gone just as fast with the next
          accepted task.
        </p>
        <p class="px-fade" style="max-width:52ch;margin-bottom:16px">
          You resolve other people's priorities for a living, and without prioritising your own it becomes
          even more to carry, which feels like a failing to simply put it down.
        </p>
        <!-- THE COMMA IN THE NEXT LINE IS DELIBERATE. It is not a typo and it
             is not standard punctuation. It forces the breath before the
             answer, which is the whole reason the line lands. Do not tidy it.

             It also closes the section: the H2 says nobody has ever asked her
             a second question, and this says nobody has asked her to stop.
             Same verb, top and tail. -->
        <p class="px-fade px-serif" style="max-width:52ch;margin-bottom:22px;font-size:1.24rem;color:#F1ECE1;font-weight:400">
          So you don't. And nothing changes.
        </p>

        <div class="px-darkbox px-fade" style="background:rgba(255,255,255,.04);border-left:2px solid #E8C65F;padding:24px 26px;border-radius:0 18px 18px 0;margin-bottom:30px">
          <p class="px-hand" style="color:#E8C65F;font-size:1.1rem;margin-bottom:10px">"How's it all going?"</p>
          <p class="px-serif" style="font-size:1.3rem;color:#F1ECE1;font-weight:500;margin-bottom:12px">Busy. Good busy.</p>
          <p style="font-size:.92rem;margin-bottom:12px">It is the most efficient sentence you own. It answers
            the question, it ends the conversation, and nobody ever hears the longer version.</p>
          <p class="px-serif" style="font-size:1.06rem;color:#F1ECE1;font-weight:400">The truth stays where
            you've always kept it.</p>
        </div>

        <!-- ▸ SLOT LEFT DELIBERATELY EMPTY.
             His mirror runs into "stories perfected from young". Hers cannot.
             The origin fork is open in W02 2.1 and nobody has said where the
             condition was set. Do not write it. Ask somebody. -->

        <p class="px-fade" style="font-size:.92rem;color:#8FA0BC">
          Read that back and <span style="color:#E8C65F">count the people you could say it to.</span>
        </p>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     03 · WHAT IT HAS ALREADY COST
     The cost, in the one unit she cannot argue with. See the
     stylesheet note for what stood here before and why it went.
     ══════════════════════════════════════════════════════════════ -->
<section id="px-cost" class="px-sec" style="background:#F1ECE1">
  <div class="px-wrap">
    <div style="max-width:640px;text-align:left;margin:0 0 clamp(28px,3.5vw,42px)">
      <h2 class="px-serif px-fade" style="font-size:clamp(26px,3.1vw,42px);line-height:1.16;color:#1F2A3D;font-weight:500;margin:0;text-wrap:balance">
        The goalposts move because you keep moving them.
      </h2>
    </div>

    <div id="px-cost-cols">
      <div class="px-fade">
        <p style="color:#5A6274;line-height:1.68;margin:0 0 16px">
          You put your desires into the future. When the launch is done. When the team settles.
          Then I&rsquo;ll book it, take the weekend, call her back.
        </p>
        <p style="color:#5A6274;line-height:1.68;margin:0 0 26px">
          All the while training yourself to live in the future.
        </p>
        <p class="px-serif px-pull">
          Arrival becomes impossible, because the moment you hit the target you are already
          onto the next one.
        </p>
      </div>

      <div class="px-fade">
        <p class="px-label" style="color:#A08A5E;margin-bottom:4px">Already spent</p>
        <ul class="px-cost-list">
          <li>Six years of <i>&ldquo;after this one.&rdquo;</i></li>
          <li>Hobbies you used to have. <i>What were they again?</i></li>
          <li>Invitations from friends have gone quiet.</li>
          <li>Days off that go on recovery, never on enjoyment.</li>
          <li>Everything you want is something for someone else.</li>
        </ul>
      </div>
    </div>

    <div class="px-fade" id="px-cost-fwd">
      <div>
        <p class="px-label" style="color:#A08A5E;margin-bottom:14px">From here</p>
        <p style="color:#5A6274;line-height:1.68;margin:0">
          The problem is, nothing dramatic happens next. Five years on you are more experienced,
          more relied on, and better at it. Your list is longer, the recovery is shorter, and you
          are still saying it has been a busy few months.
        </p>
      </div>
      <div>
        <p class="px-serif px-verdict">
          You will come back to yourself at some point. Everyone does. The only question is
          whether it happens by choice or by collapse.
        </p>
        <p style="color:#5A6274;line-height:1.68;margin:0">
          Collapse does not hand the choice back. This was never about being tired. It is about
          where the next five years go, and right now they are <b>already booked</b>.
        </p>
      </div>
    </div>
  </section>


<!-- ══════════════════════════════════════════════════════════════
     WHERE THE BAR CAME FROM
     The safest paragraph on the page and therefore the first argument
     she meets. Every line is Julia's. It asks her to concede nothing,
     which is why it can go before anything about her.

     THERE WAS AN INTERACTIVE HERE AND IT WAS CUT ON PURPOSE.
     A drag-the-bar-up toy that lifted the line every time she reached
     it. It proved a claim she accepts for free, it was abstract in a
     register that has to stay concrete, it produced no data about her,
     and being walked into a rigged game reads as being handled to a
     woman who dislikes being handled. One interactive on this page,
     and it is the sort. Do not put it back.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" style="background:#04122A">
  <div class="px-wrap">
    <h2 class="px-serif" style="font-size:clamp(26px,3.2vw,44px);line-height:1.18;color:#F1ECE1;margin-bottom:30px">
      <span class="px-line"><span style="font-weight:500">You were conditioned for this.</span></span>
    </h2>
    <!-- ▸ IMAGE 02 — DANIEL, AT HOME, AFTER EVERYONE ELSE STOPPED.
         The section's last line is "only now there's nobody left moving
         it but you", and that is the line the picture has to serve, not
         the first paragraph. A corporate office would date the whole
         thing to Daniel's past and hand the woman who has already gone
         independent a clean way out: that isn't me any more.

         So: his own space, colonised by work. Alone in an office at
         nine is ordinary. Alone at your own table at nine means
         everybody else in the house stopped and you didn't.

         The deferred object in frame is what makes it a demonstration
         rather than a portrait. Section 02 is about a thing that keeps
         being put off, and here it is sitting three feet away from him,
         not being touched. -->
    <div id="px-machine-cols">
      <div class="px-fade">
        <img id="px-machine-img" alt="Daniel working at his own table late in the evening, the rest of the house dark behind him"
             src="https://static.wixstatic.com/media/111174_2bcac7e4975a4a7685ec8662d7ff67ac~mv2.jpg"
             width="1200" height="1500" loading="lazy" decoding="async">
      </div>
      <div>
        <p class="px-fade">
          The corporate world has a word for it. <span style="color:#E8C65F">Insecure overachiever.</span>
          They didn&rsquo;t put it there. They recognised it, priced it, and hired it. Someone who keeps
          running, because the performance covers everything underneath it. They&rsquo;re the most
          profitable people in the building.
        </p>
        <p class="px-fade" style="margin-top:18px">
          Ninety nine percent has never been enough, so you go for a hundred and two. You keep exceeding
          it, so every year the number goes up. It&rsquo;s by design. Why would anyone interrupt someone
          who keeps delivering?
        </p>
        <!-- The exit does not end the arrangement, so this sits with the
             argument rather than in a band underneath it. The last line
             is the one the section is built to land on and is set as
             display type. It also runs the right column down to the foot
             of the plate, which is why the section reads as two even
             columns rather than a tall picture and a short paragraph. -->
        <div class="px-fade px-mach-close">
          <p style="margin-bottom:16px">
            Maybe you already left. Went out on your own, or you&rsquo;re halfway there.
          </p>
          <p class="px-serif" style="font-size:clamp(19px,2.1vw,27px);line-height:1.32;
             color:#F1ECE1;font-weight:400;margin:0">
            The target still moves. Only now there&rsquo;s nobody left moving it but you.
          </p>
        </div>
      </div>
    </div>

    <!-- Daniel's only first-person line before section 06, and the one
         place gold is used in this section. Full width and centred so
         it reads as him stepping in front of the argument rather than
         as a caption to the picture. -->
    <!-- One sentence per inline-block, so the quote breaks BETWEEN the
         two sentences first and only wraps inside one of them when the
         screen is genuinely too narrow. A plain max-width cannot
         guarantee two lines, because the marker face is not loaded at
         layout time and its metrics differ from the fallback. -->
    <p class="px-hand px-fade" id="px-machine-quote">
      <span>&ldquo;I was the most profitable person in that building.</span>
      <span>It took me another two years to work out that was the problem.&rdquo;</span>
    </p>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     04 · THE PRIORITY AUDIT — the CTA
     The counterpart to the men's five-archetype panel. Same job in the
     page, opposite construction.

     WHY THIS DOES NOT NAME THE THREE PILLARS. His panel shows all five
     archetypes because the archetype IS the payoff, and he wants to be
     told which one he is. Hers hides needs / boundaries / emotions
     until after item fifteen, so that the framework arrives as
     something her own answers produced rather than something she was
     introduced to. Naming them here spoils the only turn the
     instrument has.

     SO THE DISPLAY ELEMENT IS THE STATEMENTS THEMSELVES. Recognition
     beats abstraction, and one of them will land before she has
     decided whether she is interested.

     AND THE FIRST ITEM IS LIVE. She answers it here. Any tap carries
     the answer into the audit, so the activation cost of the whole
     thing is one tap on a statement she has already recognised.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-audit-sec" style="padding:0">
<div id="pa-root">

<!-- ══════════════════════════════════════════════════════════════
     01 · INTRO, page version
     The standalone opens on a Start button. This page does not need
     one: the first statement is live here, and answering it starts the
     run in place. That single tap is the whole activation cost, and
     spending it on a page load was the largest leak in the funnel.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen is-on on-cream" id="pa-intro">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">
        <p class="pa-hand">ninety seconds for what is a priority right now</p>
        <h2 class="pa-serif">Most people are staying busy and distracted from what is most important.</h2>
        <p class="lede">Rate how often each of the following fifteen statements has been true over the
          past six months, to unlock which priority would have the most immediate impact in your life right now.</p>

        <div id="pa-first">
          <p class="pa-label">1 of 15</p>
          <div class="pa-live-row">
            <p class="q" id="pa-first-q">I say yes even when at personal capacity.</p>
            <div class="pa-scale" id="pa-first-scale" role="radiogroup" aria-labelledby="pa-first-q"></div>
          </div>
          <p class="pa-promise">Instant access to three scores for where you might be carrying more than you
            need to be. One small thing to try differently this week, and three questions worth reflecting
            on, specific to your result.</p>
          <p class="pa-fine">Answer the first one and the rest follows here. Nothing lands in your inbox.</p>
        </div>
      </div>
    </div>
  </div>
</section>


<section class="pa-screen on-cream" id="pa-items">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">

        <div class="pa-prog">
          <div class="pa-prog-top">
            <span class="pa-prog-count" id="pa-count"><b>1</b> of 15</span>
          </div>
          <div class="pa-prog-bar"><div class="pa-prog-fill" id="pa-fill"></div></div>
        </div>

        <p class="pa-ask">How often, these past six months</p>
        <h2 class="pa-serif" id="pa-stmt"></h2>

        <div class="pa-scale" id="pa-scale" role="radiogroup" aria-labelledby="pa-stmt"></div>

        <div class="pa-navrow">
          <button class="pa-quiet" type="button" id="pa-back">&larr; Back</button>
          <span class="pa-hint">Arrow keys move, Enter selects, or press 1 to 5</span>
        </div>

        <p class="sr" id="pa-live" role="status" aria-live="polite"></p>

      </div>
    </div>
  </div>
</section>

<section class="pa-screen on-cream" id="pa-turn">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">
        <p class="pa-label pa-fadein" style="color:#A08A5E">Before your result</p>
        <h2 class="pa-serif pa-fadein" style="margin-top:9px">Our approach to life is built on three pillars.</h2>

        <div id="pa-turn-grid">

          <div class="pa-ring pa-fadein">
            <svg viewBox="0 0 240 240" aria-hidden="true" focusable="false">
              <path class="arc a1" d="M 130.03 24.53 A 96 96 0 0 1 207.70 159.05"></path>
              <path class="arc a2" d="M 197.67 176.43 A 96 96 0 0 1 42.33 176.43"></path>
              <path class="arc a3" d="M 32.30 159.05 A 96 96 0 0 1 109.97 24.53"></path>
              <text class="mid" x="120" y="128">Self-Esteem</text>
            </svg>
          </div>

          <div class="pa-defs">
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Needs</h3>
              <p>What you require to operate at your potential. Met with consistency, care and respect.</p>
            </div>
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Boundaries</h3>
              <p>What you accept, and what you are able to decline. Honoured, including by you.</p>
            </div>
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Emotions</h3>
              <p>What you feel, as an internal compass that highlights your needs and your boundaries.</p>
            </div>
          </div>

        </div>

        <p class="after pa-fadein">These are not three skills to go and develop. They are three dimensions of self that were supposed to be met for you, consistently, by somebody else, a long time ago. Most of us got some of them. Whatever was missing, you found another way to fulfil, and in your case that way has worked extremely well.</p>
        <p class="after pa-fadein" style="margin-top:14px">All three hold each other up. We are only as equipped as the weakest one.</p>

        <div class="cta pa-fadein">
          <button class="pa-btn" type="button" id="pa-see">See your three <span aria-hidden="true">&rarr;</span></button>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="pa-screen on-cream" id="pa-result">

  <div class="pa-r-head">
    <div class="pa-wrap"><div class="pa-col">
      <p class="pa-label pa-gold-label">Your three</p>
      <h2 class="pa-serif">Your three foundational pillars of self.</h2>

      <p class="pa-key">Each bar shows how far that pillar is from being met in the way that would support you best. The lower the score, the more developed that pillar already is. A higher score means less practice and less focus have gone into protecting and developing that part of yourself.</p>

      <div class="pa-bars" id="pa-bars"></div>
      <p class="pa-flat" id="pa-flat" hidden></p>
    </div></div>
  </div>

  <!-- THE THROUGH LINE. Shown to everyone, before any personal copy,
       because it is the finding and it is not conditional on a score. -->
  <div class="pa-wrap"><div class="pa-col">
    <div class="pa-rule"><span>What is underneath all three</span></div>
    <div class="pa-prose">
      <p>No single response to any of the fifteen statements means much on its own, which is why this is hard to see. Combined, they reveal a pattern, and the pattern gives you clues about the outcomes in your life.</p>
      <p>The three pillars together are what fuel our self-esteem. Left underdeveloped, they leave self-esteem as something that happens to you, depending on how the quarter went. It can go the other way too. Developing yourself through the three pillars you just scored stops life outcomes being a matter of chance and makes them a matter of intentional design.</p>
    </div>

    <div class="pa-rule"><span>Where the pressure is</span></div>
    <p class="pa-pillar-name" id="pa-top-name"></p>
    <div class="pa-prose" id="pa-top-lead"></div>
    <div class="pa-inset">
      <h4>The cost</h4>
      <p id="pa-top-cost"></p>
    </div>

    <div class="pa-rule"><span>What is feeding it</span></div>
    <div class="pa-prose" id="pa-second"></div>
    <div class="pa-gap"></div>
  </div></div>
</section>

<section class="pa-screen on-cream" id="pa-her">
  <div class="pa-wrap"><div class="pa-col">

    <div class="pa-rule"><span>What you already hold</span></div>
    <p class="pa-pillar-name" id="pa-low-name"></p>
    <div class="pa-prose" id="pa-low-body"></div>

    <div class="pa-rule"><span>The first move</span></div>
    <div class="pa-inset">
      <h4 id="pa-move-head">One thing, designed on purpose</h4>
      <p id="pa-move"></p>
    </div>

    <div class="pa-rule"><span>Three questions for you</span></div>
    <div id="pa-questions"></div>
    <p class="pa-note">No right answers, and nothing here is submitted anywhere. Screenshot it, or bring it to the conversation.</p>

  </div></div>
</section>

<section class="pa-screen on-cream" id="pa-close">
  <div class="pa-wrap"><div class="pa-col">
    <div class="pa-rule"><span>Where this sits</span></div>
    <div class="pa-prose pa-sys">
      <p>Reconnect runs on three dimensions. <b>Vision</b>, how you would like things to be. <b>Values</b>, who you need to become to live that vision. <b>Velocity</b>, how you move the pieces into place to make it happen. What you just scored sits inside values, and these are changes you can start making right away.</p>
    </div>

    <div class="pa-bridge">
      <p>Needs, boundaries and emotions are not three problems to fix. They are the three dimensions every one of these conversations turns out to be about, and most people are not giving them enough attention, with intention.</p>
      <p>What they add up to is being able to handle the ambiguity and the mistakes and not having it all together, without any of it touching what you think you are worth. Worth that does not need to be won, and is not measured by how much you do, because what you do is built on real self-esteem.</p>
      <p>That is the work. And it is not left to chance.</p>
    </div>

    <div class="pa-close-cta">
      <a class="pa-btn" id="pa-cta" href="#">Bring this to a conversation <span aria-hidden="true">&rarr;</span></a>
      <button class="pa-ghost" type="button" id="pa-pdf">Save your result as a PDF</button>
    </div>
    <p class="pa-close-sub">One conversation, start from here.</p>

    <div class="pa-restart">
      <button class="pa-quiet" type="button" id="pa-again">Start again</button>
    </div>
  </div></div>
</section>

</div><!-- /pa-root -->
</section>


<!-- ══════════════════════════════════════════════════════════════
     05 · THE SYSTEM
     THIS SECTION EXISTS TO JOIN TWO TRIADS. The audit measures needs,
     boundaries and emotions. The coaching runs on vision, values and
     velocity. Two different threes, one screen apart, and until this
     section said so a reader had to work out for herself which one she
     had just been told about. The join is the point of the section:
     the audit is a piece of Values.

     WHY IT SITS AFTER THE AUDIT AND NOT BEFORE. Same reason the
     pillars stay hidden until item fifteen. She does a fragment, then
     sees the whole. Moving this above the instrument turns it into an
     introduction to a framework, which is the one order that does not
     work on this avatar.

     AND THE FAILURE STATES CARRY IT, NOT THE DEFINITIONS. The men's
     page proves the model by letting him watch the glass stay dirty.
     There is no interactive here on purpose, so the missing state of
     each dimension has to be written out. She will recognise herself
     in one of the three immediately, which the definitions alone
     never achieve.

     VALUES GETS AN EXTRA LINE THE OTHER TWO DO NOT. That asymmetry is
     the section. Do not even it up for symmetry.
     ══════════════════════════════════════════════════════════════ -->
<!-- ══════════════════════════════════════════════════════════════
     05 · THE SYSTEM
     THIS SECTION EXISTS TO JOIN TWO TRIADS. The audit measures needs,
     boundaries and emotions. The coaching runs on vision, values and
     velocity, and the audit is a piece of Values. Without the join she
     meets a second, unrelated three and has to work out for herself
     which one she was told about.

     WHY IT SITS AFTER THE AUDIT. Same reason the pillars stay hidden
     until item fifteen. She does a fragment, then sees the whole.

     THE FLIP IS THE STATE, NOT A REVEAL. Front is what the dimension
     is, back is what it looks like when it is missing, and turning a
     card over IS switching that dimension off. One gesture, not two
     things to learn. The line underneath names whichever of the eight
     combinations is showing.

     IT OPENS WITH ALL THREE PRESENT, AND THAT IS A DELIBERATE BREAK
     FROM THE MEN'S PAGE. His starts with all three panes filthy, so a
     man who never clicks still sees where he is. Opening hers on "you
     have no vision, no values and no velocity" is a verdict about her,
     which is the one thing this avatar refuses. So it opens working
     and she turns pieces OFF to see what each one was holding up.
     Testing a model is agency. Being handed a diagnosis is not.

     NOTHING IS SCORED, STORED OR SENT. The audit two sections above is
     the instrument. This is a model she can put her hands on for five
     seconds, and if it ever starts competing with the audit it should
     be cut rather than tuned.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-rcs" style="background:#04122A">
  <div class="px-wrap">
    <div style="max-width:640px;margin-bottom:clamp(26px,3.5vw,38px)">
      <h2 class="px-serif px-fade" style="font-size:clamp(26px,3.2vw,42px);line-height:1.16;color:#F1ECE1;font-weight:500;margin:0 0 16px">
        The Relational Connection System&trade;
      </h2>
      <p class="px-fade" style="margin:0 0 12px">
        Reconnect works on the three dimensions that influence your outcomes. Your audit result
        sits inside the second one.
      </p>
      <p class="px-fade" style="margin:0">
        Everyone has all three. Two of them are inherited rather than uniquely theirs, by design.
        Flip the cards to discover the signs.
      </p>
    </div>

    <div id="px-rcs-cols">
    <div id="px-rcs-grid">
      <button type="button" class="px-rcs-card" data-k="vision" style="--ac:#B1BFD7" aria-pressed="false">
        <span class="px-rcs-inner">
          <span class="px-rcs-face front">
            <svg class="px-rcs-glyph" viewBox="0 0 48 48" aria-hidden="true"><path d="M6 34 H42"/><path d="M6 34 L28 18.5"/><circle class="fill" cx="32" cy="16" r="3.4"/></svg>
            <span class="px-label">Vision</span>
            <span class="h px-serif">How you experience your life</span>
            <span class="cue">Flip it over</span>
          </span>
          <span class="px-rcs-face back">
            <svg class="px-rcs-glyph off" viewBox="0 0 48 48" aria-hidden="true"><path d="M6 34 H42"/><path class="stub" d="M6 34 L14 28.4"/></svg>
            <span class="px-label">Vision &middot; whose are you living?</span>
            <span class="p">Somebody asks what you want and you answer with what needs doing. Given a genuinely open week, you are lost for how to enjoy it.</span>
            <span class="cue">Flip it back</span>
          </span>
        </span>
      </button>
      <button type="button" class="px-rcs-card" data-k="values" style="--ac:#E8C65F" aria-pressed="false">
        <span class="px-rcs-inner">
          <span class="px-rcs-face front">
            <svg class="px-rcs-glyph" viewBox="0 0 48 48" aria-hidden="true"><path d="M10 34 H38"/><path d="M10 27 H38"/><path class="lift" d="M14 14 H34"/></svg>
            <span class="px-label">Values</span>
            <span class="h px-serif">Who you need to become to live that</span>
            <span class="cue">Flip it over</span>
          </span>
          <span class="px-rcs-face back">
            <svg class="px-rcs-glyph off" viewBox="0 0 48 48" aria-hidden="true"><path d="M10 34 H38"/><path d="M10 27 H38"/><path d="M10 20 H38"/></svg>
            <span class="px-label">Values &middot; whose are you carrying?</span>
            <span class="p">You know better. You respond the way you always do, then tell yourself you need to change that. You have told yourself that before.</span>
            <span class="cue">Flip it back</span>
          </span>
        </span>
      </button>
      <button type="button" class="px-rcs-card" data-k="velocity" style="--ac:#F1ECE1" aria-pressed="false">
        <span class="px-rcs-inner">
          <span class="px-rcs-face front">
            <svg class="px-rcs-glyph" viewBox="0 0 48 48" aria-hidden="true"><path d="M8 24 H34"/><path d="M29.5 19 L35 24 L29.5 29"/></svg>
            <span class="px-label">Velocity</span>
            <span class="h px-serif">The speed of real change</span>
            <span class="cue">Flip it over</span>
          </span>
          <span class="px-rcs-face back">
            <svg class="px-rcs-glyph off" viewBox="0 0 48 48" aria-hidden="true"><path d="M8 24 H20"/><path class="stub" d="M26 24 H36"/></svg>
            <span class="px-label">Velocity &middot; whose are you running for?</span>
            <span class="p">You know what needs to change and you have taken action. Some of it helped. It hasn&rsquo;t made the difference you want.</span>
            <span class="cue">Flip it back</span>
          </span>
        </span>
      </button>
    </div>

      <!-- The read-out. One box, eight states, and it carries the accent
           of whichever dimension is missing when exactly one of them is.
           Two or three missing and it goes neutral, because there is no
           single thing to point at any more. -->
      <div id="px-rcs-state" class="px-fade" aria-live="polite">
        <p class="lab">All three dialled in</p>
        <p class="txt">Your life, and nothing about you has been toned down. You know what you want, you
          hear yourself decline easily, and every no teaches people your values.</p>
        <p class="txt">Everything in front of you is yours, a reflection of you, and you keep choosing
          you, for you. Success, presence and peace, felt in ordinary moments.</p>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     06 · DANIEL
     The only clear glass on the site. Machine FIRST. The copy audit
     found the live women's page runs coaching credentials for three
     paragraphs and reaches the machine in the fourth, which is the
     credential for the other avatar and leans at the fatal reading.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-daniel" style="background:#F7F3EA">
  <div class="px-wrap">
    <!-- COMPACT ON PURPOSE. He is already on this page twice: the hero
         and the after hours plate in section 03. A third full height
         portrait here makes the page about him at the exact point it
         should be handing over. Small picture, same words. -->
    <div id="px-daniel-grid">
      <div>
        <img id="px-daniel-img" src="https://static.wixstatic.com/media/111174_1c14cd1cd5e5427dbd6cd38ac773a70a~mv2.jpg" width="1000" height="1250"
             loading="lazy" decoding="async"
             alt="Daniel Lawson, leaning against a stone wall, looking at the camera">
      </div>

      <div>
        <p class="px-hand px-fade" style="color:#A08A5E;font-size:1.15rem;margin:0 0 10px">and now me.</p>
        <h2 class="px-serif" style="font-size:clamp(23px,2.7vw,36px);line-height:1.18;color:#1F2A3D;margin:0 0 20px;font-weight:500">
          <span class="px-line"><span>I hit my KPIs.</span></span>
          <span class="px-line"><span style="font-style:italic;font-weight:300;color:#5A6274">Then they moved.</span></span>
        </h2>
        <p class="px-fade" style="color:#3A4557;margin-bottom:14px">
          Business development for an ASX200 tech company. I hit the target and it cost me my soul. The next
          year it went up another ten percent, so I had to do all of it again, plus more.
        </p>
        <!-- THE SECOND BUSINESS IS THE EVIDENCE, NOT A CREDENTIAL. Section
             03 claims the arrangement follows her out of the building. This
             paragraph is the only proof of that claim anywhere on the page,
             and it is his own life. Do not cut it to save space. -->
        <p class="px-fade" style="color:#3A4557;margin-bottom:14px">
          I resigned, and lost my sense of identity with it, because I had lost myself in the pursuit. Then I
          built a water sports apparel business so I could chase the wind and work from the beach. Different
          story, same result.
        </p>
        <p class="px-fade" style="color:#3A4557;margin-bottom:24px">
          I am not going to tell you I understand what it is like to be a woman in your industry. But I was in
          the machine you are in, and I did not just get out of it. I got me back. How you do that is worth
          the conversation.
        </p>
        <div class="px-fade" style="display:flex;flex-wrap:wrap;gap:12px">
          <a class="px-btn" href="https://www.parallaxxtransformations.com/contact-daniel-lawson">Start with a conversation <span aria-hidden="true">&rarr;</span></a>
          <a class="px-ghost" href="https://www.parallaxxtransformations.com/about-daniel-lawson" style="color:#5A6274;border-color:rgba(160,138,94,.5)">The full story <span aria-hidden="true">&rarr;</span></a>
        </div>

        <!-- ▸ BANNED FROM THIS SECTION, per the copy audit:
             "I'm in a relationship with a woman like you."
             "strong enough to hold what she's carrying."
             Anything that makes him the container for HER rather than the
             person holding the room. One step from "let a man lead you",
             which the banned table marks fatal with no recovery.

             AND NO PHOTOGRAPH OF HIM WITH A PARTNER, EVER, ON THIS PAGE.
             It reads as the same claim in pictures. -->
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     07 · PROOF
     THE WORDS ARE TEXT, NOT A PICTURE OF TEXT. The live testimonials
     page serves flattened PNGs with the photo, the quote and the name
     baked into one file. Google reads none of it, a screen reader gets
     nothing, and it cannot reflow on a phone. Here the quote and the
     name are real text and the face is a separate square.

     THESE THREE CAME AS THEIR OWN FILES, so the CDN crop method in
     HOW-TO — Crop faces out of Wix images.md was not needed. That
     method still applies to anything pulled off a flattened card.

     ALL THREE ARE WOMEN AND ALL THREE ARE CLIENTS. Ana runs her own
     business, which is the voice the manifest had been waiting for:
     until there was one, this was an industry rather than an avatar.
     She sits in the middle slot on purpose.

     EVERY QUOTE IS VERBATIM. Gena's and Ana's each join two sentences
     that were not adjacent, problem to outcome, with the middle
     paragraph cut. Silja's is untouched. No word was changed in any of
     them. Do not paraphrase these.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-proof" style="background:#0A1D3C">
  <div class="px-wrap px-head" style="text-align:center;margin-bottom:clamp(30px,4vw,44px)">
    <h2 class="px-serif px-fade" style="font-size:clamp(26px,3.2vw,44px);color:#F1ECE1;font-weight:500">
      They all say the <span style="font-style:italic;font-weight:300;color:#B1BFD7">same thing.</span>
    </h2>
  </div>

  <div class="px-wrap">
    <div id="px-voices">
      <figure class="px-voice">
        <img class="px-face" alt="Gena K, Bulgaria" width="640" height="640" loading="lazy" decoding="async"
             src="https://static.wixstatic.com/media/111174_b98cf690891f444587d740029b22c892~mv2.jpg">
        <blockquote>I was trying to figure it out by myself, but I couldn&rsquo;t see where the actual problem was coming from. Now I&rsquo;ve found ways to overcome the procrastination and emotional drain.</blockquote>
        <figcaption><b>Gena K</b><span>Bulgaria</span></figcaption>
      </figure>
      <figure class="px-voice">
        <img class="px-face" alt="Ana R, United Kingdom" width="640" height="640" loading="lazy" decoding="async"
             src="https://static.wixstatic.com/media/111174_abf2f5363f7e4262ba1812cb7844c6d3~mv2.jpg">
        <blockquote>I knew what I wanted in my mind, but I found it hard to articulate. There were barriers stopping me and I couldn&rsquo;t break through them alone. Now I have confidence, clarity and flow, and my business is moving forward.</blockquote>
        <figcaption><b>Ana R</b><span>United Kingdom</span></figcaption>
      </figure>
      <figure class="px-voice">
        <img class="px-face" alt="Silja T, Sweden" width="640" height="640" loading="lazy" decoding="async"
             src="https://static.wixstatic.com/media/111174_ab54167a61fe4c188544389f7f4e9d1f~mv2.jpg">
        <blockquote>Daniel didn&rsquo;t focus on my problems. We shifted to what I wanted more of, and the problems stopped existing. Now everything comes from curiosity instead of fear.</blockquote>
        <figcaption><b>Silja T</b><span>Sweden</span></figcaption>
      </figure>
    </div>
  </div>
</section>


<!-- ══════════════ THE CLOSE — the fork only ══════════════
     THE AUDIT REPEAT WAS REMOVED. It asked her to take the instrument
     she has just been sitting inside two sections earlier, in the same
     words, with the same button. On a page where the audit runs in
     place rather than on another URL, a second invitation to start it
     reads as though the page has forgotten she already did.

     What is left is the only thing this section still has to do: give
     the woman who is not booking an hour somewhere smaller to go. -->
<section class="px-sec" id="px-close" style="background:#F1ECE1">
  <div class="px-wrap px-head" style="text-align:center">
    <div class="px-fade">
      <p class="px-label" style="color:#A08A5E;margin-bottom:8px">Not ready for personal coaching?</p>
      <p class="px-serif" style="font-size:1.35rem;color:#1F2A3D;font-weight:500;margin-bottom:6px">The Reconnected Woman</p>
      <p style="color:#5A6274;font-size:.94rem;margin-bottom:14px">Start the work in a group, once a week, for the price of a lunch.</p>
      <a class="px-ghost" href="https://www.parallaxxtransformations.com/the-reconnected-woman" style="color:#5A6274;border-color:rgba(160,138,94,.5)">Take a look <span aria-hidden="true">→</span></a>
    </div>
  </div>
</section>


<!-- ══════════════ 08 · CREDENTIALS — demoted, unchanged ══════════════ -->
<section class="px-sec" id="px-creds" style="background:#04122A;padding-top:40px;padding-bottom:40px">
  <div class="px-wrap" style="text-align:center">
    <p class="px-label" style="margin-bottom:16px">Seen in · Credentialled by</p>
    <p style="color:#5E6B85;font-size:.9rem;letter-spacing:.04em">
      ABC News &nbsp;·&nbsp; Wanderlust Magazine &nbsp;·&nbsp; Brainz Magazine &nbsp;·&nbsp; Solarcon &nbsp;·&nbsp; Meta Dynamics L3 &nbsp;·&nbsp; Int'l Coach Guild
    </p>
    <div id="px-stats" style="display:flex;justify-content:center;gap:clamp(28px,6vw,80px);margin-top:28px;flex-wrap:wrap">
      <div><div class="px-serif" style="font-size:1.9rem;color:#E8C65F;font-weight:500">1000+</div><div class="px-label">lives changed</div></div>
      <div><div class="px-serif" style="font-size:1.9rem;color:#E8C65F;font-weight:500">9</div><div class="px-label">countries</div></div>
      <div><div class="px-serif" style="font-size:1.9rem;color:#E8C65F;font-weight:500">6+</div><div class="px-label">years facilitating</div></div>
    </div>
  </div>
</section>


<footer id="pt-foot">
  <div id="pt-foot-inner">

    <div id="pt-foot-top">

      <!-- ══ IDENTITY ══ The mark and the four places he posts. No
           paragraph, no CTA: both were cut, and the column is better
           for it. ══ -->
      <div id="pt-ident">
        <a id="pt-foot-logo" href="https://www.parallaxxtransformations.com" aria-label="Parallaxx Transformations — home">
          <img src="https://static.wixstatic.com/media/e1784d_fe3c841c471f47d088f0cd631a89d883~mv2.png"
               alt="Parallaxx Transformations" loading="lazy">
        </a>
        <nav id="pt-social" aria-label="Social">
          <a href="https://www.youtube.com/@ReconnectYou1/featured" target="_blank" rel="noopener">YouTube</a>
          <a href="https://www.linkedin.com/in/daniel-reconnect-you/" target="_blank" rel="noopener">LinkedIn</a>
          <a href="https://www.instagram.com/daniel.lawson__/" target="_blank" rel="noopener">Instagram</a>
          <a href="https://www.facebook.com/Kiwi.Daniel" target="_blank" rel="noopener">Facebook</a>
        </nav>
      </div>

      <!-- ══ THE TWO DOORS ══ Same names, same order as the nav. ══ -->
      <nav class="pt-door" aria-label="For men">
        <h4>For Men</h4>
        <a href="https://www.parallaxxtransformations.com/men">Start here</a>
        <a href="https://www.parallaxxtransformations.com/the-reconnected-man">The Reconnected Man</a>
        <!-- /reconnect is the five-archetype quiz, not a programme.
             It is his free way in, so it belongs beside the door. -->
        <a href="https://www.parallaxxtransformations.com/reconnect">The Archetype Quiz</a>
      </nav>

      <nav class="pt-door" aria-label="For women">
        <h4>For Women</h4>
        <a href="https://www.parallaxxtransformations.com/women">Start here</a>
        <a href="https://www.parallaxxtransformations.com/the-reconnected-woman">The Reconnected Woman</a>
        <!-- [CLIENT] /priority-audit is the URL the women's home page
             CTA uses. Confirm the page is live before publishing. -->
        <a href="https://www.parallaxxtransformations.com/priority-audit">The Priority Audit</a>
      </nav>

      <!-- ══ DANIEL ══ Not a door. The three places he shows up in
           person, smallest commitment first. GIVE IT ALL is the last
           of them rather than a banner of its own. ══ -->
      <nav class="pt-door" aria-label="Daniel">
        <h4>Daniel</h4>
        <a href="https://www.parallaxxtransformations.com/about-daniel-lawson">About</a>
        <a href="https://www.parallaxxtransformations.com/daniel-lawson-speaking">Speaking</a>
        <!-- [CLIENT] No facilitating page exists on the site yet. This
             URL is a guess and must be confirmed or repointed. -->
        <a href="https://www.parallaxxtransformations.com/facilitating">Facilitating</a>
        <a class="ext" href="https://giveitallevent.com" target="_blank" rel="noopener">GIVE IT ALL<span class="mk" aria-hidden="true">&#8599;</span></a>
      </nav>

    </div>

    <!-- ══ THE UTILITY ROW ══
         Ordered by who asks for it: the person, then the work he makes,
         then the proof. Seven of these are linked from nowhere else on
         the site, which is exactly why the row exists. ══ -->
    <nav id="pt-util" aria-label="More">
      <a href="https://www.parallaxxtransformations.com/contact-daniel-lawson">Contact</a>
      <a href="https://www.parallaxxtransformations.com/book-a-call-with-daniel-lawson">Book a Call</a>
      <a href="https://www.parallaxxtransformations.com/reconnect-you-podcast-with-daniel-lawson">Reconnect You Podcast</a>
      <a href="https://www.parallaxxtransformations.com/parallaxx-perspectives-podcast">Perspectives Podcast</a>
      <a href="https://www.parallaxxtransformations.com/blog">Blog</a>
      <a href="https://www.parallaxxtransformations.com/ptjournal">Progress Journal</a>
      <a href="https://www.parallaxxtransformations.com/three-toxic-lies">Three Toxic Lies</a>
      <!-- [CLIENT] If Identity 2.0 is aimed at one avatar, promote it
           into that door's column instead of leaving it here. -->
      <a href="https://www.parallaxxtransformations.com/your-identity-challenge">Your Identity 2.0 Challenge</a>
      <a href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">Testimonials</a>
      <a href="https://www.parallaxxtransformations.com/daniel-lawson-as-seen-in">As Seen In</a>
    </nav>

    <div id="pt-legal">
      <span>&copy; <span id="pt-year">2026</span> Parallaxx Transformations &middot; Daniel Lawson</span>
      <span class="links">
        <a href="https://www.parallaxxtransformations.com/terms-of-use">Terms of Service</a>
        <a href="https://www.parallaxxtransformations.com/privacy-policy">Privacy Policy</a>
      </span>
    </div>

  </div>
</footer>

<!-- ══════════════════════════════════════════════════════════════
     IMAGE MANIFEST — everything this page needs and does not have
     ──────────────────────────────────────────────────────────────
     01  hero-daniel-through.jpg    2400×1600  DONE. graded, in place
     02  machine-daniel-after-hours.jpg 1200×1500  home, late, alone. IN PROGRESS
     02a divider-desk-night.jpg     1800×2000  the room, always on
     02b divider-desk-cleared.jpg   1800×2000  same room, decided
     03  texture-week-grid.png      2400×1400  optional, behind the cards
     04  daniel-clear.jpg           1400×1800  the only face on the page
     05  proof-room.jpg             2000×1200  a real session, real grain
     06  voice-christine.jpg        640×640    client, not captured yet
     07  voice-02.jpg               640×640    first room, not run yet
     08  voice-03.jpg               640×640    non-consultant, not found yet
     09  og-home-hero.png           1200×630   DONE. crop of 01

     TO BE CLEAR ABOUT THE NAMING. This page is FOR women. Nothing in
     it is a PICTURE of one. Every asset above is named for what is in
     the frame, not for who the page is aimed at, because a file called
     divider-women-before.jpg is the fastest way to end up with a stock
     woman in it.

     THE RULE, AMENDED 2 AUG. Nobody appears in 02a, 02b or 03. Not a model, not a
     stock library, and above all not a generative one, which she has
     been served on every wellness page she has ever scrolled past. Her
     artefacts instead: the room, the calendar, the coat, the table. An
     empty room is something she projects herself into.

     THE FACES are Daniel's only, in 01, 02 and 04, because this page
     sells one conversation with him. 02 was added so the relationship
     starts three sections earlier than it used to. 05 and the three voices are real
     photographs of real people, or they stay empty.
     ══════════════════════════════════════════════════════════════ -->

</div><!-- /px-root -->`;

  function addFonts(){
    if (document.getElementById('px-fonts-w')) return;
    var p1=document.createElement('link'); p1.rel='preconnect'; p1.href='https://fonts.googleapis.com'; document.head.appendChild(p1);
    var p2=document.createElement('link'); p2.rel='preconnect'; p2.href='https://fonts.gstatic.com'; p2.crossOrigin=''; document.head.appendChild(p2);
    var f0=document.createElement('link'); f0.rel='stylesheet'; f0.href='https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap'; document.head.appendChild(f0);
    var f1=document.createElement('link'); f1.rel='stylesheet'; f1.href='https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap'; document.head.appendChild(f1);
    /* @font-face has to live in the DOCUMENT, not the shadow root. Chrome
       ignores font-face rules declared inside a shadow tree. */
    var ff=document.createElement('style'); ff.id='px-fonts-w'; ff.textContent=`/* LUMIOS MARKER is not on Google Fonts -- upload the .woff2 to Wix Media and
   drop the URL in below. Until then the stack falls through to Permanent
   Marker, which is the closest free marker face, so nothing renders as a
   system script by accident. */
@font-face{font-family:'Lumios Marker';font-display:swap;
  src:url('LUMIOS_MARKER_WOFF2_URL') format('woff2');}`; document.head.appendChild(ff);
  }

  function loadScript(src){
    return new Promise(function(res,rej){
      var ex=document.querySelector('script[data-px="'+src+'"]');
      if(ex){ if(ex.getAttribute('data-loaded')){res();} else { ex.addEventListener('load',function(){res();}); ex.addEventListener('error',rej);} return; }
      var s=document.createElement('script'); s.src=src; s.async=false; s.setAttribute('data-px',src);
      s.addEventListener('load',function(){ s.setAttribute('data-loaded','1'); res(); });
      s.addEventListener('error',rej);
      document.head.appendChild(s);
    });
  }

  function loadLibs(){
    var g=loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js')
      .then(function(){ return loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js'); });
    var l=loadScript('https://unpkg.com/lenis@1.1.13/dist/lenis.min.js');
    return Promise.all([ g.catch(function(){}), l.catch(function(){}) ]);
  }

  /* Rewrites ancestor heights, which moves every scroll position on the page.
     ScrollTrigger has already cached those, so a refresh has to follow.
     Returns whether anything actually changed so we do not refresh for
     nothing. */
  function collapseAncestors(host){
    var changed = false;
    try{ var h=host.getBoundingClientRect().height; if(h<50) return false;
      var n=host.parentElement,guard=0;
      while(n && n!==document.body && guard++<14){ if(n.getBoundingClientRect().height>h+600){ n.style.height='auto'; n.style.minHeight='0px'; changed = true; } n=n.parentElement; }
    }catch(e){}
    return changed;
  }

  function collapseAndRefresh(host){
    if (collapseAncestors(host) && window.ScrollTrigger) {
      try{ window.ScrollTrigger.refresh(); }catch(e){}
    }
  }

  /* The audit runs its own reveal machinery and the page module runs
     another. Both are handed the shadow root as SHADOW; neither one
     touches the document for lookups. */
  function boot(SHADOW){
    if(!SHADOW || !SHADOW.getElementById('px-root')) return;
(function(){
  const root = SHADOW;
  const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const G = window.gsap;
  const hasGSAP = !!(G && window.ScrollTrigger);
  if (hasGSAP) G.registerPlugin(window.ScrollTrigger);

  /* ══════════════ SMOOTH SCROLL ══════════════ */
  let lenis = null;
  if (window.Lenis && !reduce) {
    lenis = new window.Lenis({ lerp: 0.11 });
    const raf = t => { lenis.raf(t); requestAnimationFrame(raf); };
    requestAnimationFrame(raf);
    if (hasGSAP) lenis.on('scroll', window.ScrollTrigger.update);
  }

  /* ══════════════ IN-PAGE ANCHORS ══════════════
     THE HERO BUTTON DID NOTHING ON THE LIVE SITE AND WORKED FINE LOCALLY.
     href="#px-audit-sec" is resolved by the browser against the DOCUMENT.
     In the preview the page IS the document, so it scrolled. Deployed, the
     whole page lives inside a shadow root, the document has no element with
     that id, and the browser correctly does nothing at all. No error, no
     warning, just a button that looks alive and is not.

     So every in-page anchor is resolved against root, which is the
     document locally and the shadow root in the bundle. Both carry
     getElementById, so the same line works in both runtimes, and it is
     driven by hand rather than left to the browser.
     Lenis owns the scroll position when it is running, so scrollIntoView
     would be overwritten on its next frame; where it exists it does the
     scrolling. */
  root.addEventListener('click', e => {
    const a = e.target.closest && e.target.closest('a[href^="#"]');
    if (!a) return;
    const id = a.getAttribute('href').slice(1);
    if (!id) return;
    const target = root.getElementById(id);
    if (!target) return;
    e.preventDefault();
    if (lenis) lenis.scrollTo(target, { offset: 0, duration: 1.1 });
    else target.scrollIntoView({ behavior: reduce ? 'auto' : 'smooth', block: 'start' });
  });

  /* ══════════════ KINETIC TYPE ══════════════
     Anything already on screen at boot animates immediately with no
     ScrollTrigger. A trigger firing while layout is still settling can be
     reverted mid-tween and strand a headline half out of its clip box. */
  if (hasGSAP && !reduce) {
    const inView = el => el.getBoundingClientRect().top < window.innerHeight * 0.88;
    root.querySelectorAll('.px-line > span').forEach(l => {
      if (inView(l)) G.to(l, { y: '0%', duration: 1.05, ease: 'expo.out' });
      else G.to(l, { y: '0%', duration: 1.05, ease: 'expo.out', scrollTrigger: { trigger: l, start: 'top 88%', once: true } });
    });
    root.querySelectorAll('.px-fade').forEach(el => {
      if (inView(el)) G.to(el, { opacity: 1, y: 0, duration: .95, ease: 'power3.out' });
      else G.to(el, { opacity: 1, y: 0, duration: .95, ease: 'power3.out', scrollTrigger: { trigger: el, start: 'top 90%', once: true } });
    });
  } else {
    root.querySelectorAll('.px-line > span').forEach(l => l.style.transform = 'none');
    root.querySelectorAll('.px-fade').forEach(e => { e.style.opacity = '1'; e.style.transform = 'none'; });
  }

  /* The hero scroll-clear was removed on 2 Aug. The plate is now shown at
     full clarity from the first frame: --px-clear is pinned to 1 in the
     stylesheet and nothing drives it. Do not reinstate the blur. */

  /* ══════════════ 02 · THE MIRROR THREAD ══════════════
     Outside the GSAP branch on purpose. IntersectionObserver only, so a
     phone with no GSAP still gets the sequence, which in this section IS
     the content.

     HER LINES APPEAR. THE ANSWERS TYPE THEMSELVES.
     That asymmetry is the whole point. Her defence is reflexive, so it is
     just suddenly there, fully formed, because she has said it a thousand
     times and it costs her nothing. The answer is a thought arriving, so
     she watches it arrive. It also takes the slap out of it: a navy block
     can never land fully formed, it starts as an empty pill and grows.

     TEN PAIRS RUNS ABOUT FIFTEEN SECONDS with typing. That is longer than
     the eight-pair version and it is the price of the effect. The
     fast-scroller net below covers anyone who leaves, and the window is
     bottom-anchored so the last pair is always the one on screen.

     AND SHE CAN SCROLL BACK. The tape is taller than the screen, so the
     window auto-scrolls while it types, but the moment she scrolls up
     herself the auto-scroll stands down until she returns to the bottom.
     Nothing fights her thumb. A top fade appears whenever there is
     something above, because the scrollbar is hidden and otherwise there
     is no cue that anything is up there. */
  (function thread(){
    const tape = root.getElementById('px-tape');
    const win  = root.getElementById('px-window');
    if (!tape) return;
    const items = [...tape.children];

    const PRE = 90, READ = 300, DOTS = 190, CH = 9, STOP = 120, CLAUSE = 60, BEAT = 140;

    const typing = document.createElement('div');
    typing.id = 'px-typing';
    typing.innerHTML = '<div class="d"><i></i><i></i><i></i></div>';
    tape.appendChild(typing);

    /* cache every answer before a character is typed, so the fast-scroll net
       and reduced-motion can put the text back verbatim */
    const FULL = new WeakMap();
    items.forEach(el => { const b = el.querySelector('.px-bub'); if (b) FULL.set(el, b.textContent); });

    let killed = false;
    const wait  = ms => new Promise(r => setTimeout(r, ms));
    const frame = () => new Promise(r => requestAnimationFrame(() => requestAnimationFrame(r)));

    /* ── her thumb wins ── */
    let userScrolled = false, lastN = 0;
    const atBottom = () => !win || win.scrollHeight - win.scrollTop - win.clientHeight < 40;
    if (win) win.addEventListener('scroll', () => {
      userScrolled = !atBottom();
      win.classList.toggle('has-above', win.scrollTop > 8);
    }, { passive: true });

    const nudge = force => {
      if (!win || userScrolled) return;
      const t = performance.now();
      if (!force && t - lastN < 120) return;
      lastN = t;
      win.scrollTo({ top: win.scrollHeight, behavior: 'smooth' });
    };

    /* display:none to flex is not transitionable, so the element gets its box
       on one frame and its opacity on the next. Two rAFs, because one is not
       reliably enough for the style to have been computed. */
    const reveal = async el => {
      el.classList.add('shown');
      await frame();
      el.classList.add('on');
      nudge(true);
    };

    const typeOut = el => new Promise(res => {
      const bub = el.querySelector('.px-bub');
      const full = FULL.get(el);
      if (!bub || !full || reduce) return res();
      bub.textContent = '';
      bub.classList.add('typing');
      let i = 0;
      const tick = () => {
        if (killed) { bub.textContent = full; bub.classList.remove('typing'); return res(); }
        const c = full[i++];
        bub.textContent = full.slice(0, i);
        nudge();
        if (i >= full.length) { bub.classList.remove('typing'); nudge(true); return res(); }
        let d = CH;
        if (c === '.' || c === '?' || c === '!') d += STOP;
        else if (c === ',') d += CLAUSE;
        setTimeout(tick, d);
      };
      setTimeout(tick, CH);
    });

    const showAll = () => {
      killed = true;
      items.forEach(el => {
        el.classList.add('shown', 'on');
        const b = el.querySelector('.px-bub');
        if (b && FULL.has(el)) { b.textContent = FULL.get(el); b.classList.remove('typing'); }
      });
      typing.classList.add('gone');
      if (win && !userScrolled) win.scrollTop = win.scrollHeight;
    };

    const run = async () => {
      for (const el of items) {
        if (killed) return;
        if (el.classList.contains('px-out')) {
          await wait(PRE);
          if (killed) return;
          await reveal(el);                 // her defence, already formed
          tape.appendChild(typing);
          await wait(READ);
        } else {
          typing.classList.add('on');       // and it is already answering
          await wait(DOTS);
          if (killed) return;
          typing.classList.remove('on');
          await wait(60);
          if (killed) return;
          await reveal(el);                 // an empty pill, quietly
          tape.appendChild(typing);
          await typeOut(el);                // which then fills itself
          if (killed) return;
          await wait(BEAT);
        }
      }
      typing.classList.add('gone');
    };

    if ('IntersectionObserver' in window && !reduce) {
      const io = new IntersectionObserver(es => es.forEach(e => {
        if (e.isIntersecting) { io.disconnect(); run(); }
      }), { rootMargin: '0px 0px -24% 0px' });
      io.observe(root.querySelector('.px-phone'));

      /* the fast-scroller net: already past it, show the lot. The last pair
         is the one the section exists for and must never be left unrendered. */
      const past = new IntersectionObserver(es => es.forEach(e => {
        if (!e.isIntersecting && e.boundingClientRect.top < 0) { past.disconnect(); showAll(); }
      }), { threshold: 0 });
      past.observe(root.getElementById('px-mirror'));
    } else { showAll(); }
  })();

  /* ══════════════ 03 · THE YEARS ══════════════
     Six lines, revealed one at a time. The stagger is the whole
     mechanic: she reads five justifications, and the sixth is the
     first one again. Delivered all at once it is a list. Delivered in
     sequence it is a loop closing.

     CSS transitions and IntersectionObserver only, no GSAP, because
     this is content rather than decoration and it has to survive a
     phone that never loaded the animation library. */
  (function years(){
    const rows = [...root.querySelectorAll('.px-yr')];
    if (!rows.length) return;
    const showAll = () => rows.forEach(r => r.classList.add('on'));
    if (!('IntersectionObserver' in window) || reduce) return showAll();
    const io = new IntersectionObserver(es => es.forEach(e => {
      if (!e.isIntersecting) return;
      io.disconnect();
      rows.forEach((r, i) => setTimeout(() => r.classList.add('on'), i * 260));
    }), { rootMargin: '0px 0px -18% 0px' });
    io.observe(root.getElementById('px-years'));
  })();

  /* The old audit CTA module lived here. It wired the live first item
     to a query param on a link to /priority-audit. The instrument now
     runs in the page, so that handoff is gone and the module with it.
     See js_audit for what replaced it. */

  /* ══════════════ 02 · THE LEDGER ══════════════
     The five cost lines arrive one at a time, 190ms apart, with a
     bronze marker drawing in beside each. A list that appears whole is
     read as a block. A list that arrives is counted.

     CSS transitions and IntersectionObserver only, no GSAP, because
     this is content rather than decoration. */
  (function ledger(){
    const rows = [...root.querySelectorAll('.px-cost-list li')];
    if (!rows.length) return;
    const showAll = () => rows.forEach(r => r.classList.add('on'));
    if (!('IntersectionObserver' in window) || reduce) return showAll();
    const io = new IntersectionObserver(es => es.forEach(e => {
      if (!e.isIntersecting) return;
      io.disconnect();
      rows.forEach((r, i) => setTimeout(() => r.classList.add('on'), i * 190));
    }), { rootMargin: '0px 0px -16% 0px' });
    io.observe(root.querySelector('.px-cost-list'));
  })();

  /* ══════════════ 05 · THE THREE DIMENSIONS ══════════════
     Three cards, eight states. Turning a card over switches that
     dimension off, and the line underneath reads whatever combination
     is showing.

     STARTS WITH ALL THREE ON. A woman who never touches it reads the
     model working and three failure states in small type on the card
     fronts. She is never opened on with a verdict.

     NOTHING IS SCORED OR STORED. The audit is the instrument. This is
     five seconds with your hands on the model. */
  (function dimensions(){
    const grid = root.getElementById('px-rcs-grid');
    const out  = root.getElementById('px-rcs-state');
    if (!grid || !out) return;

    /* Keyed by what is PRESENT, sorted, so lookup is order independent.
       Each state gets a label and a paragraph: a single sentence under
       three cards reads as a caption, and this is a result. */
    const LINES = {
      "values,velocity,vision": ["All three dialled in",
        "Your life, and nothing about you has been toned down. You know what you want, you hear yourself decline easily, and every no teaches people your values.",
        "Everything in front of you is yours, a reflection of you, and you keep choosing you, for you. Success, presence and peace, felt in ordinary moments."],
      "velocity,vision": ["Who do you need to become?",
        "Your vision is clear and you are taking action towards it. But if the attributes, characteristics, standards, expectations, needs and boundaries do not match that vision, you will keep recreating the same outcomes.",
        "Running faster at a vision on old conditioning looks like productivity without progress."],
      "values,velocity": ["Where are you actually going?",
        "You are already showing up as the best version of you, and proud of every bit of action you take building what you have got. But it feels like someone else's life. Maybe someone else's dream.",
        "That is what happens without your own vision for how you want to live and experience it."],
      "values,vision": ["Need to get moving?",
        "You have the dreams, the ambition, and a strong sense of who you are. But six years have passed and life still looks the same. Why has nothing changed?",
        "It is the frameworks and the structure that create momentum and tangible progress."],
      "vision": ["Vision dialled in?",
        "You know how you want to experience your life and who is in it. You can see it, feel it, almost taste it. But you keep attracting the same people and the same circumstances on repeat, and nothing you do seems to change that.",
        "Not without effort, just without insight for what's missing."],
      "values": ["Strong sense of self?",
        "You know who you are, and more than that, you are proud of how you show up in uncertainty and challenge. You know your needs, your boundaries and your expectations, and you communicate them.",
        "But life does not really feel like yours, and it doesn't seem to change."],
      "velocity": ["You can run!",
        "The amount you do could be worn like a badge of honour. No one could call you lazy. Ever.",
        "But without a sense of direction and measurable checkpoints, over time it never adds up to much personal meaning or purpose."],
      "": ["Wake Up!",
        "Good morning. There is a moment in everyone's life when we realise the way we have been living isn't working for us. We have been making it work for someone else.",
        "This is your moment, and the good news is there is a system for working out what to focus on first."]
    };

    /* the panel takes the accent of the missing dimension when exactly
       one is off. Two or three off and there is nothing single to point
       at, so it goes neutral. */
    const AC = { vision: '#B1BFD7', values: '#E8C65F', velocity: '#F1ECE1' };

    /* Each card is as tall as its tallest face, because the faces are
       absolutely positioned for the flip and would otherwise collapse
       the button to nothing. The sizer is the front face cloned,
       hidden from sight and from screen readers. */
    [...grid.querySelectorAll('.px-rcs-card')].forEach(card => {
      const wrap = document.createElement('span');
      wrap.className = 'px-rcs-sizewrap';
      wrap.setAttribute('aria-hidden', 'true');
      ['.front', '.back'].forEach(sel => {
        const sizer = card.querySelector(sel).cloneNode(true);
        sizer.classList.add('px-rcs-sizer');
        sizer.setAttribute('aria-hidden', 'true');
        sizer.querySelectorAll('*').forEach(n => n.removeAttribute('id'));
        wrap.appendChild(sizer);
      });
      card.querySelector('.px-rcs-inner').appendChild(wrap);
    });

    const read = () => {
      const cards = [...grid.querySelectorAll('.px-rcs-card')];
      const off = cards.filter(c => c.getAttribute('aria-pressed') === 'true').map(c => c.dataset.k);
      const on  = cards.filter(c => c.getAttribute('aria-pressed') !== 'true')
                       .map(c => c.dataset.k).sort().join(',');
      /* Two paragraphs, always. The copy is written as setup then
         landing, and the break is what stops the box reading as a wall. */
      const [lab, p1, p2] = LINES[on] || LINES[''];
      const L = out.querySelector('.lab');
      const T = [...out.querySelectorAll('.txt')];
      if (L.textContent === lab) return;
      out.style.setProperty('--sa', off.length === 1 ? AC[off[0]] : '#7C89A3');
      out.classList.add('swap');
      setTimeout(() => {
        L.textContent = lab;
        T[0].textContent = p1;
        if (T[1]) T[1].textContent = p2;
        out.classList.remove('swap');
      }, reduce ? 0 : 230);
    };

    grid.addEventListener('click', e => {
      const card = e.target.closest('.px-rcs-card');
      if (!card) return;
      card.setAttribute('aria-pressed', card.getAttribute('aria-pressed') === 'true' ? 'false' : 'true');
      read();
    });
  })();

  /* ══════════════ RESPONSIVE COLLAPSE ══════════════ */
  const stacks = [['px-mirror-grid', 1040], ['px-reframe-cols', 900], ['px-rf-grid', 760], ['px-daniel-cols', 900], ['px-proof', 820]];
  stacks.forEach(([id]) => { const g = root.getElementById(id); if (g) g.dataset.cols = g.style.gridTemplateColumns; });
  const layout = () => stacks.forEach(([id, bp]) => {
    const g = root.getElementById(id); if (!g) return;
    g.style.gridTemplateColumns = window.innerWidth < bp ? '1fr' : g.dataset.cols;
  });
  window.addEventListener('resize', layout, { passive: true });
  layout();

  /* ══════════════ FOOTER YEAR ══════════════
     The footer is built into this page rather than placed as its own
     element. This is the only line of script it has: a stale copyright
     year is the one thing in a footer nobody remembers to update, and
     it is what makes a site look abandoned. */
  const year = root.getElementById('pt-year');
  if (year) year.textContent = String(new Date().getFullYear());

  /* ══════════════ SITE NAV ══════════════
     PtNav is built into this page rather than placed in the Wix header
     strip, for the same reason as the footer: the strip would not load it.
     PtNav v3.dc.html stays the source of record and this copy will not
     track it. Change one and the other does not move.

     Two things had to change coming in. The active prop is gone, because
     there is no Wix instance to set one, so the section is worked out from
     the URL and nothing else. And every lookup goes through root, which is
     the shadow root once this is bundled.

     document.addEventListener is deliberately left alone below: the
     outside-click and Escape handlers have to hear events that never reach
     the shadow root. */
  (function siteNav(){

    const nav = root.getElementById('pt-nav');
    if(!nav) return;
    const links  = root.getElementById('pt-links');
    const burger = root.getElementById('pt-burger');
    const items  = [...nav.querySelectorAll('.pt-has-drop')];
    const MOBILE = () => window.innerWidth < 1024;

    /* ── WHERE YOU ARE ──────────────────────────────────────────────
       Worked out from the URL, because this element lives ONCE in the
       site's header strip and therefore serves every page from a single
       instance. A per-page the active prop cannot do that: one instance
       carries one value, so "For Women" would sit underlined on the
       men's page too.

       The prop still wins when it is set, for the Wix editor preview
       and for any page whose path does not match the table below. */
    const PATHS = [
      [/^\/(men|the-reconnected-man|the-archetype-quiz)\b/,      'men'],
      [/^\/(women|the-reconnected-woman|priority-audit)\b/,      'women'],
      [/^\/about-daniel-lawson\b/,                              'about'],
      [/^\/(daniel-lawson-speaking|facilitating)\b/,             'about'],
      [/^\/testimonials-daniel-lawson\b/,                       'testimonials'],
      [/^\/(contact-daniel-lawson|book-a-call)/,                 'contact']
    ];
    const fromPath = () => {
      const p = (location.pathname || '').replace(/\/+$/, '').toLowerCase() || '/';
      for(const [re, key] of PATHS) if(re.test(p)) return key;
      return '';
    };
    const active = fromPath();
    if(active){
      const el = nav.querySelector('[data-nav="'+active+'"]');
      if(el) el.classList.add('is-active');
    }

    /* ── NAVY ON SCROLL ─────────────────────────────────────────── */
    let ticking = false;
    const onScroll = () => {
      if(ticking) return; ticking = true;
      requestAnimationFrame(() => {
        nav.classList.toggle('is-stuck', window.scrollY > 40);
        ticking = false;
      });
    };
    window.addEventListener('scroll', onScroll, {passive:true});
    onScroll();

    /* ── MOBILE PANEL ───────────────────────────────────────────── */
    const closeMenu = () => {
      nav.classList.remove('menu-open');
      burger.setAttribute('aria-expanded','false');
      items.forEach(closeDrop);
    };
    burger.addEventListener('click', () => {
      const open = !nav.classList.contains('menu-open');
      nav.classList.toggle('menu-open', open);
      burger.setAttribute('aria-expanded', String(open));
      if(!open) items.forEach(closeDrop);
    });

    /* ── SUBMENUS ───────────────────────────────────────────────────
       Desktop hover and keyboard focus are pure CSS. The caret only
       exists for pointers that cannot hover, and for anyone who would
       rather tap the arrow than the word. */
    function closeDrop(it){
      it.classList.remove('is-open');
      const c = it.querySelector('.pt-caret');
      if(c) c.setAttribute('aria-expanded','false');
    }
    items.forEach(it => {
      const caret = it.querySelector('.pt-caret');
      caret.addEventListener('click', e => {
        e.preventDefault(); e.stopPropagation();
        const open = !it.classList.contains('is-open');
        items.forEach(closeDrop);
        it.classList.toggle('is-open', open);
        caret.setAttribute('aria-expanded', String(open));
      });
    });

    /* ── DISMISSAL ──────────────────────────────────────────────── */
    links.querySelectorAll('a').forEach(a =>
      a.addEventListener('click', () => { if(MOBILE()) closeMenu(); }));

    /* composedPath, not e.target. A click inside a shadow root is RETARGETED
       by the time it reaches the document: e.target becomes the host element,
       so nav.contains(e.target) is false for a click on the burger itself and
       this handler closed the panel in the same tick the burger opened it.
       The menu looked completely dead. composedPath() reports the real path
       through the shadow tree, and in the light DOM it still contains nav, so
       one line covers both runtimes. */
    document.addEventListener('click', e => {
      const path = e.composedPath ? e.composedPath() : [e.target];
      if(path.indexOf(nav) === -1){ items.forEach(closeDrop); if(MOBILE()) closeMenu(); }
    });

    document.addEventListener('keydown', e => {
      if(e.key !== 'Escape') return;
      items.forEach(closeDrop);
      if(nav.classList.contains('menu-open')){ closeMenu(); burger.focus(); }
    });

    /* Crossing the breakpoint with the phone panel open would leave a
       stranded overlay on the desktop layout. */
    let wasMobile = MOBILE();
    window.addEventListener('resize', () => {
      const now = MOBILE();
      if(now !== wasMobile){ wasMobile = now; closeMenu(); }
    });
  })();
})();

/* ══════════════════════════════════════════════════════════════════
   04 · THE PRIORITY AUDIT — the standalone's own logic, in the page

   PORTED, NOT REIMPLEMENTED. This is the component body from
   Priority Audit.dc.html with four changes, all of them noted inline:
   the Start button is replaced by the live first statement, the review
   deep links are removed, the restart clears that first statement, and
   root is resolved from the page rather than a shadow root.

   Everything else is the canonical build: the tie case, the flat
   spread showing all three question sets, the 300ms advance, the
   roving tabindex on the scale, the print stylesheet path. Writing a
   second implementation is what produced two instruments that scored
   the same answers differently.
   ══════════════════════════════════════════════════════════════════ */
(function priorityAudit(){
  const root = SHADOW.getElementById('pa-root');
  if (!root) return;

    /* ══════════════ CONFIG ══════════════
       The only block that normally needs touching. */
    const CONFIG = {
      conversationUrl: 'https://www.parallaxxtransformations.com/contact-daniel-lawson',
      advanceDelay: 300          /* ms after a tap before the next item. 0 disables. */
    };

    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const G = window.gsap;
    const hasGSAP = !!(G && window.ScrollTrigger);
    if (hasGSAP) G.registerPlugin(window.ScrollTrigger);

    const $ = id => SHADOW.getElementById(id);

    /* FOCUS, ACROSS BOTH RUNTIMES.
       In the Design Code runtime root is the #pa-root element and the
       focused node is document.activeElement. In the deployed bundle
       root is a shadow root, and document.activeElement returns the
       shadow HOST rather than the button inside it, which would break
       arrow-key movement on the scale. ShadowRoot.activeElement is the
       one that answers correctly, and a plain element does not have the
       property at all, so the fallback covers the light DOM case. */
    const focused = () => root.activeElement || document.activeElement;

    /* ══════════════ THE SCALE ══════════════
       Five points, frequency, neutral labels, same direction on every
       item. Graded rather than binary because her own noun for the
       problem is trade off, which is by definition a matter of degree.
       A binary forces her to overstate, she notices, and the result
       stops feeling accurate to somebody who reads data all day. */
    const SCALE = ['Never','Rarely','Sometimes','Often','Nearly always'];

    /* ══════════════ THE FIFTEEN ══════════════
       Five per pillar, shuffled so no two consecutive items share one.
       Every statement is an observation she could make out loud in a
       meeting without it costing her anything. Not one of them is a
       verdict she has to accept before she can answer. */
    const ITEMS = [
      { p:'boundaries', t:'I say yes even when at personal capacity.' },
      { p:'emotions',   t:'When someone asks how I am, I answer with what I have been doing.' },
      { p:'needs',      t:'I eat multi-tasking with phone or computer, or I do not eat.' },
      { p:'boundaries', t:'My calendar is full with tasks for others.' },
      { p:'emotions',   t:'I finish something good and feel very little.' },
      { p:'needs',      t:'I would rather do it myself than explain how I want it done.' },
      { p:'boundaries', t:'I cancel personal plans for things that come up.' },
      { p:'emotions',   t:'My worth feels tied to my recent deliverables.' },
      { p:'needs',      t:'I run on less sleep than I would accept from anyone on my team.' },
      { p:'boundaries', t:'I take responsibility for undesignated tasks and overflow.' },
      { p:'emotions',   t:'It is easier to keep moving than it is to dwell on it.' },
      { p:'needs',      t:'Time scheduled for myself is the first to be moved.' },
      { p:'boundaries', t:'When there is a gap in work, I am lost with what to do.' },
      { p:'emotions',   t:'A good result is not good enough.' },
      { p:'needs',      t:'I do not share what I need with others. It takes too long.' }
    ];

    const NAMES = { needs:'Needs', boundaries:'Boundaries', emotions:'Emotions' };

    /* Dead heat order. Only reached when two pillars tie AND the spread
       is wide enough that the flat spread copy does not already own the
       result. Documented so it is a decision rather than an accident. */
    const TIEBREAK = ['boundaries','emotions','needs'];

    /* ══════════════ RESULT COPY ══════════════ */
    const TOP = {
      needs: {
        lead: ['Needs scored highest. Not self-care, which you would rightly ignore, and not rest.',
               'Needs are the baseline you operate from. What you require to keep performing at the level you perform at, and whether any of it is negotiable. Yours has become the first thing negotiated away. You skip the meal, you move the appointment that was yours, you run on less sleep than you would accept from anyone on your team, and you would rather do a thing yourself than take the time to ask.',
               'Consider this. Two of the needs in this model are to know you mattered, and to be recognised for who you are rather than what you produced. Where those two go unmet, the thing that reliably grows in their place is overachievement. As a solution. It worked, it still works, and it is a large part of why you are where you are. It is also why nothing you deliver settles the question for long.'],
        cost: 'A version of you running at max capacity and calling it fine, because there is nothing left to compare it to. And people around you who think you are coping, because you have been careful to make sure of it. And you are still not left with the thing you want most.',
        move: 'One thing in the diary this week that is yours and does not move for anything short of a genuine emergency. One. Not a routine. One personal item that survives contact with a busy Tuesday.',
        qs: [
          'What is the first thing to disappear when the week gets busy, and how long has that been the answer?',
          'What would you say to somebody on your team who was operating the way you are?',
          'When did you decide it was quicker not to ask?'
        ]
      },
      boundaries: {
        lead: ['Boundaries scored highest. The difficulty is that your reason for not declining is a good one. You are paid to deliver, somebody is waiting, and it genuinely would be faster to just do it. Every one of those is true, which is why you still accept it. But a diary filled by other people is a diary of their priorities and their objectives, in life and in business, not yours.',
               'A boundary is the line where you end and everybody else begins, and it is how anyone finds out who you are and what is important to you. Where that line had to be given up early, what grows in its place is a very high commitment to other people and a very small one to yourself. Your personal philosophy for your own life gets limited to whatever survived that trade.'],
        cost: 'A calendar you did not fill, and your own things scheduled into whatever is left, which is nothing. Then the dinner you cancelled, again, on somebody who has stopped expecting you to come. And when a gap of quiet does appear, you are not sure what to do with it.',
        move: 'One no, this week, at work, small. Not the hard one. The one where a professional sounding sentence would normally close it down before you had finished thinking. Watch what it actually costs, which is likely to be some nerves and nothing else.',
        qs: [
          'What is the sentence you use on yourself that makes declining impossible?',
          'Who in your working life has never heard you say no, and what have they concluded from that?',
          'What would you take on if your yes were scarce?'
        ]
      },
      emotions: {
        lead: ['Emotions scored highest. Which does not mean you are emotional, and it does not mean you should be.',
               'It likely means you know what you think. When a feeling arrives as information, there is no time scheduled for it, so it gets filed for another moment. By the time that moment comes, it has gone.',
               'Emotions are a compass rather than a problem to manage. Most of us end up as walking barometers, checking the temperature of the room before we share what we actually think. You are extremely good at that. It is a large part of what makes you good in the room. It also means the person whose temperature never gets checked is you.'],
        cost: 'Work that finished hours ago, projects completed, and what should feel like achievement and success likely does not arrive. Overwhelm, stress and distraction are more common than presence, meaning and a sense of purpose.',
        move: 'One thing you finish this week, give it two minutes before you start the next one. Long enough to notice what finishing it feels like, and to mark it, even the small ones. This is the step where something you did becomes something you have.',
        qs: [
          'When was the last time something you made was finished, and you were the one who said so?',
          'Whose voice is it when you decide something is not ready yet?',
          'What would you have to stop achieving before you found out what you are worth without it?'
        ]
      }
    };

    /* WHAT IS FEEDING IT. One paragraph on how the second pillar keeps
       the first one running. Six ordered pairs, because which one is
       second changes the mechanism, not just the wording. */
    const SECOND = {
      'needs|boundaries': 'Every yes you have already given is spent before the week starts, so there is no room left for anything of your own. You get by on your bare minimum rather than moving towards your highest ideals.',
      'needs|emotions':   'This is what makes the first one look reasonable. If what you are worth is attached to what you delivered lately, then an hour spent on yourself is an hour of self-worth given up. Rescheduling your personal time reads as the strategic call, and it comes out the same way every time.',
      'boundaries|needs': 'This is what makes the extra yes so easy to give. Whatever it costs comes out of your side: the meal, the hour, the thing you had planned. Nobody sees that part and nobody mentions it, so the yes goes on looking like it costs nothing.',
      'boundaries|emotions': 'Saying no is not really a calendar decision for you. If good is the minimum, and what you are worth depends on what you delivered lately, then every no costs you something that matters a lot more than an hour. That is why the reason arrives so fast. You never weighed it up. You already knew what it would cost, and the polite sentence was out before you finished thinking.',
      'emotions|needs':   'This is what removes the alternative. Nothing in the week is in a position to tell you how you are doing, because everything that was not work has already been moved. So the only insight is the one the work gives you, and the work is not qualified to give it.',
      'emotions|boundaries': 'This is what keeps the question at bay. A diary with nothing loose in it never leaves you anywhere long enough to ask what the last thing was worth. Which is convenient, and it is not an accident, and some part of you has known that for a while.'
    };

    /* WHAT YOU ALREADY HOLD. The lowest pillar, written as a real
       strength with a specific observation. Not optional, and never a
       consolation prize. */
    const LOW = {
      needs:      'You have protected a solid baseline. Whatever else is running, you have not made yourself the first thing that gets cut. That is a strong place to keep building from.',
      boundaries: 'Your yes still costs you, which means it still means something. You have held on to the line between what you will carry and what you will not, and many people have traded it without noticing.',
      emotions:   'You can tell when something is good, and you can feel it land. Protect that one. It is the first thing the other two come for.'
    };

    const FLAT = 'There is no clear leader here, which is worth as much as a clear one. Either the last few months have been unusually even, or all three have been running together long enough that you no longer notice any of them separately. Take the questions under whichever of the three you found hardest to read honestly.';

    /* ══════════════ STATE. Memory only. ══════════════
       No localStorage and no sessionStorage anywhere in this file.
       Storage APIs are unavailable in the environments this ships to,
       and a failed write takes the whole result screen with it. */
    const answers = new Array(ITEMS.length).fill(null);
    let current = 0;
    let advanceTimer = null;
    let lastResult = null;

    /* ══════════════ SCREENS ══════════════ */
    const RESULT_SCREENS = ['pa-result','pa-her','pa-close'];
    const ALL_SCREENS = ['pa-intro','pa-items','pa-turn'].concat(RESULT_SCREENS);

    function show(list){
      ALL_SCREENS.forEach(id => {
        const el = $(id); if (!el) return;
        el.classList.toggle('is-on', list.indexOf(id) > -1);
      });
      revealFades();
    }

    /* Fades are a delivery mechanism, never a gate. If GSAP never
       loads, or motion is reduced, everything is simply visible. */
    function revealFades(){
      const nodes = Array.prototype.slice.call(root.querySelectorAll('.pa-fadein'));
      if (reduce){ nodes.forEach(n => n.classList.add('is-in')); return; }
      nodes.forEach((n, i) => {
        const box = n.getBoundingClientRect();
        const near = box.top < window.innerHeight + 120;
        if (!near){ n.classList.add('is-in'); return; }
        window.setTimeout(() => n.classList.add('is-in'), 60 + i * 70);
      });
    }

    function scrollTopOf(id){
      const el = $(id); if (!el) return;
      const y = el.getBoundingClientRect().top + window.pageYOffset - 12;
      try { window.scrollTo({ top: Math.max(0, y), behavior: reduce ? 'auto' : 'smooth' }); }
      catch (e) { window.scrollTo(0, Math.max(0, y)); }
    }

    /* ══════════════ ITEM RENDERING ══════════════ */
    const scaleBox = $('pa-scale');

    function renderItem(){
      const n = ITEMS.length;
      $('pa-count').innerHTML = '<b>' + (current + 1) + '</b> of ' + n;
      $('pa-fill').style.width = Math.max(2, Math.round(((current) / n) * 100)) + '%';
      $('pa-stmt').textContent = ITEMS[current].t;
      $('pa-back').style.visibility = current === 0 ? 'hidden' : 'visible';
      $('pa-live').textContent = 'Statement ' + (current + 1) + ' of ' + n + '. ' + ITEMS[current].t;

      scaleBox.innerHTML = '';
      const picked = answers[current];
      SCALE.forEach((label, i) => {
        const v = i + 1;
        const b = document.createElement('button');
        b.type = 'button';
        b.className = 'pa-opt';
        b.setAttribute('role', 'radio');
        b.setAttribute('aria-checked', picked === v ? 'true' : 'false');
        b.setAttribute('data-v', String(v));
        /* Roving tabindex: one stop for the whole group. Focus lands on
           the current answer, or on the first point if there is none. */
        b.tabIndex = (picked === v || (picked === null && i === 0)) ? 0 : -1;
        b.innerHTML = '<span class="dot" aria-hidden="true"></span><span>' + label + '</span>';
        b.addEventListener('click', () => pick(v));
        b.addEventListener('keydown', onScaleKey);
        scaleBox.appendChild(b);
      });
    }

    function optionAt(i){ return scaleBox.children[i] || null; }

    function moveFocus(delta){
      const kids = Array.prototype.slice.call(scaleBox.children);
      let i = kids.indexOf(focused());
      if (i < 0) i = kids.findIndex(k => k.tabIndex === 0);
      if (i < 0) i = 0;
      let next = i + delta;
      if (next < 0) next = kids.length - 1;
      if (next > kids.length - 1) next = 0;
      kids.forEach(k => { k.tabIndex = -1; });
      kids[next].tabIndex = 0;
      kids[next].focus();
    }

    /* ARROWS MOVE, ENTER SELECTS. Deliberately not the auto-select
       radio pattern: this group advances the screen on select, so
       arrowing through it would skip her past four statements. */
    function onScaleKey(e){
      const k = e.key;
      if (k === 'ArrowDown' || k === 'ArrowRight'){ e.preventDefault(); moveFocus(1); return; }
      if (k === 'ArrowUp' || k === 'ArrowLeft'){ e.preventDefault(); moveFocus(-1); return; }
      if (k === 'Enter' || k === ' ' || k === 'Spacebar'){
        e.preventDefault();
        const v = Number(e.currentTarget.getAttribute('data-v'));
        pick(v);
      }
    }

    function pick(v){
      window.clearTimeout(advanceTimer);
      answers[current] = v;
      Array.prototype.slice.call(scaleBox.children).forEach((b, i) => {
        b.setAttribute('aria-checked', i + 1 === v ? 'true' : 'false');
        b.tabIndex = i + 1 === v ? 0 : -1;
      });
      const go = () => {
        if (current < ITEMS.length - 1){ current++; renderItem(); focusScale(); }
        else turn();
      };
      /* No fake loading between the last item and the turn. */
      if (CONFIG.advanceDelay > 0) advanceTimer = window.setTimeout(go, CONFIG.advanceDelay);
      else go();
    }

    function focusScale(){
      /* Only pull focus if the keyboard is already in the group. A mouse
         user who has just tapped should not have the page jump. */
      if (!scaleBox.contains(focused())) return;
      const target = Array.prototype.slice.call(scaleBox.children).find(k => k.tabIndex === 0);
      if (target) target.focus();
    }

    function back(){
      window.clearTimeout(advanceTimer);
      if (current > 0){ current--; renderItem(); focusScale(); }
    }

    /* ══════════════ SCORING ══════════════
       Each pillar is five items on a one to five scale, so each scores
       5 to 25 and the three are directly comparable. There is no grand
       total, because a grand total invites a band, and there is no
       version of this where she is told she is severe. */
    function score(){
      const totals = { needs:0, boundaries:0, emotions:0 };
      const fives  = { needs:0, boundaries:0, emotions:0 };
      ITEMS.forEach((item, i) => {
        const v = answers[i] === null ? 1 : answers[i];
        totals[item.p] += v;
        if (v === 5) fives[item.p] += 1;
      });
      const ranked = Object.keys(totals).map(k => ({ key:k, total:totals[k], fives:fives[k] }))
        .sort((a, b) => {
          if (b.total !== a.total) return b.total - a.total;
          if (b.fives !== a.fives) return b.fives - a.fives;
          return TIEBREAK.indexOf(a.key) - TIEBREAK.indexOf(b.key);
        });
      const spread = ranked[0].total - ranked[2].total;
      /* A DEAD HEAT AT THE TOP IS ITS OWN CASE, and it is not the same as a
         flat spread. 25 / 25 / 18 is not flat, but there is still no highest
         pillar, and the result copy opens with the words "you scored highest
         on". Painting one of two identical bars coral and then telling her it
         is the one under pressure is a verdict invented by a tiebreak, which
         is exactly the thing this instrument does not do. So when the top two
         are level, the coral comes off, the sentence says so, and she gets
         both sets of questions. */
      const tied = ranked[0].total === ranked[1].total;
      return { ranked: ranked, spread: spread, flat: spread <= 3, tied: tied };
    }

    /* ══════════════ THE TURN ══════════════ */
    function turn(){
      show(['pa-turn']);
      scrollTopOf('pa-turn');
    }

    /* ══════════════ THE RESULT ══════════════ */
    function paras(id, list){
      const el = $(id); el.innerHTML = '';
      list.forEach(t => { const p = document.createElement('p'); p.textContent = t; el.appendChild(p); });
    }

    function qset(pillarKey, label){
      const wrap = document.createElement('div');
      wrap.className = 'pa-qset';
      if (label){
        const h = document.createElement('h4');
        h.textContent = label;
        wrap.appendChild(h);
      }
      const ul = document.createElement('ul');
      ul.className = 'pa-qs';
      TOP[pillarKey].qs.forEach(q => {
        const li = document.createElement('li');
        li.textContent = q;
        ul.appendChild(li);
      });
      wrap.appendChild(ul);
      return wrap;
    }

    function renderResult(res){
      const top = res.ranked[0].key;
      const second = res.ranked[1].key;
      const low = res.ranked[2].key;

      /* Bars. Ranked, gold hairline, coral fill on the highest only. */
      /* On a dead heat the top two are both under the pressure, so neither
         one gets to be the thing feeding the other. */
      const TAGS = res.tied
        ? ['Where the pressure is', 'Where the pressure is', 'What you already hold']
        : ['Where the pressure is', 'What is feeding it', 'What you already hold'];

      const bars = $('pa-bars');
      bars.innerHTML = '';
      res.ranked.forEach((r, i) => {
        const pct = Math.round((r.total / 25) * 100);
        const d = document.createElement('div');
        /* No coral on a dead heat: there is no single highest bar to fill. */
        d.className = 'pa-bar' + (i === 0 && !res.tied ? ' is-top' : '');
        if (res.tied && i < 2) d.classList.add('is-pressure');
        d.innerHTML =
          '<div class="pa-bar-head">' +
            '<span class="name">' + NAMES[r.key] + '</span>' +
            '<span class="val">' + r.total + '<i>&thinsp;/&thinsp;25</i></span>' +
          '</div>' +
          '<div class="pa-track"><div class="pa-fill" data-w="' + pct + '"></div></div>' +
          '<p class="pa-bar-tag">' + TAGS[i] + '</p>';
        bars.appendChild(d);
      });

      const flat = $('pa-flat');
      flat.hidden = !res.flat;
      flat.textContent = res.flat ? FLAT : '';

      $('pa-top-name').textContent = res.tied ? (NAMES[top] + ' and ' + NAMES[second]) : NAMES[top];
      const lead = TOP[top].lead.slice();
      if (res.tied){
        lead.unshift(NAMES[top] + ' and ' + NAMES[second].toLowerCase() +
          ' came out level, on the same score. Read this one first and the other one straight after, because on those numbers neither is the answer on its own.');
      }
      paras('pa-top-lead', lead);
      $('pa-top-cost').textContent = TOP[top].cost;
      paras('pa-second', [ SECOND[top + '|' + second] ]);

      $('pa-low-name').textContent = NAMES[low];
      paras('pa-low-body', [ LOW[low] ]);
      $('pa-move').textContent = TOP[top].move;

      /* On a flat spread all three question sets are shown, because the
         flat copy tells her to take whichever she found hardest to read
         honestly, and she cannot do that if two of them are hidden. */
      const qbox = $('pa-questions');
      qbox.innerHTML = '';
      if (res.flat){
        res.ranked.forEach(r => qbox.appendChild(qset(r.key, 'If you take ' + NAMES[r.key].toLowerCase())));
      } else if (res.tied){
        [top, second].forEach(k => qbox.appendChild(qset(k, NAMES[k])));
      } else {
        qbox.appendChild(qset(top, ''));
      }

      lastResult = res;
    }

    /* Bars animate on reveal only, and have a static fallback. */
    function animateBars(){
      const fills = Array.prototype.slice.call(root.querySelectorAll('.pa-fill'));
      if (reduce){ fills.forEach(f => { f.style.transition = 'none'; f.style.width = f.dataset.w + '%'; }); return; }
      fills.forEach(f => { f.style.width = '0%'; });
      window.setTimeout(() => {
        fills.forEach((f, i) => window.setTimeout(() => { f.style.width = f.dataset.w + '%'; }, i * 130));
      }, 120);
    }

    function finish(){
      renderResult(score());
      show(RESULT_SCREENS);
      scrollTopOf('pa-result');
      animateBars();
      if (hasGSAP && !reduce){ try { window.ScrollTrigger.refresh(); } catch(e){} }
    }

    /* ══════════════ WIRING ══════════════ */
    /* THE START BUTTON IS GONE. The first statement is live on the intro
       screen, so answering it IS the start: it records answer 1, moves to
       item 2, and hands the section the whole viewport. */
    (function liveFirst(){
      const box = $('pa-first-scale');
      if (!box) return;
      $('pa-first-q').textContent = ITEMS[0].t;
      SCALE.forEach((label, i) => {
        const b = document.createElement('button');
        b.type = 'button'; b.className = 'pa-opt';
        b.setAttribute('role', 'radio'); b.setAttribute('aria-checked', 'false');
        b.setAttribute('data-v', String(i + 1));
        b.innerHTML = '<span class="dot" aria-hidden="true"></span><span>' + label + '</span>';
        b.addEventListener('click', () => {
          answers[0] = i + 1;
          current = 1;
          renderItem();
          show(['pa-items']);
          scrollTopOf('pa-items');
        });
        box.appendChild(b);
      });
    })();
    $('pa-back').addEventListener('click', back);
    $('pa-see').addEventListener('click', finish);
    $('pa-pdf').addEventListener('click', () => { window.print(); });
    $('pa-again').addEventListener('click', () => {
      for (let i = 0; i < answers.length; i++) answers[i] = null;
      current = 0; lastResult = null;
      root.querySelectorAll('#pa-first-scale .pa-opt')
          .forEach(b => b.setAttribute('aria-checked', 'false'));
      show(['pa-intro']); scrollTopOf('pa-intro');
    });
    $('pa-cta').setAttribute('href', CONFIG.conversationUrl);

    /* Number keys are a shortcut on the items screen, nothing more. */
    document.addEventListener('keydown', e => {
      const items = $('pa-items');
      if (!items || !items.classList.contains('is-on')) return;
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      if (e.key >= '1' && e.key <= '5'){ e.preventDefault(); pick(Number(e.key)); return; }
      if (e.key === 'Backspace'){ e.preventDefault(); back(); }
    });

    /* ══════════════ SMOOTH SCROLL ══════════════
       Optional, and the page is complete without it. */
    if (window.Lenis && !reduce){
      try {
        const lenis = new window.Lenis({ lerp: 0.11 });
        const raf = t => { lenis.raf(t); window.requestAnimationFrame(raf); };
        window.requestAnimationFrame(raf);
        if (hasGSAP) lenis.on('scroll', window.ScrollTrigger.update);
      } catch(e){}
    }

    renderItem();
    revealFades();

    /* The standalone's ?state= review deep links are deliberately not
       ported. On a home page a stray query string would drop a visitor
       into somebody else's result. */



})();
  }

  class ParallaxxHomeWomen extends HTMLElement {
    connectedCallback(){
      if (this._mounted) return; this._mounted = true;
      addFonts();
      var shadow = this.attachShadow({mode:'open'});
      shadow.innerHTML = '<style>'+CSS+'</style>'+HTML;
      var host = this;
      /* THE HOST ITSELF, NOT JUST ITS ANCESTORS.
         collapseAndRefresh only walks upward. If the Wix editor has given
         the widget its own fixed height -- which it does by default -- the
         element keeps it and the surplus renders as empty page background
         under the content. Clearing it here is the difference between a
         page and a page followed by a white cliff. Added 4 Aug 2026. */
      try{ host.style.height='auto'; host.style.minHeight='0px'; }catch(e){}
      loadLibs().then(function(){ try{ boot(shadow); }catch(e){ console.error('[px] boot failed:', e); } })
        .catch(function(){ try{ boot(shadow); }catch(e){} });
      requestAnimationFrame(function(){ collapseAndRefresh(host); });
      [400,1200,2500].forEach(function(t){ setTimeout(function(){ collapseAndRefresh(host); }, t); });
      window.addEventListener('resize', function(){ collapseAndRefresh(host); }, {passive:true});
    }
  }
  customElements.define('parallaxx-home-women', ParallaxxHomeWomen);
})();
