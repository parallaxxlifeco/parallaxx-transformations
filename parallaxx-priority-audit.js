/* PARALLAXX TRANSFORMATIONS - The Priority Audit. Wix Custom Element. Tag: parallaxx-priority-audit.
   GENERATED by build-priority-audit-bundle.py from "Priority Audit.dc.html".
   DO NOT EDIT THIS FILE - edit the .dc.html and rerun the build.

   DEPLOY AS A CUSTOM ELEMENT, NOT AN HTML EMBED. An embed renders inside a
   fixed-height iframe, hides the site header and footer, and puts every word
   on a filesusr.com origin Google will not attribute to the site. Bump the
   ?v= on the source URL on every deploy so the CDN does not serve the old
   bundle. Page SEO lives in Wix page settings, not in here.

   In the Wix editor: Add -> Embed Code -> Custom Element.
     Source    the hosted URL of this file, with ?v= bumped
     Tag name  parallaxx-priority-audit
   Leave the site Header and Footer ON. This is an instrument on a page,
   not a full-bleed page of its own. */
(function(){
  if (customElements.get('parallaxx-priority-audit')) return;

  var CSS = `/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX TRANSFORMATIONS  ·  THE PRIORITY AUDIT
   -------------------------------------------------------------------
   Fifteen statements about her working week, five per pillar, five
   point frequency scale. The three pillars stay hidden until item
   fifteen is answered, then arrive as the thing her own answers
   produced. Three scores, ranked. No grand total. No band. No
   severity anywhere.

   THIS IS THE COUNTERPART TO THE MEN'S FIVE ARCHETYPE QUIZ, NOT A
   COPY OF IT. His ends in a label, because he arrives already
   doubting himself and a label is a relief. Joining a category is the
   one thing this avatar will not do, so hers ends in a measurement
   she can read and argue with.

   SCREENS
     01 INTRO   two sentences, one button, no form and no email
     02 ITEMS   one at a time, five tap targets, back preserves answers
     03 TURN    cream. the pillars revealed. no score on this screen
     04 RESULT  bars, then six blocks, one scrolling column
     05 CLOSE   the conversation, the PDF, and the bridge sentence

   ═══════════════════════════════════════════════════════════════════
   THE ACCENT RULE.  GOLD IS DANIEL. CORAL IS HER MOVE.
   -------------------------------------------------------------------
   GOLD  #E8C65F  everything Daniel says. Section labels, hairlines,
                  the handwriting, the bridge quote, the selected
                  state on the scale (that is a reading being taken,
                  not an action being sold).
   CORAL #FF501F  her moves only. Start. See your three. Bring this to
                  a conversation. And the fill on her highest bar,
                  which the build spec calls for by name.

   Coral never appears as decorative emphasis in prose. Structural
   emphasis inside body copy is cream on navy, ink on cream. Nothing
   pulses. Backgrounds alternate navy and cream, never two creams
   adjacent: navy intro, navy items, cream turn, navy result head,
   cream her half, navy close.
   ═══════════════════════════════════════════════════════════════════ */

  /* THE RESET IS WRAPPED IN :where() ON PURPOSE.
     It has to be scoped, because in the Design Code runtime this sheet is
     loaded into the live page and an unscoped star-margin-zero rule would flatten
     the Wix header and footer sitting around it. But an ID-scoped star selector scores
     an ID, which beats every class in this stylesheet: .pa-wrap lost its
     max-width padding, .pa-col lost its auto margins, and the whole
     component rendered hard against the left edge. :where() scopes the
     selector while contributing nothing to specificity, so the reset is
     scoped AND still the weakest rule in the file. */
  :where(#pa-root) *,:where(#pa-root) *::before,:where(#pa-root) *::after{box-sizing:border-box;margin:0;padding:0}
  #pa-root ::selection{background:#FF501F;color:#fff}

  #pa-root{
    --navy:#04122A; --navy-2:#0A1D3C; --navy-ink:#030C1C;
    --cream:#F1ECE1; --cream-2:#F7F3EA;
    --gold:#E8C65F; --gold-lift:#F3DD93; --bronze:#A08A5E;
    --coral:#FF501F; --coral-lift:#FF6A3D; --coral-ink:#C0391A;
    --mist:#B1BFD7; --slate:#7C89A3; --label:#5E6B85;
    --ink:#1F2A3D; --ink-soft:#5A6274; --line:#12233F; --line-cream:#DED6C4;
    background:var(--navy);
    color:var(--mist);
    font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,sans-serif;
    font-size:16px;line-height:1.7;position:relative;overflow-x:clip;
    -webkit-font-smoothing:antialiased;
  }

  .pa-serif{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em;line-height:1.18;color:var(--cream)}
  .pa-hand{font-family:'Lumios Marker','Permanent Marker',cursive;color:var(--gold);line-height:1.35}
  .pa-wrap{max-width:1240px;margin:0 auto;padding-left:clamp(20px,4vw,52px);padding-right:clamp(20px,4vw,52px)}
  .pa-col{max-width:660px;margin-left:auto;margin-right:auto}
  .pa-label{font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;color:var(--label)}
  .pa-gold-label{color:var(--gold);opacity:.9}
  #pa-root strong{font-weight:600;color:var(--cream)}
  .on-cream strong{color:var(--ink)}
  .sr{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap}

  /* ── BUTTONS ─────────────────────────────────────────────────── */
  .pa-btn{display:inline-flex;align-items:center;justify-content:center;gap:.55em;
    background:var(--coral);color:#fff;font-weight:700;font-size:.98rem;font-family:inherit;
    padding:1.05em 2.2em;border:0;border-radius:999px;text-decoration:none;cursor:pointer;
    transition:background .3s,transform .3s,box-shadow .3s;
    box-shadow:0 10px 30px -12px rgba(255,80,31,.5)}
  .pa-btn:hover{background:var(--coral-lift);transform:translateY(-2px)}
  .pa-btn:focus-visible{outline:2px solid var(--gold);outline-offset:3px}

  .pa-ghost{display:inline-flex;align-items:center;justify-content:center;gap:.55em;
    color:var(--mist);background:none;border:1px solid rgba(232,198,95,.4);
    font-weight:600;font-size:.94rem;font-family:inherit;padding:1em 2em;border-radius:999px;
    text-decoration:none;cursor:pointer;transition:background .3s,border-color .3s,color .3s}
  .pa-ghost:hover{background:rgba(241,236,225,.08);border-color:var(--cream);color:var(--cream)}
  .pa-ghost:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .on-cream .pa-ghost{color:var(--ink-soft);border-color:rgba(160,138,94,.55)}
  .on-cream .pa-ghost:hover{background:rgba(160,138,94,.1);border-color:var(--bronze);color:var(--ink)}

  .pa-quiet{background:none;border:0;color:var(--slate);font-family:inherit;font-size:.8rem;
    letter-spacing:.06em;cursor:pointer;padding:.6em .2em;transition:color .25s}
  .pa-quiet:hover{color:var(--cream)}
  .pa-quiet:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .on-cream .pa-quiet{color:var(--ink-soft)}
  .on-cream .pa-quiet:hover{color:var(--ink)}

  /* ── SCREENS ─────────────────────────────────────────────────── */
  .pa-screen{display:none}
  .pa-screen.is-on{display:block}
  .pa-stage{min-height:clamp(520px,78svh,860px);display:flex;flex-direction:column;justify-content:center;
    padding:clamp(40px,7vw,86px) 0}
  .pa-fadein{opacity:0;transform:translateY(16px);
    transition:opacity .6s cubic-bezier(.16,1,.3,1),transform .6s cubic-bezier(.16,1,.3,1)}
  .pa-fadein.is-in{opacity:1;transform:none}

  /* ── 01 INTRO ────────────────────────────────────────────────── */
  #pa-intro{background:var(--navy)}
  #pa-intro .pa-col{text-align:center;max-width:880px}
  #pa-intro h1{font-size:clamp(1.7rem,3.5vw,2.55rem);margin:20px auto 0;max-width:none}
  #pa-intro h1 .l{display:block}
  #pa-intro h1 em .l{display:block}
  #pa-intro h1 em{font-style:normal;color:var(--mist);font-weight:300;display:block;font-size:.55em;
    margin:.52em auto 0;letter-spacing:0;max-width:none;line-height:1.3}
  #pa-intro p.lede{margin:26px auto 0;max-width:min(830px,100%);font-size:.97rem;
    line-height:1.62;text-wrap:pretty}
  #pa-intro .pa-hand{margin-top:34px;font-size:clamp(1rem,2.4vw,1.22rem)}
  #pa-intro .cta{margin-top:36px;display:flex;flex-direction:column;align-items:center;gap:15px}
  #pa-intro .meta{font-size:.72rem;letter-spacing:.16em;text-transform:uppercase;color:var(--slate);font-weight:700}

  .pa-legend{margin:38px auto 0;max-width:560px;border-top:1px solid rgba(232,198,95,.28);padding-top:16px;
    display:flex;justify-content:space-between;gap:10px;
    font-size:.66rem;letter-spacing:.16em;text-transform:uppercase;font-weight:700;color:var(--label)}
  .pa-legend i{font-style:normal;color:var(--slate)}
  @media(max-width:520px){.pa-legend i{display:none}}

  /* ── 02 ITEMS ────────────────────────────────────────────────── */
  #pa-items{background:var(--navy)}
  .pa-prog{max-width:660px;margin:0 auto 40px;width:100%}
  .pa-prog-top{display:flex;align-items:baseline;justify-content:space-between;gap:14px;margin-bottom:11px}
  .pa-prog-count{font-family:'Poppins',sans-serif;font-size:.9rem;color:var(--cream);letter-spacing:.02em}
  .pa-prog-count b{color:var(--gold);font-weight:500}
  .pa-prog-bar{height:2px;background:var(--line);border-radius:99px;overflow:hidden}
  .pa-prog-fill{height:100%;width:0;background:linear-gradient(90deg,var(--gold-lift),var(--gold));
    border-radius:99px;transition:width .45s cubic-bezier(.16,1,.3,1)}

  #pa-stmt{font-size:clamp(1.12rem,2.2vw,1.5rem);line-height:1.34;min-height:2.7em;
    display:flex;align-items:center;max-width:34ch;margin-bottom:28px}
  .pa-ask{font-size:.7rem;letter-spacing:.2em;text-transform:uppercase;color:var(--slate);font-weight:700;margin-bottom:18px}

  .pa-scale{display:flex;flex-direction:column;gap:9px}
  .pa-opt{display:flex;align-items:center;gap:15px;width:100%;text-align:left;
    background:rgba(18,35,63,.42);border:1px solid var(--line);border-radius:14px;
    padding:16px 19px;color:var(--mist);font-family:inherit;font-size:1rem;font-weight:500;
    cursor:pointer;transition:border-color .22s,background .22s,transform .22s,color .22s}
  .pa-opt:hover{border-color:rgba(232,198,95,.55);background:rgba(18,35,63,.78);transform:translateX(3px);color:var(--cream)}
  .pa-opt:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
  .pa-opt .dot{flex:0 0 auto;width:26px;height:26px;border-radius:50%;display:grid;place-items:center;
    border:1px solid var(--line);transition:background .22s,border-color .22s}
  .pa-opt .dot::after{content:'';width:9px;height:9px;border-radius:50%;background:transparent;transition:background .22s}
  .pa-opt:hover .dot{border-color:rgba(232,198,95,.7)}
  .pa-opt[aria-checked="true"]{border-color:var(--gold);background:rgba(232,198,95,.09);color:var(--cream)}
  .pa-opt[aria-checked="true"] .dot{border-color:var(--gold)}
  .pa-opt[aria-checked="true"] .dot::after{background:var(--gold)}

  .pa-navrow{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-top:26px}
  .pa-hint{font-size:.7rem;letter-spacing:.1em;color:var(--label)}
  @media(max-width:620px){.pa-hint{display:none}}

  /* ── 03 THE TURN ─────────────────────────────────────────────── */
  #pa-turn{background:var(--cream)}
  #pa-turn .pa-col{max-width:820px}
  #pa-turn .pa-stage{min-height:0;padding:clamp(46px,6vw,74px) 0}

  /* ── THE RING ──────────────────────────────────────────────────────
     UNSCORED, AND IT STAYS THAT WAY. The V1 wheel had nine boxes to
     fill in, which fifteen items cannot populate: that is under two
     questions per sub-area, and a two-item scale is noise. It also
     said 'neglect' nine times before she had answered anything.

     So this is not a chart. It is a picture of a claim: those fifteen
     were three things, and the three make one thing. The last line of
     this screen asserts that each one holds the other two up, and
     until now that assertion had no picture. Three equal arcs, equal
     lengths, no numbers, no totals, no boxes.

     Gold family because it is Daniel naming his own framework. Coral
     never appears here; nothing on this screen is her move except the
     button. */
  #pa-turn-grid{display:grid;grid-template-columns:210px 1fr;gap:clamp(22px,3vw,38px);
    align-items:center;margin-top:24px}
  .pa-ring{width:100%;max-width:210px;margin:0 auto}
  .pa-ring svg{width:100%;height:auto;display:block;overflow:visible}
  .pa-ring .arc{fill:none;stroke-width:15;stroke-linecap:round;
    stroke-dasharray:181;stroke-dashoffset:181;
    transition:stroke-dashoffset 1.1s cubic-bezier(.16,1,.3,1)}
  .pa-ring .arc.a1{stroke:#A08A5E}
  .pa-ring .arc.a2{stroke:#C9A96B;transition-delay:.13s}
  .pa-ring .arc.a3{stroke:#E8C65F;transition-delay:.26s}
  .pa-ring.is-in .arc{stroke-dashoffset:0}
  .pa-ring .mid{font-family:'Poppins','Montserrat',sans-serif;font-weight:500;fill:#A08A5E;
    font-size:19px;letter-spacing:.02em;text-anchor:middle}
  /* The swatch is the only thing tying an arc to its definition, so it
     carries the same three values in the same order. */
  .pa-def{display:grid;grid-template-columns:auto 1fr;gap:0 12px;align-items:start}
  .pa-def .sw{width:10px;height:10px;border-radius:50%;margin-top:.52em}
  .pa-def:nth-child(1) .sw{background:#A08A5E}
  .pa-def:nth-child(2) .sw{background:#C9A96B}
  .pa-def:nth-child(3) .sw{background:#E8C65F}
  .pa-def h3,.pa-def p{grid-column:2}

  @media(max-width:720px){
    #pa-turn-grid{grid-template-columns:1fr;gap:22px;margin-top:20px}
    .pa-ring{max-width:132px}
  }
  /* NO GSAP, NO MOTION, NO PROBLEM. The ring is complete on arrival. */
  @media(prefers-reduced-motion:reduce){
    .pa-ring .arc{stroke-dashoffset:0;transition:none}
  }
  #pa-turn h2{color:var(--ink);font-size:clamp(1.55rem,3.1vw,2.15rem);font-weight:500}
  .pa-defs{display:flex;flex-direction:column;gap:0}
  .pa-def{padding:14px 0;border-top:1px solid rgba(160,138,94,.42)}
  .pa-def:last-child{border-bottom:1px solid rgba(160,138,94,.42)}
  .pa-def h3{font-family:'Poppins',sans-serif;font-weight:500;font-size:1.08rem;color:var(--ink);
    letter-spacing:-.01em;margin-bottom:2px}
  .pa-def p{color:var(--ink-soft);font-size:.92rem;line-height:1.55;max-width:52ch}
  #pa-turn .after{margin-top:22px;color:var(--ink-soft);max-width:56ch;font-size:.95rem}
  #pa-turn .cta{margin-top:26px}

  /* ── 04 RESULT ───────────────────────────────────────────────── */
  #pa-result{background:var(--navy)}
  .pa-r-head{padding:clamp(46px,7vw,88px) 0 clamp(28px,4vw,40px);
    background:radial-gradient(120% 90% at 50% 0%,rgba(18,35,63,.9),transparent 70%)}
  .pa-r-head h2{font-size:clamp(1.7rem,4vw,2.7rem);margin:12px 0 0;max-width:20ch}

  /* THE BARS. Gold hairline track, coral fill on the highest only.
     The number sits beside the bar because this avatar reads data all
     day and a bar with no digit on it reads as an opinion. */
  .pa-key{margin:20px 0 0;font-size:.93rem;color:var(--mist);max-width:58ch;
    border-left:1px solid rgba(232,198,95,.45);padding-left:16px}
  .pa-bars{margin:30px 0 0;display:flex;flex-direction:column;gap:32px}
  /* The tag names the block this bar owns further down the page. Gold,
     because it is Daniel labelling the reading, not her taking an action. */
  .pa-bar-tag{margin-top:8px;font-size:.62rem;letter-spacing:.2em;text-transform:uppercase;
    font-weight:700;color:var(--label)}
  .pa-bar.is-top .pa-bar-tag,.pa-bar.is-pressure .pa-bar-tag{color:var(--gold)}
  .pa-bar-head{display:flex;align-items:baseline;justify-content:space-between;gap:14px;margin-bottom:9px}
  .pa-bar .name{font-family:'Poppins',sans-serif;font-size:1.06rem;color:var(--slate);letter-spacing:-.01em;transition:color .3s}
  .pa-bar .val{font-family:'Poppins',sans-serif;font-size:1.06rem;color:var(--slate);transition:color .3s}
  .pa-bar .val i{font-style:normal;opacity:.45;font-size:.82em}
  .pa-track{height:8px;border-radius:99px;background:rgba(232,198,95,.13);
    box-shadow:inset 0 0 0 1px rgba(232,198,95,.3);overflow:hidden}
  .pa-fill{height:100%;width:0;border-radius:99px;background:rgba(177,191,215,.45);
    transition:width .9s cubic-bezier(.16,1,.3,1)}
  .pa-bar.is-top .name,.pa-bar.is-top .val{color:var(--cream)}
  .pa-bar.is-top .pa-fill{background:var(--coral)}
  @media(prefers-reduced-motion:reduce){.pa-fill{transition:none}}

  .pa-flat{margin:30px 0 0;padding:20px 22px;border-left:2px solid var(--gold);
    background:rgba(18,35,63,.5);border-radius:0 14px 14px 0;color:var(--mist);font-size:.97rem}
  .pa-flat[hidden]{display:none}
  .pa-gap{height:clamp(40px,6vw,72px)}

  /* Section rules. The label is Daniel naming the block, so it is gold. */
  .pa-rule{display:flex;align-items:center;gap:16px;margin:clamp(44px,6vw,68px) 0 22px}
  .pa-rule span{white-space:nowrap;font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;
    font-weight:700;color:var(--gold)}
  .pa-rule::after{content:'';flex:1;height:1px;background:linear-gradient(90deg,rgba(232,198,95,.4),transparent)}
  .on-cream .pa-rule span{color:var(--bronze)}
  .on-cream .pa-rule::after{background:linear-gradient(90deg,rgba(160,138,94,.55),transparent)}

  .pa-prose p{margin-bottom:1.1em;font-size:1.02rem}
  .pa-prose p:last-child{margin-bottom:0}
  .pa-pillar-name{font-family:'Poppins',sans-serif;font-weight:500;letter-spacing:-.015em;
    font-size:clamp(1.5rem,3.2vw,2.1rem);color:var(--cream);margin-bottom:16px}
  .on-cream .pa-pillar-name{color:var(--ink)}
  .pa-inset{margin:26px 0;padding:20px 22px;border-left:2px solid rgba(232,198,95,.55);
    background:rgba(18,35,63,.42);border-radius:0 14px 14px 0}
  .pa-inset h4{font-family:'Poppins',sans-serif;font-weight:500;font-size:.98rem;color:var(--gold);
    letter-spacing:.01em;margin-bottom:7px}
  .pa-inset p{font-size:.99rem;color:var(--mist)}

  /* Her half of the result, on cream. */
  #pa-her{background:var(--cream);color:var(--ink-soft);padding:clamp(48px,7vw,88px) 0}
  #pa-her .pa-prose p{color:var(--ink-soft)}
  #pa-her .pa-inset{border-left-color:var(--bronze);background:rgba(255,255,255,.62);
    box-shadow:0 12px 34px -26px rgba(31,42,61,.55)}
  #pa-her .pa-inset h4{color:var(--bronze)}
  #pa-her .pa-inset p{color:var(--ink-soft)}
  #pa-her .pa-rule:first-of-type{margin-top:0}

  .pa-qs{margin:14px 0 0;list-style:none;display:flex;flex-direction:column;gap:0}
  .pa-qs li{padding:19px 0;border-top:1px solid rgba(160,138,94,.4);
    font-family:'Poppins',sans-serif;font-weight:400;font-size:1.06rem;line-height:1.45;
    color:var(--ink);letter-spacing:-.005em}
  .pa-qs li:last-child{border-bottom:1px solid rgba(160,138,94,.4)}
  .pa-qset + .pa-qset{margin-top:30px}
  .pa-qset h4{font-size:.66rem;letter-spacing:.22em;text-transform:uppercase;font-weight:700;
    color:var(--bronze);margin-bottom:2px}
  .pa-note{margin-top:18px;font-size:.87rem;color:#8A8580}

  /* ── 05 CLOSE ────────────────────────────────────────────────── */
  #pa-close{background:var(--navy);padding:clamp(48px,7vw,92px) 0 clamp(54px,8vw,100px)}
  .pa-bridge{border-left:2px solid var(--gold);padding:24px 26px;background:rgba(18,35,63,.5);
    border-radius:0 16px 16px 0;font-family:'Poppins',sans-serif;font-weight:300;
    font-size:clamp(1rem,2.1vw,1.2rem);line-height:1.55;color:var(--cream);letter-spacing:-.01em}
  .pa-bridge p + p{margin-top:.9em}
  .pa-bridge p:last-child{color:var(--gold-lift)}
  .pa-close-cta{margin-top:38px;display:flex;flex-wrap:wrap;gap:14px;align-items:center}
  .pa-close-sub{margin-top:16px;font-size:.87rem;color:var(--slate);max-width:52ch}
  .pa-restart{margin-top:34px;padding-top:24px;border-top:1px solid rgba(232,198,95,.16)}

  /* ── RESPONSIVE ──────────────────────────────────────────────── */
  @media(max-width:620px){
    #pa-stmt{max-width:none;min-height:0;margin-bottom:22px}
    #pa-intro h1 .l,#pa-intro h1 em .l{display:inline}
    .pa-opt{padding:15px 17px;font-size:.96rem}
    .pa-stage{min-height:clamp(480px,72svh,760px);padding:clamp(30px,7vw,60px) 0}
    .pa-close-cta{flex-direction:column;align-items:stretch}
    .pa-close-cta .pa-btn,.pa-close-cta .pa-ghost{width:100%}
  }

  /* ── REDUCED MOTION. Nothing is gated on an animation. ───────── */
  @media(prefers-reduced-motion:reduce){
    #pa-root *{animation:none!important}
    .pa-fadein{opacity:1!important;transform:none!important;transition:none!important}
    .pa-btn:hover,.pa-opt:hover{transform:none}
  }

  /* ── PRINT. The ghost CTA is window.print(), so this IS the PDF.
     Navy prints as a wall of ink, so the whole result flattens to
     white with ink type, and the controls go. ─────────────────── */
  @media print{
    #pa-root,#pa-result,#pa-close,#pa-her,.pa-r-head{background:#fff!important;color:#1F2A3D!important}
    #pa-root{font-size:11pt}
    .pa-serif,.pa-pillar-name,.pa-r-head h2{color:#04122A!important}
    .pa-prose p,.pa-inset p,.pa-flat{color:#3A4459!important}
    .pa-label,.pa-rule span,.pa-inset h4,.pa-qset h4{color:#8A6F45!important}
    .pa-bar .name,.pa-bar .val{color:#1F2A3D!important}
    .pa-key{color:#3A4459!important;border-left-color:#A08A5E!important}
    .pa-bar-tag{color:#8A6F45!important}
    .pa-track{background:#EDE7DC!important;box-shadow:inset 0 0 0 1px #C9BE9F!important}
    .pa-fill{background:#B9C2CF!important}
    .pa-bar.is-top .pa-fill{background:#FF501F!important}
    .pa-inset,.pa-flat,.pa-bridge{background:#F7F3EA!important;border-left-color:#A08A5E!important}
    .pa-bridge{color:#1F2A3D!important}
    .pa-qs li{color:#1F2A3D!important;border-color:#C9BE9F!important}
    .pa-btn,.pa-ghost,.pa-quiet,.pa-restart,.pa-close-cta,.pa-close-sub{display:none!important}
    .pa-fadein{opacity:1!important;transform:none!important}
    .pa-rule{break-after:avoid}
    .pa-inset,.pa-bar{break-inside:avoid}
    /* Screen breathing room becomes dead paper. */
    .pa-gap{height:0}
    .pa-r-head{padding-top:0}
    .pa-rule{margin-top:clamp(20px,3vw,30px)}
  }`;

  var HTML = `<div id="pa-root">

<!-- ══════════════════════════════════════════════════════════════
     01 · INTRO
     One screen, one button, and a legend. No form, no email, no
     promise of a type at the end.

     THE HEADLINE IS A FACT ABOUT THE ARRANGEMENT, NOT A VERDICT ON
     HER. Behind despite the success, not because of it. Her work is
     load bearing and nothing on this screen touches it.

     THE WINDOW IS SIX MONTHS, and it is stated here and repeated on
     every single item. Left open she answers for her best week,
     because answering for her best week is the habit the instrument
     is measuring.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen is-on" id="pa-intro">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">
        <p class="pa-label pa-gold-label pa-fadein">The Priority Audit</p>
        <h1 class="pa-serif pa-fadein"><span class="l">Why you always feel behind</span><span class="l">despite your success</span>
          <em><span class="l">You&rsquo;ve achieved so much.</span><span class="l">So why does it still feel like it&rsquo;s never enough?</span></em>
        </h1>
        <p class="lede pa-fadein">Rank how true the following 15 statements are for the past six months, to unlock what priority would have the most immediate impact in your life right now.</p>
        <div class="pa-legend pa-fadein" aria-hidden="true">
          <span>Never</span><i>Rarely</i><i>Sometimes</i><i>Often</i><span>Nearly always</span>
        </div>
        <div class="cta pa-fadein">
          <button class="pa-btn" type="button" id="pa-start">Start the audit <span aria-hidden="true">&rarr;</span></button>
          <span class="meta">About 90 seconds, all results on screen</span>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     02 · THE FIFTEEN
     One at a time, full width, five tap targets. Presented in the
     shuffled order from the build spec, so consecutive items never
     sit in the same pillar and the grouping is invisible.

     PROGRESS IS "6 OF 15", NEVER A PERCENTAGE. A percentage is a
     completion metric, and completion metrics are the thing.

     ACCESSIBILITY. The scale is one radiogroup labelled by the
     statement. Arrows move focus, Enter or Space selects, 1 to 5 is
     a shortcut, and the count change is announced politely.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen" id="pa-items">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">

        <div class="pa-prog">
          <div class="pa-prog-top">
            <span class="pa-prog-count" id="pa-count"><b>1</b> of 15</span>
          </div>
          <div class="pa-prog-bar"><div class="pa-prog-fill" id="pa-fill"></div></div>
        </div>

        <p class="pa-ask">How often, these past six months</p>
        <h2 class="pa-serif" id="pa-stmt"></h2>

        <div class="pa-scale" id="pa-scale" role="radiogroup" aria-labelledby="pa-stmt"></div>

        <div class="pa-navrow">
          <button class="pa-quiet" type="button" id="pa-back">&larr; Back</button>
          <span class="pa-hint">Arrow keys move, Enter selects, or press 1 to 5</span>
        </div>

        <p class="sr" id="pa-live" role="status" aria-live="polite"></p>

      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     03 · THE TURN
     Immediately after item fifteen, before any score. This is the
     whole reason the pillars were hidden: she meets the framework
     through her own data instead of through an introduction to it.

     Cream, because the change of material IS the reveal. Nothing on
     this screen is a number.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen on-cream" id="pa-turn">
  <div class="pa-stage">
    <div class="pa-wrap">
      <div class="pa-col">
        <p class="pa-label pa-fadein" style="color:#A08A5E">Before your result</p>
        <h2 class="pa-serif pa-fadein" style="margin-top:9px">Our approach to life is built on three pillars.</h2>

        <div id="pa-turn-grid">

          <div class="pa-ring pa-fadein">
            <svg viewBox="0 0 240 240" aria-hidden="true" focusable="false">
              <path class="arc a1" d="M 130.03 24.53 A 96 96 0 0 1 207.70 159.05"></path>
              <path class="arc a2" d="M 197.67 176.43 A 96 96 0 0 1 42.33 176.43"></path>
              <path class="arc a3" d="M 32.30 159.05 A 96 96 0 0 1 109.97 24.53"></path>
              <text class="mid" x="120" y="128">Self-Esteem</text>
            </svg>
          </div>

          <div class="pa-defs">
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Needs</h3>
              <p>What you require to operate at your potential. Met with consistency, care and respect.</p>
            </div>
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Boundaries</h3>
              <p>What you accept, and what you are able to decline. Honoured, including by you.</p>
            </div>
            <div class="pa-def pa-fadein">
              <span class="sw" aria-hidden="true"></span>
              <h3>Emotions</h3>
              <p>What you feel, as an internal compass that highlights your needs and your boundaries.</p>
            </div>
          </div>

        </div>

        <p class="after pa-fadein">These are not three skills to go and develop. They are three dimensions of self that were supposed to be met for you, consistently, by somebody else, a long time ago. Most of us got some of them. Whatever was missing, you found another way to fulfil, and in your case that way has worked extremely well.</p>
        <p class="after pa-fadein" style="margin-top:14px">All three hold each other up. We are only as equipped as the weakest one.</p>

        <div class="cta pa-fadein">
          <button class="pa-btn" type="button" id="pa-see">See your three <span aria-hidden="true">&rarr;</span></button>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     04 · THE RESULT
     Bars, then six blocks, one scrolling column, screenshottable.
     Three scores out of twenty five each. No grand total, because a
     grand total invites a band, and there is no version of this
     where she is told she is severe.

     The comparison on the screen is between her three, never against
     a population.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen" id="pa-result">

  <div class="pa-r-head">
    <div class="pa-wrap"><div class="pa-col">
      <p class="pa-label pa-gold-label">Your three</p>
      <h2 class="pa-serif">Your three foundational pillars of self.</h2>

      <p class="pa-key">Each bar shows how far that pillar is from being met in the way that would support you best. The lower the score, the more developed that pillar already is. A higher score means less practice and less focus have gone into protecting and developing that part of yourself.</p>

      <div class="pa-bars" id="pa-bars"></div>
      <p class="pa-flat" id="pa-flat" hidden></p>
    </div></div>
  </div>

  <!-- THE THROUGH LINE. Shown to everyone, before any personal copy,
       because it is the finding and it is not conditional on a score. -->
  <div class="pa-wrap"><div class="pa-col">
    <div class="pa-rule"><span>What is underneath all three</span></div>
    <div class="pa-prose">
      <p>No single response to any of the fifteen statements means much on its own, which is why this is hard to see. Combined, they reveal a pattern, and the pattern gives you clues about the outcomes in your life.</p>
      <p>The three pillars together are what fuel our self-esteem. Left underdeveloped, they leave self-esteem as something that happens to you, depending on how the quarter went. It can go the other way too. Developing yourself through the three pillars you just scored stops life outcomes being a matter of chance and makes them a matter of intentional design.</p>
    </div>

    <div class="pa-rule"><span>Where the pressure is</span></div>
    <p class="pa-pillar-name" id="pa-top-name"></p>
    <div class="pa-prose" id="pa-top-lead"></div>
    <div class="pa-inset">
      <h4>The cost</h4>
      <p id="pa-top-cost"></p>
    </div>

    <div class="pa-rule"><span>What is feeding it</span></div>
    <p class="pa-pillar-name" id="pa-second-name"></p>
    <div class="pa-prose" id="pa-second"></div>
    <div class="pa-gap"></div>
  </div></div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     HER HALF, ON CREAM
     What she already holds, the one rep, and the three questions.
     The questions are where the clarity she connects with actually
     lives, so nothing gets shortened to make room for anything else.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen on-cream" id="pa-her">
  <div class="pa-wrap"><div class="pa-col">

    <div class="pa-rule"><span>What you already hold</span></div>
    <p class="pa-pillar-name" id="pa-low-name"></p>
    <div class="pa-prose" id="pa-low-body"></div>

    <div class="pa-rule"><span>The first move</span></div>
    <div class="pa-inset">
      <h4 id="pa-move-head">One thing, designed on purpose</h4>
      <p id="pa-move"></p>
    </div>

    <div class="pa-rule"><span>Three questions for you</span></div>
    <div id="pa-questions"></div>
    <p class="pa-note">No right answers, and nothing here is submitted anywhere. Screenshot it, or bring it to the conversation.</p>

  </div></div>
</section>


<!-- ══════════════════════════════════════════════════════════════
     05 · THE CLOSE
     Two actions. The handoff has to name the work, because the audit
     and the coaching are the same three things and nothing should
     need re-explaining on the call.
     ══════════════════════════════════════════════════════════════ -->
<section class="pa-screen" id="pa-close">
  <div class="pa-wrap"><div class="pa-col">

    <div class="pa-rule" style="margin-top:0"><span>Where this sits</span></div>
    <div class="pa-prose" style="margin-bottom:clamp(38px,5vw,58px)">
      <p>Reconnect runs on three dimensions. Vision, how you would like things to be. Values, who you need to become to live that vision. Velocity, how you move the pieces into place to make it happen. What you just scored sits inside values, and these are changes you can start making right away.</p>
    </div>

    <div class="pa-bridge">
      <p>Needs, boundaries and emotions are not three problems to fix. They are the three dimensions every one of these conversations turns out to be about, and most people are not giving them enough attention, with intention.</p>
      <p>What they add up to is being able to handle the ambiguity and the mistakes and not having it all together, without any of it touching what you think you are worth. Worth that does not need to be won, and is not measured by how much you do, because what you do is built on real self-esteem.</p>
      <p>That is the work. And it is not left to chance.</p>
    </div>

    <div class="pa-close-cta">
      <a class="pa-btn" id="pa-cta" href="#">Bring this to a conversation <span aria-hidden="true">&rarr;</span></a>
      <button class="pa-ghost" type="button" id="pa-pdf">Save your result as a PDF</button>
    </div>
    <p class="pa-close-sub">One conversation, start from here.</p>

    <div class="pa-restart">
      <button class="pa-quiet" type="button" id="pa-again">Start again</button>
    </div>
  </div></div>
</section>

</div><!-- /pa-root -->`;

  function addFonts(){
    if (document.getElementById('pa-fonts')) return;
    var p1=document.createElement('link'); p1.rel='preconnect'; p1.href='https://fonts.googleapis.com'; document.head.appendChild(p1);
    var p2=document.createElement('link'); p2.rel='preconnect'; p2.href='https://fonts.gstatic.com'; p2.crossOrigin=''; document.head.appendChild(p2);
    var f0=document.createElement('link'); f0.rel='stylesheet'; f0.href='https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap'; document.head.appendChild(f0);
    var f1=document.createElement('link'); f1.rel='stylesheet'; f1.href='https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap'; document.head.appendChild(f1);
    /* @font-face has to live in the DOCUMENT, not the shadow root. Chrome
       ignores font-face rules declared inside a shadow tree. */
    var ff=document.createElement('style'); ff.id='pa-fonts'; ff.textContent=`/* LUMIOS MARKER is not on Google Fonts -- upload the .woff2 to Wix Media and
   drop the URL in below. Until then the stack falls through to Permanent
   Marker, which is the closest free marker face, so nothing renders as a
   system script by accident. */
@font-face{font-family:'Lumios Marker';font-display:swap;
  src:url('LUMIOS_MARKER_WOFF2_URL') format('woff2');}`; document.head.appendChild(ff);
  }

  function loadScript(src){
    return new Promise(function(res,rej){
      var ex=document.querySelector('script[data-px="'+src+'"]');
      if(ex){ if(ex.getAttribute('data-loaded')){res();} else { ex.addEventListener('load',function(){res();}); ex.addEventListener('error',rej);} return; }
      var s=document.createElement('script'); s.src=src; s.async=false; s.setAttribute('data-px',src);
      s.addEventListener('load',function(){ s.setAttribute('data-loaded','1'); res(); });
      s.addEventListener('error',rej);
      document.head.appendChild(s);
    });
  }

  /* Every animation in here has a no-GSAP path and a reduced-motion path
     that simply shows the content, so a library that never arrives costs
     the visitor nothing but the easing. */
  function loadLibs(){
    var g=loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js')
      .then(function(){ return loadScript('https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js'); });
    var l=loadScript('https://unpkg.com/lenis@1.1.13/dist/lenis.min.js');
    return Promise.all([ g.catch(function(){}), l.catch(function(){}) ]);
  }

  /* Wix wraps a custom element in a fixed-height container sized from the
     editor. This component changes height four times (intro, items, turn,
     result), so the wrapper has to be told to follow it. */
  function collapseAncestors(host){
    var changed = false;
    try{ var h=host.getBoundingClientRect().height; if(h<50) return false;
      var n=host.parentElement,guard=0;
      while(n && n!==document.body && guard++<14){ if(n.getBoundingClientRect().height>h+600){ n.style.height='auto'; n.style.minHeight='0px'; changed = true; } n=n.parentElement; }
    }catch(e){}
    return changed;
  }

  function collapseAndRefresh(host){
    var changed = collapseAncestors(host);
    if (changed && window.ScrollTrigger) { try{ window.ScrollTrigger.refresh(); }catch(e){} }
  }

  function boot(root){
    if(!root || !root.getElementById('pa-root')) return;


    /* ══════════════ CONFIG ══════════════
       The only block that normally needs touching. */
    const CONFIG = {
      conversationUrl: 'https://www.parallaxxtransformations.com/contact-daniel-lawson',
      advanceDelay: 300          /* ms after a tap before the next item. 0 disables. */
    };

    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const G = window.gsap;
    const hasGSAP = !!(G && window.ScrollTrigger);
    if (hasGSAP) G.registerPlugin(window.ScrollTrigger);

    const $ = id => root.getElementById(id);

    /* FOCUS, ACROSS BOTH RUNTIMES.
       In the Design Code runtime root is the #pa-root element and the
       focused node is document.activeElement. In the deployed bundle
       root is a shadow root, and document.activeElement returns the
       shadow HOST rather than the button inside it, which would break
       arrow-key movement on the scale. ShadowRoot.activeElement is the
       one that answers correctly, and a plain element does not have the
       property at all, so the fallback covers the light DOM case. */
    const focused = () => root.activeElement || document.activeElement;

    /* ══════════════ THE SCALE ══════════════
       Five points, frequency, neutral labels, same direction on every
       item. Graded rather than binary because her own noun for the
       problem is trade off, which is by definition a matter of degree.
       A binary forces her to overstate, she notices, and the result
       stops feeling accurate to somebody who reads data all day. */
    const SCALE = ['Never','Rarely','Sometimes','Often','Nearly always'];

    /* ══════════════ THE FIFTEEN ══════════════
       Five per pillar, shuffled so no two consecutive items share one.
       Every statement is an observation she could make out loud in a
       meeting without it costing her anything. Not one of them is a
       verdict she has to accept before she can answer. */
    const ITEMS = [
      { p:'boundaries', t:'I say yes even when at personal capacity.' },
      { p:'emotions',   t:'When someone asks how I am, I answer with what I have been doing.' },
      { p:'needs',      t:'I eat multi-tasking with phone or computer, or I do not eat.' },
      { p:'boundaries', t:'My calendar is full with tasks for others.' },
      { p:'emotions',   t:'I finish something good and feel very little.' },
      { p:'needs',      t:'I would rather do it myself than explain how I want it done.' },
      { p:'boundaries', t:'I cancel personal plans for things that come up.' },
      { p:'emotions',   t:'My worth feels tied to my recent deliverables.' },
      { p:'needs',      t:'I run on less sleep than I would accept from anyone on my team.' },
      { p:'boundaries', t:'I take responsibility for undesignated tasks and overflow.' },
      { p:'emotions',   t:'It is easier to keep moving than it is to dwell on it.' },
      { p:'needs',      t:'Time scheduled for myself is the first to be moved.' },
      { p:'boundaries', t:'When there is a gap in work, I am lost with what to do.' },
      { p:'emotions',   t:'A good result is not good enough.' },
      { p:'needs',      t:'I do not share what I need with others. It takes too long.' }
    ];

    const NAMES = { needs:'Needs', boundaries:'Boundaries', emotions:'Emotions' };

    /* Dead heat order. Only reached when two pillars tie AND the spread
       is wide enough that the flat spread copy does not already own the
       result. Documented so it is a decision rather than an accident. */
    const TIEBREAK = ['boundaries','emotions','needs'];

    /* ══════════════ RESULT COPY ══════════════
       Result Copy v3, clean, 3 August 2026. Sourced from The Self-Esteem
       Triad, pages 5, 7, 11, 12, 14, 17, 19, 24 and 27. Replace any of it
       the moment a real woman says it better. */
    const TOP = {
      needs: {
        lead: [
          'Needs scored highest. Not self-care, which you would rightly ignore, and not rest.',
          'Needs are the baseline you operate from. What you require to keep performing at the level you perform at, and whether any of it is negotiable. Yours has become the first thing negotiated away. You skip the meal, you move the appointment that was yours, you run on less sleep than you would accept from anyone on your team, and you would rather do a thing yourself than take the time to ask.',
          'Consider this. Two of the needs in this model are to know you mattered, and to be recognised for who you are rather than what you produced. Where those two go unmet, the thing that reliably grows in their place is overachievement. As a solution. It worked, it still works, and it is a large part of why you are where you are. It is also why nothing you deliver settles the question for long.'
        ],
        cost: 'A version of you running at max capacity and calling it fine, because there is nothing left to compare it to. And people around you who think you are coping, because you have been careful to make sure of it. And you are still not left with the thing you want most.',
        move: 'One thing in the diary this week that is yours and does not move for anything short of a genuine emergency. One. Not a routine. One personal item that survives contact with a busy Tuesday.',
        qs: [
          'What is the first thing to disappear when the week gets busy, and how long has that been the answer?',
          'What would you say to somebody on your team who was operating the way you are?',
          'When did you decide it was quicker not to ask?'
        ]
      },
      boundaries: {
        lead: [
          'Boundaries scored highest. The difficulty is that your reason for not declining is a good one. You are paid to deliver, somebody is waiting, and it genuinely would be faster to just do it. Every one of those is true, which is why you still accept it. But a diary filled by other people is a diary of their priorities and their objectives, in life and in business, not yours.',
          'A boundary is the line where you end and everybody else begins, and it is how anyone finds out who you are and what is important to you. Where that line had to be given up early, what grows in its place is a very high commitment to other people and a very small one to yourself. Your personal philosophy for your own life gets limited to whatever survived that trade.'
        ],
        cost: 'A calendar you did not fill, and your own things scheduled into whatever is left, which is nothing. Then the dinner you cancelled, again, on somebody who has stopped expecting you to come. And when a gap of quiet does appear, you are not sure what to do with it.',
        move: 'One no, this week, at work, small. Not the hard one. The one where a professional sounding sentence would normally close it down before you had finished thinking. Watch what it actually costs, which is likely to be some nerves and nothing else.',
        qs: [
          'What is the sentence you use on yourself that makes declining impossible?',
          'Who in your working life has never heard you say no, and what have they concluded from that?',
          'What would you take on if your yes were scarce?'
        ]
      },
      emotions: {
        lead: [
          'Emotions scored highest. Which does not mean you are emotional, and it does not mean you should be.',
          'It likely means you know what you think. When a feeling arrives as information, there is no time scheduled for it, so it gets filed for another moment. By the time that moment comes, it has gone.',
          'Emotions are a compass rather than a problem to manage. Most of us end up as walking barometers, checking the temperature of the room before we share what we actually think. You are extremely good at that. It is a large part of what makes you good in the room. It also means the person whose temperature never gets checked is you.'
        ],
        cost: 'Work that finished hours ago, projects completed, and what should feel like achievement and success likely does not arrive. Overwhelm, stress and distraction are more common than presence, meaning and a sense of purpose.',
        move: 'One thing you finish this week, give it two minutes before you start the next one. Long enough to notice what finishing it feels like, and to mark it, even the small ones. This is the step where something you did becomes something you have.',
        qs: [
          'When was the last time something you made was finished, and you were the one who said so?',
          'Whose voice is it when you decide something is not ready yet?',
          'What would you have to stop achieving before you found out what you are worth without it?'
        ]
      }
    };

    /* WHAT IS FEEDING IT. Six ordered pairs, because which one comes second
       changes the mechanism rather than just the wording. These no longer
       open by naming the second pillar, so the block carries its name as a
       heading instead, the same way the two blocks either side of it do. */
    const SECOND = {
      'needs|boundaries': 'Every yes you have already given is spent before the week starts, so there is no room left for anything of your own. You get by on your bare minimum rather than moving towards your highest ideals.',
      'needs|emotions':   'This is what makes the first one look reasonable. If what you are worth is attached to what you delivered lately, then an hour spent on yourself is an hour of self-worth given up. Rescheduling your personal time reads as the strategic call, and it comes out the same way every time.',
      'boundaries|needs': 'This is what keeps the first one cheap. The thing an extra yes costs is something of yours, and yours has no price on it. Nothing gets invoiced, nobody notices, and so the yes stays free to give.',
      /* FLAGGED, NOT CHANGED. Section 13 of the copy doc: this paragraph
         describes the cost coming out of her side, which is the needs
         mechanism, so a woman whose second pillar is emotions currently
         reads a needs explanation. The replacement is written and waiting
         in that doc. Do not silently swap it. */
      'boundaries|emotions': 'This is what makes the extra yes so easy to give. Whatever it costs comes out of your side: the meal, the hour, the thing you had planned. Nobody sees that part and nobody mentions it, so the yes goes on looking like it costs nothing.',
      'emotions|needs':   'This is what removes the alternative. Nothing in the week is in a position to tell you how you are doing, because everything that was not work has already been moved. So the only insight is the one the work gives you, and the work is not qualified to give it.',
      'emotions|boundaries': 'This is what keeps the question at bay. A diary with nothing loose in it never leaves you anywhere long enough to ask what the last thing was worth. Which is convenient, and it is not an accident, and some part of you has known that for a while.'
    };

    /* WHAT YOU ALREADY HOLD. The one that survived. Never a consolation
       prize, and never optional to show. */
    const LOW = {
      needs:      'You have protected a solid baseline. Whatever else is running, you have not made yourself the first thing that gets cut. That is a strong place to keep building from.',
      boundaries: 'Your yes still costs you, which means it still means something. You have held on to the line between what you will carry and what you will not, and many people have traded it without noticing.',
      emotions:   'You can tell when something is good, and you can feel it land. Protect that one. It is the first thing the other two come for.'
    };

    const FLAT = 'There is no clear leader here, which is worth as much as a clear one. Either the last few months have been unusually even, or all three have been running together long enough that you no longer notice any of them separately. Take the questions under whichever of the three you found hardest to read honestly.';

    /* ══════════════ STATE. Memory only. ══════════════
       No localStorage and no sessionStorage anywhere in this file.
       Storage APIs are unavailable in the environments this ships to,
       and a failed write takes the whole result screen with it. */
    const answers = new Array(ITEMS.length).fill(null);
    let current = 0;
    let advanceTimer = null;
    let lastResult = null;

    /* ══════════════ SCREENS ══════════════ */
    const RESULT_SCREENS = ['pa-result','pa-her','pa-close'];
    const ALL_SCREENS = ['pa-intro','pa-items','pa-turn'].concat(RESULT_SCREENS);

    function show(list){
      ALL_SCREENS.forEach(id => {
        const el = $(id); if (!el) return;
        el.classList.toggle('is-on', list.indexOf(id) > -1);
      });
      revealFades();
    }

    /* Fades are a delivery mechanism, never a gate. If GSAP never
       loads, or motion is reduced, everything is simply visible. */
    function revealFades(){
      const nodes = Array.prototype.slice.call(root.querySelectorAll('.pa-fadein'));
      if (reduce){ nodes.forEach(n => n.classList.add('is-in')); return; }
      nodes.forEach((n, i) => {
        const box = n.getBoundingClientRect();
        const near = box.top < window.innerHeight + 120;
        if (!near){ n.classList.add('is-in'); return; }
        window.setTimeout(() => n.classList.add('is-in'), 60 + i * 70);
      });
    }

    function scrollTopOf(id){
      const el = $(id); if (!el) return;
      const y = el.getBoundingClientRect().top + window.pageYOffset - 12;
      try { window.scrollTo({ top: Math.max(0, y), behavior: reduce ? 'auto' : 'smooth' }); }
      catch (e) { window.scrollTo(0, Math.max(0, y)); }
    }

    /* ══════════════ ITEM RENDERING ══════════════ */
    const scaleBox = $('pa-scale');

    function renderItem(){
      const n = ITEMS.length;
      $('pa-count').innerHTML = '<b>' + (current + 1) + '</b> of ' + n;
      $('pa-fill').style.width = Math.max(2, Math.round(((current) / n) * 100)) + '%';
      $('pa-stmt').textContent = ITEMS[current].t;
      $('pa-back').style.visibility = current === 0 ? 'hidden' : 'visible';
      $('pa-live').textContent = 'Statement ' + (current + 1) + ' of ' + n + '. ' + ITEMS[current].t;

      scaleBox.innerHTML = '';
      const picked = answers[current];
      SCALE.forEach((label, i) => {
        const v = i + 1;
        const b = document.createElement('button');
        b.type = 'button';
        b.className = 'pa-opt';
        b.setAttribute('role', 'radio');
        b.setAttribute('aria-checked', picked === v ? 'true' : 'false');
        b.setAttribute('data-v', String(v));
        /* Roving tabindex: one stop for the whole group. Focus lands on
           the current answer, or on the first point if there is none. */
        b.tabIndex = (picked === v || (picked === null && i === 0)) ? 0 : -1;
        b.innerHTML = '<span class="dot" aria-hidden="true"></span><span>' + label + '</span>';
        b.addEventListener('click', () => pick(v));
        b.addEventListener('keydown', onScaleKey);
        scaleBox.appendChild(b);
      });
    }

    function optionAt(i){ return scaleBox.children[i] || null; }

    function moveFocus(delta){
      const kids = Array.prototype.slice.call(scaleBox.children);
      let i = kids.indexOf(focused());
      if (i < 0) i = kids.findIndex(k => k.tabIndex === 0);
      if (i < 0) i = 0;
      let next = i + delta;
      if (next < 0) next = kids.length - 1;
      if (next > kids.length - 1) next = 0;
      kids.forEach(k => { k.tabIndex = -1; });
      kids[next].tabIndex = 0;
      kids[next].focus();
    }

    /* ARROWS MOVE, ENTER SELECTS. Deliberately not the auto-select
       radio pattern: this group advances the screen on select, so
       arrowing through it would skip her past four statements. */
    function onScaleKey(e){
      const k = e.key;
      if (k === 'ArrowDown' || k === 'ArrowRight'){ e.preventDefault(); moveFocus(1); return; }
      if (k === 'ArrowUp' || k === 'ArrowLeft'){ e.preventDefault(); moveFocus(-1); return; }
      if (k === 'Enter' || k === ' ' || k === 'Spacebar'){
        e.preventDefault();
        const v = Number(e.currentTarget.getAttribute('data-v'));
        pick(v);
      }
    }

    function pick(v){
      window.clearTimeout(advanceTimer);
      answers[current] = v;
      Array.prototype.slice.call(scaleBox.children).forEach((b, i) => {
        b.setAttribute('aria-checked', i + 1 === v ? 'true' : 'false');
        b.tabIndex = i + 1 === v ? 0 : -1;
      });
      const go = () => {
        if (current < ITEMS.length - 1){ current++; renderItem(); focusScale(); }
        else turn();
      };
      /* No fake loading between the last item and the turn. */
      if (CONFIG.advanceDelay > 0) advanceTimer = window.setTimeout(go, CONFIG.advanceDelay);
      else go();
    }

    function focusScale(){
      /* Only pull focus if the keyboard is already in the group. A mouse
         user who has just tapped should not have the page jump. */
      if (!scaleBox.contains(focused())) return;
      const target = Array.prototype.slice.call(scaleBox.children).find(k => k.tabIndex === 0);
      if (target) target.focus();
    }

    function back(){
      window.clearTimeout(advanceTimer);
      if (current > 0){ current--; renderItem(); focusScale(); }
    }

    /* ══════════════ SCORING ══════════════
       Each pillar is five items on a one to five scale, so each scores
       5 to 25 and the three are directly comparable. There is no grand
       total, because a grand total invites a band, and there is no
       version of this where she is told she is severe. */
    function score(){
      const totals = { needs:0, boundaries:0, emotions:0 };
      const fives  = { needs:0, boundaries:0, emotions:0 };
      ITEMS.forEach((item, i) => {
        const v = answers[i] === null ? 1 : answers[i];
        totals[item.p] += v;
        if (v === 5) fives[item.p] += 1;
      });
      const ranked = Object.keys(totals).map(k => ({ key:k, total:totals[k], fives:fives[k] }))
        .sort((a, b) => {
          if (b.total !== a.total) return b.total - a.total;
          if (b.fives !== a.fives) return b.fives - a.fives;
          return TIEBREAK.indexOf(a.key) - TIEBREAK.indexOf(b.key);
        });
      const spread = ranked[0].total - ranked[2].total;
      /* A DEAD HEAT AT THE TOP IS ITS OWN CASE, and it is not the same as a
         flat spread. 25 / 25 / 18 is not flat, but there is still no highest
         pillar, and the result copy opens with the words "you scored highest
         on". Painting one of two identical bars coral and then telling her it
         is the one under pressure is a verdict invented by a tiebreak, which
         is exactly the thing this instrument does not do. So when the top two
         are level, the coral comes off, the sentence says so, and she gets
         both sets of questions. */
      const tied = ranked[0].total === ranked[1].total;
      return { ranked: ranked, spread: spread, flat: spread <= 3, tied: tied };
    }

    /* ══════════════ THE TURN ══════════════ */
    function turn(){
      show(['pa-turn']);
      scrollTopOf('pa-turn');
    }

    /* ══════════════ THE RESULT ══════════════ */
    function paras(id, list){
      const el = $(id); el.innerHTML = '';
      list.forEach(t => { const p = document.createElement('p'); p.textContent = t; el.appendChild(p); });
    }

    function qset(pillarKey, label){
      const wrap = document.createElement('div');
      wrap.className = 'pa-qset';
      if (label){
        const h = document.createElement('h4');
        h.textContent = label;
        wrap.appendChild(h);
      }
      const ul = document.createElement('ul');
      ul.className = 'pa-qs';
      TOP[pillarKey].qs.forEach(q => {
        const li = document.createElement('li');
        li.textContent = q;
        ul.appendChild(li);
      });
      wrap.appendChild(ul);
      return wrap;
    }

    function renderResult(res){
      const top = res.ranked[0].key;
      const second = res.ranked[1].key;
      const low = res.ranked[2].key;

      /* Bars. Ranked, gold hairline, coral fill on the highest only. */
      /* On a dead heat the top two are both under the pressure, so neither
         one gets to be the thing feeding the other. */
      const TAGS = res.tied
        ? ['Where the pressure is', 'Where the pressure is', 'What you already hold']
        : ['Where the pressure is', 'What is feeding it', 'What you already hold'];

      const bars = $('pa-bars');
      bars.innerHTML = '';
      res.ranked.forEach((r, i) => {
        const pct = Math.round((r.total / 25) * 100);
        const d = document.createElement('div');
        /* No coral on a dead heat: there is no single highest bar to fill. */
        d.className = 'pa-bar' + (i === 0 && !res.tied ? ' is-top' : '');
        if (res.tied && i < 2) d.classList.add('is-pressure');
        d.innerHTML =
          '<div class="pa-bar-head">' +
            '<span class="name">' + NAMES[r.key] + '</span>' +
            '<span class="val">' + r.total + '<i>&thinsp;/&thinsp;25</i></span>' +
          '</div>' +
          '<div class="pa-track"><div class="pa-fill" data-w="' + pct + '"></div></div>' +
          '<p class="pa-bar-tag">' + TAGS[i] + '</p>';
        bars.appendChild(d);
      });

      const flat = $('pa-flat');
      flat.hidden = !res.flat;
      flat.textContent = res.flat ? FLAT : '';

      $('pa-top-name').textContent = res.tied ? (NAMES[top] + ' and ' + NAMES[second]) : NAMES[top];
      const lead = TOP[top].lead.slice();
      if (res.tied){
        lead.unshift(NAMES[top] + ' and ' + NAMES[second].toLowerCase() +
          ' came out level, on the same score. Read this one first and the other one straight after, because on those numbers neither is the answer on its own.');
      }
      paras('pa-top-lead', lead);
      $('pa-top-cost').textContent = TOP[top].cost;
      $('pa-second-name').textContent = NAMES[second];
      paras('pa-second', [ SECOND[top + '|' + second] ]);

      $('pa-low-name').textContent = NAMES[low];
      paras('pa-low-body', [ LOW[low] ]);
      $('pa-move').textContent = TOP[top].move;

      /* On a flat spread all three question sets are shown, because the
         flat copy tells her to take whichever she found hardest to read
         honestly, and she cannot do that if two of them are hidden. */
      const qbox = $('pa-questions');
      qbox.innerHTML = '';
      if (res.flat){
        res.ranked.forEach(r => qbox.appendChild(qset(r.key, 'If you take ' + NAMES[r.key].toLowerCase())));
      } else if (res.tied){
        [top, second].forEach(k => qbox.appendChild(qset(k, NAMES[k])));
      } else {
        qbox.appendChild(qset(top, ''));
      }

      lastResult = res;
    }

    /* Bars animate on reveal only, and have a static fallback. */
    function animateBars(){
      const fills = Array.prototype.slice.call(root.querySelectorAll('.pa-fill'));
      if (reduce){ fills.forEach(f => { f.style.transition = 'none'; f.style.width = f.dataset.w + '%'; }); return; }
      fills.forEach(f => { f.style.width = '0%'; });
      window.setTimeout(() => {
        fills.forEach((f, i) => window.setTimeout(() => { f.style.width = f.dataset.w + '%'; }, i * 130));
      }, 120);
    }

    function finish(){
      renderResult(score());
      show(RESULT_SCREENS);
      scrollTopOf('pa-result');
      animateBars();
      if (hasGSAP && !reduce){ try { window.ScrollTrigger.refresh(); } catch(e){} }
    }

    /* ══════════════ WIRING ══════════════ */
    $('pa-start').addEventListener('click', () => {
      current = 0; renderItem(); show(['pa-items']); scrollTopOf('pa-items');
    });
    $('pa-back').addEventListener('click', back);
    $('pa-see').addEventListener('click', finish);
    $('pa-pdf').addEventListener('click', () => { window.print(); });
    $('pa-again').addEventListener('click', () => {
      for (let i = 0; i < answers.length; i++) answers[i] = null;
      current = 0; lastResult = null;
      show(['pa-intro']); scrollTopOf('pa-intro');
    });
    $('pa-cta').setAttribute('href', CONFIG.conversationUrl);

    /* Number keys are a shortcut on the items screen, nothing more. */
    document.addEventListener('keydown', e => {
      const items = $('pa-items');
      if (!items || !items.classList.contains('is-on')) return;
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      if (e.key >= '1' && e.key <= '5'){ e.preventDefault(); pick(Number(e.key)); return; }
      if (e.key === 'Backspace'){ e.preventDefault(); back(); }
    });

    /* ══════════════ SMOOTH SCROLL ══════════════
       Optional, and the page is complete without it. */
    if (window.Lenis && !reduce){
      try {
        const lenis = new window.Lenis({ lerp: 0.11 });
        const raf = t => { lenis.raf(t); window.requestAnimationFrame(raf); };
        window.requestAnimationFrame(raf);
        if (hasGSAP) lenis.on('scroll', window.ScrollTrigger.update);
      } catch(e){}
    }

    renderItem();
    revealFades();

    /* ══════════════ REVIEW DEEP LINKS ══════════════
       ?state=needs | boundaries | emotions | flat jumps straight to a
       result so all five verification states can be rendered without
       fifteen taps. Harmless in production, and it is how the phone
       viewport screenshot gets taken. */
    (function(){
      let q = '';
      try { q = new URLSearchParams(window.location.search).get('state') || ''; } catch(e){ q = ''; }
      if (!q) return;
      const setAll = v => ITEMS.forEach((it, i) => { answers[i] = v; });
      if (q === 'flat'){
        /* Close but with a clear top, which is the ordinary flat spread.
           A dead heat is a different case and has its own link. */
        setAll(3);
        answers[0] = 4; answers[3] = 4;   /* boundaries 17 */
        answers[2] = 4;                   /* needs 16 */
        answers[4] = 2;                   /* emotions 14 */
      } else if (q === 'tie'){
        setAll(3);
        ITEMS.forEach((it, i) => { if (it.p === 'needs' || it.p === 'boundaries') answers[i] = 5; });
      } else if (TOP[q]){
        setAll(2);
        ITEMS.forEach((it, i) => { if (it.p === q) answers[i] = 5; });
        /* Second place is deliberately not a tie, so the feeding block
           is exercised rather than the tiebreak. */
        const other = Object.keys(TOP).filter(k => k !== q)[0];
        ITEMS.forEach((it, i) => { if (it.p === other) answers[i] = 3; });
      } else { return; }
      finish();
    })();
  }

  class ParallaxxPriorityAudit extends HTMLElement {
    connectedCallback(){
      if (this._mounted) return; this._mounted = true;
      addFonts();
      var shadow = this.attachShadow({mode:'open'});
      shadow.innerHTML = '<style>'+CSS+'</style>'+HTML;
      var host = this;
      loadLibs().then(function(){ try{ boot(shadow); }catch(e){ console.error('[pa] boot failed:', e); } })
        .catch(function(){ try{ boot(shadow); }catch(e){} });
      requestAnimationFrame(function(){ collapseAndRefresh(host); });
      [400,1200,2500].forEach(function(t){ setTimeout(function(){ collapseAndRefresh(host); }, t); });
      window.addEventListener('resize', function(){ collapseAndRefresh(host); }, {passive:true});
      /* The screen swaps change the element's height by thousands of pixels
         and Wix has already cached the old one. */
      new MutationObserver(function(){ collapseAndRefresh(host); })
        .observe(shadow, {subtree:true, attributes:true, attributeFilter:['class']});
    }
  }
  customElements.define('parallaxx-priority-audit', ParallaxxPriorityAudit);
})();
