/* PARALLAXX TRANSFORMATIONS - The Reconnected Man. Tag: parallaxx-reconnected-man.
   GENERATED by build_bundle_man_v2.py from reconnected-man-v2-preview.html,
   which is the approved design. DO NOT EDIT THIS FILE, edit that one and rerun.

   Renders into the LIGHT DOM on purpose. The stylesheet is scoped to #rm-page
   and the behaviour script uses document.querySelector, so light DOM keeps the
   videos, the accordion, the modal and every reveal working as approved. */
(function () {
  if (customElements.get('parallaxx-reconnected-man')) return;

  var CSS = `:root{color-scheme:light}body{margin:0;padding:0;font:14px -apple-system,BlinkMacSystemFont,sans-serif;background:#faf9f5;color:#141413}img{max-width:100%}[hidden]:not([hidden=until-found i]){display:none!important}
/* ══ APPLICATION MODAL ══ */
#rm-apply-modal{display:none;position:fixed;inset:0;z-index:9999;
  align-items:center;justify-content:center;padding:16px}
#rm-apply-modal.is-open{display:flex}
#rm-apply-modal .modal-backdrop{position:absolute;inset:0;
  background:rgba(0,0,0,.85);backdrop-filter:blur(6px);cursor:pointer}
#rm-apply-modal .modal-box{position:relative;z-index:1;background:var(--card);
  border:1px solid var(--border);border-top:2px solid var(--gold);
  width:100%;max-width:580px;max-height:min(92vh,940px);
  display:flex;flex-direction:column;overflow-y:auto;-webkit-overflow-scrolling:touch}
#rm-apply-modal .modal-header{display:flex;justify-content:space-between;align-items:center;
  padding:15px 22px;border-bottom:1px solid var(--border);background:var(--card);
  z-index:2;flex-shrink:0}
#rm-apply-modal .modal-title{font-family:var(--head);font-size:14px;
  letter-spacing:.15em;text-transform:uppercase;color:var(--cream)}
#rm-apply-modal .modal-close{background:none;border:1px solid var(--border);
  color:var(--slate);width:32px;height:32px;display:flex;align-items:center;
  justify-content:center;cursor:pointer;font-size:20px;line-height:1;
  transition:color .2s,border-color .2s;flex-shrink:0}
#rm-apply-modal .modal-close:hover{color:var(--cream);border-color:var(--gold)}
#rm-apply-modal .modal-body{padding:0}
#rm-apply-modal .modal-body iframe{display:block;width:100%;border:none}


/* ══════════════════════════════════════════════════════════════════════
   THE RECONNECTED MAN · proposed page
   Built on the v10 system the Reconnected Woman page now runs: same
   tokens, same grounds, same [data-r] depth reveals, same masked
   headlines, same motion contract (reduce || noIO -> everything gets
   its end state, plus a 4s failsafe).

   WHAT IS HIS AND STAYS HIS. Three things the women's page does not
   have and should not: the diagonal gold hatch over the hero, the gold
   bloom behind the section heads, and the marker hand for the lowercase
   asides. They are the whole visual difference between the two doors.
   ══════════════════════════════════════════════════════════════════════ */
:root{
  --navy:#04122A; --black:#061938; --ink-navy:#030C1C; --card:#0A1D3C;
  --border:#1B2C46;
  --cream:#F1ECE1; --cream-2:#F7F3EA; --line-cream:#DED6C4;
  --coral:#FF501F; --coral-lift:#FF6A3D;
  --gold:#E8C65F; --gold-lift:#F3DD93; --gold-dark:#C9A227;
  --bronze:#A08A5E; --bronze-ink:#6E5C33;  /* bronze fails AA on cream; -ink is the text weight */
  --mist:#B1BFD7; --slate:#7C89A3;
  --ink:#1F2A3D; --ink-soft:#5A6274;
  --head:'Poppins',system-ui,-apple-system,sans-serif;
  --body:'Montserrat',system-ui,-apple-system,sans-serif;
  --hand:'Permanent Marker',cursive;
  --gut:clamp(20px,4vw,52px);
  --col:1240px;
  --ease:cubic-bezier(.16,1,.3,1);
}
#rm-page *{box-sizing:border-box}

#rm-page{margin:0;background:var(--ink-navy);color:var(--mist);
  font-family:var(--body);font-size:16px;line-height:1.68;
  -webkit-font-smoothing:antialiased;overflow-x:clip}

/* ── the rail ─────────────────────────────────────────────────── */
#rail{position:fixed;top:0;bottom:0;left:calc(var(--gut) / 2 - 1px);
  width:9px;z-index:60;pointer-events:none;display:flex;justify-content:center}
#rail .bar{width:1px;height:100%;background:var(--slate);opacity:.22}
/* Where you are standing against the sightline. The rail without it is
   a hairline; with it, it is the one piece of chrome that tracks him. */
#rail .pos{position:absolute;left:50%;width:3px;height:46px;top:0;
  transform:translateX(-50%);border-radius:3px;
  background:var(--gold);opacity:.9;box-shadow:0 0 14px rgba(232,198,95,.55)}
@media(max-width:820px){#rail{left:7px}}

/* ── grounds ──────────────────────────────────────────────────── */
.sec{padding:clamp(52px,6.5vw,92px) var(--gut);position:relative}
.sec.ink{background:var(--ink-navy)}
.sec.dark{background:var(--navy)}
.sec.black{background:var(--black)}
.sec.cream{background:var(--cream);color:var(--ink)}
.sec.cream-2{background:var(--cream-2);color:var(--ink)}
.in{max-width:var(--col);margin:0 auto}
.narrow{max-width:1040px}
.mid{max-width:820px}

/* THE GOLD BLOOM behind a section head. The women's page has no
   equivalent. It is what stops a flat navy field reading as a slide. */
.glow{position:absolute;top:-6%;left:0;width:min(720px,92%);height:56%;
  pointer-events:none;
  background:radial-gradient(ellipse at 22% 30%,rgba(232,198,95,.11),transparent 66%)}

/* ── type ─────────────────────────────────────────────────────── */
.ey{font-family:var(--head);font-size:.62rem;font-weight:800;letter-spacing:.24em;
  text-transform:uppercase;color:var(--gold);margin:0 0 13px;max-width:none}
.sec.cream .ey,.sec.cream-2 .ey{color:var(--bronze-ink)}
:where(#rm-page) h1,:where(#rm-page) h2,:where(#rm-page) h3,:where(#rm-page) h4{font-family:var(--head);margin:0;text-wrap:balance}
:where(#rm-page) h2{font-size:clamp(27px,3.4vw,42px);font-weight:700;line-height:1.1;
  letter-spacing:-.02em;color:var(--cream);margin-bottom:16px}
:where(#rm-page) h2 .hit{color:var(--gold-lift)}
.sec.cream h2,.sec.cream-2 h2{color:var(--ink)}
:where(#rm-page) h3{font-size:clamp(19px,2vw,25px);font-weight:600;line-height:1.28;color:var(--cream)}
.sec.cream h3,.sec.cream-2 h3{color:var(--ink)}
:where(#rm-page) p{margin:0 0 14px;max-width:64ch}
.lede{font-size:clamp(16px,1.55vw,19px);line-height:1.55;color:var(--mist)}
.sec.cream .lede,.sec.cream-2 .lede{color:var(--ink-soft)}
.dim{color:var(--slate)}
.sec.cream .dim,.sec.cream-2 .dim{color:var(--ink-soft)}
:where(#rm-page) em{font-style:italic}
.divider{width:46px;height:1px;background:var(--gold);margin:0 0 18px}
.sec.cream .divider,.sec.cream-2 .divider{background:var(--bronze)}
.centre{text-align:center}
.centre p,.centre .lede{margin-left:auto;margin-right:auto}
.centre .divider{margin-left:auto;margin-right:auto}

/* HIS MARKER HAND. Lowercase, never a heading, never a claim. It is the
   aside in his own writing, and it is the loudest single thing that says
   this is the men's door. */
.hand{font-family:var(--hand);font-weight:400;text-transform:lowercase;
  font-size:clamp(17px,1.9vw,24px);line-height:1.25;color:var(--gold);
  margin:0 0 14px;letter-spacing:.01em}
.sec.cream .hand,.sec.cream-2 .hand{color:var(--bronze-ink)}
.hand.sm{font-size:clamp(15px,1.5vw,19px)}

/* ── buttons ──────────────────────────────────────────────────── */
.btn{display:inline-block;font-family:var(--head);font-size:.76rem;font-weight:700;
  letter-spacing:.13em;text-transform:uppercase;text-decoration:none;border:none;
  background:var(--coral);color:#fff;padding:16px 30px;border-radius:2px;cursor:pointer;
  transition:background .22s ease,transform .22s var(--ease)}
.btn:hover{background:var(--coral-lift);transform:translateY(-2px)}
.btn:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
.btn.ghost{background:transparent;color:var(--gold);border:1px solid rgba(232,198,95,.45)}
.btn.ghost:hover{background:rgba(232,198,95,.1)}
.btn.full{display:block;width:100%;text-align:center}

/* ── placeholders, his convention ─────────────────────────────── */
.ph{position:relative;display:flex;flex-direction:column;align-items:center;
  justify-content:center;text-align:center;gap:10px;padding:28px;
  border:2px dashed var(--coral);border-radius:16px;min-height:200px;
  background:repeating-linear-gradient(45deg,rgba(255,80,31,.05) 0 12px,rgba(255,80,31,.10) 12px 24px)}
.ph b{font-family:var(--head);font-size:.64rem;font-weight:800;letter-spacing:.18em;
  text-transform:uppercase;color:var(--coral-lift)}
.ph span{font-size:.85rem;line-height:1.6;max-width:40ch;color:#E6BCAC}
.sec.cream .ph span,.sec.cream-2 .ph span{color:#8A6F62}

/* ══ REVEAL SYSTEM · depth-graded, one-shot. Lifted from v10. ══ */
[data-r]{opacity:0;transform:translateY(var(--dy,12px));
  transition:opacity .85s ease,transform .85s var(--ease)}
[data-r="far"]{--dy:4px}
[data-r="mid"]{--dy:12px}
[data-r="near"]{--dy:28px}
[data-r].seen{opacity:1;transform:none}
#rm-page.no-anim [data-r]{opacity:1!important;transform:none!important}

.mask{overflow:hidden;display:block}
.mask > span{display:block;transform:translateY(104%);
  transition:transform 1.05s var(--ease)}
.mask.seen > span{transform:translateY(0)}
#rm-page.no-anim .mask > span{transform:none!important}

/* ══ 01 · HERO ══ */
#hero{position:relative;min-height:clamp(560px,72svh,780px);display:flex;
  align-items:flex-end;padding:0;overflow:hidden;
  background:radial-gradient(120% 90% at 50% 14%,#0A2148 0%,#061938 44%,#030C1E 100%)}
/* HIS HATCH. A -45deg gold thread at .03, 58px apart. The other door opens on a
   photograph; this one opens on a made surface, and the hatch is what
   keeps a gradient-only hero from reading as empty. */
/* THE PLATE. The settle is ported from the women's build: the frame opens
   tighter and eases out over 2.6s, so the picture fills in while the
   headline is still being read. This hero has been gradient-only since
   launch and the .dc.html has carried a TODO for it since August. The
   motion is built; the photograph is the thing to shoot. */
#hero .plate{position:absolute;inset:0;z-index:0;overflow:hidden}
#hero .shot{position:absolute;inset:-2% 0;
  background-image:url('/assets/img/rm-hero.jpg');
  background-size:cover;background-position:50% 42%;
  transform-origin:50% 32%;
  animation:heroSettle 2.6s cubic-bezier(.16,1,.3,1) .18s both}
@keyframes heroSettle{
  from{transform:scale(1.085) translateY(1.9%)}
  to{transform:scale(1) translateY(0)}}
@media(prefers-reduced-motion:reduce){#hero .shot{animation:none}}
#hero .scrim{position:absolute;inset:0;z-index:1;pointer-events:none;
  background:
    radial-gradient(ellipse 52% 84% at 50% 100%,rgba(3,12,28,.97) 0%,rgba(3,12,28,.91) 28%,
      rgba(3,12,28,.56) 58%,rgba(3,12,28,.18) 82%,rgba(3,12,28,0) 100%),
    linear-gradient(0deg,rgba(3,12,28,.35) 0%,rgba(3,12,28,.30) 34%,
      rgba(3,12,28,.29) 66%,rgba(3,12,28,.34) 100%)}
#hero .hatch{position:absolute;inset:0;pointer-events:none;z-index:1;
  background-image:repeating-linear-gradient(-45deg,transparent,transparent 58px,
    rgba(232,198,95,.03) 58px,rgba(232,198,95,.03) 59px)}
#hero .bloom{position:absolute;top:34%;left:50%;z-index:1;pointer-events:none;
  width:min(760px,110vw);height:min(760px,110vw);transform:translate(-50%,-50%);
  background:radial-gradient(circle,rgba(232,198,95,.10) 0%,rgba(232,198,95,.04) 38%,transparent 68%);
  animation:bloomSettle 2.6s var(--ease) .18s both}
@keyframes bloomSettle{from{opacity:0;transform:translate(-50%,-50%) scale(.9)}
  to{opacity:1;transform:translate(-50%,-50%) scale(1)}}
#hero .hin{position:relative;z-index:2;width:100%;max-width:var(--col);margin:0 auto;
  padding:clamp(70px,10vh,110px) var(--gut) clamp(52px,8vh,96px);text-align:center}
#hero h1{font-size:clamp(44px,8.4vw,104px);font-weight:700;line-height:.96;
  letter-spacing:-.035em;color:var(--cream);margin:0 0 26px}
#hero h1 .key{color:var(--gold)}
#hero .sub{font-size:clamp(16px,1.7vw,20px);line-height:1.5;color:var(--mist);
  max-width:min(620px,100%);margin:0 auto}
#hero .sub span,#hero .sub b{display:block;text-wrap:balance}
#hero .sub span{margin-bottom:.5em}
#hero .sub b{color:var(--cream);font-weight:600}
#hero .btn{margin-top:26px}
#hero .next{margin:20px auto 0;max-width:none;font-family:var(--head);font-size:.66rem;
  font-weight:700;letter-spacing:.16em;text-transform:uppercase;color:var(--gold)}
#hero .cue{position:absolute;left:50%;bottom:20px;z-index:3;color:var(--gold);
  opacity:.5;font-size:1.3rem;line-height:1;transform:translateX(-50%);
  animation:cue 2.6s ease-in-out infinite}
@keyframes cue{0%,100%{transform:translateX(-50%) translateY(0)}
  50%{transform:translateX(-50%) translateY(6px)}}

/* ══ 02 · PROMISE BAND ══ */
#promise{text-align:center;padding-top:clamp(28px,3.4vw,46px);
  padding-bottom:clamp(28px,3.4vw,46px)}
.promise-rule{width:0;height:1px;background:rgba(232,198,95,.6);margin:0 auto 20px;
  transition:width 1.2s var(--ease)}
.promise-rule.on{width:42px}
.promise-line{font-family:var(--head);font-weight:400;font-size:clamp(17px,2vw,26px);
  line-height:1.22;letter-spacing:-.018em;color:var(--cream);margin:0 auto;text-wrap:balance}
@media(min-width:800px){.promise-line{white-space:nowrap}}
.promise-sub{font-family:var(--head);font-weight:400;font-size:clamp(14px,1.15vw,17px);
  line-height:1.4;color:var(--slate);max-width:520px;margin:10px auto 0}

/* ══ 03 · VSL ══ */
.vidwrap{position:relative;aspect-ratio:16/9;margin-top:24px;background:#000;
  overflow:hidden;border:1px solid var(--border);display:flex;
  align-items:center;justify-content:center}
.ring{width:78px;height:78px;border-radius:50%;border:2px solid var(--gold);
  background:rgba(6,25,56,.85);display:flex;align-items:center;justify-content:center;
  transition:transform .25s var(--ease),box-shadow .3s ease}
.ring::after{content:"";border-style:solid;border-width:14px 0 14px 22px;
  border-color:transparent transparent transparent var(--gold);margin-left:5px}
.vidwrap:hover .ring{transform:scale(1.07);box-shadow:0 0 26px rgba(255,80,31,.4)}
.vquote{font-family:var(--head);font-weight:300;font-style:italic;
  font-size:clamp(16px,1.7vw,21px);line-height:1.5;color:var(--cream);
  border-left:2px solid var(--gold);padding-left:22px;margin:26px auto 0;max-width:68ch}
.vquote cite{display:block;margin-top:10px;font-family:var(--head);font-size:.62rem;
  font-weight:800;letter-spacing:.2em;text-transform:uppercase;color:var(--slate);font-style:normal}

/* ══ 04 · RECOGNITION · the staged load ══ */
#recognition .load{list-style:none;margin:0;padding:0;max-width:900px}
#recognition .load li{font-family:var(--head);font-weight:300;
  font-size:clamp(16px,1.8vw,22px);line-height:1.4;color:var(--cream);
  padding:16px 0 16px 26px;border-bottom:1px solid rgba(232,198,95,.14);
  position:relative;opacity:0;transition:opacity .5s ease}
#recognition .load li::before{content:"";position:absolute;left:0;top:26px;
  width:14px;height:1px;background:rgba(232,198,95,.5);
  transition:width .45s var(--ease),background .45s ease}
#recognition .load li.on{opacity:1;animation:settle .5s var(--ease)}
#recognition .load li:hover::before{width:20px;background:var(--gold)}
@keyframes settle{0%{filter:blur(2px)}100%{filter:blur(0)}}
#rm-page.no-anim #recognition .load li{opacity:1!important}
#recogCloser{font-family:var(--head);font-weight:700;
  font-size:clamp(18px,2.2vw,27px);line-height:1.3;color:var(--cream);
  margin-top:28px;opacity:0;transition:opacity .8s ease}
#recogCloser span{display:block;color:var(--gold)}
#recogCloser.on{opacity:1}
#rm-page.no-anim #recogCloser{opacity:1!important}

/* ══ 05 · TRAP · the costumes fall away ══ */
.costumes{list-style:none;display:flex;flex-wrap:wrap;gap:10px;
  margin:24px 0 0;padding:0;max-width:none}
.costumes li{font-family:var(--head);font-size:.68rem;font-weight:700;
  letter-spacing:.16em;text-transform:uppercase;color:var(--cream);
  border:1px solid var(--border);background:var(--card);padding:11px 17px;
  transition:color 1s ease,opacity 1s ease,transform 1s var(--ease),border-color 1s ease}
#trapRig.shifted .costumes li{opacity:.42;transform:scale(.97);
  border-color:rgba(27,44,70,.6)}
.trap-under{font-family:var(--head);font-weight:300;font-style:italic;
  font-size:clamp(17px,2.1vw,25px);line-height:1.4;color:var(--cream);
  border-left:2px solid var(--gold);padding-left:24px;text-align:left;
  max-width:44ch;margin:30px auto 0;opacity:0;transform:translateY(8px);
  transition:opacity .9s ease,transform .9s var(--ease)}
#trapRig.shifted .trap-under{opacity:1;transform:none}
#rm-page.no-anim .trap-under{opacity:1!important;transform:none!important}

/* ══ 06 · HOW · the runner travels the track ══ */
.steps .row{display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(20px,3vw,38px);
  position:relative}
.steps .track{position:absolute;top:22px;left:6%;right:6%;height:1px;
  background:rgba(160,138,94,.35);overflow:visible}
.steps .runner{position:absolute;top:50%;left:0;width:7px;height:7px;border-radius:50%;
  background:var(--bronze);transform:translate(-50%,-50%);opacity:0;
  box-shadow:0 0 0 4px rgba(160,138,94,.18)}
.steps.on .runner{animation:run 3.4s linear forwards}
@keyframes run{0%{left:0;opacity:0}6%{opacity:1}94%{opacity:1}100%{left:100%;opacity:0}}
.step{position:relative;text-align:left}
.step .n{width:44px;height:44px;border-radius:50%;border:1px solid rgba(160,138,94,.55);
  display:flex;align-items:center;justify-content:center;margin:0 0 16px;
  font-family:var(--head);font-size:.8rem;font-weight:700;color:var(--bronze-ink);
  background:var(--cream);position:relative;z-index:1;
  transition:border-color .45s ease,color .45s ease,background .45s ease,
    transform .45s var(--ease),box-shadow .45s ease}
.steps.on .step.lit .n{box-shadow:0 0 0 5px rgba(160,138,94,.14);
  transform:translateY(-3px);border-color:var(--bronze);color:var(--ink)}
.step h3{margin-bottom:8px}
.step p{font-size:1rem;line-height:1.6;margin:0;max-width:30ch;color:var(--ink-soft)}
.why-not{margin:26px 0 0;font-family:var(--head);font-size:clamp(18px,1.9vw,23px);
  font-weight:600;color:var(--ink);max-width:none}
.why-not span{display:block;color:var(--bronze-ink);margin-top:4px}
@media(min-width:820px){.why-not{white-space:nowrap}}
.rcs{margin:32px 0 0;padding-top:18px;border-top:1px solid var(--line-cream);
  display:flex;flex-wrap:wrap;align-items:baseline;gap:8px 18px}
.rcs .k{font-family:var(--head);font-size:clamp(15px,1.5vw,17px);font-weight:700;color:var(--ink)}
.rcs .v{font-family:var(--head);font-size:.66rem;font-weight:800;letter-spacing:.2em;
  text-transform:uppercase;color:var(--bronze-ink)}

/* ══ 07 · WHAT THIS ISN'T · struck items recede ══ */
/* Five words, one row, one gesture. A 2px line draws across each in turn
   from the left, and the word dims behind it. Ported from the other door,
   where the same list is the section's whole argument. */
.nots{list-style:none;margin:22px 0 0;padding:0;display:flex;flex-wrap:wrap;
  gap:12px clamp(14px,1.9vw,26px);max-width:none}
@media(min-width:1000px){.nots{flex-wrap:nowrap}}
.nots li{position:relative;font-family:var(--head);white-space:nowrap;
  font-size:clamp(15px,2.05vw,27px);font-weight:600;color:var(--slate);
  transition:color 1s ease}
.nots li::after{content:"";position:absolute;left:-4px;right:-4px;top:52%;height:2px;
  background:var(--gold-dark);transform:scaleX(0);transform-origin:left center;
  transition:transform .95s var(--ease)}
.nots li.struck::after{transform:scaleX(1)}
.nots li.struck{color:#5A6880}
.stand{margin:28px 0 0;font-family:var(--head);font-size:clamp(18px,2vw,25px);
  font-weight:600;line-height:1.4;color:var(--cream);max-width:none}
/* Label left, answer right. No numerals: three negations are a set, not a
   sequence, and numbering a set is decoration. */
.not3{list-style:none;margin:30px 0 0;padding:0;border-top:1px solid var(--border)}
.not3 .r{display:grid;grid-template-columns:minmax(0,300px) minmax(0,1fr);gap:6px 34px;
  padding:16px 0;border-bottom:1px solid var(--border);
  transition:background .3s ease,padding-left .3s var(--ease)}
.not3 .r:hover{background:rgba(232,198,95,.05);padding-left:12px}
.not3 h3{font-size:clamp(17px,1.8vw,21px);font-weight:600;color:var(--cream)}
.not3 p{font-size:1rem;margin:0;color:var(--slate)}
@media(max-width:720px){.not3 .r{grid-template-columns:1fr;gap:4px}}

/* ══ 08 · DIFFERENCE · the release ══ */
#difference .question{font-family:var(--head);font-weight:300;
  font-size:clamp(19px,2.7vw,34px);line-height:1.35;color:var(--cream);
  max-width:22ch;margin:0 0 18px}
#difference.released .question{animation:lift 1.1s var(--ease)}
@keyframes lift{from{transform:translateY(6px)}to{transform:none}}
.release-note{font-family:var(--head);font-size:clamp(15px,1.5vw,18px);font-weight:600;
  line-height:1.45;color:var(--bronze);max-width:34ch;margin:16px 0 0;
  opacity:0;transition:opacity .9s ease}
.release-note.on{opacity:1}
#rm-page.no-anim .release-note{opacity:1!important}
.lifts{list-style:none;margin:0;padding:0}
.lifts li{display:grid;grid-template-columns:40px 1fr;gap:0 14px;align-items:baseline;
  padding:15px 14px 15px 0;border-bottom:1px solid rgba(232,198,95,.16);
  transition:opacity .9s ease,transform 1s var(--ease),background .3s ease}
/* Dark section: the hover lifts a little gold in, it does not flash white. */
.lifts li:hover{background:rgba(232,198,95,.055)}
.lifts li:hover .k{color:var(--gold);-webkit-text-stroke-color:transparent}
/* Solid gold, not a hollow bronze outline that disappears on navy. */
.lifts .k{font-family:var(--head);font-size:1.3rem;font-weight:400;
  color:rgba(232,198,95,.78);-webkit-text-stroke:0;line-height:1;
  transition:color .3s ease}
.lifts h3{font-size:16px;margin-bottom:2px}
.lifts p{font-size:1rem;margin:0;color:var(--mist)}
.split{display:grid;grid-template-columns:1fr 1fr;gap:clamp(26px,4vw,56px);align-items:start}

/* ══ 09 · BIO ══ */
.biogrid{display:flex;align-items:center;justify-content:center;gap:clamp(26px,4vw,58px)}
.biogrid > :first-child{flex:0 0 auto}
.biogrid .reelwrap{height:clamp(300px,36vw,440px);width:auto;aspect-ratio:9/16;
  background:#000;border:1px solid var(--line-cream);display:flex;
  align-items:center;justify-content:center}
.biogrid .txt{max-width:56ch}
.biorole{font-family:var(--head);font-size:clamp(17px,1.9vw,22px);font-weight:600;
  color:var(--ink);line-height:1.35;margin-bottom:14px}
.tags{display:flex;flex-wrap:wrap;gap:9px;margin-top:20px}
.tags span{font-family:var(--head);font-size:.6rem;font-weight:700;letter-spacing:.15em;
  text-transform:uppercase;color:#4A443C;border:1px solid rgba(160,138,94,.55);
  padding:8px 13px;border-radius:999px}

/* ══ 10 · VOICES ══
   Round, face-cropped, ringed in gold. The ring is a box-shadow so it sits
   outside the circle and never eats into the face. Subgrid keeps the three
   names and the three captions on one line whatever length the quotes run. */
.voices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));
  grid-template-rows:auto auto auto;
  column-gap:clamp(24px,3.2vw,50px);row-gap:0;margin-top:clamp(30px,4vw,46px)}
.voice{margin:0;text-align:center;display:grid;grid-row:span 3;
  grid-template-rows:subgrid;align-content:start}
.voice img{width:clamp(112px,13vw,152px);height:auto;aspect-ratio:1/1;
  object-fit:cover;display:block;margin:0 auto;border-radius:50%;
  box-shadow:0 0 0 3px var(--black),0 0 0 4px var(--gold);
  filter:saturate(.92) brightness(.98)}
.voice blockquote{margin:22px 0 0;font-family:var(--head);font-weight:500;
  font-size:clamp(15px,1.5vw,17.5px);line-height:1.55;color:var(--cream);text-wrap:pretty}
.voice .who{margin:16px 0 0;font-family:var(--head);font-size:.62rem;font-weight:800;
  letter-spacing:.2em;text-transform:uppercase;color:var(--gold)}
.reels{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:clamp(18px,2.4vw,34px);
  max-width:720px;margin:clamp(30px,4vw,44px) auto 0}
.reel{margin:0}
.reel .rwrap{aspect-ratio:9/16;background:#000;border:1px solid var(--border);
  display:flex;align-items:center;justify-content:center;transition:border-color .3s ease}
.reel .rwrap{position:relative}
.reel .rwrap .ring{width:56px;height:56px}
.len{position:absolute;right:10px;bottom:10px;padding:4px 9px;background:rgba(4,18,42,.85);
  font-family:var(--head);font-size:.6rem;font-weight:700;letter-spacing:.08em;color:var(--mist)}
.vidwrap,.reelwrap{position:relative}
/* ── Real video, poster-first ──────────────────────────────────────────
   preload="none" so four videos cost nothing until one is asked for. The
   poster carries the frame; the button sits over it and starts playback,
   then hands over to native controls. */
.vidwrap video,.reelwrap video,.rwrap video{
  position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
  display:block;background:transparent;border:0}
/* Once it is actually playing it gets its own black ground back. */
.playing video{background:#000}
.vplay{position:absolute;inset:0;width:100%;height:100%;border:0;padding:0;
  background:transparent;cursor:pointer;display:flex;
  align-items:center;justify-content:center;z-index:2}
.vplay:focus-visible{outline:2px solid var(--gold);outline-offset:-4px}
.playing .vplay,.playing .len,.playing .src{display:none}
/* The preview has no access to the site's /assets, so an unresolved poster
   would read as a broken box. This is what sits behind it instead. The two
   descendant selectors are matched exactly: .biogrid .reelwrap and .reel .rwrap
   each set background:#000 at a higher specificity, so a bare .reelwrap here
   loses to them and only the VSL would pick up the hatch. */
.vidwrap,
.biogrid .reelwrap,
.reel .rwrap{
  background:var(--card) repeating-linear-gradient(-45deg,
    rgba(232,198,95,.10) 0 1px, transparent 1px 14px)}
/* Playing, each gets its black ground back so letterboxing reads correctly. */
.vidwrap.playing,.biogrid .reelwrap.playing,.reel .rwrap.playing{background:#000}
.reel .rwrap .ring::after{border-width:10px 0 10px 15px}
.reel:hover .rwrap{border-color:rgba(232,198,95,.45)}
@media(max-width:560px){.reels{grid-template-columns:1fr;max-width:300px}}
.pending{margin:26px auto 0;max-width:none;text-align:center;font-family:var(--head);
  font-size:.6rem;font-weight:800;letter-spacing:.18em;text-transform:uppercase;
  color:var(--coral-lift)}
@media(max-width:860px){
  .voices{grid-template-columns:1fr;grid-template-rows:none;row-gap:40px;max-width:520px;
    margin-left:auto;margin-right:auto}
  .voice{display:block!important}
}

/* ══ 11 · THE STANDARD · rows respond ══ */
/* Your layout, ported from the live page. The head sits in a sticky rail
   and the three standards travel past it, so the right half of a wide
   screen carries content instead of sitting empty. The spine fades at the
   foot rather than stopping, and each node carries its own glow. */
.vals-split{display:grid;grid-template-columns:.88fr 1.12fr;
  gap:clamp(38px,5vw,84px);align-items:start}
.vals-rail{position:sticky;top:clamp(88px,12vh,132px)}
.vals-rail h2{margin-bottom:16px}
.vals-rail .lede{max-width:34ch}
.vals-rail .hand.sm{margin-top:18px}
.vals-spine{position:relative;margin-top:6px;padding-left:clamp(30px,4vw,52px)}
.vals-spine::before{content:"";position:absolute;left:0;top:.7em;bottom:22%;width:2px;
  background:linear-gradient(180deg,rgba(232,198,95,.85),rgba(232,198,95,.32) 58%,rgba(232,198,95,0))}
.vals-spine .v{position:relative;padding:0 0 clamp(26px,3vw,40px)}
.vals-spine .v:last-child{padding-bottom:0}
.vals-spine .node{position:absolute;left:calc(-1 * clamp(30px,4vw,52px) - 5px);top:.46em;
  width:12px;height:12px;border-radius:50%;background:var(--navy);border:2px solid var(--gold);
  box-shadow:0 0 0 5px rgba(232,198,95,.10),0 0 18px rgba(232,198,95,.45);
  transition:box-shadow .5s ease,transform .5s var(--ease)}
.vals-spine h3{font-family:var(--head);font-size:clamp(27px,3.4vw,44px);font-weight:300;
  line-height:1.1;letter-spacing:-.015em;color:var(--gold);margin:0 0 10px;
  transition:transform .55s var(--ease)}
@supports (background-clip:text) or (-webkit-background-clip:text){
  .vals-spine h3{background:linear-gradient(94deg,#F7E7B4,#E8C65F 42%,#BE9A22);
    -webkit-background-clip:text;background-clip:text;color:transparent}
}
.vals-spine p{font-size:clamp(15px,1.12vw,17px);line-height:1.7;color:#9FB0CB;
  margin:0;max-width:44ch}
.vals-spine .v:hover .node{transform:scale(1.18);
  box-shadow:0 0 0 7px rgba(232,198,95,.14),0 0 26px rgba(232,198,95,.7)}
.vals-spine .v:hover h3{transform:translateX(7px)}
@media(max-width:900px){
  .vals-split{grid-template-columns:1fr;gap:30px}
  .vals-rail{position:static}
}

/* ══ 11 · INTENTION ══ */
#intention{text-align:center;padding-top:clamp(30px,3.4vw,48px);
  padding-bottom:clamp(30px,3.4vw,48px)}
#intention .promise-line{color:var(--cream)}
#intention .ey{margin-bottom:10px}
#intention .promise-sub{margin-top:7px}
/* The rail has travelled the whole page. Here it stops being a rail and
   becomes one line. Ported from the other door, where this section is the
   quietest on the page on purpose. */
.oneline{width:0;height:1px;background:var(--gold);margin:0 auto 22px;
  transition:width 1.8s var(--ease)}
#intention.on .oneline{width:min(460px,72%)}
#rm-page.no-anim .oneline{width:min(460px,72%)}

/* ══ 12 · OFFER ══ */
.pricegrid{display:grid;grid-template-columns:minmax(0,.82fr) minmax(0,1.18fr);
  gap:clamp(20px,2.6vw,40px);align-items:stretch;max-width:1000px;
  margin:clamp(28px,4vw,42px) auto 0;text-align:left}
@media(max-width:860px){.pricegrid{grid-template-columns:1fr;max-width:560px}}
.opencard{background:var(--card);border:1px solid var(--border);border-top:2px solid var(--gold);
  padding:26px 28px 28px;text-align:center;display:flex;flex-direction:column;justify-content:center}
.opencard .free{font-family:var(--head);font-size:clamp(34px,5vw,54px);font-weight:700;
  color:var(--gold);line-height:1;margin-bottom:14px}
.opencard .when{font-family:var(--head);font-size:.95rem;font-weight:600;color:var(--cream)}
.opencard .when span{display:block;margin-top:3px;font-weight:500;color:var(--mist)}
.opencard .where{margin-top:10px;font-size:.9rem;color:var(--slate)}
.opencard .lede{margin:16px 0 0;font-size:.95rem}
.stay{background:var(--card);border:1px solid var(--border);padding:26px 28px 28px;text-align:left}
.stay h3{margin-bottom:8px}
.stay ul{list-style:none;margin:22px 0 0;padding:0}
.stay li{position:relative;padding:9px 0 9px 26px;border-top:1px solid var(--border);
  font-size:1rem;color:var(--mist)}
.stay li:last-child{border-bottom:1px solid var(--border)}
.stay li::before{content:"";position:absolute;left:0;top:1.35em;width:11px;height:1px;
  background:var(--gold)}
.pricenote{margin:26px 0 0;font-size:.95rem;color:var(--slate)}
.pricenote b{color:var(--gold);font-weight:600}
.cta-row{margin:30px 0 0;text-align:center}
.cta-row .fine{margin:14px auto 0;font-size:.9rem;color:var(--slate);max-width:44ch}

/* ══ 13 · AFTER · cards respond ══ */
.after-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:26px}
.after-grid > div{border:1px solid var(--line-cream);padding:22px 20px;
  transition:background .3s ease,transform .35s var(--ease)}
.after-grid > div:hover{background:#FFFDF7;transform:translateY(-3px)}
.after-grid h3{font-size:17px;margin-bottom:6px}
.after-grid p{font-size:1rem;margin:0;color:var(--ink-soft)}

/* ══ 14 · FAQ · answers open ══ */
.faq{max-width:820px}
.faq details{border-bottom:1px solid rgba(241,236,225,.13)}
.faq details:first-of-type{border-top:1px solid rgba(241,236,225,.13)}
.faq summary{list-style:none;cursor:pointer;padding:18px 34px 18px 0;position:relative;
  font-family:var(--head);font-size:16px;font-weight:600;color:var(--cream);line-height:1.45;
  transition:color .28s ease}
.faq summary::-webkit-details-marker{display:none}
.faq summary::after{content:"+";position:absolute;right:0;top:16px;width:27px;height:27px;
  border:1px solid rgba(255,80,31,.4);border-radius:50%;color:var(--coral);
  display:flex;align-items:center;justify-content:center;font-size:17px;
  transition:transform .3s var(--ease),background .3s ease,box-shadow .3s ease}
.faq details[open] summary::after{transform:rotate(45deg);background:rgba(255,80,31,.12)}
.faq summary:hover{color:var(--gold-lift)}
.faq summary:hover::after{border-color:var(--coral);box-shadow:0 0 16px rgba(255,80,31,.28)}
.faq summary:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
.faq .ans{padding:0 0 20px;font-size:1rem;line-height:1.8;color:var(--mist);max-width:66ch}
.faq details[open] .ans{animation:faqIn .42s var(--ease)}
@keyframes faqIn{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}

/* ══ 15 · CLOSE ══ */
#cta-final{text-align:center}
#cta-final h2{font-size:clamp(26px,4.4vw,52px);margin-bottom:18px}
#cta-final .lede{margin-bottom:30px}

/* ══ REVIEW CHROME ══ */
.centre .note{margin-left:auto;margin-right:auto}
.sec.cream .note,.sec.cream-2 .note{border-left-color:rgba(160,138,94,.6);color:#6E655B}
.sec.cream .note b,.sec.cream-2 .note b{color:#7A6742}
:where(#rm-page) code{font-family:ui-monospace,Menlo,monospace;font-size:.9em;color:var(--gold-lift)}
.tbc{color:var(--coral);border-bottom:1px dotted var(--coral)}

@media(max-width:860px){
  .split,.after-grid{grid-template-columns:1fr}
  .biogrid{flex-direction:column}
}
@media(max-width:760px){
  .steps .row{grid-template-columns:1fr;gap:30px}
  .steps .track{display:none}
}
@media(prefers-reduced-motion:reduce){
  *{animation:none!important;transition:none!important}
}

/* ══ MOBILE ══════════════════════════════════════════════════════════
   The same pass the women's page had: spacing and alignment only, nothing
   here changes a word or a behaviour. Vertical rhythm that reads as air at
   1440 reads as a gap at 390. */
@media(max-width:860px){
  /* Each figure claims three subgrid rows so the names line up across the
     row. In one column that becomes nine rows and eight gaps, and the three
     of them drift apart. */
  .voice{grid-row:auto}
  /* Full phone width and centred, rather than a thumbnail left where the
     desktop layout dropped it. */
  .biogrid{display:block;text-align:center}
  .biogrid .reelwrap,.biogrid .reel{width:100%;max-width:none;height:auto;margin:0 auto 26px}
  .biogrid > :last-child{text-align:left}
}
@media(max-width:640px){
  .steps .row{gap:22px}
  .step h4{margin:10px 0 5px}
  .why-not{margin-top:34px}
  /* The negations centre rather than ragging left. */
  .nots{justify-content:center;text-align:center;gap:10px 16px}
  /* One column, and padding the row can carry at this width. */
  .lifts li{grid-template-columns:1fr;gap:8px;padding:14px 18px 15px}
  .rcs{justify-content:center;text-align:center;margin-top:24px}
  .after-grid > div{padding:15px 18px 16px}
  .cta-row .fine{margin-top:24px}
}

/* ══ 06 · DISTINCTION · THE CLEARING ═══════════════════════════════════
   The section is subtraction: five things this is not, and a standing line
   that says nobody is being asked to be more of anything. So the picture is
   removal, not addition. The pane starts filled with the page's own hatch
   and each crossing clears a band of it. What is left when the five are gone
   is the frame that was under it the whole time.

   Hovering a line you have already crossed puts its band back, so you can
   see what that one took out. That is the only interaction, and it is the
   thing that makes the pane legible rather than decorative. */
.clr{
  display:grid;grid-template-columns:minmax(0,.95fr) minmax(0,1.05fr);
  gap:clamp(26px,4vw,64px);align-items:start;margin:22px 0 0;
}
@media(max-width:820px){ .clr{grid-template-columns:1fr;gap:26px} }
.clr-l .nots{flex-direction:column;align-items:flex-start;gap:9px;margin:0}
.clr-l .nots li{font-size:clamp(16px,1.5vw,21px)}
.clr-l .stand{margin-top:26px;opacity:.42;transition:opacity 1.1s var(--ease)}
.clr.done .clr-l .stand{opacity:1}
#rm-page.no-anim .clr-l .stand{opacity:1!important}
/* In the right column the three answers stack: label over answer, so each is a
   block rather than a wide row. */
.clr-r .not3{margin:0;border-top:none}
.clr-r .not3 .r{grid-template-columns:1fr;gap:5px;padding:0 0 16px}
.clr-r .not3 .r:first-child{padding-top:0}
.clr-r .not3 .r:last-child{border-bottom:none;padding-bottom:0}
.clr-r .not3 .r:hover{background:transparent;padding-left:0}

/* ══ 04 · RECOGNITION · two columns ════════════════════════════════════
   Six lines at display size down the full width was a paragraph's worth of
   words taking a screen and a half. Three and three, at a size you read
   rather than announce. */
#recognition .load{
  display:grid;grid-template-columns:repeat(2,minmax(0,1fr));
  grid-template-rows:repeat(3,auto);grid-auto-flow:column;
  column-gap:clamp(26px,3.6vw,60px);max-width:none;
}
#recognition .load li{
  font-size:clamp(15px,1.15vw,17.5px);line-height:1.5;padding:12px 0 12px 24px;
}
#recognition .load li::before{top:21px}
@media(max-width:760px){
  #recognition .load{grid-template-columns:1fr;grid-template-rows:none;grid-auto-flow:row}
}
#recogCloser{margin-top:26px}
`;
  var HTML = `<div id="rm-page">
<parallaxx-nav active="men"></parallaxx-nav>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&family=Montserrat:wght@400;500;600;700&family=Permanent+Marker&display=swap">
<style>
/* ══════════════════════════════════════════════════════════════════════
   THE RECONNECTED MAN · proposed page
   Built on the v10 system the Reconnected Woman page now runs: same
   tokens, same grounds, same [data-r] depth reveals, same masked
   headlines, same motion contract (reduce || noIO -> everything gets
   its end state, plus a 4s failsafe).

   WHAT IS HIS AND STAYS HIS. Three things the women's page does not
   have and should not: the diagonal gold hatch over the hero, the gold
   bloom behind the section heads, and the marker hand for the lowercase
   asides. They are the whole visual difference between the two doors.
   ══════════════════════════════════════════════════════════════════════ */
:root{
  --navy:#04122A; --black:#061938; --ink-navy:#030C1C; --card:#0A1D3C;
  --border:#1B2C46;
  --cream:#F1ECE1; --cream-2:#F7F3EA; --line-cream:#DED6C4;
  --coral:#FF501F; --coral-lift:#FF6A3D;
  --gold:#E8C65F; --gold-lift:#F3DD93; --gold-dark:#C9A227;
  --bronze:#A08A5E; --bronze-ink:#6E5C33;  /* bronze fails AA on cream; -ink is the text weight */
  --mist:#B1BFD7; --slate:#7C89A3;
  --ink:#1F2A3D; --ink-soft:#5A6274;
  --head:'Poppins',system-ui,-apple-system,sans-serif;
  --body:'Montserrat',system-ui,-apple-system,sans-serif;
  --hand:'Permanent Marker',cursive;
  --gut:clamp(20px,4vw,52px);
  --col:1240px;
  --ease:cubic-bezier(.16,1,.3,1);
}
#rm-page *{box-sizing:border-box}

#rm-page{margin:0;background:var(--ink-navy);color:var(--mist);
  font-family:var(--body);font-size:16px;line-height:1.68;
  -webkit-font-smoothing:antialiased;overflow-x:clip}

/* ── the rail ─────────────────────────────────────────────────── */
#rail{position:fixed;top:0;bottom:0;left:calc(var(--gut) / 2 - 1px);
  width:9px;z-index:60;pointer-events:none;display:flex;justify-content:center}
#rail .bar{width:1px;height:100%;background:var(--slate);opacity:.22}
/* Where you are standing against the sightline. The rail without it is
   a hairline; with it, it is the one piece of chrome that tracks him. */
#rail .pos{position:absolute;left:50%;width:3px;height:46px;top:0;
  transform:translateX(-50%);border-radius:3px;
  background:var(--gold);opacity:.9;box-shadow:0 0 14px rgba(232,198,95,.55)}
@media(max-width:820px){#rail{left:7px}}

/* ── grounds ──────────────────────────────────────────────────── */
.sec{padding:clamp(52px,6.5vw,92px) var(--gut);position:relative}
.sec.ink{background:var(--ink-navy)}
.sec.dark{background:var(--navy)}
.sec.black{background:var(--black)}
.sec.cream{background:var(--cream);color:var(--ink)}
.sec.cream-2{background:var(--cream-2);color:var(--ink)}
.in{max-width:var(--col);margin:0 auto}
.narrow{max-width:1040px}
.mid{max-width:820px}

/* THE GOLD BLOOM behind a section head. The women's page has no
   equivalent. It is what stops a flat navy field reading as a slide. */
.glow{position:absolute;top:-6%;left:0;width:min(720px,92%);height:56%;
  pointer-events:none;
  background:radial-gradient(ellipse at 22% 30%,rgba(232,198,95,.11),transparent 66%)}

/* ── type ─────────────────────────────────────────────────────── */
.ey{font-family:var(--head);font-size:.62rem;font-weight:800;letter-spacing:.24em;
  text-transform:uppercase;color:var(--gold);margin:0 0 13px;max-width:none}
.sec.cream .ey,.sec.cream-2 .ey{color:var(--bronze-ink)}
:where(#rm-page) h1,:where(#rm-page) h2,:where(#rm-page) h3,:where(#rm-page) h4{font-family:var(--head);margin:0;text-wrap:balance}
:where(#rm-page) h2{font-size:clamp(27px,3.4vw,42px);font-weight:700;line-height:1.1;
  letter-spacing:-.02em;color:var(--cream);margin-bottom:16px}
:where(#rm-page) h2 .hit{color:var(--gold-lift)}
.sec.cream h2,.sec.cream-2 h2{color:var(--ink)}
:where(#rm-page) h3{font-size:clamp(19px,2vw,25px);font-weight:600;line-height:1.28;color:var(--cream)}
.sec.cream h3,.sec.cream-2 h3{color:var(--ink)}
:where(#rm-page) p{margin:0 0 14px;max-width:64ch}
.lede{font-size:clamp(16px,1.55vw,19px);line-height:1.55;color:var(--mist)}
.sec.cream .lede,.sec.cream-2 .lede{color:var(--ink-soft)}
.dim{color:var(--slate)}
.sec.cream .dim,.sec.cream-2 .dim{color:var(--ink-soft)}
:where(#rm-page) em{font-style:italic}
.divider{width:46px;height:1px;background:var(--gold);margin:0 0 18px}
.sec.cream .divider,.sec.cream-2 .divider{background:var(--bronze)}
.centre{text-align:center}
.centre p,.centre .lede{margin-left:auto;margin-right:auto}
.centre .divider{margin-left:auto;margin-right:auto}

/* HIS MARKER HAND. Lowercase, never a heading, never a claim. It is the
   aside in his own writing, and it is the loudest single thing that says
   this is the men's door. */
.hand{font-family:var(--hand);font-weight:400;text-transform:lowercase;
  font-size:clamp(17px,1.9vw,24px);line-height:1.25;color:var(--gold);
  margin:0 0 14px;letter-spacing:.01em}
.sec.cream .hand,.sec.cream-2 .hand{color:var(--bronze-ink)}
.hand.sm{font-size:clamp(15px,1.5vw,19px)}

/* ── buttons ──────────────────────────────────────────────────── */
.btn{display:inline-block;font-family:var(--head);font-size:.76rem;font-weight:700;
  letter-spacing:.13em;text-transform:uppercase;text-decoration:none;border:none;
  background:var(--coral);color:#fff;padding:16px 30px;border-radius:2px;cursor:pointer;
  transition:background .22s ease,transform .22s var(--ease)}
.btn:hover{background:var(--coral-lift);transform:translateY(-2px)}
.btn:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
.btn.ghost{background:transparent;color:var(--gold);border:1px solid rgba(232,198,95,.45)}
.btn.ghost:hover{background:rgba(232,198,95,.1)}
.btn.full{display:block;width:100%;text-align:center}

/* ── placeholders, his convention ─────────────────────────────── */
.ph{position:relative;display:flex;flex-direction:column;align-items:center;
  justify-content:center;text-align:center;gap:10px;padding:28px;
  border:2px dashed var(--coral);border-radius:16px;min-height:200px;
  background:repeating-linear-gradient(45deg,rgba(255,80,31,.05) 0 12px,rgba(255,80,31,.10) 12px 24px)}
.ph b{font-family:var(--head);font-size:.64rem;font-weight:800;letter-spacing:.18em;
  text-transform:uppercase;color:var(--coral-lift)}
.ph span{font-size:.85rem;line-height:1.6;max-width:40ch;color:#E6BCAC}
.sec.cream .ph span,.sec.cream-2 .ph span{color:#8A6F62}

/* ══ REVEAL SYSTEM · depth-graded, one-shot. Lifted from v10. ══ */
[data-r]{opacity:0;transform:translateY(var(--dy,12px));
  transition:opacity .85s ease,transform .85s var(--ease)}
[data-r="far"]{--dy:4px}
[data-r="mid"]{--dy:12px}
[data-r="near"]{--dy:28px}
[data-r].seen{opacity:1;transform:none}
#rm-page.no-anim [data-r]{opacity:1!important;transform:none!important}

.mask{overflow:hidden;display:block}
.mask > span{display:block;transform:translateY(104%);
  transition:transform 1.05s var(--ease)}
.mask.seen > span{transform:translateY(0)}
#rm-page.no-anim .mask > span{transform:none!important}

/* ══ 01 · HERO ══ */
#hero{position:relative;min-height:clamp(560px,72svh,780px);display:flex;
  align-items:flex-end;padding:0;overflow:hidden;
  background:radial-gradient(120% 90% at 50% 14%,#0A2148 0%,#061938 44%,#030C1E 100%)}
/* HIS HATCH. A -45deg gold thread at .03, 58px apart. The other door opens on a
   photograph; this one opens on a made surface, and the hatch is what
   keeps a gradient-only hero from reading as empty. */
/* THE PLATE. The settle is ported from the women's build: the frame opens
   tighter and eases out over 2.6s, so the picture fills in while the
   headline is still being read. This hero has been gradient-only since
   launch and the .dc.html has carried a TODO for it since August. The
   motion is built; the photograph is the thing to shoot. */
#hero .plate{position:absolute;inset:0;z-index:0;overflow:hidden}
#hero .shot{position:absolute;inset:-2% 0;
  background-image:url('/assets/img/rm-hero.jpg');
  background-size:cover;background-position:50% 42%;
  transform-origin:50% 32%;
  animation:heroSettle 2.6s cubic-bezier(.16,1,.3,1) .18s both}
@keyframes heroSettle{
  from{transform:scale(1.085) translateY(1.9%)}
  to{transform:scale(1) translateY(0)}}
@media(prefers-reduced-motion:reduce){#hero .shot{animation:none}}
#hero .scrim{position:absolute;inset:0;z-index:1;pointer-events:none;
  background:
    radial-gradient(ellipse 52% 84% at 50% 100%,rgba(3,12,28,.97) 0%,rgba(3,12,28,.91) 28%,
      rgba(3,12,28,.56) 58%,rgba(3,12,28,.18) 82%,rgba(3,12,28,0) 100%),
    linear-gradient(0deg,rgba(3,12,28,.35) 0%,rgba(3,12,28,.30) 34%,
      rgba(3,12,28,.29) 66%,rgba(3,12,28,.34) 100%)}
#hero .hatch{position:absolute;inset:0;pointer-events:none;z-index:1;
  background-image:repeating-linear-gradient(-45deg,transparent,transparent 58px,
    rgba(232,198,95,.03) 58px,rgba(232,198,95,.03) 59px)}
#hero .bloom{position:absolute;top:34%;left:50%;z-index:1;pointer-events:none;
  width:min(760px,110vw);height:min(760px,110vw);transform:translate(-50%,-50%);
  background:radial-gradient(circle,rgba(232,198,95,.10) 0%,rgba(232,198,95,.04) 38%,transparent 68%);
  animation:bloomSettle 2.6s var(--ease) .18s both}
@keyframes bloomSettle{from{opacity:0;transform:translate(-50%,-50%) scale(.9)}
  to{opacity:1;transform:translate(-50%,-50%) scale(1)}}
#hero .hin{position:relative;z-index:2;width:100%;max-width:var(--col);margin:0 auto;
  padding:clamp(70px,10vh,110px) var(--gut) clamp(52px,8vh,96px);text-align:center}
#hero h1{font-size:clamp(44px,8.4vw,104px);font-weight:700;line-height:.96;
  letter-spacing:-.035em;color:var(--cream);margin:0 0 26px}
#hero h1 .key{color:var(--gold)}
#hero .sub{font-size:clamp(16px,1.7vw,20px);line-height:1.5;color:var(--mist);
  max-width:min(620px,100%);margin:0 auto}
#hero .sub span,#hero .sub b{display:block;text-wrap:balance}
#hero .sub span{margin-bottom:.5em}
#hero .sub b{color:var(--cream);font-weight:600}
#hero .btn{margin-top:26px}
#hero .next{margin:20px auto 0;max-width:none;font-family:var(--head);font-size:.66rem;
  font-weight:700;letter-spacing:.16em;text-transform:uppercase;color:var(--gold)}
#hero .cue{position:absolute;left:50%;bottom:20px;z-index:3;color:var(--gold);
  opacity:.5;font-size:1.3rem;line-height:1;transform:translateX(-50%);
  animation:cue 2.6s ease-in-out infinite}
@keyframes cue{0%,100%{transform:translateX(-50%) translateY(0)}
  50%{transform:translateX(-50%) translateY(6px)}}

/* ══ 02 · PROMISE BAND ══ */
#promise{text-align:center;padding-top:clamp(28px,3.4vw,46px);
  padding-bottom:clamp(28px,3.4vw,46px)}
.promise-rule{width:0;height:1px;background:rgba(232,198,95,.6);margin:0 auto 20px;
  transition:width 1.2s var(--ease)}
.promise-rule.on{width:42px}
.promise-line{font-family:var(--head);font-weight:400;font-size:clamp(17px,2vw,26px);
  line-height:1.22;letter-spacing:-.018em;color:var(--cream);margin:0 auto;text-wrap:balance}
@media(min-width:800px){.promise-line{white-space:nowrap}}
.promise-sub{font-family:var(--head);font-weight:400;font-size:clamp(14px,1.15vw,17px);
  line-height:1.4;color:var(--slate);max-width:520px;margin:10px auto 0}

/* ══ 03 · VSL ══ */
.vidwrap{position:relative;aspect-ratio:16/9;margin-top:24px;background:#000;
  overflow:hidden;border:1px solid var(--border);display:flex;
  align-items:center;justify-content:center}
.ring{width:78px;height:78px;border-radius:50%;border:2px solid var(--gold);
  background:rgba(6,25,56,.85);display:flex;align-items:center;justify-content:center;
  transition:transform .25s var(--ease),box-shadow .3s ease}
.ring::after{content:"";border-style:solid;border-width:14px 0 14px 22px;
  border-color:transparent transparent transparent var(--gold);margin-left:5px}
.vidwrap:hover .ring{transform:scale(1.07);box-shadow:0 0 26px rgba(255,80,31,.4)}
.vquote{font-family:var(--head);font-weight:300;font-style:italic;
  font-size:clamp(16px,1.7vw,21px);line-height:1.5;color:var(--cream);
  border-left:2px solid var(--gold);padding-left:22px;margin:26px auto 0;max-width:68ch}
.vquote cite{display:block;margin-top:10px;font-family:var(--head);font-size:.62rem;
  font-weight:800;letter-spacing:.2em;text-transform:uppercase;color:var(--slate);font-style:normal}

/* ══ 04 · RECOGNITION · the staged load ══ */
#recognition .load{list-style:none;margin:0;padding:0;max-width:900px}
#recognition .load li{font-family:var(--head);font-weight:300;
  font-size:clamp(16px,1.8vw,22px);line-height:1.4;color:var(--cream);
  padding:16px 0 16px 26px;border-bottom:1px solid rgba(232,198,95,.14);
  position:relative;opacity:0;transition:opacity .5s ease}
#recognition .load li::before{content:"";position:absolute;left:0;top:26px;
  width:14px;height:1px;background:rgba(232,198,95,.5);
  transition:width .45s var(--ease),background .45s ease}
#recognition .load li.on{opacity:1;animation:settle .5s var(--ease)}
#recognition .load li:hover::before{width:20px;background:var(--gold)}
@keyframes settle{0%{filter:blur(2px)}100%{filter:blur(0)}}
#rm-page.no-anim #recognition .load li{opacity:1!important}
#recogCloser{font-family:var(--head);font-weight:700;
  font-size:clamp(18px,2.2vw,27px);line-height:1.3;color:var(--cream);
  margin-top:28px;opacity:0;transition:opacity .8s ease}
#recogCloser span{display:block;color:var(--gold)}
#recogCloser.on{opacity:1}
#rm-page.no-anim #recogCloser{opacity:1!important}

/* ══ 05 · TRAP · the costumes fall away ══ */
.costumes{list-style:none;display:flex;flex-wrap:wrap;gap:10px;
  margin:24px 0 0;padding:0;max-width:none}
.costumes li{font-family:var(--head);font-size:.68rem;font-weight:700;
  letter-spacing:.16em;text-transform:uppercase;color:var(--cream);
  border:1px solid var(--border);background:var(--card);padding:11px 17px;
  transition:color 1s ease,opacity 1s ease,transform 1s var(--ease),border-color 1s ease}
#trapRig.shifted .costumes li{opacity:.42;transform:scale(.97);
  border-color:rgba(27,44,70,.6)}
.trap-under{font-family:var(--head);font-weight:300;font-style:italic;
  font-size:clamp(17px,2.1vw,25px);line-height:1.4;color:var(--cream);
  border-left:2px solid var(--gold);padding-left:24px;text-align:left;
  max-width:44ch;margin:30px auto 0;opacity:0;transform:translateY(8px);
  transition:opacity .9s ease,transform .9s var(--ease)}
#trapRig.shifted .trap-under{opacity:1;transform:none}
#rm-page.no-anim .trap-under{opacity:1!important;transform:none!important}

/* ══ 06 · HOW · the runner travels the track ══ */
.steps .row{display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(20px,3vw,38px);
  position:relative}
.steps .track{position:absolute;top:22px;left:6%;right:6%;height:1px;
  background:rgba(160,138,94,.35);overflow:visible}
.steps .runner{position:absolute;top:50%;left:0;width:7px;height:7px;border-radius:50%;
  background:var(--bronze);transform:translate(-50%,-50%);opacity:0;
  box-shadow:0 0 0 4px rgba(160,138,94,.18)}
.steps.on .runner{animation:run 3.4s linear forwards}
@keyframes run{0%{left:0;opacity:0}6%{opacity:1}94%{opacity:1}100%{left:100%;opacity:0}}
.step{position:relative;text-align:left}
.step .n{width:44px;height:44px;border-radius:50%;border:1px solid rgba(160,138,94,.55);
  display:flex;align-items:center;justify-content:center;margin:0 0 16px;
  font-family:var(--head);font-size:.8rem;font-weight:700;color:var(--bronze-ink);
  background:var(--cream);position:relative;z-index:1;
  transition:border-color .45s ease,color .45s ease,background .45s ease,
    transform .45s var(--ease),box-shadow .45s ease}
.steps.on .step.lit .n{box-shadow:0 0 0 5px rgba(160,138,94,.14);
  transform:translateY(-3px);border-color:var(--bronze);color:var(--ink)}
.step h3{margin-bottom:8px}
.step p{font-size:1rem;line-height:1.6;margin:0;max-width:30ch;color:var(--ink-soft)}
.why-not{margin:26px 0 0;font-family:var(--head);font-size:clamp(18px,1.9vw,23px);
  font-weight:600;color:var(--ink);max-width:none}
.why-not span{display:block;color:var(--bronze-ink);margin-top:4px}
@media(min-width:820px){.why-not{white-space:nowrap}}
.rcs{margin:32px 0 0;padding-top:18px;border-top:1px solid var(--line-cream);
  display:flex;flex-wrap:wrap;align-items:baseline;gap:8px 18px}
.rcs .k{font-family:var(--head);font-size:clamp(15px,1.5vw,17px);font-weight:700;color:var(--ink)}
.rcs .v{font-family:var(--head);font-size:.66rem;font-weight:800;letter-spacing:.2em;
  text-transform:uppercase;color:var(--bronze-ink)}

/* ══ 07 · WHAT THIS ISN'T · struck items recede ══ */
/* Five words, one row, one gesture. A 2px line draws across each in turn
   from the left, and the word dims behind it. Ported from the other door,
   where the same list is the section's whole argument. */
.nots{list-style:none;margin:22px 0 0;padding:0;display:flex;flex-wrap:wrap;
  gap:12px clamp(14px,1.9vw,26px);max-width:none}
@media(min-width:1000px){.nots{flex-wrap:nowrap}}
.nots li{position:relative;font-family:var(--head);white-space:nowrap;
  font-size:clamp(15px,2.05vw,27px);font-weight:600;color:var(--slate);
  transition:color 1s ease}
.nots li::after{content:"";position:absolute;left:-4px;right:-4px;top:52%;height:2px;
  background:var(--gold-dark);transform:scaleX(0);transform-origin:left center;
  transition:transform .95s var(--ease)}
.nots li.struck::after{transform:scaleX(1)}
.nots li.struck{color:#5A6880}
.stand{margin:28px 0 0;font-family:var(--head);font-size:clamp(18px,2vw,25px);
  font-weight:600;line-height:1.4;color:var(--cream);max-width:none}
/* Label left, answer right. No numerals: three negations are a set, not a
   sequence, and numbering a set is decoration. */
.not3{list-style:none;margin:30px 0 0;padding:0;border-top:1px solid var(--border)}
.not3 .r{display:grid;grid-template-columns:minmax(0,300px) minmax(0,1fr);gap:6px 34px;
  padding:16px 0;border-bottom:1px solid var(--border);
  transition:background .3s ease,padding-left .3s var(--ease)}
.not3 .r:hover{background:rgba(232,198,95,.05);padding-left:12px}
.not3 h3{font-size:clamp(17px,1.8vw,21px);font-weight:600;color:var(--cream)}
.not3 p{font-size:1rem;margin:0;color:var(--slate)}
@media(max-width:720px){.not3 .r{grid-template-columns:1fr;gap:4px}}

/* ══ 08 · DIFFERENCE · the release ══ */
#difference .question{font-family:var(--head);font-weight:300;
  font-size:clamp(19px,2.7vw,34px);line-height:1.35;color:var(--cream);
  max-width:22ch;margin:0 0 18px}
#difference.released .question{animation:lift 1.1s var(--ease)}
@keyframes lift{from{transform:translateY(6px)}to{transform:none}}
.release-note{font-family:var(--head);font-size:clamp(15px,1.5vw,18px);font-weight:600;
  line-height:1.45;color:var(--bronze);max-width:34ch;margin:16px 0 0;
  opacity:0;transition:opacity .9s ease}
.release-note.on{opacity:1}
#rm-page.no-anim .release-note{opacity:1!important}
.lifts{list-style:none;margin:0;padding:0}
.lifts li{display:grid;grid-template-columns:40px 1fr;gap:0 14px;align-items:baseline;
  padding:15px 14px 15px 0;border-bottom:1px solid rgba(232,198,95,.16);
  transition:opacity .9s ease,transform 1s var(--ease),background .3s ease}
/* Dark section: the hover lifts a little gold in, it does not flash white. */
.lifts li:hover{background:rgba(232,198,95,.055)}
.lifts li:hover .k{color:var(--gold);-webkit-text-stroke-color:transparent}
/* Solid gold, not a hollow bronze outline that disappears on navy. */
.lifts .k{font-family:var(--head);font-size:1.3rem;font-weight:400;
  color:rgba(232,198,95,.78);-webkit-text-stroke:0;line-height:1;
  transition:color .3s ease}
.lifts h3{font-size:16px;margin-bottom:2px}
.lifts p{font-size:1rem;margin:0;color:var(--mist)}
.split{display:grid;grid-template-columns:1fr 1fr;gap:clamp(26px,4vw,56px);align-items:start}

/* ══ 09 · BIO ══ */
.biogrid{display:flex;align-items:center;justify-content:center;gap:clamp(26px,4vw,58px)}
.biogrid > :first-child{flex:0 0 auto}
.biogrid .reelwrap{height:clamp(300px,36vw,440px);width:auto;aspect-ratio:9/16;
  background:#000;border:1px solid var(--line-cream);display:flex;
  align-items:center;justify-content:center}
.biogrid .txt{max-width:56ch}
.biorole{font-family:var(--head);font-size:clamp(17px,1.9vw,22px);font-weight:600;
  color:var(--ink);line-height:1.35;margin-bottom:14px}
.tags{display:flex;flex-wrap:wrap;gap:9px;margin-top:20px}
.tags span{font-family:var(--head);font-size:.6rem;font-weight:700;letter-spacing:.15em;
  text-transform:uppercase;color:#4A443C;border:1px solid rgba(160,138,94,.55);
  padding:8px 13px;border-radius:999px}

/* ══ 10 · VOICES ══
   Round, face-cropped, ringed in gold. The ring is a box-shadow so it sits
   outside the circle and never eats into the face. Subgrid keeps the three
   names and the three captions on one line whatever length the quotes run. */
.voices{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));
  grid-template-rows:auto auto auto;
  column-gap:clamp(24px,3.2vw,50px);row-gap:0;margin-top:clamp(30px,4vw,46px)}
.voice{margin:0;text-align:center;display:grid;grid-row:span 3;
  grid-template-rows:subgrid;align-content:start}
.voice img{width:clamp(112px,13vw,152px);height:auto;aspect-ratio:1/1;
  object-fit:cover;display:block;margin:0 auto;border-radius:50%;
  box-shadow:0 0 0 3px var(--black),0 0 0 4px var(--gold);
  filter:saturate(.92) brightness(.98)}
.voice blockquote{margin:22px 0 0;font-family:var(--head);font-weight:500;
  font-size:clamp(15px,1.5vw,17.5px);line-height:1.55;color:var(--cream);text-wrap:pretty}
.voice .who{margin:16px 0 0;font-family:var(--head);font-size:.62rem;font-weight:800;
  letter-spacing:.2em;text-transform:uppercase;color:var(--gold)}
.reels{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:clamp(18px,2.4vw,34px);
  max-width:720px;margin:clamp(30px,4vw,44px) auto 0}
.reel{margin:0}
.reel .rwrap{aspect-ratio:9/16;background:#000;border:1px solid var(--border);
  display:flex;align-items:center;justify-content:center;transition:border-color .3s ease}
.reel .rwrap{position:relative}
.reel .rwrap .ring{width:56px;height:56px}
.len{position:absolute;right:10px;bottom:10px;padding:4px 9px;background:rgba(4,18,42,.85);
  font-family:var(--head);font-size:.6rem;font-weight:700;letter-spacing:.08em;color:var(--mist)}
.vidwrap,.reelwrap{position:relative}
/* ── Real video, poster-first ──────────────────────────────────────────
   preload="none" so four videos cost nothing until one is asked for. The
   poster carries the frame; the button sits over it and starts playback,
   then hands over to native controls. */
.vidwrap video,.reelwrap video,.rwrap video{
  position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
  display:block;background:transparent;border:0}
/* Once it is actually playing it gets its own black ground back. */
.playing video{background:#000}
.vplay{position:absolute;inset:0;width:100%;height:100%;border:0;padding:0;
  background:transparent;cursor:pointer;display:flex;
  align-items:center;justify-content:center;z-index:2}
.vplay:focus-visible{outline:2px solid var(--gold);outline-offset:-4px}
.playing .vplay,.playing .len,.playing .src{display:none}
/* The preview has no access to the site's /assets, so an unresolved poster
   would read as a broken box. This is what sits behind it instead. The two
   descendant selectors are matched exactly: .biogrid .reelwrap and .reel .rwrap
   each set background:#000 at a higher specificity, so a bare .reelwrap here
   loses to them and only the VSL would pick up the hatch. */
.vidwrap,
.biogrid .reelwrap,
.reel .rwrap{
  background:var(--card) repeating-linear-gradient(-45deg,
    rgba(232,198,95,.10) 0 1px, transparent 1px 14px)}
/* Playing, each gets its black ground back so letterboxing reads correctly. */
.vidwrap.playing,.biogrid .reelwrap.playing,.reel .rwrap.playing{background:#000}
.reel .rwrap .ring::after{border-width:10px 0 10px 15px}
.reel:hover .rwrap{border-color:rgba(232,198,95,.45)}
@media(max-width:560px){.reels{grid-template-columns:1fr;max-width:300px}}
.pending{margin:26px auto 0;max-width:none;text-align:center;font-family:var(--head);
  font-size:.6rem;font-weight:800;letter-spacing:.18em;text-transform:uppercase;
  color:var(--coral-lift)}
@media(max-width:860px){
  .voices{grid-template-columns:1fr;grid-template-rows:none;row-gap:40px;max-width:520px;
    margin-left:auto;margin-right:auto}
  .voice{display:block!important}
}

/* ══ 11 · THE STANDARD · rows respond ══ */
/* Your layout, ported from the live page. The head sits in a sticky rail
   and the three standards travel past it, so the right half of a wide
   screen carries content instead of sitting empty. The spine fades at the
   foot rather than stopping, and each node carries its own glow. */
.vals-split{display:grid;grid-template-columns:.88fr 1.12fr;
  gap:clamp(38px,5vw,84px);align-items:start}
.vals-rail{position:sticky;top:clamp(88px,12vh,132px)}
.vals-rail h2{margin-bottom:16px}
.vals-rail .lede{max-width:34ch}
.vals-rail .hand.sm{margin-top:18px}
.vals-spine{position:relative;margin-top:6px;padding-left:clamp(30px,4vw,52px)}
.vals-spine::before{content:"";position:absolute;left:0;top:.7em;bottom:22%;width:2px;
  background:linear-gradient(180deg,rgba(232,198,95,.85),rgba(232,198,95,.32) 58%,rgba(232,198,95,0))}
.vals-spine .v{position:relative;padding:0 0 clamp(26px,3vw,40px)}
.vals-spine .v:last-child{padding-bottom:0}
.vals-spine .node{position:absolute;left:calc(-1 * clamp(30px,4vw,52px) - 5px);top:.46em;
  width:12px;height:12px;border-radius:50%;background:var(--navy);border:2px solid var(--gold);
  box-shadow:0 0 0 5px rgba(232,198,95,.10),0 0 18px rgba(232,198,95,.45);
  transition:box-shadow .5s ease,transform .5s var(--ease)}
.vals-spine h3{font-family:var(--head);font-size:clamp(27px,3.4vw,44px);font-weight:300;
  line-height:1.1;letter-spacing:-.015em;color:var(--gold);margin:0 0 10px;
  transition:transform .55s var(--ease)}
@supports (background-clip:text) or (-webkit-background-clip:text){
  .vals-spine h3{background:linear-gradient(94deg,#F7E7B4,#E8C65F 42%,#BE9A22);
    -webkit-background-clip:text;background-clip:text;color:transparent}
}
.vals-spine p{font-size:clamp(15px,1.12vw,17px);line-height:1.7;color:#9FB0CB;
  margin:0;max-width:44ch}
.vals-spine .v:hover .node{transform:scale(1.18);
  box-shadow:0 0 0 7px rgba(232,198,95,.14),0 0 26px rgba(232,198,95,.7)}
.vals-spine .v:hover h3{transform:translateX(7px)}
@media(max-width:900px){
  .vals-split{grid-template-columns:1fr;gap:30px}
  .vals-rail{position:static}
}

/* ══ 11 · INTENTION ══ */
#intention{text-align:center;padding-top:clamp(30px,3.4vw,48px);
  padding-bottom:clamp(30px,3.4vw,48px)}
#intention .promise-line{color:var(--cream)}
#intention .ey{margin-bottom:10px}
#intention .promise-sub{margin-top:7px}
/* The rail has travelled the whole page. Here it stops being a rail and
   becomes one line. Ported from the other door, where this section is the
   quietest on the page on purpose. */
.oneline{width:0;height:1px;background:var(--gold);margin:0 auto 22px;
  transition:width 1.8s var(--ease)}
#intention.on .oneline{width:min(460px,72%)}
#rm-page.no-anim .oneline{width:min(460px,72%)}

/* ══ 12 · OFFER ══ */
.pricegrid{display:grid;grid-template-columns:minmax(0,.82fr) minmax(0,1.18fr);
  gap:clamp(20px,2.6vw,40px);align-items:stretch;max-width:1000px;
  margin:clamp(28px,4vw,42px) auto 0;text-align:left}
@media(max-width:860px){.pricegrid{grid-template-columns:1fr;max-width:560px}}
.opencard{background:var(--card);border:1px solid var(--border);border-top:2px solid var(--gold);
  padding:26px 28px 28px;text-align:center;display:flex;flex-direction:column;justify-content:center}
.opencard .free{font-family:var(--head);font-size:clamp(34px,5vw,54px);font-weight:700;
  color:var(--gold);line-height:1;margin-bottom:14px}
.opencard .when{font-family:var(--head);font-size:.95rem;font-weight:600;color:var(--cream)}
.opencard .when span{display:block;margin-top:3px;font-weight:500;color:var(--mist)}
.opencard .where{margin-top:10px;font-size:.9rem;color:var(--slate)}
.opencard .lede{margin:16px 0 0;font-size:.95rem}
.stay{background:var(--card);border:1px solid var(--border);padding:26px 28px 28px;text-align:left}
.stay h3{margin-bottom:8px}
.stay ul{list-style:none;margin:22px 0 0;padding:0}
.stay li{position:relative;padding:9px 0 9px 26px;border-top:1px solid var(--border);
  font-size:1rem;color:var(--mist)}
.stay li:last-child{border-bottom:1px solid var(--border)}
.stay li::before{content:"";position:absolute;left:0;top:1.35em;width:11px;height:1px;
  background:var(--gold)}
.pricenote{margin:26px 0 0;font-size:.95rem;color:var(--slate)}
.pricenote b{color:var(--gold);font-weight:600}
.cta-row{margin:30px 0 0;text-align:center}
.cta-row .fine{margin:14px auto 0;font-size:.9rem;color:var(--slate);max-width:44ch}

/* ══ 13 · AFTER · cards respond ══ */
.after-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:26px}
.after-grid > div{border:1px solid var(--line-cream);padding:22px 20px;
  transition:background .3s ease,transform .35s var(--ease)}
.after-grid > div:hover{background:#FFFDF7;transform:translateY(-3px)}
.after-grid h3{font-size:17px;margin-bottom:6px}
.after-grid p{font-size:1rem;margin:0;color:var(--ink-soft)}

/* ══ 14 · FAQ · answers open ══ */
.faq{max-width:820px}
.faq details{border-bottom:1px solid rgba(241,236,225,.13)}
.faq details:first-of-type{border-top:1px solid rgba(241,236,225,.13)}
.faq summary{list-style:none;cursor:pointer;padding:18px 34px 18px 0;position:relative;
  font-family:var(--head);font-size:16px;font-weight:600;color:var(--cream);line-height:1.45;
  transition:color .28s ease}
.faq summary::-webkit-details-marker{display:none}
.faq summary::after{content:"+";position:absolute;right:0;top:16px;width:27px;height:27px;
  border:1px solid rgba(255,80,31,.4);border-radius:50%;color:var(--coral);
  display:flex;align-items:center;justify-content:center;font-size:17px;
  transition:transform .3s var(--ease),background .3s ease,box-shadow .3s ease}
.faq details[open] summary::after{transform:rotate(45deg);background:rgba(255,80,31,.12)}
.faq summary:hover{color:var(--gold-lift)}
.faq summary:hover::after{border-color:var(--coral);box-shadow:0 0 16px rgba(255,80,31,.28)}
.faq summary:focus-visible{outline:2px solid var(--gold);outline-offset:3px}
.faq .ans{padding:0 0 20px;font-size:1rem;line-height:1.8;color:var(--mist);max-width:66ch}
.faq details[open] .ans{animation:faqIn .42s var(--ease)}
@keyframes faqIn{from{opacity:0;transform:translateY(-6px)}to{opacity:1;transform:none}}

/* ══ 15 · CLOSE ══ */
#cta-final{text-align:center}
#cta-final h2{font-size:clamp(26px,4.4vw,52px);margin-bottom:18px}
#cta-final .lede{margin-bottom:30px}

/* ══ REVIEW CHROME ══ */
.centre .note{margin-left:auto;margin-right:auto}
.sec.cream .note,.sec.cream-2 .note{border-left-color:rgba(160,138,94,.6);color:#6E655B}
.sec.cream .note b,.sec.cream-2 .note b{color:#7A6742}
:where(#rm-page) code{font-family:ui-monospace,Menlo,monospace;font-size:.9em;color:var(--gold-lift)}
.tbc{color:var(--coral);border-bottom:1px dotted var(--coral)}

@media(max-width:860px){
  .split,.after-grid{grid-template-columns:1fr}
  .biogrid{flex-direction:column}
}
@media(max-width:760px){
  .steps .row{grid-template-columns:1fr;gap:30px}
  .steps .track{display:none}
}
@media(prefers-reduced-motion:reduce){
  *{animation:none!important;transition:none!important}
}

/* ══ MOBILE ══════════════════════════════════════════════════════════
   The same pass the women's page had: spacing and alignment only, nothing
   here changes a word or a behaviour. Vertical rhythm that reads as air at
   1440 reads as a gap at 390. */
@media(max-width:860px){
  /* Each figure claims three subgrid rows so the names line up across the
     row. In one column that becomes nine rows and eight gaps, and the three
     of them drift apart. */
  .voice{grid-row:auto}
  /* Full phone width and centred, rather than a thumbnail left where the
     desktop layout dropped it. */
  .biogrid{display:block;text-align:center}
  .biogrid .reelwrap,.biogrid .reel{width:100%;max-width:none;height:auto;margin:0 auto 26px}
  .biogrid > :last-child{text-align:left}
}
@media(max-width:640px){
  .steps .row{gap:22px}
  .step h4{margin:10px 0 5px}
  .why-not{margin-top:34px}
  /* The negations centre rather than ragging left. */
  .nots{justify-content:center;text-align:center;gap:10px 16px}
  /* One column, and padding the row can carry at this width. */
  .lifts li{grid-template-columns:1fr;gap:8px;padding:14px 18px 15px}
  .rcs{justify-content:center;text-align:center;margin-top:24px}
  .after-grid > div{padding:15px 18px 16px}
  .cta-row .fine{margin-top:24px}
}

/* ══ 06 · DISTINCTION · THE CLEARING ═══════════════════════════════════
   The section is subtraction: five things this is not, and a standing line
   that says nobody is being asked to be more of anything. So the picture is
   removal, not addition. The pane starts filled with the page's own hatch
   and each crossing clears a band of it. What is left when the five are gone
   is the frame that was under it the whole time.

   Hovering a line you have already crossed puts its band back, so you can
   see what that one took out. That is the only interaction, and it is the
   thing that makes the pane legible rather than decorative. */
.clr{
  display:grid;grid-template-columns:minmax(0,.95fr) minmax(0,1.05fr);
  gap:clamp(26px,4vw,64px);align-items:start;margin:22px 0 0;
}
@media(max-width:820px){ .clr{grid-template-columns:1fr;gap:26px} }
.clr-l .nots{flex-direction:column;align-items:flex-start;gap:9px;margin:0}
.clr-l .nots li{font-size:clamp(16px,1.5vw,21px)}
.clr-l .stand{margin-top:26px;opacity:.42;transition:opacity 1.1s var(--ease)}
.clr.done .clr-l .stand{opacity:1}
#rm-page.no-anim .clr-l .stand{opacity:1!important}
/* In the right column the three answers stack: label over answer, so each is a
   block rather than a wide row. */
.clr-r .not3{margin:0;border-top:none}
.clr-r .not3 .r{grid-template-columns:1fr;gap:5px;padding:0 0 16px}
.clr-r .not3 .r:first-child{padding-top:0}
.clr-r .not3 .r:last-child{border-bottom:none;padding-bottom:0}
.clr-r .not3 .r:hover{background:transparent;padding-left:0}

/* ══ 04 · RECOGNITION · two columns ════════════════════════════════════
   Six lines at display size down the full width was a paragraph's worth of
   words taking a screen and a half. Three and three, at a size you read
   rather than announce. */
#recognition .load{
  display:grid;grid-template-columns:repeat(2,minmax(0,1fr));
  grid-template-rows:repeat(3,auto);grid-auto-flow:column;
  column-gap:clamp(26px,3.6vw,60px);max-width:none;
}
#recognition .load li{
  font-size:clamp(15px,1.15vw,17.5px);line-height:1.5;padding:12px 0 12px 24px;
}
#recognition .load li::before{top:21px}
@media(max-width:760px){
  #recognition .load{grid-template-columns:1fr;grid-template-rows:none;grid-auto-flow:row}
}
#recogCloser{margin-top:26px}
</style>

<div id="rail" aria-hidden="true"><span class="bar"></span><span class="pos" id="railPos"></span></div>



<!-- ══ 01 · HERO ══════════════════════════════════════════════════ -->
<section class="sec ink" id="hero">
  <div class="plate" aria-hidden="true"><div class="shot"></div></div>
  <div class="scrim" aria-hidden="true"></div>
  <div class="hatch" aria-hidden="true"></div>
  <div class="bloom" aria-hidden="true"></div>
  <div class="hin">
    <h1>
      <span class="mask"><span>The</span></span>
      <span class="mask"><span class="key">Reconnected</span></span>
      <span class="mask"><span>Man</span></span>
    </h1>
    <p class="sub" data-r="mid">
      <b>For men who are needed, but want more from life.</b>
    </p>
    <a class="btn" href="#offer" data-r="mid">Register for the next open session</a>
    <p class="next" data-r="far">Weekly Tuesday &nbsp;·&nbsp; 4:30 Bali &nbsp;·&nbsp; 10:30 CET</p>
  </div>
  <span class="cue" aria-hidden="true">&#8595;</span>
</section>

<!-- ══ 02 · PROMISE ═══════════════════════════════════════════════ -->
<section class="sec black" id="promise">
  <div class="in">
    <div class="promise-rule" id="promiseRule" aria-hidden="true"></div>
    <p class="promise-line mask"><span>Decide what you want. Ask for it. And see what happens.</span></p>
    <p class="promise-sub" data-r="far">More of you, not a different you.</p>

  </div>
</section>

<!-- ══ 03 · VSL ═══════════════════════════════════════════════════ -->
<section class="sec dark" id="vsl">
  <div class="in narrow centre">
    <p class="ey" data-r="far">Watch this first</p>
    <div class="divider" data-r="far"></div>
    <p class="lede" data-r="mid">Reconnect with brothers who hold you to a higher standard.</p>
    <div class="vidwrap" data-r="near"><video preload="none" playsinline poster="/assets/img/rm-vsl-poster-v2.jpg" src="https://video.wixstatic.com/video/111174_7af3a35d66374744965fbdaa2f9a3a9b/1080p/mp4/file.mp4"></video><button class="vplay" type="button" aria-label="Play video"><span class="ring" aria-hidden="true"></span></button><span class="len">2:38</span></div>
    <p class="vquote" data-r="mid">“The results you're getting today are not a reflection of who you are today. They're a reflection of who you've been.”</p>


  </div>
</section>

<!-- ══ 04 · RECOGNITION ═══════════════════════════════════════════ -->
<section class="sec ink" id="recognition">
  <div class="glow" aria-hidden="true"></div>
  <div class="in narrow">
    <p class="ey" data-r="far">Sounds like you?</p>
    <ul class="load" id="loadList">
      <li>Rehearsing who you need to be before you walk through the door.</li>
      <li>Satisfied. Comfortable. But can't remember the last time you felt anything.</li>
      <li>Turning what you wanted into a project, because a project felt safer.</li>
      <li>Fixing everything, even what wasn't yours to fix.</li>
      <li>Waited three years to get here. But now what.</li>
      <li>Forever learning, and starting is still waiting.</li>
    </ul>
    <p id="recogCloser">You already know.<span>You've just got very good at talking over it.</span></p>

  </div>
</section>

<!-- ══ 05 · TRAP ══════════════════════════════════════════════════ -->
<section class="sec dark" id="trap">
  <div class="in narrow" id="trapRig">
    <h2 data-r="mid">It never looks like avoidance <span class="hit">from the inside</span>.</h2>
    <p class="lede" data-r="mid">Every man decided the thing he wanted could wait. None of them called it that.</p>
    <ul class="costumes" data-r="mid">
      <li>Consideration</li><li>Realism</li><li>Responsibility</li>
      <li>Standards</li><li>Learning</li><li>Planning</li>
    </ul>
    <p class="trap-under">&ldquo;It's a caretaking strategy. And it's protecting you from asking the hard thing.&rdquo;</p>

  </div>
</section>

<!-- ══ 06 · HOW IT WORKS ══════════════════════════════════════════ -->
<section class="sec cream" id="how">
  <div class="in">
    <p class="ey" data-r="far">How It Works</p>
    <div class="steps" id="steps">
      <div class="row">
        <div class="track" aria-hidden="true"><span class="runner"></span></div>
        <div class="step" data-r="mid"><span class="n">01</span>
          <h3>Decide What You Want</h3>
          <p>The personal unmet values and needs.</p></div>
        <div class="step" data-r="mid"><span class="n">02</span>
          <h3>Asking For It</h3>
          <p>No hedge, and no half question.</p></div>
        <div class="step" data-r="mid"><span class="n">03</span>
          <h3>The Answer In Reality</h3>
          <p>Not your assumption of it.</p></div>
      </div>
    </div>
    <p class="why-not" data-r="mid">A simple ask you already know you should make.<span>So why haven't you?</span></p>
    <div class="rcs" data-r="far">
      <span class="k">The Relational Connection System&trade;</span>
      <span class="v">Vision</span><span class="v">Values</span><span class="v">Velocity</span>
    </div>

  </div>
</section>

<!-- ══ 07 · WHAT THIS ISN'T ═══════════════════════════════════════ -->
<section class="sec black" id="distinction">
  <div class="glow" aria-hidden="true"></div>
  <div class="in narrow">
    <h2 data-r="mid">What this isn't.</h2>
    <div class="clr">
      <div class="clr-l">
        <ul class="nots" id="notsList" data-r="mid">
          <li>No drumming</li><li>No ice baths</li><li>No breathwork</li>
          <li>No alpha</li><li>No guru</li>
        </ul>
        <p class="stand" data-r="mid">We're not asking you to become a different man.</p>
      </div>
      <div class="clr-r" data-r="mid">
        <ul class="not3" data-r="mid">
      <li class="r"><h3>Not a mastermind</h3><p>Unlike business strategy, we're not going to mastermind your wants.</p></li>
      <li class="r"><h3>Not therapy</h3><p>More than just talk, we're going for it together.</p></li>
      <li class="r"><h3>Not a lads' chat</h3><p>The conversations you don't get to have in your day-to-day.</p></li>
    </ul>
      </div>
    </div>

  </div>
</section>

<!-- ══ 08 · THE DIFFERENCE ════════════════════════════════════════ -->
<section class="sec dark" id="difference">
  <div class="in narrow">
    <div class="split">
      <div>
        <p class="ey" data-r="far">The difference</p>
        <p class="question">You can witness your truth, sit with it, and choose to simply feel it for what it is.</p>
        <p class="release-note">Be seen, heard and witnessed in your truth, amongst brothers you won't find among friends.</p>
      </div>
      <ul class="lifts" data-r="mid">
        <li><span class="k">01</span><div><h3>Seen</h3><p>Not the version you take to work.</p></div></li>
        <li><span class="k">02</span><div><h3>Heard</h3><p>All the way to the end of the sentence.</p></div></li>
        <li><span class="k">03</span><div><h3>Witnessed</h3><p>Somebody else knows now. That is what makes it real.</p></div></li>
        <li><span class="k">04</span><div><h3>A higher standard</h3><p>Brothers who expect more of you than your friends do.</p></div></li>
      </ul>
    </div>

  </div>
</section>

<!-- ══ 09 · DANIEL ════════════════════════════════════════════════ -->
<section class="sec cream-2" id="bio">
  <div class="in narrow">
    <div class="biogrid">
      <div class="reelwrap" data-r="near"><video preload="none" playsinline poster="/assets/img/rm-bio-poster.jpg" src="https://video.wixstatic.com/video/111174_4e1bcc7d4c634401adae988048a9cb1b/1080p/mp4/file.mp4"></video><button class="vplay" type="button" aria-label="Play video"><span class="ring" aria-hidden="true"></span></button><span class="len">0:56</span></div>
      <div class="txt" data-r="mid">
        <h2 style="font-size:clamp(24px,3vw,36px)">Daniel Lawson</h2>
        <p class="biorole">I stopped asking for what I wanted, and called it something more respectable.</p>
        <p>A coach asked me what my relationship values were. I said trust, honesty, respect, loyalty. He told me those were baselines, that if you don't have them you don't have a relationship, and asked what was truly important to me. I went blank.</p>
        <p>I sat in a café and didn't move until I had something. What came out wasn't a list. It was a different question: how do I want to feel? That opened the whole framework.</p>
        <p><strong>I had walls, that I had been calling standards.</strong></p>
        <div class="tags"><span>ICG Accredited</span><span>6 Years Coaching</span><span>Global Facilitator</span></div>
      </div>
    </div>

  </div>
</section>

<!-- ══ 10 · VOICES ════════════════════════════════════════════════ -->
<section class="sec black" id="voices">
  <div class="in">
    <h2 data-r="near">From men inside</h2>
    <div class="voices" data-r="mid">
      <figure class="voice">
        <img src="/assets/img/rm-voice-jordi.jpg" alt="Jordi" width="304" height="304" decoding="async">
        <blockquote>&ldquo;I dropped something that had been working for years. Being certain about a decision, whatever comes of it. That's what I'm proud of.&rdquo;</blockquote>
        <p class="who">Jordi</p>
      </figure>
      <figure class="voice">
        <img src="/assets/img/rm-voice-harrison.jpg" alt="Harrison" width="304" height="304" decoding="async">
        <blockquote>&ldquo;There was so much resistance to doing it. I did it anyway, and it was really nice. Then I realised I'd treated myself as well. I can't remember the last time I did that.&rdquo;</blockquote>
        <p class="who">Harrison</p>
      </figure>
      <figure class="voice">
        <img src="/assets/img/rm-voice-alex.jpg" alt="Alex" width="304" height="304" decoding="async">
        <blockquote>&ldquo;I realised I can do a lot of this as a relationship with myself. Understood, supported, appreciated. I want that with someone. I want it with myself first.&rdquo;</blockquote>
        <p class="who">Alex</p>
      </figure>
    </div>
    <div class="reels" data-r="mid">
      <figure class="reel"><div class="rwrap"><video preload="none" playsinline poster="/assets/img/rm-voice-1-poster.jpg" src="https://video.wixstatic.com/video/111174_59a18a858df34f798fbeb5ae8d5b19b8/1080p/mp4/file.mp4"></video><button class="vplay" type="button" aria-label="Play video"><span class="ring" aria-hidden="true"></span></button><span class="len">1:19</span></div></figure>
      <figure class="reel"><div class="rwrap"><video preload="none" playsinline poster="/assets/img/rm-voice-2-poster.jpg" src="https://video.wixstatic.com/video/111174_a6961c80d0464fbe8437a38fa69071da/720p/mp4/file.mp4"></video><button class="vplay" type="button" aria-label="Play video"><span class="ring" aria-hidden="true"></span></button><span class="len">1:07</span></div></figure>
    </div>

  </div>
</section>

<!-- ══ 11 · THE STANDARD ══════════════════════════════════════════ -->
<section class="sec dark" id="values">
  <div class="glow" aria-hidden="true"></div>
  <div class="in narrow">
    <div class="vals-split">
      <div class="vals-rail" data-r="mid">
        <p class="hand">what we stand for</p>
        <h2>What we value.</h2>
        <p class="lede">We all have skin in the game and play full out. We require the same from you upon entry.</p>
        <p class="hand sm">we become your brotherhood</p>
      </div>
      <div class="vals-spine" data-r="mid">
        <div class="v"><span class="node" aria-hidden="true"></span><h3>Dedication</h3>
          <p>We all commit to the process by showing up like this work matters, because it does.</p></div>
        <div class="v"><span class="node" aria-hidden="true"></span><h3>Punctuality</h3>
          <p>As a sign of respect to ourselves and each other. Time is a sacred resource here.</p></div>
        <div class="v"><span class="node" aria-hidden="true"></span><h3>Raw Vulnerability</h3>
          <p>Being the best with imperfection. Real, unfiltered, and fully present.</p></div>
      </div>
    </div>

  </div>
</section>

<!-- ══ 11 · ONE INTENTION ═════════════════════════════════════════ -->
<section class="sec ink" id="intention">
  <div class="in narrow centre">
    <div class="oneline" aria-hidden="true"></div>
    <p class="ey" data-r="far">One intention</p>
    <p class="promise-line" data-r="mid">Your ask. Either response is a win.</p>
    <p class="promise-sub" data-r="far">Yes is what you want. No is truth and clarity.</p>

  </div>
</section>

<!-- ══ 12 · THE OFFER ═════════════════════════════════════════════ -->
<section class="sec dark" id="offer">
  <div class="in narrow centre">
    <p class="ey" data-r="far">The offer</p>
    <h2 data-r="mid">The open session.</h2>
    <p class="lede" data-r="mid">Come, sit in one, and decide from the session rather than from this page.</p>
    <div class="pricegrid" data-r="mid">
      <div class="opencard">
        <div class="free">Free</div>
        <div class="when">Apply for the next open day<span>Weekly Tuesday &middot; 4:30 Bali &middot; 10:30 CET</span></div>
        <div class="where">Online, wherever you are</div>
        <p class="lede">Come, sit in it, and decide from the session rather than from this page.</p>
      </div>
      <div class="stay">
        <h3>Stay, and here's the four weeks.</h3>
        <p class="dim">Decide what you want. Ask for it. And see what happens.</p>
        <ul>
          <li>One live session a week, in four-week cycles</li>
          <li>Live facilitation and hot seat work</li>
          <li>A defined theme each month, with the prompt a few days ahead</li>
          <li>Access to the group between sessions</li>
          <li>First access to new frameworks and resources</li>
        </ul>
        <p class="pricenote"><b>&euro;89 a month</b>, and your membership price is grandfathered. It never increases as long as you stay. Memberships roll over monthly. Cancel anytime.</p>
      </div>
    </div>
    <div class="cta-row" data-r="mid">
      <button class="btn" type="button" data-trm-modal>Register For The Open Session</button>
      <p class="fine">The short application first is to ensure a strong mutual fit, for you and for the group.</p>
    </div>

  </div>
</section>

<!-- ══ 13 · AFTER FOUR WEEKS ══════════════════════════════════════ -->
<section class="sec cream-2" id="after">
  <div class="in narrow">
    <p class="ey" data-r="far">After four weeks</p>
    <h2 data-r="mid">You'll have made the ask.</h2>
    <div class="after-grid" data-r="mid">
      <div><h3>Continue to build on it</h3><p>More reps, with the same ask or a different one.</p></div>
      <div><h3>Continue more personally</h3><p>Credit your membership toward 1:1 work in Reconnect.</p></div>
      <div><h3>Continue on your own</h3><p>Take what you've got, and go use it.</p></div>
    </div>

  </div>
</section>

<!-- ══ 14 · FAQ ═══════════════════════════════════════════════════ -->
<section class="sec black" id="faq">
  <div class="in narrow">
    <p class="ey" data-r="far">Common questions</p>
    <h2 data-r="mid">Before you come.</h2>
    <div class="faq" data-r="mid">
      <details><summary>Do I need to be in a relationship for this?</summary>
        <div class="ans">No. The men in here are single, married, separated, and everything in between. What they have in common is the pattern, not the postcode. One man rehearses who he needs to be before he opens the front door, another sits in the car outside a bit longer. Same pattern.</div></details>
      <details><summary>I already talk to my mates. Why would I pay for this?</summary>
        <div class="ans">Because of what gets said. Your mates get the version of you that's fine. In here the conversation goes where it doesn't go over a pint, and there's someone holding it so it lands somewhere rather than trailing off.</div></details>
      <details><summary>Isn't this just another men's group?</summary>
        <div class="ans">Most of them run on more force. More discipline, more commitment, more accountability. Force is usually what's already failing. We work the other way: find what the habit is paying you, and go around it.</div></details>
      <details><summary>Can I come once before I decide?</summary>
        <div class="ans">Yes. Open days run through the year and there's no charge to sit in one. Apply for the next one, come, and decide from the session rather than from this page.</div></details>
      <details><summary>Do I have to talk?</summary>
        <div class="ans">No. Plenty of men get their money's worth watching someone else's hot seat, because the thing being opened up is usually theirs too.</div></details>
      <details><summary>Are there upsells coming?</summary>
        <div class="ans">No. There's 1:1 work in Reconnect if you decide you want it deeper, and your membership comes off the cost. Same work, same direction. More personal.</div></details>
    </div>

  </div>
</section>

<!-- ══ 15 · CLOSE ═════════════════════════════════════════════════ -->
<section class="sec ink" id="cta-final">
  <div class="glow" aria-hidden="true"></div>
  <div class="in narrow centre">
    <p class="ey" data-r="far">Ready?</p>
    <h2 class="mask" id="closeMask"><span>Know what you want, and ask for it in four weeks.</span></h2>
    <p class="lede" data-r="mid">Apply for the next open day. Free to sit in, short application form first.</p>
    <a class="btn" href="#offer" data-r="mid">Register for the open session</a>
    <p class="hand sm" data-r="far" style="margin-top:32px">you're joining a living conversation</p>

  </div>
</section>
<parallaxx-footer></parallaxx-footer>

<div id="rm-apply-modal" role="dialog" aria-modal="true" aria-label="Application Form">
  <div class="modal-backdrop"></div>
  <div class="modal-box">
    <div class="modal-header">
      <span class="modal-title">Registration</span>
      <button class="modal-close" aria-label="Close form">&times;</button>
    </div>
    <div class="modal-body">
      <!-- THE ID APPEARS THREE TIMES AND ALL THREE MATTER. src is what loads.
           id="form-<ID>" and data-form-id are what GHL's form_embed.js matches
           on when it posts a height back, so a swap that misses either one
           loads the right form at the wrong height and never resizes it. -->
      <iframe
        src="https://api.leadconnectorhq.com/widget/form/UxzzQRv0AbKKQZ0P9bau"
        height="700"
        title="The Reconnected Man Application Form"
        id="form-UxzzQRv0AbKKQZ0P9bau"
        data-form-id="UxzzQRv0AbKKQZ0P9bau"
        data-form-name="The Reconnected Man Application Form"
      ></iframe>
    </div>
  </div>
</div>

</div>`;

  function once(id, make) {
    if (document.getElementById(id)) return;
    var el = make(); el.id = id; document.head.appendChild(el);
  }

  function addHead() {
    once('rm-pre1', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.googleapis.com'; return l; });
    once('rm-pre2', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.gstatic.com'; l.crossOrigin = ''; return l; });
    once('rm-font0', function () { var l = document.createElement('link'); l.rel = 'stylesheet'; l.href = 'https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Poppins:wght@400;500;600;700;800&family=Permanent+Marker&display=swap'; return l; });
    once('rm-font1', function () { var l = document.createElement('link'); l.rel = 'stylesheet'; l.href = 'https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&family=Montserrat:wght@400;500;600;700&family=Permanent+Marker&display=swap'; return l; });
    once('rm-css', function () { var st = document.createElement('style'); st.textContent = CSS; return st; });
    if (!document.querySelector('meta[name="viewport"]')) {
      var mv = document.createElement('meta'); mv.name = 'viewport';
      mv.content = 'width=device-width, initial-scale=1'; document.head.appendChild(mv);
    }
  }

  function script(src, id) {
    if (document.getElementById(id)) return;
    var sc = document.createElement('script'); sc.id = id; sc.src = src; sc.async = false;
    document.head.appendChild(sc);
  }

  function boot() {

(function(){
  var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var noIO   = !('IntersectionObserver' in window);
  if(reduce || noIO){ (document.getElementById('rm-page')||document.body).classList.add('no-anim'); }

  function obs(cb, opts){
    if(reduce || noIO) return null;
    return new IntersectionObserver(function(es){
      es.forEach(function(e){ if(e.isIntersecting) cb(e.target); });
    }, opts || { rootMargin:'0px 0px -12% 0px' });
  }

  /* ── depth-graded arrival, one-shot, with the 4s failsafe ───── */
  var reveals = [].slice.call(document.querySelectorAll('[data-r]'));
  if(reduce || noIO){
    reveals.forEach(function(el){ el.classList.add('seen'); });
  } else {
    var ro = obs(function(el){ el.classList.add('seen'); ro.unobserve(el); });
    reveals.forEach(function(el){ ro.observe(el); });
    setTimeout(function(){ reveals.forEach(function(el){ el.classList.add('seen'); }); }, 4000);
  }

  /* ── hero masked headline, on load ─────────────────────────── */
  [].slice.call(document.querySelectorAll('#hero .mask')).forEach(function(m,i){
    setTimeout(function(){ m.classList.add('seen'); }, reduce ? 0 : 120 + i * 130);
  });

  function once(sel, cls, margin, delay){
    var el = typeof sel === 'string' ? document.querySelector(sel) : sel;
    if(!el) return;
    if(reduce || noIO){ el.classList.add(cls); return; }
    var io = obs(function(t){
      if(delay){ setTimeout(function(){ t.classList.add(cls); }, delay); }
      else { t.classList.add(cls); }
      io.unobserve(t);
    }, { rootMargin: margin || '0px 0px -18% 0px' });
    io.observe(el);
  }

  once('#promiseRule', 'on');
  once('#promise .mask', 'seen', null, 260);
  once('#closeMask', 'seen');
  once('#trapRig', 'shifted', '0px 0px -20% 0px', 2100);

  /* ── RECOGNITION · the staged load ─────────────────────────── */
  var rows   = [].slice.call(document.querySelectorAll('#loadList li'));
  var closer = document.getElementById('recogCloser');
  if(reduce || noIO){
    rows.forEach(function(r){ r.classList.add('on'); });
    if(closer) closer.classList.add('on');
  } else {
    var ioR = obs(function(){
      rows.forEach(function(r,i){ setTimeout(function(){ r.classList.add('on'); }, 120 + i*420); });
      setTimeout(function(){ if(closer) closer.classList.add('on'); }, 120 + rows.length*420 + 620);
      ioR.unobserve(document.getElementById('recognition'));
    }, { rootMargin:'0px 0px -22% 0px' });
    ioR.observe(document.getElementById('recognition'));
    setTimeout(function(){
      rows.forEach(function(r){ r.classList.add('on'); });
      if(closer) closer.classList.add('on');
    }, 6000);
  }

  /* ── HOW · the runner travels, numerals light as it passes ── */
  (function runner(){
    var steps = document.getElementById('steps');
    if(!steps) return;
    var lits = [].slice.call(steps.querySelectorAll('.step'));
    if(reduce || noIO){ lits.forEach(function(s){ s.classList.add('lit'); }); return; }
    var io = obs(function(el){
      el.classList.add('on');
      lits.forEach(function(s,i){ setTimeout(function(){ s.classList.add('lit'); }, 420 + i*1100); });
      io.unobserve(el);
    }, { rootMargin:'0px 0px -20% 0px' });
    io.observe(steps);
  })();

  /* ── DISTINCTION · the struck items recede ─────────────────── */
  (function crossings(){
    var list = document.getElementById('notsList');
    var row  = document.querySelector('.clr');
    if(!list) return;
    var items = [].slice.call(list.querySelectorAll('li'));
    var reduce = matchMedia('(prefers-reduced-motion:reduce)').matches;
    var noIO   = !('IntersectionObserver' in window);
    function finish(){ if(row) row.classList.add('done'); }
    if(reduce || noIO){
      items.forEach(function(i){ i.classList.add('struck'); });
      finish(); return;
    }
    var io = new IntersectionObserver(function(es){
      es.forEach(function(e){
        if(!e.isIntersecting) return;
        items.forEach(function(it,i){
          setTimeout(function(){ it.classList.add('struck'); }, 220 + i*430);
        });
        setTimeout(finish, 220 + items.length*430 + 400);
        io.unobserve(e.target);
      });
    }, { rootMargin:'0px 0px -20% 0px' });
    io.observe(list);
    setTimeout(function(){
      items.forEach(function(i){ i.classList.add('struck'); }); finish();
    }, 4000);
  })();

  /* ── DIFFERENCE · the release is announced ─────────────────── */
  (function release(){
    var sec  = document.getElementById('difference');
    var note = sec ? sec.querySelector('.release-note') : null;
    if(!sec) return;
    if(reduce || noIO){ if(note) note.classList.add('on'); return; }
    var io = obs(function(el){
      el.classList.add('released');
      setTimeout(function(){ if(note) note.classList.add('on'); }, 1500);
      io.unobserve(el);
    }, { rootMargin:'0px 0px -30% 0px' });
    io.observe(sec);
  })();
})();

;

/* RAIL · the position marker, ported from the women's build. */
(function () {
  var railPos = document.getElementById('railPos');
  if (!railPos) return;
  function movePos(){
    var h = document.documentElement.scrollHeight - window.innerHeight;
    var p = h > 0 ? Math.min(Math.max(window.scrollY / h, 0), 1) : 0;
    railPos.style.top = (p * (window.innerHeight - 46)).toFixed(1) + 'px';
  }
  window.addEventListener('scroll', movePos, { passive: true });
  window.addEventListener('resize', movePos);
  movePos();
})();

/* ── One Intention: the rail resolves into a single line ───────────── */
(function oneLine(){
  var sec = document.getElementById('intention');
  if(!sec) return;
  var reduce = matchMedia('(prefers-reduced-motion:reduce)').matches;
  if(reduce || !('IntersectionObserver' in window)){ sec.classList.add('on'); return; }
  var io = new IntersectionObserver(function(es){
    es.forEach(function(e){ if(e.isIntersecting){ sec.classList.add('on'); io.disconnect(); } });
  }, { threshold:0.35 });
  io.observe(sec);
  setTimeout(function(){ sec.classList.add('on'); }, 4000);
})();

/* ── Video: one poster-first handler for all four ──────────────────────── */
(function video(){
  var vids = [].slice.call(document.querySelectorAll('.vplay'));
  vids.forEach(function(btn){
    var wrap = btn.parentNode, v = wrap.querySelector('video');
    if(!v) return;
    btn.addEventListener('click', function(){
      document.querySelectorAll('video').forEach(function(o){
        if(o !== v && !o.paused){ o.pause(); o.parentNode.classList.remove('playing'); }
      });
      wrap.classList.add('playing');
      v.controls = true;
      var pr = v.play();
      if(pr && pr.catch) pr.catch(function(){ /* poster stays, nothing to do */ });
    });
    v.addEventListener('ended', function(){ wrap.classList.remove('playing'); v.controls = false; });
  });
})();

(function applyModal(){
  var modal = document.getElementById('rm-apply-modal');
  if(!modal) return;
  var mBack = modal.querySelector('.modal-backdrop');
  var mClose = modal.querySelector('.modal-close');
  function openModal(){
    modal.classList.add('is-open'); document.body.style.overflow='hidden'; mClose.focus();
  }
  function closeModal(){
    modal.classList.remove('is-open'); document.body.style.overflow='';
  }
  [].slice.call(document.querySelectorAll('[data-trm-modal]'))
    .forEach(function(el){ el.addEventListener('click', openModal); });
  mBack.addEventListener('click', closeModal);
  mClose.addEventListener('click', closeModal);
  document.addEventListener('keydown', function(e){
    if(e.key === 'Escape' && modal.classList.contains('is-open')) closeModal();
  });
})();

  }

  var El = function () { return Reflect.construct(HTMLElement, [], El); };
  El.prototype = Object.create(HTMLElement.prototype);
  El.prototype.constructor = El;
  Object.setPrototypeOf(El, HTMLElement);

  El.prototype.connectedCallback = function () {
    if (this._rmUp) return;
    this._rmUp = true;
    addHead();
    this.innerHTML = HTML;
    script('/parallaxx-nav.js', 'rm-nav-js');
    script('/parallaxx-footer.js', 'rm-foot-js');
    script('https://link.msgsndr.com/js/form_embed.js', 'rm-ghl-js');
    try { boot(); } catch (e) { /* the page renders fully visible without it */ }
  };

  customElements.define('parallaxx-reconnected-man', El);
})();
