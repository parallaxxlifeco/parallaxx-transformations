/* PARALLAXX TRANSFORMATIONS - The Reconnected Woman. Tag: parallaxx-reconnected-woman.
   GENERATED by build_bundle_v10.py from reconnected-woman-v10-preview.html,
   which is the approved design. DO NOT EDIT THIS FILE, edit that one and rerun.

   Renders into the LIGHT DOM on purpose. The stylesheet is scoped to #rw-page
   and the behaviour script uses document.querySelector, so light DOM keeps the
   accordion, the modal, the videos and the reveals working as approved. */
(function () {
  if (customElements.get('parallaxx-reconnected-woman')) return;

  var CSS = `

:root{
  --navy:#04122A; --black:#061938; --ink-navy:#030C1C; --card:#0A1D3C;
  --border:#1B2C46;
  --cream:#F1ECE1; --cream-2:#F7F3EA; --line-cream:#DED6C4;
  --coral:#FF501F; --coral-lift:#FF6A3D;
  --gold:#E8C65F; --gold-lift:#F3DD93; --gold-dark:#C9A227;
  --bronze:#A08A5E;
  --mist:#B1BFD7; --slate:#7C89A3;
  --ink:#1F2A3D; --ink-soft:#5A6274;
  --head:'Poppins',system-ui,-apple-system,sans-serif;
  --body:'Montserrat',system-ui,-apple-system,sans-serif;
  /* The site wrapper, taken from .px-wrap / .sp-wrap / .pa-wrap on the
     other pages: 1240 with a clamp(20px,4vw,52px) gutter. This page was
     running a 1160 column inside a 92px gutter, which is what made it
     read as a single strip down the middle. */
  --gut:clamp(20px,4vw,52px);
  --col:1240px;
  --rail-w:1px; --rail-col:var(--slate); --rail-op:.3;
  --ease:cubic-bezier(.16,1,.3,1);
}
#rw-page *{box-sizing:border-box}
#rw-page{
  margin:0; background:var(--ink-navy); color:var(--mist);
  font-family:var(--body); font-size:16px; line-height:1.68;
  -webkit-font-smoothing:antialiased; overflow-x:hidden;
}

/* ═══ THE RAIL — site-wide sightline, carrying this page's load ═══ */
/* ── THE SIGHTLINE ─────────────────────────────────────────────
   A fixed reference at the edge of every page, and the marker that
   shows where the reader is standing against it. It states nothing
   else: the load / stuck / release states it used to carry were
   designer's logic, unreadable to anyone who had not been told. */
#rail{
  position:fixed; top:0; bottom:0; left:calc(var(--gut) / 2 - 1px);
  width:9px; z-index:60; pointer-events:none;
  display:flex; justify-content:center;
}
#rail .bar{width:1px; height:100%; background:var(--slate); opacity:.22}
@media(max-width:820px){ #rail{left:7px} }

/* ═══ SECTION SHELLS ═══ */
.sec{padding:clamp(52px,6.5vw,92px) var(--gut); position:relative}
.sec.ink{background:var(--ink-navy)}
.sec.dark{background:var(--navy)}
.sec.black{background:var(--black)}
.sec.cream{background:var(--cream); color:var(--ink)}
.sec.cream-2{background:var(--cream-2); color:var(--ink)}
.in{max-width:var(--col); margin:0 auto}
.narrow{max-width:1040px}

.ey{
  font-family:var(--head); font-size:.62rem; font-weight:800;
  letter-spacing:.24em; text-transform:uppercase;
  color:var(--gold); margin:0 0 13px; max-width:none;
}
.sec.cream .ey,.sec.cream-2 .ey{color:var(--bronze)}

:where(#rw-page) h1,:where(#rw-page) h2,:where(#rw-page) h3,:where(#rw-page) h4{font-family:var(--head); margin:0; text-wrap:balance}
:where(#rw-page) h2{
  font-size:clamp(27px,3.4vw,42px); font-weight:700; line-height:1.1;
  letter-spacing:-.02em; color:var(--cream); margin-bottom:16px;
}
:where(#rw-page) h2 .hit{color:var(--gold-lift)}
.sec.cream h2,.sec.cream-2 h2{color:var(--ink)}
:where(#rw-page) h3{font-size:clamp(19px,2vw,25px); font-weight:600; line-height:1.28; color:var(--cream)}
.sec.cream h3,.sec.cream-2 h3{color:var(--ink)}
:where(#rw-page) p{margin:0 0 14px; max-width:64ch}
.lede{font-size:clamp(16px,1.55vw,19px); line-height:1.55; color:var(--mist)}
.sec.cream .lede,.sec.cream-2 .lede{color:var(--ink-soft)}
.dim{color:var(--slate)}
.sec.cream .dim,.sec.cream-2 .dim{color:var(--ink-soft)}
:where(#rw-page) em{font-style:italic}

.divider{width:46px; height:1px; background:var(--gold); margin:0 0 18px}
.sec.cream .divider,.sec.cream-2 .divider{background:var(--bronze)}

.btn{
  display:inline-block; font-family:var(--head); font-size:.76rem; font-weight:700;
  letter-spacing:.13em; text-transform:uppercase; text-decoration:none;
  background:var(--coral); color:#fff; padding:16px 30px; border-radius:2px;
  transition:background .22s ease, transform .22s var(--ease);
}
.btn:hover{background:var(--coral-lift); transform:translateY(-2px)}
.btn:focus-visible{outline:2px solid var(--gold); outline-offset:3px}
.btn.ghost{
  background:transparent; color:var(--gold); border:1px solid rgba(232,198,95,.45);
}
.btn.ghost:hover{background:rgba(232,198,95,.1)}

/* ═══ IMAGE PLACEHOLDERS ═══ */
.ph{
  position:relative; display:flex; flex-direction:column;
  align-items:center; justify-content:center; text-align:center; gap:10px;
  padding:28px; border:2px dashed var(--coral); border-radius:16px;
  background:repeating-linear-gradient(45deg,
    rgba(255,80,31,.05) 0 12px, rgba(255,80,31,.10) 12px 24px);
  min-height:200px;
}
.ph b{
  font-family:var(--head); font-size:.64rem; font-weight:800;
  letter-spacing:.18em; text-transform:uppercase; color:var(--coral-lift);
}
.ph span{font-size:.85rem; line-height:1.6; max-width:40ch; color:#E6BCAC}
.ph i{
  font-style:normal; font-family:var(--head); font-size:.6rem; font-weight:700;
  letter-spacing:.13em; text-transform:uppercase; color:#B08574;
}
.sec.cream .ph span,.sec.cream-2 .ph span{color:#8A6F62}
.sec.cream .ph i,.sec.cream-2 .ph i{color:#A0806F}

/* ═══ REVEAL SYSTEM — depth-graded, one-shot ═══ */
[data-r]{
  opacity:0; transform:translateY(var(--dy,12px));
  transition:opacity .85s ease, transform .85s var(--ease);
}
[data-r="far"]{--dy:4px}
[data-r="mid"]{--dy:12px}
[data-r="near"]{--dy:28px}
[data-r].seen{opacity:1; transform:none}
#rw-page.no-anim [data-r]{opacity:1!important; transform:none!important}

/* ═══ 01 · HERO ═══════════════════════════════════════════════
   Full bleed at the size it was. Two fixes inside that: the crop
   starts at the very top of the frame so his head keeps its
   headroom and is never sliced by the viewport, and the plate is
   graded down hard so it reads as the room he is holding rather
   than as a portrait competing with the headline. */
#hero{
  position:relative; min-height:clamp(560px,72svh,780px);
  display:flex; align-items:flex-end; padding:0;
  background:var(--ink-navy); overflow:hidden;
}
#hero .plate{position:absolute; inset:0; will-change:transform}
#hero .shot{
  position:absolute; inset:-2% 0;
  background-image:url('https://static.wixstatic.com/media/RW_HERO_CIRCLE~mv2.jpg');
  /* The crop is trimmed to just above his head and anchored near the top,
     so the gap above stays about 16-22px at every hero height instead of
     growing on tall windows and slicing him on short ones. */
  background-size:cover; background-position:50% 3%;
  /* THE SETTLE. It opens a little tighter and eases out to the frame
     over 2.6s, so the room fills in — the women at the lower edges
     arrive — while she is still reading the headline. Deliberately NOT
     coupled to scroll: a reveal driven by the scrollbar completes as
     the hero leaves the screen, which spends it where nobody is
     looking. The origin sits near his head so he holds still and the
     edges are what open out. */
  transform-origin:50% 32%;
  animation:heroSettle 2.6s cubic-bezier(.16,1,.3,1) .18s both;
}
@keyframes heroSettle{
  from{transform:scale(1.085) translateY(1.9%)}
  to  {transform:scale(1) translateY(0)}
}
@media(prefers-reduced-motion:reduce){ #hero .shot{animation:none} }
/* The scrim is centre-weighted, not bottom-weighted. The copy runs down
   the middle, and the women sit at the left and right edges — a full-width
   bottom band buys legibility by erasing exactly the people the picture is
   there to show. So the middle carries the darkness and the edges stay open. */
#hero .scrim{
  position:absolute; inset:0; z-index:1;
  background:
    radial-gradient(ellipse 52% 84% at 50% 100%, rgba(3,12,28,.97) 0%, rgba(3,12,28,.91) 28%, rgba(3,12,28,.56) 58%, rgba(3,12,28,.18) 82%, rgba(3,12,28,0) 100%),
    linear-gradient(0deg, rgba(3,12,28,.35) 0%, rgba(3,12,28,.30) 34%, rgba(3,12,28,.29) 66%, rgba(3,12,28,.34) 100%);
}
#hero .hin{
  position:relative; z-index:2; text-shadow:0 1px 20px rgba(3,12,28,.7); width:100%; max-width:var(--col);
  margin:0 auto; padding:0 var(--gut) clamp(52px,8vh,96px);
  text-align:center;
}
.mask{overflow:hidden; display:block}
.mask > span{
  display:block; transform:translateY(104%);
  transition:transform 1.05s var(--ease);
}
.mask.seen > span{transform:translateY(0)}
#rw-page.no-anim .mask > span{transform:none!important}
#hero h1{
  font-size:clamp(44px,8.4vw,104px); font-weight:700; line-height:.96;
  letter-spacing:-.035em; color:var(--cream); margin:0 0 26px;
}
#hero h1 .sm{
  display:block; font-size:.34em; font-weight:600; letter-spacing:.02em;
  color:var(--slate); margin-bottom:.3em;
}
/* Gold marks the word the page turns on, not the article in front of it. */
#hero h1 .key{color:var(--gold)}
/* Broken at the sentence, not left to the wrap. Each line is a beat, and
   none of them ends on an orphan. The measure is set so all three hold. */
#hero .sub{
  font-size:clamp(16px,1.7vw,20px); line-height:1.5; color:var(--mist);
  max-width:min(620px,100%); margin-left:auto; margin-right:auto;
}
#hero .sub span,#hero .sub b{display:block; text-wrap:balance}
#hero .sub span{margin-bottom:.5em}
#hero .sub b{color:var(--cream); font-weight:600}
#hero .btn{margin-top:26px}
#hero .next{
  margin-top:20px; font-family:var(--head); font-size:.66rem; font-weight:700;
  letter-spacing:.16em; text-transform:uppercase; color:var(--gold);
}
#hero .cue{
  position:absolute; left:50%; bottom:20px; z-index:3;
  color:var(--gold); opacity:.5; font-size:1.3rem; line-height:1;
  transform:translateX(-50%); animation:cue 2.6s ease-in-out infinite;
}
@keyframes cue{0%,100%{transform:translateX(-50%) translateY(0)}50%{transform:translateX(-50%) translateY(6px)}}
@media(max-width:760px){ #hero .shot{background-position:50% 2%} }

/* ═══ 02 · VSL ═══ */
.vidwrap{position:relative; aspect-ratio:16/9; margin-top:24px; background:#000; overflow:hidden}
.vidwrap video{width:100%; height:100%; object-fit:cover; display:block}
/* BIO — the reel is sized in HEIGHT, to sit near the height of the words, and
   takes its width from that through aspect-ratio. Centred rather than top-
   aligned, so the small residual difference reads as deliberate rather than as
   a column that ran out of text. */
.biogrid{display:flex; align-items:center; justify-content:center;
  gap:clamp(26px,4vw,58px)}
.biogrid > :first-child{flex:0 0 auto}
.biogrid .reelwrap{height:clamp(340px,40vw,500px); width:auto}
/* The bio is six short sentences. Left at full width they run out long before
   the reel does, so the measure is cut to about 56 characters: the text gains
   the lines it needs to stand alongside a bigger video, and the pair is centred
   so neither one is stranded against a gutter. */
.biogrid > :last-child{flex:0 1 500px; min-width:0}
@media(max-width:820px){
  .biogrid{display:block; text-align:center}
  /* Full phone width and centred: at 280px in a 350px column it read as a
     thumbnail that had been left where the desktop layout dropped it. */
  .biogrid .reelwrap{height:auto; width:100%; max-width:none; margin:0 auto 26px}
  .biogrid > :last-child{text-align:left}
}
.reelwrap{position:relative; aspect-ratio:9/16; background:#000; overflow:hidden; border-radius:2px}
.reelwrap video{width:100%; height:100%; object-fit:cover; display:block}
/* The gold play mark, same as the one already on the live page. */
.vplay{
  position:absolute; inset:0; margin:auto; width:clamp(58px,7vw,84px); height:clamp(58px,7vw,84px);
  padding:0; border:0; background:transparent; cursor:pointer; overflow:visible;
  transition:transform .25s var(--ease), opacity .3s ease;
}
.vplay svg{width:100%; height:100%; display:block}
.vplay:hover{transform:scale(1.06)}
.vplay:focus-visible{outline:2px solid var(--gold); outline-offset:4px}
.vplay.gone{opacity:0; pointer-events:none}
.pullquote{
  font-family:var(--head); font-size:clamp(17px,1.9vw,23px); font-style:italic;
  font-weight:500; line-height:1.5; color:var(--gold-lift);
  border-left:2px solid var(--gold); padding-left:22px; max-width:60ch; margin:24px 0 0;
}

/* ═══ 03 · RECOGNITION — the accumulation ═══ */
/* ── 03 · THE GAP ───────────────────────────────────────────────
   The parallax angle, drawn. Every row carries two marks for the
   same fact: a hollow one at the fixed origin — the reading from
   outside, which never moves, because from out there nothing about
   her has changed — and a filled coral one at her own position.
   The distance between them is the discrepancy, and it opens row
   by row. Six rows stacked make a fan, and the fan is the section:
   she is the only one standing where the difference is visible. */
#recognition .load{list-style:none; margin:22px 0 0; padding:0; max-width:none}
#recognition .load li{
  position:relative; display:grid;
  grid-template-columns:clamp(40px,4.6vw,72px) minmax(0,1fr);
  gap:0 clamp(16px,2vw,26px); align-items:baseline;
  padding:12px 0; border-top:1px solid rgba(160,138,94,.24);
  font-size:clamp(16px,1.6vw,19px); line-height:1.5; color:var(--mist);
  opacity:0; transform:translateY(10px);
  transition:opacity .7s ease, transform .7s var(--ease), color .7s ease;
}
#recognition .load li.on{opacity:1; transform:none}

.gauge{position:relative; height:.72em; align-self:baseline}
.gauge .axis{
  position:absolute; left:3px; top:50%; right:0; height:1px;
  background:rgba(160,138,94,.16); transform:translateY(-50%);
}
.gauge .fix{                      /* seen from outside · never moves */
  position:absolute; left:3px; top:50%; width:7px; height:7px;
  border:1px solid var(--slate); border-radius:50%;
  background:var(--ink-navy); transform:translate(-50%,-50%);
}
.gauge .span{                     /* the discrepancy */
  position:absolute; left:3px; top:50%; height:1px; width:0;
  background:var(--bronze); transform:translateY(-50%);
  transition:width .85s var(--ease) .12s;
}
.gauge .mov{                      /* her position */
  position:absolute; left:3px; top:50%; width:7px; height:7px;
  border-radius:50%; background:var(--bronze);
  transform:translate(-50%,-50%) scale(0);
  transition:left .85s var(--ease) .12s, transform .45s var(--ease) .5s;
}
#recognition .load li.on .gauge .span{width:var(--g)}
#recognition .load li.on .gauge .mov{left:calc(3px + var(--g)); transform:translate(-50%,-50%) scale(1)}
#recognition .load li:nth-child(1){--g:14%}
#recognition .load li:nth-child(2){--g:30%}
#recognition .load li:nth-child(3){--g:46%}
#recognition .load li:nth-child(4){--g:62%}
#recognition .load li:nth-child(5){--g:80%}
#recognition .load li:nth-child(6){--g:calc(100% - 6px)}

/* weight accrues: each line sits a shade brighter and heavier than the last */
#recognition .load li:nth-child(n+5){color:#C4D0E2}
#recognition .load li:nth-child(n+7){color:var(--cream)}
#recognition .closer{
  margin:26px 0 0; padding:22px 0 0; border-top:1px solid rgba(160,138,94,.42);
  max-width:none; position:relative;
  opacity:0; transform:translateY(16px);
  transition:opacity .9s ease, transform .9s var(--ease);
}
#recognition .closer.on{opacity:1; transform:none}
#recognition .closer p{
  font-family:var(--head); font-size:clamp(19px,2.2vw,28px); font-weight:600;
  line-height:1.3; color:var(--cream); margin:0; max-width:none;
}
#recognition .closer p .hit{display:block; color:var(--gold-lift); margin-top:5px}
/* The cost line holds one line once there is room for it. */
@media(min-width:900px){ #recognition .closer p .hit{white-space:nowrap} }

/* ═══ 04 · TRAP — two options, one dead end ═══ */
.trap-rig{margin:26px 0 0; position:relative}
.trap-pair{display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:clamp(16px,3vw,40px)}
.trap-opt{
  border:1px solid var(--border); border-top:2px solid var(--slate);
  background:var(--card); padding:20px 22px 22px;
}
.trap-opt .q{
  font-family:var(--head); font-size:clamp(18px,2.1vw,25px); font-weight:600;
  color:var(--cream); margin:0 0 10px;
}
.trap-opt .a{font-size:1rem; line-height:1.6; color:var(--slate); margin:0}
.trap-svg{display:block; width:100%; height:96px; overflow:visible}
.trap-svg path{
  stroke:var(--slate); stroke-width:1; fill:none;
  stroke-dasharray:var(--len,300); stroke-dashoffset:var(--len,300);
  transition:stroke-dashoffset 1.1s ease .25s;
}
.trap-rig.on .trap-svg path{stroke-dashoffset:0}
.trap-svg .node{fill:var(--slate); opacity:0; transition:opacity .5s ease 1.25s}
.trap-rig.on .trap-svg .node{opacity:1}
.deadend{
  text-align:center; font-family:var(--head); font-size:.62rem; font-weight:800;
  letter-spacing:.2em; text-transform:uppercase; color:var(--slate);
  opacity:0; transition:opacity .6s ease 1.4s;
}
.trap-rig.on .deadend{opacity:1}
.breakout{
  margin:28px 0 0; padding:22px 26px; max-width:none;
  border-left:2px solid var(--gold); background:rgba(232,198,95,.06);
  font-family:var(--head); font-size:clamp(19px,2.3vw,28px); font-weight:600;
  color:var(--cream);
  opacity:0; transform:translateX(-26px);
  transition:opacity .8s ease .2s, transform .9s var(--ease) .2s;
}
.trap-rig.on .breakout{opacity:1; transform:none}
/* The whole statement holds one line, and the turn takes the coral. */
#trap h2{font-size:clamp(24px,3vw,38px)}
@media(min-width:900px){ #trap h2{white-space:nowrap} }

/* The not-ready exit sits at the objection, per your v2 note — but it is an
   exit, not part of the argument, so it is pushed clear and demoted. */
.notready{
  margin:clamp(38px,5vw,62px) auto 0; padding-top:20px;
  border-top:1px solid var(--border);
  font-size:.95rem; color:var(--slate); max-width:62ch;
  text-align:center;
}
/* The second sentence is the explanation, not part of the offer, so it sits
   under it rather than running on. */
#rw-page .notready span{display:block; margin-top:2px}
.notready a{color:var(--coral-lift)}

/* ═══ 05 · HOW — the only earned sequence ═══ */
.steps{position:relative; margin:30px 0 0}
.steps .track{
  position:absolute; left:0; right:0; top:19px; height:1px;
  background:var(--line-cream);
}
.steps .track i{
  position:absolute; left:0; top:0; height:100%; width:0;
  background:var(--bronze); transition:width 3.4s linear;
  display:block;
}
.steps.on .track i{width:100%}
.steps .row{
  display:grid; grid-template-columns:repeat(3,minmax(0,1fr));
  gap:clamp(20px,4vw,54px); position:relative;
}
.step .n{
  display:inline-flex; align-items:center; justify-content:center;
  width:38px; height:38px; border-radius:50%;
  background:var(--cream); border:1px solid var(--line-cream);
  font-family:var(--head); font-size:.72rem; font-weight:800; color:var(--bronze);
  transition:border-color .5s ease, color .5s ease, background .5s ease;
}
.steps.on .step.lit .n{border-color:var(--bronze); color:var(--ink); background:var(--cream-2)}
.step h4{margin:18px 0 7px; font-size:clamp(17px,1.8vw,21px); font-weight:600; color:var(--ink)}
.step p{margin:0; font-size:1rem; line-height:1.6; color:var(--ink-soft); max-width:30ch}
.rcs{
  margin:32px 0 0; padding-top:18px; border-top:1px solid var(--line-cream);
  display:flex; flex-wrap:wrap; align-items:baseline; gap:8px 18px;
}
.rcs .k{
  font-family:var(--head); font-size:clamp(15px,1.5vw,17px); font-weight:700; color:var(--ink);
}
.rcs .v{
  font-family:var(--head); font-size:.66rem; font-weight:800; letter-spacing:.2em;
  text-transform:uppercase; color:var(--bronze);
}
.why-not{
  margin:26px 0 0; font-family:var(--head); font-size:clamp(18px,1.9vw,23px);
  font-weight:600; color:var(--ink); max-width:none;
}
.why-not span{display:block; color:var(--bronze); margin-top:4px}
@media(min-width:820px){ .why-not{white-space:nowrap} }

/* ═══ 06 · DISTINCTION — negation ═══ */
/* All five hold one row: they are one gesture, not a list. */
.nots{
  list-style:none; margin:22px 0 0; padding:0;
  display:flex; flex-wrap:wrap; gap:12px clamp(14px,1.9vw,26px);
}
.nots li{
  position:relative; font-family:var(--head); white-space:nowrap;
  font-size:clamp(15px,2.05vw,27px); font-weight:600; color:var(--slate);
  transition:color 1s ease;
}
@media(min-width:1000px){ .nots{flex-wrap:nowrap} }
.nots li:after{
  content:''; position:absolute; left:-4px; right:-4px; top:52%; height:2px;
  background:var(--bronze); transform:scaleX(0); transform-origin:left center;
  transition:transform .95s var(--ease);
}
.nots li.struck:after{transform:scaleX(1)}
.nots li.struck{color:#5A6880}
.stand{
  margin:28px 0 0; font-family:var(--head);
  font-size:clamp(18px,2vw,25px); font-weight:600; line-height:1.4;
  color:var(--cream); max-width:none;
}
@media(min-width:760px){ .stand{white-space:nowrap} }
.stand + p{margin-top:14px; color:var(--mist); max-width:44ch}
.not3{margin:30px 0 0; border-top:1px solid var(--border)}
.not3 .r{
  display:grid; grid-template-columns:minmax(0,300px) minmax(0,1fr);
  gap:6px 34px; padding:16px 0; border-bottom:1px solid var(--border);
}
.not3 .r h4{font-size:clamp(17px,1.8vw,21px); font-weight:600; color:var(--cream)}
.not3 .r p{margin:0; color:var(--slate); font-size:1rem}
@media(max-width:720px){ .not3 .r{grid-template-columns:1fr; gap:4px} }

/* ═══ 07 · FRIENDS — the closed loop ═══ */
/* ── 07 · FRIENDS ───────────────────────────────────────────────
   No device. Three statements that escalate, each given its own
   air and separated by a rule, with the last one carrying the
   weight — it is the line that closes the door on the alternative. */
/* Two columns, like The Difference, What This Isn't and The Standard
   Here: the label and headline hold the left, the argument runs down
   the right. A single narrow column stranded against a 1240 wrapper
   was the only section on the page ignoring its own grid, which is
   what made it read as unfinished rather than spare. */
.split{
  display:grid; grid-template-columns:minmax(0,360px) minmax(0,1fr);
  gap:clamp(24px,4vw,72px); align-items:start;
}
@media(max-width:860px){ .split{grid-template-columns:1fr; gap:18px} }
.split h2{margin-bottom:0}
.claims{margin:0; max-width:none}
.claims p{max-width:none; text-wrap:pretty}
.claims .one{
  font-family:var(--head); font-weight:500;
  font-size:clamp(18px,2.1vw,26px); line-height:1.36; color:var(--cream);
  margin:0 0 22px;
}
.claims .two{
  font-size:clamp(15px,1.5vw,18px); line-height:1.62; color:var(--mist);
  margin:0 0 26px; padding-bottom:26px;
  border-bottom:1px solid var(--border);
}
.claims .three{
  font-family:var(--head); font-weight:600;
  font-size:clamp(17px,1.9vw,23px); line-height:1.4; color:var(--gold-lift);
  margin:0;
}

/* ═══ 08 · DIFFERENCE — the release ═══ */
/* Two lines, not four: balance splits it evenly across a wider measure. */
#difference .question{
  font-family:var(--head); font-size:clamp(20px,2.5vw,32px); font-weight:600;
  line-height:1.26; color:var(--ink); max-width:min(940px,100%);
  margin:0 0 22px; text-wrap:balance;
}
#difference .putdown{max-width:min(760px,100%); font-size:clamp(16px,1.6vw,18px); color:var(--ink-soft); text-wrap:pretty}
#difference .putdown b{color:var(--ink); font-weight:600}
/* ── 08 · THE TRANSFER ──────────────────────────────────────────
   Not four features in four boxes. Four things she has been
   holding, moving off her side and docking against the held edge
   on the right — which is dead line until they arrive, and gold
   once all four are over there. She is not carrying less. It is
   being carried somewhere else. */
.lifts{list-style:none; margin:32px 0 0; padding:0; display:grid; gap:1px; background:var(--line-cream)}
.lifts li{
  background:var(--cream-2); padding:18px 24px 20px;
  display:grid;
  grid-template-columns:minmax(0,250px) minmax(0,1fr) clamp(96px,13vw,168px);
  gap:6px clamp(20px,3vw,34px); align-items:center;
  opacity:0; transform:translateY(20px);
  transition:opacity .9s ease, transform 1s var(--ease);
}
.lifts li.lifted{opacity:1; transform:none}
.lifts li h4{font-size:clamp(18px,1.9vw,23px); font-weight:600; color:var(--ink)}
.lifts li p{margin:0; color:var(--ink-soft); font-size:1rem}

.xfer{position:relative; height:16px; display:block}
.xfer .rail{
  position:absolute; left:0; right:0; top:50%; height:1px;
  background:var(--line-cream); transform:translateY(-50%);
}
.xfer .edge{                       /* the held side */
  position:absolute; right:0; top:-19px; bottom:-19px; width:2px;
  background:#DCD3BF; transition:background .9s ease, box-shadow .9s ease;
}
.xfer .blk{                        /* the thing she was holding */
  position:absolute; left:0; top:50%; width:15px; height:15px;
  transform:translateY(-50%); background:var(--bronze);
  transition:left 1.15s var(--ease), background 1.15s ease;
}
.lifts li.lifted .xfer .blk{left:calc(100% - 15px); background:var(--gold)}
.lifts.handed .xfer .edge{background:var(--gold); box-shadow:-3px 0 14px rgba(232,198,95,.5)}
@media(max-width:760px){
  .lifts li{grid-template-columns:1fr; gap:8px}
  .xfer{margin-top:4px}
  .xfer .edge{top:-6px; bottom:-6px}
}

/* ═══ 09 · OUTCOME — the vector reverses ═══ */
/* ── 09 · THE TURN ──────────────────────────────────────────────
   One line high. Six identical marks — same count, same size, same
   weight — all facing away from her. Nothing is added and nothing
   removed: they simply turn, from the far one inward, until every
   one of them faces the mark on the left. Same standard, turned
   inwards. */
.vector{
  margin:24px 0 0; display:flex; align-items:center;
  gap:clamp(16px,3vw,34px); flex-wrap:wrap;
}
.vector svg{width:min(360px,74vw); height:auto; overflow:visible}
.vector .axis{stroke:var(--border); stroke-width:1}
.vector .her{
  fill:var(--navy); stroke:var(--slate); stroke-width:1.6;
  transition:stroke .8s ease, fill .8s ease;
}
.vector.on .her{stroke:var(--gold); fill:rgba(232,198,95,.16)}
.vector .ch{
  fill:none; stroke:var(--slate); stroke-width:2.4;
  stroke-linecap:round; stroke-linejoin:round;
  transform-box:fill-box; transform-origin:center;
  transition:transform 1.05s var(--ease), stroke 1.05s ease;
}
.vector.on .ch{transform:scaleX(-1); stroke:var(--gold)}
.vector .ch:nth-of-type(1){transition-delay:.62s}
.vector .ch:nth-of-type(2){transition-delay:.50s}
.vector .ch:nth-of-type(3){transition-delay:.38s}
.vector .ch:nth-of-type(4){transition-delay:.26s}
.vector .ch:nth-of-type(5){transition-delay:.14s}
.vector .ch:nth-of-type(6){transition-delay:.02s}
.vector .cap{
  font-family:var(--head); font-size:.66rem; font-weight:800;
  letter-spacing:.2em; text-transform:uppercase; color:var(--slate);
}

/* ═══ 11 · LISTEN — the waveform ═══ */
.player{
  margin:24px 0 0; background:var(--card); border:1px solid var(--border);
  border-radius:3px; padding:22px 24px 24px;
}
.player .t{
  font-family:var(--head); font-size:1rem; font-weight:600; color:var(--cream);
  margin-bottom:16px;
}
#wave{display:block; width:100%; height:76px; cursor:pointer}
#wave:focus-visible{outline:1px solid rgba(232,198,95,.7); outline-offset:4px}
.player .play{line-height:1}
.player.is-playing .play{background:rgba(232,198,95,.24)}
.player .meta{
  display:flex; align-items:center; gap:16px; margin-top:14px;
  font-family:var(--head); font-size:.68rem; font-weight:700;
  letter-spacing:.12em; text-transform:uppercase; color:var(--slate);
}
.player .play{
  width:42px; height:42px; border-radius:50%; flex:none;
  border:1px solid rgba(232,198,95,.5); background:rgba(232,198,95,.1);
  color:var(--gold); font-size:.8rem; cursor:pointer;
  display:flex; align-items:center; justify-content:center;
}

/* ═══ 12 · VOICES ═════════════════════════════════════════════
   Three women, three kinds of proof: a boundary held, a thing finally
   done, and what the ninety minutes changed. Portrait, then the words,
   then who said them and what about. No card and no border — the page
   has enough boxes, and a quote in a box reads as marketing rather
   than as somebody talking. */
.voices{
  display:grid; grid-template-columns:repeat(3,minmax(0,1fr));
  grid-template-rows:auto auto auto auto;
  column-gap:clamp(24px,3.2vw,50px); row-gap:0; margin-top:clamp(30px,4vw,46px);
}
@media(max-width:860px){
  .voices{grid-template-columns:1fr; grid-template-rows:none; row-gap:40px; max-width:520px}
  .voice{display:block !important}
}
/* Subgrid so the three names and the three context lines sit on the same line
   across the row, whatever length each quote runs to. */
.voice{margin:0; text-align:center; display:grid; grid-row:span 4; grid-template-rows:subgrid; align-content:start}
/* Round, face-cropped, ringed in gold. The ring is a box-shadow rather than a
   border so it sits outside the circle and never eats into the face, and the
   second, wider shadow is the navy the section already uses — it reads as a
   hairline of air between portrait and ring. */
.voice img{
  /* height:auto matters. The width/height attributes on the img are there to
     reserve the space before the file loads, but the browser maps them to
     presentational width and height -- and with only width set in CSS, the
     height stayed at the attribute's 280px while the width clamped to 152.
     That is what turned the circles into ovals. */
  width:clamp(112px,13vw,152px); height:auto; aspect-ratio:1/1; object-fit:cover;
  display:block; margin:0 auto; border-radius:50%;
  box-shadow:0 0 0 3px var(--navy), 0 0 0 4px var(--gold);
  filter:saturate(.9) brightness(.98);
}
.voice blockquote{
  margin:22px 0 0; font-family:var(--head); font-weight:500;
  font-size:clamp(15px,1.5vw,17.5px); line-height:1.55; color:var(--cream);
  text-wrap:pretty;
}
.voice .who{
  margin:16px 0 0; font-family:var(--head); font-size:.62rem; font-weight:800;
  letter-spacing:.2em; text-transform:uppercase; color:var(--gold);
}
/* One line, always. The context is a caption, not a sentence to be read as
   prose, and a caption that wraps stops looking like a caption. */
.voice .about{
  margin:6px auto 0; font-size:.78rem; line-height:1.5;
  font-style:italic; color:var(--slate); max-width:32ch;
}
/* One line once there is room for one. No ellipsis at any width — a truncated
   caption is worse than a wrapped one. */
@media(min-width:1240px){ .voice .about{white-space:nowrap; max-width:100%} }

/* ═══ 13 · VALUES — standards as held lines ═══ */
.stds{margin:28px 0 0; border-top:1px solid var(--line-cream)}
.std{
  display:grid; grid-template-columns:minmax(0,240px) minmax(0,1fr);
  gap:6px 34px; align-items:baseline; padding:16px 0 16px;
  border-bottom:1px solid var(--line-cream); position:relative;
}
.std:after{
  content:''; position:absolute; left:0; bottom:-1px; height:1px; width:0;
  background:var(--bronze); transition:width 1s var(--ease);
}
.std.held:after{width:100%}
.std h4{font-family:var(--head); font-size:clamp(18px,1.9vw,23px); font-weight:600; color:var(--ink)}
.std p{margin:0; color:var(--ink-soft); font-size:1rem}
@media(max-width:720px){ .std{grid-template-columns:1fr; gap:4px} }

/* ═══ 14 · INTENTION — one unbroken line ═══ */
/* ══ ALIGNMENT ════════════════════════════════════════════════
   Taken from v9, which already had a rule: the OFFER is centred —
   hero, promise, One Intention, the open session and the close —
   and the ARGUMENT is left. Every section that reasons with her
   sits left, like About and Speaking, which carry no centred text
   at all. */
#intention{text-align:center; padding-top:clamp(52px,7vw,92px); padding-bottom:clamp(52px,7vw,92px)}
#intention .oneline{
  width:0; height:1px; background:var(--gold); margin:0 auto 28px;
  transition:width 1.8s var(--ease);
}
#intention.on .oneline{width:min(560px,80%)}
#intention h2{margin-bottom:16px}
#intention p{margin:0 auto; max-width:34ch; color:var(--mist)}

/* ═══ THE OPEN DATE — written once, in #rw-open ═══════════════
   Every date on this page is printed from one attribute, so moving the
   session is a single edit and cannot leave a stale date behind in the
   place nobody remembered to check. If the date passes without being
   updated the page stops naming it rather than advertising a day that
   has gone. */
.cnt{
  font-family:var(--head); font-size:.62rem; font-weight:800;
  letter-spacing:.2em; text-transform:uppercase; color:var(--gold);
}
#hero .next .cnt:not(:empty):before{content:'\\00a0\\00b7\\00a0\\00a0'}
#hero .next .cnt{color:var(--mist)}
.opencard .cnt{margin:14px 0 0}
.opencard .cnt:empty{display:none}
@media(max-width:640px){
  #hero .next .cnt:not(:empty){display:block; margin-top:5px}
  #hero .next .cnt:not(:empty):before{content:none}
}

/* ═══ 15 · PRICING ═══ */
/* The two halves of the offer, side by side rather than stacked: what is free
   this month on the left, what it becomes if she stays on the right. Gold marks
   the free one; the membership is framed the same way but quieter. */
.pricegrid{
  display:grid; grid-template-columns:minmax(0,.82fr) minmax(0,1.18fr);
  gap:clamp(20px,2.6vw,40px); align-items:stretch;
  max-width:1000px; margin:clamp(28px,4vw,42px) auto 0; text-align:left;
}
@media(max-width:860px){ #rw-page .pricegrid{grid-template-columns:1fr; max-width:560px} }
.opencard{
  max-width:none; margin:0; background:var(--card);
  border:1px solid var(--border); border-top:2px solid var(--gold);
  padding:26px 28px 28px; text-align:center;
  display:flex; flex-direction:column; justify-content:center;
}
.opencard .free{
  font-family:var(--head); font-size:clamp(34px,5vw,54px); font-weight:700;
  color:var(--gold); line-height:1; margin-bottom:14px;
}
.opencard .when{font-family:var(--head); font-size:.95rem; font-weight:600; color:var(--cream)}
/* Date on one line, clock on the next. Run together they broke after the
   separator and left it hanging at the end of a line. */
.opencard .when span{display:block; margin-top:3px; font-weight:500; color:var(--mist)}
.opencard .where{font-size:.95rem; color:var(--slate); margin-top:4px}
.opencard .lede{
  margin:18px 0 0; padding-top:16px; border-top:1px solid var(--border);
  font-size:1rem; max-width:none; text-wrap:pretty;
}
.stay{
  max-width:none; margin:0; background:var(--card);
  border:1px solid var(--border); padding:26px 28px 28px;
}
.stay h3{margin-bottom:8px}
.stay{text-align:left}
.stay ul{list-style:none; margin:22px 0 0; padding:0}
.stay li{
  position:relative; padding:9px 0 9px 26px; border-top:1px solid var(--border);
  font-size:1rem; color:var(--mist);
}
.stay li:last-child{border-bottom:1px solid var(--border)}
.stay li:before{
  content:''; position:absolute; left:0; top:1.35em; width:11px; height:1px; background:var(--gold);
}
.pricenote{margin:26px 0 0; font-size:.95rem; color:var(--slate)}
.pricenote b{color:var(--gold); font-weight:600}
.cta-row{margin:30px 0 0; text-align:center}
.cta-row .fine{margin:14px auto 0; font-size:.9rem; color:var(--slate); max-width:44ch}

/* ═══ 16 · AFTER ═══ */
.after-grid{display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:1px; background:var(--line-cream); margin-top:26px}
.after-grid > div{background:var(--cream-2); padding:20px 22px 22px}
.after-grid h4{font-size:clamp(16px,1.7vw,19px); font-weight:600; color:var(--ink); margin-bottom:7px}
.after-grid p{margin:0; font-size:1rem; color:var(--ink-soft)}
@media(max-width:760px){ .after-grid{grid-template-columns:1fr} }

/* ═══ 17 · FAQ ═══ */
.faq{margin:24px 0 0; border-top:1px solid var(--border)}
.faq details{border-bottom:1px solid var(--border)}
.faq summary{
  list-style:none; cursor:pointer; padding:15px 40px 15px 0; position:relative;
  font-family:var(--head); font-size:clamp(16px,1.7vw,19px); font-weight:600; color:var(--cream);
}
.faq summary::-webkit-details-marker{display:none}
.faq summary:after{
  content:'+'; position:absolute; right:6px; top:50%; transform:translateY(-50%);
  width:26px; height:26px; border-radius:50%; border:1px solid var(--coral);
  background:rgba(255,80,31,.12); color:var(--coral-lift);
  display:flex; align-items:center; justify-content:center; font-size:.9rem;
  transition:transform .3s ease;
}
.faq details[open] summary:after{content:'+'; transform:translateY(-50%) rotate(45deg)}
.faq summary:focus-visible{outline:2px solid var(--gold); outline-offset:2px}
.faq .ans{padding:0 40px 18px 0; color:var(--mist); font-size:1rem; max-width:70ch}

/* ═══ 18 · CLOSE ═══ */
#cta-final{text-align:center}
#cta-final h2{font-size:clamp(27px,4vw,50px); max-width:none; margin:0 auto 18px}
#cta-final h2.twoline span{display:block}
#cta-final .when{margin:0 auto 30px; color:var(--mist); max-width:46ch}

/* ═══ TRAP · THE VANTAGE SHIFT ════════════════════════════════
   The parallax argument, made literal. Once "doing different"
   lands, the two options are seen from a new position: they turn
   and displace, the nearer of the two travelling further than the
   other. The dead-end node does not move at all, because nothing
   about the options has changed — only where she is standing. */
.trap-rig{perspective:1400px}
.trap-pair{transform-style:preserve-3d}
.trap-opt{
  transition:transform 1.4s var(--ease), opacity 1.2s ease,
             border-color 1.2s ease, background 1.2s ease;
}
.trap-rig.shifted .trap-opt{
  opacity:.62; border-top-color:#4A5670; background:rgba(10,29,60,.6);
}
.trap-rig.shifted .trap-opt:nth-child(1){transform:translateX(-30px) rotateY(-11deg)}
.trap-rig.shifted .trap-opt:nth-child(2){transform:translateX(-11px) rotateY(-11deg)}
.trap-svg, .deadend{transition:opacity 1.2s ease}
.trap-rig.shifted .trap-svg{opacity:.4}
.trap-rig.shifted .deadend{opacity:.45}
.trap-rig.shifted .breakout{border-left-color:var(--gold-lift)}

/* ═══ ADDED MOTION ═══════════════════════════════════════════ */

/* rail · position marker — where you are standing */
#rail .pos{
  position:absolute; left:50%; width:3px; height:46px; top:0;
  transform:translateX(-50%); border-radius:3px;
  background:var(--gold); opacity:.9;
  box-shadow:0 0 14px rgba(232,198,95,.55);
}

#hero .btn{position:relative}
.btn{overflow:hidden}
.btn:after{
  content:''; position:absolute; inset:0; transform:translateX(-101%);
  background:linear-gradient(90deg,transparent,rgba(255,255,255,.22),transparent);
  transition:transform .65s var(--ease);
}
.btn:hover:after{transform:translateX(101%)}

/* 02 · promise band */
#promise{text-align:center; padding-top:clamp(28px,3.4vw,46px); padding-bottom:clamp(28px,3.4vw,46px)}
.promise-rule{
  width:0; height:1px; background:rgba(232,198,95,.6); margin:0 auto 20px;
  transition:width 1.2s var(--ease);
}
.promise-rule.on{width:42px}
/* One line. It is the whole offer, and a break in it costs the sentence. */
.promise-line{
  font-family:var(--head); font-weight:400;
  font-size:clamp(17px,2vw,26px); line-height:1.22; letter-spacing:-.018em;
  color:var(--cream); max-width:none; margin:0 auto; text-wrap:balance;
}
@media(min-width:800px){ .promise-line{white-space:nowrap} }
.promise-sub{
  font-family:var(--head); font-weight:400; text-align:center;
  font-size:clamp(14px,1.15vw,17px); line-height:1.4; color:var(--slate);
  max-width:520px; margin:10px auto 0;
}

/* 03 · recognition */
#recognition .load li.on{animation:settle .5s var(--ease)}
@keyframes settle{
  0%{filter:blur(2px)} 100%{filter:blur(0)}
}

/* 05 · how · a runner travels the track */
.steps .track{overflow:visible}
.steps .runner{
  position:absolute; top:50%; left:0; width:7px; height:7px; border-radius:50%;
  background:var(--bronze); transform:translate(-50%,-50%); opacity:0;
  box-shadow:0 0 0 4px rgba(160,138,94,.18);
}
.steps.on .runner{animation:run 3.4s linear forwards}
@keyframes run{
  0%{left:0; opacity:0}
  6%{opacity:1}
  94%{opacity:1}
  100%{left:100%; opacity:0}
}
.step .n{transition:border-color .45s ease, color .45s ease, background .45s ease, transform .45s var(--ease), box-shadow .45s ease}
.steps.on .step.lit .n{box-shadow:0 0 0 5px rgba(160,138,94,.14)}
.steps.on .step.lit .n{transform:translateY(-3px)}

/* 06 · distinction · struck items recede */
.nots li{transition:color 1s ease, opacity 1s ease, transform 1s var(--ease)}
.nots li.struck{opacity:.55; transform:scale(.97)}
.not3 .r{transition:background .3s ease, padding-left .3s var(--ease)}
.not3 .r:hover{background:rgba(232,198,95,.05); padding-left:12px}

/* 08 · difference · the release blooms, rows respond */
.lifts li{transition:opacity .9s ease, transform 1s var(--ease), background .3s ease}
.lifts li:hover{background:#FFFDF7}
#difference.released .question{animation:lift 1.1s var(--ease)}
@keyframes lift{from{transform:translateY(6px)}to{transform:none}}

/* 09 · outcome · the caption turns with the arrow */
.vector .cap{transition:opacity .45s ease, color .45s ease}
.vector .cap.swap{opacity:0}
.vector.on .cap{color:var(--gold-lift)}

/* 11 · listen · bars grow, then the played head pulses */
.player{transition:border-color .4s ease}
.player:hover{border-color:rgba(232,198,95,.4)}
.player .play{transition:background .25s ease, transform .25s var(--ease)}
.player .play:hover{background:rgba(232,198,95,.22); transform:scale(1.06)}

/* 13 · values · rows respond */
.std{transition:padding-left .35s var(--ease)}
.std:hover{padding-left:12px}

/* 16 · after · cards respond */
.after-grid > div{transition:background .3s ease, transform .35s var(--ease)}
.after-grid > div:hover{background:#FFFDF7; transform:translateY(-3px)}

/* 17 · faq · answers open rather than appear */
.faq details[open] .ans{animation:faqIn .42s var(--ease)}
@keyframes faqIn{from{opacity:0; transform:translateY(-6px)}to{opacity:1; transform:none}}

@media(prefers-reduced-motion:reduce){
  *{animation:none!important; transition:none!important}
  #hero .plate{transform:none!important}
  #rail .pos{display:none}
}
@media(max-width:760px){
  .trap-pair{grid-template-columns:1fr}
  .steps .row{grid-template-columns:1fr; gap:30px}
  .steps .track{display:none}
}

/* ═══ THE APPLICATION MODAL ═══════════════════════════════════
   Carried over from v9 unchanged in behaviour. The three form ids
   below must always move together; see the note in the markup. */
#rw-apply-modal{display:none; position:fixed; inset:0; z-index:9999;
  align-items:center; justify-content:center; padding:16px}
#rw-apply-modal.is-open{display:flex}
#rw-apply-modal .modal-backdrop{position:absolute; inset:0;
  background:rgba(0,0,0,.85); backdrop-filter:blur(6px); cursor:pointer}
#rw-apply-modal .modal-box{
  position:relative; z-index:1; background:var(--card);
  border:1px solid var(--border); border-top:2px solid var(--gold);
  width:100%; max-width:580px; max-height:min(92vh,940px);
  display:flex; flex-direction:column; overflow-y:auto;
  -webkit-overflow-scrolling:touch;
}
#rw-apply-modal .modal-header{
  display:flex; justify-content:space-between; align-items:center;
  padding:15px 22px; border-bottom:1px solid var(--border);
  background:var(--card); z-index:2; flex-shrink:0;
}
#rw-apply-modal .modal-title{font-family:var(--head); font-size:14px;
  letter-spacing:.15em; text-transform:uppercase; color:var(--cream)}
#rw-apply-modal .modal-close{
  background:none; border:1px solid var(--border); color:var(--slate);
  width:32px; height:32px; display:flex; align-items:center;
  justify-content:center; cursor:pointer; font-size:20px; line-height:1;
  transition:color .2s, border-color .2s; flex-shrink:0;
}
#rw-apply-modal .modal-close:hover{color:var(--cream); border-color:var(--gold)}
#rw-apply-modal .modal-body{padding:0}
#rw-apply-modal .modal-body iframe{display:block; width:100%; border:none}


/* ═══ MOBILE ═══════════════════════════════════════════════════
   The page was composed on a wide screen, and vertical rhythm that reads as
   air at 1440 reads as a gap at 390. These are spacing and alignment only:
   nothing below changes what the page says or how it behaves. */
@media(max-width:860px){
  /* The desktop voices grid gives each figure four subgrid rows so the names
     and captions line up across the row. In one column that becomes twelve
     rows and eleven gaps, which is why the three of them drifted so far
     apart on a phone. */
  .voice{grid-row:auto}
}
@media(max-width:640px){
  /* The hero strip breaks where the sense breaks, not mid-phrase. */
  #hero .next .sep{display:none}
  #hero .next .nd{display:block; margin-top:6px}

  /* 05 · The numeral sat 18px off its own heading and 30px off the next
     block, so the three steps read as six separate things. */
  .steps .row{gap:22px}
  .step h4{margin:10px 0 5px}
  .why-not{margin-top:34px}

  /* 06 · The negations centre rather than ragging left. */
  .nots{justify-content:center; text-align:center; gap:10px 16px}

  /* 08 · The transfer rows. */
  .lifts li{padding:14px 18px 15px}

  /* 10 · The reel takes the width of the phone and sits centre; the
     credentials centre under it. */
  .rcs{justify-content:center; text-align:center; margin-top:24px}
  /* the system name takes its own line so the three words below it read as
     one centred group rather than an orphan and a pair */
  #how .rcs .k{flex-basis:100%; margin-bottom:2px}

  /* 13 · The standards. */
  .std{padding:12px 0}

  /* 15 · The application note needs air under the button. */
  .cta-row .fine{margin-top:24px}

  /* 16 · After. */
  .after-grid > div{padding:15px 18px 16px}
}
`;
  var HTML = `<div id="rw-page">
<parallaxx-nav active="women"></parallaxx-nav>
<div id="rw-open" hidden
     data-open-at="2026-09-22T08:30:00+02:00"
     data-open-tz="08:30 CET"
     data-open-times="08:30 CET / 14:30 GMT+8"></div>
<div id="rail"><div class="bar"></div><div class="pos" id="railPos"></div></div>
<!-- ═══════════ 01 · HERO ═══════════ -->
<section class="sec" id="hero">
  <div class="plate"><div class="shot"></div></div>
  <div class="scrim"></div>
  <div class="hin">
    <h1>
      <span class="mask"><span class="sm">The</span></span>
      <span class="mask"><span class="key">Reconnected</span></span>
      <span class="mask"><span>Woman</span></span>
    </h1>
    <p class="sub" data-r="near"><span>You've competed with men. Led men. Carried men.</span><b>The masculine is provided.</b><b>You don't have to, for a change.</b></p>
    <button class="btn" type="button" data-trw-modal data-r="mid">Come To The Open Session</button>
    <div class="next" data-r="far">Next open session<span class="sep">&nbsp;&middot;&nbsp;</span><span class="nd"><span class="js-open-date">Tuesday 22 September</span><span class="js-open-tz"> &nbsp;&middot;&nbsp; 08:30 CET</span></span><span class="cnt js-open-count"></span></div>
</div>
  <div class="cue">&darr;</div>
</section>

<!-- ═══════════ 02 · THE PROMISE — its own beat, below the fold ═══════════ -->
<section class="sec black" id="promise">
  <div class="in">
    <div class="promise-rule" id="promiseRule"></div>
    <p class="promise-line"><span class="mask"><span>Work out what you want, and ask for it in four weeks.</span></span></p>
    <p class="promise-sub" data-r="far">Without slowing down or doing less.</p>
</div>
</section>

<!-- ═══════════ 03 · VSL ═══════════ -->
<section class="sec dark" id="vsl">
  <div class="in narrow">
    <p class="ey" data-r="far">Watch This First</p>
    <div data-r="mid">
     <div class="vidwrap" data-par="0.10">
      <video id="rw-vsl" playsinline preload="metadata"
             poster="https://static.wixstatic.com/media/RW_VSL_POSTER~mv2.jpg">
        <source src="https://video.wixstatic.com/video/111174_c76bb418f85d49579bd90c14e6addf79/1080p/mp4/file.mp4" type="video/mp4">
      </video>
      <button class="vplay" type="button" aria-label="Play video"><svg viewBox="0 0 96 96" aria-hidden="true"><circle cx="48" cy="48" r="46" fill="rgba(6,25,56,0.85)" stroke="#E8C65F" stroke-width="2"/><polygon points="40,30 68,48 40,66" fill="#E8C65F"/></svg></button>
     </div>
    </div>
    <p class="pullquote" data-r="mid">"I've got no interest in you becoming more feminine, or more spiritual, or more of anything. I want to see you being more of you."</p>
  </div>
</section>

<!-- ═══════════ 03 · RECOGNITION — the accumulation ═══════════ -->
<section class="sec ink" id="recognition">
  <div class="in narrow">
    <p class="ey" data-r="far">Sounds Like You?</p>
    <div class="divider" data-r="far"></div>
    <ul class="load" id="loadList">
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You said yes again this week. There was no room for it.</span></li>
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You're the one everyone talks to. Nobody asks about you.</span></li>
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You hit the target. The target moves.</span></li>
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You cancelled on someone who matters. Again.</span></li>
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You're holding it together for more people than you'd admit to.</span></li>
      <li><span class="gauge"><i class="axis"></i><i class="span"></i><i class="fix"></i><i class="mov"></i></span><span class="txt">You look at the men around you and not one of them is carrying anything for you.</span></li>
    </ul>
    <div class="closer" id="recogCloser">
      <p>You're winning. You're delivering.<span class="hit">And you're the only one who feels what it costs.</span></p>
    </div>
</div>
</section>

<!-- ═══════════ 04 · TRAP — two options, one dead end ═══════════ -->
<section class="sec dark" id="trap">
  <div class="in narrow">
    <h2 data-r="near">You've had two options. <span class="hit">Neither works.</span></h2>
    <div class="trap-rig" id="trapRig">
      <div class="trap-pair">
        <div class="trap-opt">
          <p class="q">&ldquo;Slow down. Do less.&rdquo;</p>
          <p class="a">Like being asked to stop being you.</p>
        </div>
        <div class="trap-opt">
          <p class="q">&ldquo;Push harder.&rdquo;</p>
          <p class="a">You're already at full capacity.</p>
        </div>
      </div>
      <svg class="trap-svg" viewBox="0 0 600 96" preserveAspectRatio="none" aria-hidden="true">
        <path style="--len:300" d="M150,0 L150,44 Q150,64 300,64" />
        <path style="--len:300" d="M450,0 L450,44 Q450,64 300,64" />
        <path style="--len:40" d="M300,64 L300,84" />
        <circle class="node" cx="300" cy="88" r="4" />
      </svg>
      <p class="deadend">Same place</p>
      <p class="breakout">So this is about doing different.</p>
    </div>
    <p class="notready">Not ready? <a href="#">Take the Priority Audit</a>.<span>Ten minutes to find out which priority gives you the most return.</span></p>
</div>
</section>

<!-- ═══════════ 05 · HOW — the only earned sequence ═══════════ -->
<section class="sec cream" id="how">
  <div class="in">
    <p class="ey" data-r="far">How It Works</p>
    <div class="steps" id="stepsRig">
      <div class="track"><i></i><em class="runner"></em></div>
      <div class="row">
        <div class="step">
          <span class="n">01</span>
          <h4>Work Out What You Want</h4>
          <p>The personal unmet values and needs.</p>
        </div>
        <div class="step">
          <span class="n">02</span>
          <h4>Prioritising Yourself</h4>
          <p>Asking for, and declining, with real intention.</p>
        </div>
        <div class="step">
          <span class="n">03</span>
          <h4>Your Outcome In Reality</h4>
          <p>Not your assumption of it.</p>
        </div>
      </div>
    </div>
    <p class="why-not" data-r="mid">A simple outcome you already know you should have.<span>So why haven't you?</span></p>
    <div class="rcs" data-r="far">
      <span class="k">The Relational Connection System&trade;</span>
      <span class="v">Vision</span><span class="v">Values</span><span class="v">Velocity</span>
    </div>
</div>
</section>

<!-- ═══════════ 06 · DISTINCTION — negation ═══════════ -->
<section class="sec black" id="distinction">
  <div class="in">
    <p class="ey" data-r="far">Be Clear On What This Is</p>
    <h2 data-r="near">What This Isn't</h2>
    <ul class="nots" id="notsList">
      <li>No sage</li>
      <li>No palo santo</li>
      <li>No goddess circle</li>
      <li>No womb wisdom</li>
      <li>No priestess</li>
    </ul>
    <p class="stand" data-r="mid">We're not asking you to soften or surrender.</p>
    <p data-r="mid">This is somewhere your competence has nothing to do.</p>
    <div class="not3" data-r="mid">
      <div class="r"><h4>Not A Women's Circle</h4><p>No performance of it.</p></div>
      <div class="r"><h4>Not A Sisterhood</h4><p>No loyalty pact, no comfort without change.</p></div>
      <div class="r"><h4>Not A Friendly Coffee Chat</h4><p>No deflection into talking about other people.</p></div>
    </div>
</div>
</section>

<!-- ═══════════ 07 · FRIENDS — the closed loop ═══════════ -->
<section class="sec dark" id="friends">
  <div class="in">
    <div class="split">
     <div>
      <h2 data-r="near">Your friends can't do this</h2>
     </div>
     <div class="claims">
      <p class="one" data-r="near">Friendship runs on co-regulation. You leave feeling better and nothing has changed.</p>
      <p class="two" data-r="mid">Put yourself in a room of women exactly like you and you'll find a way to help. You always do.</p>
      <p class="three" data-r="mid">Every other room like this one is run by someone exactly like you.</p>
     </div>
    </div>
</div>
</section>

<!-- ═══════════ 08 · DIFFERENCE — the release ═══════════ -->
<section class="sec cream" id="difference">
  <div class="in">
    <p class="ey" data-r="far">The Difference</p>
    <p class="question" data-r="near">When were you last seen, heard, witnessed and held in a masculine presence without a romantic intent?</p>
    <p class="putdown" data-r="mid">You've been over-functioning your masculinity to make up for the masculine you didn't get from the men in your world. <b>Put it down for a change.</b></p>
    <ul class="lifts" id="liftList">
      <li><h4>Structure is held</h4><p>Someone else facilitates the room, the logistics, the pace.</p><span class="xfer"><i class="rail"></i><i class="edge"></i><i class="blk"></i></span></li>
      <li><h4>It's taken care of</h4><p>Nothing here for you to do.</p><span class="xfer"><i class="rail"></i><i class="edge"></i><i class="blk"></i></span></li>
      <li><h4>Nothing to manage</h4><p>Say the true thing. No cleaning up after it.</p><span class="xfer"><i class="rail"></i><i class="edge"></i><i class="blk"></i></span></li>
      <li><h4>No scorecard</h4><p>Nothing to win, so your competence has nothing to do.</p><span class="xfer"><i class="rail"></i><i class="edge"></i><i class="blk"></i></span></li>
    </ul>
</div>
</section>

<!-- ═══════════ 09 · OUTCOME — the vector reverses ═══════════ -->
<section class="sec dark" id="outcome">
  <div class="in narrow">
    <p class="ey" data-r="far">What Changes</p>
    <h2 data-r="near">You already have the integrity.<br>It points outward.</h2>
    <div class="vector" id="vectorRig">
      <svg viewBox="0 0 380 34" aria-hidden="true">
        <g transform="translate(0,17)">
          <line class="axis" x1="14" y1="0" x2="366" y2="0" />
        <path class="ch" d="M330,-8 L339,0 L330,8" />
        <path class="ch" d="M282,-8 L291,0 L282,8" />
        <path class="ch" d="M234,-8 L243,0 L234,8" />
        <path class="ch" d="M186,-8 L195,0 L186,8" />
        <path class="ch" d="M138,-8 L147,0 L138,8" />
        <path class="ch" d="M90,-8 L99,0 L90,8" />
          <circle class="her" cx="34" cy="0" r="10" />
        </g>
      </svg>
      <span class="cap" id="vecCap">Pointed outward</span>
    </div>
    <p class="lede" data-r="mid" style="margin-top:26px">Our work here is turning it inward. Same standard, turned inwards.</p>
</div>
</section>

<!-- ═══════════ 10 · BIO ═══════════ -->
<section class="sec cream-2" id="bio">
  <div class="in">
    <div class="biogrid">
      <div data-r="mid">
       <div data-par="0.13">
        <div class="reelwrap">
          <video id="rw-bio" playsinline preload="metadata"
                 poster="https://static.wixstatic.com/media/111174_52bb7073da274289b13738d153e1334e~mv2.jpg">
            <source src="https://video.wixstatic.com/video/111174_4e1bcc7d4c634401adae988048a9cb1b/1080p/mp4/file.mp4" type="video/mp4">
          </video>
          <button class="vplay" type="button" aria-label="Play video"><svg viewBox="0 0 96 96" aria-hidden="true"><circle cx="48" cy="48" r="46" fill="rgba(6,25,56,0.85)" stroke="#E8C65F" stroke-width="2"/><polygon points="40,30 68,48 40,66" fill="#E8C65F"/></svg></button>
        </div>
       </div>
      </div>
      <div>
        <h2 data-r="near" style="margin-bottom:16px">Daniel Lawson</h2>
        <p data-r="mid">I don't know what it's like to be a woman. I'm not one.</p>
        <p data-r="mid">I do know what it's like to be obsessed with productivity and to compete for success.</p>
        <p data-r="mid">Business development, tech company, Australia. I hit my target and it cost me my soul. The next year the number went up another 10%.</p>
        <p data-r="mid">I resigned. Then I lost my sense of identity.</p>
        <p data-r="mid" style="font-family:var(--head); font-weight:600; color:var(--ink)">I got out. And I understand the codes.</p>
        <div class="rcs" data-r="far">
          <span class="v">ICG Accredited</span><span class="v">6 Years Coaching</span><span class="v">Global Facilitator</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ═══════════ 11 · LISTEN ═══════════ -->
<section class="sec dark" id="listen">
  <div class="in narrow">
    <h2 data-r="near">What kind of conversation to expect?</h2>
    <p class="lede" data-r="mid">Here's a recent summary of us together.</p>
    <div class="player" data-r="mid">
      <div class="t">The Reconnected Woman &mdash; a summary of a recent session</div>
      <audio id="rw-listen" preload="none" src="/audio/reconnected-woman-summary.mp3"></audio>
      <canvas id="wave" width="1200" height="152" role="slider" tabindex="0"
              aria-label="Seek through the session summary"
              aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"></canvas>
      <div class="meta">
        <button class="play" id="rw-listen-play" type="button" aria-label="Play the session summary">&#9654;</button>
        <span><span id="rw-now">0:00</span> / 10:45 &nbsp;&middot;&nbsp; 5.2 MB</span>
      </div>
    </div>
    <p class="pullquote" data-r="mid">"Life is not about trying to feel better. Life is about feeling more."</p>
</div>
</section>

<!-- ═══════════ 12 · VOICES ═══════════ -->
<!-- ═══════════ 12 · VOICES ═══════════ -->
<section class="sec black" id="voices">
  <div class="in">
    <h2 data-r="near">From women inside</h2>
    <div class="voices" data-r="mid">
      <figure class="voice">
        <img src="https://static.wixstatic.com/media/RW_VOICE_CHRISTINE~mv2.jpg" alt="Christine" width="280" height="280" loading="lazy" decoding="async">
        <blockquote>&ldquo;This time I stood my ground. He said it's the first time in fourteen years I'd really discussed something with him, and the first time he understood me.&rdquo;</blockquote>
        <p class="who">Christine</p>
        <p class="about">On ending a disagreement she'd normally have absorbed.</p>
      </figure>
      <figure class="voice">
        <img src="https://static.wixstatic.com/media/RW_VOICE_DANI~mv2.jpg" alt="Dani" width="280" height="280" loading="lazy" decoding="async">
        <blockquote>&ldquo;I bit the bullet. I'm not going to sit with this any longer. It's terrifying, and I feel very proud of myself.&rdquo;</blockquote>
        <p class="who">Dani</p>
        <p class="about">On the business she'd kept in the background for six years.</p>
      </figure>
      <figure class="voice">
        <img src="https://static.wixstatic.com/media/RW_VOICE_JULIA~mv2.jpg" alt="Julia" width="280" height="280" loading="lazy" decoding="async">
        <blockquote>&ldquo;I'm more tapped into myself, and because of that everybody around me can be more tapped into themselves.&rdquo;</blockquote>
        <p class="who">Julia</p>
        <p class="about">On what ninety minutes a week changed.</p>
      </figure>
    </div>
  </div>
</section>

<!-- ═══════════ 13 · VALUES — standards as held lines ═══════════ -->
<section class="sec cream" id="values">
  <div class="in">
    <p class="ey" data-r="far">What We Stand For</p>
    <h2 data-r="near">Our standards together</h2>
    <div class="stds" id="stdList">
      <div class="std"><h4>Your Space</h4><p>Not for the women. Not to contribute.</p></div>
      <div class="std"><h4>Your Truth</h4><p>That's the contribution. Performance is at the door.</p></div>
      <div class="std"><h4>Punctuality</h4><p>Show up like it matters, because you do.</p></div>
      <div class="std"><h4>Curiosity</h4><p>Over judgment. Of each other, and of yourself.</p></div>
      <div class="std"><h4>Accountability</h4><p>One intention out of every session. Yours to keep.</p></div>
    </div>
</div>
</section>

<!-- ═══════════ 14 · INTENTION — one unbroken line ═══════════ -->
<section class="sec ink" id="intention">
  <div class="in">
    <div class="oneline"></div>
    <p class="ey" data-r="far">One Intention</p>
    <h2 data-r="near">An outcome specifically for you.</h2>
    <p data-r="mid">And the line you'll hold to meet it.</p>
</div>
</section>

<!-- ═══════════ 15 · PRICING ═══════════ -->
<section class="sec dark" id="pricing" style="text-align:center">
  <div class="in">
    <h2 data-r="near" style="max-width:24ch; margin-left:auto; margin-right:auto">The first session of every month is open.</h2>
    <div class="pricegrid" data-r="mid">
    <div class="opencard">
      <div class="free">Free</div>
      <div class="when"><span class="js-open-date">Tuesday 22 September</span><span class="js-open-times">08:30 CET / 14:30 GMT+8</span></div>
      <div class="where">Online, wherever you are</div>
      <p class="cnt js-open-count"></p>
      <p class="lede">Come, sit in it, and decide from the session rather than from this page.</p>
    </div>
    <div class="stay">
      <h3>Stay, and here's the four weeks.</h3>
      <p class="dim">Work out what you want, and ask for it.</p>
      <ul>
        <li>One live session a week, in four-week cycles</li>
        <li>Live facilitation and hot seat work</li>
        <li>Access to the group between sessions</li>
        <li>First access to new frameworks and resources</li>
        <li>Complimentary 1:1 call on week 6</li>
      </ul>
      <p class="pricenote"><b>&euro;89 a month</b>, and your membership price is grandfathered. It never increases as long as you stay. Memberships roll over monthly. Cancel anytime.</p>
    </div>
    </div>
    <div class="cta-row" data-r="mid">
      <button class="btn" type="button" data-trw-modal>Register For The Open Session</button>
      <p class="fine">The short application first is to ensure a strong mutual fit, for you and for our community.</p>
    </div>
  </div>
</section>

<!-- ═══════════ 16 · AFTER ═══════════ -->
<section class="sec cream-2" id="after">
  <div class="in">
    <p class="ey" data-r="far">After Four Weeks</p>
    <h2 data-r="near">You'll own a new experience of your life.</h2>
    <div class="after-grid" data-r="mid">
      <div><h4>Continue to build upon it</h4><p>Build the reps, with the same one or a different one.</p></div>
      <div><h4>Continue more personalised</h4><p>Credit your membership to personal 1:1 access.</p></div>
      <div><h4>Continue alone</h4><p>Take what you've gained, and celebrate your wins.</p></div>
    </div>
  </div>
</section>

<!-- ═══════════ 17 · FAQ ═══════════ -->
<section class="sec black" id="faq">
  <div class="in narrow">
    <p class="ey" data-r="far">Common Questions</p>
    <h2 data-r="near">Before You Come</h2>
    <div class="faq" data-r="mid">
      <details><summary>Isn't this just another women's circle?</summary><div class="ans">No. Most are built around becoming more feminine, and there's a performance in that. Here we call it as it is, so you can put something down instead of picking more up.</div></details>
      <details><summary>Why is a man hosting it?</summary><div class="ans">Because the women who join want a healthy masculine presence in their life, one they are not being led by, competing with, or taking instruction from. Just the masculine holding the room, so they can stop for a change.</div></details>
      <details><summary>I have great friends. Do I need this?</summary><div class="ans">Friendship runs on co-regulation and the women you'd go to are running your operating system. I'm not here to be your friend.</div></details>
      <details><summary>Can I come once before I decide?</summary><div class="ans">Yes. The first session of every month is open and there's no charge for it. Come, sit in it, and decide from the room rather than from this page. After that it's &euro;89 a month, and you're grandfathered: your price never increases as long as you stay.</div></details>
      <details><summary>Where does it run?</summary><div class="ans">Online, so you can join from wherever you are. Nothing to travel to and nothing to arrange around it.</div></details>
      <details><summary>Do I need to attend every session?</summary><div class="ans">No. Life moves and the group knows it. Come when you can, and know that the women who turn up regularly are the ones who feel it shift.</div></details>
      <details><summary>When do sessions run?</summary><div class="ans">One sitting time, set so it works across the time zones the group is spread over, and we stay fluid with what the group asks for. Come to the open session and you'll see how it sits with your week.</div></details>
      <details><summary>What does the fee include?</summary><div class="ans">Facilitation, hot seat work, resources, and the people you'll meet. It's largely a filter.</div></details>
      <details><summary>Are there upsells coming?</summary><div class="ans">No. There's the 1:1 Reconnect Program if you decide you want more, faster, deeper, and your membership investment is discounted from that. Same work, same direction. More personal.</div></details>
    </div>
  </div>
</section>

<!-- ═══════════ 18 · CLOSE ═══════════ -->
<section class="sec ink" id="cta-final">
  <div class="in">
    <p class="ey" data-r="far">Ready?</p>
    <h2 data-r="near" class="twoline">Work out what you want,<span>and ask for it in four weeks.</span></h2>
    <p class="when" data-r="mid">Next open session <span class="js-open-date" data-fallback="is being confirmed">Tuesday 22 September</span>. Free to attend, short application form first.</p>
    <button class="btn" type="button" data-trw-modal data-r="mid">Register For The Open Session</button>
  </div>
</section>
<parallaxx-footer></parallaxx-footer>

<div id="rw-apply-modal" role="dialog" aria-modal="true" aria-label="Application Form">
  <div class="modal-backdrop"></div>
  <div class="modal-box">
    <div class="modal-header">
      <span class="modal-title">Registration</span>
      <button class="modal-close" aria-label="Close form">&times;</button>
    </div>
    <div class="modal-body">
      <!-- THE ID APPEARS THREE TIMES AND ALL THREE MATTER. src is what loads.
           id="form-<ID>" and data-form-id are what GHL's form_embed.js matches
           on when it posts a height back, so a swap that misses either one
           loads the right form at the wrong height and never resizes it.
           Change all three together. -->
      <iframe
        src="https://api.leadconnectorhq.com/widget/form/0RT5EVaTlGxo6QBjyIPT"
        height="700"
        title="The Reconnected Woman Application Form"
        id="form-0RT5EVaTlGxo6QBjyIPT"
        data-form-id="0RT5EVaTlGxo6QBjyIPT"
        data-form-name="The Reconnected Woman Application Form"
      ></iframe>
    </div>
  </div>
</div>

</div>`;

  function once(id, make) {
    if (document.getElementById(id)) return;
    var el = make(); el.id = id; document.head.appendChild(el);
  }

  function addHead() {
    once('rw-pre1', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.googleapis.com'; return l; });
    once('rw-pre2', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.gstatic.com'; l.crossOrigin = ''; return l; });
    once('rw-font0', function () { var l = document.createElement('link'); l.rel = 'stylesheet'; l.href = 'https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap'; return l; });
    once('rw-css', function () { var st = document.createElement('style'); st.textContent = CSS; return st; });
    if (!document.querySelector('meta[name="viewport"]')) {
      var mv = document.createElement('meta'); mv.name = 'viewport';
      mv.content = 'width=device-width, initial-scale=1'; document.head.appendChild(mv);
    }
  }

  function script(src, id) {
    return new Promise(function (res) {
      if (document.getElementById(id)) return res();
      var sc = document.createElement('script'); sc.id = id; sc.src = src; sc.async = false;
      sc.onload = res; sc.onerror = res; document.head.appendChild(sc);
    });
  }

  function boot() {


(function(){
  var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var noIO   = !('IntersectionObserver' in window);
  if(reduce || noIO){ document.body.classList.add('no-anim'); }

  function obs(cb, opts){
    if(reduce || noIO) return null;
    return new IntersectionObserver(function(es){
      es.forEach(function(e){ if(e.isIntersecting) cb(e.target, this); }, this);
    }, opts || { rootMargin:'0px 0px -12% 0px' });
  }

  /* ── depth-graded arrival, one-shot ─────────────────────────── */
  var reveals = [].slice.call(document.querySelectorAll('[data-r]'));
  if(reduce || noIO){
    reveals.forEach(function(el){ el.classList.add('seen'); });
  } else {
    var ro = obs(function(el){ el.classList.add('seen'); ro.unobserve(el); });
    reveals.forEach(function(el){ ro.observe(el); });
    /* failsafe: nothing stays hidden if an observer never fires */
    setTimeout(function(){ reveals.forEach(function(el){ el.classList.add('seen'); }); }, 4000);
  }

  /* ── hero masked headline, on load ──────────────────────────── */
  var masks = [].slice.call(document.querySelectorAll('#hero .mask'));
  masks.forEach(function(m, i){
    setTimeout(function(){ m.classList.add('seen'); }, reduce ? 0 : 120 + i * 130);
  });

  /* The rail no longer changes state; Recognition just paces its rows. */
  function setLoad(){}

  /* ── 03 · RECOGNITION ───────────────────────────────────────── */
  var rows   = [].slice.call(document.querySelectorAll('#loadList li'));
  var closer = document.getElementById('recogCloser');
  function runLoad(){
    rows.forEach(function(r, i){
      setTimeout(function(){ r.classList.add('on'); setLoad(i + 1); }, 120 + i * 420);
    });
    setTimeout(function(){
      closer.classList.add('on');
      setLoad(rows.length + 2);
    }, 120 + rows.length * 420 + 620);
  }
  if(reduce || noIO){
    rows.forEach(function(r){ r.classList.add('on'); });
    closer.classList.add('on');
  } else {
    var ioR = obs(function(el){ runLoad(); ioR.unobserve(el); }, { rootMargin:'0px 0px -22% 0px' });
    ioR.observe(document.getElementById('recognition'));
  }

  /* ── generic "add class once on arrival" helper ─────────────── */
  function once(sel, cls, margin){
    var el = typeof sel === 'string' ? document.querySelector(sel) : sel;
    if(!el) return;
    if(reduce || noIO){ el.classList.add(cls); return; }
    var io = obs(function(t){ t.classList.add(cls); io.unobserve(t); },
                 { rootMargin: margin || '0px 0px -18% 0px' });
    io.observe(el);
  }
  once('#trapRig',   'on');
  once('#vectorRig', 'on');
  once('#intention', 'on');
  once('#promiseRule', 'on');

  /* ── 04 · TRAP · the vantage shifts after the breakout ──────── */
  (function vantage(){
    var rig = document.getElementById('trapRig'); if(!rig) return;
    if(reduce || noIO){ rig.classList.add('shifted'); return; }
    var io = obs(function(el){
      setTimeout(function(){ el.classList.add('shifted'); }, 2100);
      io.unobserve(el);
    }, { rootMargin:'0px 0px -20% 0px' });
    io.observe(rig);
  })();

  /* ── 02 · PROMISE · rule draws, then the line lifts ─────────── */
  (function promise(){
    var m = document.querySelector('#promise .mask');
    if(!m) return;
    if(reduce || noIO){ m.classList.add('seen'); return; }
    var io = obs(function(el){
      setTimeout(function(){ el.classList.add('seen'); }, 260);
      io.unobserve(el);
    });
    io.observe(m);
  })();


  /* ── 08 · DIFFERENCE · the release is announced ─────────────── */
  (function release(){
    var sec  = document.getElementById('difference');
    var note = sec ? sec.querySelector('.release-note') : null;
    if(!sec) return;
    if(reduce || noIO){ if(note) note.classList.add('on'); return; }
    var io = obs(function(el){
      el.classList.add('released');
      setTimeout(function(){ if(note) note.classList.add('on'); }, 1500);
      io.unobserve(el);
    }, { rootMargin:'0px 0px -30% 0px' });
    io.observe(sec);
  })();

  /* ── 09 · OUTCOME · the caption turns with the arrow ────────── */
  (function turn(){
    var rig = document.getElementById('vectorRig');
    var cap = document.getElementById('vecCap');
    if(!rig || !cap) return;
    if(reduce || noIO){ cap.textContent = 'Now turned inwards'; return; }
    var io = obs(function(el){
      setTimeout(function(){
        cap.classList.add('swap');
        setTimeout(function(){
          cap.textContent = 'Now turned inwards';
          cap.classList.remove('swap');
        }, 420);
      }, 520);
      io.unobserve(el);
    });
    io.observe(rig);
  })();

  /* ══ PARALLAX · images sit further away than type ═════════════
     The law, made physical: as you move, the near layer (the copy)
     travels the full distance and the far layer (the photograph)
     travels a fraction of it. This is the one thing on the page
     that IS scroll-coupled, and deliberately so — atmosphere may
     track scroll, content still resolves once and stays put. */
  var pars = [].slice.call(document.querySelectorAll('[data-par]'));
  var parTick = false;
  function parallax(){
    parTick = false;
    var vh = window.innerHeight;
    for(var i = 0; i < pars.length; i++){
      var el = pars[i], r = el.getBoundingClientRect();
      if(r.bottom < -240 || r.top > vh + 240) continue;
      var off = ((r.top + r.height / 2) - vh / 2) / vh;      /* -1 … 1 */
      var f   = parseFloat(el.getAttribute('data-par')) || 0.12;
      el.style.transform = 'translate3d(0,' + (off * f * -100).toFixed(1) + 'px,0)';
    }
  }
  function onParScroll(){
    if(parTick) return;
    parTick = true;
    requestAnimationFrame(parallax);
  }
  if(!reduce){
    window.addEventListener('scroll', onParScroll, { passive:true });
    window.addEventListener('resize', onParScroll);
    parallax();
  }

  /* ── RAIL · the position marker. Where you are standing. ───── */
  var railPos = document.getElementById('railPos');
  function movePos(){
    if(!railPos) return;
    var h = document.documentElement.scrollHeight - window.innerHeight;
    var p = h > 0 ? Math.min(Math.max(window.scrollY / h, 0), 1) : 0;
    railPos.style.top = (p * (window.innerHeight - 46)).toFixed(1) + 'px';
  }
  window.addEventListener('scroll', movePos, { passive:true });
  window.addEventListener('resize', movePos);
  movePos();

  /* ── 05 · HOW · steps light in sequence ─────────────────────── */
  var stepsRig = document.getElementById('stepsRig');
  var stepEls  = [].slice.call(document.querySelectorAll('#stepsRig .step'));
  var TRACK_MS = 3400;                       /* must match the CSS fill */
  if(reduce || noIO){
    stepsRig.classList.add('on'); stepEls.forEach(function(s){ s.classList.add('lit'); });
  } else {
    var ioS = obs(function(el){
      el.classList.add('on');
      /* Each step lights at the instant the fill actually arrives at it,
         measured off the live layout rather than guessed at. */
      var track = el.querySelector('.track');
      var tw = track ? track.getBoundingClientRect().width : 1;
      var tl = track ? track.getBoundingClientRect().left  : 0;
      stepEls.forEach(function(st){
        var n = st.querySelector('.n');
        var r = n.getBoundingClientRect();
        var frac = Math.min(Math.max(((r.left + r.width / 2) - tl) / tw, 0), 1);
        setTimeout(function(){ st.classList.add('lit'); }, 60 + frac * TRACK_MS);
      });
      ioS.unobserve(el);
    });
    ioS.observe(stepsRig);
  }

  /* ── 06 · DISTINCTION · strike each one through ─────────────── */
  var nots = [].slice.call(document.querySelectorAll('#notsList li'));
  if(reduce || noIO){
    nots.forEach(function(n){ n.classList.add('struck'); });
  } else {
    var ioN = obs(function(el){
      nots.forEach(function(n, i){ setTimeout(function(){ n.classList.add('struck'); }, 220 + i * 430); });
      ioN.unobserve(el);
    });
    ioN.observe(document.getElementById('notsList'));
  }

  /* ── 08 · DIFFERENCE · four things lift off ─────────────────── */
  var lifts = [].slice.call(document.querySelectorAll('#liftList li'));
  if(reduce || noIO){
    lifts.forEach(function(l){ l.classList.add('lifted'); });
    var ll=document.getElementById('liftList'); if(ll) ll.classList.add('handed');
  } else {
    var ioL = obs(function(el){
      lifts.forEach(function(l, i){ setTimeout(function(){ l.classList.add('lifted'); }, 150 + i * 520); });
      /* the held edge only lights once all four are across */
      setTimeout(function(){ el.classList.add('handed'); }, 150 + (lifts.length - 1) * 520 + 1150);
      ioL.unobserve(el);
    });
    ioL.observe(document.getElementById('liftList'));
  }

  /* ── 13 · VALUES · each standard draws its line ─────────────── */
  var stds = [].slice.call(document.querySelectorAll('#stdList .std'));
  if(reduce || noIO){
    stds.forEach(function(s){ s.classList.add('held'); });
  } else {
    var ioV = obs(function(el){
      stds.forEach(function(s, i){ setTimeout(function(){ s.classList.add('held'); }, 120 + i * 250); });
      ioV.unobserve(el);
    });
    ioV.observe(document.getElementById('stdList'));
  }

  /* ── 11 · LISTEN · the transport ─────────────────────────────
     The waveform is the seek surface, not a picture of one. PEAKS are the
     real thing: short-window RMS taken off audio/reconnected-woman-summary.mp3
     with ffmpeg, 190 bars across 10:45, percentile-scaled so the loud
     syllables still move at this width instead of averaging into a band.

     preload="none" means the 5 MB file is not fetched until she asks for it,
     which also means duration is NaN until then -- so the total in the meta
     line is written into the markup and only the elapsed side is live. */
  (function listen(){
    var c = document.getElementById('wave'); if(!c) return;
    var g = c.getContext('2d'); if(!g) return;
    var a    = document.getElementById('rw-listen');
    var btn  = document.getElementById('rw-listen-play');
    var now  = document.getElementById('rw-now');
    var wrap = document.querySelector('#listen .player');

    var PEAKS = [0.378,0.16,0.317,0.562,0.347,0.518,0.675,0.495,0.581,0.362,0.343,0.16,0.166,0.35,0.334,0.16,0.32,0.305,0.322,0.302,0.16,0.31,0.526,0.207,0.515,0.345,0.598,0.334,0.212,0.315,0.681,0.522,0.685,0.536,0.465,0.31,0.556,1,0.583,0.831,0.644,0.638,0.562,0.607,0.451,0.503,0.544,0.266,0.568,0.34,0.569,0.266,0.802,0.477,0.552,0.572,0.846,0.877,0.419,0.377,0.412,0.16,0.687,0.483,0.67,0.512,0.914,0.51,0.5,0.573,0.304,0.48,0.416,0.736,0.494,0.681,0.812,0.656,0.896,1,0.68,1,0.387,0.24,0.459,0.283,0.408,0.692,0.662,0.51,0.603,0.395,0.821,0.319,0.468,0.624,0.604,0.839,0.812,0.498,0.661,0.298,0.835,0.537,0.601,0.616,0.45,0.305,0.263,0.569,0.16,0.326,0.704,1,0.661,0.414,0.673,0.413,0.288,0.313,0.38,0.457,0.394,0.416,0.293,0.35,0.503,0.392,0.425,0.216,0.16,0.64,0.637,0.429,0.536,0.652,0.488,0.563,0.372,0.83,0.589,0.559,0.533,0.213,0.344,0.388,0.227,0.615,0.454,0.586,0.333,0.546,1,0.726,0.562,0.516,0.813,0.317,1,0.484,0.552,0.255,0.997,0.322,0.537,0.225,0.332,0.19,0.337,0.49,0.3,0.258,0.389,0.458,0.367,0.36,0.416,0.237,0.274,0.19,0.184,0.298,0.16,0.467,0.461,0.346,0.428,0.389,0.481,0.411];
    var W = c.width, H = c.height, mid = H / 2, n = PEAKS.length, gap = W / n;
    var bars = [];
    for(var i = 0; i < n; i++) bars.push(Math.max(3, PEAKS[i] * H * 0.86));

    var grown = 0;      /* 0..1, the arrival draw */
    var played = 0;     /* 0..1, how far she has listened */

    function paint(){
      g.clearRect(0, 0, W, H);
      var lim = Math.floor(n * grown);
      for(var i = 0; i < lim; i++){
        g.fillStyle = (i < n * played) ? 'rgba(232,198,95,.85)' : 'rgba(177,191,215,.42)';
        g.fillRect(i * gap, mid - bars[i] / 2, Math.max(1.5, gap - 2.4), bars[i]);
      }
    }

    function fmt(s){
      if(!isFinite(s) || s < 0) s = 0;
      var m = Math.floor(s / 60), r = Math.floor(s % 60);
      return m + ':' + (r < 10 ? '0' : '') + r;
    }
    function sync(){
      var d = a ? a.duration : 0;
      played = (isFinite(d) && d > 0) ? a.currentTime / d : 0;
      if(now) now.textContent = fmt(a ? a.currentTime : 0);
      c.setAttribute('aria-valuenow', String(Math.round(played * 100)));
      paint();
    }
    function toggle(){
      if(!a) return;
      if(a.paused){ var pr = a.play(); if(pr && pr.catch) pr.catch(function(){}); }
      else { a.pause(); }
    }
    function seekTo(p){
      if(!a) return;
      var d = a.duration;
      if(!isFinite(d) || d <= 0) return;
      a.currentTime = (p < 0 ? 0 : (p > 1 ? 1 : p)) * d;
      sync();
    }

    if(btn) btn.addEventListener('click', toggle);
    if(a){
      a.addEventListener('play', function(){
        if(wrap) wrap.classList.add('is-playing');
        if(btn){ btn.innerHTML = '&#10073;&#10073;'; btn.setAttribute('aria-label', 'Pause the session summary'); }
      });
      a.addEventListener('pause', function(){
        if(wrap) wrap.classList.remove('is-playing');
        if(btn){ btn.innerHTML = '&#9654;'; btn.setAttribute('aria-label', 'Play the session summary'); }
      });
      a.addEventListener('ended', function(){
        if(wrap) wrap.classList.remove('is-playing');
        if(btn){ btn.innerHTML = '&#9654;'; btn.setAttribute('aria-label', 'Play the session summary'); }
        a.currentTime = 0; sync();
      });
      a.addEventListener('timeupdate', sync);
      a.addEventListener('loadedmetadata', sync);
    }
    c.addEventListener('click', function(e){
      var r = c.getBoundingClientRect();
      seekTo((e.clientX - r.left) / r.width);
    });
    c.addEventListener('keydown', function(e){
      if(e.key === ' ' || e.key === 'Enter'){ toggle(); e.preventDefault(); return; }
      if(!a) return;
      var d = a.duration; if(!isFinite(d) || d <= 0) return;
      if(e.key === 'ArrowRight'){ a.currentTime = Math.min(d, a.currentTime + 15); e.preventDefault(); sync(); }
      else if(e.key === 'ArrowLeft'){ a.currentTime = Math.max(0, a.currentTime - 15); e.preventDefault(); sync(); }
    });

    if(reduce || noIO){ grown = 1; paint(); return; }
    paint();
    var io = obs(function(el){
      var t0 = null;
      function step(ts){
        if(!t0) t0 = ts;
        grown = Math.min((ts - t0) / 1100, 1);
        paint();
        if(grown < 1) requestAnimationFrame(step);
      }
      requestAnimationFrame(step);
      io.unobserve(el);
    });
    io.observe(c);
    setTimeout(function(){ if(grown < 1){ grown = 1; paint(); } }, 6000);
  })();

  /* ── the open date ───────────────────────────────────────────
     One attribute, every instance. The calendar date is read straight out
     of the ISO string rather than through the viewer's clock, because a
     reader far enough west would otherwise be shown the day before. The
     countdown compares calendar days, not elapsed hours, so "Tomorrow"
     means tomorrow rather than "in 23 hours". */
  (function openDate(){
    var src = document.getElementById('rw-open'); if(!src) return;
    var at = src.getAttribute('data-open-at') || '';
    var m = at.match(/^(\d{4})-(\d{2})-(\d{2})/); if(!m) return;

    var evUTC = Date.UTC(+m[1], +m[2] - 1, +m[3]);
    var now = new Date();
    var todayUTC = Date.UTC(now.getFullYear(), now.getMonth(), now.getDate());
    var days = Math.round((evUTC - todayUTC) / 86400000);
    var past = days < 0;

    var pretty = new Date(evUTC + 43200000).toLocaleDateString('en-GB', {
      weekday:'long', day:'numeric', month:'long', timeZone:'UTC'
    });

    function set(sel, text, hideWhenPast){
      var els = document.querySelectorAll(sel);
      for(var i = 0; i < els.length; i++){
        if(past && hideWhenPast){ els[i].style.display = 'none'; continue; }
        els[i].textContent = text;
      }
    }
    var dates = document.querySelectorAll('.js-open-date');
    for(var i = 0; i < dates.length; i++){
      dates[i].textContent = past
        ? (dates[i].getAttribute('data-fallback') || 'date to be announced')
        : pretty;
    }
    set('.js-open-tz', ' \u00a0\u00b7\u00a0 ' + (src.getAttribute('data-open-tz') || ''), true);
    set('.js-open-times', src.getAttribute('data-open-times') || '', true);
    set('.js-open-count',
        days > 1 ? 'In ' + days + ' days' : days === 1 ? 'Tomorrow' : days === 0 ? 'Today' : '',
        true);
  })();

  /* ── video · load on click, not on arrival ──────────────────── */
  [].slice.call(document.querySelectorAll('.vplay')).forEach(function(btn){
    btn.addEventListener('click', function(){
      var v = btn.parentNode.querySelector('video');
      if(!v) return;
      v.setAttribute('controls','');
      btn.classList.add('gone');
      var pr = v.play();
      if(pr && pr.catch) pr.catch(function(){ btn.classList.remove('gone'); });
    });
  });


  /* ── the application modal, carried from v9 ──────────────────
     GHL's form_embed.js parks the iframe offscreen until it decides
     the form is ready, as inline styles. This modal starts hidden, so
     the script never sees it become visible and never un-parks it —
     the form loads 9999px to the left of a 76px-tall box. Un-park it
     ourselves on open, and twice more on a timer in case the script
     initialises after the first click. */
  var modal = document.getElementById('rw-apply-modal');
  if(modal){
    var mBack = modal.querySelector('.modal-backdrop');
    var mClose = modal.querySelector('.modal-close');
    function unparkForm(){
      var f = modal.querySelector('iframe'); if(!f) return;
      f.style.position='static'; f.style.left='auto'; f.style.top='auto';
      f.style.visibility='visible'; f.style.opacity='1'; f.style.pointerEvents='auto';
    }
    function openModal(){
      modal.classList.add('is-open'); document.body.style.overflow='hidden';
      unparkForm(); setTimeout(unparkForm,350); setTimeout(unparkForm,1400);
      mClose.focus();
    }
    function closeModal(){
      modal.classList.remove('is-open'); document.body.style.overflow='';
    }
    [].slice.call(document.querySelectorAll('[data-trw-modal]'))
      .forEach(function(el){ el.addEventListener('click', openModal); });
    mBack.addEventListener('click', closeModal);
    mClose.addEventListener('click', closeModal);
    document.addEventListener('keydown', function(e){
      if(e.key === 'Escape' && modal.classList.contains('is-open')) closeModal();
    });
  }

})();

  }

  var El = function () { return Reflect.construct(HTMLElement, [], El); };
  El.prototype = Object.create(HTMLElement.prototype);
  El.prototype.constructor = El;
  Object.setPrototypeOf(El, HTMLElement);

  El.prototype.connectedCallback = function () {
    if (this._rwUp) return;
    this._rwUp = true;
    addHead();
    this.innerHTML = HTML;
    script('/parallaxx-nav.js', 'rw-nav-js');
    script('/parallaxx-footer.js', 'rw-foot-js');
    script('https://link.msgsndr.com/js/form_embed.js', 'rw-ghl-js');
    try { boot(); } catch (e) { /* the page renders fully visible without it */ }
  };

  customElements.define('parallaxx-reconnected-woman', El);
})();
