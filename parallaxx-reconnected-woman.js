/* PARALLAXX TRANSFORMATIONS - The Reconnected Woman. Tag: parallaxx-reconnected-woman.
   GENERATED by build_bundle_from_preview.py from reconnected-woman-v9-preview.html,
   which is the approved design. DO NOT EDIT THIS FILE, edit that one and rerun.

   Renders into the LIGHT DOM on purpose. The stylesheet is scoped to #trw-page
   and the behaviour script uses document.querySelector, so light DOM keeps the
   accordion, the modal and the video working exactly as approved. */
(function () {
  if (customElements.get('parallaxx-reconnected-woman')) return;

  var CSS = `
  :root {
    --trw-black:      #061938;
    --trw-dark:       #04122A;
    --trw-card:       #0A1D3C;
    --trw-border:     #1B2C46;
    /* ── ALIGNED TO THE v4 SYSTEM · 14 Aug ──────────────────────────
       These were the May-era values and they are the reason this page
       and The Reconnected Man do not read as the same site. The gold was
       #D4B84A, which predates v4 by a whole revision, and the orange,
       cream and text greys were each a step off their v4 counterparts.
       Close enough to look like a mistake rather than a choice.

       THE TWO FONTS ARE SWAPPED, AND THAT IS THE LARGEST SINGLE CHANGE
       HERE. This page set Montserrat as display and Poppins as body. The
       system does the opposite: Poppins is the display face and
       Montserrat is the body. Nothing downstream needed touching -- the
       variables are named for their ROLE, so exchanging the two values
       re-cast all thirty-three usages at once. */
    --trw-gold:       #E8C65F;
    --trw-gold-light: #F3DD93;
    --trw-gold-dark:  #C9A227;
    --trw-orange:     #FF501F;
    --trw-orange-dk:  #FF6A3D;
    --trw-white:      #ffffff;
    --trw-cream:      #F1ECE1;
    --trw-muted:      #7C89A3;
    --trw-text:       #B1BFD7;
    --trw-head:       'Poppins', system-ui, sans-serif;
    --trw-body:       'Montserrat', system-ui, sans-serif;
  }

  #trw-page *, #trw-page *::before, #trw-page *::after { box-sizing: border-box; margin: 0; padding: 0; }

  #trw-page {
    background: var(--trw-black);
    color: var(--trw-text);
    font-family: var(--trw-body);
    font-size: 17px;
    line-height: 1.75;
    -webkit-font-smoothing: antialiased;
    width: 100%;
    overflow-x: clip;
  }

  /* WEIGHT COMES DOWN WITH THE FONT SWAP, or the page reads as shouting.
     700 was right for Montserrat as a display face. Poppins at 700 is a
     different animal, and the system's note on this is explicit: Poppins
     at 300 goes weak and spindly at large sizes, so display sits at
     400/500 and the tracking goes slightly negative. */
  #trw-page h1, #trw-page h2, #trw-page h3, #trw-page h4 {
    font-family: var(--trw-head);
    line-height: 1.15;
    letter-spacing: -0.018em;
    color: var(--trw-cream);
    font-weight: 400;
  }
  #trw-page #hero h1, #trw-page #cta-final h2 { font-weight: 500; }

  #trw-page p { color: var(--trw-text); font-weight: 300; }
  #trw-page strong { color: var(--trw-cream); font-weight: 600; }
  #trw-page a { color: var(--trw-gold); }
  #trw-page img { display: block; max-width: 100%; }

  #trw-page .container      { max-width: 860px;  margin: 0 auto; padding: 0 28px; }
  #trw-page .container-wide { max-width: 1080px; margin: 0 auto; padding: 0 28px; }

  #trw-page .overline {
    display: block;
    font-family: var(--trw-body);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--trw-gold);
    margin-bottom: 14px;
  }

  #trw-page .divider {
    width: 48px; height: 1px;
    background: var(--trw-gold);
    margin: 24px auto 0;
  }
  #trw-page .divider--left { margin-left: 0; }

  #trw-page section { padding: 64px 0; }

  /* BUTTONS */
  /* .px-btn, rebuilt here. A square 12px tracked-uppercase slab is the
     one component that gave this page away at a glance: the rest of the
     site uses a coral pill in sentence case with a soft coral shadow
     under it, and hover goes LIGHTER and lifts rather than darker. The
     shadow is carried at rest, not only on hover, which is what makes
     the button sit above the page instead of on it. */
  #trw-page .btn {
    display: inline-flex; align-items: center; justify-content: center; gap: .55em;
    padding: 1.05em 2.2em;
    font-family: var(--trw-body);
    font-size: .98rem;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    text-decoration: none;
    cursor: pointer;
    border: none; border-radius: 999px;
    transition: background .3s, transform .3s, box-shadow .3s;
    white-space: nowrap;
  }
  #trw-page .btn--gold {
    background: var(--trw-orange); color: var(--trw-white);
    box-shadow: 0 10px 30px -12px rgba(255,80,31,.5);
  }
  #trw-page .btn--gold:hover {
    background: var(--trw-orange-dk);
    transform: translateY(-2px);
    box-shadow: 0 16px 38px -14px rgba(255,80,31,.6);
  }
  /* .px-ghost: the system's secondary is a gold-outlined pill on navy,
     not a coral-outlined square. */
  #trw-page .btn--outline {
    background: transparent;
    color: var(--trw-text);
    border: 1px solid rgba(232,198,95,.4);
    font-weight: 600; font-size: .94rem; padding: 1em 2em;
  }
  #trw-page .btn--outline:hover { background: rgba(232,198,95,.08); border-color: var(--trw-gold); color: var(--trw-cream); }
  #trw-page .btn--full { display: block; text-align: center; width: 100%; }

  /* HERO */
  #trw-page #hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 118px 28px 72px;
    position: relative;
    overflow: hidden;
    background: var(--trw-black);
  }
  #trw-page #hero::before {
    content: '';
    position: absolute; inset: 0;
    background-image: repeating-linear-gradient(
      -45deg, transparent, transparent 56px,
      rgba(212,184,74,0.04) 56px, rgba(212,184,74,0.04) 57px
    );
    pointer-events: none;
  }
  #trw-page #hero::after {
    content: '';
    position: absolute;
    top: 40%; left: 50%;
    transform: translate(-50%, -50%);
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(212,184,74,0.07) 0%, transparent 70%);
    pointer-events: none;
  }
  #trw-page .hero-inner { position: relative; z-index: 1; }
  #trw-page .hero-kicker {
    font-size: .66rem; font-weight: 700;
    letter-spacing: .22em; text-transform: uppercase;
    color: var(--trw-gold); margin-bottom: 32px;
    animation: trwFadeUp 0.7s ease 0.1s both;
  }
  /* 800 was a Montserrat weight. At 108px in Poppins it is a wall, and
     the system runs its hero at 500 that size: composure rather than a
     shout, which is also the argument the copy is making. Cream instead
     of pure white for the same reason -- #fff on navy was the one value
     on this page that never appeared in the palette. Line-height comes
     off 0.95, which was set to stop a heavy face colliding with itself
     and now just looks cramped. */
  #trw-page #hero h1 {
    font-size: clamp(40px, 11vw, 108px);
    font-weight: 500; color: var(--trw-cream);
    line-height: 1.02; margin-bottom: 28px;
    overflow-wrap: break-word;
    animation: trwFadeUp 0.7s ease 0.25s both;
  }
  #trw-page #hero h1 .accent { color: var(--trw-gold); }
  /* THE SECOND LINE WAS THE LOUDER OF THE TWO AND IT IS THE QUIETER
     CLAIM · 14 Aug. Both lines sat at the same size and the second was
     wrapped in <strong>, so the setup read light and the payoff read
     bold -- the wrong way round for a line whose whole move is "you
     don't have to". It steps down to 0.84 of the line above and takes a
     normal weight, and holds the reader with colour instead: cream
     against the muted blue over it. #6B91AE was also off-palette, one
     of the last May-era values on the page. */
  #trw-page .hero-tagline {
    font-size: clamp(18px, 2.4vw, 22px);
    font-weight: 300; color: var(--trw-muted);
    max-width: 520px; margin: 0 auto 52px;
    letter-spacing: 0.02em;
    animation: trwFadeUp 0.7s ease 0.4s both;
  }
  #trw-page .hero-tagline strong {
    display: inline-block; margin-top: .5em;
    font-size: .84em; font-weight: 400;
    color: var(--trw-cream); letter-spacing: 0;
  }
  #trw-page #hero .btn { animation: trwFadeUp 0.7s ease 0.55s both; }
  #trw-page .hero-rule {
    margin-top: 48px; padding-top: 32px;
    border-top: 1px solid var(--trw-border);
    font-size: 12px; letter-spacing: 0.28em;
    text-transform: uppercase; color: var(--trw-muted);
    overflow-wrap: break-word;
    animation: trwFadeUp 0.7s ease 0.7s both;
  }

  @keyframes trwFadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* VSL */
  #trw-page #vsl { background: var(--trw-dark); padding: 52px 0; }
  #trw-page #vsl .section-head { text-align: center; margin-bottom: 40px; }

  /* VIDEO */
  #trw-page .video-wrap {
    background: #000;
    border: 1px solid var(--trw-border);
    overflow: hidden; position: relative;
  }
  #trw-page .video-wrap video { display: block; width: 100%; height: auto; }
  #trw-page .video-wrap--landscape { aspect-ratio: 16/9; }
  #trw-page .video-wrap--landscape video { width: 100%; height: 100%; object-fit: cover; }
  #trw-page .video-wrap--reel { aspect-ratio: 9/16; }
  #trw-page .video-wrap--reel video { width: 100%; height: 100%; object-fit: cover; }

  /* Video play overlay */
  #trw-page .video-play-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 2;
    transition: background 0.3s ease, opacity 0.4s ease;
  }
  #trw-page .video-play-overlay:hover { background: rgba(6, 25, 56, 0.18); }
  #trw-page .video-play-overlay svg {
    width: 96px; height: 96px;
    filter: drop-shadow(0 8px 24px rgba(0, 0, 0, 0.5));
    transition: transform 0.25s ease;
  }
  #trw-page .video-play-overlay:hover svg { transform: scale(1.08); }
  #trw-page .video-play-overlay.is-hidden { opacity: 0; pointer-events: none; }
  @media (max-width: 600px) {
    #trw-page .video-play-overlay svg { width: 68px; height: 68px; }
  }

  /* CRISIS */
  #trw-page #crisis {
    background: var(--trw-dark);
    border-top: 1px solid var(--trw-border);
    border-bottom: 1px solid var(--trw-border);
    text-align: center; padding: 52px 0;
  }
  #trw-page #crisis blockquote {
    font-size: clamp(20px, 3vw, 28px);
    font-weight: 300; color: var(--trw-cream);
    line-height: 1.6; max-width: 740px;
    margin: 0 auto 32px; font-style: italic;
  }
  #trw-page #crisis .stand {
    font-size: 14px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--trw-gold);
  }

  /* INITIATION */
  #trw-page #initiation { background: var(--trw-black); }
  #trw-page #initiation .section-head { text-align: center; margin-bottom: 40px; }
  #trw-page #initiation h2 { font-size: clamp(26px, 3.5vw, 40px); }

  #trw-page .archetype-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 1px; background: var(--trw-border);
    border: 1px solid var(--trw-border); margin-bottom: 40px;
  }
  #trw-page .archetype-card {
    background: var(--trw-card); padding: 28px 24px;
    position: relative; display: flex; align-items: flex-start; gap: 18px;
  }
  #trw-page .archetype-card::before {
    content: ''; position: absolute; top: 0; left: 0;
    width: 100%; height: 2px;
    background: linear-gradient(90deg, var(--trw-gold-dark), var(--trw-gold));
  }
  #trw-page .archetype-num {
    font-family: var(--trw-head); font-size: 32px; font-weight: 300;
    color: var(--trw-gold); opacity: 0.5; line-height: 1.2;
    flex-shrink: 0; min-width: 36px;
  }
  #trw-page .archetype-card p { font-size: 15px; line-height: 1.75; color: var(--trw-cream); padding-top: 4px; }
  #trw-page .archetype-body { padding-top: 4px; flex: 1; }
  #trw-page .archetype-body p { padding-top: 0; }
  #trw-page .archetype-label {
    display: block;
    font-family: var(--trw-body);
    font-size: .66rem;
    font-weight: 700;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--trw-gold);
    margin-bottom: 10px;
    line-height: 1.3;
  }

  /* DISTINCTION */
  #trw-page #distinction { background: var(--trw-black); }
  #trw-page #distinction .section-head { text-align: center; margin-bottom: 40px; }
  #trw-page #distinction h2 { font-size: clamp(26px, 3.5vw, 40px); }

  #trw-page .position-box {
    border: 1px solid var(--trw-border); border-left: 3px solid var(--trw-gold);
    background: var(--trw-card); padding: 28px 36px; text-align: center;
  }
  #trw-page .position-box p { font-size: 19px; font-weight: 300; color: var(--trw-cream); line-height: 1.7; }

  /* ENTRY */
  #trw-page #entry { background: var(--trw-dark); text-align: center; }
  #trw-page #entry h2 { font-size: clamp(26px, 3.5vw, 40px); margin-bottom: 24px; }
  #trw-page #entry .lead { font-size: 18px; max-width: 620px; margin: 0 auto 44px; line-height: 1.85; }

  /* VALUES */
  #trw-page #values { background: var(--trw-black); }
  #trw-page #values .section-head { text-align: center; margin-bottom: 40px; }
  #trw-page #values h2 { font-size: clamp(26px, 3.5vw, 40px); }

  #trw-page .values-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 16px; margin-bottom: 28px;
  }
  #trw-page .value-card {
    padding: 28px 24px 24px; border: 1px solid var(--trw-border);
    position: relative; overflow: hidden;
  }
  #trw-page .value-card::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0;
    height: 2px; background: var(--trw-gold);
    transform: scaleX(0); transform-origin: left; transition: transform 0.35s ease;
  }
  #trw-page .value-card:hover::after { transform: scaleX(1); }
  #trw-page .value-card h3 { font-size: .66rem; letter-spacing: .22em; text-transform: uppercase; color: var(--trw-gold); margin-bottom: 16px; }
  #trw-page .value-card p { font-size: 16px; }

  #trw-page .innercircle-box {
    text-align: center; padding: 28px 40px;
    border: 1px solid var(--trw-border); background: var(--trw-card);
  }
  #trw-page .innercircle-box p { font-size: 19px; font-weight: 300; color: var(--trw-cream); max-width: 600px; margin: 0 auto; }

  /* PRICING */
  #trw-page #pricing { background: var(--trw-dark); text-align: center; }
  #trw-page #pricing h2 { font-size: clamp(26px, 3.5vw, 40px); margin-bottom: 16px; }
  #trw-page #pricing .lead { font-size: 17px; max-width: 480px; margin: 0 auto; }

  #trw-page .price-card {
    max-width: 500px; margin: 52px auto 0;
    background: var(--trw-card); border: 1px solid var(--trw-border);
    position: relative; overflow: hidden;
  }
  #trw-page .price-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--trw-gold-dark), var(--trw-gold), var(--trw-gold-dark));
  }
  #trw-page .price-top { padding: 28px 36px 20px; border-bottom: 1px solid var(--trw-border); }
  #trw-page .price-amount { font-family: var(--trw-head); font-size: 80px; font-weight: 300; color: var(--trw-white); line-height: 1; }
  #trw-page .price-amount sup { font-size: 30px; vertical-align: top; margin-top: 14px; display: inline-block; color: var(--trw-gold); }
  #trw-page .price-note { font-size: 12px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--trw-muted); margin-top: 10px; }
  #trw-page .price-features { list-style: none; padding: 16px 36px; }
  #trw-page .price-features li {
    display: flex; align-items: flex-start; gap: 14px;
    padding: 11px 0; border-bottom: 1px solid var(--trw-border);
    font-size: 16px; color: var(--trw-cream);
  }
  #trw-page .price-features li:last-child { border-bottom: none; }
  #trw-page .price-features li::before { content: '\\2014'; color: var(--trw-gold); flex-shrink: 0; margin-top: 1px; }
  #trw-page .price-action { padding: 0 36px 28px; }

  /* BONUS */
  #trw-page #bonus { background: var(--trw-black); }
  #trw-page #bonus .section-head { text-align: center; margin-bottom: 32px; }
  #trw-page #bonus h2 { font-size: clamp(26px, 3.5vw, 40px); }

  #trw-page .bonus-card {
    display: grid; grid-template-columns: 200px 1fr;
    border: 1px solid var(--trw-border); overflow: hidden;
    max-width: 740px; margin: 0 auto;
  }
  #trw-page .bonus-left {
    background: var(--trw-gold); padding: 28px 20px;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center; text-align: center; gap: 6px;
  }
  #trw-page .bonus-left .tag { font-family: var(--trw-head); font-size: 11px; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(0,0,0,0.55); }
  #trw-page .bonus-left .title { font-family: var(--trw-head); font-size: 20px; font-weight: 400; color: var(--trw-black); line-height: 1.2; }
  #trw-page .bonus-right { background: var(--trw-card); padding: 24px 32px; }
  #trw-page .bonus-right h3 { font-size: 22px; color: var(--trw-white); margin-bottom: 14px; }
  #trw-page .bonus-right p { font-size: 16px; margin-bottom: 20px; }
  #trw-page .value-strip { display: flex; align-items: center; gap: 14px; }
  #trw-page .value-strip .was { font-size: 14px; color: var(--trw-muted); text-decoration: line-through; }
  #trw-page .value-strip .now { font-family: var(--trw-head); font-size: 15px; letter-spacing: 0.1em; color: var(--trw-gold); }

  /* BIO */
  #trw-page #bio { background: var(--trw-dark); }
  #trw-page .bio-grid { display: grid; grid-template-columns: 300px 1fr; gap: 48px; align-items: center; }
  #trw-page .bio-img-wrap { position: relative; }
  #trw-page .bio-img-wrap::after {
    content: ''; position: absolute; bottom: -14px; right: -14px;
    width: 55%; height: 55%; border: 1px solid var(--trw-gold);
    pointer-events: none; z-index: 0;
  }
  #trw-page .bio-img-wrap > * { position: relative; z-index: 1; }
  #trw-page .bio-text h2 { font-size: clamp(24px, 3vw, 36px); margin-bottom: 6px; }
  #trw-page .bio-role { display: block; font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--trw-gold); margin-bottom: 24px; }
  #trw-page .bio-text p { margin-bottom: 14px; font-size: 15px; line-height: 1.85; }
  #trw-page .tags { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 20px; }
  #trw-page .tag { padding: 6px 13px; border: 1px solid var(--trw-border); font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--trw-muted); }

  /* FAQ */
  #trw-page #faq { background: var(--trw-black); }
  #trw-page #faq .section-head { text-align: center; margin-bottom: 40px; }
  #trw-page #faq h2 { font-size: clamp(26px, 3.5vw, 40px); }
  #trw-page .faq-wrap { max-width: 760px; margin: 0 auto; }
  #trw-page .faq-item { border-bottom: 1px solid var(--trw-border); }
  #trw-page .faq-item:first-of-type { border-top: 1px solid var(--trw-border); }
  #trw-page .faq-btn {
    width: 100%; background: none; border: none; padding: 18px 0;
    display: flex; justify-content: space-between; align-items: center;
    gap: 24px; cursor: pointer; text-align: left;
  }
  #trw-page .faq-q { font-family: var(--trw-head); font-size: 17px; color: var(--trw-cream); letter-spacing: 0.03em; }
  #trw-page .faq-ico {
    flex-shrink: 0; width: 26px; height: 26px;
    border: 1px solid var(--trw-border); display: flex; align-items: center;
    justify-content: center; color: var(--trw-gold); font-size: 18px;
    transition: transform 0.3s ease, border-color 0.3s;
  }
  #trw-page .faq-item.open .faq-ico { transform: rotate(45deg); border-color: var(--trw-gold); }
  #trw-page .faq-body { overflow: hidden; max-height: 0; transition: max-height 0.38s ease; }
  #trw-page .faq-body-inner { padding: 0 0 26px; font-size: 16px; line-height: 1.9; color: #6B91AE; max-width: 640px; }

  /* PHILOSOPHY */
  #trw-page #philosophy { background: var(--trw-dark); text-align: center; }
  #trw-page .philosophy-inner { max-width: 660px; margin: 0 auto; }
  #trw-page .philosophy-inner .headline { font-family: var(--trw-head); font-size: clamp(32px, 5vw, 56px); color: var(--trw-white); margin-bottom: 32px; line-height: 1.05; }
  #trw-page .philosophy-inner .headline em { display: block; color: var(--trw-gold); font-style: normal; }
  #trw-page .philosophy-inner p { font-size: 17px; line-height: 1.9; margin-bottom: 18px; }
  #trw-page .philosophy-inner .closing { font-size: 18px; font-style: italic; color: var(--trw-gold); margin-top: 24px; }

  /* BRAND MARK (top-left, fixed) */
  #trw-page .trw-brand {
    position: fixed;
    top: 18px;
    left: 22px;
    z-index: 50;
    display: inline-flex;
    flex-direction: column;
    gap: 2px;
    padding: 9px 14px 8px;
    background: rgba(6,25,56,0.62);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(212,184,74,0.22);
    text-decoration: none;
    line-height: 1;
    transition: border-color 0.25s ease, background 0.25s ease, transform 0.25s ease;
  }
  #trw-page .trw-brand:hover {
    border-color: var(--trw-gold);
    background: rgba(6,25,56,0.85);
    transform: translateY(-1px);
  }
  #trw-page .trw-brand .trw-brand-name {
    font-family: var(--trw-body);
    font-size: .66rem;
    font-weight: 700;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--trw-cream);
  }
  #trw-page .trw-brand .trw-brand-sub {
    font-family: var(--trw-body);
    font-size: 8px;
    font-weight: 600;
    letter-spacing: 0.32em;
    text-transform: uppercase;
    color: var(--trw-gold);
  }
  #trw-page .trw-brand:hover .trw-brand-name { color: var(--trw-white); }

  /* FINAL CTA */
  #trw-page #cta-final { background: var(--trw-black); border-top: 1px solid var(--trw-border); text-align: center; padding: 80px 28px; }
  #trw-page #cta-final h2 { font-size: clamp(36px, 6vw, 64px); margin-bottom: 20px; }
  #trw-page #cta-final .sub { font-size: 18px; color: #6B91AE; max-width: 460px; margin: 0 auto 52px; }

  /* SCROLL ANIMATIONS */
  #trw-page .anim { opacity: 0; transform: translateY(32px); transition: opacity 0.65s ease, transform 0.65s ease; }
  #trw-page .anim.in-view { opacity: 1; transform: translateY(0); }
  #trw-page .anim-scale { opacity: 0; transform: scale(0.96); transition: opacity 0.6s ease, transform 0.6s ease; }
  #trw-page .anim-scale.in-view { opacity: 1; transform: scale(1); }
  #trw-page .anim-left { opacity: 0; transform: translateX(-32px); transition: opacity 0.65s ease, transform 0.65s ease; }
  #trw-page .anim-left.in-view { opacity: 1; transform: translateX(0); }
  #trw-page .anim-right { opacity: 0; transform: translateX(32px); transition: opacity 0.65s ease, transform 0.65s ease; }
  #trw-page .anim-right.in-view { opacity: 1; transform: translateX(0); }
  #trw-page .anim-d1 { transition-delay: 0.1s; }
  #trw-page .anim-d2 { transition-delay: 0.2s; }
  #trw-page .anim-d3 { transition-delay: 0.3s; }

  @media (prefers-reduced-motion: reduce) {
    #trw-page .anim, #trw-page .anim-scale, #trw-page .anim-left, #trw-page .anim-right,
    #trw-page .hero-kicker, #trw-page #hero h1, #trw-page .hero-tagline,
    #trw-page #hero .btn, #trw-page .hero-rule {
      animation: none; opacity: 1; transform: none; transition: none;
    }
  }

  /* MODAL */
  #trw-apply-modal { display: none; position: fixed; inset: 0; z-index: 9999; align-items: center; justify-content: center; padding: 16px; }
  #trw-apply-modal.is-open { display: flex; }
  #trw-apply-modal .modal-backdrop { position: absolute; inset: 0; background: rgba(0,0,0,0.85); backdrop-filter: blur(6px); cursor: pointer; }
  /* THE NAVY TAIL UNDER THE FORM · 14 Aug.
     The iframe carried height="1439" and the box was max-height:90vh with
     overflow-y:auto, so the box scrolled through 1439px of iframe. The form
     inside is shorter than that and its background is transparent, so
     everything past the last field was the card's own navy showing through
     the empty part of the frame: several hundred pixels of blue to scroll
     past, underneath a form that had already ended.

     Fixed by making the box a panel rather than a scroller. It is a flex
     column, it no longer scrolls itself, and the body fills whatever is
     left under the header. The iframe fills the body, so its height is the
     panel's height by construction and can never exceed its own content:
     a long form scrolls inside its own frame, which is what an embedded
     form should do anyway.

     Nothing here depends on knowing how tall the form is, and that is the
     point. That number belongs to GHL and changes whenever the form does,
     so any fix written against today's value is a fix with an expiry date
     on it. */
  /* CORRECTION · 14 Aug, second pass. Making the box a fixed-height panel
     removed the SCROLLING tail and replaced it with a standing one: the
     panel was min(90vh,880px) whatever the form measured, so a form that
     ends at 460px left 420px of navy under it with nothing to scroll.
     Same blue, now permanent.

     The box hugs the frame instead. GHL's form_embed.js posts a height
     back and writes it as an inline style on the iframe, and an inline
     style beats anything here, so the frame is the height of the form and
     the box is the height of the frame. Capped at the viewport, and the
     cap is the only case where the box scrolls: a form taller than the
     screen has to go somewhere. */
  #trw-apply-modal .modal-box {
    position: relative; z-index: 1; background: var(--trw-card);
    border: 1px solid var(--trw-border); border-top: 2px solid var(--trw-gold);
    width: 100%; max-width: 580px;
    max-height: min(92vh, 940px);
    display: flex; flex-direction: column;
    overflow-y: auto; -webkit-overflow-scrolling: touch;
  }
  #trw-apply-modal .modal-header {
    display: flex; justify-content: space-between; align-items: center;
    padding: 15px 22px; border-bottom: 1px solid var(--trw-border);
    background: var(--trw-card); z-index: 2; flex-shrink: 0;
  }
  #trw-apply-modal .modal-title { font-family: var(--trw-head); font-size: 14px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--trw-cream); }
  #trw-apply-modal .modal-close {
    background: none; border: 1px solid var(--trw-border); color: var(--trw-muted);
    width: 32px; height: 32px; display: flex; align-items: center; justify-content: center;
    cursor: pointer; font-size: 20px; line-height: 1;
    transition: color 0.2s, border-color 0.2s; flex-shrink: 0;
  }
  #trw-apply-modal .modal-close:hover { color: var(--trw-cream); border-color: var(--trw-gold); }
  /* NO height ON THE FRAME. It was height:100% in the first pass, which
     overrode nothing -- GHL sets an inline height and inline wins -- but
     it did stop the frame shrinking when there was no inline height yet,
     and it made the body claim the panel's full height regardless. The
     frame's height is GHL's to set; the attribute in the markup is only
     the value before its script runs. */
  #trw-apply-modal .modal-body { padding: 0; }
  #trw-apply-modal .modal-body iframe { display: block; width: 100%; border: none; }

  /* RESPONSIVE */
  @media (max-width: 900px) {
    #trw-page .archetype-grid { grid-template-columns: 1fr; }
    #trw-page .values-grid    { grid-template-columns: 1fr; }
    #trw-page .bonus-card     { grid-template-columns: 1fr; }
    #trw-page .bonus-left     { flex-direction: row; justify-content: center; padding: 28px; gap: 16px; }
  }
  @media (max-width: 600px) {
    #trw-page .bonus-left     { flex-direction: column; padding: 24px; gap: 8px; }
  }
  @media (max-width: 600px) {
    #trw-page { font-size: 15px; line-height: 1.65; }
    #trw-page section { padding: 56px 0 44px; }
    #trw-page #hero { min-height: auto; padding: 80px 20px 60px; }
    #trw-page .container, #trw-page .container-wide { padding: 0 20px; }
    #trw-page .hero-rule { letter-spacing: 0.08em; font-size: 10px; }
    #trw-page .hero-tagline { font-size: 15px; line-height: 1.55; margin-bottom: 40px; }
    #trw-page #hero h1 { margin-bottom: 22px; }

    #trw-page #initiation h2,
    #trw-page #distinction h2,
    #trw-page #entry h2,
    #trw-page #values h2,
    #trw-page #pricing h2,
    #trw-page #bonus h2,
    #trw-page #faq h2 { font-size: 22px; line-height: 1.2; }

    #trw-page .section-head { margin-bottom: 28px; }

    #trw-page #crisis blockquote { font-size: 17px; line-height: 1.5; margin-bottom: 22px; }
    #trw-page #crisis .stand { font-size: 10px; letter-spacing: 0.08em; }

    #trw-page .archetype-card { padding: 22px 18px; gap: 14px; }
    #trw-page .archetype-card p { font-size: 14px; line-height: 1.55; }
    #trw-page .archetype-num { font-size: 26px; min-width: 28px; }

    #trw-page .position-box { padding: 24px 20px; }
    #trw-page .position-box p { font-size: 15px; line-height: 1.55; }

    #trw-page #entry .lead { font-size: 15px; line-height: 1.65; margin-bottom: 32px; }

    #trw-page .value-card { padding: 22px 18px 20px; }
    #trw-page .value-card p { font-size: 14px; line-height: 1.55; }
    #trw-page .innercircle-box { padding: 24px 20px; }
    #trw-page .innercircle-box p { font-size: 15px; line-height: 1.55; }

    #trw-page #pricing .lead { font-size: 14px; }
    #trw-page .price-card { margin-top: 36px; }
    #trw-page .price-top { padding: 22px 24px 16px; }
    #trw-page .price-amount { font-size: 60px; }
    #trw-page .price-amount sup { font-size: 22px; margin-top: 10px; }
    #trw-page .price-features { padding: 12px 18px; }
    #trw-page .price-features li { font-size: 13px; padding: 9px 0; line-height: 1.5; text-align: left; }
    #trw-page .price-action { padding: 0 24px 22px; }

    #trw-page .bonus-right { padding: 22px 20px; }
    #trw-page .bonus-right h3 { font-size: 18px; margin-bottom: 10px; }
    #trw-page .bonus-right p { font-size: 14px; line-height: 1.55; margin-bottom: 14px; }

    #trw-page .bio-grid { grid-template-columns: 1fr; gap: 28px; }
    #trw-page .bio-img-wrap { max-width: 220px; margin: 0 auto; }
    #trw-page .bio-text h2 { font-size: 22px; }
    #trw-page .bio-text p { font-size: 14px; line-height: 1.6; }

    #trw-page .faq-btn { padding: 14px 0; }
    #trw-page .faq-q { font-size: 15px; }
    #trw-page .faq-body-inner { font-size: 14px; line-height: 1.6; padding-bottom: 20px; }

    #trw-page #philosophy .headline { font-size: 24px; line-height: 1.15; margin-bottom: 22px; }
    #trw-page #philosophy p { font-size: 15px; line-height: 1.65; margin-bottom: 14px; }
    #trw-page #philosophy .closing { font-size: 16px; }

    #trw-page .trw-brand { top: 12px; left: 14px; padding: 7px 11px 6px; }
    #trw-page .trw-brand .trw-brand-name { font-size: 11px; letter-spacing: 0.16em; }
    #trw-page .trw-brand .trw-brand-sub { font-size: 7px; letter-spacing: 0.28em; }
    #trw-page #cta-final { padding: 56px 20px; }
    #trw-page #cta-final h2 { font-size: 30px; line-height: 1.1; margin-bottom: 14px; }
    #trw-page #cta-final .sub { font-size: 14px; line-height: 1.55; margin-bottom: 36px; }

    #trw-page .btn { padding: 14px 36px; font-size: 11px; }
  }

  /* ===== v8 additions ===== */
  #trw-page #recognition { background: var(--trw-black); text-align: center; padding: 88px 0; }
  #trw-page #trap        { background: var(--trw-dark);  text-align: center; padding: 58px 0;
                           border-top: 1px solid var(--trw-border); border-bottom: 1px solid var(--trw-border); }
  #trw-page #friends     { background: var(--trw-black); text-align: center; padding: 88px 0; }
  #trw-page #difference  { background: var(--trw-dark);  text-align: center; padding: 88px 0;
                           border-top: 1px solid var(--trw-border); border-bottom: 1px solid var(--trw-border); }
  #trw-page #recognition .section-head,
  #trw-page #trap .section-head,
  #trw-page #friends .section-head,
  #trw-page #difference .section-head { text-align: center; margin-bottom: 40px; }
  #trw-page #trap .section-head { margin-bottom: 22px; }
  #trw-page #recognition h2,
  #trw-page #trap h2,
  #trw-page #friends h2,
  #trw-page #difference h2 { font-size: clamp(26px, 3.5vw, 40px); line-height: 1.15; }
  #trw-page #recognition .recog-list { text-align: left; }

  #trw-page .preframe {
    font-family: var(--trw-body); font-size: 17px; font-weight: 300;
    color: var(--trw-cream); line-height: 1.6; text-align: center;
    max-width: 720px; margin: 0 auto;
  }
  #trw-page .vsl-caption {
    font-family: var(--trw-body); font-size: 16px; font-weight: 300; font-style: italic;
    color: var(--trw-text); line-height: 1.7; text-align: center;
    max-width: 760px; margin: 34px auto 0; border-left: 2px solid var(--trw-gold);
    padding-left: 22px; text-align: left;
  }

  #trw-page .recog-list {
    max-width: 1000px; margin: 44px auto 0;
    display: grid; grid-template-columns: 1fr 1fr;
    column-gap: 48px; row-gap: 0; align-items: stretch;
  }
  #trw-page .recog-line {
    font-family: var(--trw-body); font-size: 20px; font-weight: 300;
    color: var(--trw-cream); line-height: 1.55;
    padding: 20px 0 20px 30px; border-bottom: 1px solid var(--trw-border);
    position: relative;
  }
  #trw-page .recog-line::before {
    content: ''; position: absolute; left: 0; top: 30px;
    width: 14px; height: 1px; background: var(--trw-gold);
  }
  #trw-page .recog-line:nth-last-child(-n+2) { border-bottom: none; }
  #trw-page .recog-closer {
    margin-top: 40px; text-align: center;
    font-family: var(--trw-head); font-size: 22px; font-weight: 500;
    color: var(--trw-white); line-height: 1.5;
  }

  #trw-page .trap-grid {
    display: grid; grid-template-columns: 1fr 1fr; gap: 18px;
    margin-top: 26px; max-width: 900px; margin-left: auto; margin-right: auto;
  }
  #trw-page .trap-card {
    background: var(--trw-card); border: 1px solid var(--trw-border);
    padding: 20px 24px; text-align: center;
  }
  #trw-page .trap-card h3 {
    font-family: var(--trw-head); font-size: 19px; font-weight: 600;
    color: var(--trw-white); margin-bottom: 6px; line-height: 1.3;
  }
  #trw-page .trap-card p {
    font-family: var(--trw-body); font-size: 15px; font-weight: 300;
    color: var(--trw-text); line-height: 1.5;
  }
  #trw-page #trap .pull-line { margin-top: 28px; font-size: 24px; }
  #trw-page .pull-line {
    margin-top: 44px; text-align: center;
    font-family: var(--trw-head); font-size: 28px; font-weight: 600;
    color: var(--trw-gold); line-height: 1.3;
  }

  #trw-page .chip-row {
    display: flex; flex-wrap: wrap; justify-content: center; gap: 12px;
    margin-top: 40px;
  }
  #trw-page .chip {
    font-family: var(--trw-body); font-size: 12px; font-weight: 600;
    letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--trw-cream); border: 1px solid var(--trw-border);
    background: var(--trw-card); padding: 11px 18px;
  }

  #trw-page .tile-grid {
    display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px;
    margin-top: 44px;
  }
  #trw-page .tile {
    border: 1px solid var(--trw-border); background: var(--trw-card);
    padding: 26px 24px; position: relative; overflow: hidden;
  }
  #trw-page .tile::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0;
    height: 2px; background: var(--trw-gold);
  }
  #trw-page .tile h3 {
    font-family: var(--trw-head); font-size: 17px; font-weight: 600;
    color: var(--trw-gold); margin-bottom: 10px; letter-spacing: 0.02em;
  }
  #trw-page .tile p {
    font-family: var(--trw-body); font-size: 15px; font-weight: 300;
    color: var(--trw-text); line-height: 1.6;
  }

  #trw-page .quote-big {
    font-family: var(--trw-head); font-size: 30px; font-weight: 300;
    color: var(--trw-white); line-height: 1.4; text-align: center;
    max-width: 860px; margin: 0 auto;
  }
  #trw-page .under-quote {
    font-family: var(--trw-body); font-size: 17px; font-weight: 300;
    color: var(--trw-text); line-height: 1.7; text-align: center;
    max-width: 760px; margin: 28px auto 0;
  }
  #trw-page .friends-body {
    max-width: 720px; margin: 40px auto 0; text-align: center;
  }
  #trw-page .friends-body p {
    font-family: var(--trw-body); font-size: 18px; font-weight: 300;
    color: var(--trw-cream); line-height: 1.7; margin-bottom: 18px;
  }
  #trw-page .bio-text .bio-role {
    font-family: var(--trw-head); font-size: 21px; font-weight: 500;
    letter-spacing: 0; text-transform: none; color: var(--trw-white);
    line-height: 1.4; margin-bottom: 20px; display: block;
  }
  #trw-page .cta-secondary {
    margin-top: 26px; font-family: var(--trw-body); font-size: 14px;
    font-weight: 300; color: var(--trw-text);
  }
  #trw-page .cta-secondary a { color: var(--trw-gold); text-decoration: none; border-bottom: 1px solid rgba(212,184,74,0.4); }

  /* Give the wide one-line blocks room so each sentence holds a single line. */
  #trw-page .hero-tagline { max-width: 860px; margin-bottom: 34px; }
  #trw-page #distinction .under-quote { max-width: 900px; }
  #trw-page #friends .container { max-width: 1080px; }
  #trw-page .friends-body { max-width: 1000px; }
  #trw-page .friends-body p { margin-bottom: 12px; }
  #trw-page #cta-final h2 { max-width: 900px; margin-left: auto; margin-right: auto; }

  /* Tighter vertical rhythm across the page. */
  #trw-page section { padding: 58px 0; }
  #trw-page #recognition, #trw-page #friends, #trw-page #difference { padding: 62px 0; }
  #trw-page .section-head { margin-bottom: 28px; }
  #trw-page #vsl .section-head { margin-bottom: 22px; }
  #trw-page .recog-list { margin-top: 28px; }
  #trw-page .recog-closer { margin-top: 26px; }
  #trw-page .chip-row { margin-top: 26px; }
  #trw-page .tile-grid { margin-top: 30px; }
  #trw-page .friends-body { margin-top: 24px; }
  #trw-page #friends .pull-line { margin-top: 30px; }
  #trw-page #difference .under-quote { margin-top: 20px; }

  #trw-page .trw-mt { margin-top: 30px; }
  @media (max-width: 760px) { #trw-page .trw-mt { margin-top: 20px; } }

  /* trap heading sits on one line */
  #trw-page #trap h2 { font-size: clamp(22px, 2.6vw, 32px); max-width: none; }

  /* No orphans. Balance headings and short blocks, avoid single-word last lines
     in body copy. Chrome and Safari honour both; older engines just ignore them
     and fall back to normal wrapping. */
  #trw-page h1,
  #trw-page h2,
  #trw-page h3,
  #trw-page .hero-tagline,
  #trw-page .pull-line,
  #trw-page .recog-closer,
  #trw-page .quote-big,
  #trw-page .archetype-label,
  #trw-page .bio-role,
  #trw-page .position-box p,
  #trw-page .price-note,
  #trw-page #cta-final .sub { text-wrap: balance; }

  #trw-page .recog-line,
  #trw-page .preframe,
  #trw-page .under-quote,
  #trw-page .vsl-caption,
  #trw-page .friends-body p,
  #trw-page .tile p,
  #trw-page .trap-card p,
  #trw-page .value-card p,
  #trw-page .archetype-card p,
  #trw-page .bio-text p,
  #trw-page .cta-secondary,
  #trw-page .faq-body-inner p,
  #trw-page .price-features li { text-wrap: pretty; }

  @media (max-width: 760px) {
    /* My base rules sit after the page's own media block, so mobile spacing has
       to be restated here or the desktop values win. */
    #trw-page section { padding: 44px 0; }
    #trw-page #recognition, #trw-page #trap,
    #trw-page #friends, #trw-page #difference { padding: 44px 0; }
    #trw-page #hero { padding: 74px 20px 52px; }
    #trw-page .section-head { margin-bottom: 20px; }
    #trw-page .recog-list { margin-top: 16px; }
    #trw-page .recog-closer { margin-top: 20px; }
    #trw-page .chip-row { margin-top: 20px; gap: 8px; }
    #trw-page .chip { font-size: 11px; padding: 9px 13px; letter-spacing: 0.1em; }
    #trw-page .tile-grid { margin-top: 22px; }
    #trw-page .trap-grid { margin-top: 20px; }
    #trw-page .friends-body { margin-top: 18px; }
    #trw-page #friends .pull-line,
    #trw-page #trap .pull-line { margin-top: 22px; }
    #trw-page #difference .under-quote { margin-top: 16px; }
    #trw-page .hero-tagline { max-width: 340px; margin-bottom: 28px; }
    #trw-page .cta-secondary { margin-top: 16px; }

    /* 1. hero foot: the rule and the strip were carrying desktop margins */
    #trw-page #hero { padding: 74px 20px 38px; }
    #trw-page .hero-rule { margin-top: 26px; padding-top: 20px; }

    /* 2. five chips onto two rows */
    #trw-page .chip-row { gap: 6px; }
    #trw-page .chip { font-size: 9px; padding: 7px 9px; letter-spacing: 0.04em; }

    /* 3. adjacent sections were stacking two full paddings */
    #trw-page section { padding: 36px 0; }
    #trw-page #recognition, #trw-page #trap,
    #trw-page #friends, #trw-page #difference { padding: 36px 0; }
    #trw-page #hero { padding: 74px 20px 38px; }

    /* 4. four credential tags onto two rows */
    #trw-page .bio-text .tags { gap: 6px; }
    #trw-page .bio-text .tag { font-size: 9px; padding: 6px 9px; letter-spacing: 0.03em; }

    /* 5. friends headline holds one line */
    #trw-page #friends h2 { font-size: 19px; }

    /* the two paddings plus the page's own #distinction rule were stacking */
    #trw-page #distinction { padding: 36px 0 30px; }
    #trw-page #friends { padding: 30px 0 36px; }
    #trw-page .archetype-grid { margin-bottom: 0; }
    #trw-page #recognition h2, #trw-page #trap h2,
    #trw-page #friends h2, #trw-page #difference h2 { font-size: 22px; line-height: 1.2; }
    #trw-page .trap-grid { grid-template-columns: 1fr; gap: 16px; }
    #trw-page .tile-grid { grid-template-columns: 1fr; gap: 14px; }
    #trw-page .recog-list { grid-template-columns: 1fr; column-gap: 0; max-width: 560px; }
    #trw-page .recog-line { font-size: 16px; padding: 16px 0 16px 24px; }
    #trw-page .recog-line:nth-last-child(-n+2) { border-bottom: 1px solid var(--trw-border); }
    #trw-page .recog-line:last-child { border-bottom: none; }
    #trw-page .recog-line::before { top: 25px; width: 10px; }
    #trw-page .recog-closer { font-size: 17px; }
    #trw-page .quote-big { font-size: 21px; }
    #trw-page .under-quote { font-size: 15px; }
    #trw-page .pull-line { font-size: 20px; }
    #trw-page .preframe { font-size: 15px; }
    #trw-page .vsl-caption { font-size: 14px; }
    #trw-page .friends-body p { font-size: 15px; }
    #trw-page .trap-card { padding: 26px 20px; }
    #trw-page .trap-card h3 { font-size: 18px; }
    #trw-page .bio-text .bio-role { font-size: 17px; }
  }

  /* Small phones. The block above is tuned for ~390; these hold the same line
     counts down at 360 and below. */
  @media (max-width: 380px) {
    #trw-page #friends h2 { font-size: 17px; }
    #trw-page .chip { font-size: 8.5px; padding: 7px 8px; letter-spacing: 0.03em; }
    #trw-page .bio-text .tag { font-size: 8.5px; padding: 6px 8px; letter-spacing: 0.02em; }
    #trw-page .recog-line { font-size: 15px; }
  }

  /* ---- OPEN SESSION, added after the pilot run ---- */
  #trw-page .hero-open {
    font-size: 12px; letter-spacing: 0.18em; text-transform: uppercase;
    color: var(--trw-gold); margin-top: 20px;
  }
  #trw-page .hero-open .js-open-count { color: var(--trw-muted); }

  #trw-page .step-label {
    display: block; max-width: 500px; margin: 0 auto 12px; text-align: left;
    font-size: 11px; letter-spacing: 0.24em; text-transform: uppercase;
    color: var(--trw-gold);
  }
  #trw-page .step-then {
    max-width: 500px; margin: 30px auto 12px; text-align: left;
    font-size: 11px; letter-spacing: 0.24em; text-transform: uppercase;
    color: var(--trw-muted);
  }
  #trw-page .open-card {
    max-width: 500px; margin: 0 auto; text-align: left;
    background: var(--trw-card); border: 1px solid var(--trw-border);
    position: relative; overflow: hidden;
  }
  #trw-page .open-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--trw-gold-dark), var(--trw-gold), var(--trw-gold-dark));
  }
  #trw-page .open-top {
    padding: 28px 36px 20px; border-bottom: 1px solid var(--trw-border);
    text-align: center;
  }
  /* Same correction as .price-amount. These are display figures, and the
     system sets display figures light -- the size carries the weight. */
  #trw-page .open-amount {
    font-family: var(--trw-head); font-size: 64px; font-weight: 300;
    color: var(--trw-cream); line-height: 1;
  }
  #trw-page .open-when {
    font-size: 12px; letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--trw-gold); margin-top: 12px;
  }
  #trw-page .open-count {
    display: block; font-size: 12px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--trw-muted); margin-top: 6px;
  }
  #trw-page .open-body { padding: 22px 36px 0; font-size: 16px; color: var(--trw-cream); }
  #trw-page .open-gate { padding: 14px 36px 0; font-size: 14px; color: var(--trw-muted); line-height: 1.6; }
  #trw-page .open-action { padding: 22px 36px 28px; }
  #trw-page #pricing .price-card { margin-top: 0; }

  /* Five standards, not three: 3 across then 2 across, on a 6 column base. */
  #trw-page .values-grid { grid-template-columns: repeat(6, 1fr); }
  #trw-page .values-grid > .value-card { grid-column: span 2; }
  #trw-page .values-grid > .value-card:nth-child(4),
  #trw-page .values-grid > .value-card:nth-child(5) { grid-column: span 3; }

  @media (max-width: 760px) {
    #trw-page .values-grid { grid-template-columns: 1fr; }
    #trw-page .values-grid > .value-card,
    #trw-page .values-grid > .value-card:nth-child(4),
    #trw-page .values-grid > .value-card:nth-child(5) { grid-column: 1 / -1; }
    #trw-page .hero-open { font-size: 10px; letter-spacing: 0.12em; margin-top: 16px; }
    #trw-page .hero-open .ho-time { display: none; }
    #trw-page .step-label, #trw-page .step-then { font-size: 10px; }
    #trw-page .open-top { padding: 22px 24px 16px; }
    #trw-page .open-amount { font-size: 52px; }
    #trw-page .open-when { font-size: 10px; letter-spacing: 0.08em; }
    #trw-page .open-count { font-size: 10.5px; letter-spacing: 0.14em; }
    #trw-page .open-body { padding: 18px 24px 0; font-size: 14px; }
    #trw-page .open-gate { padding: 12px 24px 0; font-size: 13px; }
    #trw-page .open-action { padding: 18px 24px 24px; }
  }

  /* ---- THE DIFFERENCE: a framed panel, not another list ----
     Everything above this section is hairline rules and bordered cards, so a
     row of items in either language reads as more of the same. This is the
     pivot of the page, so it gets a device used nowhere else: a bracketed
     panel, a glow behind the question, and outlined numerals. */
  #trw-page #difference { padding: 76px 0; }

  #trw-page .dif-eyebrow {
    display: block; font-size: 12px; letter-spacing: 0.3em; text-transform: uppercase;
    color: var(--trw-gold); margin-bottom: 30px;
  }
  #trw-page .dif-panel {
    position: relative; max-width: 940px; margin: 0 auto;
    padding: 54px 56px 48px;
    border: 1px solid rgba(212, 184, 74, 0.20);
    background:
      radial-gradient(120% 85% at 50% 0%, rgba(212, 184, 74, 0.085), rgba(212, 184, 74, 0) 62%),
      var(--trw-card);
  }
  #trw-page .dif-panel::before,
  #trw-page .dif-panel::after {
    content: ''; position: absolute; width: 46px; height: 46px;
    border: 2px solid var(--trw-gold); pointer-events: none;
  }
  #trw-page .dif-panel::before { top: -1px; left: -1px; border-right: none; border-bottom: none; }
  #trw-page .dif-panel::after { bottom: -1px; right: -1px; border-left: none; border-top: none; }

  #trw-page .dif-panel .quote-big { margin-top: 0; }
  #trw-page .dif-panel .under-quote { margin-top: 18px; }

  #trw-page .dif-set {
    display: grid; grid-template-columns: repeat(2, 1fr);
    gap: 30px 52px; margin-top: 46px; text-align: left;
  }
  #trw-page .dif-cell { display: flex; align-items: flex-start; gap: 20px; }
  #trw-page .dif-fig {
    flex: 0 0 auto; font-family: var(--trw-head); font-size: 46px; font-weight: 300;
    line-height: 0.95; letter-spacing: -0.02em;
    color: transparent; -webkit-text-stroke: 1px rgba(212, 184, 74, 0.95);
    transition: color 0.3s ease, -webkit-text-stroke-color 0.3s ease;
  }
  #trw-page .dif-cell:hover .dif-fig { color: rgba(212, 184, 74, 0.16); -webkit-text-stroke-color: var(--trw-gold); }
  #trw-page .dif-cell h3 {
    font-family: var(--trw-head); font-size: 17px; font-weight: 600;
    color: var(--trw-gold); letter-spacing: 0.02em; margin-bottom: 8px; line-height: 1.25;
  }
  #trw-page .dif-cell p { font-size: 14.5px; line-height: 1.55; color: var(--trw-text); text-wrap: pretty; }

  @media (max-width: 760px) {
    #trw-page #difference { padding: 44px 0; }
    #trw-page .dif-eyebrow { font-size: 10px; letter-spacing: 0.24em; margin-bottom: 20px; }
    #trw-page .dif-panel { padding: 34px 22px 30px; }
    #trw-page .dif-panel::before, #trw-page .dif-panel::after { width: 30px; height: 30px; }
    #trw-page .dif-set { grid-template-columns: 1fr; gap: 20px; margin-top: 28px; }
    #trw-page .dif-cell { gap: 14px; }
    #trw-page .dif-fig { font-size: 32px; -webkit-text-stroke-width: 0.9px; }
    #trw-page .dif-cell h3 { font-size: 15px; margin-bottom: 5px; }
    #trw-page .dif-cell p { font-size: 13px; }
  }

  /* ---- MOBILE TYPE PASS ----
     The phone was running body copy at desktop-ish sizes, so almost every
     line wrapped two or three deep and the page ran past 8000px. Sizes come
     down, the leading comes with them, and the padding stops carrying
     desktop values. Nothing changes above 760. */
  @media (max-width: 760px) {
    #trw-page .recog-line { font-size: 14.5px; line-height: 1.5; padding: 12px 0 12px 20px; }
    #trw-page .recog-line::before { top: 21px; width: 9px; }
    #trw-page .recog-closer { font-size: 15.5px; line-height: 1.4; margin-top: 16px; }

    #trw-page .quote-big { font-size: 18.5px; line-height: 1.3; }
    #trw-page .under-quote { font-size: 13.5px; line-height: 1.55; }
    #trw-page .pull-line { font-size: 17.5px; line-height: 1.35; }
    #trw-page .preframe { font-size: 14px; }
    #trw-page .vsl-caption { font-size: 13px; line-height: 1.6; }
    #trw-page .friends-body p { font-size: 14px; line-height: 1.55; }

    #trw-page .trap-card { padding: 20px 18px; }
    #trw-page .trap-card h3 { font-size: 16px; }
    #trw-page .trap-card p { font-size: 13.5px; }

    #trw-page .archetype-num { font-size: 22px; }
    #trw-page .archetype-card h3 { font-size: 13px; letter-spacing: 0.14em; }
    #trw-page .archetype-card p { font-size: 13.5px; line-height: 1.5; }

    #trw-page .bio-text .bio-role { font-size: 15.5px; line-height: 1.35; }
    #trw-page .bio-text p { font-size: 14px; line-height: 1.55; }

    #trw-page .values-grid { gap: 10px; }
    #trw-page .value-card { padding: 15px 16px 14px; }
    #trw-page .value-card h3 { font-size: 11.5px; letter-spacing: 0.16em; margin-bottom: 7px; }
    #trw-page .value-card p { font-size: 13.5px; line-height: 1.5; }

    #trw-page #pricing .lead { font-size: 13.5px; }
    #trw-page .open-amount { font-size: 42px; }
    #trw-page .price-amount { font-size: 52px; }
    #trw-page .price-amount sup { font-size: 19px; margin-top: 8px; }
    #trw-page .open-body { font-size: 13.5px; line-height: 1.55; }
    #trw-page .open-gate { font-size: 12.5px; line-height: 1.5; }
    #trw-page .price-features { padding: 10px 24px; }
    #trw-page .price-features li { font-size: 13.5px; padding: 9px 0; gap: 11px; }

    #trw-page .faq-q { font-size: 14px; line-height: 1.35; }
    #trw-page .faq-body-inner p { font-size: 13.5px; line-height: 1.55; }
  }

  @media (max-width: 380px) {
    #trw-page .recog-line { font-size: 13.5px; }
    #trw-page .quote-big { font-size: 17px; }
    #trw-page .value-card p, #trw-page .price-features li { font-size: 13px; }
  }


parallaxx-nav{display:block;position:relative;z-index:200}
parallaxx-footer{display:block;position:relative;z-index:2;text-align:left}

/* ═══════════════════════════════════════════════════════════════════════
   v4 ALIGNMENT LAYER · 14 Aug
   -----------------------------------------------------------------------
   The re-skin before this changed the tokens, the two fonts and the
   button, and left every section's LAYOUT exactly as it was. That was
   scoped deliberately and it was the wrong call: the ask was to align the
   designs, and a page can wear the right typeface and still be built out
   of components the rest of the site retired. The recognition list is the
   clearest case -- a two-column run of em-dashed lines with a hairline
   under each is not a pattern that exists on The Reconnected Man.

   This layer re-cuts the sections onto the patterns that page actually
   uses. It sits last in the sheet on purpose: this file has accumulated
   several rounds of mobile patches that come after the base rules, so
   anything earlier would lose to them at exactly the widths that matter.

   Copy is untouched throughout. Structure is CSS-only wherever it can be,
   which is why the numerals are counters rather than new markup.
   ═══════════════════════════════════════════════════════════════════════ */

  /* ── THE SHELL ─────────────────────────────────────────────────────
     .px-sec: the system's section is padded on all four sides and scales
     with the viewport. This page had a flat 64px vertical and relied on
     .container for the horizontal, which is why its rhythm never matched. */
  #trw-page section { padding: clamp(52px,6.5vw,92px) clamp(20px,4vw,52px); }
  #trw-page #hero { padding: clamp(90px,10vw,130px) clamp(20px,4vw,52px) clamp(52px,6.5vw,92px); }

  /* .px-label. Four different label sizes existed on this page; the
     system has one. */
  #trw-page .overline {
    font-size: .66rem; letter-spacing: .22em; font-weight: 700;
    color: var(--trw-gold); margin-bottom: 16px;
  }
  /* The centred 48px gold bar under every section head has no counterpart
     in the system, which marks sections with the label alone and saves
     rules for where they carry meaning. */
  #trw-page .divider { display: none; }
  #trw-page .section-head { margin-bottom: clamp(26px,3vw,38px); }

  /* ── HORIZON LINES ─────────────────────────────────────────────────
     Fourteen navy sections back to back is the exact fault the man's page
     was rebuilt to cure: with no change of ground the eye has nothing to
     measure progress against and the page reads as one long column. Three
     cream breaks, spaced so they land as a rhythm rather than a stripe. */
  #trw-page #distinction,
  #trw-page #bio { background: var(--trw-cream); color: #3A3630; }
  #trw-page #distinction  h1, #trw-page #distinction  h2, #trw-page #distinction  h3,
  #trw-page #bio h1, #trw-page #bio h2, #trw-page #bio h3 { color: #1E2A3D; }
  /* #A08A5E on cream is 2.83:1, and these are 10.5px tracked uppercase --
     the least legible shape text takes. The system's bronze is a rule and
     border colour; for a LABEL on cream it has to come down to ink. */
  #trw-page #distinction  .overline,
  #trw-page #bio .overline { color: #7A6742; }
  /* CONTRAST ON CREAM · corrected 14 Aug.
     Every value here was taken from the light end of the system's cream
     range at once -- #5E5850 body, a .55-alpha numeral stroke, a
     .35-alpha chip border with grey type inside it -- and the result is a
     large field where nothing has any weight and reading it is work. On
     his page #5E5850 is the SECONDARY colour and #3A3630 carries the
     body; the outlined numerals sit against a much smaller area of cream
     than they do here, so the same alpha reads as far fainter. Body goes
     to #3A3630, the numerals and the chips come up, and the ink stays
     for anything that was already ink. */
  #trw-page #distinction p, #trw-page #bio p { color: #3A3630; }
  #trw-page #distinction .under-quote {
    color: #3A3630; text-align: left; max-width: 62ch;
    margin: clamp(20px,2.4vw,28px) auto 0;
    padding: 18px 24px 18px 22px;
    border-left: 2px solid #A08A5E; border-radius: 0 14px 14px 0;
    background: linear-gradient(100deg, rgba(255,255,255,.9), rgba(255,255,255,.2) 62%, transparent);
    font-family: var(--trw-head); font-style: italic; font-weight: 300;
    font-size: clamp(16px,1.5vw,20px); line-height: 1.45;
  }
  /* The outline came off. Hollow figures worked against navy on his
     page; on cream they were the palest thing in the section. */
  #trw-page #bio .tag { color: #4A443C; border-color: rgba(160,138,94,.55); }
  /* Two elements inside the bio carry an explicit cream or white of their
     own, which beat the generic p rule above and rendered invisible the
     moment the section changed ground. Caught by walking the cream
     sections for any text lighter than its background rather than by
     eye, because this is exactly the kind of thing an eye skips. */
  #trw-page #bio .bio-role { color: #1E2A3D; }
  #trw-page #bio strong, #trw-page #bio b { color: #1E2A3D; }
  #trw-page #distinction strong { color: #1E2A3D; }

  /* ── RECOGNITION ───────────────────────────────────────────────────
     THE NUMERALS ARE GONE AND THEY WERE A MISREAD OF THE PATTERN.
     On his page the outlined figures label THREE CATEGORIES of man --
     an enumerated set, where the number is doing real work. These are
     six recognitions, and she is meant to scan them and have one or two
     land, not read a numbered indictment of six things wrong with her.
     Numbering turned a mirror into a charge sheet. Borrowing a pattern
     is not the same as borrowing its semantics.

     THIS SECTION GOES BACK TO NAVY, TOO. It was made cream for rhythm,
     which put the hardest copy on the page into the lowest-contrast
     block on the page: warm grey body text spread across a wide cream
     field, with a white wash over it that cream cannot show. Cream is
     right for the sections that explain; this one lands. Distinction and
     the bio still change ground, so the horizon lines survive.

     What is kept from the system: the hairline grid, the lit wash, the
     column rhythm, and the gold-ruled closing statement. The structure
     is the pattern. The numerals were decoration on top of it. */
  /* NO LINE OVER TWO ROWS, AND THE COLUMN COUNT IS WHAT DELIVERS IT.
     The longest recognition is 78 characters. Two lines means roughly 39
     per line, which at this size wants about 320px of text width, so a
     three-column row needs ~1240px of viewport before it can hold them.
     Below that the answer is fewer columns, not smaller type: shrinking
     the type to fit three columns is how a section ends up unreadable at
     the exact widths most people are on.

     Measured at 1600, 1360, 1240, 1100, 900, 780, 600 and 390 rather than
     reasoned about -- the wrap point moves with the font's actual metrics
     and no arithmetic here is worth trusting over the browser. */
  /* max-width on the list did nothing until this: the list sits inside
     .container, which is 860px, so three columns were 268px wide no
     matter what the list asked for. That 268 is why every measurement
     came back at three and four lines. */
  #trw-page #recognition .container { max-width: 1320px; }
  #trw-page .recog-list {
    grid-template-columns: repeat(3,1fr);
    column-gap: 0; row-gap: 0;
    border-top: 1px solid rgba(232,198,95,.22);
    max-width: 1320px; margin: clamp(26px,3vw,34px) auto 0;
  }
  @media (max-width: 1240px) {
    #trw-page .recog-list { grid-template-columns: repeat(2,1fr); max-width: 900px; }
    #trw-page .recog-line:not(:nth-child(3n+1)) { border-left: none; }
    #trw-page .recog-line:nth-child(3n+1) { padding-left: 28px; }
    #trw-page .recog-line:nth-child(3n)   { padding-right: 28px; }
    #trw-page .recog-line:nth-child(even)  { border-left: 1px solid rgba(232,198,95,.16); }
    #trw-page .recog-line:nth-child(odd)   { padding-left: 0; }
    #trw-page .recog-line:nth-child(even)  { padding-right: 0; }
    #trw-page .recog-line:nth-child(n+3)   { border-top: 1px solid rgba(232,198,95,.16); }
    #trw-page .recog-line:nth-child(n+4)::before { top: 1px; }
    #trw-page .recog-line:nth-child(odd)::before  { left: 0; }
    #trw-page .recog-line:nth-child(even)::before { left: 28px; }
  }
  @media (max-width: 780px) {
    #trw-page .recog-list { grid-template-columns: 1fr; max-width: 560px; }
    #trw-page .recog-line { border-left: none !important; padding-left: 0 !important; padding-right: 0 !important; }
    #trw-page .recog-line::before { left: 0 !important; }
    #trw-page .recog-line + .recog-line { border-top: 1px solid rgba(232,198,95,.16); }
  }
  #trw-page .recog-line {
    position: relative; isolation: isolate;
    padding: 26px 28px 26px; border-bottom: none;
    font-size: clamp(16px,1.1vw,17.5px); line-height: 1.55;
    /* Full cream, not the muted body blue. Six short lines carrying the
       whole recognition need to read at a glance, and this is the one
       block on the page where the reader is looking for herself. */
    color: var(--trw-cream); font-weight: 300;
    transition: transform .45s cubic-bezier(.2,.7,.3,1);
  }
  /* The em-dash marker goes with the numerals. A gold hairline above each
     line marks it without counting it. */
  #trw-page .recog-line::before {
    content: ""; position: absolute; top: 0; left: 28px; width: 22px; height: 2px;
    background: rgba(232,198,95,.55); border: none; border-radius: 0;
    transition: width .45s cubic-bezier(.2,.7,.3,1), background .45s ease;
  }
  #trw-page .recog-line:nth-child(3n+1)::before { left: 0; }
  #trw-page .recog-line::after {
    content: ""; position: absolute; inset: 0; z-index: -1;
    background: linear-gradient(180deg, rgba(18,35,63,.85), rgba(18,35,63,0) 76%);
    opacity: .5; transition: opacity .45s ease;
  }
  #trw-page .recog-line:hover { transform: translateY(-4px); }
  #trw-page .recog-line:hover::after { opacity: 1; }
  #trw-page .recog-line:hover::before { width: 40px; background: var(--trw-gold); }
  #trw-page .recog-line:not(:nth-child(3n+1)) { border-left: 1px solid rgba(232,198,95,.16); }
  #trw-page .recog-line:nth-child(3n+1) { padding-left: 0; }
  #trw-page .recog-line:nth-child(3n)   { padding-right: 0; }
  #trw-page .recog-line:nth-child(n+4) { border-top: 1px solid rgba(232,198,95,.16); }
  #trw-page .recog-line:nth-child(n+4)::before { top: 1px; }
  /* The closer, gold-ruled on navy rather than on cream. */
  #trw-page .recog-closer {
    text-align: left; max-width: 74ch;
    margin: clamp(26px,3vw,38px) 0 0;
    padding: 22px 28px 22px 26px;
    border-left: 2px solid var(--trw-gold); border-radius: 0 14px 14px 0;
    background: linear-gradient(100deg, rgba(18,35,63,.9), rgba(18,35,63,.35) 62%, transparent);
    box-shadow: 0 24px 48px -36px rgba(3,12,28,.9);
    font-family: var(--trw-head); font-style: italic; font-weight: 300;
    font-size: clamp(18px,1.7vw,24px); line-height: 1.4;
    color: var(--trw-cream);
  }

  /* ── DISTINCTION → THE SAME TRIPTYCH ───────────────────────────────
     The cards go. Same reason they went on his page: a rounded box with a
     border is the component the system retired, and these already carry
     their own numerals so the pattern fits without touching the markup. */
  /* The grid itself was drawing the cards' 1px separators by showing its
     own border colour through a 1px gap -- a navy sheet behind the whole
     block. On cream that sheet IS the block. Background and border off,
     gap closed, and the separators become real hairlines on the cards. */
  #trw-page .archetype-grid {
    border-top: 1px solid rgba(160,138,94,.34);
    background: none; border: none; gap: 0;
    margin-top: clamp(26px,3vw,34px); margin-bottom: 0;
  }
  /* Numeral over the text, not beside it. The card was a flex row with
     the figure in a fixed gutter, which is a card's anatomy; the pattern
     stacks them so the figure reads as the column's mark. */
  #trw-page .archetype-card {
    background: none; border: none; border-radius: 0;
    padding: 22px 26px 18px; position: relative; isolation: isolate;
    display: block;
    transition: transform .45s cubic-bezier(.2,.7,.3,1);
  }
  #trw-page .archetype-card::before {
    content: ""; position: absolute; inset: 0; z-index: -1; top: 0; left: 0;
    width: auto; height: auto; border: none; border-radius: 0;
    background: linear-gradient(180deg, rgba(255,255,255,.72), rgba(255,255,255,0) 72%);
    opacity: .55; transition: opacity .45s ease;
  }
  #trw-page .archetype-card:hover { transform: translateY(-4px); }
  #trw-page .archetype-card:hover::before { opacity: 1; }
  #trw-page .archetype-card + .archetype-card { border-left: 1px solid rgba(160,138,94,.22); }
  /* HIGHLIGHT. The section was three shades of the same warm grey and
     nothing in it asked to be looked at. Three additions, all from the
     system rather than new: a gold rule over each column, the numerals
     filled instead of hollow so they read as marks rather than ghosts,
     and the column the eye lands on lifting on hover. */
  #trw-page .archetype-card::after {
    content: ""; position: absolute; top: 0; left: 26px; width: 26px; height: 2px;
    background: rgba(160,138,94,.75);
    transition: width .45s cubic-bezier(.2,.7,.3,1), background .45s ease;
  }
  #trw-page .archetype-card:first-child::after { left: 0; }
  #trw-page .archetype-card:hover::after { width: 46px; background: #A08A5E; }
  #trw-page .archetype-num {
    font-size: clamp(30px,2.9vw,42px); font-weight: 300; opacity: 1;
    color: rgba(160,138,94,.62); margin-bottom: 10px;
  }
  @supports (-webkit-text-stroke: 1px currentColor) {
    #trw-page .archetype-num { color: transparent; -webkit-text-stroke: 1.1px rgba(160,138,94,.55); }
  }
  #trw-page .archetype-label { color: #1E2A3D; }
  #trw-page .archetype-card p { color: #5E5850; }
  /* The chips were the weakest thing in the section: #8A8073 on cream is
     about 2.4:1, which is under the floor for text this small even before
     it is set in tracked uppercase. Ink at .55 border instead. */
  #trw-page .chip {
    border-color: rgba(160,138,94,.55); color: #5E5850; background: none;
    border-radius: 999px; font-weight: 700;
  }

  /* ── VALUES → THE SPINE ────────────────────────────────────────────
     The five value cards become the lit vertical spine his page uses for
     exactly this content: a standard, stated. Name at display size rather
     than as a 13px label, a node at each one, and no boxes. Rows here, and
     columns in the two triptychs above, so no two sections repeat a shape. */
  /* THE SPLIT, same .82fr/1.12fr as his page. On a 1360 screen the spine
     alone used the left half and left the right empty, which is the
     wasted-space complaint arriving by another route. The head sits in a
     sticky rail and the five standards travel past it. */
  #trw-page #values .container-wide {
    display: grid; grid-template-columns: .88fr 1.12fr;
    gap: clamp(38px,5vw,84px); align-items: start; max-width: 1180px;
  }
  #trw-page #values .section-head {
    position: sticky; top: clamp(88px,12vh,132px);
    text-align: left; margin-bottom: 0;
  }
  #trw-page #values .section-head h2 { text-align: left; }
  @media (max-width: 900px) {
    #trw-page #values .container-wide { grid-template-columns: 1fr; gap: 26px; }
    #trw-page #values .section-head { position: static; }
  }
  #trw-page .values-grid {
    display: block; position: relative;
    margin-top: clamp(26px,3vw,34px);
    padding-left: clamp(30px,4vw,52px);
  }
  #trw-page .values-grid::before {
    content: ""; position: absolute; left: 0; top: .7em; bottom: 16%;
    width: 2px;
    background: linear-gradient(180deg, rgba(232,198,95,.85), rgba(232,198,95,.32) 58%, rgba(232,198,95,0));
  }
  #trw-page .value-card {
    background: none; border: none; border-radius: 0;
    padding: 0 0 clamp(16px,1.7vw,22px); position: relative;
    /* The card was overflow:hidden to clip its old underline sweep. The
       node hangs 57px outside the card onto the spine, so hidden ate it. */
    overflow: visible;
  }
  #trw-page .value-card:last-child { padding-bottom: 0; }
  #trw-page .value-card::after {
    content: ""; position: absolute;
    left: calc(-1 * clamp(30px,4vw,52px) - 4px); top: .38em;
    width: 10px; height: 10px; border-radius: 50%;
    background: var(--trw-black); border: 2px solid var(--trw-gold);
    box-shadow: 0 0 0 5px rgba(232,198,95,.10), 0 0 18px rgba(232,198,95,.45);
    transform: none; transition: transform .5s cubic-bezier(.2,.7,.3,1), box-shadow .5s ease;
  }
  #trw-page .value-card:hover::after {
    transform: scale(1.18);
    box-shadow: 0 0 0 7px rgba(232,198,95,.14), 0 0 26px rgba(232,198,95,.7);
  }
  /* DOWN FROM 44px. Display size was right on his page, where the three
     values carry a section on their own. Five short standards at 44px
     turn a list into a full screen of scrolling for about twenty words,
     and the size stopped being emphasis and became volume. 30px still
     reads as display against 15px body -- the ratio does the work, not
     the absolute. Spacing and the node come down with it. */
  #trw-page .value-card h3 {
    font-family: var(--trw-head);
    font-size: clamp(21px,2.1vw,30px); font-weight: 300;
    letter-spacing: -.015em; line-height: 1.1; text-transform: none;
    color: var(--trw-gold); margin: 0 0 10px;
    transition: transform .55s cubic-bezier(.2,.7,.3,1);
  }
  #trw-page .value-card:hover h3 { transform: translateX(7px); }
  #trw-page .value-card p {
    font-size: clamp(14px,1vw,15.5px); line-height: 1.65;
    color: #9FB0CB; max-width: 44ch;
  }

  /* ── FAQ ───────────────────────────────────────────────────────────
     The system's accordion: coral ring that rotates to a cross, cream
     hairlines at .12, and the question in body weight rather than heavy. */
  #trw-page .faq-ico { border-color: rgba(255,80,31,.4); color: var(--trw-orange); }
  #trw-page .faq-item.open .faq-ico,
  #trw-page .faq-item.is-open .faq-ico { border-color: var(--trw-orange); background: rgba(255,80,31,.12); }


  /* ── PRICING: ONE CARD ─────────────────────────────────────────────
     The inclusions move inside the open-session card and the price
     becomes a line under the button. .price-card, .price-top,
     .price-amount, .step-label and .step-then are unused now; their
     rules are left in place rather than hunted through five media
     blocks, since nothing renders them. */
  #trw-page .open-card { max-width: 620px; margin: clamp(26px,3vw,36px) auto 0; }
  #trw-page .open-card .price-features { padding: 4px 0 2px; margin: 0; }
  #trw-page .open-card .price-features li { border-bottom-color: rgba(232,198,95,.14); }
  #trw-page .open-card .price-features li:last-child { border-bottom: none; }
  #trw-page .open-action { margin-top: clamp(18px,2vw,24px); }
  #trw-page .open-gate { margin-top: 14px; font-size: .8rem; line-height: 1.55; text-align: center; }
  /* The price is the consequence of saying yes, not a second offer, so
     it sits at footnote size under the button in the muted grey rather
     than at 80px in its own card. */
  #trw-page .price-after {
    margin-top: 8px; text-align: center;
    font-size: .84rem; line-height: 1.6; color: var(--trw-muted);
  }
  #trw-page .price-after strong { color: var(--trw-cream); font-weight: 600; }

  /* The inclusions sit on the card's own 36px gutter rather than flush to
     its edge, and the card carries the bottom padding the footnote was
     standing on. Before this the last line touched the border. */
  #trw-page .open-card { padding-bottom: clamp(20px,2.2vw,26px); }
  #trw-page .open-card .price-features { padding: 18px 36px 2px; }
  #trw-page .open-card .open-action { padding: 0 36px; margin-top: clamp(16px,1.6vw,20px); }
  #trw-page .open-card .price-after { padding: 0 36px; }
  @media (max-width: 700px) {
    #trw-page .open-card .price-features,
    #trw-page .open-card .open-action,
    #trw-page .open-card .price-after { padding-left: 24px; padding-right: 24px; }
  }

  /* THE MARKER IS DRAWN, NOT TYPED. It was an em dash, which is banned in
     copy and read as one here even though it is a bullet. This is the
     man's ticket marker: a coral dot with a soft ring, set on the cap
     height of the first line rather than the baseline. */
  #trw-page .open-card .price-features li { position: relative; display: block; padding-left: 22px; }
  #trw-page .open-card .price-features li::before {
    content: ''; position: absolute; left: 0; top: .95em;
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--trw-orange); box-shadow: 0 0 0 3px rgba(255,80,31,.14);
    margin: 0; transition: box-shadow .4s ease;
  }
  #trw-page .open-card .price-features li:hover::before { box-shadow: 0 0 0 5px rgba(255,80,31,.2); }

  /* ── INTERACTION LAYER ─────────────────────────────────────────────
     Light, not motion. Everything here is a shadow or a colour change
     on the six things that are actually pressable, and the transforms
     that already existed keep the distances they had. Nothing new moves
     more than 2px, because a page that lifts everywhere reads as a demo
     rather than as a product.

     All of it sits behind (hover:hover) and (pointer:fine), so a phone
     never inherits a sticky :hover state from a tap, and all of it drops
     under prefers-reduced-motion. */
  @media (hover: hover) and (pointer: fine) {

    /* THE PRIMARY. The bloom is a third shadow on the same element, so
       there is no pseudo and no stacking context to get wrong: the drop
       stays, a 1px warm rim appears at the edge, and a wide soft coral
       haze opens behind it. The scale is 1.2%, which reads as pressure
       rather than as growth. */
    #trw-page .btn--gold:hover {
      transform: translateY(-2px) scale(1.012);
      box-shadow:
        0 16px 38px -14px rgba(255,80,31,.62),
        0 0 0 1px rgba(255,138,90,.34),
        0 0 30px rgba(255,80,31,.34);
    }
    #trw-page .btn--gold:active { transform: translateY(0) scale(.994); }

    /* THE SECONDARY glows in gold rather than coral, so the two buttons
       never compete for the same eye. */
    #trw-page .btn--outline:hover {
      box-shadow: 0 0 0 1px rgba(232,198,95,.34), 0 0 22px rgba(232,198,95,.16);
    }

    /* THE CARD. No lift: it is 620px of content and moving it moves the
       whole section. It warms at the edge instead, and the gold top rule
       is what carries the change. */
    #trw-page .open-card { transition: box-shadow .45s ease, border-color .45s ease; }
    #trw-page .open-card:hover {
      box-shadow: 0 34px 74px -46px rgba(0,0,0,.95), 0 0 0 1px rgba(232,198,95,.20);
    }
    #trw-page .open-card .price-features li:hover::before {
      box-shadow: 0 0 0 5px rgba(255,80,31,.2), 0 0 14px rgba(255,80,31,.6);
    }

    /* THE FAQ. Ten rows with no hover state at all read as text; the ring
       is the affordance, so it is the ring that answers. */
    #trw-page .faq-btn .faq-q { transition: color .28s ease; }
    #trw-page .faq-btn:hover .faq-q { color: var(--trw-gold-light); }
    #trw-page .faq-btn:hover .faq-ico {
      border-color: var(--trw-orange);
      background: rgba(255,80,31,.10);
      box-shadow: 0 0 16px rgba(255,80,31,.28);
    }

    /* THE PLAY BUTTON gets the same coral halo as the primary, since it
       is the primary of its own section. */
    #trw-page .video-play-overlay svg { transition: transform .25s ease, filter .3s ease; }
    #trw-page .video-play-overlay:hover svg {
      filter: drop-shadow(0 8px 24px rgba(0,0,0,.5)) drop-shadow(0 0 18px rgba(255,80,31,.5));
    }

    /* THE RECOGNITION LINES already lift 4px and grow their rule. Colour
       is not available here: they are already full cream, and dimming the
       resting state to make room for a hover would undo the contrast pass.
       So the hover is light rather than ink, a soft gold bloom behind the
       line she is on. */
    #trw-page .recog-line { transition: transform .45s cubic-bezier(.2,.7,.3,1), text-shadow .35s ease; }
    #trw-page .recog-line:hover { text-shadow: 0 0 24px rgba(232,198,95,.28); }

    /* THE DISTINCTION COLUMNS. The numeral already fills on hover; the
       gold rule under it now carries a short glow so the column reads as
       one object rather than as two effects. */
    #trw-page .archetype-card:hover::after { box-shadow: 0 0 14px rgba(232,198,95,.45); }
  }

  @media (prefers-reduced-motion: reduce) {
    #trw-page .btn--gold:hover,
    #trw-page .btn--gold:active,
    #trw-page .recog-line:hover,
    #trw-page .archetype-card:hover,
    #trw-page .value-card:hover h3,
    #trw-page .video-play-overlay:hover svg { transform: none; }
  }



  /* ══════════════ v10 additions · 2 September 2026 ══════════════════════
     Four new sections and one new block inside the offer card. Nothing
     here invents a component: The Promise and One Intention share a
     single device used exactly twice, and How It Works and After Four
     Weeks both reuse .archetype-grid, the numbered card row that What
     This Isn't already uses. Two identical grids in one scroll is a
     rhythm; three different ones is a page that cannot decide.
     ═══════════════════════════════════════════════════════════════════ */

  /* THE PROMISE and ONE INTENTION. One display sentence, one lighter
     line under it. The Promise sits between the hero and the VSL, so it
     runs on black and leans on the hero above it rather than opening
     with its own eyebrow. One Intention sits between two dark sections,
     so it takes the bracketed treatment The Trap uses. */
  #trw-page #promise   { background: var(--trw-black); text-align: center; padding: 60px 0 56px; }
  #trw-page #intention { background: var(--trw-dark); text-align: center; padding: 64px 0;
                         border-top: 1px solid var(--trw-border);
                         border-bottom: 1px solid var(--trw-border); }
  #trw-page .promise-line {
    font-family: var(--trw-head); font-size: clamp(25px, 3.8vw, 40px); font-weight: 400;
    color: var(--trw-cream); line-height: 1.18; letter-spacing: -0.018em;
    max-width: 800px; margin: 0 auto; text-wrap: balance;
  }
  #trw-page .promise-sub {
    font-family: var(--trw-head); font-size: clamp(16px, 1.9vw, 21px); font-weight: 300;
    color: var(--trw-muted); line-height: 1.4; margin: 18px auto 0; max-width: 640px;
  }
  #trw-page #intention .overline { margin-bottom: 18px; }

  /* HOW IT WORKS · THE ROUTE ROW.
     Not .archetype-grid. That component reads as navy at the top of this
     file and is re-themed for cream several hundred lines further down,
     where What This Isn't and the bio now live -- so on navy it renders
     as a white card wash with bronze numerals and ink labels, which is
     to say invisible. This is the same anatomy for the ground it is
     actually on: a hairline over the row, a gold segment over each
     column, an outlined numeral, a tracked label. Same structure, other
     side of the horizon line. */
  #trw-page #how { background: var(--trw-black); padding: 72px 0;
                   border-bottom: 1px solid var(--trw-border); }
  #trw-page #how .section-head { text-align: center; margin-bottom: 6px; }
  #trw-page #how .pull-line { margin-top: 38px; }
  #trw-page .route-row {
    list-style: none; margin: clamp(26px,3vw,34px) 0 0; padding: 0;
    display: grid; grid-template-columns: repeat(3, 1fr);
    border-top: 1px solid rgba(232,198,95,.26);
  }
  #trw-page .route-step {
    position: relative; padding: 24px 26px 6px;
    transition: transform .45s cubic-bezier(.2,.7,.3,1);
  }
  #trw-page .route-step + .route-step { border-left: 1px solid rgba(232,198,95,.14); }
  #trw-page .route-step::after {
    content: ""; position: absolute; top: 0; left: 26px; width: 26px; height: 2px;
    background: rgba(232,198,95,.7);
    transition: width .45s cubic-bezier(.2,.7,.3,1), background .45s ease;
  }
  #trw-page .route-step:first-child::after { left: 0; }
  #trw-page .route-step:hover { transform: translateY(-4px); }
  #trw-page .route-step:hover::after { width: 46px; background: var(--trw-gold); }
  #trw-page .route-fig {
    display: block; font-family: var(--trw-head);
    font-size: clamp(30px,2.9vw,42px); font-weight: 300; line-height: 1.1;
    color: rgba(232,198,95,.5); margin-bottom: 10px;
  }
  @supports (-webkit-text-stroke: 1px currentColor) {
    #trw-page .route-fig { color: transparent; -webkit-text-stroke: 1.1px rgba(232,198,95,.5); }
  }
  #trw-page .route-step h3 {
    font-family: var(--trw-body); font-size: .7rem; font-weight: 700;
    letter-spacing: .2em; text-transform: uppercase; line-height: 1.35;
    color: var(--trw-gold); margin: 0 0 10px;
  }
  #trw-page .route-step p { font-size: 15px; line-height: 1.7; color: var(--trw-text); }
  #trw-page .how-system {
    margin-top: 22px; text-align: center;
    font-family: var(--trw-body); font-size: 12px; font-weight: 600;
    letter-spacing: 0.16em; text-transform: uppercase; color: var(--trw-muted);
  }

  /* AFTER FOUR WEEKS · THE SPINE.
     The Standard's lit vertical spine, reused. It is the current navy
     component for a list of stated things, and it carries no numerals --
     which is right here, because these are three parallel ways on rather
     than three ordered steps. The offer sits between the two spines. */
  #trw-page #after { background: var(--trw-black); padding: 72px 0;
                     border-bottom: 1px solid var(--trw-border); }
  #trw-page #after .section-head { text-align: center; margin-bottom: 6px; }
  #trw-page #after h2 { font-size: clamp(23px, 3vw, 34px); }
  #trw-page #after .values-grid { max-width: 620px; margin-left: auto; margin-right: auto; }
  #trw-page #after .value-card h3 { font-size: clamp(19px,1.8vw,26px); }

  /* THE FOUR WEEKS, INSIDE THE OPEN CARD. The open session is the only
     decision on this page, so what happens if she stays is stated here,
     above the inclusions, rather than as a second offer with a headline
     of its own. */
  #trw-page .open-stay { padding: 22px 36px 0; text-align: center; }
  #trw-page .open-stay-h {
    font-family: var(--trw-head); font-size: 21px; font-weight: 400;
    color: var(--trw-cream); line-height: 1.25;
  }
  #trw-page .open-stay-s {
    font-family: var(--trw-body); font-size: 14px; font-weight: 500;
    color: var(--trw-gold); line-height: 1.5; margin-top: 8px;
  }
  /* The lead grew from two short lines to one sentence. 480px broke it
     into four. */
  #trw-page #pricing .lead { max-width: 580px; }
  #trw-page .pa-roll { display: block; margin-top: 7px; color: var(--trw-muted); }

  /* LISTEN · A TRANSPORT, NOT <audio controls>.
     The native widget is a light grey pill that Chrome, Safari and Firefox
     each draw differently and none of them let you restyle past the edges.
     On this ground it reads as a browser artefact dropped into the page.
     The page already has a play affordance -- the gold ring over both
     videos -- so the audio borrows it, and the section stops announcing
     that it is a different kind of media from the two above it. */
  #trw-page #listen {
    background: var(--trw-dark); padding: 72px 0;
    border-top: 1px solid var(--trw-border);
    border-bottom: 1px solid var(--trw-border);
  }
  #trw-page #listen .section-head { text-align: center; margin-bottom: 16px; }
  #trw-page #listen h2 { font-size: clamp(24px,3.2vw,38px); }
  #trw-page #listen .preframe { color: var(--trw-muted); font-size: 16px; }

  #trw-page .aud {
    margin: clamp(26px,3vw,34px) 0 0;
    background: var(--trw-card); border: 1px solid var(--trw-border);
    padding: clamp(20px,2.4vw,26px);
    position: relative; overflow: hidden;
  }
  #trw-page .aud::before {
    content: ""; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--trw-gold-dark), var(--trw-gold), var(--trw-gold-dark));
  }
  #trw-page .aud-row { display: flex; align-items: center; gap: clamp(16px,2vw,22px); }
  #trw-page .aud-play {
    flex: 0 0 auto; width: 62px; height: 62px; padding: 0;
    background: none; border: none; border-radius: 50%; cursor: pointer;
    transition: transform .3s ease;
  }
  #trw-page .aud-play svg { display: block; width: 100%; height: 100%; }
  #trw-page .aud-play:hover { transform: scale(1.06); }
  #trw-page .aud-play:focus-visible { outline: 2px solid var(--trw-gold); outline-offset: 3px; }
  /* Pause bars swap in for the triangle while it plays, so the one control
     says what it will do next rather than what it is. */
  #trw-page .aud .aud-bars { display: none; }
  #trw-page .aud.is-playing .aud-tri { display: none; }
  #trw-page .aud.is-playing .aud-bars { display: block; }

  #trw-page .aud-body { flex: 1 1 auto; min-width: 0; }
  #trw-page .aud-title {
    font-family: var(--trw-body); font-size: 14px; font-weight: 500;
    color: var(--trw-cream); line-height: 1.45; margin-bottom: 6px;
  }
  /* THE BAR IS 4px. THE THING YOU CLICK IS 22px.
     Drawn at 4 and hit at 22, with the rail as a ::before rather than the
     element's own background. A 4px-tall seek target is roughly a fifth of
     the 24px minimum a thumb can reliably land on, and in a headless click
     test at the midpoint it seeked to 0:00 -- which is what it would do to
     anyone on a phone trying to skip forward. */
  #trw-page .aud-track {
    position: relative; height: 22px; cursor: pointer; background: none;
  }
  #trw-page .aud-track::before {
    content: ""; position: absolute; left: 0; right: 0; top: 50%;
    transform: translateY(-50%); height: 4px; border-radius: 2px;
    background: rgba(232,198,95,.16);
  }
  #trw-page .aud-track:focus-visible { outline: 2px solid var(--trw-gold); outline-offset: 2px; }
  #trw-page .aud-fill {
    position: absolute; left: 0; top: 50%; transform: translateY(-50%);
    height: 4px; width: 0; border-radius: 2px; background: var(--trw-gold);
  }
  /* The play head, so the bar says where you are and not only how far. */
  #trw-page .aud-fill::after {
    content: ""; position: absolute; right: -4px; top: 50%;
    transform: translateY(-50%); width: 9px; height: 9px; border-radius: 50%;
    background: var(--trw-gold); box-shadow: 0 0 0 4px rgba(232,198,95,.16);
    opacity: 0; transition: opacity .25s ease;
  }
  #trw-page .aud.is-playing .aud-fill::after,
  #trw-page .aud-track:hover .aud-fill::after { opacity: 1; }
  #trw-page .aud-meta {
    font-family: var(--trw-body); font-size: 11px; font-weight: 600;
    letter-spacing: .16em; text-transform: uppercase;
    color: var(--trw-muted); margin-top: 6px;
  }
  #trw-page #listen .vsl-caption cite {
    display: block; margin-top: 10px; font-style: normal;
    font-family: var(--trw-body); font-size: 10.5px; font-weight: 700;
    letter-spacing: .18em; text-transform: uppercase; color: var(--trw-muted);
  }

  /* THE CLOSE IS NOW A SENTENCE, not three words, and 64px put it on
     four lines on a laptop. */
  #trw-page #cta-final h2 { font-size: clamp(27px, 4vw, 44px); line-height: 1.16; }

  @media (max-width: 760px) {
    #trw-page #promise, #trw-page #intention { padding: 42px 0; }
    #trw-page #how, #trw-page #after { padding: 46px 0; }
    #trw-page .route-row { grid-template-columns: 1fr; }
    #trw-page .route-step { padding: 20px 0 6px; }
    #trw-page .route-step + .route-step { border-left: none; }
    #trw-page .route-step::after { left: 0; }
    #trw-page .route-fig { font-size: 30px; }
    #trw-page .open-stay { padding: 18px 24px 0; }
    #trw-page .open-stay-h { font-size: 16.5px; }
    #trw-page .open-stay-s { font-size: 13px; }
    #trw-page .how-system { font-size: 10.5px; letter-spacing: 0.1em; }
  }
  @media (max-width: 560px) {
    #trw-page .aud-row { gap: 14px; }
    #trw-page .aud-play { width: 52px; height: 52px; }
    #trw-page .aud-title { font-size: 13px; }
    #trw-page .aud-meta { font-size: 10px; letter-spacing: .1em; }
    #trw-page #cta-final h2 { font-size: 23px; }
    #trw-page .promise-line { font-size: 20px; }
    #trw-page .promise-sub { font-size: 14.5px; }
  }


  /* ══════════════ THE PHONE PASS · 2 September 2026 ═══════════════════
     LAST IN THE FILE ON PURPOSE, and it has to stay last. Almost every
     rule below is a size that some earlier block also sets, at equal
     specificity, so order is what decides them. Move this up and half of
     it stops applying with no error and no visible clue.

     WHAT IT IS FIXING. Read at 390, the same job was being done at six
     different sizes: .dif-cell p at 13, .trap-card and .archetype-card p
     at 13.5, .bio-text and .value-card p at 14, .route-step p at 15,
     .bio-role at 15.5, .recog-line and .under-quote at 16. Nothing chose
     that spread -- it is where each section happened to land, and the
     large end is simply the sections nobody had opened on a phone.

     THE SCALE. Body 14. Section heads 20. The raised gold lines 17.
     Numerals and the price keep their jobs as figures. Nothing here
     changes a desktop value.
     ═══════════════════════════════════════════════════════════════════ */
  @media (max-width: 560px) {

    /* VERTICAL. Recognition, The Trap, What This Isn't and Friends were
       tuned already and keep exactly what they were given -- 30 to 36.
       These are the ten that were still carrying the desktop 52. */
    #trw-page #promise, #trw-page #vsl, #trw-page #how,
    #trw-page #bio, #trw-page #listen, #trw-page #values,
    #trw-page #intention, #trw-page #pricing, #trw-page #after,
    #trw-page #faq { padding-top: 38px; padding-bottom: 38px; }
    #trw-page #difference { padding-top: 40px; padding-bottom: 40px; }

    /* THE HERO. 43px over three lines was most of the first screen
       before a word of the argument. */
    #trw-page #hero h1 { font-size: 37px; }
    /* "IN 13 DAYS" was breaking after the 13. */
    #trw-page .hero-open .js-open-count { white-space: nowrap; }

    /* SECTION HEADS, one size across all eleven. */
    #trw-page #recognition h2, #trw-page #trap h2, #trw-page #friends h2,
    #trw-page #difference h2, #trw-page #distinction h2, #trw-page #values h2,
    #trw-page #pricing h2, #trw-page #faq h2, #trw-page #bio h2,
    #trw-page #listen h2, #trw-page #after h2 { font-size: 20px; line-height: 1.2; }

    /* BODY, down to 14 wherever it was 15, 15.5 or 16. */
    #trw-page .route-step p,
    #trw-page #listen .preframe,
    #trw-page #distinction .under-quote { font-size: 14px; }
    #trw-page .recog-line { font-size: 14.5px; }
    #trw-page .bio-text .bio-role { font-size: 14.5px; }
    #trw-page .faq-q { font-size: 13.5px; }
    #trw-page .trap-card h3 { font-size: 15px; }

    /* THE RAISED LINES. "So this is about doing different" was 24px --
       the loudest thing on the phone by six points -- while the identical
       device two sections later ran at 17.5. Same device, same size. */
    #trw-page .pull-line,
    #trw-page #trap .pull-line { font-size: 17px; }
    #trw-page .recog-closer { font-size: 16.5px; }
    #trw-page .quote-big { font-size: 17px; }

    /* THE TWO SPINES, one size. */
    #trw-page .value-card h3,
    #trw-page #after .value-card h3 { font-size: 18.5px; }

    /* THE OFFER. The figure stays a figure, just a smaller one. */
    #trw-page .open-amount { font-size: 36px; }
    /* The date and the times break as two whole units or not at all. At
       390 the line was one character too long and dropped GMT+8 onto a
       line of its own, which reads as a third time zone. */
    #trw-page .open-when .ow-nw { white-space: nowrap; }
  }

#trw-page .anim, #trw-page .anim-scale, #trw-page .anim-left, #trw-page .anim-right,
#trw-page .hero-kicker, #trw-page #hero h1, #trw-page .hero-tagline,
#trw-page #hero .btn, #trw-page .hero-rule {
  opacity: 1 !important; transform: none !important; animation: none !important;
}

/* THE ENDLESS SCROLL. Wix measures the custom element and writes the result
   back as a min-height on the element itself, so the element's height feeds
   its own measurement and the page grew without bound: 11,456,701px of empty
   navy under the footer. Pinning the host to its content breaks the loop.
   Measured on the live page: 11,456,701px -> 8,710px. */
parallaxx-reconnected-woman {
  display: block !important;
  height: auto !important;
  min-height: 0 !important;
  max-height: none !important;
}
`;
  var HTML = `<div id="trw-page" data-open-at="2026-09-15T08:30:00+02:00">
<parallaxx-nav active="women"></parallaxx-nav>


<section id="hero">
  <div class="hero-inner">
    <h1>The<br><span class="accent">Reconnected</span><br>Woman</h1>
    <p class="hero-tagline">You've competed with men. Led men. Carried men.<br><strong>The masculine is provided. You don't have to, for a change.</strong></p>
    <button class="btn btn--gold" data-trw-modal>Come To The Open Session</button>
    <p class="hero-open">Next open session &nbsp;&middot;&nbsp; Tuesday 15 September <span class="ho-time">&nbsp;&middot;&nbsp; 08:30 CET</span> &nbsp;&middot;&nbsp; <span class="js-open-count"></span></p>
  </div>
</section>


<section id="promise">
  <div class="container">
    <p class="promise-line anim">Work out what you want, and ask for it in four weeks.</p>
    <p class="promise-sub anim anim-d1">Without slowing down or doing less.</p>
  </div>
</section>


<section id="vsl">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">Watch This First</span>
      <div class="divider"></div>
    </div>
    <p class="preframe anim anim-d1" style="margin-top:0;">Nobody here is going to teach you how to be a woman.</p>
    <div class="video-wrap video-wrap--landscape anim anim-d1" style="margin-top:40px;">
      <video id="trw-vsl-video" playsinline preload="metadata">
        <source src="https://video.wixstatic.com/video/111174_c76bb418f85d49579bd90c14e6addf79/1080p/mp4/file.mp4" type="video/mp4">
      </video>
      <button class="video-play-overlay" id="trw-vsl-play" type="button" aria-label="Play video">
        <svg viewBox="0 0 96 96" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <circle cx="48" cy="48" r="46" fill="rgba(6,25,56,0.85)" stroke="#E8C65F" stroke-width="2"/>
          <polygon points="40,30 68,48 40,66" fill="#E8C65F"/>
        </svg>
      </button>
    </div>
    <p class="vsl-caption anim anim-d2">"I've got no interest in you becoming more feminine, or more spiritual, or more of anything. I want to see you being more of you."</p>
  </div>
</section>


<section id="recognition">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">Sounds Like You?</span>
      <div class="divider"></div>
    </div>
    <div class="recog-list">
      <p class="recog-line anim">You said yes again this week. There was no room for it.</p>
      <p class="recog-line anim">You're the one everyone talks to. Nobody asks about you.</p>
      <p class="recog-line anim">You hit the target. The target moves.</p>
      <p class="recog-line anim">You cancelled on someone who matters. Again.</p>
      <p class="recog-line anim">You're holding it together for more people than you'd admit to.</p>
      <p class="recog-line anim">You look at the men around you and not one of them is carrying anything for you.</p>
    </div>
    <p class="recog-closer anim">You're winning. You're delivering.<br>And you're the only one who feels what it costs.</p>
  </div>
</section>


<section id="trap">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">The Trap</span>
      <h2>You've had two options. Neither works.</h2>
      <div class="divider"></div>
    </div>
    <div class="trap-grid">
      <div class="trap-card anim anim-d1">
        <h3>&ldquo;Slow down. Do less.&rdquo;</h3>
        <p>Like being asked to stop being you.</p>
      </div>
      <div class="trap-card anim anim-d2">
        <h3>&ldquo;Push harder.&rdquo;</h3>
        <p>You're already at full capacity.</p>
      </div>
    </div>
    <p class="pull-line anim anim-d3">So this is about doing different.</p>
    <p class="cta-secondary anim anim-d3">Not ready? <a href="https://www.parallaxxtransformations.com/priority-audit" target="_top">Take the Priority Audit</a>. Ten minutes to find out which priority gives you the most return.</p>
  </div>
</section>


<section id="how">
  <div class="container-wide">
    <div class="section-head anim">
      <span class="overline">How It Works</span>
      <div class="divider"></div>
    </div>
    <ol class="route-row">
      <li class="route-step anim anim-d1">
        <span class="route-fig" aria-hidden="true">01</span>
        <h3>Work Out What You Want</h3>
        <p>The personal unmet values and needs.</p>
      </li>
      <li class="route-step anim anim-d2">
        <span class="route-fig" aria-hidden="true">02</span>
        <h3>Prioritising Yourself</h3>
        <p>Asking for, and declining, with real intention.</p>
      </li>
      <li class="route-step anim anim-d3">
        <span class="route-fig" aria-hidden="true">03</span>
        <h3>Your Outcome In Reality</h3>
        <p>Not your assumption of it.</p>
      </li>
    </ol>
    <p class="pull-line anim">A simple outcome you already know you should have.<br>So why haven't you?</p>
    <p class="how-system anim">The Relational Connection System&trade; &mdash; vision, values, velocity.</p>
  </div>
</section>


<section id="distinction">
  <div class="container-wide">
    <div class="section-head anim">
      <span class="overline">Be Clear On What This Is</span>
      <h2>What This Isn't</h2>
      <div class="divider"></div>
    </div>
    <div class="chip-row anim anim-d1">
      <span class="chip">No sage</span>
      <span class="chip">No palo santo</span>
      <span class="chip">No goddess circle</span>
      <span class="chip">No womb wisdom</span>
      <span class="chip">No priestess</span>
    </div>
    <p class="under-quote anim anim-d2" style="margin-top:24px;">Nobody's asking you to soften or surrender.<br>This is somewhere your competence has nothing to do.</p>
    <div class="archetype-grid trw-mt">
      <div class="archetype-card anim anim-d1">
        <div class="archetype-num" aria-hidden="true">01</div>
        <div class="archetype-body">
          <h3 class="archetype-label">Not A Women's Circle</h3>
          <p>No performance of it.</p>
        </div>
      </div>
      <div class="archetype-card anim anim-d2">
        <div class="archetype-num" aria-hidden="true">02</div>
        <div class="archetype-body">
          <h3 class="archetype-label">Not A Sisterhood</h3>
          <p>No loyalty pact, no comfort without change.</p>
        </div>
      </div>
      <div class="archetype-card anim anim-d3">
        <div class="archetype-num" aria-hidden="true">03</div>
        <div class="archetype-body">
          <h3 class="archetype-label">Not A Friendly Coffee Chat</h3>
          <p>No deflection into talking about other people.</p>
        </div>
      </div>
    </div>
  </div>
</section>


<section id="friends">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">Friends And Peers</span>
      <h2>Your friends can't do this</h2>
      <div class="divider"></div>
    </div>
    <div class="friends-body">
      <p class="anim anim-d1">Friendship runs on co-regulation. You leave feeling better and nothing has changed.</p>
      <p class="anim anim-d2">Put yourself in a room of women exactly like you and you'll find a way to help. You always do.</p>
    </div>
    <p class="pull-line anim anim-d3">Every other room like this one is run by someone exactly like you.</p>
  </div>
</section>


<section id="difference">
  <div class="container-wide">
    <span class="dif-eyebrow anim">The Difference</span>
    <div class="dif-panel anim anim-d1">
    <p class="quote-big">When were you last seen, heard, witnessed and held in a masculine presence without a romantic intent?</p>
    <p class="under-quote anim anim-d2">You've been over-functioning your masculinity to make up for the masculine you didn't get from the men in your world. Put it down for a change.</p>
    <div class="dif-set">
      <div class="dif-cell">
        <span class="dif-fig" aria-hidden="true">01</span>
        <div>
          <h3>Structure is held</h3>
          <p>Someone else facilitates the room, the logistics, the pace.</p>
        </div>
      </div>
      <div class="dif-cell">
        <span class="dif-fig" aria-hidden="true">02</span>
        <div>
          <h3>It's taken care of</h3>
          <p>Nothing here for you to do.</p>
        </div>
      </div>
      <div class="dif-cell">
        <span class="dif-fig" aria-hidden="true">03</span>
        <div>
          <h3>Nothing to manage</h3>
          <p>Say the true thing. No cleaning up after it.</p>
        </div>
      </div>
      <div class="dif-cell">
        <span class="dif-fig" aria-hidden="true">04</span>
        <div>
          <h3>No scorecard</h3>
          <p>Nothing to win, so your competence has nothing to do.</p>
        </div>
      </div>
    </div>
    </div><!-- .dif-panel -->
  </div>
</section>


<!-- WHAT CHANGES: hidden until the pilot returns real outcomes.
<section id="outcome">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">What Changes</span>
      <h2>You already have the integrity.<br>It points outward.</h2>
      <div class="divider"></div>
    </div>
    <p class="under-quote anim anim-d1">Our work here is turning it inward. Same standard, pointed at you.</p>
  </div>
</section>
-->


<section id="bio">
  <div class="container-wide">
    <div class="bio-grid">
      <div class="bio-img-wrap anim-left">
        <div class="video-wrap video-wrap--reel">
          <video id="trw-bio-video" playsinline preload="metadata" poster="https://static.wixstatic.com/media/111174_52bb7073da274289b13738d153e1334e~mv2.jpg">
            <source src="https://video.wixstatic.com/video/111174_4e1bcc7d4c634401adae988048a9cb1b/1080p/mp4/file.mp4" type="video/mp4">
          </video>
          <button class="video-play-overlay" id="trw-bio-play" type="button" aria-label="Play bio video">
            <svg viewBox="0 0 96 96" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <circle cx="48" cy="48" r="46" fill="rgba(6,25,56,0.85)" stroke="#E8C65F" stroke-width="2"/>
              <polygon points="40,30 68,48 40,66" fill="#E8C65F"/>
            </svg>
          </button>
        </div>
      </div>
      <div class="bio-text anim-right">
        <span class="overline">Who's Running It</span>
        <h2>Daniel Lawson</h2>
        <span class="bio-role">I don't know what it's like to be a woman. I'm not one.</span>
        <p>I do know what it's like to be obsessed with productivity and to compete for success.</p>
        <p>Business development, tech company, Australia. I hit my target and it cost me my soul. The next year the number went up another 10%.</p>
        <p>I resigned. Then I lost my sense of identity.</p>
        <p><strong>I got out. And I understand the codes.</strong></p>
        <div class="tags">
          <span class="tag">ICG Accredited</span>
          <span class="tag">6 Years Coaching</span>
          <span class="tag">Global Facilitator</span>
        </div>
      </div>
    </div>
  </div>
</section>


<section id="listen">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">Listen</span>
      <h2>What kind of conversation to expect?</h2>
      <div class="divider"></div>
    </div>
    <p class="preframe anim anim-d1">Here's a recent summary of us together.</p>

    <figure class="aud anim anim-d1">
      <audio id="trw-listen" preload="none" src="/audio/reconnected-woman-summary.mp3"></audio>
      <div class="aud-row">
        <button class="aud-play" id="trw-listen-play" type="button" aria-label="Play the session summary">
          <svg viewBox="0 0 96 96" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <circle cx="48" cy="48" r="46" fill="rgba(6,25,56,0.85)" stroke="#E8C65F" stroke-width="2"/>
            <polygon class="aud-tri" points="40,30 68,48 40,66" fill="#E8C65F"/>
            <g class="aud-bars" fill="#E8C65F">
              <rect x="37" y="31" width="8" height="34" rx="1"/>
              <rect x="51" y="31" width="8" height="34" rx="1"/>
            </g>
          </svg>
        </button>
        <div class="aud-body">
          <p class="aud-title">The Reconnected Woman &mdash; a summary of a recent session</p>
          <div class="aud-track" id="trw-listen-track" role="slider" tabindex="0"
               aria-label="Seek through the summary"
               aria-valuemin="0" aria-valuemax="100" aria-valuenow="0">
            <span class="aud-fill" id="trw-listen-fill"></span>
          </div>
          <p class="aud-meta"><span id="trw-listen-now">0:00</span> / 10:45 &nbsp;&middot;&nbsp; 5.2 MB</p>
        </div>
      </div>
    </figure>

    <p class="vsl-caption anim anim-d2">"Life is not about trying to feel better. Life is about feeling more."<cite>Daniel, in the session</cite></p>

    <!-- TWO PARTICIPANT EXCERPTS BELONG HERE, one or two lines each, for the
         reader who will not press play. They are NOT in the markup yet and
         deliberately so: a visible "pending" box is a placeholder, and a
         placeholder that reaches main reaches the live site -- which is
         exactly what happened on the Speaking page. The build now fails on
         that class of thing rather than shipping it. Add the quotes when the
         consent is in, as .vsl-caption blocks alongside the one above. -->
  </div>
</section>


<!-- VOICES: hidden until quotes are in.
<section id="voices">
  <div class="container-wide">
    <div class="section-head anim">
      <span class="overline">Proof</span>
      <h2>From women already in the work</h2>
      <div class="divider"></div>
    </div>
  </div>
</section>
-->


<section id="values">
  <div class="container-wide">
    <div class="section-head anim">
      <span class="overline">What We Stand For</span>
      <h2>The Standard Here</h2>
      <div class="divider"></div>
    </div>
    <div class="values-grid">
      <div class="value-card anim anim-d1">
        <h3>Your Space</h3>
        <p>Not for the women. Not to contribute.</p>
      </div>
      <div class="value-card anim anim-d2">
        <h3>Your Truth</h3>
        <p>That's the contribution. Performance is at the door.</p>
      </div>
      <div class="value-card anim anim-d3">
        <h3>Punctuality</h3>
        <p>Show up like it matters, because you do.</p>
      </div>
      <div class="value-card anim anim-d3">
        <h3>Curiosity</h3>
        <p>Over judgment. Of each other, and of yourself.</p>
      </div>
      <div class="value-card anim anim-d3">
        <h3>Accountability</h3>
        <p>One intention out of every session. Yours to keep.</p>
      </div>
    </div>
  </div>
</section>


<section id="intention">
  <div class="container">
    <span class="overline anim">One Intention</span>
    <p class="promise-line anim anim-d1">An outcome specifically for you.</p>
    <p class="promise-sub anim anim-d2">And the line you'll hold to meet it.</p>
  </div>
</section>


<section id="pricing">
  <div class="container">
    <h2>The Open Session</h2>
    <p class="lead">The first session of every month is open. Come, sit in it, and decide from the session rather than from this page.</p>

    <!-- ONE CARD, ONE ASK. Two cards asked her to choose between the
         free session and the paid month before she had been to either,
         and both buttons went to the same form -- so the choice was
         presented and then not honoured. The open session is the only
         decision on this page; the price is what happens afterwards, so
         it is subtext under the button rather than a second offer with
         its own headline. Every word here already existed on the page. -->
    <div class="open-card anim-scale">
      <div class="open-top">
        <div class="open-amount">Free</div>
        <div class="open-when"><span class="ow-nw">Tuesday 15 September &middot;</span> <span class="ow-nw">08:30 CET / 14:30 GMT+8</span><br>Online, wherever you are</div>
        <span class="open-count js-open-count"></span>
      </div>

      <div class="open-stay">
        <p class="open-stay-h">Stay, and here's the four weeks.</p>
        <p class="open-stay-s">Work out what you want, and ask for it.</p>
      </div>

      <ul class="price-features">
        <li>One live session a week, in four-week cycles</li>
        <li>Live facilitation and hot seat work</li>
        <li>Access to the group between sessions</li>
        <li>First access to new frameworks and resources</li>
        <li>Complimentary 1:1 call on week 6</li>
      </ul>

      <div class="open-action">
        <button class="btn btn--gold btn--full" data-trw-modal>Register For The Open Session</button>
      </div>

      <p class="open-gate">The short application first is to ensure a strong mutual fit, for you and for our community.</p>
      <p class="price-after"><strong>&euro;89 a month</strong>, and your membership price is grandfathered. It never increases as long as you stay.<span class="pa-roll">Memberships roll over monthly. Cancel anytime.</span></p>
    </div>
  </div>
</section>

<section id="after">
  <div class="container">
    <div class="section-head anim">
      <span class="overline">After Four Weeks</span>
      <h2>You'll own a new experience of your life.</h2>
      <div class="divider"></div>
    </div>
    <div class="values-grid">
      <div class="value-card anim anim-d1">
        <h3>Continue to build upon it</h3>
        <p>Build the reps, with the same one or a different one.</p>
      </div>
      <div class="value-card anim anim-d2">
        <h3>Continue more personalised</h3>
        <p>Credit your membership to personal 1:1 access.</p>
      </div>
      <div class="value-card anim anim-d3">
        <h3>Continue alone</h3>
        <p>Take what you've gained, and celebrate your wins.</p>
      </div>
    </div>
  </div>
</section>


<section id="faq">
  <div class="container-wide">
    <div class="section-head anim">
      <span class="overline">Common Questions</span>
      <h2>Before You Come</h2>
      <div class="divider"></div>
    </div>
    <div class="faq-wrap">
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Isn't this just another women's circle?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>No. Most are built around becoming more feminine, and there's a performance in that. Here we call it as it is, so you can put something down instead of picking more up.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Why is a man hosting it?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>Because the women who join want a healthy masculine presence in their life, one they are not being led by, competing with, or taking instruction from. Just the masculine holding the room, so they can stop for a change.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">I have great friends. Do I need this?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>Friendship runs on co-regulation and the women you'd go to are running your operating system. I'm not here to be your friend.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Can I come once before I decide?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>Yes. The first session of every month is open and there's no charge for it. Come, sit in it, and decide from the room rather than from this page. After that it's &euro;89 a month, and you're grandfathered: your price never increases as long as you stay.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Where does it run?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>Online, so you can join from wherever you are. Nothing to travel to and nothing to arrange around it.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Do I need to attend every session?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>No. Life moves and the group knows it. Come when you can, and know that the women who turn up regularly are the ones who feel it shift.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">When do sessions run?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>One sitting time, set so it works across the time zones the group is spread over, and we stay fluid with what the group asks for. Come to the open session and you'll see how it sits with your week.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">What does the fee include?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>Facilitation, hot seat work, resources, and the people you'll meet. It's largely a filter.</p></div></div>
      </div>
      <div class="faq-item anim">
        <button class="faq-btn" type="button">
          <span class="faq-q">Are there upsells coming?</span>
          <span class="faq-ico">+</span>
        </button>
        <div class="faq-body"><div class="faq-body-inner"><p>No. There's the 1:1 Reconnect Program if you decide you want more, faster, deeper, and your membership investment is discounted from that. Same work, same direction. More personal.</p></div></div>
      </div>
    </div>
  </div>
</section>

<section id="cta-final">
  <div class="container">
    <span class="overline anim">Ready?</span>
    <h2 class="anim anim-d1">Work out what you want,<br>and ask for it in four weeks.</h2>
    <p class="sub anim anim-d2">Next open session Tuesday 15 September. Free to attend, short application form first.</p>
    <button class="btn btn--gold anim anim-d3" data-trw-modal>Register For The Open Session</button>
  </div>
</section>
<parallaxx-footer></parallaxx-footer>





</div><!-- #trw-page -->


<!-- Apply modal -->
<div id="trw-apply-modal" role="dialog" aria-modal="true" aria-label="Application Form">
  <div class="modal-backdrop"></div>
  <div class="modal-box">
    <div class="modal-header">
      <span class="modal-title">Registration</span>
      <button class="modal-close" aria-label="Close form">&times;</button>
    </div>
    <div class="modal-body">
      <!-- THE ID APPEARS THREE TIMES AND ALL THREE MATTER · 14 Aug.
           src is what loads. id="form-<ID>" and data-form-id are what GHL's
           form_embed.js matches on when it posts a height back, so a swap
           that misses either one loads the right form at the wrong height
           and never resizes it. Change all three together. -->
      <iframe
        src="https://api.leadconnectorhq.com/widget/form/0RT5EVaTlGxo6QBjyIPT"
        height="700"
        title="The Reconnected Woman Application Form"
        id="form-0RT5EVaTlGxo6QBjyIPT"
        data-form-id="0RT5EVaTlGxo6QBjyIPT"
        data-form-name="The Reconnected Woman Application Form"
      ></iframe>
    </div>
  </div>
</div>`;

  function once(id, make) {
    if (document.getElementById(id)) return;
    var el = make(); el.id = id; document.head.appendChild(el);
  }

  function addHead() {
    once('trw-pre1', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.googleapis.com'; return l; });
    once('trw-pre2', function () { var l = document.createElement('link'); l.rel = 'preconnect'; l.href = 'https://fonts.gstatic.com'; l.crossOrigin = ''; return l; });
    once('trw-font0', function () { var l = document.createElement('link'); l.rel = 'stylesheet'; l.href = 'https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap'; return l; });
    once('trw-css', function () { var st = document.createElement('style'); st.textContent = CSS; return st; });
    /* The Wix page collapses the site header and footer, so the viewport meta
       has to come from here or the page lays out at 980px on a phone and every
       max-width 760px rule is dead. */
    if (!document.querySelector('meta[name="viewport"]')) {
      var mv = document.createElement('meta'); mv.name = 'viewport';
      mv.content = 'width=device-width, initial-scale=1'; document.head.appendChild(mv);
    }
  }

  function script(src, id) {
    return new Promise(function (res) {
      if (document.getElementById(id)) return res();
      var sc = document.createElement('script'); sc.id = id; sc.src = src; sc.async = false;
      sc.onload = res; sc.onerror = res; document.head.appendChild(sc);
    });
  }

  function boot() {

(function() {
  // FAQ accordion
  document.querySelectorAll('#trw-page .faq-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const item   = btn.closest('.faq-item');
      const body   = item.querySelector('.faq-body');
      const isOpen = item.classList.contains('open');
      document.querySelectorAll('#trw-page .faq-item').forEach(i => {
        i.classList.remove('open');
        i.querySelector('.faq-body').style.maxHeight = null;
        i.querySelector('.faq-btn').setAttribute('aria-expanded', 'false');
      });
      if (!isOpen) {
        item.classList.add('open');
        body.style.maxHeight = body.scrollHeight + 'px';
        btn.setAttribute('aria-expanded', 'true');
      }
    });
  });

  // Apply modal
  const modal    = document.getElementById('trw-apply-modal');
  const backdrop = modal.querySelector('.modal-backdrop');
  const closeBtn = modal.querySelector('.modal-close');
  /* GHL's form_embed.js parks the iframe offscreen until it decides the form
     is ready: position absolute, left -9999px, visibility hidden, opacity 0,
     all as INLINE styles. Our modal starts display:none, so the script never
     saw it become visible and never un-parked it. The form loaded fine and
     sat 9999px to the left of the box, which measured 76px tall: the header,
     and nothing under it.

     So un-park it ourselves on open. Twice more on a timer, because the
     script re-applies the parking if it initialises after the first click. */
  function unparkForm() {
    var frame = modal.querySelector('iframe');
    if (!frame) return;
    frame.style.position = 'static';
    frame.style.left = 'auto';
    frame.style.top = 'auto';
    frame.style.visibility = 'visible';
    frame.style.opacity = '1';
    frame.style.pointerEvents = 'auto';
  }
  function openModal()  { modal.classList.add('is-open');    document.body.style.overflow = 'hidden'; unparkForm(); setTimeout(unparkForm, 350); setTimeout(unparkForm, 1400); closeBtn.focus(); }
  function closeModal() { modal.classList.remove('is-open'); document.body.style.overflow = ''; }
  document.querySelectorAll('[data-trw-modal]').forEach(el => el.addEventListener('click', openModal));
  backdrop.addEventListener('click', closeModal);
  closeBtn.addEventListener('click', closeModal);
  document.addEventListener('keydown', e => { if (e.key === 'Escape' && modal.classList.contains('is-open')) closeModal(); });

  // Video play overlays ‚Äî works for any video paired with a sibling .video-play-overlay button
  document.querySelectorAll('#trw-page .video-play-overlay').forEach(overlay => {
    overlay.addEventListener('click', () => {
      const video = overlay.parentElement.querySelector('video');
      if (!video) return;
      video.controls = true;
      video.play().catch(() => {});
      overlay.classList.add('is-hidden');
    });
  });

  // Scroll animations
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in-view');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });
  document.querySelectorAll('#trw-page .anim, #trw-page .anim-scale, #trw-page .anim-left, #trw-page .anim-right').forEach(el => observer.observe(el));
})();

;

(function () {
  var root = document.getElementById('trw-page');
  if (!root) return;
  var at = root.getAttribute('data-open-at');
  if (!at) return;
  var t = new Date(at).getTime();
  if (!t) return;
  var days = Math.ceil((t - new Date().getTime()) / 86400000);
  var txt = '';
  if (days > 1) txt = 'In ' + days + ' days';
  else if (days === 1) txt = 'Tomorrow';
  else if (days === 0) txt = 'Today';
  else txt = 'Date to be confirmed';
  var els = document.querySelectorAll('.js-open-count');
  for (var i = 0; i < els.length; i++) els[i].textContent = txt;
})();

;

(function () {
  function retarget(root) {
    if (!root) return;
    root.querySelectorAll('a[href]').forEach(function (a) {
      if (!a.target) a.target = '_top';
    });
  }
  function watch(tag) {
    customElements.whenDefined(tag).then(function () {
      var el = document.querySelector(tag);
      if (!el) return;
      var apply = function () { retarget(el.shadowRoot); };
      apply();
      if (el.shadowRoot && window.MutationObserver) {
        new MutationObserver(apply).observe(el.shadowRoot, {childList: true, subtree: true});
      }
      [60, 300, 1200].forEach(function (t) { setTimeout(apply, t); });
    });
  }
  watch('parallaxx-nav');
  watch('parallaxx-footer');
})();

;

(function () {
  var a = document.getElementById('trw-listen');
  if (!a) return;
  var wrap  = document.querySelector('#trw-page .aud');
  var btn   = document.getElementById('trw-listen-play');
  var track = document.getElementById('trw-listen-track');
  var fill  = document.getElementById('trw-listen-fill');
  var now   = document.getElementById('trw-listen-now');
  if (!wrap || !btn || !track || !fill || !now) return;

  function fmt(s) {
    if (!isFinite(s) || s < 0) s = 0;
    var m = Math.floor(s / 60), r = Math.floor(s % 60);
    return m + ':' + (r < 10 ? '0' : '') + r;
  }
  function paint() {
    var d = a.duration;
    var p = (isFinite(d) && d > 0) ? (a.currentTime / d) * 100 : 0;
    fill.style.width = p + '%';
    track.setAttribute('aria-valuenow', String(Math.round(p)));
    now.textContent = fmt(a.currentTime);
  }
  function toggle() { if (a.paused) { a.play(); } else { a.pause(); } }

  btn.addEventListener('click', toggle);
  a.addEventListener('play',  function () { wrap.classList.add('is-playing');
    btn.setAttribute('aria-label', 'Pause the session summary'); });
  a.addEventListener('pause', function () { wrap.classList.remove('is-playing');
    btn.setAttribute('aria-label', 'Play the session summary'); });
  a.addEventListener('ended', function () { wrap.classList.remove('is-playing');
    a.currentTime = 0; paint(); });
  a.addEventListener('timeupdate', paint);

  /* preload="none" means the 5MB file is not fetched until she asks for it,
     which also means duration is NaN until then -- so the total in the meta
     line is written into the markup and only the elapsed side is live. */
  track.addEventListener('click', function (e) {
    var d = a.duration;
    if (!isFinite(d) || d <= 0) return;
    var r = track.getBoundingClientRect();
    var p = (e.clientX - r.left) / r.width;
    a.currentTime = (p < 0 ? 0 : (p > 1 ? 1 : p)) * d;
    paint();
  });
  track.addEventListener('keydown', function (e) {
    var d = a.duration;
    if (e.key === ' ' || e.key === 'Enter') { toggle(); e.preventDefault(); return; }
    if (!isFinite(d) || d <= 0) return;
    if (e.key === 'ArrowRight') { a.currentTime = Math.min(d, a.currentTime + 15); e.preventDefault(); }
    else if (e.key === 'ArrowLeft') { a.currentTime = Math.max(0, a.currentTime - 15); e.preventDefault(); }
    paint();
  });
})();

  }

  var El = function () { return Reflect.construct(HTMLElement, [], El); };
  El.prototype = Object.create(HTMLElement.prototype);
  El.prototype.constructor = El;
  Object.setPrototypeOf(El, HTMLElement);

  El.prototype.connectedCallback = function () {
    if (this._trwUp) return;
    this._trwUp = true;
    addHead();
    this.innerHTML = HTML;
    script('https://parallaxxlifeco.github.io/parallaxx-transformations/parallaxx-nav.js?v=20260812a', 'trw-nav-js');
    script('https://parallaxxlifeco.github.io/parallaxx-transformations/parallaxx-footer.js?v=20260812a', 'trw-foot-js');
    script('https://link.msgsndr.com/js/form_embed.js', 'trw-ghl-js');
    try { boot(); } catch (e) { /* the page renders fully visible without it */ }
  };

  customElements.define('parallaxx-reconnected-woman', El);
})();
