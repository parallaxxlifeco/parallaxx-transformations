/* PARALLAXX TRANSFORMATIONS - Testimonials Wix Custom Element. Tag: parallaxx-testimonials.
   GENERATED by build-testimonials-bundle.py from "Parallaxx Testimonials v4.dc.html".
   DO NOT EDIT THIS FILE - edit the .dc.html and rerun the build.
   In the Wix editor: turn the site Header + Footer OFF for this page. It
   carries PtNav v3 and PtFooter v3 baked in. */
(function(){
  if (customElements.get('parallaxx-testimonials')) return;

  var CSS = `/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · TESTIMONIALS · v4
   -------------------------------------------------------------------
   WHAT WAS WRONG WITH THE LIVE PAGE, IN ORDER OF COST

   1. THE PROOF WAS A PICTURE OF PROOF.
      All ten testimonials were flattened PNGs -- photo, quote, name and
      country baked into one 810x1013 image. Google indexes NONE of it.
      A screen reader gets nothing. A phone cannot reflow it, so the
      body text rendered at roughly 7px on a 390px screen. The entire
      page contained five sentences of machine-readable text:

        "What outcomes do seek in life?"   (sic -- the typo is live)
        the Reconnect You line
        "What Others are Saying:"
        "Your Time to Reconnect Too?"
        "Learn More"

      A testimonials page that Google cannot read is not a testimonials
      page. It is a photo album. This is the single largest fix here and
      everything else is secondary to it.

   2. TWO CAROUSELS HID EIGHT OF THE TEN STORIES.
      Side-by-side galleries, four slides each, advanced by a chevron.
      Carousel telemetry is brutally consistent: slide 1 gets seen, slide
      2 gets seen by a minority, slides 3+ effectively do not exist. Ten
      strong stories were being used to show two.

   3. THERE WAS NO VIDEO -- ONLY A SUBSCRIBE BANNER.
      The block at the top of the page is a YouTube channel widget. Its
      call to action is "SUBSCRIBE", pointing off-site, above the fold,
      before a single testimonial. The one page whose entire job is to
      hold somebody until they are convinced was opening with an exit.

   4. NOTHING WAS SEGMENTED.
      A man arriving from the men's page met a blonde woman on the
      Millennium Bridge. A woman arriving from the women's page met a US
      Navy chief. Both bounce off the first card. The site sorts by
      avatar everywhere else and then stops sorting exactly where
      recognition matters most.

   5. THE CLOSE WAS GENERIC.
      One "Learn More" to /reconnect, which flattens the sort the rest
      of the site spends its whole architecture building.

   -------------------------------------------------------------------
   WHAT THIS PAGE DOES INSTEAD

     TWO RULES THIS PAGE IS EDITED BY.

     1. NEVER COUNT THEM.
        Not in a heading, not in a switch label, not in an eyebrow.
        "Ten people" and "All 10" read as a CEILING -- they tell a
        visitor this is everyone there has ever been, and ten is a
        small number to have changed in six years. What is here is a
        SELECTION out of more than a thousand. The copy implies the
        thousand; it does not enumerate the ten. The only numerals
        allowed in visible copy are the count strip (1000+, 9, 6+),
        which are business scale and make the work sound larger.
        If an edit wants "all eleven" or "Men 6", the answer is no --
        say "all of them".

     2. NO EM DASHES. NO EN DASHES USED AS A SENTENCE BREAK.
        Not in copy, not in placeholder text, not in a caption. A dash
        splicing two clauses is nearly always a full stop or a comma
        that has not been decided on yet, and deciding is what makes
        the line land. Six of them were removed on this pass and every
        single one became a shorter, harder sentence. If a future edit
        reaches for one, use a full stop.

     3. NEVER DEFEND THE PROOF.
        The first pass carried lines like "Nobody prompted this",
        "that part is not a coincidence" and "nothing has been
        rewritten". Every one presumes the reader is suspicious and
        then argues with them -- which is the fastest way to MAKE a
        reader suspicious. Nobody arrives here accusing Daniel of
        forging testimonials. Confident proof states the thing and
        stops; it does not litigate.
        The honesty lives in the BUILD, not the copy: real names, real
        countries, real faces, verbatim words, no invented star
        rating. That does the work silently and it does it better.

     01 HERO        the claim, in their words, not Daniel's
     02 THE PATTERN five verbatim fragments. earns the hero in 3 seconds.
     03 HEAR ONE    49-sec clip up front. long interviews behind facades.
     04 THE WALL    all ten, as TEXT, with a real All/Men/Women switch
     05 CREDS       demoted. proof closes, it does not open.
     06 THE FORK    two doors, not one button.

   -------------------------------------------------------------------
   NO CONTENT DEPENDS ON JAVASCRIPT.
   The men's page lost its headline twice to GSAP tweens stranded by a
   ScrollTrigger refresh. A PROOF page cannot have that failure mode --
   if the script dies, the visitor must still see ten testimonials.

   So: everything is visible in the stylesheet. The reveal animation is
   applied BY SCRIPT (it adds .px-js to the root, which is what hides
   things), never by the stylesheet. Script fails -> page reads.
   The filter is the same: the wall is unfiltered CSS by default and
   the switch only exists once JS has confirmed it can drive it.

   The disclosures are <details>/<summary>. No JS at all, keyboard
   operable for free, and Google indexes closed <details> content.

   -------------------------------------------------------------------
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  Lumios Marker  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   Gold  -- Daniel's voice: the handwriting, section labels, hairlines,
            the name of whoever is speaking.
   Coral -- the READER's move: the filter switch, the play buttons, the
            two doors at the end. Nothing else on this page is coral.
            The filter is coral because it is the only thing here the
            visitor actually operates.
   ═══════════════════════════════════════════════════════════════════ */

  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
  ::selection{background:#FF501F;color:#fff}
  img{display:block;max-width:100%}

  #px-root{background:#04122A;color:#B1BFD7;
    font-family:'Montserrat',system-ui,-apple-system,sans-serif;
    font-size:16px;line-height:1.7;overflow-x:clip;position:relative;
    -webkit-font-smoothing:antialiased}
  #px-root h1,#px-root h2,#px-root h3,#px-root h4,#px-root p{margin:0}
  h1,h2,h3,h4{text-wrap:balance}
  p{text-wrap:pretty}

  .px-serif{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;letter-spacing:-.018em}
  .px-hand{font-family:'Lumios Marker','Permanent Marker',cursive}
  .px-wrap{max-width:1240px;margin:0 auto}
  .px-sec{padding:clamp(52px,6.5vw,92px) clamp(20px,4vw,52px);position:relative}
  .px-label{font-size:.7rem;letter-spacing:.2em;text-transform:uppercase;font-weight:700;color:#67748E}
  .px-rule{width:56px;height:1px;background:rgba(232,198,95,.5);border:0}

  .px-btn{display:inline-flex;align-items:center;gap:.55em;background:#FF501F;color:#fff;
    font-weight:700;font-size:.98rem;padding:1.05em 2.2em;border-radius:999px;text-decoration:none;
    transition:background .3s,transform .3s;box-shadow:0 10px 30px -12px rgba(255,80,31,.5)}
  .px-btn:hover{background:#FF6A3D;transform:translateY(-2px)}
  .px-ghost{display:inline-flex;align-items:center;gap:.55em;color:#B1BFD7;
    border:1px solid rgba(232,198,95,.4);font-weight:600;font-size:.94rem;
    padding:1em 2em;border-radius:999px;text-decoration:none;transition:background .3s,border-color .3s,color .3s}
  .px-ghost:hover{background:rgba(241,236,225,.08);border-color:#F1ECE1;color:#F1ECE1}

  /* REVEAL. Note the direction of the logic: the stylesheet leaves
     everything VISIBLE, and .px-js -- which only ever gets added by the
     script, at boot -- is what hides it ready to animate. A dead script
     is therefore indistinguishable from reduced-motion, which is the
     correct failure. */
  .px-js .px-fade{opacity:0;transform:translateY(20px);will-change:transform,opacity}
  @media(prefers-reduced-motion:reduce){
    .px-js .px-fade{opacity:1!important;transform:none!important}
    *{animation:none!important;transition:none!important}
  }

/* ═══ 01 · HERO ═══════════════════════════════════════════════════
   The headline is NOT a claim about Daniel. It is a claim about the
   ten people below, and the section immediately under it proves the
   claim with their own words before anything is asked of the reader.

   THE FIND: they independently reach for the same three words for why
   it had not worked yet. Four are quoted; there are more than four.
     "trying to figure it out by myself"        Gena
     "couldn't shift my thinking on my own"     Katherine
     "couldn't break through them alone"        Ana
     "couldn't pivot to the next level on my own" Tobias
   It is also, word for word, the mechanism the
   sorting page opens on -- "you stopped asking anyone for anything."
   The testimonials page should not invent a headline when the clients
   have already written one. */
  #px-hero{padding-top:clamp(120px,15vw,190px);text-align:center;
    background:radial-gradient(120% 85% at 50% 0%,#0A1D3C 0%,#061938 45%,#04122A 100%)}
  #px-hero .px-hand{font-size:clamp(15px,3.6vw,22px);color:#E8C65F;display:block;margin-bottom:20px}
  #px-hero h1{font-size:clamp(30px,5.6vw,62px);line-height:1.1;color:#F1ECE1;
    font-weight:500;max-width:17ch;margin:0 auto 24px}
  #px-hero h1 em{font-style:italic;font-weight:300;color:#E8C65F}
  #px-hero .lede{font-size:clamp(15px,1.7vw,18.5px);line-height:1.7;color:#B1BFD7;
    max-width:50ch;margin:0 auto}
  #px-hero .lede b{color:#F1ECE1;font-weight:600}

  /* THE COUNT. Three facts, no adjectives. It sits inside the hero
     rather than in its own band because it is the SUBTITLE of the
     claim, not a separate boast. */
  /* GRID, NOT WRAPPING FLEX. On a phone the three figures wrapped 2 + 1
     and the last one sat alone under a gap, which reads as a mistake
     rather than as a row. Three equal columns hold at every width; the
     type comes down instead. */
  #px-count{display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(10px,5vw,64px);
    margin-top:clamp(30px,4vw,44px);
    padding-top:clamp(26px,3vw,34px);border-top:1px solid rgba(241,236,225,.09);
    max-width:660px;margin-left:auto;margin-right:auto}
  #px-count div{text-align:center}
  #px-count b{display:block;font-family:'Poppins','Montserrat',sans-serif;
    font-size:clamp(1.5rem,3vw,2rem);font-weight:500;color:#E8C65F;line-height:1.1}
  #px-count span{font-size:.68rem;letter-spacing:.14em;text-transform:uppercase;
    font-weight:700;color:#67748E;line-height:1.4}

/* ═══ 02 · THE PATTERN ════════════════════════════════════════════
   Fragments, verbatim, attributed, from four different countries.
   This is the cheapest section on the page to read and the most
   expensive one to argue with. It runs BEFORE the video and before the
   wall on purpose: it earns the headline in about three seconds, which
   is roughly what a testimonials page gets before the visitor decides
   whether the rest is worth scrolling.

   DO NOT PARAPHRASE THESE. Each is a contiguous clause lifted from the
   card below it. If a quote here ever stops matching the wall, the
   whole section becomes a liability. */
  #px-pattern{background:#061938;border-top:1px solid rgba(241,236,225,.07);
    border-bottom:1px solid rgba(241,236,225,.07)}
  /* ── THE BAND EARNS ITS HEIGHT, THIRD ATTEMPT ────────────────────
     v1: heading across the top, four fragments in one row. Left a dead
     rectangle bottom-right and stranded the handwriting bottom-left.
     v2: two columns, heading left, fragments 2x2 right. Fixed the big
     hole and opened a smaller one: the left column ran 184px of content
     inside a 310px row, so 41% of it was empty. The band was only 8.7%
     of the page, which is cheap, but it was not USING what it took, and
     that is what reads as bloat rather than the raw height.
     v3, here: one row of three, full width, heading and handwriting
     sharing a line above them. No empty column anywhere.

     AND IT WENT FROM FOUR QUOTES TO THREE, which is a content fix as
     much as a layout one. "on my own" was appearing twice, in
     Katherine's and Tobias's, and a repeated phrasing inside a section
     whose entire argument is INDEPENDENT people reaching for the same
     words reads like the fourth one was found rather than noticed.
     Three distinct phrasings is a harder claim than four with a
     duplicate in it.

     Tobias came out rather than Katherine, on spread: Bulgaria, the UK
     and Australia is three continents, while keeping Tobias would have
     made it three European countries with Sweden already spoken for by
     Silja downstairs. "Different lives" is the heading; the countries
     have to carry it. */
  #px-pattern-head{display:flex;align-items:flex-end;justify-content:space-between;
    gap:clamp(20px,4vw,60px);flex-wrap:wrap;margin-bottom:clamp(26px,3.2vw,42px)}

  #px-frags{display:grid;grid-template-columns:repeat(3,1fr);gap:clamp(22px,3vw,44px)}
  @media(max-width:820px){#px-frags{grid-template-columns:1fr;gap:26px}}
  /* Middle column drops half a step so three across does not land as a
     table. Off on one column, where it would only look like a mistake. */
  @media(min-width:821px){#px-frags > *:nth-child(2){margin-top:clamp(14px,2vw,26px)}}

  .px-frag{border-left:1px solid rgba(232,198,95,.3);padding-left:clamp(16px,1.6vw,22px);
    background:linear-gradient(to right,rgba(232,198,95,.05),rgba(232,198,95,0) 62%)}

  /* THE REPEATED PHRASE IS THE POINT, SO IT IS THE SIZE.
     It was bold inline inside the sentence, which made it a detail in a
     quote. Pulled onto its own line at nearly twice the size, the four
     of them scan vertically as one repeated statement before a single
     sentence has been read, which is the entire job of the section. */
  .px-frag .lead{display:block;font-family:'Poppins','Montserrat',sans-serif;
    font-style:italic;font-weight:300;font-size:clamp(15px,1.15vw,17px);
    line-height:1.4;color:#7C89A3}
  .px-frag .key{display:block;font-family:'Poppins','Montserrat',sans-serif;
    font-style:normal;font-weight:400;font-size:clamp(22px,2.1vw,31px);
    line-height:1.16;letter-spacing:-.015em;color:#F1ECE1;margin-top:5px}
  /* EMPHASIS INSIDE A QUOTE IS INK, NEVER AN ACCENT.
     These three phrases were coral, and coral was the wrong colour for
     them twice over.

     SEMANTICALLY: the accent rule this whole site is built on says gold
     is Daniel's voice and coral is the READER'S MOVE -- his buttons,
     his controls, his doors. "by myself" is neither. It is a client
     speaking. Painting it coral quietly claims a client's sentence as
     an interface element. The women's page states the correct rule
     outright: structural emphasis inside body copy is INK, never an
     accent.

     VISUALLY: five coral phrases in a row, directly under a coral nav
     pill and directly above a coral play button, a coral switch and ten
     coral toggles. Coral stopped meaning "this is your move" and
     started meaning "this is important", which is a job it cannot do
     while also being the button colour. Everything lit up at once.

     Cream carries it perfectly well here, because everything around it
     is light italic at #F1ECE1's dimmer neighbours -- the emphasis
     comes from WEIGHT and BRIGHTNESS against soft italics, which is
     what emphasis is supposed to come from. */
  .px-frag q{color:#9FB2CC}
  /* NO FACES IN THIS BAND. Tried, compared, removed.
     They were added when the section read as plain, and they were the
     wrong fix for it: what was actually wrong was the layout hole and
     the repeated phrase being buried at body size. Fixing those two
     fixed the section. The faces were a third change riding along.

     Side by side the cost is obvious. A 34px circle sits low and left
     of the phrase and takes the eye down into the attribution, which is
     the least important line in the block. The band's whole job is to
     make somebody hear one sentence four times before they read a
     single full quote, and anything that competes for the first glance
     works against that.

     They are also duplication. All four of these people appear again,
     larger, roughly one screen further down, where a face does real
     work verifying a long story. Spending them here weakens both.

     If this band ever needs more weight, it comes from the phrase or
     the spacing, not from portraits. */
  .px-frag .who{margin-top:clamp(13px,1.5vw,18px)}
  .px-frag cite{font-style:normal;font-size:.72rem;letter-spacing:.11em;
    text-transform:uppercase;font-weight:700;color:#8394AD;line-height:1.45}

/* ═══ 03 · HEAR ONE ═══════════════════════════════════════════════
   VIDEO FACADE. A YouTube <iframe> pulls roughly a megabyte of
   third-party JavaScript and a fistful of requests BEFORE anyone has
   pressed anything. Five of them stacked would be the slowest page on
   the site, and most visitors play none.

   So these are posters. The real player is built on CLICK, with
   autoplay, and replaces the button in place. Zero cost until asked.

   THE PLAY BUTTON IS NOT CENTRED. These are talking heads shot on a
   webcam and the speaker is dead centre in frame; a centred button
   lands on his mouth, which is the one part of a testimonial that has
   to look like it is speaking. Bottom-left instead. */
  /* ── THE VIDEO BLOCK IS CREAM, AND THIS IS THE ANSWER TO "IT IS ALL
     ONE FLAT COLOUR" ──────────────────────────────────────────────
     The page ran navy from the nav to the fork: roughly four thousand
     pixels of one value, with the only relief at the very bottom. That
     is what reads as flat, and no amount of extra typography fixes it,
     because the problem is TONAL and typography is not a tone.

     The honest fix would be a photograph. There isn't one. Every image
     in the project folder is Daniel or a stock-looking couple, and
     neither belongs on a page that is supposed to be about ten other
     people. proof-room.jpg -- "a real session, real grain" -- has been
     on the image manifest since the women's page and has never been
     shot. Putting decorative imagery here instead would turn this into
     every other coach site, which is the one thing the whole redesign
     is trying not to be.

     So the break is tonal, and it costs nothing and claims nothing.
     The page now reads navy / navy / CREAM / navy / navy / cream: two
     light bands at roughly the thirds, and the fork's cream at the end
     stops arriving out of nowhere.

     IT ALSO SUITS THE CONTENT. Video posters are dark. On navy they
     were dark rectangles on a dark ground and the whole block sat
     back. On cream they punch, which is correct: this is the one place
     on the page where somebody actually speaks. */
  #px-hear{background:#F1ECE1}
  #px-hear .px-label{color:#A08A5E}
  /* !important IS LOAD-BEARING ON BOTH OF THESE, and it is not
     laziness. The headings on this page carry their colour as an
     INLINE style (that is the house pattern -- see every h2 below),
     and inline beats a stylesheet rule at any specificity. Without
     !important the h2 stayed #F1ECE1 and rendered cream on cream:
     "Hear them" vanished completely and only the italic half of the
     line survived. Anything overriding a section's ground colour on
     this page has to out-rank an inline declaration or it silently
     erases text. */
  #px-hear h2{color:#1E2A3D!important}
  #px-hear h2 span{color:#6E7686!important}
  #px-hear .px-vid:not(.px-vid-yt){background:#DAD4C7;
    box-shadow:0 26px 60px -34px rgba(30,42,61,.65), 0 0 0 1px rgba(30,42,61,.14)}
  #px-hear .px-vid:hover{box-shadow:0 30px 70px -32px rgba(30,42,61,.75), 0 0 0 1px rgba(255,80,31,.4)}
  /* AND THIS ONE LOST A SPECIFICITY TIE, which is the quieter version
     of the same bug. "#px-hear .pull" and "#px-lead .pull" are both one
     id plus one class, so the tie breaks on SOURCE ORDER -- and #px-lead
     is declared further down the sheet, so it won and the pull quote
     rendered cream on cream too. Adding the second id makes the
     override outrank it outright rather than by accident of position,
     so moving these blocks around later cannot resurrect the bug. */
  #px-hear #px-lead .pull{color:#1E2A3D}
  #px-hear #px-lead .px-vid-cap,#px-hear .px-vid-cap{color:#A08A5E}
  #px-hear .px-vid-cap span{color:#6E6960}
  /* The placeholders keep the coral hatch -- it is a build marker and it
     is supposed to be impossible to miss on any ground -- but its text
     has to invert or it disappears into the cream. */
  .px-vid{position:relative;display:block;width:100%;padding:0;border:0;cursor:pointer;
    background:#061938;border-radius:16px;overflow:hidden;aspect-ratio:16/9;
    box-shadow:0 26px 60px -34px rgba(3,12,28,.95), 0 0 0 1px rgba(232,198,95,.14);
    transition:transform .3s ease, box-shadow .3s ease}
  .px-vid:hover{transform:translateY(-3px);
    box-shadow:0 30px 70px -32px rgba(3,12,28,1), 0 0 0 1px rgba(232,198,95,.34)}
  .px-vid:focus-visible{outline:2px solid #FF501F;outline-offset:3px}
  .px-vid > img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
    transition:filter .3s ease}
  .px-vid .play{position:absolute;left:18px;bottom:18px;width:54px;height:54px;
    border-radius:999px;background:rgba(255,80,31,.94);
    box-shadow:0 10px 30px -8px rgba(255,80,31,.6);transition:transform .3s ease}
  .px-vid .play::after{content:'';position:absolute;top:50%;left:53%;
    transform:translate(-50%,-50%);border-style:solid;border-width:9px 0 9px 15px;
    border-color:transparent transparent transparent #fff}
  .px-vid:hover .play{transform:scale(1.09)}
  /* SAY THE RUNTIME. These interviews are twelve to fifteen minutes.
     Pretending otherwise just ambushes people; naming it costs a few
     clicks and improves every one that is left. */
  .px-vid .len{position:absolute;right:11px;bottom:11px;padding:5px 10px;border-radius:8px;
    background:rgba(4,18,42,.82);backdrop-filter:blur(6px);
    font-size:.68rem;font-weight:700;letter-spacing:.06em;color:#B1BFD7}
  .px-vid iframe,.px-vid video{position:absolute;inset:0;width:100%;height:100%;border:0}
  .px-vid-cap{margin-top:12px;font-size:.8rem;font-weight:700;letter-spacing:.1em;
    text-transform:uppercase;color:#E8C65F}
  .px-vid-cap span{display:block;text-transform:none;letter-spacing:0;
    font-weight:500;font-size:.85rem;color:#7C89A3;margin-top:3px}

  /* THE LEAD IS A PAIR.
     One video was one argument told once, and on a page that is 5 men
     and 5 women everywhere else -- ten cards, a 5/5 filter, two equal
     doors at the close -- a single male face in the highest-status
     object on the page quietly contradicted all of it.

     TWO IS THE CEILING, NOT THE FLOOR. One video is a command and two
     is a menu; the men's home page already learned that lesson at five.
     Two is the exact number that says "both of these are true" without
     turning proof into a playlist. The four long interviews stay below,
     where a menu is what you actually want.

     THEY MUST NOT SAY THE SAME THING. His clip is the MEN's mechanism
     -- he kept it all internally, he never asked. Hers has to be the
     women's -- everyone leans on her, nobody thinks to offer, she
     stopped expecting them to. A woman restating his line would be
     balance on the surface and a wasted slot underneath. Two mechanisms,
     one page: same move, opposite costume. */
  #px-lead{display:grid;grid-template-columns:1fr 1fr;
    gap:clamp(20px,2.8vw,40px);max-width:1080px;margin:0 auto;align-items:start}
  @media(max-width:820px){#px-lead{grid-template-columns:1fr;max-width:560px}}

  /* COLLAPSE TO ONE, AUTOMATICALLY.
     Delete the placeholder column and :only-child restores the original
     single-video layout -- the wide measure, the big pull quote, the
     lot. There is no CSS anyone has to remember to change, which means
     this cannot rot into a half-migrated state if her clip never
     arrives. It also works in the other direction: paste a third column
     in and the pair becomes a row of three. */
  #px-lead > *:only-child{max-width:760px;margin:0 auto}

  #px-lead .pull{font-family:'Poppins','Montserrat',sans-serif;font-style:italic;
    font-weight:300;font-size:clamp(17px,1.75vw,24px);line-height:1.36;color:#F1ECE1;
    text-align:center;margin:22px auto 12px;max-width:34ch}
  #px-lead .pull b{font-style:normal;font-weight:600}
  #px-lead > *:only-child .pull{font-size:clamp(20px,2.4vw,32px);max-width:44ch;margin-top:26px}
  #px-lead .px-vid-cap{text-align:center}

  /* The lead placeholder carries a third line the small ones do not --
     WHO TO ASK. A to-do that names the next action gets done; one that
     only names the gap gets looked at. */
  .px-ph em{font-style:normal;font-size:.72rem;line-height:1.6;color:#8E9BB2;max-width:36ch}
  /* HIERARCHY BETWEEN PLACEHOLDERS. In the build state there are five
     dashed coral boxes on one screen and they were reading as a single
     undifferentiated field -- which flattens the one that actually
     matters. The lead slot is the urgent one; the four interview slots
     are a backlog. So the backlog sits back. Costs nothing once they
     are all filled, and until then it tells you what to do first. */

  /* TWO ACROSS, NOT FOUR. With the lead pair gone these are the whole
     section rather than a footnote under it, so they get the room. Four
     across made each one 230px of navy with three lines of type in it,
     which read as a button bar; at two across they read as four
     conversations you could choose between. */
  #px-more-vids{display:grid;grid-template-columns:1fr 1fr;
    gap:clamp(18px,2.2vw,30px);max-width:980px;margin:clamp(28px,3.4vw,42px) auto 0}
  @media(max-width:700px){#px-more-vids{grid-template-columns:1fr;max-width:460px}}

  /* ── TYPOGRAPHIC COVERS, AND THIS WAS A DEAD END REACHED HONESTLY ──
     Three ways of using YouTube's own images were tried and all three
     failed for the same underlying reason.

     1. maxresdefault as-is. Magenta and purple bokeh against navy, four
        different designs, and drop-shadowed display type baked into the
        pixels. "FEAR RAN MY LIFE" is typography inside a JPEG: invisible
        to Google, invisible to a screen reader. That is precisely the
        fault this whole redesign exists to undo, walking back in
        through the video block.
     2. Duotone plus crop. The navy tint does kill the palette clash
        completely. The baked-in type does not move: it sits dead centre
        and runs most of the width, so every zoom that loses the words
        also loses the face, and you end up with an extreme close-up of
        somebody's eye socket. Tested at 140%, 175% and 250%. All three
        still carried readable fragments of the headline.
     3. Storyboard frames (hq1/hq2/hq3). These ARE clean of text, which
        looked like the answer for about a minute. They are grabs from a
        Zoom active-speaker view, so whoever is talking fills the frame,
        and across four videos they show Daniel three times out of four.
        A testimonials page whose client covers are all the coach is
        worse than no covers.

     So the covers are made of type. Navy, gold hairline, the client's
     name and country as real indexable text, and the video's framing
     line set in italic without quote marks so it can never be mistaken
     for something the client said. It costs nothing, it cannot clash,
     and it says who is speaking, which is the one job a cover has here.

     [CLIENT] THIS IS THE INTERIM, NOT THE IDEAL. Upload custom
     thumbnails on YouTube: one still of the CLIENT, no text, framed the
     same way four times. Then add a .cover span back to each button
     with background-image set to the maxresdefault URL, and the rule
     below does the rest. Everything is already in place for it. */
  /* #px-hear wins on specificity over a bare class, so the cream-band
     override (#px-hear .px-vid{background:#DAD4C7}) was painting over
     these covers and leaving pale cards with navy text on them. The
     id has to be carried here too. Same trap as the headline and the
     pull quote earlier: anything the cream band sets has to be
     out-ranked explicitly, not by source order. */
  /* ── REAL COVERS NOW. THE TYPE CAME OFF THE IMAGE. ───────────────
     The name and the framing line used to sit ON the card because there
     was nothing else to put there. With photographs they move
     underneath, for a reason that is specific to these four frames:
     they are Zoom captures, and in Silja's and Brian's the face sits
     LOW, around 55-85% of the height. A bottom scrim carrying a name
     would have run straight across two of the four chins.

     So the image stays clean, the caption goes below it, and the only
     thing overlaid is the play button in the corner on a soft radial.

     The navy gradient stays as the card's own background. If an image
     is ever missing the card reads as a dark card rather than as a
     hole, and the caption underneath still says who it is. */
  #px-hear .px-vid-yt,.px-vid-yt{background:linear-gradient(160deg,#0B2148 0%,#061938 55%,#04122A 100%);
    display:block;padding:0}
  .px-vid-yt .cover{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;
    filter:saturate(.94) brightness(.94);transition:filter .35s ease,transform .45s ease}
  .px-vid-yt:hover .cover{filter:saturate(1) brightness(1);transform:scale(1.035)}
  /* A soft corner shade so the play button always has something to sit
     on, whatever the frame behind it is doing. */
  .px-vid-yt .shade{position:absolute;inset:0;pointer-events:none;
    background:radial-gradient(120% 90% at 92% 96%,rgba(4,18,42,.72) 0%,rgba(4,18,42,0) 58%)}

  .px-vidcap{margin-top:14px}
  .px-vidcap .nm{color:#1E2A3D;font-weight:700;font-size:clamp(15px,1.2vw,17px);line-height:1.3}
  .px-vidcap .nm i{font-style:normal;font-size:.66rem;letter-spacing:.16em;text-transform:uppercase;
    font-weight:700;color:#A08A5E;margin-left:.7em}
  .px-vidcap .line{display:block;margin-top:5px;font-family:'Poppins','Montserrat',sans-serif;
    font-style:italic;font-weight:300;font-size:clamp(14.5px,1.1vw,16.5px);line-height:1.45;color:#6E6960}
    font-size:clamp(16px,1.35vw,19px);line-height:1.3}
  .px-vid-yt:hover .line{color:#B1BFD7}
  .px-vid-yt .play{width:50px;height:50px;left:auto;right:clamp(18px,2vw,24px);
    bottom:clamp(18px,2vw,24px);z-index:2}
  .px-vid-yt .play::after{border-width:8px 0 8px 13px}

  /* PLACEHOLDER. Loud, dashed, coral, and impossible to ship by
     accident. A slot with no data-yt renders as this instead of a
     broken thumbnail. Nothing on this page is allowed to imply a video
     exists before it does. */
  .px-ph{position:relative;display:flex;flex-direction:column;align-items:center;
    justify-content:center;text-align:center;gap:9px;padding:24px;aspect-ratio:16/9;
    background:repeating-linear-gradient(45deg,rgba(255,80,31,.05) 0 12px,rgba(255,80,31,.10) 12px 24px);
    border:2px dashed #FF501F;border-radius:16px}
  .px-ph b{font-size:.66rem;letter-spacing:.18em;text-transform:uppercase;color:#FF501F;font-weight:800}
  .px-ph span{font-size:.84rem;line-height:1.6;max-width:32ch;color:#E6BCAC}

/* ═══ 04 · THE WALL ═══════════════════════════════════════════════
   THE SWITCH.
   One page, one wall, a real filter. The alternative -- two stacked
   sections -- makes whichever half comes second read as an afterthought,
   and splitting into two URLs halves the proof density on both.

   It is deep-linkable. /testimonials#men lands with the men's five
   already showing, so the men's page and the women's page can each link
   people into the half that is theirs without anyone maintaining a
   second page. It defaults to ALL, because a cold visitor who has not
   told us who they are should see all ten.

   NO COUNTS IN THE LABELS. An earlier pass had "Men 5 / Women 5" on
   the grounds that a number is a promise and a bare label is a mystery
   box. True in general, wrong here: the number also tells a visitor
   exactly how many men have ever recommended this, which is the one
   figure this page must never volunteer. The wall sits directly
   underneath and answers it in a glance anyway. */
  #px-wall-sec{background:#0A1D3C}
  #px-switch{display:inline-flex;gap:4px;padding:4px;border-radius:999px;
    background:rgba(4,18,42,.7);border:1px solid rgba(241,236,225,.12)}
  #px-switch button{appearance:none;border:0;cursor:pointer;background:transparent;
    font-family:inherit;font-size:.84rem;font-weight:700;letter-spacing:.06em;
    color:#8E9BB2;padding:.72em 1.5em;border-radius:999px;
    min-height:40px;display:inline-flex;align-items:center;justify-content:center;
    transition:background .25s ease,color .25s ease}
  #px-switch button:hover{color:#F1ECE1}
  #px-switch button[aria-selected="true"]{background:#FF501F;color:#fff}
  #px-switch button:focus-visible{outline:2px solid #E8C65F;outline-offset:2px}
  #px-switch b{font-weight:700;opacity:.65;margin-left:.45em}
  /* The switch is not rendered until the script confirms it can drive
     it -- otherwise a no-JS visitor gets three dead buttons above a
     wall that never changes. */
  #px-switch-wrap{display:none;margin-bottom:clamp(28px,3.4vw,40px)}
  .px-js #px-switch-wrap{display:block}

  /* STRETCH, NOT START. align-items:start left three cards of wildly
     different length hanging at different depths and the wall read as
     debris. Stretched, every row shares a floor and the wall reads as a
     wall. What the cards do INSIDE that floor is a separate question,
     answered at .px-outcome below. */
  /* EXPLICIT COLUMN COUNTS, NOT auto-fill.
     auto-fill decides the column count from available width, which
     means nothing in CSS or JS can know what it settled on without
     measuring it back. The orphan rule below has to know, so the
     breakpoints are declared outright. Same result at every width,
     now legible to the code that depends on it. */
  #px-wall{display:grid;grid-template-columns:repeat(3,1fr);
    gap:clamp(16px,1.8vw,24px);align-items:stretch}
  @media(max-width:1040px){#px-wall{grid-template-columns:repeat(2,1fr)}}
  @media(max-width:700px){#px-wall{grid-template-columns:1fr}}

  /* THE ORPHAN SITS IN THE MIDDLE.
     Nine cards give a clean 3x3 on All, but the filter does not inherit
     that: Men is four, which renders as a row of three plus one card
     hanging off the left edge looking like a loading error.

     Centring it is the whole fix. A lone card under the middle of the
     row above reads as a deliberate close; the same card hard against
     the left margin reads as something that failed to arrive. Nothing
     else changes, and no further testimonial has to be cut to satisfy
     a grid.

     It cannot be done in CSS alone. :nth-of-type counts every article
     in source order including the ones the filter has hidden, so the
     rule would fire on the wrong card the instant anything is filtered.
     It is applied from JS instead, by the same function that does the
     filtering, so the two can never disagree. Three columns only: at
     two there is no middle to sit in, and at one there is no orphan. */
  .px-card[hidden]{display:none}

  /* THE CARD.
     Every one of these ten testimonials arrived in the same three
     beats, unprompted: what it was like, what happened, what it is
     like now. That structure is a gift and the card is built on it.

     BEFORE and NOW are always open. Those are the two the visitor is
     actually shopping for -- recognition, then outcome. The middle beat
     (what Daniel did) goes behind a disclosure, because it is the one
     that varies most in length and it is the one a skimmer skips
     anyway. Nothing load-bearing is hidden.

     <details> is deliberate: no JavaScript, keyboard operable for free,
     and search engines index the closed content. A JS accordion would
     have cost all three. */
  .px-card{background:#061938;border:1px solid rgba(241,236,225,.10);border-radius:16px;
    padding:clamp(24px,2.4vw,32px);display:flex;flex-direction:column;
    transition:border-color .3s ease,transform .3s ease}
  .px-card:hover{border-color:rgba(232,198,95,.3);transform:translateY(-2px)}

  /* SMALL FACES, AND THIS IS THE WHOLE DESIGN RULE OF THE SECTION:
     THE WORDS ARE THE PROOF. THE FACE IS ONLY THE VERIFICATION.
     A face has to be big enough to say "a human being said this" and
     not one pixel bigger. The moment it is large enough to admire as a
     photograph it starts competing with the sentence it exists to
     authenticate -- and it invites the one question you never want
     asked here, which is whether these people are real.

     Every face below is cropped out of the original flattened card with
     Wix's own /v1/crop/ transform. No re-upload, no reshoot, no new
     asset to manage -- and the words beside it are now text. */
  .px-who{display:flex;align-items:center;gap:14px;margin-bottom:24px}
  /* 76px, NOT 60. The 60px rule was inherited from the home pages,
     where the testimonials are a supporting section three quarters of
     the way down and a large face would compete with the argument. On
     THIS page they are not supporting anything -- they are the whole
     argument, and "these are real people" is the entire claim. The
     faces are also the only photographic material the page has. Bigger
     is both truer and the cheapest texture available. Still nowhere
     near portrait scale, so they cannot start reading as IMAGERY. */
  .px-face{width:76px;height:76px;flex:0 0 auto;object-fit:cover;border-radius:999px;
    filter:saturate(.92) contrast(1.02);
    box-shadow:0 0 0 1px rgba(232,198,95,.30), 0 8px 18px -10px rgba(3,12,28,.9)}
  .px-who .n{font-size:.95rem;font-weight:700;color:#F1ECE1;line-height:1.35}
  .px-who .n span{display:block;font-size:.7rem;font-weight:700;letter-spacing:.14em;
    text-transform:uppercase;color:#E8C65F;margin-top:3px}

  /* 11px, not 9.9. BEFORE and NOW are the only signposts inside a card
     and they were the smallest type on the page. Uppercase at heavy
     weight with .2em tracking survives small sizes better than most
     things, which is how it got away with it, but under 10px it stops
     being read and starts being a texture above a paragraph. */
  .px-beat{font-size:.69rem;letter-spacing:.18em;text-transform:uppercase;
    font-weight:800;color:#6A7893;margin-bottom:9px}

  /* ── WHY THE CARD BODY IS NOT POPPINS ────────────────────────────
     It was, at weight 300, 19.5px, leading 1.46 -- and it was genuinely
     hard to read. Three separate causes, all pulling the same way:

     1. POPPINS IS A DISPLAY FACE. It is geometric: perfectly circular
        o, e, a, c, with very small apertures. At 300 the strokes go
        thin and those already-tight counters start closing up, so the
        letters stop resolving individually and the line turns into
        texture. The women's page hit the same wall from the other end
        and its own note says it: "Poppins at 300 goes weak and spindly
        at 60px." If it is spindly in a HEADLINE it has no business
        carrying a six-line paragraph.

     2. THE LEADING WAS TIGHTER THAN THE PAGE'S. Body copy on this site
        is 1.7. The card had been pushed down to 1.46, which is a
        HEADLINE setting -- correct for two lines, punishing for six,
        because the eye loses its return path on every carriage.

     3. IT WAS A SOLID LUMP. Five or six lines, no internal break, no
        paragraph space, tight leading. Any one of those is survivable.
        Together they make a block the eye slides off.

     The fix is not a bigger font. It is the READING face at the
     PAGE's leading. Montserrat is a neutral grotesque with open
     apertures and it is what the rest of the site reads in.

     Poppins keeps every job it is actually good at: the hero, the
     section headings, the fragment strip (one line each), the video
     pull quote. It just stops doing the one job it is bad at.

     BEFORE AND NOW ARE NOW THE SAME SIZE AND WEIGHT, deliberately.
     The hierarchy between them was never carried by type -- it is
     carried by the beat labels, the colour step (#DDE5F0 to cream) and
     the gold rule down the outcome. Those still work, and the card
     gained a consistent reading rhythm by giving up a distinction it
     did not need. */
  .px-before{font-weight:400;font-size:clamp(15.5px,1.12vw,16.5px);
    line-height:1.75;color:#DDE5F0;margin-bottom:22px}
  /* THE ONLY CREAM IN THE CARD IS THE OUTCOME. One thing per card is
     allowed to be the brightest thing, and on a testimonials page it is
     never the problem. */
  /* NO margin-top:auto HERE, AND THAT IS A REVERSAL.
     Pinning the outcome to the card floor was meant to give a row of
     three a shared baseline you could scan for results. In practice it
     put a hole in the MIDDLE of every card whose story ran shorter than
     its neighbours: a visible empty band between the disclosure and the
     outcome, which reads as a rendering fault rather than as rhythm.
     Trailing space at the BOTTOM of a short card is the normal,
     invisible thing. A gap in the middle is not. The cards still
     stretch, so the row keeps its shared floor; the content simply
     sits at the top of it, the way content does everywhere else. */
  .px-outcome{}
  .px-now{font-weight:400;font-size:clamp(15.5px,1.12vw,16.5px);line-height:1.75;
    color:#F1ECE1;border-left:2px solid rgba(232,198,95,.45);padding-left:17px}

  /* THE DISCLOSURE IS A DIVIDER, NOT A HEADING.
     v1 put the toggle immediately above the outcome block and it read
     as though "What changed" was the LABEL for the outcome -- so the
     card appeared to have two headings and no middle. A hairline above
     it and the outcome carrying its own beat label fixes the reading
     order without moving anything. */
  .px-card details{margin:0 0 18px;padding-top:15px;
    border-top:1px solid rgba(241,236,225,.09)}
  /* THE TOGGLE RESTS QUIET AND WAKES CORAL.
     Ten of these are on screen at once. At full coral they were ten
     orange stamps scattered through the wall, and they were louder than
     the words they sit between. Coral still MEANS the same thing here
     (it is a control, the reader operates it) -- it just does not need
     to shout at rest. The caret stays coral so the affordance is never
     in doubt, and the whole thing goes coral on hover and focus. One
     stays lit: the one you are pointing at. */
  /* THE TOGGLE WAS A 21px TAP TARGET.
     It is the only control inside a card and it measured 139x21, against
     a 44px minimum on touch. Padding grows the hit area and a matching
     negative margin takes the layout change back out, so the card looks
     identical and the thumb gets somewhere to land. */
  .px-card summary{list-style:none;cursor:pointer;display:inline-flex;align-items:center;
    gap:.5em;font-size:.76rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;
    color:#8FA0BC;transition:color .25s ease;
    padding:11px 12px 11px 0;margin:-11px 0}
  .px-card summary .cx{color:#FF501F}
  .px-card summary::-webkit-details-marker{display:none}
  .px-card summary:hover,.px-card details[open] summary{color:#F1ECE1}
  .px-card summary:focus-visible{outline:2px solid #E8C65F;outline-offset:3px;border-radius:3px}
  .px-card summary .cx{display:inline-block;transition:transform .25s ease;font-size:1.15em;line-height:1}
  .px-card details[open] summary .cx{transform:rotate(90deg)}
  .px-card details[open] summary .lbl-c{display:none}
  .px-card summary .lbl-o{display:none}
  .px-card details[open] summary .lbl-o{display:inline}
  .px-card details p{margin-top:14px;font-size:clamp(14.5px,1.05vw,15.5px);
    line-height:1.75;color:#93A3BF}

  /* EMPTY STATE. Only reachable if a filter is ever added with no
     matching cards. Cheap insurance against a blank band. */
  #px-empty{display:none;text-align:center;color:#7C89A3;padding:40px 0}

/* ═══ 05 · CREDS ══════════════════════════════════════════════════ */
  #px-creds{background:#04122A;padding-top:44px;padding-bottom:44px;
    border-top:1px solid rgba(241,236,225,.07)}

/* ═══ 06 · THE FORK ═══════════════════════════════════════════════
   TWO DOORS, NOT ONE BUTTON.
   Somebody who has just read five stories that sound like their own
   life should not be handed the same generic programme link as somebody
   who read the other five. And, per the sorting page's rule: NAME THE
   SITUATION, NEVER THE BRAND. "The Reconnected Man" means nothing to
   someone who has spent ninety seconds on this page. A sentence about
   his marriage means everything. */
  #px-fork{background:#F1ECE1;color:#5A6274;text-align:center}
  #px-doors{display:grid;grid-template-columns:1fr 1fr;gap:clamp(14px,2vw,22px);
    max-width:940px;margin:clamp(26px,3vw,38px) auto 0;text-align:left}
  @media(max-width:760px){#px-doors{grid-template-columns:1fr}}
  .px-door{display:flex;flex-direction:column;background:#fff;
    border:1px solid rgba(30,42,61,.12);border-radius:18px;
    padding:clamp(22px,2.6vw,32px);text-decoration:none;
    box-shadow:0 24px 50px -34px rgba(30,42,61,.5);
    transition:border-color .3s ease,transform .3s ease,box-shadow .3s ease}
  .px-door:hover,.px-door:focus-visible{border-color:rgba(255,80,31,.55);
    transform:translateY(-3px);box-shadow:0 30px 60px -34px rgba(30,42,61,.6);outline:none}
  .px-door .tag{font-size:.7rem;letter-spacing:.18em;text-transform:uppercase;
    font-weight:800;color:#98814F;margin-bottom:13px}
  .px-door h3{font-family:'Poppins','Montserrat',sans-serif;font-weight:400;
    font-size:clamp(18px,2vw,24px);line-height:1.26;color:#1E2A3D;margin-bottom:11px}
  .px-door p{font-size:.92rem;line-height:1.68;color:#5E5850;margin-bottom:20px}
  .px-door .go{margin-top:auto;font-size:.86rem;font-weight:700;color:#FF501F;
    display:inline-flex;align-items:center;gap:.5em}

  /* ONE LINE, BUT ONLY WHERE ONE LINE FITS.
     nowrap as a flat rule overflowed the viewport by 89px at 390px:
     the line simply cannot fit on a phone at any size worth reading.
     So it is a class rather than an inline style, and it is released
     below 620px, where the heading wraps to two of its own accord.
     Forcing a single line at every width would have traded a tidy
     desktop for a horizontal scrollbar on mobile. */
  .px-oneline{white-space:nowrap}
  @media(max-width:620px){.px-oneline{white-space:normal}}

  #px-fork .small{margin-top:clamp(26px,3vw,36px);font-size:.9rem;color:#6E6960}
  #px-fork .small a{color:#1E2A3D;font-weight:700;text-decoration:none;
    border-bottom:1px solid rgba(255,80,31,.45)}

/* ═══ MOBILE ══════════════════════════════════════════════════════
   The size override needs !important because the headings carry their
   clamps inline, and inline beats a media query at any specificity.
   The clamps bottom out around 27-30px; on a 390px screen with 40px of
   padding that is roughly eleven characters a line, which is what turns
   headlines into ragged three-word stacks. */
  @media(max-width:560px){
    h1.px-serif,h2.px-serif,h3.px-serif{font-size:clamp(21px,6.4vw,28px)!important;line-height:1.24!important}
    .px-sec{padding-top:clamp(40px,10vw,54px)!important;padding-bottom:clamp(40px,10vw,54px)!important}
    #px-hero{padding-top:104px!important}
    .px-hand{font-size:clamp(14px,4.6vw,20px)!important}
    #px-wall{grid-template-columns:1fr}
    /* 44px is the tap minimum on touch and these were 32. Height comes
       from min-height rather than padding so the pill keeps its shape. */
    #px-switch button{padding:.7em .95em;font-size:.8rem;min-height:44px}
    #px-count b{font-size:clamp(1.35rem,7vw,1.7rem)}
    #px-count span{font-size:.66rem;letter-spacing:.07em}
  }

/* ═══════════════════════════════════════════════════════════════════
   PtNav v3 · BAKED IN VERBATIM from "PtNav v3.dc.html"
   Do not hand-edit. If the component changes, re-copy it.
   ═══════════════════════════════════════════════════════════════════ */

/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · PRIMARY NAVIGATION · v3
   -------------------------------------------------------------------
   STRUCTURE (this is the whole argument of the redesign)
     LOGO          home. no "Home" text item — the mark does that job.
     FOR MEN   ▾   → The Reconnected Man
     FOR WOMEN ▾   → The Reconnected Woman
     ABOUT
     TESTIMONIALS
     CONTACT       coral pill, far right.

   WHY THE DOORS COME FIRST
   The sorting page's entire premise is that a visitor must choose a
   side before anything else can land. The nav has to agree with that.
   "For Men" and "For Women" are therefore the first and largest thing
   after the mark, and everything that is ABOUT DANIEL (About,
   Testimonials) sits after the reader's own doors, not before them.

   THE PARENTS ARE REAL LINKS
   "For Men" goes to the men's page; the dropdown is an addition, not
   the only way through. A nav item that can only be hovered is a dead
   end on touch and a dead end for keyboards.

   ───────────────────────────────────────────────────────────────────
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   Gold marks where you ARE (the active item, the hovered child).
   Coral marks the one thing you can DO (Contact). Nothing else in
   this bar is allowed to be coral — the moment a second thing is,
   the pill stops meaning anything.
   ═══════════════════════════════════════════════════════════════════ */
:root{
  --px-navy:#061938;
  --px-navy-deep:#04122A;
  --px-navy-ink:#030C1C;
  --px-gold:#E8C65F;
  --px-gold-lift:#F3DD93;
  --px-coral:#FF501F;
  --px-coral-lift:#FF6A3D;
  --px-cream:#F1ECE1;
  --px-mist:#B1BFD7;
  --px-slate:#7C89A3;
}

/* ═══ SHELL ═══════════════════════════════════════════════════════ */
#pt-nav,#pt-nav *{box-sizing:border-box}
#pt-nav{
  position:fixed;top:0;left:0;right:0;z-index:90;
  font-family:'Montserrat',system-ui,-apple-system,sans-serif;
  border-bottom:1px solid transparent;
  background:transparent;
  transition:background .4s ease,border-color .4s ease,backdrop-filter .4s ease;
  -webkit-font-smoothing:antialiased;
}
/* Navy on scroll. NEVER black. */
#pt-nav.is-stuck{
  background:rgba(6,25,56,.92);
  backdrop-filter:blur(16px);
  -webkit-backdrop-filter:blur(16px);
  border-bottom-color:rgba(232,198,95,.16);
}
#pt-bar{
  max-width:1360px;margin:0 auto;
  padding:16px clamp(20px,4vw,52px);
  display:flex;align-items:center;gap:clamp(16px,2.2vw,28px);
  position:relative;
}

/* ═══ LOGO ════════════════════════════════════════════════════════ */
#pt-logo{flex:0 0 auto;display:flex;align-items:center;line-height:0;
  border-radius:6px;text-decoration:none;transition:opacity .25s ease}
#pt-logo:hover{opacity:.82}
#pt-logo:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:6px}
/* If the CDN ever fails, the alt text renders as ordinary cream wordmark
   text rather than a giant blue underlined link. */
#pt-logo img{height:30px;width:auto;display:block;
  font-size:.82rem;font-weight:700;letter-spacing:.02em;color:#F1ECE1}
@media(max-width:600px){#pt-logo img{height:26px}}

/* ═══ LINK RAIL ═══════════════════════════════════════════════════ */
#pt-links{
  display:flex;align-items:center;gap:clamp(20px,2.4vw,34px);
  margin-left:auto;
}
.pt-item{position:relative;display:flex;align-items:center}
.pt-link{
  display:inline-flex;align-items:center;gap:.35em;
  font-size:.9rem;font-weight:600;letter-spacing:.005em;
  color:#B1BFD7;white-space:nowrap;text-decoration:none;
  padding:6px 0;border-radius:4px;
  transition:color .2s ease;
}
.pt-link:hover,.pt-link:focus-visible{color:#F1ECE1;outline:none}
.pt-link:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:4px}

/* WHERE YOU ARE. Gold, plus a rule under it so it survives being
   read by someone who cannot separate gold from mist. */
.pt-item.is-active > .pt-link,
.pt-link.is-active{color:#E8C65F}
.pt-item.is-active > .pt-link::after,
.pt-link.is-active::after{
  content:"";position:absolute;left:0;right:0;bottom:-2px;height:2px;
  background:rgba(232,198,95,.75);border-radius:2px;
}
.pt-link{position:relative}

/* ═══ CARET + DROPDOWN ════════════════════════════════════════════ */
.pt-caret{
  background:none;border:none;padding:6px 2px;margin:0;
  font-family:inherit;font-size:.72em;line-height:1;
  color:inherit;cursor:pointer;
  display:inline-flex;align-items:center;
  transition:transform .25s ease,color .2s ease;
  color:#7C89A3;
}
.pt-item:hover .pt-caret{color:#B1BFD7}
.pt-caret:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:3px;border-radius:4px}
.pt-item:hover .pt-caret,
.pt-item.is-open .pt-caret{transform:rotate(180deg)}

.pt-menu{
  position:absolute;top:100%;left:50%;
  transform:translateX(-50%);
  min-width:252px;
  display:none;flex-direction:column;gap:2px;
  margin-top:14px;
  background:rgba(4,18,42,.98);
  backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);
  border:1px solid rgba(232,198,95,.18);
  border-radius:14px;padding:10px;
  box-shadow:0 30px 60px -20px rgba(3,12,28,.8);
}
/* The 14px gap between the item and the menu would break a hover
   chain, so the bridge below keeps the pointer "inside" while it
   crosses. Without it the menu flickers shut mid-travel. */
.pt-menu::before{content:"";position:absolute;top:-16px;left:0;right:0;height:16px}

/* Desktop opens on hover AND on keyboard focus. No JS involved. */
@media(min-width:1024px){
  .pt-item:hover .pt-menu,
  .pt-item:focus-within .pt-menu,
  .pt-item.is-open .pt-menu{display:flex}
}
.pt-menu a{
  padding:11px 14px;border-radius:9px;
  font-size:.88rem;font-weight:500;color:#D7DEEA;
  white-space:nowrap;text-decoration:none;
  transition:background .2s ease,color .2s ease;
}
.pt-menu a:hover,.pt-menu a:focus-visible{
  background:rgba(232,198,95,.1);color:#E8C65F;outline:none}
.pt-menu a:focus-visible{box-shadow:inset 0 0 0 1px rgba(232,198,95,.55)}
.pt-menu .pt-sub{
  display:block;font-size:.63rem;letter-spacing:.18em;text-transform:uppercase;
  font-weight:800;color:#7C89A3;margin-bottom:3px}
.pt-menu a:hover .pt-sub{color:rgba(232,198,95,.72)}

/* ═══ CONTACT · THE ONLY CORAL IN THE BAR ═════════════════════════ */
#pt-cta-wrap{display:flex;align-items:center;gap:14px;flex:0 0 auto;margin-left:clamp(8px,1.6vw,20px)}
#pt-cta{
  display:inline-flex;align-items:center;gap:.5em;
  background:#FF501F;color:#FFFFFF;
  font-weight:700;font-size:.86rem;
  padding:.72em 1.45em;border-radius:999px;
  white-space:nowrap;text-decoration:none;
  transition:background .25s ease,transform .25s ease,box-shadow .25s ease;
}
#pt-cta:hover,#pt-cta:focus-visible{
  background:#FF6A3D;transform:translateY(-1px);
  box-shadow:0 10px 24px -10px rgba(255,80,31,.7);outline:none}
#pt-cta:focus-visible{outline:2px solid #F3DD93;outline-offset:3px}
#pt-cta .arrow{font-size:.9em;transition:transform .25s ease}
#pt-cta:hover .arrow{transform:translateX(3px)}

/* ═══ BURGER ══════════════════════════════════════════════════════ */
#pt-burger{
  display:none;background:none;border:none;cursor:pointer;
  flex-direction:column;justify-content:center;gap:5px;
  padding:8px;margin-right:-8px;border-radius:6px}
#pt-burger:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:2px}
#pt-burger span{display:block;width:24px;height:2px;background:#F1ECE1;border-radius:2px;
  transition:transform .3s ease,opacity .2s ease}
#pt-nav.menu-open #pt-burger span:nth-child(1){transform:translateY(7px) rotate(45deg)}
#pt-nav.menu-open #pt-burger span:nth-child(2){opacity:0}
#pt-nav.menu-open #pt-burger span:nth-child(3){transform:translateY(-7px) rotate(-45deg)}

/* ═══ MOBILE ══════════════════════════════════════════════════════ */
@media(max-width:1023px){
  #pt-burger{display:flex}
  #pt-nav{background:rgba(6,25,56,.92);backdrop-filter:blur(16px);
    -webkit-backdrop-filter:blur(16px)}

  #pt-links{
    position:absolute;top:100%;left:0;right:0;
    flex-direction:column;align-items:stretch;gap:2px;
    margin-left:0;
    /* SOLID, AND NO BACKDROP FILTER ON THIS ONE. The bar above keeps its
       blur; this panel cannot have one. Nested backdrop-filters inside a
       shadow root make Chromium composite this element against an already
       filtered backdrop, and the page behind reads straight through a
       background that is 98.5% opaque. On the built-in version of this nav
       the hero headline was legible through the open menu. The bar's own
       blur still gives the effect where it matters. */
    background:#061938;
    border-bottom:1px solid rgba(232,198,95,.16);
    padding:10px clamp(16px,4vw,44px) 22px;
    max-height:calc(100vh - 68px);overflow-y:auto;
    display:none;
  }
  #pt-nav.menu-open #pt-links{display:flex}

  .pt-item{flex-wrap:wrap;justify-content:space-between;
    border-bottom:1px solid rgba(241,236,225,.07)}
  .pt-item:last-child{border-bottom:none}
  .pt-link{flex:1 1 auto;padding:15px 2px;font-size:1rem}
  .pt-item.is-active > .pt-link::after,.pt-link.is-active::after{display:none}
  .pt-caret{flex:0 0 auto;padding:15px 10px;font-size:.85em}

  /* Child list is a plain indented block on mobile. A floating panel
     inside a panel is a place to get lost. */
  .pt-menu{
    position:static;transform:none;min-width:0;width:100%;
    margin:0 0 12px;padding:0 0 0 14px;
    background:transparent;backdrop-filter:none;-webkit-backdrop-filter:none;
    border:none;border-left:2px solid rgba(232,198,95,.28);
    border-radius:0;box-shadow:none;
    flex-basis:100%;
  }
  .pt-menu::before{display:none}
  .pt-item.is-open .pt-menu{display:flex}
  .pt-menu a{padding:12px 12px;font-size:.94rem}

  #pt-cta-wrap{margin-left:auto}
  #pt-cta{font-size:.8rem;padding:.68em 1.2em}
  #pt-cta .cta-long{display:none}
}
@media(max-width:420px){
  #pt-cta{padding:.66em 1.05em}
  #pt-cta .arrow{display:none}
}


/* ═══ THE 320px BAR ═══════════════════════════════════════════════
   At 320 the logo, the Contact pill and the burger add up to more than
   the bar, and the burger hangs 9px off the right edge. Nothing here is
   a redesign: the logo loses 4px, the bar loses 6px of padding either
   side, and the pill tightens. Added 4 Aug 2026 to all three pages and
   to PtNav v3.dc.html, which stays the source of record. */
@media(max-width:360px){
  #pt-bar{padding-left:14px;padding-right:14px;gap:10px}
  #pt-logo img{height:22px}
  #pt-cta{padding:.6em .82em;font-size:.8rem}
  #pt-burger{width:34px;margin-right:-4px}
}

@media(prefers-reduced-motion:reduce){
  #pt-nav,#pt-nav *{transition:none!important}
}

/* ═══════════════════════════════════════════════════════════════════
   PtFooter v3 · BAKED IN VERBATIM from "PtFooter v3.dc.html"
   Do not hand-edit. If the component changes, re-copy it.
   ═══════════════════════════════════════════════════════════════════ */

/* ═══════════════════════════════════════════════════════════════════
   PARALLAXX · SITE FOOTER · v3  ·  THREE COLUMNS + UTILITY ROW
   -------------------------------------------------------------------
   THE PROBLEM WITH v2
   Twenty-four links in four equal columns. Every one of them the same
   size, the same colour, the same weight. On a site whose entire
   premise is "pick one of two doors", the last thing on the page was
   a sitemap that made twenty-four things look equally important.

   THE FIX IS NOT FEWER LINKS. IT IS HIERARCHY.
   Seven pages on this site are linked from NOWHERE except this footer:
   Blog, both podcasts, Progress Journal, Three Toxic Lies, Identity
   2.0, As Seen In. Delete them from here and they are orphaned:
   unreachable by a human, and invisible to search because nothing
   points at them. So they stay. They just stop shouting.

   WHAT THE VISITOR SEES NOW
     THREE COLUMNS, LARGE   the two doors, then Daniel. nine links.
                            the whole site, as far as anyone arriving
                            cares.
     ONE QUIET ROW, SMALL   everything else, inline, dot-separated,
                            in the register of a footnote rather than
                            a menu. Reachable. Not competing.

   Reading order does the work: doors first at full weight, then the
   row you only look at if you were already looking for something
   specific, then legal. Each tier is quieter than the one above it.

   WHY THERE IS NO BLURB UNDER THE LOGO
   A footer does not need to restate the pitch. Everything above it has
   already spoken, and by the time anyone is down here they are looking
   for a link, not for the argument again.

   WHY THE GIVE IT ALL BANNER IS GONE
   It was a full-width tinted panel with a badge and two lines of copy,
   for a brand that reads as a men's event. Under a footer that serves
   two avatars it was the single loudest thing on the page, and it was
   shouting at half the audience. It is now one gold line in Daniel's
   column, which is the correct size for a sibling brand.

   ───────────────────────────────────────────────────────────────────
   TOKENS shared with GIVE IT ALL:  navy #061938  gold #E8C65F  Montserrat
   TOKENS Parallaxx only:  coral #FF501F  Poppins  cream #F1ECE1

   THE ACCENT RULE. GOLD IS DANIEL. CORAL IS YOU.
   There is NO coral down here, on purpose. Coral marks the reader's
   move, and every ask has already been made: the nav carries a coral
   Contact pill on every page, and the page above closes with its own.
   GIVE IT ALL is gold, because it is his world, not your move.
   ═══════════════════════════════════════════════════════════════════ */
:root{
  --px-navy:#061938;
  --px-navy-deep:#04122A;
  --px-navy-ink:#030C1C;
  --px-gold:#E8C65F;
  --px-gold-lift:#F3DD93;
  --px-coral:#FF501F;
  --px-coral-lift:#FF6A3D;
  --px-cream:#F1ECE1;
  --px-mist:#B1BFD7;
  --px-slate:#7C89A3;
}

#pt-foot,#pt-foot *{box-sizing:border-box}
#pt-foot{
  background:#030C1C;
  border-top:1px solid rgba(232,198,95,.16);
  font-family:'Montserrat',system-ui,-apple-system,sans-serif;
  color:#B1BFD7;-webkit-font-smoothing:antialiased}
#pt-foot-inner{max-width:1360px;margin:0 auto;
  padding:clamp(34px,3.6vw,46px) clamp(20px,4vw,52px) 24px}

/* ═══ TIER 1 · IDENTITY + THE THREE COLUMNS ═══════════════════════
   Two doors and Daniel. The doors are the reader's two routes in;
   the third is everywhere Daniel himself shows up, which is what the
   GIVE IT ALL bar used to be doing at ten times the size. */
#pt-foot-top{
  display:grid;
  grid-template-columns:minmax(260px,1fr) repeat(3,minmax(0,.72fr));
  gap:clamp(30px,3.4vw,52px);
  align-items:start;
  margin-bottom:clamp(28px,3vw,36px)}
/* Identity spans the full width once the columns need the room. */
@media(max-width:1080px){#pt-foot-top{grid-template-columns:repeat(3,minmax(0,1fr));gap:38px 30px}
  #pt-ident{grid-column:1/-1}}
/* THREE COLUMNS ALL THE WAY DOWN. This used to fall to two at 680 and to
   one at 440, which on a 390px phone made the footer 878px tall, taller
   than most of the sections above it and by a long way the tallest thing
   nobody came for. Held at three it is 563px, a third shorter, and the
   two-up version was the worst of both: it put Daniel alone on a row and
   left half of it empty.

   Holding three also keeps the footer the same object it is on desktop.
   Three doors, three columns, whatever the device. The type comes down
   at the bottom breakpoint to buy the width, which is the right trade
   for a block that is scanned rather than read. */
@media(max-width:680px){#pt-foot-top{gap:30px 20px}}
@media(max-width:440px){
  #pt-foot-top{gap:22px 12px}
  .pt-door h4{font-size:.6rem;letter-spacing:.12em;margin-bottom:10px;padding-bottom:8px}
  .pt-door a{font-size:.78rem;line-height:1.28;padding:4px 0}
}

#pt-foot-logo{display:inline-block;line-height:0;margin-bottom:14px;text-decoration:none}
#pt-foot-logo img{height:30px;width:auto;display:block;
  font-size:.8rem;font-weight:700;color:#F1ECE1}

#pt-social{display:flex;gap:18px;flex-wrap:wrap;margin-top:16px}
#pt-social a{font-size:.85rem;font-weight:600;color:#B1BFD7;
  text-decoration:none;transition:color .2s ease}
#pt-social a:hover,#pt-social a:focus-visible{color:#E8C65F;outline:none}

/* ── THE DOORS. Deliberately larger than anything else down here. ── */
.pt-door h4{
  font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;
  font-weight:800;color:#A88F4E;margin:0 0 12px;
  padding-bottom:9px;border-bottom:1px solid rgba(232,198,95,.18)}
/* THE WHOLE BLOCK IS DELIBERATELY SHALLOW. Nothing down here is being
   sold. It exists so that seven otherwise-orphaned pages are reachable
   and so somebody hunting for terms or a podcast can find them. Every
   vertical value in this file was cut roughly a third for that reason,
   and the type went with it. If it starts feeling like a section again,
   it has grown.

   .92rem, not 1.02. At 1.02 these matched the page's body copy exactly,
   which made the footer read as another content section rather than as
   the foot of the page. They are still the largest thing down here, and
   still clearly larger than the utility row at .82, so the hierarchy the
   note above describes is intact. The size came off the type and went
   into padding instead, so the tap target grew rather than shrank.

   margin-bottom went 1px -> 9px on 5 Aug. The padding already made each
   link a decent target, but stacked with 1px between them the boxes
   were effectively continuous, so an imprecise tap in a column landed
   on a neighbour rather than on nothing. The targets are the same size;
   there is simply somewhere to miss now. */
.pt-door a{
  display:block;color:#D7DEEA;font-size:.92rem;font-weight:500;
  line-height:1.3;padding:5px 0;margin-bottom:9px;text-decoration:none;
  border-radius:3px;transition:color .2s ease}
.pt-door a:first-of-type{padding-top:0}
.pt-door a:last-child{margin-bottom:0}
.pt-door a:hover,.pt-door a:focus-visible{color:#E8C65F;outline:none}

/* THE SIBLING BRAND, AS A LINE RATHER THAN A BANNER.
   It stays GOLD, because gold is Daniel and GIVE IT ALL is his room.
   That single colour is now the whole signal — it used to be a
   42px badge, a border, a tinted panel and two lines of body copy,
   which made a men's-retreat brand the loudest thing in a footer
   serving two avatars. */
.pt-door a.ext{color:#E8C65F;font-weight:600}
.pt-door a.ext:hover,.pt-door a.ext:focus-visible{color:#F3DD93}
.pt-door a.ext .mk{font-size:.8em;margin-left:.35em;opacity:.8}

/* ═══ TIER 2 · THE UTILITY ROW ════════════════════════════════════
   Everything the site has that is not a door. Small, inline, dot
   separated. This is the register of a footnote, and that is the
   point: it is here for the person who came looking for it, and
   invisible to the person who did not. */
/* SPACING, NOT SIZE. Measured on a 390px viewport, adjacent links in
   this row sat 0px apart: the dot separator is an ::after INSIDE the
   anchor, so each link's hit box ran all the way to the next one's.
   Nothing here is too small to hit; the problem was that a near miss
   could not miss. A thumb aimed at "Blog" drifting three pixels right
   opened "Progress Journal", and a wrong destination is worse than a
   dead tap, because there is no retry, only a back button.

   So the dot now hugs the link it belongs to and the flex gap carries
   the separation. Visually the row is within a couple of pixels of
   where it was. Between the hit boxes there is now 18px of nothing. */
#pt-util{
  display:flex;flex-wrap:wrap;align-items:center;
  gap:9px 18px;
  padding-top:18px;border-top:1px solid rgba(241,236,225,.1);
  margin-bottom:14px}
#pt-util a{
  color:#6E7B94;font-size:.82rem;text-decoration:none;
  padding:3px 0;border-radius:3px;white-space:nowrap;
  transition:color .2s ease}
#pt-util a:hover,#pt-util a:focus-visible{color:#E8C65F;outline:none}
/* Separator TRAILS its link rather than leading the next one. With
   ::before, every line the row wraps onto opens with an orphaned dot.
   Trailing means a wrapped line starts on a word, which is what the
   eye expects, and the dot left at a line end reads as continuation. */
#pt-util a:not(:last-child)::after{
  content:"·";color:#3E4A63;margin:0 0 0 9px;display:inline-block}

/* ═══ TIER 3 · LEGAL ══════════════════════════════════════════════ */
#pt-legal{
  display:flex;justify-content:space-between;gap:16px;flex-wrap:wrap;
  color:#5E6B85;font-size:.8rem;line-height:1.6}
#pt-legal .links{display:inline-flex;gap:18px;flex-wrap:wrap}
#pt-legal a{color:inherit;text-decoration:none;
  border-bottom:1px solid rgba(232,198,95,.4);padding-bottom:1px;
  transition:color .2s ease,border-color .2s ease}
#pt-legal a:hover,#pt-legal a:focus-visible{
  color:#B1BFD7;border-bottom-color:#E8C65F;outline:none}

#pt-foot a:focus-visible{outline:2px solid rgba(232,198,95,.7);outline-offset:3px}
@media(prefers-reduced-motion:reduce){#pt-foot,#pt-foot *{transition:none!important}}`;

  var HTML = `<div id="px-root">

<!-- ══════════════ THE SHELL IS BAKED IN ══════════════
     PtNav v3 and PtFooter v3 are embedded in this file rather than
     imported or placed around it in the editor, so the page ships as
     one self-contained element with the shell it was designed against.

     ▸▸ IN THE WIX EDITOR: turn the site Header and Footer OFF for this
        page, AND do not also place the PtNav / PtFooter components
        around it. They are already here. Doing both gives two navs and
        two footers.

     ▸▸ THESE ARE COPIES. If PtNav v3 or PtFooter v3 changes, this page
        does not follow. Re-copy the CSS block in the helmet, the markup
        below, and the logic in the script at the foot of the file.

     One deliberate change from the component: the nav's active item is
     read from the URL only. v3 accepts an "active" prop for the editor
     preview, but a baked copy has no props to receive, and its own path
     table already carries
       [/^\/testimonials-daniel-lawson/, 'testimonials']
     so this page lights the right item on the live URL by itself.
     ═══════════════════════════════════════════════════════════════ -->
<header id="pt-nav">
  <div id="pt-bar">

    <!-- ══ LOGO = HOME. No text "Home" item; the mark is the link. ══ -->
    <a id="pt-logo" href="https://www.parallaxxtransformations.com" aria-label="Parallaxx Transformations — home">
      <img src="https://static.wixstatic.com/media/e1784d_fe3c841c471f47d088f0cd631a89d883~mv2.png"
           alt="Parallaxx Transformations">
    </a>

    <nav id="pt-links" aria-label="Primary">

      <!-- ══ FOR MEN ══ parent is a real link to the men's page ══ -->
      <div class="pt-item pt-has-drop" data-nav="men">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/men">For Men</a>
        <button class="pt-caret" type="button" aria-label="Show For Men menu" aria-expanded="false" aria-controls="pt-menu-men">&#9662;</button>
        <div class="pt-menu" id="pt-menu-men">
          <a href="https://www.parallaxxtransformations.com/the-reconnected-man">
            <span class="pt-sub">Programme</span>The Reconnected Man</a>
        </div>
      </div>

      <!-- ══ FOR WOMEN ══ -->
      <div class="pt-item pt-has-drop" data-nav="women">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/women">For Women</a>
        <button class="pt-caret" type="button" aria-label="Show For Women menu" aria-expanded="false" aria-controls="pt-menu-women">&#9662;</button>
        <div class="pt-menu" id="pt-menu-women">
          <a href="https://www.parallaxxtransformations.com/the-reconnected-woman">
            <span class="pt-sub">Programme</span>The Reconnected Woman</a>
        </div>
      </div>

      <div class="pt-item" data-nav="about">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/about-daniel-lawson">About</a>
      </div>

      <div class="pt-item" data-nav="testimonials">
        <a class="pt-link" href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">Testimonials</a>
      </div>

    </nav>

    <div id="pt-cta-wrap">
      <!-- CONTACT. Coral pill — Parallaxx-only accent. GIA uses a gold
           pill in this exact position: same shape, different colour.
           That is the whole family/identity trick. -->
      <a id="pt-cta" data-nav="contact" href="https://www.parallaxxtransformations.com/contact-daniel-lawson">
        Contact<span class="cta-long">&nbsp;</span><span class="arrow" aria-hidden="true">&#8594;</span>
      </a>
      <button id="pt-burger" type="button" aria-label="Menu" aria-expanded="false" aria-controls="pt-links">
        <span></span><span></span><span></span>
      </button>
    </div>

  </div>
</header>

<!-- ══════════════ 01 · HERO ══════════════
     The eyebrow is Daniel's handwriting, so it is allowed to be a
     count. The headline is not his voice at all -- it is a statement
     about them, and the next section hands over the receipts. -->
<section class="px-sec" id="px-hero">
  <div class="px-wrap" style="max-width:900px">
    <h1 class="px-serif px-fade">They all tried to do&nbsp;it <em>on their&nbsp;own</em> first.</h1>
    <p class="lede px-fade">Men and women, from Europe to the US to Australia. Different lives,
      different circumstance. <b>They all say the same thing.</b></p>

    <div id="px-count" class="px-fade">
      <div><b>1000+</b><span>lives changed</span></div>
      <div><b>9</b><span>countries</span></div>
      <div><b>6+</b><span>years facilitating</span></div>
    </div>
  </div>
</section>

<!-- ══════════════ 02 · THE PATTERN ══════════════
     FIVE PEOPLE, FIVE COUNTRIES, ONE SENTENCE.
     Each fragment is a contiguous clause lifted from the card further
     down -- no splicing, no joins, no tidying. The bolded words are the
     only editorial act in the section and they are bolded, not added.

     If any quote in the wall is ever edited, edit it here too or delete
     it from here. A fragment that no longer matches its card turns the
     strongest section on the page into the weakest.  ══════════════ -->
<section class="px-sec" id="px-pattern">
  <div class="px-wrap">

    <div id="px-pattern-head">
      <div>
        <div class="px-fade" style="display:flex;align-items:center;gap:16px;margin-bottom:12px">
          <hr class="px-rule">
          <p class="px-label">What they keep saying</p>
        </div>
        <h2 class="px-serif px-fade" style="font-size:clamp(25px,2.9vw,40px);line-height:1.16;color:#F1ECE1">
          Different lives.<br>Almost the same words.
        </h2>
      </div>
      <p class="px-hand px-fade" style="color:#E8C65F;font-size:1.35rem;white-space:nowrap">
        the pattern is clear
      </p>
    </div>

    <div id="px-frags">
      <figure class="px-frag px-fade">
        <blockquote><span class="lead">I was trying to figure it out</span><span class="key">by myself.</span></blockquote>
        <figcaption class="who"><cite>Gena K &middot; Bulgaria</cite></figcaption>
      </figure>
      <figure class="px-frag px-fade">
        <blockquote><span class="lead">I couldn't break through them</span><span class="key">alone.</span></blockquote>
        <figcaption class="who"><cite>Ana R &middot; United Kingdom</cite></figcaption>
      </figure>
      <figure class="px-frag px-fade">
        <blockquote><span class="lead">I couldn't shift my thinking</span><span class="key">on my own.</span></blockquote>
        <figcaption class="who"><cite>Katherine J &middot; Australia</cite></figcaption>
      </figure>
    </div>

  </div>
</section>

<!-- ══════════════ 03 · HEAR ONE ══════════════
     A written quote can be written by anyone. A face saying it cannot.
     So the video comes before the wall.

     ONE CLIP, NOT FIVE. The full interviews are twelve to fifteen
     minutes; nobody commits to that before they have been given a
     reason. The 49-second clip IS the reason, and it is the same asset
     the men's home page leads its proof section with, so it is already
     graded, already hosted on Wix, and already known to convert.

     THE QUOTE IS VERBATIM SPEECH FROM THE CLIP. The YouTube thumbnail
     for this interview reads "My wife couldn't reach me" -- that is
     Daniel's editorial framing and it appears nowhere in the
     transcript. In quotation marks, attributed as speech, on the one
     page whose entire job is being verifiably true, that would be a
     small lie. This is what he actually says.

     [CLIENT] NAME HIM. The card underneath currently reads "Reconnect
     client" because the men's page never attributed this clip either.
     A first name and a country here is worth more than the clip is --
     an unnamed face is the one thing on this page a sceptic can
     dismiss. If he has not consented to being named, say so instead:
     "name withheld at his request" is far stronger than silence.
     ══════════════════════════════════════════════════════════════ -->
<section class="px-sec" id="px-hear">
  <div class="px-wrap">
    <div class="px-fade" style="text-align:center;margin-bottom:clamp(26px,3vw,36px)">
      <p class="px-label" style="margin-bottom:12px">The full conversations</p>
      <h2 class="px-serif" style="font-size:clamp(24px,3vw,40px);line-height:1.2;color:#F1ECE1">
        Hear them <span style="font-style:italic;color:#B1BFD7">say it themselves.</span>
      </h2>
      <p style="max-width:46ch;margin:16px auto 0;font-size:.95rem;line-height:1.7;color:#6E6960">
        Sat down with each of them after the work was done. These run long because that is how long it takes.
      </p>
    </div>

    <!-- ══ THE 49-SECOND TEASER AND THE EMPTY "HERS" SLOT BOTH CAME
         OUT OF THIS PAGE, 4 AUG. NOT A DELETION, A RELOCATION. ══

         The short cuts belong on the avatar pages. The full
         conversations belong here. That split fixes a promise this page
         was breaking.

         The men's home page shows Brian's 49-second cut, calls it one
         of five client stories, and its own note says "the other four
         are the reason to click through" to testimonials. Then
         testimonials opened with the same clip. Whoever followed that
         link was handed back the thing they had just watched.

         So:
           HOME (men)    Brian's 49s cut    -> "full conversation on testimonials"
           HOME (women)  Silja's 50s cut    -> "full conversation on testimonials"
           TESTIMONIALS  the full versions of both, and the others

         And the empty "her clip" placeholder is gone with it, because
         the gap it marked is now filled by Silja's full interview
         sitting in the row below.

         WHAT THIS PAGE GIVES UP, stated plainly: cold traffic landing
         here directly no longer gets a short clip to warm them before
         four long-form interviews. That is a real cost and it is worth
         paying, because most people arriving here have come from an
         avatar page and have already watched fifty seconds of exactly
         this. They arrive wanting the long version. The hero and the
         fragment strip do the warming for everyone else. ══════════ -->


    <!-- ══════════════ THE LONG INTERVIEWS ══════════════
         Four client stories, click-to-load. Nothing is requested from
         youtube-nocookie.com until a cover is pressed.

         THE COVERS ARE TREATED, AND THEY HAD TO BE. YouTube's own
         thumbnails are built for a competitive feed: magenta and purple
         bokeh, drop-shadowed display type baked into the pixels, a
         yellow highlight box. Four of them, all designed differently,
         dropped into a navy and gold page. Two problems, and the second
         is the serious one.

         1. THEY FIGHT THE PALETTE. Purple against navy, and no two of
            the four agree with each other.
         2. THEY ARE WORDS AS PIXELS. "FEAR RAN MY LIFE" is typography
            baked into a JPEG, unreadable to Google and to a screen
            reader. That is the exact fault this entire redesign exists
            to undo, walking back in through the video block.

         So each cover is desaturated, tinted to the site navy, scrimmed
         at the bottom, and framed on the CLIENT rather than the split
         screen, which pushes most of the baked-in type out of shot. The
         name underneath is real text.

         Done in CSS on YouTube's own CDN image. Nothing to export,
         nothing to upload, and nothing to redo when a video is
         replaced. Swapping data-yt is the whole maintenance story.

         COVERS ARE LIVE ON WIX MEDIA as of 5 Aug. Sources of record are
            in covers/ in the repo push folder. To replace one, upload the
            new file and swap the media id in the matching src; keep the
            /v1/fill/w_1600,h_900,al_c,q_85,enc_auto/ transform, which is
            what gets AVIF and WebP served instead of the raw JPEG.

            They are 1600x900, exact 16:9, sRGB JPEG at quality 88,
            roughly 85 to 105 KB each. The Zoom participant label has
            been cropped off the bottom of every one, because text baked
            into pixels is the one thing these covers exist to avoid.

            Do NOT replace the thumbnails on YouTube with these. The
            loud ones are doing a different job in a competitive feed
            and swapping them will cost you click-through there.

         [CLIENT] ADD THE RUNTIMES. I could not read them off the API.
         Drop <span class="len">13 min</span> inside each button. These
         are long-form, and naming the length up front costs a few
         clicks and improves every one that is left.
         ══════════════════════════════════════════════════════════ -->
    <div id="px-more-vids">
      <div class="px-fade">
        <button class="px-vid px-vid-yt" data-yt="1bGNFw9Df1Y" aria-label="Play the client story of Gary M">
          <img class="cover" alt="" width="1600" height="900" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_fca924bae4e6495493e7e4e0364ed87b~mv2.jpg/v1/fill/w_1600,h_900,al_c,q_85,enc_auto/cover-gary-m.jpg">
          <span class="shade" aria-hidden="true"></span>
          <span class="play" aria-hidden="true"></span>
        </button>
        <p class="px-vidcap"><span class="nm">Gary M<i>United Kingdom</i></span><span class="line">Fear ran his life for as long as he can remember.</span></p>
      </div>
      <div class="px-fade">
        <button class="px-vid px-vid-yt" data-yt="FK-QjyTK6r8" aria-label="Play the client story of Benjamin W">
          <img class="cover" alt="" width="1600" height="900" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_c7c9f30e0ec4421c8967e82239e45ce7~mv2.jpg/v1/fill/w_1600,h_900,al_c,q_85,enc_auto/cover-benjamin-w.jpg">
          <span class="shade" aria-hidden="true"></span>
          <span class="play" aria-hidden="true"></span>
        </button>
        <p class="px-vidcap"><span class="nm">Benjamin W</span><span class="line">Burnt out, people pleasing, and finally feeling alive again.</span></p>
      </div>
      <div class="px-fade">
        <button class="px-vid px-vid-yt" data-yt="4DkQ1C2HEho" aria-label="Play the client story of Silja T">
          <img class="cover" alt="" width="1600" height="900" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_f25a4bdb39fa49b5a3d84cefb56f8337~mv2.jpg/v1/fill/w_1600,h_900,al_c,q_85,enc_auto/cover-silja-t.jpg">
          <span class="shade" aria-hidden="true"></span>
          <span class="play" aria-hidden="true"></span>
        </button>
        <p class="px-vidcap"><span class="nm">Silja T<i>Sweden</i></span><span class="line">Emotionally numb as a trader, and what finally changed.</span></p>
      </div>
      <div class="px-fade">
        <button class="px-vid px-vid-yt" data-yt="xaxf237lnFs" aria-label="Play the client story of Brian B">
          <img class="cover" alt="" width="1600" height="900" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_ef86dba130b04d0fabc8fee6eaea726d~mv2.jpg/v1/fill/w_1600,h_900,al_c,q_85,enc_auto/cover-brian-b.jpg">
          <span class="shade" aria-hidden="true"></span>
          <span class="play" aria-hidden="true"></span>
        </button>
        <p class="px-vidcap"><span class="nm">Brian B</span><span class="line">Why success got lonely.</span></p>
      </div>
    </div>
  </div>
</section>

<!-- ══════════════ 04 · THE WALL ══════════════
     ALL TEN, AS TEXT, IN FULL.
     Every quote below was previously a PNG. Every one of them is now
     indexable, reflowable and readable aloud.

     EVERY WORD IS VERBATIM off the original cards. The only edits are
     mechanical: curly quotes and apostrophes, and the three beats split
     onto their own elements exactly where the original card broke its
     paragraphs. DO NOT PARAPHRASE THESE -- if a claim ever needs
     softening, drop the testimonial rather than edit what somebody
     said. Note this rule lives in the COMMENTS and never in the copy:
     the page does not tell visitors it is verbatim, it just is.

     THE SUBTITLE UNDER EACH NAME IS EDITORIAL, NOT A QUOTE. It is set
     in gold, which is Daniel's voice, precisely so it never reads as
     something the client said. Facts only -- where they are, what they
     do, how far in they were.  ══════════════════════════════════════ -->
<section class="px-sec" id="px-wall-sec">
  <div class="px-wrap">

    <div class="px-fade" style="margin-bottom:clamp(24px,3vw,34px)">
      <div style="display:flex;align-items:center;gap:16px;margin-bottom:10px">
        <hr class="px-rule">
        <p class="px-label">In their own words</p>
      </div>
      <h2 class="px-serif" style="font-size:clamp(24px,3vw,40px);line-height:1.2;color:#F1ECE1;max-width:20ch">
        All of them. <span style="font-style:italic;color:#B1BFD7">Start anywhere.</span>
      </h2>
    </div>

    <!-- THE SWITCH. Hidden until the script proves it works. -->
    <div id="px-switch-wrap" class="px-fade">
      <div id="px-switch" role="tablist" aria-label="Filter testimonials">
        <button type="button" role="tab" data-f="all"   aria-selected="true"  aria-controls="px-wall">All</button>
        <button type="button" role="tab" data-f="men"   aria-selected="false" aria-controls="px-wall">Men</button>
        <button type="button" role="tab" data-f="women" aria-selected="false" aria-controls="px-wall">Women</button>
      </div>
    </div>

    <div id="px-wall">

      <!-- ── 01 · GENA K ─────────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="women">
        <div class="px-who">
          <img class="px-face" alt="Gena K, Bulgaria" width="640" height="640" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_b98cf690891f444587d740029b22c892~mv2.jpg">
          <div class="n">Gena K<span>Bulgaria</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I was stuck in self-sabotaging patterns I couldn't understand. Procrastination and emotional overload were draining my energy. I was trying to figure it out by myself, but I couldn't see where the actual problem was coming from.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel helped me scan my subconscious. He showed me that everything we do or don't do comes from our belief system. Sometimes those beliefs are hidden, especially when you're trying to solve it alone.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I've found ways to overcome the procrastination and emotional drain. The sessions were a game-changer. I can only highly recommend his coaching to everyone who wants to reach their ideal life.&rdquo;</p>
          </div>
      </article>

      <!-- ── 02 · BRANDON L ──────────────────────────────────────── -->
      <!-- THE HIGHEST-STAKES STORY ON THE SITE. Addiction, PTSD, "a
           path to death". It is also the most verifiable: he is in
           dress uniform in the source photograph, which is why the
           subtitle says US Navy. The credential is the FACT, not the
           picture -- at 60px the uniform cannot do that work, so the
           words do it instead. -->
      <article class="px-card px-fade" data-g="men">
        <div class="px-who">
          <img class="px-face" alt="Brandon L, United States" width="260" height="260" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_3db56c0e87c84875a9f6d059a2a768eb~mv2.png/v1/crop/x_370,y_10,w_260,h_260,q_90,enc_auto/f.png">
          <div class="n">Brandon L<span>United States &middot; US Navy</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I struggled with addiction, depression, an overbearing ego, and PTSD while navigating military to civilian life. I was down a path to death. I didn't love myself, constantly compared myself to others, and couldn't live in the moment.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Reconnect showed me that even the smallest changes could lead to significant outcomes. I learned to love myself, stop comparing, and live in the moment.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I feel reborn with goals and satisfaction. I appreciate my achievements while learning from experiences. If you're contemplating your own journey, ask yourself if you're truly content. Embracing change might be the key to discovering happiness.&rdquo;</p>
          </div>
      </article>

      <!-- ── 03 · SILJA T ────────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="women">
        <div class="px-who">
          <img class="px-face" alt="Silja T, Sweden" width="640" height="640" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_ab54167a61fe4c188544389f7f4e9d1f~mv2.jpg">
          <div class="n">Silja T<span>Sweden &middot; trader</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I was trapped by shame. I couldn't see that 70% of my losing trades were the same pattern. The emotional burden blinded me. I'd think &lsquo;I'm never gonna understand this&rsquo; and my mind would block immediately.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel didn't focus on my problems. We shifted to what I wanted more of, and the problems stopped existing.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now everything comes from curiosity instead of fear. I feel calm and expanded instead of heavy. The shame and guilt have evaporated. Concepts that seemed impossible now make total sense. In less than two months, my eyes have opened in a way I couldn't have imagined.&rdquo;</p>
          </div>
      </article>

      <!-- ── 04 · TOBIAS M ───────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="men">
        <div class="px-who">
          <img class="px-face" alt="Tobias M, Sweden" width="290" height="290" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_392b733e816548dcbfadab2fe214e05c~mv2.png/v1/crop/x_300,y_555,w_290,h_290,q_90,enc_auto/f.png">
          <div class="n">Tobias M<span>Sweden</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I was feeling stuck. I had a good self-development practice, but I couldn't pivot to the next level on my own. My perspective was limited and I wasn't creating the results I wanted.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel helped me see what I couldn't see myself. Just a few coaching sessions shifted everything. My perspective changed and the results started coming.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I'm amazed at how quickly things have moved. If you take your journey seriously and you're ready to pivot to the next level, Daniel can definitely help you get there. He's an absolute star. Highly recommend.&rdquo;</p>
          </div>
      </article>

      <!-- ── 05 · KATHERINE J ────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="women">
        <div class="px-who">
          <img class="px-face" alt="Katherine J, Australia" width="380" height="380" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_87a6895c57c24b39a19c1272352e7e85~mv2.png/v1/crop/x_205,y_70,w_380,h_380,q_90,enc_auto/f.png">
          <div class="n">Katherine J<span>Australia &middot; business owner</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I was scattered and unfocused. My business, relationships, and emotions all felt overwhelming. My mindset was holding me back, but I couldn't shift my thinking on my own. I wasn't experiencing life the way I wanted to.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel supported and challenged me across all these areas. He helped me shift my mindset and refocus my thinking. Working with him for a few months has totally changed my life.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I feel more centred and present. The results are undeniable. I'm reaping the rewards in how I'm experiencing my life. Everything has shifted. Highly recommended. You need to work with this man!&rdquo;</p>
          </div>
      </article>

      <!-- ── 06 · DAVE M ─────────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="men">
        <div class="px-who">
          <img class="px-face" alt="Dave M, United Kingdom" style="object-position:64% 30%" width="600" height="420" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_5cda9ea93a0040a091beb7caebdde4b7~mv2.png/v1/crop/x_60,y_30,w_600,h_420,q_90,enc_auto/f.png">
          <div class="n">Dave M<span>United Kingdom</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I felt lost, suppressing my true feelings out of fear and shame. I was chasing instant gratification instead of meaningful change. I lacked profound life awareness and wasn't pursuing passions that enriched my life. I thought I had to settle.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel's approach to vulnerability offered me a lifeline. Reconnect taught me to confront my quest for instant gratification and embrace meaningful changes instead.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I cherish incredible friendships, possess profound life awareness, and pursue passions that enrich my life. I don't have to settle. I can live a life filled with joy and excitement. This could be the bridge to discovering what you truly seek in life.&rdquo;</p>
          </div>
      </article>

      <!-- ── 07 · ANA R ──────────────────────────────────────────── -->
      <!-- SHE RUNS HER OWN BUSINESS, and that is stated. Until there
           was a voice like hers the women's material read as an
           industry rather than an avatar. -->
      <article class="px-card px-fade" data-g="women">
        <div class="px-who">
          <img class="px-face" alt="Ana R, United Kingdom" width="640" height="640" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_abf2f5363f7e4262ba1812cb7844c6d3~mv2.jpg">
          <div class="n">Ana R<span>United Kingdom &middot; business owner</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I had resistance I wasn't sure how to get through. My business wasn't moving forward. I knew what I wanted in my mind, but I found it hard to articulate. There were barriers stopping me, and I couldn't break through them alone.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel dug deep into my psyche. We strategized moving through my fears and discovered what my ideal business actually looked like. The tools and time he gives you are invaluable.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I have confidence, clarity, and flow. We broke through the barriers that were holding me back. My business is moving forward. It has been incredible and truly transformative. He is your man if you're ready to break through.&rdquo;</p>
          </div>
      </article>

      <!-- ── 08 · ALEX H ─────────────────────────────────────────── -->
      <!-- THREE SESSIONS IN, and the card says so. Every other story
           here is months long; one that is honest about being early is
           worth more than a fourth "totally changed my life", because
           it is the one a sceptic can picture themselves in. -->
      <article class="px-card px-fade" data-g="men">
        <div class="px-who">
          <img class="px-face" alt="Alex H, Denmark" style="object-position:58% 34%" width="600" height="420" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_7a60aa68544a411783b1bffd0d38cbaa~mv2.png/v1/crop/x_70,y_455,w_600,h_420,q_90,enc_auto/f.png">
          <div class="n">Alex H<span>Denmark &middot; three sessions in</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I had gone astray, prioritising things that didn't really matter. I wanted to reconnect with myself and better myself, but I wasn't sure what I actually wanted to improve.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;The biggest breakthrough has been connecting with my emotions again, not just in my mind. I operate a lot from my mind, but getting in touch with the essence of myself has been amazing.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;In just three sessions, I've gotten more than I was expecting. I'm becoming aware of what I want to focus on and what's important to me. Having this container keeps me going in the right direction. That's an amazing feeling.&rdquo;</p>
          </div>
      </article>

      <!-- ── 09 · LAURA B ────────────────────────────────────────── -->
      <article class="px-card px-fade" data-g="women">
        <div class="px-who">
          <img class="px-face" alt="Laura B, Latvia" width="370" height="370" loading="lazy" decoding="async"
               src="https://static.wixstatic.com/media/111174_e59465cb13ff4fb4beb4bc538c525fe3~mv2.png/v1/crop/x_215,y_35,w_370,h_370,q_90,enc_auto/f.png">
          <div class="n">Laura B<span>Latvia</span></div>
        </div>
        <p class="px-beat">Before</p>
        <p class="px-before">&ldquo;I wasn't taking full responsibility for my own life. I didn't understand my values or have a clear vision. Facing my shadows and vulnerabilities felt overwhelming. I needed someone to guide me to find my path.&rdquo;</p>
        <details>
          <summary><span class="cx" aria-hidden="true">&rsaquo;</span><span class="lbl-c">What changed</span><span class="lbl-o">Close</span></summary>
          <p>&ldquo;Daniel explains things in a way that leads you to find the answers yourself. He does it with such ease and calmness that trusting him comes naturally. Somehow he makes facing your shadows and vulnerabilities exciting rather than overwhelming.&rdquo;</p>
        </details>
        <div class="px-outcome">
          <p class="px-beat">Now</p>
          <p class="px-now">&ldquo;Now I feel the shift in my mindset. I'm on the way to becoming the best version of myself. It has been an incredible journey of self-development. I cannot recommend Daniel highly enough.&rdquo;</p>
          </div>
      </article>

      <!-- ── SASA (Serbia) WAS HERE AND WAS CUT, 4 AUG ──────────────
           Not for space. Four reasons, in order of weight:

           1. LEAST VERIFIABLE PERSON ON THE PAGE. Every other entry
              carries a surname initial and a country. He had a first
              name only. On a page whose whole job is being believed,
              the entry a sceptic can wave away cheapest is the one
              with the least identity attached to it.
           2. IT REVIEWED THE SERVICE, NOT HIS LIFE. "Daniel's ability
              to pinpoint the right issue is amazing. Every session is
              tailored to my specific needs." Every other card here is
              a story about a person; that one was a product review,
              and it read like one sitting beside them.
           3. THE MOST ABSTRACT OUTCOME. "Empowered yet relaxed... the
              right tools and conscious awareness." Nothing a reader
              can picture. Compare Silja's "in less than two months" or
              Brandon's "a path to death" to "reborn".
           4. WEAKEST PHOTOGRAPH. A tight low-resolution selfie at an
              odd angle, and the only face that would not crop cleanly
              out of its source card.

           His full verbatim text is still in the original flattened PNG
           (111174_4c27bf81d4914ca1aa6e699a7a1af2a7~mv2.png) if it is
           ever wanted back. Restore his fragment in section 02 too.
           ══════════════════════════════════════════════════════════ -->

    </div>

    <p id="px-empty">Nothing here yet.</p>
  </div>
</section>

<!-- ══════════════ 05 · CREDS ══════════════
     DEMOTED ON PURPOSE. Proof closes, it does not open. On a page
     where ten named strangers have just spoken, a logo row at the top
     would be Daniel talking over them. ══════════════════════════════ -->
<section class="px-sec" id="px-creds">
  <div class="px-wrap" style="text-align:center">
    <p class="px-label" style="margin-bottom:14px">Seen in &middot; Credentialled by</p>
    <p style="color:#5E6B85;font-size:.9rem;letter-spacing:.04em">
      ABC News &nbsp;&middot;&nbsp; Wanderlust Magazine &nbsp;&middot;&nbsp; Brainz Magazine &nbsp;&middot;&nbsp; Solarcon &nbsp;&middot;&nbsp; Meta Dynamics L3 &nbsp;&middot;&nbsp; Int'l Coach Guild
    </p>
  </div>
</section>

<!-- ══════════════ 06 · THE FORK ══════════════
     [CLIENT] BOTH HREFS ARE THE SAME PLACEHOLDERS THE SORTING PAGE
     CARRIES. /men and /women are not confirmed URLs. Do not publish
     until these point at real pages. ═══════════════════════════════ -->
<section class="px-sec" id="px-fork">
  <div class="px-wrap" style="max-width:940px">
    <p class="px-hand px-fade" style="color:#A08A5E;font-size:1.35rem;margin-bottom:12px">Which is you</p>
    <h2 class="px-serif px-fade px-oneline" style="font-size:clamp(23px,2.85vw,38px);line-height:1.16;color:#1E2A3D;font-weight:500;max-width:none;margin:0 auto">
      They all stopped doing it on their own.
    </h2>

    <div id="px-doors">
      <!-- ══ THE DOORS NAME THE MOVE, NOT THE CIRCUMSTANCE ══
           v1 lifted these straight off the sorting page, and on a
           sorting page they are right: somebody who has read nothing
           needs a situation specific enough to recognise. By the time
           somebody reaches the BOTTOM of this page they have read nine
           stories and they do not need convincing, they need a door
           they fit through. And the old pair had a doorframe on them.

           "Twenty years in, and she has told you something is missing"
           asks for a long marriage AND a partner who has spoken up. A
           single man, a divorced man, a man two years in, a man whose
           wife has said nothing and that IS the problem: none of them
           are in that sentence.

           "The most capable person in every room, and never in one you
           didn't have to run" asks her to be senior enough to be
           running rooms. It described an executive rather than the
           move.

           Both now name the move instead, and it is the same move
           wearing opposite costumes, which is the argument the whole
           site is built on. Nobody has to qualify for either.

           TWO CORRECTIONS FROM DANIEL, 5 AUG, AND THEY BOTH SHARPEN IT.

           HIS: "you stopped bringing it up" was wrong. He never brought
           it up. There was no lapse to date, no better version of him
           that used to talk and went quiet. "Stopped" implies a decline
           and invites him to look for when it started; "never" is the
           harder and truer read, and it is present tense, which is
           where he actually is.

           HERS: "Nobody asks what you need" put the fault outside her,
           which made it a complaint about the people around her and
           something she cannot act on. The real move is that she does
           not think about what she needs. That is hers to change, and
           it is also the exact mirror of his: he never brought it up
           with anyone, she never brought it up with herself.

           Both "somewhere in there" clauses are gone. It was hedging,
           and it appeared twice, so it read as a formula rather than as
           an observation. ═══════════════════════════════════════ -->
      <a class="px-door px-fade" href="https://www.parallaxxtransformations.com/men">
        <span class="tag">For men</span>
        <h3>You handle it. You always handle it.</h3>
        <p>So nobody thinks to ask how you're doing. You never brought it up, and you don't ask anyone for anything.</p>
        <span class="go">This is me <span aria-hidden="true">&rarr;</span></span>
      </a>
      <a class="px-door px-fade" href="https://www.parallaxxtransformations.com/women">
        <span class="tag">For women</span>
        <h3>Everyone leans on you. You don't think about what you need.</h3>
        <p>You got good at not needing much. It stopped being a question you asked yourself.</p>
        <span class="go">This is me <span aria-hidden="true">&rarr;</span></span>
      </a>
    </div>

    <!-- "Not sure which? Start with how I got here" was removed 5 Aug.
         It offered a third way out of a two-door choice nobody needs
         help with: a reader knows whether they are a man or a woman
         before they arrive. All it did was give somebody who had just
         read nine testimonials a link away from both doors, at the
         exact moment the page had finished earning the click. If an
         About link is ever wanted down here, it belongs in the footer,
         which already carries one. -->
  </div>
</section>

<!-- ══ PtFooter v3, baked in. See the note at the top. ══ -->
<footer id="pt-foot">
  <div id="pt-foot-inner">

    <div id="pt-foot-top">

      <!-- ══ IDENTITY ══ The mark and the four places he posts. No
           paragraph, no CTA: both were cut, and the column is better
           for it. ══ -->
      <div id="pt-ident">
        <a id="pt-foot-logo" href="https://www.parallaxxtransformations.com" aria-label="Parallaxx Transformations — home">
          <img src="https://static.wixstatic.com/media/e1784d_fe3c841c471f47d088f0cd631a89d883~mv2.png"
               alt="Parallaxx Transformations" loading="lazy">
        </a>
        <nav id="pt-social" aria-label="Social">
          <a href="https://www.youtube.com/@ReconnectYou1/featured" target="_blank" rel="noopener">YouTube</a>
          <a href="https://www.linkedin.com/in/daniel-reconnect-you/" target="_blank" rel="noopener">LinkedIn</a>
          <a href="https://www.instagram.com/daniel.lawson__/" target="_blank" rel="noopener">Instagram</a>
          <a href="https://www.facebook.com/Kiwi.Daniel" target="_blank" rel="noopener">Facebook</a>
        </nav>
      </div>

      <!-- ══ THE TWO DOORS ══ Same names, same order as the nav. ══ -->
      <nav class="pt-door" aria-label="For men">
        <h4>For Men</h4>
        <a href="https://www.parallaxxtransformations.com/men">Start here</a>
        <a href="https://www.parallaxxtransformations.com/the-reconnected-man">The Reconnected Man</a>
        <!-- /reconnect is the five-archetype quiz, not a programme.
             It is his free way in, so it belongs beside the door. -->
        <a href="https://www.parallaxxtransformations.com/reconnect">The Archetype Quiz</a>
      </nav>

      <nav class="pt-door" aria-label="For women">
        <h4>For Women</h4>
        <a href="https://www.parallaxxtransformations.com/women">Start here</a>
        <a href="https://www.parallaxxtransformations.com/the-reconnected-woman">The Reconnected Woman</a>
        <!-- [CLIENT] /priority-audit is the URL the women's home page
             CTA uses. Confirm the page is live before publishing. -->
        <a href="https://www.parallaxxtransformations.com/priority-audit">The Priority Audit</a>
      </nav>

      <!-- ══ DANIEL ══ Not a door. The three places he shows up in
           person, smallest commitment first. GIVE IT ALL is the last
           of them rather than a banner of its own. ══ -->
      <nav class="pt-door" aria-label="Daniel">
        <h4>Daniel</h4>
        <a href="https://www.parallaxxtransformations.com/about-daniel-lawson">About</a>
        <a href="https://www.parallaxxtransformations.com/daniel-lawson-speaking">Speaking</a>
        <!-- [CLIENT] No facilitating page exists on the site yet. This
             URL is a guess and must be confirmed or repointed. -->
        <a href="https://www.parallaxxtransformations.com/facilitating">Facilitating</a>
        <a class="ext" href="https://giveitallevent.com" target="_blank" rel="noopener">GIVE IT ALL<span class="mk" aria-hidden="true">&#8599;</span></a>
      </nav>

    </div>

    <!-- ══ THE UTILITY ROW ══
         Ordered by who asks for it: the person, then the work he makes,
         then the proof. Seven of these are linked from nowhere else on
         the site, which is exactly why the row exists. ══ -->
    <nav id="pt-util" aria-label="More">
      <a href="https://www.parallaxxtransformations.com/contact-daniel-lawson">Contact</a>
      <a href="https://www.parallaxxtransformations.com/book-a-call-with-daniel-lawson">Book a Call</a>
      <a href="https://www.parallaxxtransformations.com/reconnect-you-podcast-with-daniel-lawson">Reconnect You Podcast</a>
      <a href="https://www.parallaxxtransformations.com/parallaxx-perspectives-podcast">Perspectives Podcast</a>
      <a href="https://www.parallaxxtransformations.com/blog">Blog</a>
      <a href="https://www.parallaxxtransformations.com/ptjournal">Progress Journal</a>
      <a href="https://www.parallaxxtransformations.com/three-toxic-lies">Three Toxic Lies</a>
      <!-- [CLIENT] If Identity 2.0 is aimed at one avatar, promote it
           into that door's column instead of leaving it here. -->
      <a href="https://www.parallaxxtransformations.com/your-identity-challenge">Your Identity 2.0 Challenge</a>
      <a href="https://www.parallaxxtransformations.com/testimonials-daniel-lawson">Testimonials</a>
      <a href="https://www.parallaxxtransformations.com/daniel-lawson-as-seen-in">As Seen In</a>
    </nav>

    <div id="pt-legal">
      <span>&copy; <span id="pt-year">2026</span> Parallaxx Transformations &middot; Daniel Lawson</span>
      <span class="links">
        <a href="https://www.parallaxxtransformations.com/terms-of-use">Terms of Service</a>
        <a href="https://www.parallaxxtransformations.com/privacy-policy">Privacy Policy</a>
      </span>
    </div>

  </div>
</footer>

</div><!-- /px-root -->

<!-- ══════════════════════════════════════════════════════════════
     STRUCTURED DATA
     A Review object each, so the testimonials exist for Google as
     REVIEWS and not merely as body text. This is the second half of
     fix #1 -- the PNGs meant Google had no idea this business had ever
     been reviewed by anybody.

     NO aggregateRating IS DECLARED, and that is deliberate. None of
     these people gave a star rating; inventing a star average to win
     a rich snippet is exactly the kind of thing that
     earns a manual action, and on a page about honesty it would be a
     strange thing to lie on. Reviews without ratings are still parsed
     and still attributed.

     [CLIENT] Check the itemReviewed name and url match the Wix
     business listing exactly, or Google will treat them as unrelated.
     ══════════════════════════════════════════════════════════════ -->`;

  function addFonts(){
    if (document.getElementById('px-fonts-testimonials')) return;
    var p1=document.createElement('link'); p1.rel='preconnect'; p1.href='https://fonts.googleapis.com'; document.head.appendChild(p1);
    var p2=document.createElement('link'); p2.rel='preconnect'; p2.href='https://fonts.gstatic.com'; p2.crossOrigin=''; document.head.appendChild(p2);
    var f0=document.createElement('link'); f0.rel='stylesheet'; f0.href='https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,500&family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap'; document.head.appendChild(f0);
    var f1=document.createElement('link'); f1.rel='stylesheet'; f1.href='https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap'; document.head.appendChild(f1);
    /* @font-face has to live in the DOCUMENT, not the shadow root. Chrome
       ignores font-face rules declared inside a shadow tree. */
    var ff=document.createElement('style'); ff.id='px-fonts-testimonials'; ff.textContent=`/* LUMIOS MARKER is not on Google Fonts -- upload the .woff2 to Wix Media and
   drop the URL in below. Until then the stack falls through to Permanent
   Marker, which is the closest free marker face, so nothing renders as a
   system script by accident. */
@font-face{font-family:'Lumios Marker';font-display:swap;
  src:url('LUMIOS_MARKER_WOFF2_URL') format('woff2');}`; document.head.appendChild(ff);
  }

  /* STRUCTURED DATA GOES IN THE DOCUMENT, NOT THE SHADOW ROOT.
     Nine Review objects. A crawler reads ld+json from the document only; the
     same script inside a shadow tree is invisible, which would have quietly
     undone the main point of rebuilding this page as text. */
  function addSchema(){
    if (document.getElementById('px-ld')) return;
    var s=document.createElement('script'); s.type='application/ld+json';
    s.id='px-ld'; s.textContent=`{
  "@context": "https://schema.org",
  "@type": "ProfessionalService",
  "name": "Parallaxx Transformations",
  "url": "https://www.parallaxxtransformations.com",
  "founder": { "@type": "Person", "name": "Daniel Lawson" },
  "review": [
    { "@type":"Review","author":{"@type":"Person","name":"Gena K"},"reviewBody":"I was stuck in self-sabotaging patterns I couldn't understand. Procrastination and emotional overload were draining my energy. I was trying to figure it out by myself, but I couldn't see where the actual problem was coming from. Now I've found ways to overcome the procrastination and emotional drain. The sessions were a game-changer." },
    { "@type":"Review","author":{"@type":"Person","name":"Brandon L"},"reviewBody":"I struggled with addiction, depression, an overbearing ego, and PTSD while navigating military to civilian life. Reconnect showed me that even the smallest changes could lead to significant outcomes. Now I feel reborn with goals and satisfaction." },
    { "@type":"Review","author":{"@type":"Person","name":"Silja T"},"reviewBody":"I was trapped by shame. The emotional burden blinded me. Daniel didn't focus on my problems; we shifted to what I wanted more of, and the problems stopped existing. Now everything comes from curiosity instead of fear." },
    { "@type":"Review","author":{"@type":"Person","name":"Tobias M"},"reviewBody":"I was feeling stuck. I couldn't pivot to the next level on my own. Daniel helped me see what I couldn't see myself. Now I'm amazed at how quickly things have moved." },
    { "@type":"Review","author":{"@type":"Person","name":"Katherine J"},"reviewBody":"I was scattered and unfocused. My mindset was holding me back, but I couldn't shift my thinking on my own. Now I feel more centred and present. The results are undeniable." },
    { "@type":"Review","author":{"@type":"Person","name":"Dave M"},"reviewBody":"I felt lost, suppressing my true feelings out of fear and shame. I thought I had to settle. Now I cherish incredible friendships, possess profound life awareness, and pursue passions that enrich my life." },
    { "@type":"Review","author":{"@type":"Person","name":"Ana R"},"reviewBody":"I had resistance I wasn't sure how to get through. There were barriers stopping me, and I couldn't break through them alone. Now I have confidence, clarity, and flow, and my business is moving forward." },
    { "@type":"Review","author":{"@type":"Person","name":"Alex H"},"reviewBody":"I had gone astray, prioritising things that didn't really matter. The biggest breakthrough has been connecting with my emotions again. In just three sessions, I've gotten more than I was expecting." },
    { "@type":"Review","author":{"@type":"Person","name":"Laura B"},"reviewBody":"I wasn't taking full responsibility for my own life. I needed someone to guide me to find my path. Now I feel the shift in my mindset. I cannot recommend Daniel highly enough." }
  ]
}`; document.head.appendChild(s);
  }


/* ═══════════════════════════════════════════════════════════════════
   TESTIMONIALS · BEHAVIOUR
   -------------------------------------------------------------------
   Three things, in strict order of how much they matter:
     1. THE FILTER      the only feature. must never lose a card.
     2. THE FACADES     zero third-party bytes until a click.
     3. THE REVEAL      decoration. dies silently and harmlessly.

   Everything here is defensive by construction. The page is fully
   readable before a line of this runs, and every block is wrapped so
   that one throwing cannot take the next one down with it.
   ═══════════════════════════════════════════════════════════════════ */
function initTestimonials(root){
  /* The caller supplies the root. Deliberately no document fallback here:
     in the Wix bundle this whole file runs inside a shadow root, where a
     document lookup returns null silently. The build fails rather than
     ship one, so the fallback lives in the bootstrap at the foot of this
     file, which the build strips. */
  if(!root) return;
  if(root.dataset.pxInit) return;      /* DC can mount twice */
  root.dataset.pxInit = '1';

  var reduce = window.matchMedia &&
               window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  /* ══════════════ 1 · THE FILTER ══════════════
     The switch markup is in the HTML but hidden by CSS until .px-js
     lands on the root. So a browser that never runs this file shows
     ten cards and no dead controls, which is the correct degradation
     for a page whose only job is to be read.

     DEEP LINKS. #men and #women are read on arrival, so the men's page
     can point straight at the men's five. The hash is kept in sync with
     replaceState -- NOT pushState -- so the browser Back button leaves
     the page rather than stepping back through filter changes, which is
     what visitors actually expect from a control that looks like a
     segmented button.  ═════════════════════════════════════════════ */
  try{
    var wall   = root.querySelector('#px-wall');
    var sw     = root.querySelector('#px-switch');
    var empty  = root.querySelector('#px-empty');
    if(wall && sw){
      var cards = [].slice.call(wall.querySelectorAll('.px-card'));
      var btns  = [].slice.call(sw.querySelectorAll('button'));

      /* ── CENTRE A LONE LAST CARD ─────────────────────────────────
         Read the column count back off the computed grid rather than
         re-deriving it from window width, so the breakpoints live in
         exactly one place (the stylesheet) and this cannot drift out of
         step with them.

         ONE CODE PATH FOR EVERY COLUMN COUNT. The obvious version was
         "at three columns, put it in track 2" -- but nine cards leave an
         orphan at TWO columns as well (9 % 2 = 1), and track 2 of 2 is
         the right-hand edge, which is worse than the left. Rather than
         branch per width, the orphan is made to span the whole row and
         then constrained to exactly one track's width and auto-margined.
         That lands it dead centre at any column count, and it is the
         reason the track width is read back in pixels: the card has to
         match its siblings exactly, and a percentage would not account
         for the gap.

         Every card is reset first. Without that, a card centred under
         one filter keeps its width and grid-column under the next and
         ends up floating in the middle of a full row. */
      var centreOrphan = function(){
        cards.forEach(function(c){
          c.style.gridColumn = ''; c.style.width = ''; c.style.marginLeft = ''; c.style.marginRight = '';
        });
        var tracks = getComputedStyle(wall).gridTemplateColumns.split(' ').filter(Boolean);
        var n = tracks.length;
        if(n < 2) return;                       /* single column: no orphan possible */
        var vis = cards.filter(function(c){ return !c.hidden; });
        if(vis.length <= n || vis.length % n !== 1) return;
        var last = vis[vis.length - 1];
        last.style.gridColumn = '1 / -1';
        last.style.width = tracks[0];           /* already a px string */
        last.style.marginLeft = 'auto';
        last.style.marginRight = 'auto';
      };

      /* Crossing a breakpoint changes the track count, so the decision
         has to be retaken. Debounced, because resize fires continuously
         while a window is being dragged. */
      var rt;
      window.addEventListener('resize', function(){
        clearTimeout(rt);
        rt = setTimeout(centreOrphan, 140);
      });

      var apply = function(f, focusIt){
        var shown = 0;
        cards.forEach(function(c){
          var on = (f === 'all') || (c.getAttribute('data-g') === f);
          c.hidden = !on;
          if(on) shown++;
        });
        btns.forEach(function(b){
          b.setAttribute('aria-selected', String(b.getAttribute('data-f') === f));
          b.tabIndex = (b.getAttribute('data-f') === f) ? 0 : -1;
        });
        if(empty) empty.style.display = shown ? 'none' : 'block';
        centreOrphan();

        /* Close every disclosure on a filter change. Leaving one open
           means the next filter renders a card taller than its
           neighbours for no reason the visitor can see. */
        cards.forEach(function(c){
          var d = c.querySelector('details'); if(d) d.open = false;
        });

        try{
          var h = (f === 'all') ? ' ' : '#' + f;
          history.replaceState(null, '', h);
        }catch(e){}

        if(focusIt){
          var b = sw.querySelector('button[data-f="' + f + '"]');
          if(b) b.focus();
        }
      };

      sw.addEventListener('click', function(e){
        var b = e.target.closest('button[data-f]');
        if(b) apply(b.getAttribute('data-f'), false);
      });

      /* ARROW KEYS. role="tablist" is a promise that left/right work.
         Declaring the role without wiring the keys is worse than not
         declaring it, because a screen reader will announce a keyboard
         model the page does not honour. */
      sw.addEventListener('keydown', function(e){
        var i = btns.indexOf(document.activeElement);
        if(i < 0) return;
        var n = null;
        if(e.key === 'ArrowRight') n = (i + 1) % btns.length;
        else if(e.key === 'ArrowLeft') n = (i - 1 + btns.length) % btns.length;
        else if(e.key === 'Home') n = 0;
        else if(e.key === 'End') n = btns.length - 1;
        if(n === null) return;
        e.preventDefault();
        apply(btns[n].getAttribute('data-f'), true);
      });

      var h0 = (location.hash || '').replace('#','').toLowerCase();
      apply((h0 === 'men' || h0 === 'women') ? h0 : 'all', false);

      /* Someone editing the URL by hand, or following a second in-page
         link, should still land on the right view. */
      window.addEventListener('hashchange', function(){
        var h = (location.hash || '').replace('#','').toLowerCase();
        apply((h === 'men' || h === 'women') ? h : 'all', false);
      });
    }
  }catch(e){ if(window.console) console.warn('px filter', e); }

  /* ══════════════ 2 · VIDEO FACADE ══════════════
     Nothing is requested from youtube-nocookie.com or video.wixstatic
     until the poster is pressed. Then the button BECOMES the player,
     in place, already playing.

     data-mp4 wins over data-yt when both are present: a self-hosted
     file is faster, has no third-party cookies, and keeps the visitor
     on this page instead of one tap from the YouTube app.
     ══════════════════════════════════════════════════════════════ */
  try{
    root.querySelectorAll('.px-vid').forEach(function(btn){
      btn.addEventListener('click', function(){
        if(btn.dataset.live) return;
        var mp4 = btn.getAttribute('data-mp4');
        var yt  = btn.getAttribute('data-yt');
        if(!mp4 && !yt) return;
        btn.dataset.live = '1';
        var el;
        if(mp4){
          /* IT HAS SOUND, so it gets native controls and real audio.
             Muting a man who is TALKING would leave a stranger moving
             his mouth at you. */
          el = document.createElement('video');
          el.src = mp4;
          el.controls = true; el.autoplay = true; el.preload = 'auto';
          el.playsInline = true; el.setAttribute('playsinline','');
          el.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;background:#04122A';
        } else {
          el = document.createElement('iframe');
          el.src = 'https://www.youtube-nocookie.com/embed/' + yt +
                   '?autoplay=1&rel=0&modestbranding=1&playsinline=1';
          el.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; picture-in-picture';
          el.setAttribute('allowfullscreen','');
          el.title = btn.getAttribute('aria-label') || 'Client story';
        }
        btn.innerHTML = '';
        btn.appendChild(el);
        btn.style.cursor = 'default';
        if(mp4){ var p = el.play(); if(p && p.catch) p.catch(function(){}); }
      });
    });
  }catch(e){ if(window.console) console.warn('px video', e); }

  /* ══════════════ 3 · THE REVEAL ══════════════
     Applied last, and it is the ONLY thing that adds .px-js. So if
     anything above had thrown hard enough to stop execution, the page
     would simply never hide anything -- which is the failure mode you
     want on a page made entirely of proof.

     IntersectionObserver only. No GSAP, no ScrollTrigger, no Lenis.
     The home pages need those for pinned scenes; a wall of cards does
     not, and every one of them is another way for a headline to end up
     stranded mid-tween.  ═══════════════════════════════════════════ */
  try{
    if(reduce || !('IntersectionObserver' in window)) return;
    root.classList.add('px-js');
    var els = [].slice.call(root.querySelectorAll('.px-fade'));
    var show = function(el, d){
      el.style.transition = 'opacity .8s cubic-bezier(.22,.61,.36,1) ' + d +
                            'ms, transform .8s cubic-bezier(.22,.61,.36,1) ' + d + 'ms';
      el.style.opacity = '1';
      el.style.transform = 'none';
    };
    var io = new IntersectionObserver(function(entries){
      entries.forEach(function(en){
        if(!en.isIntersecting) return;
        /* Stagger by position within the parent, not globally --
           otherwise the tenth card on a long page waits most of a
           second after it is already on screen. */
        var sibs = en.target.parentNode ? [].slice.call(en.target.parentNode.children) : [];
        var i = sibs.indexOf(en.target);
        show(en.target, Math.min(i < 0 ? 0 : i, 5) * 65);
        io.unobserve(en.target);
      });
    }, { rootMargin: '0px 0px -8% 0px', threshold: 0.05 });

    els.forEach(function(el){
      /* Anything already on screen at boot animates immediately. A
         trigger that fires while layout is still settling can be
         reverted mid-flight and strand an element at opacity 0. */
      if(el.getBoundingClientRect().top < window.innerHeight * 0.92) show(el, 0);
      else io.observe(el);
    });

    /* LAST RESORT. If the observer never fires for any reason -- a
       zero-height ancestor, a container that scrolls something other
       than the window -- everything is forced visible after 3s. A
       testimonials page is never allowed to end up blank. */
    setTimeout(function(){
      els.forEach(function(el){
        if(getComputedStyle(el).opacity === '0'){ el.style.opacity='1'; el.style.transform='none'; }
      });
    }, 3000);
  }catch(e){
    root.classList.remove('px-js');
    if(window.console) console.warn('px reveal', e);
  }
}

/* ═══ PtNav v3 LOGIC · BAKED IN VERBATIM ═══
   One edit against the component: it takes a ROOT. In Design Code and in
   the standalone preview that root is `document`; in the deployed Wix
   bundle the whole page lives in a shadow root, where
   document.getElementById returns null and fails SILENTLY. The nav would
   render perfectly and do nothing at all. Element lookups go through the
   root; the outside-click and Escape listeners stay on `document`,
   because those have to hear events from the whole page. */
function initPtNav(R){
  R = R || document;

    const nav = R.getElementById('pt-nav');
    if(!nav) return;
    const links  = R.getElementById('pt-links');
    const burger = R.getElementById('pt-burger');
    const items  = [...nav.querySelectorAll('.pt-has-drop')];
    const MOBILE = () => window.innerWidth < 1024;

    /* ── WHERE YOU ARE ──────────────────────────────────────────────
       Worked out from the URL, because this element lives ONCE in the
       site's header strip and therefore serves every page from a single
       instance. A per-page `active` prop cannot do that: one instance
       carries one value, so "For Women" would sit underlined on the
       men's page too.

       The prop still wins when it is set, for the Wix editor preview
       and for any page whose path does not match the table below. */
    const PATHS = [
      [/^\/(men|the-reconnected-man|the-archetype-quiz)\b/,      'men'],
      [/^\/(women|the-reconnected-woman|priority-audit)\b/,      'women'],
      [/^\/about-daniel-lawson\b/,                              'about'],
      [/^\/(daniel-lawson-speaking|facilitating)\b/,             'about'],
      [/^\/testimonials-daniel-lawson\b/,                       'testimonials'],
      [/^\/(contact-daniel-lawson|book-a-call)/,                 'contact']
    ];
    const fromPath = () => {
      const p = (location.pathname || '').replace(/\/+$/, '').toLowerCase() || '/';
      for(const [re, key] of PATHS) if(re.test(p)) return key;
      return '';
    };
    const active = fromPath();   /* baked in: no props, URL only */
    if(active){
      const el = nav.querySelector('[data-nav="'+active+'"]');
      if(el) el.classList.add('is-active');
    }

    /* ── NAVY ON SCROLL ─────────────────────────────────────────── */
    let ticking = false;
    const onScroll = () => {
      if(ticking) return; ticking = true;
      requestAnimationFrame(() => {
        nav.classList.toggle('is-stuck', window.scrollY > 40);
        ticking = false;
      });
    };
    window.addEventListener('scroll', onScroll, {passive:true});
    onScroll();

    /* ── MOBILE PANEL ───────────────────────────────────────────── */
    const closeMenu = () => {
      nav.classList.remove('menu-open');
      burger.setAttribute('aria-expanded','false');
      items.forEach(closeDrop);
    };
    burger.addEventListener('click', () => {
      const open = !nav.classList.contains('menu-open');
      nav.classList.toggle('menu-open', open);
      burger.setAttribute('aria-expanded', String(open));
      if(!open) items.forEach(closeDrop);
    });

    /* ── SUBMENUS ───────────────────────────────────────────────────
       Desktop hover and keyboard focus are pure CSS. The caret only
       exists for pointers that cannot hover, and for anyone who would
       rather tap the arrow than the word. */
    function closeDrop(it){
      it.classList.remove('is-open');
      const c = it.querySelector('.pt-caret');
      if(c) c.setAttribute('aria-expanded','false');
    }
    items.forEach(it => {
      const caret = it.querySelector('.pt-caret');
      caret.addEventListener('click', e => {
        e.preventDefault(); e.stopPropagation();
        const open = !it.classList.contains('is-open');
        items.forEach(closeDrop);
        it.classList.toggle('is-open', open);
        caret.setAttribute('aria-expanded', String(open));
      });
    });

    /* ── DISMISSAL ──────────────────────────────────────────────── */
    links.querySelectorAll('a').forEach(a =>
      a.addEventListener('click', () => { if(MOBILE()) closeMenu(); }));

    /* composedPath, not e.target. A click inside a shadow root is RETARGETED
       by the time it reaches the document: e.target becomes the host element,
       so nav.contains(e.target) is false for a click on the burger itself and
       this handler closed the panel in the same tick the burger opened it.
       The menu looked completely dead. composedPath() reports the real path
       through the shadow tree, and in the light DOM it still contains nav, so
       one line covers both runtimes. */
    document.addEventListener('click', e => {
      const path = e.composedPath ? e.composedPath() : [e.target];
      if(path.indexOf(nav) === -1){ items.forEach(closeDrop); if(MOBILE()) closeMenu(); }
    });

    document.addEventListener('keydown', e => {
      if(e.key !== 'Escape') return;
      items.forEach(closeDrop);
      if(nav.classList.contains('menu-open')){ closeMenu(); burger.focus(); }
    });

    /* Crossing the breakpoint with the phone panel open would leave a
       stranded overlay on the desktop layout. */
    let wasMobile = MOBILE();
    window.addEventListener('resize', () => {
      const now = MOBILE();
      if(now !== wasMobile){ wasMobile = now; closeMenu(); }
    });
}

/* ═══ PtFooter v3 LOGIC · BAKED IN VERBATIM · root-aware, as above ═══ */
function initPtFooter(R){
  R = R || document;

    /* The only script in the footer. A stale copyright year is a small
       thing that makes a site look abandoned, and it is the one piece
       of a footer nobody remembers to update. */
    const y = R.getElementById('pt-year');
    if(y) y.textContent = String(new Date().getFullYear());
}

  /* ══════════════ ANCESTOR COLLAPSE ══════════════
     Wix wraps a custom element in containers with fixed heights, so the page
     renders inside a short box with its own scrollbar. This walks up and
     releases them. One shot per element via a WeakSet, because re-running it
     on every resize is what once grew a document to Chrome's 2^24 clamp. */
  var PX_SANE_MAX = 200000;
  var pxCollapsed = new WeakSet();

  function collapseAncestors(host){
    try{
      if (document.documentElement.scrollHeight > PX_SANE_MAX) return;
      var h = host.getBoundingClientRect().height;
      if (h < 50) return;
      var n = host.parentElement, guard = 0;
      while(n && n !== document.body && guard++ < 14){
        if(!pxCollapsed.has(n) && n.getBoundingClientRect().height > h + 600){
          pxCollapsed.add(n);
          n.style.height = 'auto';
          n.style.minHeight = '0px';
        }
        n = n.parentElement;
      }
    }catch(e){}
  }

  function boot(root){
    var pxRoot = root.getElementById('px-root');
    if(!pxRoot) return;
    initTestimonials(pxRoot);
    try{ initPtNav(root); }catch(e){ if(window.console) console.warn('[px] ptnav', e); }
    try{ initPtFooter(root); }catch(e){ if(window.console) console.warn('[px] ptfooter', e); }
  }

  class ParallaxxTestimonials extends HTMLElement {
    connectedCallback(){
      if (this._mounted) return; this._mounted = true;
      addFonts();
      addSchema();
      var shadow = this.attachShadow({mode:'open'});
      shadow.innerHTML = '<style>'+CSS+'</style>'+HTML;
      var host = this;
      /* The host's own fixed height, not just its ancestors'. Left set, the
         surplus renders as empty page background under the content. */
      try{ host.style.height='auto'; host.style.minHeight='0px'; }catch(e){}
      try{ boot(shadow); }catch(e){ console.error('[px] boot failed:', e); }
      requestAnimationFrame(function(){ collapseAncestors(host); });
      [400,1200,2500].forEach(function(t){ setTimeout(function(){ collapseAncestors(host); }, t); });
      var pxRT;
      window.addEventListener('resize', function(){
        clearTimeout(pxRT);
        pxRT = setTimeout(function(){ collapseAncestors(host); }, 250);
      }, {passive:true});
    }
  }
  customElements.define('parallaxx-testimonials', ParallaxxTestimonials);
})();
